---
title: "DIAL: Decoupling Intent and Action via Latent World Modeling for End-to-End VLA"
title_zh: DIAL：通过潜空间世界建模实现端到端 VLA 的意图与动作解耦
authors: "Yi Chen, Yuying Ge, Hui Zhou, Mingyu Ding, Yixiao Ge, Xihui Liu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29844v1"
tags: ["query:robot"]
score: 10.0
evidence: VLA模型中用于高层决策的潜空间世界建模
tldr: 本研究针对现有视觉-语言-动作（VLA）模型直接映射低级动作导致的高层决策能力不足及训练不稳定问题，提出了DIAL框架。该框架通过潜空间世界建模将高层意图与低级执行解耦：System-2利用VLM生成潜空间视觉预测作为意图瓶颈，System-1则据此解码具体动作。通过两阶段训练策略，DIAL在减少10倍数据需求的同时，在RoboCasa基准测试中达到SOTA，并展现出强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有VLA模型将VLM仅视为多模态编码器直接映射动作，忽略了其高层决策潜力且易导致预训练语义在微调中退化。
method: 提出DIAL框架，通过VLM构建System-2进行潜空间视觉预测以提取意图，并结合轻量化System-1利用潜空间逆动力学解码具体动作。
result: 在RoboCasa基准测试中取得SOTA性能，仅需以往方法1/10的演示数据，并在真实人形机器人任务中表现出极强的零样本泛化能力。
conclusion: 通过潜空间意图解耦和两阶段优化策略，DIAL成功平衡了高层语义理解与低级动作控制，显著提升了VLA模型的效率与鲁棒性。
---

## 摘要
预训练视觉-语言模型 (VLM) 显著加速了视觉-语言-动作 (VLA) 模型的发展。然而，大多数现有的端到端 VLA 主要将 VLM 视为多模态编码器，直接将视觉-语言特征映射到低级动作。这种范式未能充分利用 VLM 在高级决策方面的潜力，并引入了训练不稳定性，经常导致其丰富的语义表示退化。为解决这些局限性，我们提出了 DIAL，这是一个通过可微潜空间意图瓶颈连接高级决策与低级电机执行的框架。具体而言，基于 VLM 的“系统-2”通过在 VLM 的原生特征空间内合成潜空间视觉预见来进行潜空间世界建模；这种预见显式地编码了意图，并作为结构化瓶颈。随后，轻量级的“系统-1”策略通过潜空间逆动力学，将预测的意图与当前观测共同解码为精确的机器人动作。为确保优化稳定性，我们采用了两阶段训练范式：首先是解耦预热阶段，在统一的特征空间内，系统-2 学习预测潜空间未来，而系统-1 在真实未来信息的引导下学习电机控制；随后进行无缝的端到端联合优化。这使得动作感知梯度能够以受控的方式微调 VLM 骨干网络，从而保留预训练知识。在 RoboCasa GR1 Tabletop 基准测试上的广泛实验表明，DIAL 树立了新的最先进水平，仅需比以往方法少 10 倍的演示数据即可实现卓越性能。此外，通过利用异构人类演示，DIAL 学习了具有物理基础的操作先验，并在人形机器人的真实世界部署中，对未见物体和新颖配置表现出鲁棒的零样本泛化能力。

## Abstract
The development of Vision-Language-Action (VLA) models has been significantly accelerated by pre-trained Vision-Language Models (VLMs). However, most existing end-to-end VLAs treat the VLM primarily as a multimodal encoder, directly mapping vision-language features to low-level actions. This paradigm underutilizes the VLM's potential in high-level decision making and introduces training instability, frequently degrading its rich semantic representations. To address these limitations, we introduce DIAL, a framework bridging high-level decision making and low-level motor execution through a differentiable latent intent bottleneck. Specifically, a VLM-based System-2 performs latent world modeling by synthesizing latent visual foresight within the VLM's native feature space; this foresight explicitly encodes intent and serves as the structural bottleneck. A lightweight System-1 policy then decodes this predicted intent together with the current observation into precise robot actions via latent inverse dynamics. To ensure optimization stability, we employ a two-stage training paradigm: a decoupled warmup phase where System-2 learns to predict latent futures while System-1 learns motor control under ground-truth future guidance within a unified feature space, followed by seamless end-to-end joint optimization. This enables action-aware gradients to refine the VLM backbone in a controlled manner, preserving pre-trained knowledge. Extensive experiments on the RoboCasa GR1 Tabletop benchmark show that DIAL establishes a new state-of-the-art, achieving superior performance with 10x fewer demonstrations than prior methods. Furthermore, by leveraging heterogeneous human demonstrations, DIAL learns physically grounded manipulation priors and exhibits robust zero-shot generalization to unseen objects and novel configurations during real-world deployment on a humanoid robot.

---

## 论文详细总结（自动生成）

### DIAL: 通过潜空间世界建模实现端到端 VLA 的意图与动作解耦 —— 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型通常将预训练的视觉-语言模型（VLM）仅视为多模态特征编码器，直接将特征映射到低级动作。这种做法存在三大缺陷：
    1.  **潜力低估**：未能充分利用 VLM 的高层决策和推理能力。
    2.  **训练不稳定**：低级动作监督信号容易导致 VLM 丰富的语义表示发生退化或“崩溃”。
    3.  **结构矛盾**：分层规划模型（生成文本/代码）存在不可微的断层且延迟高，而端到端模型又缺乏明确的意图约束。
*   **研究背景**：受人类认知中“系统-1（直觉/反射）”与“系统-2（理性/规划）”的启发，研究者试图构建一种既能保留 VLM 泛化性，又能实现精确动作控制的端到端架构。

#### 2. 方法论：DIAL 框架
*   **核心思想**：引入**可微潜空间意图瓶颈（Latent Intent Bottleneck）**，将高层意图合成与低级动作执行解耦。
*   **关键技术细节**：
    *   **System-2（大脑/意图合成）**：基于 Qwen2.5-VL 等预训练 VLM。它不直接输出动作，而是通过潜空间世界建模（LWM），在 VLM 原生的 ViT 特征空间内预测未来的“视觉预见（Visual Foresight）”。
    *   **System-1（小脑/动作解码）**：一个轻量级的流匹配（Flow-matching）策略网络。它将当前观测特征与 System-2 预测的潜空间意图进行对比，通过**潜空间逆动力学（Latent Inverse Dynamics）**推导出具体的机器人动作。
    *   **统一特征空间**：System-1 和 System-2 共享同一个冻结的预训练 ViT 编码器，确保意图与观测在同一流形下，避免跨模态对齐开销。
*   **优化策略（两阶段训练）**：
    1.  **解耦预热（Decoupled Warmup）**：System-2 学习预测未来状态，System-1 在真实未来特征引导下学习控制，防止联合训练初期的梯度干扰。
    2.  **端到端协同（End-to-End Synergy）**：将两者统一，动作梯度的流向可以穿过潜空间意图瓶颈回传至 VLM 骨干，使其生成的意图具备“动作感知”能力。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 RoboCasa GR1 Tabletop 基准，包含 24 类任务（18 类物体搬运，6 类涉及微波炉、橱柜等关节物体的交互）。
    *   **人类数据**：引入 EgoDex 数据集（约 2.7 万条人类演示），用于跨物种学习。
    *   **真实世界**：在 IRON-R01-1.11 人形机器人上进行部署，测试抓取、放置和倒水等任务。
*   **对比方法**：
    *   **基础策略**：Diffusion Policy, UWM, FLARE。
    *   **SOTA VLA 模型**：GR00T-N1.6, 以及基于 Qwen3-VL 的多种变体（$\pi$-Qwen3, FAST-Qwen3 等）。
*   **泛化性测试**：设置了未见外观、未见组合、未见物体类型以及真实环境下的干扰物鲁棒性测试。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **训练规模**：文中提到在全量数据模式下进行了 160,000 步训练，在少样本模式下进行了 40,000 步训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖 24 个任务，每个任务评估 50 个回合。
    *   消融实验非常详尽，涵盖了界面设计（如何传递意图）、特征空间选择（DINO-v2 vs 原生 ViT）、预热策略的必要性等。
    *   真实世界实验包含 3 类 OOD（分布外）泛化挑战。
*   **充分性评价**：实验设计较为充分且客观。通过对比不同规模的数据集（全量 vs 10% 样本），验证了模型极高的**数据效率**（比以往方法高 10 倍）。同时，通过跨物种（人到机器人）的数据实验，证明了框架的扩展性。

#### 6. 主要结论与发现
*   **性能领先**：DIAL 在 RoboCasa 基准上达到了 70.2% 的成功率，显著超过此前最强的 FLARE (55.0%)。
*   **数据效率**：仅需 10% 的机器人演示数据，DIAL 的表现即可超越使用 100% 数据的传统 VLA 模型。
*   **意图有效性**：可视化分析（PCA 降维）证实，System-2 预测的潜空间特征确实捕捉到了任务相关的未来状态变化，而非简单的图像重建。
*   **泛化能力**：通过吸收人类数据，模型在未见过的物体和复杂组合任务中表现出强大的零样本迁移能力。

#### 7. 优点与亮点
*   **结构优雅**：通过潜空间瓶颈解决了端到端模型中“黑盒映射”的问题，使高层规划与低级控制各司其职。
*   **可微性**：不同于输出文本的层级模型，DIAL 的意图瓶颈是连续且可微的，允许动作梯度优化 VLM。
*   **稳定性高**：两阶段训练范式有效解决了 VLM 在微调过程中的表示崩溃问题。
*   **部署友好**：System-1 极其轻量，且无需生成高分辨率像素，降低了推理延迟。

#### 8. 不足与局限
*   **视觉编码器限制**：目前为了稳定特征空间，ViT 部分是冻结的，这可能限制了模型学习特定机器人视觉特征的能力（作者提到未来将探索 EMA 稳定下的全参数微调）。
*   **任务多样性**：虽然在桌面操作上表现优异，但尚未在更广阔的移动导航或全身协调任务中进行验证。
*   **对预测质量的依赖**：如果 System-2 预测的未来状态严重偏离物理规律，System-1 的逆动力学解码可能会失效。

（完）
