---
title: "StreamingVLA: Streaming Vision-Language-Action Model with Action Flow Matching and Adaptive Early Observation"
title_zh: StreamingVLA：结合动作流匹配与自适应早期观测的流式视觉-语言-动作模型
authors: "Yiran Shi, Dongqi Guo, Tianchen Zhao, Feng Gao, Liangzhi Shi, Chao Yu, ZhiJian Mo, Qihua Xiao, XiaoShuai Peng, Qingmin Liao, Yu Wang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28565v1"
tags: ["query:robot"]
score: 10.0
evidence: 具有动作流匹配的流式VLA模型，用于实时控制
tldr: 针对视觉-语言-动作（VLA）模型在边缘设备部署时因顺序执行导致的延迟和停顿问题，本文提出了StreamingVLA。该模型通过引入动作流匹配和自适应早期观测机制，实现了观测、生成与执行阶段的异步并行处理。在保持性能的同时，该方法显著提升了推理速度和动作流畅度，将延迟降低了2.4倍，执行停顿减少了6.5倍，为实时机器人控制提供了高效方案。
selection_source: fresh_fetch
motivation: 传统的VLA模型因观测、生成和执行阶段必须顺序进行，导致系统频繁停顿且延迟较高，难以满足实时部署需求。
method: 采用动作流匹配替代动作分块以重叠生成与执行阶段，并设计动作显著性感知的自适应观测机制来重叠执行与观测阶段。
result: 在不损失性能的前提下，StreamingVLA实现了2.4倍的延迟加速，并将执行停顿频率降低了6.5倍。
conclusion: 通过将VLA流程转变为流式并行处理模式，StreamingVLA有效解决了实时机器人控制中的效率与流畅性瓶颈。
---

## 摘要
视觉-语言-动作（VLA）模型在自然语言驱动的感知与控制方面展现出了卓越的性能。然而，VLA 模型的高计算成本带来了显著的效率挑战，特别是在实际部署中资源受限的边缘平台。由于 VLA 的不同阶段（观测、动作生成和执行）必须顺序进行，且需等待前一阶段完成，系统面临频繁的停顿和高延迟。为解决这一问题，我们进行了系统分析以识别实现快速且流畅生成的挑战，并提出以“流式”方式使 VLA 具备跨阶段异步并行的能力。首先，我们消除了对动作分块（action chunking）的依赖，并采用了动作流匹配（action flow matching），该方法学习动作流的轨迹而非对分块动作进行去噪，从而实现了动作生成与执行延迟的重叠。其次，我们设计了一种动作显著性感知的自适应观测机制，从而实现了执行与观测延迟的重叠。在不牺牲性能的前提下，StreamingVLA 实现了显著的加速并提高了执行的流畅度。该模型实现了 2.4 倍的延迟加速，并将执行停顿减少了 6.5 倍。

## Abstract
Vision-language-action (VLA) models have demonstrated exceptional performance in natural language-driven perception and control. However, the high computational cost of VLA models poses significant efficiency challenges, particularly for resource-constrained edge platforms in real-world deployments. However, since different stages of VLA (observation, action generation and execution) must proceed sequentially, and wait for the completion of the preceding stage, the system suffers from frequent halting and high latency. To address this, We conduct a systematic analysis to identify the challenges for fast and fluent generation, and propose enabling VLAs with the ability to asynchronously parallelize across VLA stages in a "streaming" manner. First, we eliminate the reliance on action chunking and adopt action flow matching, which learns the trajectory of action flows rather than denoising chunk-wise actions. It overlaps the latency of action generation and execution. Second, we design an action saliency-aware adaptive observation mechanism, thereby overlapping the latency of execution and observation. Without sacrificing performance, StreamingVLA achieves substantial speedup and improves the fluency of execution. It achieves a 2.4 $\times$ latency speedup and reduces execution halting by 6.5 $\times$.

---

## 论文详细总结（自动生成）

### StreamingVLA 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在机器人控制中表现出色，但在实际部署（尤其是边缘设备）时面临严重的**效率瓶颈**。传统的 VLA 推理遵循“观测（Observation）→ 动作生成（Generation）→ 动作执行（Execution）”的顺序流程。即使采用了动作分块（Action Chunking）技术，各阶段之间仍存在显著的等待时间，导致机器人动作出现**高延迟**和**频繁停顿（Stuttering/Halting）**。
本文的研究动机在于打破这种顺序执行的限制，通过**流式（Streaming）异步并行处理**，让观测、生成和执行阶段在时间上重叠，从而实现快速且流畅的机器人控制。

#### 2. 方法论：核心思想与关键技术
StreamingVLA 提出了两个核心技术来重叠不同阶段的延迟：

*   **动作流匹配（Action Flow Matching, AFM）：**
    *   **核心思想：** 放弃传统的动作分块去噪（Diffusion），转而学习动作轨迹的“流”。它将动作建模为对物理状态或特征空间状态的连续更新。
    *   **技术细节：** 引入**基于状态的建模**，通过预测速度场 $v_\theta$ 来更新状态。为了适配复杂的控制器（如 LIBERO 中的 OSC 姿态控制），模型维护一个与物理状态对齐的“动作空间状态”。
    *   **架构调整：** 修改了归一化层（Normalization），移除偏移项以保持动作与状态之间的**加性（Additivity）**，确保训练稳定。这使得动作可以“逐个生成、逐个执行”，实现了生成与执行阶段的重叠。
*   **动作显著性感知的自适应早期观测（Adaptive Early Observation, AEO）：**
    *   **核心思想：** 在当前动作序列执行完之前，提前启动下一轮 VLM 观测。
    *   **关键技术：** 并非所有动作都适合提前观测。论文设计了一个轻量级的**显著性预测器**（基于 Transformer），评估剩余动作对环境改变的程度（动作显著性）。如果显著性低（环境变化小），则触发早期观测；反之则等待，以避免因观测不一致导致的性能下降。

#### 3. 实验设计
*   **数据集/场景：** 使用了 **LIBERO** 仿真基准测试，包含四个子任务集：LIBERO-Spatial（空间关系）、LIBERO-Object（物体操作）、LIBERO-Goal（目标达成）和 LIBERO-Long（长程任务）。
*   **Benchmark：** 以 SOTA 模型 **$\pi_{0.5}$** 为基础模型。
*   **对比方法：** 
    *   原始 $\pi_{0.5}$（不同分块长度 $h=5, 10$）。
    *   **RTC**（实时分块流策略）。
    *   **SmolVLA**（轻量级 VLA 早期观测）。
    *   **VLASH**（基于未来状态感知的异步推理）。
    *   **Temporal Ensembling**（时间集成法）。
*   **真实世界验证：** 在 Franka Panda 机械臂上进行了“取放物体”任务的实机部署。

#### 4. 资源与算力
*   **硬件：** 训练使用了 **4 台 NVIDIA A800-SXM4-80GB GPU**。
*   **训练时长：** 
    *   主模型训练：60,000 次迭代，耗时约 **10-20 GPU 小时**。
    *   自适应预测器训练：54,000 次迭代，耗时约 **3-4 GPU 小时**。
*   **推理：** 在单张 A800 GPU 上进行评估。

#### 5. 实验数量与充分性
*   **实验规模：** 每个任务套件包含 10 个任务，每个任务进行 50 次 Rollout（总计约 2000 次仿真实验），样本量充足。
*   **消融实验：** 针对动作流匹配（AFM）、归一化修改（NM）、状态对齐（SA）以及不同的早期观测策略（随机 vs 自适应）进行了详细的消融研究。
*   **客观性：** 实验涵盖了从仿真到真机的迁移，对比了多种最新的异步执行基准，评价指标包括成功率、单步动作耗时（$T_{action}$）和停顿间隙（$T_{halt}$），评估维度全面且客观。

#### 6. 主要结论与发现
*   **性能保持：** 在几乎不损失成功率（下降小于 0.5%）的前提下，显著提升了效率。
*   **速度提升：** 实现了 **2.4 倍** 的端到端延迟加速。
*   **流畅度改善：** 将执行过程中的停顿时间减少了 **6.5 倍**，使机器人动作更加连贯。
*   **自适应性：** 证明了“动作显著性”在早期观测中的重要性，自适应策略比固定步数的早期观测更能平衡速度与精度。

#### 7. 优点
*   **系统性分析：** 论文对 VLA 的运行时瓶颈进行了深入的定量分析，找出了优化目标。
*   **流式架构：** 提出的 AFM 机制巧妙地解决了动作分块必须等待整体去噪完成的弊端。
*   **轻量化插件：** 自适应观测预测器开销极小（仅占推理时间的 5%），但对性能保护作用显著。
*   **实用性强：** 能够直接应用于现有的基于扩散或流匹配的 VLA 模型，具有较好的通用性。

#### 8. 不足与局限
*   **训练依赖：** 虽然比全量微调轻量，但 AFM 和预测器仍需要针对特定任务或数据集进行一定程度的训练/微调。
*   **阈值敏感性：** 自适应观测中的显著性阈值可能需要根据不同场景手动调优，以达到最佳的加速比。
*   **硬件假设：** 实验主要在高性能 A800 上完成，在算力更弱的嵌入式端（如 Jetson Orin）上的表现虽有预期提升，但未给出详细的端侧功耗与性能曲线。

（完）
