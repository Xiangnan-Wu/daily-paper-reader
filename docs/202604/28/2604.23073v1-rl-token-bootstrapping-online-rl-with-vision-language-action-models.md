---
title: "RL Token: Bootstrapping Online RL with Vision-Language-Action Models"
authors: "Charles Xu, Jost Tobias Springenberg, Michael Equi, Ali Amin, Adnan Esmail, Sergey Levine, Liyiming Ke"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.23073v1"
tags: ["query:robot"]
score: 10.0
evidence: 预训练VLA模型的在线强化学习微调
tldr: 视觉-语言-动作（VLA）模型在处理复杂现实任务时往往缺乏精度和速度。本文提出 RL Token (RLT) 方法，通过在预训练 VLA 中引入紧凑的“RL token”作为特征接口，并训练轻量级 Actor-Critic 头部，实现了高效的在线强化学习微调。该方法仅需数小时实机练习，即可显著提升模型在螺丝安装、扎带紧固等高难度任务中的成功率和执行速度，甚至在某些任务上超越了人类遥操作的表现。
selection_source: fresh_fetch
motivation: 预训练的 VLA 模型虽然具备通用能力，但在执行高精度、高速度要求的现实机器人任务时表现不足，且传统的强化学习微调效率较低。
method: 通过在 VLA 中提取紧凑的 RL token 作为任务相关的预训练知识表征，并在此基础上训练小型 Actor-Critic 网络来优化动作输出。
result: 在四项真实机器人挑战任务中，RLT 将任务关键环节的速度提升了高达 3 倍，并在数小时内显著提高了成功率。
conclusion: RLT 证明了通过轻量级接口对大型 VLA 进行在线强化学习微调的可行性，为实现高效、精准的机器人操作提供了新途径。
---

## Abstract
Vision-language-action (VLA) models can learn to perform diverse manipulation skills "out of the box," but achieving the precision and speed that real-world tasks demand requires further fine-tuning -- for example, via reinforcement learning (RL). We introduce a lightweight method that enables sample-efficient online RL fine-tuning of pretrained VLAs using just a few hours of real-world practice. We (1) adapt the VLA to expose an "RL token," a compact readout representation that preserves task-relevant pretrained knowledge while serving as an efficient interface for online RL, and (2) train a small actor-critic head on this RL token to refine the actions, while anchoring the learned policy to the VLA. Online RL with the RL token (RLT) makes it possible to fine-tune even large VLAs with RL quickly and efficiently. Across four real-robot tasks (screw installation, zip tie fastening, charger insertion, and Ethernet insertion), RLT improves the speed on the hardest part of the task by up to 3x and raises success rates significantly within minutes to a few hours of practice. It can even surpass the speed of human teleoperation on some of the tasks.

---

## 论文详细总结（自动生成）

这篇论文由 Physical Intelligence 团队发表，提出了一种名为 **RL Token (RLT)** 的方法，旨在解决视觉-语言-动作（VLA）模型在处理高精度机器人任务时速度慢、精度不足的问题。

### 1. 核心问题与整体含义
*   **研究动机**：通用的 VLA 模型（如 $\pi_0$）虽然具备强大的泛化能力，但在执行需要毫米级精度的任务（如插拔、拧螺丝）时，往往表现得犹豫、缓慢且容易失败。
*   **核心挑战**：直接对拥有数十亿参数的 VLA 进行在线强化学习（RL）微调极其耗时且采样效率低下；而从头训练小型 RL 模型又会丢失 VLA 预训练的通用感知知识。
*   **整体含义**：论文提出了一种轻量级接口方案，通过从冻结的 VLA 中提取紧凑的表征（RL Token），在保留预训练知识的同时，利用高效的在线 RL 在数小时内完成特定任务的精度优化。

### 2. 方法论
*   **核心思想**：将 VLA 视为特征提取器和行为先验，通过一个轻量级的“RL 头部”进行局部动作微调。
*   **关键技术细节**：
    1.  **RL Token 提取**：在冻结的 VLA 内部引入一个可学习的 Transformer 编码器-解码器。编码器将 VLA 的高维内部特征压缩成一个紧凑的向量（RL Token），解码器通过重构原始特征来确保该 Token 保留了任务相关信息。
    2.  **轻量级 Actor-Critic**：在 RL Token 之上构建小型 MLP 网络。Actor 不仅接收状态，还接收 VLA 预测的“参考动作块”（Reference Action Chunk）。
    3.  **动作微调与正则化**：Actor 学习对 VLA 的动作进行修正，而非从头探索。通过 BC 正则化项（将 Actor 动作锚定在 VLA 动作附近）和参考动作丢弃（Dropout）技术，确保训练的稳定性。
    4.  **算法流程**：采用异步离策（Off-policy）Actor-Critic 框架（基于 TD3），配合高更新采样比（UTD=5），在机器人实机练习过程中不断更新。

### 3. 实验设计
*   **实验场景**：四项极具挑战性的真实机器人任务：
    *   **螺丝安装**（M3 螺丝，亚毫米级对齐）。
    *   **扎带紧固**（双臂协作，柔性物体）。
    *   **以太网线插入**（狭窄插槽）。
    *   **充电器插入**（接触密集型任务）。
*   **Benchmark 与对比方法**：
    *   **基准**：基础 VLA 模型（$\pi_{0.6}$）。
    *   **对比方法**：HIL-SERL（从头开始的 RL）、PLD（残差 RL）、DSRL（潜空间引导 RL）、DAgger（基于干预的模仿学习）。
    *   **评估指标**：成功率、吞吐量（每 10 分钟成功次数）。

### 4. 资源与算力
*   **训练时长**：每个任务的实机交互时间在 **15 分钟到 5 小时** 之间。
*   **算力细节**：文中未详细列出具体的 GPU 型号和数量，但强调了 RL 头部是极小的 MLP（256-512 隐藏层维度），且 VLA 主干在 RL 阶段是冻结的，这表明该方法对实时算力的要求远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个真实任务进行了深入评估，每个任务在测试阶段运行 50 次。
*   **消融实验**：非常充分。分别验证了 RL Token、动作块（Chunking）、BC 正则化、参考动作输入（Pass-Through）的必要性。
*   **客观性**：实验涵盖了从“关键环节”到“全任务流程”的评估，对比了当前主流的多种机器人 RL 方案，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：RLT 在所有任务上均显著提升了成功率（如螺丝任务从 20% 提升至 65%）。
*   **速度提升**：在关键环节的处理速度提升了 **3 倍**，且在以太网插入任务中，机器人的速度甚至**超越了人类遥操作**。
*   **涌现行为**：RL 使得机器人学会了演示数据中没有的策略，例如通过微小的“晃动”来辅助插入。
*   **表征优势**：实验证明，从 VLA 提取的 RL Token 远优于标准的视觉编码器（如 ResNet）。

### 7. 优点
*   **采样效率极高**：能在数小时内完成复杂任务的实机微调，具有极强的实用价值。
*   **架构优雅**：通过 RL Token 巧妙地解决了大模型“难微调”与小模型“无先验”的矛盾。
*   **鲁棒性强**：通过动作块和正则化技术，有效缓解了稀疏奖励下的信用分配问题。

### 8. 不足与局限
*   **人工依赖**：训练过程仍需要人类提供成功/失败的标签，以及在必要时进行干预。
*   **阶段划分**：目前需要人工指定任务的“关键环节”进行针对性训练，尚未实现全自动的任务切分。
*   **任务特定**：虽然利用了通用的 VLA 特征，但 RL 微调后的策略仍然是针对特定任务的，缺乏跨任务的直接迁移能力。

（完）
