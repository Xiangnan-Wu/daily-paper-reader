---
title: "CF-VLA: Efficient Coarse-to-Fine Action Generation for Vision-Language-Action Policies"
authors: "Fan Du, Feng Yan, Jianxiong Wu, Xinrun Xu, Weiye Zhang, Weinong Wang, Yu Guo, Bin Qian, Zhihai He"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24622v1"
tags: ["query:robot"]
score: 10.0
evidence: VLA策略的高效动作生成
tldr: 针对基于流的视觉-语言-动作（VLA）策略在实时推理中效率与质量难以兼得的问题，本文提出了CF-VLA框架。该方法将动作生成重构为“粗到细”的两阶段过程：首先通过粗略阶段将高斯噪声转化为结构化的动作感知起点，随后通过精细阶段进行单步局部修正。实验表明，CF-VLA在低推理步数下实现了卓越的性能，显著降低了采样延迟，并在真实机器人任务中取得了领先的成功率。
selection_source: fresh_fetch
motivation: 传统的基于流的VLA策略从无信息的高斯噪声开始推理，导致多步采样效率低下，难以满足实时性要求。
method: 提出一种两阶段生成架构，先通过粗略预测器构建结构化初始动作，再利用精细阶段进行单步误差修正。
result: "在CALVIN和LIBERO基准测试中，该方法在极低推理步数下将采样延迟降低了75.4%，且真实机器人成功率达到83.0%。"
conclusion: 结构化的“由粗到细”生成策略能有效平衡VLA策略的推理效率与动作质量，是实现高效具身智能的有效途径。
---

## Abstract
Flow-based vision-language-action (VLA) policies offer strong expressivity for action generation, but suffer from a fundamental inefficiency: multi-step inference is required to recover action structure from uninformative Gaussian noise, leading to a poor efficiency-quality trade-off under real-time constraints. We address this issue by rethinking the role of the starting point in generative action modeling. Instead of shortening the sampling trajectory, we propose CF-VLA, a coarse-to-fine two-stage formulation that restructures action generation into a coarse initialization step that constructs an action-aware starting point, followed by a single-step local refinement that corrects residual errors. Concretely, the coarse stage learns a conditional posterior over endpoint velocity to transform Gaussian noise into a structured initialization, while the fine stage performs a fixed-time refinement from this initialization. To stabilize training, we introduce a stepwise strategy that first learns a controlled coarse predictor and then performs joint optimization. Experiments on CALVIN and LIBERO show that our method establishes a strong efficiency-performance frontier under low-NFE (Number of Function Evaluations) regimes: it consistently outperforms existing NFE=2 methods, matches or surpasses the NFE=10 $π_{0.5}$ baseline on several metrics, reduces action sampling latency by 75.4\%, and achieves the best average real-robot success rate of 83.0\%, outperforming MIP by 19.5 points and $π_{0.5}$ by 4.0 points. These results suggest that structured, coarse-to-fine generation enables both strong performance and efficient inference. Our code is available at https://github.com/EmbodiedAI-RoboTron/CF-VLA.

---

## 论文详细总结（自动生成）

这篇论文提出了 **CF-VLA**，一种针对视觉-语言-动作（VLA）策略的高效“由粗到精”动作生成框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：基于流匹配（Flow-matching）的 VLA 策略（如 $\pi_{0.5}$）虽然表达能力强，但在推理时面临严重的效率瓶颈。它们通常从无信息的**高斯噪声**开始，需要多次迭代（高 NFE）才能恢复出有意义的动作结构，这导致在实时机器人控制中难以平衡“生成质量”与“推理速度”。
*   **研究动机**：作者认为效率低下的根源在于起点。标准方法在推理初期浪费了大量计算资源进行“全局传输”（从噪声到动作流形）。如果能直接从一个接近动作流形的“动作先验引导”起点开始，仅需极少步数的局部修正即可完成生成。

### 2. 方法论：核心思想与关键技术
CF-VLA 将动作生成重构为两个显式阶段：
*   **粗略初始化阶段（Coarse Stage）**：
    *   **目标**：将纯高斯噪声转换为“动作先验引导（AP-guided）”的初始化分布。
    *   **技术**：学习一个关于终点速度的条件后验分布。通过 **KL 散度损失**进行监督，使模型学习到一个随机的、任务相关的初始化分布，而非简单的点回归。
*   **精细细化阶段（Fine Stage）**：
    *   **目标**：从粗略起点出发，进行单步局部修正以恢复最终动作。
    *   **技术**：在初始化点附近的局部平滑区域内，使用单步一阶更新（MSE 监督）来捕捉精细细节。
*   **分步训练策略（Stepwise Training）**：
    *   **阶段 I（稳定化）**：先训练终点表示，并使用受控的代理分布（Proxy）训练细化分支，避免初期粗略输出不稳定导致训练崩溃。
    *   **阶段 II（联合优化）**：在粗略输出结构化后，进行全流程联合训练，使两个阶段相互适配。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：评估多任务泛化能力（包含 Spatial, Object, Goal, Long 四个子集）。
    *   **CALVIN**：评估长程指令遵循能力（D→D 设置，连续完成 1-5 个任务）。
    *   **真实机器人**：在 Franka 机器人上执行 5 类任务（抓取、放置、擦桌子、倒水、双臂折叠毛巾）。
*   **对比方法（Baselines）**：
    *   **高 NFE 方法**：$\pi_{0.5}$ (NFE=10), InstructVLA, Flower VLA 等。
    *   **低 NFE 方法**：MIP (NFE=2), PI-VLA (NFE=1), UniAct 等。
    *   **自回归方法**：OpenVLA, UniVLA 等。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：采用 PyTorch FSDP（完全分片数据并行），全局 Batch Size 为 16，学习率为 $5 \times 10^{-5}$。
*   **时长**：在 LIBERO 上训练了 60,000 步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了大规模仿真（LIBERO 和 CALVIN 上的数千次测试）和 5 项具有挑战性的真实机器人任务（每项任务 20-40 次测试）。
*   **消融实验**：非常充分。作者分别验证了阶段 I 稳定化、阶段 II 联合优化、方差建模以及细化步骤的必要性。
*   **客观性**：通过在相同的回放过滤数据上重新训练 $\pi_{0.5}$ 和 MIP 等基准模型，确保了对比的公平性。此外，还验证了该方法在不同模型骨干（如 PaliGemma 2）上的可迁移性。

### 6. 主要结论与发现
*   **性能领先**：在 NFE=2 的严苛限制下，CF-VLA 在 LIBERO 上的平均成功率（96.5%）超过了 NFE=10 的 $\pi_{0.5}$ 基准（95.7%）。
*   **效率提升**：动作采样延迟降低了 **75.4%**（从 29.17ms 降至 7.81ms）。
*   **长程任务优势**：在 CALVIN 5 步连续任务中，CF-VLA 显著优于其他低 NFE 模型，平均序列长度从 3.43 提升至 3.67。
*   **真实机器人表现**：在接触密集型（擦桌子）和双臂协作（折毛巾）任务中表现优异，平均成功率 83.0%，比 MIP 高出 19.5 个百分点。

### 7. 优点与亮点
*   **即插即用**：该框架不依赖特定的 VLM 骨干，可无缝集成到任何基于流的 VLA 策略中。
*   **重构生成逻辑**：不同于以往单纯缩短采样路径的研究，CF-VLA 通过“重塑起点”解决了生成效率问题，具有很强的启发性。
*   **鲁棒性**：通过引入方差感知的粗略阶段，模型能更好地处理动作的多峰分布和不确定性。

### 8. 不足与局限
*   **性能上限**：虽然在低 NFE 下表现卓越，但与集成世界模型（World Model）或显式规划模块的高复杂度模型（如 Cosmos Policy）相比，在某些极端复杂的任务上仍有差距。
*   **感知依赖**：真实机器人实验显示，在视觉遮挡（如倒水时相机被瓶子遮挡）或极高精度的双臂时序同步方面，模型仍存在失败案例。
*   **超参数敏感性**：粗略阶段的方差（noise_var）和损失权重（$\lambda$）需要针对不同场景进行一定程度的调优。

（完）
