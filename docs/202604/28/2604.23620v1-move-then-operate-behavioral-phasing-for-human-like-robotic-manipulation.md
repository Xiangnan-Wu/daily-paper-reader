---
title: "Move-Then-Operate: Behavioral Phasing for Human-Like Robotic Manipulation"
authors: "Haoming Xu, Lei Lei, Jie Gu, Chu Tang, Jingmin Chen, Ruiqi Wang"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23620v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人操作的视觉-语言-动作框架
tldr: 提出一种 VLA 框架，将机器人操作解耦为移动和操作阶段以提高成功率。
selection_source: fresh_fetch
motivation: 用于机器人操作的视觉-语言-动作框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
We present Move-Then-Operate, a Vision language action framework that explicitly decouples robotic manipulation into two distinct behavioral phases: coarse relocation (move) and contact-critical interaction (operate). Unlike monolithic policies that conflate these heterogeneous regimes, our architecture employs a dual-expert policy routed by a learnable phase selector, introducing a structural inductive bias that isolates phase-specific dynamics. Phase labels are automatically generated via an MLLM-based pipeline conditioned on lightweight contextual cues such as end-effector velocity and subtask decomposition to ensure alignment with human motor patterns. Evaluated on the RoboTwin2 benchmark, our method achieves an average success rate of $68.9\%$, outperforming the monolithic $π_0$ baseline by $24\%$. It matches or exceeds models trained on $10\times$ more data and reaches peak performance in $40\%$ fewer training steps, demonstrating that architectural disentanglement of move and operate phases is a highly effective and efficient strategy for mastering high-precision manipulation.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Move-Then-Operate**，一个受人类运动模式启发的视觉-语言-动作（VLA）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 VLA 模型大多采用“单体式（Monolithic）”策略，即用同一个网络同时处理粗略的重定位（Move）和精细的接触交互（Operate）。
*   **研究动机**：
    *   **梯度干扰**：在机器人演示数据中，移动阶段的样本量远超操作阶段，且两者的动作分布截然不同。单体模型在优化时，大幅度的移动信号会掩盖微小的操作信号，导致高精度任务表现不佳。
    *   **人类启发**：人类操作具有明显的两阶段特征（Fitts' Law）：先快速接近目标，再进行基于反馈的精细调整。
    *   **目标**：通过结构化解耦，引入归纳偏置，使模型能够分别学习粗略移动和精细操作的动力学。

### 2. 方法论
*   **核心思想**：将机器人操作显式解耦为“移动”和“操作”两个阶段，并采用**双专家策略（Dual-expert policy）**，由一个可学习的阶段选择器（Router）进行路由。
*   **关键技术细节**：
    *   **双专家架构**：共享一个视觉-语言（VLM）主干网络，后端连接两个独立的专家头（$E_{move}$ 和 $E_{operate}$）。专家模型基于**条件流匹配（Conditional Flow Matching, CFM）**构建。
    *   **分块路由（Chunk-level Routing）**：不同于 Token 级别的 MoE，该模型在动作块（Action Chunk）级别进行路由，确保动作序列的时间连续性。
    *   **MLLM 自动标注管线**：利用多模态大模型（如 Seed 1.6 Vision）对原始演示视频进行自动分割。通过提示词（Prompt）约束，结合末端执行器速度和子任务逻辑，生成高质量的阶段标签（Move/Operate）。
    *   **训练策略**：采用教师强制（Teacher-forcing）训练专家，同时训练路由器的交叉熵损失。为了参数效率，使用了 LoRA 适配器。

### 3. 实验设计
*   **数据集与基准**：在 **RoboTwin2** 仿真基准上进行评估。
*   **实验场景**：涵盖 8 个具有挑战性的任务，包括点击闹钟、按铃、移动药瓶、放置面包篮、放置罐头、放置空杯、放置汉堡薯条、按压订书机。
*   **对比方法**：
    *   经典基准：ACT, RDT。
    *   单体式 SOTA：$\pi_0$（基础模型）。
    *   大数据量模型：$\pi_{0.5}^*$ 和 GO-1*（这些模型使用了比本文多 10 倍的数据）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率提及**：作者强调了该方法在训练效率上的优势，指出其达到峰值性能所需的训练步数比标准流程减少了 **40%**，且通过 LoRA 保持了参数的高效利用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个任务上进行了详尽测试，每个任务进行 100 次试验以计算成功率。
    *   进行了多任务预训练（50 个任务）和特定任务微调的对比。
    *   **消融实验**：对比了“随机路由”和“反向路由（故意选错专家）”，验证了专家专业化和路由准确性的必要性。
*   **充分性评价**：实验设计较为全面，不仅对比了同量级模型，还跨量级对比了大数据量模型，证明了架构设计的优越性。实验过程遵循了标准的机器人学习评估协议，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：在相同数据量下，平均成功率达到 **68.9%**，比单体式 $\pi_0$ 基准高出 **24.1%**。
*   **数据效率**：仅用 1/10 的数据量，就能在接触密集型任务（如按铃、按压订书机）中匹配甚至超越拥有 10 倍数据的模型。
*   **训练效率**：解耦架构降低了优化难度，模型收敛速度显著加快。
*   **专家专业化**：消融实验证明，Move 专家和 Operate 专家确实学习到了互不兼容的动作分布，错误的路由会导致任务彻底失败。

### 7. 优点
*   **结构化偏置**：通过简单的物理逻辑解耦，解决了 VLA 模型中长程移动与精细交互的冲突问题。
*   **自动标注**：利用 MLLM 解决大规模数据标注难题，降低了人工成本。
*   **高效率**：在数据和算力受限的情况下，提供了实现高精度操作的可行路径。

### 8. 不足与局限
*   **视觉泛化风险**：在视觉多样性极高的任务（如放置不同形状的罐头）中，虽然动作控制精准，但受限于训练数据的视觉覆盖范围，表现略逊于超大规模预训练模型。
*   **路由依赖**：系统的成功高度依赖于路由器的准确性，一旦阶段判断错误，后续动作将完全失效。
*   **场景局限**：目前主要在 RoboTwin2 仿真环境中验证，尚未展示在复杂现实世界非结构化环境中的鲁棒性。

（完）
