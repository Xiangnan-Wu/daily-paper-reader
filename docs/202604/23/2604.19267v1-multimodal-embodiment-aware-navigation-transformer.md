---
title: Multimodal embodiment-aware navigation transformer
title_zh: 多模态具身感知导航 Transformer
authors: "Louis Dezons, Quentin Picard, Rémi Marsal, François Goulette, David Filliat"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19267v1"
tags: ["query:robot"]
score: 9.0
evidence: 使用扩散模型生成轨迹的多模态机器人导航策略
tldr: "ViLiNT是一种多模态具身感知导航模型，旨在解决地面机器人在环境或配置变化时的避障鲁棒性问题。它通过Transformer融合RGB图像、3D LiDAR、目标嵌入和机器人具身描述符，并利用扩散模型生成轨迹。结合自动生成的路径净空预测头，ViLiNT能根据机器人尺寸筛选最优路径。实验显示其在仿真中成功率比SOTA视觉模型提升166%，并在实地部署中表现出极强的越野导航鲁棒性。"
selection_source: fresh_fetch
motivation: 现有的监督学习导航模型在面对环境、机器人或传感器配置变化时，避障能力会显著下降。
method: 提出ViLiNT模型，利用Transformer融合多模态数据并结合扩散模型生成轨迹，同时引入具身令牌和路径净空预测头进行轨迹评分。
result: "在三种仿真环境中，ViLiNT的导航成功率平均比SOTA视觉基准模型NoMaD提升了166%，并在真实世界越野场景中得到验证。"
conclusion: 结合多模态融合与具身感知的碰撞预测机制，能显著提升地面机器人在复杂环境下的导航鲁棒性。
---

## 摘要
使用监督学习训练的地面机器人目标条件导航模型展现出良好的零样本迁移能力，但在分布偏移（即环境、机器人或传感器配置变化）下，其避障能力仍会下降。我们提出了 ViLiNT，这是一种基于注意力的多模态目标导航策略，在来自多个平台和环境的异构数据上进行训练，通过两个关键特性提高了鲁棒性。首先，我们利用 Transformer 架构融合了 RGB 图像、3D 激光雷达（LiDAR）点云、目标嵌入和机器人具身描述符，以捕捉互补的几何与外观线索。Transformer 的输出用于调节一个生成可行驶轨迹的扩散模型。其次，利用自动生成的离线标签，我们训练了一个路径净空预测头，用于对扩散模型生成的轨迹进行评分和排序。扩散调节以及轨迹排序头都依赖于机器人的具身标记（embodiment token），这使得我们的模型能够根据机器人的尺寸生成并选择轨迹。在三个模拟环境中，ViLiNT 的平均成功率比同类最先进的纯视觉基准模型（NoMaD）提高了 166%。这种性能提升通过在障碍区导航的漫游车实地部署得到了证实。这些结果表明，将多模态融合与我们的碰撞预测机制相结合，可以提高越野导航的鲁棒性。

## Abstract
Goal-conditioned navigation models for ground robots trained using supervised learning show promising zero-shot transfer, but their collision-avoidance capability nevertheless degrades under distribution shift, i.e. environmental, robot or sensor configuration changes. We propose ViLiNT a multimodal, attention-based policy for goal navigation, trained on heterogeneous data from multiple platforms and environments, which improves robustness with two key features. First, we fuse RGB images, 3D LiDAR point clouds, a goal embedding and a robot's embodiment descriptor with a transformer architecture to capture complementary geometry and appearance cues. The transformer's output is used to condition a diffusion model that generates navigable trajectories. Second, using automatically generated offline labels, we train a path clearance prediction head for scoring and ranking trajectories produced by the diffusion model. The diffusion conditioning as well as the trajectory ranking head depend on a robot's embodiment token that allows our model to generate and select trajectories with respect to the robot's dimensions. Across three simulated environments, ViLiNT improves Success Rate on average by 166\% over equivalent state-of-the-art vision-only baseline (NoMaD). This increase in performance is confirmed through real-world deployments of a rover navigating in obstacle fields. These results highlight that combining multimodal fusion with our collision prediction mechanism leads to improved off-road navigation robustness.

---

## 论文详细总结（自动生成）

### 论文总结：Multimodal Embodiment-aware Navigation Transformer (ViLiNT)

#### 1. 核心问题与整体含义（研究动机和背景）
在复杂越野环境下，地面机器人的自主导航面临环境多变（如雨、雾、草地）、传感器退化及机器人平台差异等挑战。现有的端到端导航模型（如纯视觉模型）虽然在零样本迁移上表现良好，但在面对**分布偏移**（环境改变、机器人尺寸变化或传感器配置变动）时，其避障鲁棒性会显著下降。论文的核心目标是开发一种能够同时利用多种传感器模态（RGB+LiDAR），并显式感知机器人自身尺寸（具身感知）的导航策略，以提高在未知环境下的安全性和适应性。

#### 2. 方法论：核心思想与技术细节
ViLiNT 采用了一种基于 Transformer 的多模态融合架构，结合扩散模型（Diffusion Model）进行轨迹生成。
*   **多模态 Token 化**：
    *   **图像**：使用预训练的 DUNE 编码器提取特征。
    *   **LiDAR**：使用 PointTransformerV3 处理点云，并将其映射到极坐标网格（扇区+环），通过广义平均池化（GeM Pooling）生成固定数量的 Token。
    *   **具身与目标**：将机器人的长宽尺寸（Embodiment）和 2D 目标坐标编码为 Token。
*   **Transformer 融合层**：将上述所有 Token 输入 Transformer 编码器，通过自注意力机制捕捉几何（LiDAR）与外观（图像）的互补线索。
*   **轨迹生成（扩散模型）**：利用 Transformer 输出的场景描述符作为条件，驱动扩散模型生成多条候选路径（路点序列）。
*   **路径净空预测头（Clearance Prediction Head）**：
    *   这是一个关键创新。该模块通过交叉注意力机制，结合机器人尺寸信息，预测每条候选轨迹相对于障碍物的安全距离（净空值）。
    *   **自动标签生成**：利用训练集中的 LiDAR 数据自动计算轨迹与障碍物的距离，无需人工标注。
*   **推理与筛选**：在推理阶段，模型生成多个候选轨迹，优先选择预测净空值最高（最安全）且最接近目标的路径。

#### 3. 实验设计
*   **数据集**：整合了 7 个异构数据集，包括 RELLIS-3D、TartanDrive 2.0、SCAND、Grand-tour 等，总计约 98.5 小时的导航数据，涵盖越野、人行道等多种地形。
*   **仿真场景**：在 Isaac-Sim 中构建了三个难度等级的越野环境（灌木丛、狭窄通道、杂乱障碍区）。
*   **Benchmark 与对比方法**：
    *   **NoMaD-FT**：最先进的纯视觉扩散导航模型（经过微调）。
    *   **NoMaD-Col**：在 NoMaD 基础上增加视觉碰撞预测的版本。
    *   **TEB-Elev**：基于高程图的传统局部规划器（非学习类基准）。
    *   **消融实验**：ViLiNT-Image-Mask（仅 LiDAR）和 ViLiNT-LiDAR-Mask（仅视觉）。
*   **真实世界部署**：使用 Clearpath Husky 机器人在实际障碍物场进行零样本迁移测试。

#### 4. 资源与算力
*   **推理端**：在机器人端使用 **Nvidia AGX Orin** 嵌入式计算平台实时运行。
*   **训练端**：论文**未明确说明**具体的训练 GPU 型号、数量及总训练时长。文中提到对基准模型进行了 20 个 epoch 的微调，并使用了 Flash-Attention 等优化技术。

#### 5. 实验数量与充分性
*   **实验规模**：在仿真环境中，针对每种方法在三个场景下各进行了 100 次独立运行，提供了统计学上的成功率（SR）和碰撞率（CR）。
*   **充分性与客观性**：
    *   实验涵盖了从纯视觉到多模态的对比，验证了 LiDAR 对避障的关键作用。
    *   通过改变输入向量中的机器人尺寸，验证了模型确实具有“具身感知”能力（尺寸变大时，模型会自动选择更宽敞的路径）。
    *   对比了传统规划器（TEB），承认了传统方法在参数调优后的优异表现，但也指出了其缺乏泛化性的弱点，实验对比相对客观。

#### 6. 主要结论与发现
*   **性能飞跃**：在仿真中，ViLiNT 的平均成功率比纯视觉 SOTA 模型（NoMaD）提升了 **166%**，碰撞率大幅下降。
*   **模态互补**：LiDAR 提供了精确的几何避障能力，而图像序列有助于捕捉动态特征和语义信息，两者结合效果最优。
*   **具身感知有效性**：通过引入具身 Token，模型能够根据不同机器人的物理尺寸动态调整路径选择，实现了跨平台的通用性。
*   **零样本迁移**：模型在未见过的真实越野场景中表现出极强的鲁棒性，能够有效绕过复杂障碍物。

#### 7. 优点（亮点）
*   **多模态深度融合**：不同于简单的特征拼接，采用 Token 级别的 Transformer 融合，能更好地处理异构传感器数据。
*   **具身感知设计**：将机器人尺寸作为输入，解决了“一个模型适配多种尺寸机器人”的问题。
*   **风险规避机制**：引入显式的净空预测头对扩散模型生成的轨迹进行二次筛选，极大增强了安全性。
*   **弱监督学习**：利用 LiDAR 几何关系自动生成碰撞标签，降低了对高质量人工标注数据的依赖。

#### 8. 不足与局限
*   **解耦风险**：轨迹生成与净空预测是相对独立的，如果扩散模型未能生成任何安全路径，筛选机制也无法奏效（尽管文中提到了掩码目标进行探索的补救措施）。
*   **算力需求**：Transformer 结合扩散模型的架构对嵌入式端的实时计算能力要求较高。
*   **局部最优**：作为局部规划器，在面对复杂的死胡同或大型障碍物时，仍可能陷入局部最优，需要更高层的全局路径规划配合。
*   **训练细节缺失**：缺乏具体的算力消耗和训练收敛曲线说明，不利于复现评估。

（完）
