---
title: "EmbodiedMidtrain: Bridging the Gap between Vision-Language Models and Vision-Language-Action Models via Mid-training"
title_zh: EmbodiedMidtrain：通过中期训练弥合视觉-语言模型与视觉-语言-动作模型之间的差距
authors: "Yiyang Du, Zhanqiu Guo, Xin Ye, Liu Ren, Chenyan Xiong"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.20012v1"
tags: ["query:robot"]
score: 10.0
evidence: 通过中期训练弥合VLM与VLA之间的差距
tldr: 本研究针对视觉语言模型（VLM）与具身智能动作模型（VLA）之间的数据分布差异，提出了 EmbodiedMidtrain 框架。该方法通过轻量级邻近估计器从海量 VLM 数据中筛选出与具身任务高度对齐的样本，在 VLA 微调前进行“中阶训练”。实验证明，该方法在多个机器人操作基准上显著提升了性能，使中等规模模型能媲美更大规模的专家模型，为具身智能提供了更强的初始化能力和更好的空间推理支持。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型多直接采用未针对具身领域优化的通用 VLM，导致两者间存在显著的数据分布鸿沟并限制了下游性能。
method: 引入中阶训练阶段，利用可学习的邻近估计器从大规模 VLM 池中筛选对齐数据，在下游微调前对模型进行领域适配。
result: 在三个机器人操作基准上一致提升了性能，且在训练初期就展现出更强的初始化优势，达到了与更大规模模型竞争的水平。
conclusion: 通过在数据层面建立对齐机制，EmbodiedMidtrain 有效增强了模型的空间推理能力，成功弥合了通用视觉语言能力与具身动作之间的差距。
---

## 摘要
视觉-语言-动作模型 (VLAs) 继承了视觉-语言模型 (VLMs) 的视觉和语言能力，但大多数 VLA 是基于现成的、未针对具身领域进行适配的 VLM 构建的，这限制了它们的下游性能。在这项工作中，我们提出了 EmbodiedMidtrain，旨在弥合 VLM 与 VLA 之间的差距。我们首先刻画了两者之间的数据分布差距，表明 VLA 数据占据了与更广泛的 VLM 分布基本分离的紧凑区域，而对齐程度在不同 VLM 数据源之间以及同一数据源内部都存在显著差异。随后，我们构建了一个中期训练数据引擎，利用轻量级的可学习邻近度估计器从庞大的 VLM 池中筛选出与 VLA 最对齐的候选样本，并在下游 VLA 微调之前，使用这些精选的混合数据对 VLM 进行中期训练。在三个机器人操控基准测试上的实验表明，中期训练在不同的 VLM 骨干网络上均能持续提升性能，其结果可与专家级 VLA 以及具有更大模型规模和训练预算的现成 VLM 相媲美。进一步的分析表明，中期训练为 VLA 微调提供了更强大的初始化，其增益从训练初期就开始显现，并在整个训练过程中不断扩大。此外，该数据引擎能够捕捉数据集级和样本级的对齐信号，在保留 VLM 数据多样性的同时，更倾向于空间推理任务而非以文本为中心的任务。我们将发布所有代码、数据和模型，以供未来研究使用。

## Abstract
Vision-Language-Action Models (VLAs) inherit their visual and linguistic capabilities from Vision-Language Models (VLMs), yet most VLAs are built from off-the-shelf VLMs that are not adapted to the embodied domain, limiting their downstream performance. In this work, we propose EmbodiedMidtrain to bridge the gap between VLMs and VLAs. We first characterize the data distribution gap between them, showing that VLA data occupy compact regions that are largely separated from the broader VLM distribution, while the degree of alignment varies substantially both across and within VLM data sources. Then, we build a mid-training data engine that leverages a lightweight learnable proximity estimator to select the most VLA-aligned candidates from a large VLM pool, and mid-trains the VLM on this curated mixture before downstream VLA fine-tuning. Experiments on three robot manipulation benchmarks show that mid-training consistently improves performance across different VLM backbones, achieving results competitive with expert VLAs and off-the-shelf VLMs trained with larger model scale and training budgets. Further analysis reveals that mid-training provides a stronger initialization for VLA fine-tuning, with gains emerging from the earliest steps and widening throughout training. Moreover, the data engine captures both dataset-level and sample-level alignment signals, favoring spatial reasoning over text-centric tasks while preserving the diversity of the VLM data. We will release all code, data and models for future research.

---

## 论文详细总结（自动生成）

这是一份关于论文《EmbodiedMidtrain: Bridging the Gap between Vision-Language Models and Vision-Language-Action Models via Mid-training》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的具身智能模型（VLA）通常直接采用通用的视觉-语言模型（VLM）作为骨干网络。然而，通用 VLM 的预训练数据（如网页图文对、VQA）与具身任务数据（如机器人操作轨迹）之间存在巨大的**分布鸿沟（Distribution Gap）**。
*   **研究动机**：直接在具身数据上微调未适配的 VLM 往往效率低下，且无法充分利用 VLM 的潜力。论文旨在探索一种“中期训练（Mid-training）”阶段，通过在数据层面进行对齐，为 VLA 提供一个更适合具身推理的初始化状态。

### 2. 方法论
*   **核心思想**：在 VLA 最终微调之前，引入一个轻量级的数据筛选与中期训练环节，将 VLM 的知识分布向 VLA 的目标分布靠拢。
*   **关键技术细节**：
    1.  **分布差距分析**：利用最大均值差异（MMD）和 t-SNE 可视化量化 VLM 与 VLA 数据的距离，发现 VLA 数据分布极其紧凑且与通用 VLM 数据显著分离。
    2.  **邻近度估计器（Proximity Estimator）**：训练一个轻量级的二分类器（线性层），输入是冻结 VLM 的特征。该分类器学习区分 VLA 数据（正样本）和 VLM 数据（负样本）。
    3.  **数据筛选引擎**：利用分类器输出的概率作为“邻近度得分”，从大规模 VLM 池中筛选出得分最高的 Top-K 样本（约 120 万条），构建分布对齐的中期训练集。
    4.  **中期训练流程**：在筛选出的数据上对 VLM 进行全参数微调，随后再进行常规的 VLA 动作生成任务微调。

### 3. 实验设计
*   **数据集与场景**：
    *   **Calvin ABC-D**：测试模型在不同场景配置下的长程操作能力。
    *   **SimplerEnv Bridge**：真实到模拟（Real-to-Sim）的基准，包含多种桌面操作任务。
    *   **LIBERO-10**：包含 10 个具有挑战性的长程任务。
*   **对比方法**：
    *   **专家 VLA 模型**：OpenVLA (7.7B), $\pi_0$ (3.1B)。
    *   **现成 VLM 骨干**：Qwen2.5VL (3B/7B), Qwen3VL (2B/4B/8B/30B), Paligemma 系列, KosMos-2。
*   **评估指标**：任务成功率、连续完成任务的平均长度。

### 4. 资源与算力
*   **算力平台**：论文提到使用了 CMU 的 FLAME 中心资源以及 NCSA 的 Delta/DeltaAI 集群。
*   **训练细节**：
    *   **中期训练**：Batch size 为 256，训练 5000 步。
    *   **VLA 微调**：Batch size 为 256，训练 16000 步。
*   **具体型号**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，但从其训练步数和模型规模（1B-2B）推断，属于中等算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流机器人基准上进行了全面测试。
    *   使用了 2 种不同的 VLM 骨干（InternVL3.5-1B, Qwen3VL-2B）验证通用性。
    *   **消融实验**：对比了随机选择数据 vs. 邻近度筛选，以及不同的筛选指标（特征距离、困惑度 PPL 等）。
    *   **跨架构验证**：用 InternVL 筛选的数据去训练 Qwen，验证了筛选信号的跨模型可迁移性。
*   **充分性评价**：实验设计较为扎实，不仅关注最终性能，还深入分析了训练动力学（Training Dynamics）和数据多样性保持情况，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：经过 EmbodiedMidtrain 的 1.1B 模型在 Calvin 等基准上超过了 7B-8B 规模的现成模型，甚至优于专门的专家 VLA 模型。
*   **初始化优势**：中期训练提供的优势从微调的第一步就已显现，且随着训练进行，与基准模型的差距不断扩大，证明了其提供了本质上更好的初始化。
*   **空间推理偏好**：数据引擎自动筛选出了更多涉及空间关系、坐标定位的样本（如 RefSpatial 数据集），而排除了纯文本识别类样本，这符合具身智能的需求。
*   **多样性保持**：筛选后的数据在保持 VLM 视觉多样性的同时，实现了向具身领域的分布偏移。

### 7. 优点
*   **轻量且高效**：无需改变模型架构，仅通过数据层面的预处理即可获得显著增益。
*   **可扩展性强**：邻近度估计器训练极快（仅需几十步），易于在大规模数据集上推广。
*   **跨模型迁移**：发现数据对齐信号具有通用性，一套筛选出的数据可供不同架构的 VLM 使用。

### 8. 不足与局限
*   **通用能力损耗风险**：虽然论文提到保留了多样性，但在 BLINK 等通用视觉基准上观察到轻微的性能下降，说明中期训练可能存在一定的“灾难性遗忘”或领域过拟合风险。
*   **依赖目标分布质量**：该方法的效果高度依赖于用于训练估计器的“目标 VLA 数据”的质量和代表性。
*   **实验覆盖范围**：目前主要集中在模拟环境和 1B-2B 规模的模型，尚未在更大规模模型（如 70B+）或更复杂的真实世界多机器人场景中广泛验证。

（完）
