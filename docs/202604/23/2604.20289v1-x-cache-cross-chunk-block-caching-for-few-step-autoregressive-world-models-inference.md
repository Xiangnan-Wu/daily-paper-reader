---
title: "X-Cache: Cross-Chunk Block Caching for Few-Step Autoregressive World Models Inference"
title_zh: X-Cache：面向少步自回归世界模型推理的跨分块模块级缓存
authors: "Yixiao Zeng, Jianlei Zheng, Chaoda Zheng, Shijia Chen, Mingdian Liu, Tongping Liu, Tengwei Luo, Yu Zhang, Boyang Wang, Linkun Xu, Siyuan Lu, Bo Tian, Xianming Liu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20289v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 加速用于驾驶的自回归视频扩散世界模型
tldr: 针对自动驾驶世界模型推理成本高的问题，本文提出X-Cache，一种无需训练的跨块缓存加速方法。不同于传统的跨去噪步缓存，X-Cache利用连续生成块间的冗余，通过双指标门控机制动态决定模块重用，并强制在KV更新块进行全量计算以防止误差累积。在X-world模型上实现了2.6倍加速，且几乎不损失生成质量，为实时交互式仿真提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的扩散模型缓存技术无法应用于少步数蒸馏的自回归世界模型，导致实时交互式仿真推理效率低下。
method: 提出跨块残差缓存机制，利用结构和动作感知的指纹进行双指标门控，并结合KV更新块强制计算来切断误差传播。
result: "在多摄像头驾驶世界模型上实现了71%的模块跳过率，获得2.6倍的端到端推理加速，且保持了极高的生成质量。"
conclusion: X-Cache证明了在自回归扩散模型中利用跨时间块冗余进行推理加速的可行性，是实现高性能实时世界模拟器的关键技术。
---

## 摘要
实时世界模拟正成为自动驾驶系统可扩展评估和在线强化学习的关键基础设施。近期基于自回归视频扩散构建的驾驶世界模型实现了高保真、可控的多摄像头生成，但其推理成本仍是交互式部署的瓶颈。然而，现有的扩散缓存方法是为具有多个去噪步骤的离线视频生成设计的，无法迁移到此场景。少步蒸馏模型没有剩余的步间冗余供这些方法重用，而序列级并行化技术需要闭环交互式生成无法提供的未来条件。我们提出了 X-Cache，这是一种无需训练的加速方法，它沿着不同的轴进行缓存：跨连续的生成分块（chunks）而非跨去噪步骤。X-Cache 维护跨分块持久存在的逐模块残差缓存，并对感知结构和动作的模块输入指纹应用双指标门控机制，以独立决定每个模块应重新计算还是重用其缓存的残差。为了防止近似误差永久污染自回归 KV 缓存，X-Cache 识别 KV 更新分块（即向持久缓存写入干净键和值的前向传递），并无条件强制在这些分块上进行完整计算，从而切断误差传播。我们在 X-world 上实现了 X-Cache，这是一个基于多模块因果 DiT 构建的生产级多摄像头动作条件驾驶世界模型，具有少步去噪和滚动 KV 缓存。X-Cache 实现了 71% 的模块跳过率和 2.6 倍的实际运行时间加速，同时保持了极低的性能退化。

## Abstract
Real-time world simulation is becoming a key infrastructure for scalable evaluation and online reinforcement learning of autonomous driving systems. Recent driving world models built on autoregressive video diffusion achieve high-fidelity, controllable multi-camera generation, but their inference cost remains a bottleneck for interactive deployment. However, existing diffusion caching methods are designed for offline video generation with multiple denoising steps, and do not transfer to this scenario. Few-step distilled models have no inter-step redundancy left for these methods to reuse, and sequence-level parallelization techniques require future conditioning that closed-loop interactive generation does not provide. We present X-Cache, a training-free acceleration method that caches along a different axis: across consecutive generation chunks rather than across denoising steps. X-Cache maintains per-block residual caches that persist across chunks, and applies a dual-metric gating mechanism over a structure- and action-aware block-input fingerprint to independently decide whether each block should recompute or reuse its cached residual. To prevent approximation errors from permanently contaminating the autoregressive KV cache, X-Cache identifies KV update chunks (the forward passes that write clean keys and values into the persistent cache) and unconditionally forces full computation on these chunks, cutting off error propagation. We implement X-Cache on X-world, a production multi-camera action-conditioned driving world model built on multi-block causal DiT with few-step denoising and rolling KV cache. X-Cache achieves 71% block skip rate with 2.6x wall-clock speedup while maintaining minimum degradation.

---

## 论文详细总结（自动生成）

这篇论文由小鹏汽车（XPeng Inc.）AI Infra 团队提交，介绍了一种名为 **X-Cache** 的推理加速技术，专门针对自动驾驶中的少步自回归（Autoregressive, AR）视频扩散世界模型。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：自回归视频扩散模型（如 X-World）已成为自动驾驶闭环仿真和强化学习的关键工具。为了实现实时交互，这些模型通常经过蒸馏，仅需极少（如 4 步）的去噪步骤。
*   **核心问题**：现有的扩散模型加速技术（如 DeepCache）主要利用**跨去噪步（Cross-step）**的冗余。但在少步（Few-step）模型中，每一步的结构更新都非常显著，几乎没有冗余可言。此外，自回归生成具有严格的因果依赖，无法使用序列级并行加速。
*   **研究动机**：作者观察到，在物理连续的驾驶场景中，**跨生成分块（Cross-chunk）**（即连续的时间片段）之间存在极强的冗余性。DiT 模块在相邻时间块的相同去噪步上，输入特征高度相似。

### 2. 方法论
X-Cache 是一种无需训练（Training-free）的模块级残差缓存方法，核心思想是**跨块重用**。
*   **跨块残差缓存**：在第 $n$ 个分块生成时，缓存 DiT 模块的残差输出。在第 $n+1$ 个分块中，若满足条件，则直接跳过该模块计算，将缓存的残差加到当前输入上。
*   **双指标门控机制（Dual-Metric Gating）**：
    *   **指纹（Fingerprint）提取**：为了降低计算开销，对模块输入进行 3D 空间下采样，并加入全局均值通道和**动作条件（Action）通道**。这使得模型能敏锐感知驾驶操作的变化（如突然刹车或转向）。
    *   **判定指标**：结合余弦相似度（全局方向）和最大 Token 偏差（局部异常值）来决定是否跳过。
*   **自适应阈值**：为每个模块位置维护一个指数移动平均（EMA）阈值，根据历史相似度动态调整，避免手动调参。
*   **安全机制（防止误差累积）**：
    *   **KV 更新帧保护**：自回归模型依赖 KV Cache。X-Cache 识别出负责写入 KV Cache 的前向传递过程，并**强制进行全量计算**，从而切断近似误差在时间轴上的永久传播。
    *   **锚点模块（Anchor Blocks）**：强制计算前 $N$ 个模块（默认 $F_n=1$），确保条件信号（如 Action）能有效注入。

### 3. 实验设计
*   **实验模型**：**X-World**（基于 WAN 2.2 构建的生产级多摄像头驾驶世界模型），支持 7 摄像头 360° 环视生成，12 FPS。
*   **数据集/场景**：内部测试集，包含 13 个长序列（约 22 秒/个），涵盖三种典型场景：
    1.  **城市街道**：高密度交通、行人和复杂背景。
    2.  **高速公路**：快速行驶。
    3.  **掉头（U-turn）**：极端的航向角变化，用于压力测试跨块相似度。
*   **Benchmark**：以全量计算（Full-compute）的生成结果为基准。
*   **评估指标**：
    *   **效率**：模块跳过率（Skip Rate）、DiT 墙钟耗时、加速比。
    *   **质量**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似性）。

### 4. 资源与算力
*   **硬件平台**：使用阿里巴巴平头哥（T-Head）开发的 **Zhenwu（真武）810E** AI 加速器（文中称为 PPU）。
*   **配置**：单张 PPU 集成 96GB HBM2e 显存，支持 BF16 硬件加速。
*   **训练/推理**：所有 DiT 前向传递均在单张 PPU 上以 BF16 精度执行。文中未提及训练时长，因为该方法是**无需训练**的推理侧优化。

### 5. 实验数量与充分性
*   **实验规模**：对 13 个长序列进行了完整测试，并针对其中一个 264 帧的序列进行了详尽的消融实验。
*   **充分性评价**：
    *   **场景覆盖**：覆盖了从平稳行驶到剧烈转向的场景，具有代表性。
    *   **消融实验**：非常充分，分别验证了 KV 保护、步 0 保护、锚点模块和自适应阈值底线的必要性。
    *   **客观性**：通过与全量计算结果的逐像素对比（PSNR > 51dB, SSIM > 0.999），证明了该方法在视觉上几乎无损。

### 6. 主要结论与发现
*   **加速效果显著**：在保持极高保真度的前提下，实现了 **71% 的模块跳过率**，DiT 推理速度提升了 **2.6 倍**。
*   **跨场景稳定性**：尽管掉头场景运动剧烈，但由于运动模糊和自适应门控，其加速比和质量与城市场景基本持平。
*   **关键发现**：**KV 更新帧保护是核心安全线**。如果不开启此项，PSNR 会从 53dB 骤降至 21dB，导致严重的视觉伪影。

### 7. 优点
*   **维度创新**：首次提出在**跨时间块（Cross-chunk）**维度而非去噪步维度进行缓存，解决了少步蒸馏模型无法加速的痛点。
*   **动作感知**：将驾驶动作向量引入指纹判定，解决了传统缓存方法对控制指令不敏感的问题。
*   **生产级验证**：在复杂的多摄像头自回归模型上验证，具有极强的实用价值。

### 8. 不足与局限
*   **长时漂移风险**：虽然有 KV 保护，但在极长（如数分钟）的仿真中，微小误差是否会通过物理状态反馈产生漂移仍需进一步验证。
*   **场景局限**：实验主要集中在内部数据集，未涵盖极端天气（暴雨、黑夜）或极端激进驾驶行为，这些场景下跨块相似度可能会大幅下降。
*   **硬件依赖**：虽然原理通用，但加速比数据基于特定的 PPU 硬件，在 NVIDIA GPU 上的具体表现（受算子对齐和显存带宽影响）未直接给出。

（完）
