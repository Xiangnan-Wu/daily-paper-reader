---
title: "AdaTracker: Learning Adaptive In-Context Policy for Cross-Embodiment Active Visual Tracking"
title_zh: AdaTracker：面向跨具身主动视觉跟踪的学习自适应上下文策略
authors: "Kui Wu, Hao Chen, Jinzhu Han, Haijun Liu, Churan Wang, Yizhou Wang, Zhoujun Li, Si Liu, Fangwei Zhong"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20305v1"
tags: ["query:robot"]
score: 9.0
evidence: 跨机器人形态的自适应控制策略
tldr: 针对不同机器人形态在主动视觉跟踪中物理约束和动力学差异大的挑战，本文提出AdaTracker框架。该框架通过上下文编码器从历史轨迹中推断特定机器人的形态约束，并利用上下文感知策略动态调整控制指令，实现了跨机器人的零样本自适应跟踪。实验证明，该方法在泛化性、样本效率和真实环境部署方面均显著优于现有技术。
selection_source: fresh_fetch
motivation: 现有方法通常需为不同机器人单独训练模型，导致跨形态泛化能力差且难以扩展到新平台。
method: 提出一种自适应上下文策略学习框架，通过编码器提取机器人特定约束并结合辅助任务确保时序一致性。
result: 在仿真和真实世界测试中，AdaTracker在跨形态泛化和零样本自适应任务上均取得了领先的性能。
conclusion: AdaTracker证明了通过显式建模形态上下文，可以使用单一统一模型实现跨多样化机器人的鲁棒主动视觉跟踪。
---

## 摘要
在不同机器人上利用单一统一模型实现主动视觉跟踪具有挑战性，因为不同平台的物理约束和运动动力学差异巨大。现有方法通常为每种具身训练独立模型，导致可扩展性差且泛化能力有限。为解决这一问题，我们提出了 AdaTracker，这是一个自适应上下文策略学习框架，能够在多种机器人形态上稳健地跟踪目标。我们的核心见解是通过具身上下文编码器（Embodiment Context Encoder）显式建模特定具身的约束，该编码器能从历史记录中推断出特定具身的约束。这种上下文表示动态调节上下文感知策略（Context-Aware Policy），使其能够以零样本方式为未见过的具身推断出最优控制动作。为了增强稳健性，我们引入了两个辅助目标，以确保准确的上下文识别和时间一致性。仿真和真实世界的实验表明，AdaTracker 在跨具身泛化、样本效率和零样本自适应方面显著优于现有最先进的方法。

## Abstract
Realizing active visual tracking with a single unified model across diverse robots is challenging, as the physical constraints and motion dynamics vary drastically from one platform to another. Existing approaches typically train separate models for each embodiment, leading to poor scalability and limited generalization. To address this, we propose AdaTracker, an adaptive in-context policy learning framework that robustly tracks targets on diverse robot morphologies. Our key insight is to explicitly model embodiment-specific constraints through an Embodiment Context Encoder, which infers embodiment-specific constraints from history. This contextual representation dynamically modulates a Context-Aware Policy, enabling it to infer optimal control actions for unseen embodiments in a zero-shot manner. To enhance robustness, we introduce two auxiliary objectives to ensure accurate context identification and temporal consistency. Experiments in both simulation and the real world demonstrate that AdaTracker significantly outperforms state-of-the-art methods in cross-embodiment generalization, sample efficiency, and zero-shot adaptation.

---

## 论文详细总结（自动生成）

### AdaTracker: 面向跨具身主动视觉跟踪的学习自适应上下文策略 —— 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
主动视觉跟踪（Embodied Visual Tracking, EVT）要求机器人通过主动控制位姿，使目标始终保持在视野中心。然而，不同机器人（具身）在**相机高度（视角）**和**运动动力学（速度、惯性）**上存在巨大差异。
*   **研究动机**：现有的 EVT 方法通常针对特定机器人形态进行优化，一旦部署到新平台（如从轮式机器人换到无人机），由于“具身失配”（Embodiment Mismatch），策略往往会失效。
*   **核心问题**：如何构建一个统一的模型，使其能够无需重新训练或校准，即可在具有不同物理约束和动力学特性的异构机器人平台上实现零样本（Zero-shot）自适应跟踪。

#### 2. 方法论：核心思想与关键技术
AdaTracker 的核心思想是**上下文强化学习（In-context RL）**，通过历史交互数据推断当前机器人的物理特性。

*   **具身上下文编码器（Embodiment Context Encoder）**：
    *   输入过去 $K$ 步的视觉掩码（Mask）和动作（Action）序列。
    *   利用卷积骨干网络和 LSTM 提取潜在上下文向量 $z_t$，该向量表征了当前机器人的视角和动力学约束。
*   **辅助任务（Auxiliary Objectives）**：
    *   **上下文识别（监督学习）**：通过 MLP 预测当前步的奖励 $r_t$ 和相机的绝对高度 $h$。这迫使 $z_t$ 编码与动力学和视角相关的关键信息。
    *   **时序一致性（自监督学习）**：利用余弦相似度损失，约束同一集（Episode）内的 $z_t$ 在初期快速稳定，确保策略执行的连续性。
*   **上下文感知策略（Context-Aware Policy）**：
    *   将当前帧的视觉特征与推断出的 $z_t$ 拼接，输入到基于 LSTM 的 Actor-Critic 网络中。
    *   采用**离线强化学习（Offline RL）**算法（CQL-SAC）进行训练，从预先收集的多样化具身数据中学习。
*   **视觉感知层**：使用视觉基础模型（VFM，如 SAM2）生成语义分割掩码，以消除不同环境间的视觉域差异。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：基于 UnrealCV 构建的 5 个场景（SimpleRoom, ParkingLot, UrbanCity, UrbanRoad, SnowVillage）。
    *   **数据集规模**：收集了 190k 步的跨具身跟踪轨迹。
    *   **具身配置**：通过改变相机高度（0.3m 至 3.0m）和最大速度（0.5m/s 至 3.0m/s）组合出 16 种不同的机器人配置。
*   **Benchmark 与对比方法**：
    *   **PID + Video Tracker**：传统控制方法（SAM2 + GroundingDINO + PID）。
    *   **Online RL**：在线微调的强化学习方法。
    *   **Offline EVT**：之前的 SOTA 离线强化学习 EVT 方法。
    *   **TrackVLA**：最新的视觉-语言-动作大模型。
*   **真实世界验证**：在轮式机器人、四足机器人（宇树 Go2）和无人机（大疆晓 Spark）上进行零样本迁移测试。

#### 4. 资源与算力
*   **部署算力**：论文提到在 **Nvidia RTX A3000** GPU 上，VFM 推理与 RL 控制器的流水线运行速度约为 **20 Hz**，满足实时部署需求。
*   **训练算力**：文中**未明确说明**具体的训练总时长、使用的 GPU 数量及型号。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个不同气候和光照条件的仿真场景中进行了测试。
    *   针对 16 种具身配置进行了详尽的性能分解（AR/EL/SR 指标）。
    *   进行了 4 组消融实验，分别验证了上下文识别、时序一致性、上下文编码器整体以及 LSTM 结构的作用。
*   **充分性评价**：实验设计较为充分。不仅涵盖了分布内（In-distribution）的测试，还专门设置了**分布外（OOD）**的极端高度和速度测试，证明了模型的泛化外推能力。真实世界的跨平台部署进一步增强了结果的说服力。

#### 6. 主要结论与发现
*   **跨具身泛化能力强**：AdaTracker 在所有未见过的仿真场景和异构机器人平台上均取得了 SOTA 性能，成功解决了视角和动力学偏移问题。
*   **上下文推断是关键**：消融实验显示，如果没有显式的上下文编码，单一策略在处理多模态动作需求时会产生歧义，导致控制不稳定。
*   **零样本迁移可行**：模型能够直接从仿真环境迁移到真实的四足机器人和无人机上，无需任何微调，且在复杂地形和惯性影响下表现鲁棒。

#### 7. 优点（亮点）
*   **自适应性**：通过 In-context 方式实时推断机器人特性，避免了繁琐的手工校准或一机一模的训练。
*   **辅助任务设计**：引入高度预测和时序一致性损失，巧妙地解决了潜在空间表示不稳定导致的控制震荡问题。
*   **架构解耦**：将视觉感知（VFM）与运动控制解耦，利用掩码作为输入，极大地提升了 sim-to-real 的成功率。

#### 8. 不足与局限
*   **动作空间限制**：目前仅支持中层的速度指令接口，对于需要底层关节控制或极高动态响应的场景（如高速穿越）可能受限。
*   **感知延迟依赖**：系统性能高度依赖 VFM 的推理速度，虽然在 A3000 上达到 20Hz，但在算力更弱的嵌入式平台上可能面临挑战。
*   **任务单一性**：目前仅聚焦于视觉跟踪任务，尚未验证该框架在更复杂的复合任务（如导航+避障+交互）中的表现。

（完）
