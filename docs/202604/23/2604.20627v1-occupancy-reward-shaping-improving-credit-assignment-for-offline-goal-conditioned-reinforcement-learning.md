---
title: "Occupancy Reward Shaping: Improving Credit Assignment for Offline Goal-Conditioned Reinforcement Learning"
title_zh: 占用奖励塑造：改进离线目标条件强化学习中的信用分配
authors: "Aravind Venugopal, Jiayu Chen, Xudong Wu, Chongyi Zheng, Benjamin Eysenbach, Jeff Schneider"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20627v1"
tags: ["query:robot"]
score: 9.0
evidence: 从世界模型中提取时间信息用于强化学习
tldr: 针对离线目标条件强化学习中信用分配困难的问题，本文提出了占用奖励塑造（ORS）方法。该方法利用生成式世界模型捕获的未来状态分布，通过最优传输理论提取环境几何信息并转化为奖励函数。ORS在理论上保证不改变最优策略，在13个长程任务中将性能提升了2.2倍，并成功应用于核聚变控制等现实场景。
selection_source: fresh_fetch
motivation: 离线强化学习中动作与其长期后果之间的时间延迟导致信用分配极具挑战，尤其是在稀疏奖励设置下。
method: 利用最优传输理论从学习到的占用度量模型中提取环境几何信息，并将其转化为一种不改变最优策略的奖励塑造函数。
result: 在13个复杂的长程运动和操作任务中实现了2.2倍的性能提升，并在托卡马克核聚变控制任务中验证了有效性。
conclusion: ORS通过有效利用世界模型中的时间信息改善了信用分配，是提升离线目标条件强化学习性能的有力工具。
---

## 摘要
动作与其长期后果之间的时间滞后使得从数据中学习目标导向行为时的信用分配成为一项挑战。生成式世界模型捕捉了智能体可能访问的未来状态分布，这表明它们已经捕捉到了时间信息。如何提取这些时间信息来进行信用分配？在本文中，我们形式化地说明了存储在世界模型中的时间信息是如何编码世界的底层几何结构的。利用最优传输，我们从学习到的占用度量模型中提取这种几何结构，并将其转化为一个捕捉目标达成信息的奖励函数。我们提出的方法——占用奖励塑造（ORS），在很大程度上缓解了稀疏奖励设置下的信用分配问题。理论证明 ORS 不会改变最优策略，而在 13 个不同的长时程运动和操纵任务中，实验结果显示其性能提升了 2.2 倍。此外，我们还在 3 个托卡马克控制任务中展示了 ORS 在现实世界核聚变控制中的有效性。

## Abstract
The temporal lag between actions and their long-term consequences makes credit assignment a challenge when learning goal-directed behaviors from data. Generative world models capture the distribution of future states an agent may visit, indicating that they have captured temporal information. How can that temporal information be extracted to perform credit assignment? In this paper, we formalize how the temporal information stored in world models encodes the underlying geometry of the world. Leveraging optimal transport, we extract this geometry from a learned model of the occupancy measure into a reward function that captures goal-reaching information. Our resulting method, Occupancy Reward Shaping, largely mitigates the problem of credit assignment in sparse reward settings. ORS provably does not alter the optimal policy, yet empirically improves performance by 2.2x across 13 diverse long-horizon locomotion and manipulation tasks. Moreover, we demonstrate the effectiveness of ORS in the real world for controlling nuclear fusion on 3 Tokamak control tasks.   Code: https://github.com/aravindvenu7/occupancy_reward_shaping; Website: https://aravindvenu7.github.io/website/ors/

---

## 论文详细总结（自动生成）

### 论文总结：Occupancy Reward Shaping (ORS)

#### 1. 论文的核心问题与整体含义
*   **研究动机**：在离线目标条件强化学习（Offline GCRL）中，智能体需要仅通过静态数据集学习达成目标的行为。核心挑战在于**信用分配（Credit Assignment）**：在稀疏奖励环境下，动作与其长期后果之间存在巨大的时间滞后，导致价值函数难以准确估计。
*   **核心问题**：生成式世界模型（World Models）能够捕捉未来状态的分布（即占用度量），这其中隐含了环境的几何和时间信息。论文探讨如何从这些模型中提取时间信息，并将其转化为有效的奖励信号，以引导智能体在长程任务中学习。

#### 2. 论文提出的方法论
*   **核心思想**：利用**最优传输（Optimal Transport）**理论，将学习到的占用度量（Occupancy Measure）与目标状态之间的距离转化为标量奖励。
*   **关键技术细节**：
    1.  **学习占用度量**：使用**流匹配（Flow Matching）**技术训练一个生成模型 $d^{\pi_D}$，该模型能够预测在给定当前状态-动作对下，数据集策略未来可能访问的状态分布。
    2.  **提取几何信息**：通过计算目标状态的狄拉克分布（$\delta_g$）与占用度量分布之间的 **Wasserstein-2 距离**（$W_2^2$）来衡量当前状态与目标的“时间距离”。
    3.  **奖励塑造（Reward Shaping）**：定义奖励函数 $r_W(s, a, g) = -W_2^2(\delta_g, d^{\pi_D})$。论文证明了这种基于占用度量的奖励塑造方式属于势能奖励塑造（PBRS）的变体，**在理论上保证不会改变最优策略**。
    4.  **算法流程**：先预训练流匹配占用模型；然后训练一个神经网络来近似 $W_2^2$ 距离作为奖励函数；最后使用标准的离线 RL 算法（如 GCIQL）进行策略优化。

#### 3. 实验设计
*   **数据集与场景**：
    *   **OGBench**：包含 13 个具有挑战性的长程任务，涵盖迷宫导航（Antmaze）、方块操纵（Cube）、拼图（Puzzle）和复杂场景（Scene）。
    *   **现实世界应用**：在 DIII-D **托卡马克（Tokamak）**核聚变反应堆控制任务上进行验证，涉及等离子体压力（$\beta_N$）、电子密度和离子旋转的控制。
*   **对比方法（Baselines）**：
    *   基础方法：GCBC, GCIQL, GCIVL。
    *   度量/对比学习方法：QRL, CRL。
    *   长程/稀疏奖励专门方法：Go-Fresh（基于图的奖励塑造）、HIQL（分层 RL）、SAW、SMORE、n-step GCIQL 等。

#### 4. 资源与算力
*   **硬件**：实验使用了单张 **NVIDIA RTX A6000 GPU**。
*   **训练时长/效率**：
    *   占用模型训练：约 14ms/迭代。
    *   奖励函数训练：约 8ms/迭代。
    *   策略训练：约 6.5ms/迭代。
    *   总训练迭代次数根据任务复杂度不同，从 100 万次到 600 万次不等（例如 Antmaze-giant 需要 600 万次）。

#### 5. 实验数量与充分性
*   **实验规模**：在 13 个 OGBench 任务和 3 个托卡马克任务上进行了测试。
*   **重复实验**：OGBench 任务采用 **8 个随机种子**，托卡马克任务采用 **4 个随机种子**，并报告了 95% 的置信区间。
*   **消融实验**：针对奖励函数设计（状态奖励 vs 状态-动作奖励）、价值函数非单调性分析、超参数 $\kappa$ 的敏感性等进行了深入分析。
*   **评价**：实验覆盖了从简单到极高难度的任务，对比了超过 10 种基准算法，实验设计客观、公平且非常充分。

#### 6. 主要结论与发现
*   **性能提升**：ORS 在 13 个任务上平均比基础算法（GCIQL）性能提升了 **2.2 倍**，比之前的 SOTA 方法（Go-Fresh）提升了 24%。
*   **信用分配改善**：分析表明，ORS 显著降低了价值函数的非单调性误差（Value Non-monotonicity），使长程目标的价值估计更加平滑。
*   **现实可行性**：在复杂的非线性随机系统（核聚变控制）中，ORS 表现优于专门针对长程任务设计的基准方法，证明了其在真实物理系统中的潜力。

#### 7. 优点
*   **理论完备**：利用最优传输理论建立了占用度量与环境几何的联系，并证明了策略不变性。
*   **无需构图**：相比于传统的基于图的方法（如 Go-Fresh），ORS 不需要复杂的图构建和搜索过程，扩展性更好。
*   **通用性强**：可以作为插件应用于任何基于 TD 学习的离线 GCRL 算法之上。
*   **效果显著**：在极长程任务（如 Antmaze-giant）中表现出极强的鲁棒性。

#### 8. 不足与局限
*   **计算开销**：虽然不需要图搜索，但需要额外训练一个生成式流匹配模型和奖励模型，增加了前期的计算负担。
*   **组合爆炸问题**：在状态空间具有高度组合性且成功路径极少的任务（如多步密码锁）中，由于占用度量难以覆盖有效路径，奖励信号可能依然微弱。
*   **样本依赖**：奖励塑造的效果高度依赖于离线数据集的质量和覆盖范围，如果数据中完全缺失通往目标的路径，ORS 也无法凭空创造信号。

（完）
