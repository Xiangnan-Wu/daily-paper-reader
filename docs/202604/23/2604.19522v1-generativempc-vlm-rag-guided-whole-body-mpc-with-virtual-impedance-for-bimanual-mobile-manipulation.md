---
title: "GenerativeMPC: VLM-RAG-guided Whole-Body MPC with Virtual Impedance for Bimanual Mobile Manipulation"
title_zh: GenerativeMPC：基于 VLM-RAG 引导及虚拟阻抗的双臂移动操作全身模型预测控制
authors: "Marcelino Julio Fernando, Miguel Altamirano Cabrera, Jeffrin Sam, Yara Mahmoud, Konstantin Gubernatorov, Dzmitry Tsetserukou"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19522v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于双臂移动操作控制的VLM引导分层框架
tldr: 针对双臂移动机器人操作中语义理解与物理交互难以融合的问题，本文提出GenerativeMPC框架。该框架利用VLM-RAG技术将视觉语言上下文转化为全身模型预测控制（MPC）的动态约束和阻抗控制参数。通过经验驱动的向量数据库实现参数映射，无需重新训练即可实现上下文感知的安全交互。实验证明该方法在人机协作场景下能显著提升安全性和灵活性。
selection_source: fresh_fetch
motivation: 旨在解决端到端模型不透明以及传统控制器缺乏语义上下文，导致双臂移动机器人在复杂人机交互中难以兼顾安全与灵活性的问题。
method: 提出一种分层架构，利用VLM-RAG将语义信息映射为MPC的物理约束和阻抗控制器的虚拟刚度与阻尼参数。
result: "在仿真和实物平台上验证了系统能根据环境语义动态调整行为，如在靠近人类时自动减速60%并实现社会意识导航。"
conclusion: 该研究通过将大规模认知模型植入高频物理控制循环，为实现具备语义理解能力和安全保障的机器人操作提供了有效路径。
---

## 摘要
双臂移动操作需要高层语义推理与安全、顺应的物理交互之间的无缝集成，而端到端模型处理这一挑战的方式往往不够透明，经典控制器则缺乏解决该问题所需的上下文信息。本文提出了 GenerativeMPC，这是一种分层信息物理框架，明确地衔接了双臂移动机器人的语义场景理解与物理控制参数。该系统利用带有检索增强生成的视觉语言模型（VLM-RAG），将视觉和语言上下文转化为具身化的控制约束，具体表现为为全身模型预测控制器（MPC

## Abstract
Bimanual mobile manipulation requires a seamless integration between high-level semantic reasoning and safe, compliant physical interaction - a challenge that end-to-end models approach opaquely and classical controllers lack the context to address. This paper presents GenerativeMPC, a hierarchical cyber-physical framework that explicitly bridges semantic scene understanding with physical control parameters for bimanual mobile manipulators. The system utilizes a Vision-Language Model with Retrieval-Augmented Generation (VLM-RAG) to translate visual and linguistic context into grounded control constraints, specifically outputting dynamic velocity limits and safety margins for a Whole-Body Model Predictive Controller (MPC). Simultaneously, the VLM-RAG module modulates virtual stiffness and damping gains for a unified impedance-admittance controller, enabling context-aware compliance during human-robot interaction. Our framework leverages an experience-driven vector database to ensure consistent parameter grounding without retraining. Experimental results in MuJoCo, IsaacSim, and on a physical bimanual platform confirm a 60% speed reduction near humans and safe, socially-aware navigation and manipulation through semantic-to-physical parameter grounding. This work advances the field of human-centric cybernetics by grounding large-scale cognitive models into predictable, high-frequency physical control loops.

---

## 论文详细总结（自动生成）

这是一份关于论文《GenerativeMPC: VLM-RAG-guided Whole-Body MPC with Virtual Impedance for Bimanual Mobile Manipulation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在双臂移动机器人操作中，将高层的**语义场景理解**（如“附近有人，请小心”）与底层的**物理控制参数**（如速度限制、刚度、阻尼）无缝结合。
*   **研究背景**：
    *   **端到端模型**（End-to-End）虽然强大但具有“黑盒”属性，缺乏透明度和安全性保障。
    *   **经典控制器**（如 MPC）虽然精确稳定，但通常使用预设的固定参数，无法根据环境语义（如人的出现）动态调整物理“手感”或安全边界。
*   **整体含义**：提出 GenerativeMPC 框架，利用视觉语言模型（VLM）和检索增强生成（RAG）技术，将语义上下文转化为可解释的物理约束，实现安全、具备社会意识的人机协作。

### 2. 方法论
该框架采用三层分层架构：
*   **第一层：VLM-RAG 任务规划层**
    *   使用量化后的 **InternVL3-1B** 模型处理视觉和指令输入。
    *   引入 **ChromaDB** 向量数据库（RAG），根据当前场景检索相似的经验案例，输出物理参数：速度限制 ($v_{max}$)、安全距离 ($d_{safe}$) 以及阻抗控制的刚度 ($K_i$) 和阻尼 ($D_i$)。
*   **第二层：全身模型预测控制 (Whole-Body MPC)**
    *   **状态表示**：整合底盘位姿和双臂末端执行器位置。
    *   **动力学与代价函数**：采用单轮车模型，代价函数包含目标达成、平滑度以及基于**人工势场法 (APF)** 的障碍物规避项。
    *   **优化**：以 10Hz 频率运行，在 7 个预测步长内求解最优轨迹。
*   **第三层：统一阻抗-导纳控制层**
    *   以 50Hz 频率运行，将 MPC 的参考轨迹转化为顺应性动作。
    *   对底盘和双臂采用统一的虚拟弹簧-阻尼公式，使机器人在接触或靠近人类时表现出柔顺性。

### 3. 实验设计
*   **实验场景**：
    1.  **人类感知导航**：在仓库环境中绕过人类障碍物。
    2.  **人手避障操作**：在双臂执行任务时，动态避开进入工作空间的人手。
*   **实验平台**：
    *   **仿真**：MuJoCo（用于控制器开发）和 IsaacSim（用于光照实感的传感器验证）。
    *   **实物**：搭载 NVIDIA Jetson Orin Nano 的差速驱动底盘 + 两个 6-DoF 机械臂，配备 LiDAR 和 RGB-D 相机。
*   **对比与基准**：主要对比了仿真环境与真实硬件在相同算法下的表现（Sim-to-Real），并验证了 VLM 介入前后机器人速度和顺应性的变化。

### 4. 资源与算力
*   **计算平台**：所有计算均在车载边缘计算节点 **NVIDIA Jetson Orin Nano (16 GB)** 上运行。
*   **模型优化**：VLM 模型经过 **INT4 量化**以适应边缘端推理。
*   **训练时长**：文中未提及模型训练时长，因为其核心思想是利用预训练的 VLM 和基于经验检索的 RAG，强调**无需重新训练**（retraining-free）即可实现参数接地。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真中进行了多次导航和操作实验，给出了位置误差（7.8mm）和角度误差（1.4°）的量化数据。
    *   实物验证涵盖了从 (0,0) 到 (2.0, -1.0)m 的导航任务。
*   **充分性评价**：
    *   **客观性**：实验涵盖了从仿真到实物的迁移，并诚实地记录了由于 Modbus 通信延迟导致的性能下降（误差从 7.8mm 升至 17.5mm）。
    *   **局限性**：实验场景相对单一（主要是仓库和实验室环境），RAG 数据库初始仅接种了 5 个示例，样本多样性有待提高。

### 6. 主要结论与发现
*   **语义到物理的成功映射**：VLM-RAG 能有效根据场景调整物理参数，例如在检测到人类时，机器人自动**减速 60%**。
*   **高精度控制**：在复杂约束下，仿真中的末端执行器误差保持在 2mm 以下。
*   **Sim-to-Real 挑战**：实物部署的主要瓶颈在于硬件通信延迟（30-60ms），而非算法逻辑本身，通过系统性降低增益可以抵消部分影响。

### 7. 优点
*   **可解释性强**：相比纯端到端模型，该框架输出的是明确的物理参数，便于安全审计。
*   **无需微调**：通过 RAG 机制，机器人可以学习新经验而无需昂贵的模型重训。
*   **统一性**：将阻抗控制同时应用于底盘导航和机械臂操作，简化了控制逻辑。

### 8. 不足与局限
*   **推理延迟**：尽管使用了量化模型，但 VLM 的推理速度仍可能限制系统对突发状况的实时响应。
*   **数据库依赖**：系统的表现高度依赖于 ChromaDB 中存储的经验质量，若遇到全新场景，RAG 的泛化能力可能受限。
*   **硬件瓶颈**：实物实验受限于 Modbus 协议的延迟，未能完全发挥 MPC 在高频控制下的潜力。

（完）
