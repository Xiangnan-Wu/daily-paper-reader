---
title: "Hi-WM: Human-in-the-World-Model for Scalable Robot Post-Training"
title_zh: Hi-WM：用于可扩展机器人后训练的世界模型中人（Human-in-the-World-Model）框架
authors: "Yaxuan Li, Zhongyi Zhou, Yefei Chen, Yanjiang Guo, Jiaming Liu, Shanghang Zhang, Jianyu Chen, Yichen Zhu"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21741v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人后期训练和策略改进的动作条件世界模型
tldr: 针对机器人后训练中人工干预依赖物理环境、效率低下的问题，本文提出Hi-WM框架。该框架利用动作条件世界模型作为交互式修正平台，让机器人在虚拟模型中闭环运行，人类在模型预测出错时直接介入并提供修正动作。Hi-WM支持状态回滚和多分支采样，能高效生成针对性失败案例的修正数据。实验表明，该方法显著提升了机器人在真实环境中的任务成功率，证明了世界模型可作为可扩展的机器人训练修正基座。
selection_source: fresh_fetch
motivation: 传统的机器人人工干预训练受限于物理执行的低效和场景重置的繁琐，亟需一种脱离物理环境的可扩展修正方案。
method: 提出Hi-WM框架，通过在世界模型中进行闭环策略推演，并允许人类在虚拟环境中对失败状态进行实时修正、回滚和多分支采样。
result: "在三项真实世界任务中，Hi-WM使策略成功率平均提升37.9%，且世界模型评估结果与真实表现高度相关（r=0.953）。"
conclusion: 世界模型不仅能用于生成或评估，更可作为高效的修正基座，通过人类在环的方式实现机器人策略的大规模可扩展后训练。
---

## 摘要
后训练对于将预训练的通用机器人策略转化为可靠的任务特定控制器至关重要，但现有的人机回环（human-in-the-loop）流程仍受限于物理执行：每次纠正都需要在现实世界中耗费机器人时间、场景搭建、重置以及操作员监督。与此同时，动作条件世界模型（action-conditioned world models）的研究主要集中在想象、合成数据生成和策略评估方面。我们提出了 Hi-WM（Human-in-the-World-Model），这是一种后训练框架，它将学习到的世界模型作为可重复使用的纠错基质，用于针对失败情况的策略改进。策略首先在世界模型内部进行闭环展开（rollout）；当展开过程出现错误或容易失败时，人类直接在模型中进行干预，提供简短的纠正动作。Hi-WM 缓存中间状态并支持回滚和分支，允许将单个失败状态重复用于多次纠正延续，从而针对基础策略处理不佳的行为产生密集的监督信号。生成的纠正轨迹随后被添加回训练集进行后训练。我们在涵盖刚性和柔性物体交互的三个真实世界操作任务以及两个策略骨干网络上对 Hi-WM 进行了评估。Hi-WM 在真实世界中的成功率比基础策略平均提高了 37.9 个百分点，比世界模型闭环基准提高了 19.0 个百分点，同时世界模型评估与真实世界表现高度相关（r = 0.953）。这些结果表明，世界模型不仅可以作为生成器或评估器，还可以作为可扩展机器人后训练的有效纠错基质。

## Abstract
Post-training is essential for turning pretrained generalist robot policies into reliable task-specific controllers, but existing human-in-the-loop pipelines remain tied to physical execution: each correction requires robot time, scene setup, resets, and operator supervision in the real world. Meanwhile, action-conditioned world models have been studied mainly for imagination, synthetic data generation, and policy evaluation. We propose \textbf{Human-in-the-World-Model (Hi-WM)}, a post-training framework that uses a learned world model as a reusable corrective substrate for failure-targeted policy improvement. A policy is first rolled out in closed loop inside the world model; when the rollout becomes incorrect or failure-prone, a human intervenes directly in the model to provide short corrective actions. Hi-WM caches intermediate states and supports rollback and branching, allowing a single failure state to be reused for multiple corrective continuations and yielding dense supervision around behaviors that the base policy handles poorly. The resulting corrective trajectories are then added back to the training set for post-training. We evaluate Hi-WM on three real-world manipulation tasks spanning both rigid and deformable object interaction, and on two policy backbones. Hi-WM improves real-world success by 37.9 points on average over the base policy and by 19.0 points over a world-model closed-loop baseline, while world-model evaluation correlates strongly with real-world performance (r = 0.953). These results suggest that world models can serve not only as generators or evaluators, but also as effective corrective substrates for scalable robot post-training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hi-WM: Human-in-the-World-Model for Scalable Robot Post-Training》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何解决机器人后训练（Post-training）中人工干预成本高昂且难以扩展的问题。
*   **研究动机**：虽然预训练的通用策略已有进展，但在特定任务中仍需通过人工纠错（Human-in-the-loop）来提升可靠性。传统的纠错流程绑定在物理实体上，每次干预都需要占用真实机器人、手动重置场景并耗费大量操作时间。
*   **核心理念**：将人工干预从“物理世界”转移到“动作条件世界模型（World Model）”中。利用世界模型作为可交互的虚拟空间，实现低成本、可扩展的策略修正。

### 2. 方法论
Hi-WM 框架主要包含以下核心技术环节：
*   **交互式世界模型构建**：
    *   采用视觉编码器、动作条件潜空间动力学模型和视觉解码器。
    *   **高维动作条件**：支持 14 维连续动作空间（双臂 6-DoF 位姿 + 夹持器状态），以满足复杂操作需求。
    *   **失败数据增强**：在训练世界模型时加入失败案例和边缘状态（Edge-case），防止模型盲目预测成功，确保其能真实反映错误动作的后果。
*   **状态缓存与回滚分支机制**：
    *   在虚拟模型中推演时，系统缓存中间状态。
    *   当策略出错时，人类操作员可将状态“回滚”到失败前的一刻，并尝试不同的修正路径（Branching），从而在同一个失败点生成多条高质量的修正轨迹。
*   **硬件无关的干预接口**：支持键盘、VR 控制器或主从机械臂等多种输入设备，将人类指令映射到策略的动作空间中。
*   **后训练流程**：将世界模型中收集的修正轨迹与原始数据合并，通过模仿学习或强化学习对基础策略进行微调。

### 3. 实验设计
*   **实验场景**：涵盖了三类具有挑战性的真实世界任务：
    1.  **折叠毛巾（Fold Towel）**：涉及柔性物体操作和关键点定位。
    2.  **Push-T**：涉及刚性物体的精确位姿调整。
    3.  **绕绳索（Route Rope）**：涉及长程、大形变的柔性物体操作。
*   **对比基准（Baselines）**：
    *   **Base Policy**：仅在原始离线数据集上训练的策略。
    *   **World Model Closed-Loop (WM-CL)**：在世界模型中自动生成成功轨迹进行微调（无人工干预）。
*   **策略骨干**：测试了 Diffusion Policy (DP) 和 $\pi_0$ 两种主流架构。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **成本分析**：文中提到了经济成本模型，对比了物理干预与虚拟干预在设备部署、人力和场地上的开销，指出 Hi-WM 在大规模场景下可节省近 10 万美元的成本。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个任务和 2 种策略架构上进行了交叉验证。
*   **充分性**：
    *   **对齐性实验**：验证了世界模型预测与真实物理执行的一致性（r=0.953）。
    *   **消融实验**：研究了边缘数据量对模型保真度的影响。
    *   **扩展性实验**：测试了随着虚拟干预数据量增加，策略性能的单调提升趋势。
    *   **泛化性实验**：在未见过的背景、干扰物和外观变化下测试了策略的鲁棒性。
*   **评价**：实验设计较为全面，不仅关注成功率，还深入探讨了成本效益和模型可靠性，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：Hi-WM 使策略在真实世界中的成功率比基础策略平均提高 **37.9%**，比无人工干预的自动微调提高 **19.0%**。
*   **高相关性**：世界模型中的评估结果与真实世界表现高度相关，证明了其作为“虚拟评估器”的有效性。
*   **经济性**：随着任务复杂度和场景数量增加，Hi-WM 的边际成本远低于物理世界干预。
*   **修正价值**：实验证明，针对失败状态的“针对性修正”比单纯增加成功演示数据对策略改进更有帮助。

### 7. 优点
*   **高效扩展性**：通过回滚和分支机制，极大地提高了人工干预的产出效率（一处失败，多条修正）。
*   **解耦硬件**：实现了远程、并行的干预数据收集，无需占用昂贵的机器人硬件。
*   **真实感对齐**：通过引入失败数据和边缘案例训练世界模型，解决了以往模型容易“幻觉成功”的问题。

### 8. 不足与局限
*   **模型依赖性**：后训练的效果高度依赖于世界模型的保真度。如果任务涉及极其复杂的动力学（如流体或高速碰撞），世界模型可能难以准确模拟。
*   **人工参与**：虽然效率提升，但仍需要人类进行实时干预，尚未实现完全自动化的策略进化。
*   **Sim-to-Real 风险**：尽管相关性高，但在极端精细的操作中，虚拟修正轨迹与物理真实反馈之间仍可能存在微小偏差。

（完）
