---
title: "CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors"
title_zh: CorridorVLA：通过稀疏锚点为生成式动作头提供显式空间约束
authors: "Dachong Li, ZhuangZhuang Chen, Jin Zhang, Jianqiang Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21241v1"
tags: ["query:robot"]
score: 10.0
evidence: 具有显式空间约束的视觉-语言-动作模型
tldr: 针对视觉-语言-动作（VLA）模型中空间引导通常隐含在潜变量中的问题，本文提出CorridorVLA。该方法通过预测稀疏空间锚点（如增量位置变化）来构建显式的物理约束区域（走廊），并利用流匹配动作头进行轨迹生成。当预测轨迹偏离走廊时，模型会受到修正梯度。在LIBERO-Plus基准测试中，该方法显著提升了SmolVLA和GR00T的成功率，证明了显式物理线索对生成式动作策略的有效约束作用。
selection_source: fresh_fetch
motivation: 现有的VLA模型往往缺乏显式的空间约束，导致动作生成在复杂物理环境下不够精准。
method: 提出预测稀疏空间锚点以定义“走廊”约束区域，并结合流匹配动作头，对超出该区域的轨迹施加修正梯度。
result: "在LIBERO-Plus基准上，CorridorVLA相比基线提升了3.4%-12.4%的成功率，其中GR00T-Corr变体达到了83.21%的成功率。"
conclusion: 显式的物理空间约束能有效补充视觉和潜变量引导，提升生成式动作策略的性能和可解释性。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用中间表示来连接多模态输入与连续控制，然而空间引导往往是通过潜特征隐式注入的。我们提出了 CorridorVLA，它将稀疏空间锚点预测为增量物理变化（例如 Δ-位置），并利用这些锚点在动作生成的训练目标中施加显式的容差区域。这些锚点定义了一个引导流匹配动作头的“走廊”：当轨迹所隐含的空间演化超出该走廊时，会接收到修正梯度，而接触产生的微小偏差和执行噪声则是允许的。在更具挑战性的 LIBERO-Plus 基准测试中，CorridorVLA 在 SmolVLA 和 GR00T 上均取得了持续的性能提升，相比相应基线，成功率提高了 3.4%–12.4%；值得注意的是，我们的 GR00T-Corr 变体达到了 83.21% 的成功率。这些结果表明，与动作对齐的物理线索可以为生成式动作策略提供直接且可解释的约束，从而补充了以视觉或潜形式编码的空间引导。代码可在 https://github.com/corridorVLA 获取。

## Abstract
Vision--Language--Action (VLA) models often use intermediate representations to connect multimodal inputs with continuous control, yet spatial guidance is often injected implicitly through latent features. We propose $CorridorVLA$, which predicts sparse spatial anchors as incremental physical changes (e.g., $Δ$-positions) and uses them to impose an explicit tolerance region in the training objective for action generation. The anchors define a corridor that guides a flow-matching action head: trajectories whose implied spatial evolution falls outside it receive corrective gradients, while minor deviations from contacts and execution noise are permitted. On the more challenging LIBERO-Plus benchmark, CorridorVLA yields consistent gains across both SmolVLA and GR00T, improving success rate by $3.4\%$--$12.4\%$ over the corresponding baselines; notably, our GR00T-Corr variant reaches a success rate of $83.21\%$. These results indicate that action-aligned physical cues can provide direct and interpretable constraints for generative action policies, complementing spatial guidance encoded in visual or latent forms. Code is available at https://github.com/corridorVLA.

---

## 论文详细总结（自动生成）

### CorridorVLA：通过稀疏锚点为生成式动作头提供显式空间约束 —— 论文总结

#### 1. 核心问题与研究动机
现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）在连接多模态输入与连续控制时，通常将空间引导信息（如物体位置、移动方向）隐式地编码在潜变量（Latent Features）中。这种方式缺乏直观的物理约束，导致模型在复杂任务中生成的动作轨迹可能偏离物理常识。
**研究动机：** 探索是否可以将空间引导表达为与动作空间紧密对齐的、显式的“文本风格”物理量（如末端执行器的位移），并利用这些量在损失函数层面为动作生成提供直接约束。

#### 2. 方法论
CorridorVLA 的核心思想是预测未来的“稀疏空间锚点”，并以此构建一个“容差走廊（Corridor）”来约束动作轨迹。
*   **稀疏锚点预测：** 在 VLM 主干网络中引入可学习的槽位（Slots），预测未来 $K$ 个关键时间点的末端执行器（EE）增量位置（$\Delta$-positions）。
*   **动作空间扩展（extra-A）：** 将动作向量从单纯的控制指令扩展为包含对应位移项的增强向量，使动作头监督信号与空间锚点对齐。
*   **走廊约束目标：**
    *   **走廊缓冲损失（Buffer Loss）：** 定义一个容差区域，当生成的轨迹偏离锚点超过阈值时，施加修正梯度；微小偏差（如接触力导致的波动）则被允许。
    *   **走廊内一致性（In-corridor Consistency）：** 在轨迹进入容差区后，通过阶段性累积进度损失进一步精细化轨迹。
*   **噪声感知加权：** 结合流匹配（Flow Matching）技术，在去噪过程的后期（噪声较小时）加大空间约束的权重，确保最终输出的物理合理性。

#### 3. 实验设计
*   **数据集与基准：** 主要在 **LIBERO** 和更具挑战性的 **LIBERO-Plus** 基准上进行评估。任务涵盖长程任务（Long）、目标导向（Goal）、物体操作（Object）和空间推理（Spatial）。
*   **主干网络：** 选择了轻量级的 **SmolVLA (0.45B)** 和高性能的 **GR00T** 作为基础模型。
*   **对比方法：** 包括 $\pi_0$ (3.3B), OpenVLA, NORA, UniVLA, GraspVLA 等先进模型。
*   **评估指标：** 任务成功率（Success Rate）。

#### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。但提到使用了 LeRobot 框架和 StarVLA 的公开实现，且由于 SmolVLA 参数量较小（0.45B），该方法被描述为一种“负担得起且高效”的方案。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了多组 4-in-1 模型测试（即一个模型同时处理四类任务），并在两个不同的 VLA 架构（SmolVLA 和 GR00T）上验证了迁移性。
*   **消融实验：** 针对走廊损失组件（缓冲项 vs 一致性项）、锚点选择策略（RDP 算法 vs 均匀采样）、预测目标（绝对位置 vs 增量位置）以及主干网络交互方式进行了详尽的对比。
*   **评价：** 实验设计较为客观，通过在不同分辨率（256 vs 512）和不同基准下的表现，证明了该方法的鲁棒性。

#### 6. 主要结论与发现
*   **性能提升：** 在 LIBERO-Plus 上，CorridorVLA 显著提升了基线表现，SmolVLA 提升了 12.4%，GR00T 提升了 7.98%。
*   **最优变体：** GR00T-Corr 在 LIBERO-Plus 上达到了 **83.21%** 的成功率，处于领先水平。
*   **物理线索的有效性：** 实验证明，预测增量位置（$\Delta$-pos）比预测绝对位置更有效，因为增量信息更能体现控制的本质。
*   **显式约束的价值：** 即使不增加模型参数量，仅通过改进训练目标（损失函数），也能大幅增强生成式动作头的性能。

#### 7. 优点
*   **轻量且高效：** 仅需增加极少量的预测 Token（如 3 个），几乎不增加推理开销。
*   **可解释性强：** 空间锚点和走廊约束提供了直观的物理意义，便于理解模型为何产生特定动作。
*   **通用性：** 能够无缝集成到不同的 VLA 架构和流匹配动作头中。

#### 8. 不足与局限
*   **缺乏真机实验：** 所有的评估均在仿真环境（LIBERO）中完成，尚未在真实机器人上验证其对物理接触和传感器噪声的应对能力。
*   **对比范围有限：** 未能与基于图像或视频中间表示的方法（如 ReconVLA）进行直接的“头对头”对比，难以量化“文本风格锚点”与“视觉风格引导”的优劣。
*   **超参数敏感性：** 走廊宽度 $\delta$ 和噪声权重函数的选择可能需要针对不同机器人硬件进行精细调优。

（完）
