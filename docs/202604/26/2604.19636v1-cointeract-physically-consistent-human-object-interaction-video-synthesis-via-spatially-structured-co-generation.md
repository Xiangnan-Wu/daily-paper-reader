---
title: "CoInteract: Physically-Consistent Human-Object Interaction Video Synthesis via Spatially-Structured Co-Generation"
title_zh: CoInteract：通过空间结构化协同生成实现物理一致的人与物体交互视频合成
authors: "Xiangyang Luo, Xiaozhe Xin, Tao Feng, Xu Guo, Meiguang Jin, Junfeng Ma"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19636v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 通过扩散模型合成物理一致的人机交互视频
tldr: CoInteract是一个针对人机交互（HOI）视频生成的端到端框架，旨在解决现有扩散模型在手部/面部结构稳定性和物理接触合理性方面的不足。该方法基于Diffusion Transformer架构，引入了人类感知混合专家（MoE）机制以提升细粒度结构保真度，并采用空间结构协同生成范式，通过RGB与HOI结构双流训练注入几何先验。实验证明，该方法在结构稳定性、逻辑一致性和交互真实感上显著优于现有技术。
selection_source: fresh_fetch
motivation: 现有扩散模型在生成人机交互视频时，难以维持手部和面部的结构稳定性，且常出现违反物理规律的穿模现象。
method: 引入人类感知混合专家（MoE）进行细粒度结构优化，并结合RGB与HOI结构双流协同生成范式来注入交互几何先验。
result: 实验表明CoInteract在结构稳定性、逻辑一致性和交互真实感等多个维度上均显著优于现有的视频生成方法。
conclusion: 通过空间结构化的协同生成设计，CoInteract有效解决了人机交互视频中的物理一致性难题，提升了生成视频的实用价值。
---

## 摘要
合成人与物体交互（HOI）视频在电子商务、数字广告和虚拟营销中具有广泛的实际应用价值。然而，现有的扩散模型尽管具备照片级的渲染能力，但在以下方面仍经常失效：（i）手部和面部等敏感区域的结构稳定性，以及（ii）物理上合理的接触（例如，避免手部与物体的穿透）。我们提出了 CoInteract，这是一个以人物参考图像、产品参考图像、文本提示和语音音频为条件的端到端 HOI 视频合成框架。CoInteract 在 Diffusion Transformer (DiT) 骨干网络中引入了两种互补的设计。首先，我们提出了一种人体感知混合专家模型（Human-Aware MoE），通过空间监督路由将 token 分发给轻量级的区域专用专家，从而以极小的参数开销提高细粒度的结构保真度。其次，我们提出了空间结构化协同生成，这是一种双流训练范式，通过联合建模 RGB 外观流和辅助 HOI 结构流来注入交互几何先验。在训练过程中，HOI 流关注 RGB token，其监督信号用于正则化共享的骨干权重；在推理阶段，HOI 分支被移除，以实现零开销的 RGB 生成。实验结果表明，CoInteract 在结构稳定性、逻辑一致性和交互真实感方面显著优于现有方法。

## Abstract
Synthesizing human--object interaction (HOI) videos has broad practical value in e-commerce, digital advertising, and virtual marketing. However, current diffusion models, despite their photorealistic rendering capability, still frequently fail on (i) the structural stability of sensitive regions such as hands and faces and (ii) physically plausible contact (e.g., avoiding hand--object interpenetration). We present CoInteract, an end-to-end framework for HOI video synthesis conditioned on a person reference image, a product reference image, text prompts, and speech audio. CoInteract introduces two complementary designs embedded into a Diffusion Transformer (DiT) backbone. First, we propose a Human-Aware Mixture-of-Experts (MoE) that routes tokens to lightweight, region-specialized experts via spatially supervised routing, improving fine-grained structural fidelity with minimal parameter overhead. Second, we propose Spatially-Structured Co-Generation, a dual-stream training paradigm that jointly models an RGB appearance stream and an auxiliary HOI structure stream to inject interaction geometry priors. During training, the HOI stream attends to RGB tokens and its supervision regularizes shared backbone weights; at inference, the HOI branch is removed for zero-overhead RGB generation. Experimental results demonstrate that CoInteract significantly outperforms existing methods in structural stability, logical consistency, and interaction realism.

---

## 论文详细总结（自动生成）

# CoInteract: 通过空间结构化协同生成实现物理一致的人与物体交互视频合成

## 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**语音驱动的人与物体交互（HOI）视频合成**。尽管当前的扩散模型在生成照片级图像方面表现出色，但在复杂的交互场景中存在两个核心痛点：
*   **结构不稳定性**：手部和面部等敏感区域容易出现指头融合、五官模糊等结构性崩溃。
*   **物理违和感**：由于缺乏 3D 空间意识，生成的视频常出现“穿模”现象（如手部穿过物体表面），缺乏物理上的接触合理性。
研究动机在于将人类结构先验和交互几何约束直接嵌入到 Diffusion Transformer (DiT) 骨干网络中，使模型从单纯的“像素生成器”转变为“结构感知的交互引擎”。

## 2. 方法论
CoInteract 提出了一个端到端的框架，核心包含以下关键技术：

*   **空间结构化协同生成（Spatially-Structured Co-Generation）**：
    *   **双流训练**：同时训练 RGB 外观流和辅助的 HOI 结构流（去纹理的 3 通道渲染，包含人体网格投影和物体掩码）。
    *   **异步协同注意力（Asymmetric Co-Attention）**：在训练的第二阶段，RGB 流仅关注自身，而 HOI 流关注 RGB 和自身。这种设计允许在推理时直接移除 HOI 分支，实现**零额外开销**的生成，同时在训练时通过共享参数将几何约束注入 RGB 生成器。
*   **人体感知混合专家（Human-Aware MoE）**：
    *   在 DiT 块中引入轻量级专家（头部、手部、基础专家）。
    *   **空间监督路由**：利用面部和手部的边界框（Bbox）作为监督信号，引导路由算法将特定区域的 token 分发给专门的专家处理，提升细粒度结构的保真度。
*   **3D RoPE 坐标分配**：
    *   为历史运动帧、参考图像、RGB 流和 HOI 流分配统一的 3D 时空坐标，通过相对位置编码增强模型对时空因果性和跨流对齐的理解。

## 3. 实验设计
*   **数据集**：构建了一个大规模 HOI 视频数据集，包含 40 小时的产品演示和直播视频，过滤后保留 12,000 个高质量剪辑，并配对 RGB-HOI 表示及手/脸 Bbox。
*   **Benchmark 与对比方法**：
    *   对比了 **AnchorCrafter, Phantom, Humo, VACE, InteractAvatar, SkyReels-V3** 等 6 种先进方法。
    *   **评估指标**：涵盖视频质量（AES, IQ, Smooth）、交互质量（VLM-QA, 手部质量 HQ）、参考一致性（DINO, FaceSim）和音画同步（Sync conf）。
*   **用户研究**：邀请 24 名评估者对物体一致性、人/景一致性和交互合理性进行盲测排序。

## 4. 资源与算力
*   **初始化**：模型初始化自 WanS2V。
*   **训练细节**：采用两阶段训练。第一阶段（全双向注意力）进行 5,000 次迭代；第二阶段（异步协同注意力）进行 2,000 次迭代。
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到了使用 AdamW 优化器和 $1 \times 10^{-4}$ 的学习率。

## 5. 实验数量与充分性
*   **实验规模**：进行了定量对比、定性视觉对比、消融实验以及用户主观研究。
*   **消融实验**：系统地验证了 MoE 模块、协同生成范式以及异步掩码设计的必要性。
*   **充分性评价**：实验设计较为全面，特别是引入了基于 Gemini-3-Pro 的 VLM-QA 评估方法，尝试解决 HOI 领域缺乏标准化自动评估指标的问题。对比方法涵盖了当前主流的多种生成范式，实验结果具有较强的说服力。

## 6. 主要结论与发现
*   **协同生成的有效性**：引入辅助结构流能显著降低穿模现象，VLM-QA 指标提升了约 33.3%。
*   **MoE 的精准性**：空间监督路由能准确识别手部和面部 token，显著提升了手部关键点的检测置信度（HQ 指标）。
*   **推理效率**：通过异步注意力设计，模型在保持高性能的同时，推理成本仅为 MoE 带来的微小增量（约 1.04 倍），远低于双流同时推理的成本（约 4.13 倍）。

## 7. 优点
*   **物理一致性强**：通过几何先验注入，有效解决了视频生成中的物理穿透难题。
*   **端到端设计**：无需复杂的推理时预处理或后期增强插件，使用便捷。
*   **推理零开销**：巧妙的注意力掩码设计使得辅助分支在推理时可舍弃，不增加计算负担。
*   **结构保真度高**：MoE 机制针对性地解决了手部和面部畸变问题。

## 8. 不足与局限
*   **数据依赖性**：训练数据的 HOI 结构流依赖于 SAM3 和 SAM3D-body 等预处理工具，若底层工具提取的网格或掩码不准，会影响模型学习效果。
*   **场景覆盖**：虽然数据集在 HOI 领域算大，但 12K 剪辑相比通用视频大模型仍显不足，对于极端罕见的交互动作可能泛化受限。
*   **算力细节缺失**：未公布具体算力消耗，不利于其他研究者评估复现成本。

（完）
