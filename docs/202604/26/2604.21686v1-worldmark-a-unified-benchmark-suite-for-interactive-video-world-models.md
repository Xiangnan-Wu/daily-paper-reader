---
title: "WorldMark: A Unified Benchmark Suite for Interactive Video World Models"
title_zh: WorldMark：交互式视频世界模型的统一基准测试套件
authors: "Xiaojie Xu, Zhengyuan Lin, Kang He, Yukang Feng, Xiaofeng Mao, Yuanyang Yin, Kaipeng Zhang, Yongtao Ge"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21686v1"
tags: ["query:q15"]
score: 10.0
evidence: 交互式视频世界模型的统一基准测试套件
tldr: 针对交互式视频生成模型缺乏统一评估标准的问题，本文推出了首个统一基准测试集WorldMark。它通过统一的动作映射层实现了不同模型在相同场景和轨迹下的公平对比，包含500个涵盖多种视角和难度的测试案例，并提供模块化评估工具及在线竞技场平台，旨在推动交互式世界模型的标准化评估与发展。
selection_source: fresh_fetch
motivation: 现有交互式视频模型因使用私有场景和不同控制接口，导致缺乏统一的测试条件，难以进行公平的跨模型性能比较。
method: 开发了统一动作映射层以适配不同模型的控制格式，并构建了包含500个分级案例的测试集及涵盖视觉质量、控制对齐和世界一致性的评估工具。
result: 实现了对六个主流交互式视频模型的标准化对比，并建立了在线竞技场平台World Model Arena供用户进行实时对战评估。
conclusion: WorldMark为交互式世界模型提供了首个标准化的公平竞争平台，通过开源数据和代码促进了该领域的透明化评估与技术进步。
---

## 摘要
Genie、YUME、HY-World 和 Matrix-Game 等交互式视频生成模型正在飞速发展，但目前每个模型都在各自的基准测试上使用私有场景和轨迹进行评估，这使得跨模型的公平比较变得不可能。现有的公开基准测试虽然提供了轨迹误差、美学评分和基于视觉语言模型（VLM）的判断等有用指标，但均未能提供标准化的测试条件——即相同的场景、相同的动作序列和统一的控制接口——而这些是使具有异构输入的模型之间的指标具有可比性所必需的。我们推出了 WorldMark，这是第一个为交互式图像到视频（Image-to-Video）世界模型提供此类统一竞争平台的基准测试。WorldMark 的贡献包括：(1) 一个统一的动作映射层，将共享的 WASD 风格动作词汇表转换为每个模型的原生控制格式，从而实现在相同场景和轨迹下对六个主流模型进行公平的同类比较；(2) 一个包含 500 个评估案例的分层测试套件，涵盖了第一和第三人称视角、写实和风格化场景，以及从简单到困难、时长跨度为 20-60 秒的三个难度等级；(3) 一个用于视觉质量、控制对齐和世界一致性的模块化评估工具包，旨在让研究人员在复用标准化输入的同时，能够随着领域的发展灵活接入自定义指标。我们将发布所有数据、评估代码和模型输出以促进未来研究。除了离线指标外，我们还推出了 World Model Arena (warena.ai)，这是一个在线平台，任何人都可以让领先的世界模型进行并排对战并查看实时排行榜。

## Abstract
Interactive video generation models such as Genie, YUME, HY-World, and Matrix-Game are advancing rapidly, yet every model is evaluated on its own benchmark with private scenes and trajectories, making fair cross-model comparison impossible. Existing public benchmarks offer useful metrics such as trajectory error, aesthetic scores, and VLM-based judgments, but none supplies the standardized test conditions -- identical scenes, identical action sequences, and a unified control interface -- needed to make those metrics comparable across models with heterogeneous inputs. We introduce WorldMark, the first benchmark that provides such a common playing field for interactive Image-to-Video world models. WorldMark contributes: (1) a unified action-mapping layer that translates a shared WASD-style action vocabulary into each model's native control format, enabling apples-to-apples comparison across six major models on identical scenes and trajectories; (2) a hierarchical test suite of 500 evaluation cases covering first- and third-person viewpoints, photorealistic and stylized scenes, and three difficulty tiers from Easy to Hard spanning 20-60s; and (3) a modular evaluation toolkit for Visual Quality, Control Alignment, and World Consistency, designed so that researchers can reuse our standardized inputs while plugging in their own metrics as the field evolves. We will release all data, evaluation code, and model outputs to facilitate future research. Beyond offline metrics, we launch World Model Arena (warena.ai), an online platform where anyone can pit leading world models against each other in side-by-side battles and watch the live leaderboard.

---

## 论文详细总结（自动生成）

这是一份关于论文《WorldMark: A Unified Benchmark Suite for Interactive Video World Models》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：交互式视频生成模型（如 Genie, YUME, Matrix-Game 等）发展迅速，但评估标准极度碎片化。每个模型通常在私有场景和特定动作轨迹上进行测试，导致无法进行公平的跨模型对比。
*   **核心问题**：现有的视频评估基准（如 VBench）缺乏“动作控制”维度，而支持轨迹控制的基准（如 WorldScore）又无法适配基于键盘输入的交互模型。
*   **整体含义**：WorldMark 旨在建立首个统一的“竞技场”，通过标准化的场景、动作序列和控制接口，实现对不同交互式世界模型的公平性能度量。

### 2. 论文提出的方法论
WorldMark 的核心由三个支柱组成：
*   **统一动作映射层 (Unified Action Interface)**：
    *   定义了一套通用的 WASD（移动）+ L/R（偏航旋转）动作词汇表。
    *   开发了**动作映射适配器**，将通用指令翻译成各模型的原生格式：例如将“向前”翻译为 YUME 的文本提示词、HY-World 的 6-DoF 位姿参数或 Genie 3 的手柄信号。
*   **分层测试套件 (Hierarchical Test Suite)**：
    *   包含 500 个测试案例，分为三个难度等级：**简单**（单段动作，20s）、**中等**（两段组合，40s）、**困难**（三段复杂路径或 360° 旋转，60s）。
    *   引入 **VLM 场景感知过滤**：利用视觉语言模型分析初始图像，自动剔除物理上不合理的动作（如对着墙壁持续侧移）。
*   **多维度评估指标**：
    *   **视觉质量**：美学评分（LAION）和成像质量（MUSIQ）。
    *   **控制对齐**：利用 DROID-SLAM 重建相机轨迹，计算预测与真实的平移误差和旋转误差。
    *   **世界一致性**：结合几何重投影误差（Reprojection Error）和 VLM 评分（状态、内容、风格的一致性）。

### 3. 实验设计
*   **数据集/场景**：
    *   **图像套件**：精选 50 张参考图，涵盖自然、城市、室内场景，分为写实和风格化（如油画、赛博朋克、我的世界）两类。
    *   **视角**：每张图生成对应的第一人称和第三人称视角，共 100 张基础图。
*   **对比方法**：对比了 6 个主流模型：YUME 1.5, Matrix-Game 2.0, HY-World 1.5, HY-GameCraft, Open-Oasis, 以及闭源的 Google Genie 3。
*   **Benchmark 构成**：500 个标准化的“图像-动作”对。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的训练或推理 GPU 型号及总时长。
*   **相关提及**：文中提到当前交互式模型的推理成本较高，因此将图像套件规模控制在 50 张以保证实用性。评估过程中使用了 Gemini-3.1-Pro 作为 VLM 评分器。

### 5. 实验数量与充分性
*   **实验规模**：共进行了约 500 组标准化实验，涵盖了不同视角（1st/3rd person）和不同风格（Real/Stylized）的交叉验证。
*   **客观性与公平性**：
    *   **公平性**：所有模型在完全相同的初始图像和语义一致的动作序列下运行，消除了因测试场景不同带来的偏差。
    *   **充分性**：除了自动化指标，还进行了**人类偏好对齐研究**（20 名志愿者对 50 组视频排序），结果显示自动化指标与人类判断的 Spearman 秩相关系数 $\rho > 0.9$，证明了评估体系的有效性。

### 6. 论文的主要结论与发现
*   **质量与一致性脱节**：视觉质量（好看）与世界一致性（逻辑正确）并不正相关。例如 YUME 1.5 画面最美，但长程一致性较差；Genie 3 画面一般但世界逻辑最稳。
*   **第三人称挑战**：所有模型在第三人称视角下的表现均大幅下滑，旋转误差比第一人称高出近一个数量级，显示出角色与相机协同控制的巨大难度。
*   **领域迁移失败**：在特定领域（如 Minecraft）训练的模型（Open-Oasis）在通用场景下几乎完全失效。
*   **控制不代表质量**：某些模型（如 HY-Game）虽然能精准执行动作指令，但牺牲了画面的视觉保真度。

### 7. 优点
*   **标准化突破**：首次解决了交互式模型因输入格式异构（文本 vs 位姿 vs 向量）而无法对比的难题。
*   **模块化设计**：评估工具包是插件式的，研究者可以复用其标准化输入，同时接入未来更先进的评估指标。
*   **社区贡献**：不仅提供离线基准，还推出了 **World Model Arena** 在线竞技场，促进了该领域的透明化竞争。

### 8. 不足与局限
*   **样本规模限制**：50 张参考图虽然多样，但对于全面覆盖广阔的现实世界可能仍显不足。
*   **几何指标依赖**：控制对齐高度依赖 SLAM 算法，在低纹理或极度模糊的生成视频中，SLAM 可能失效导致评分偏差。
*   **VLM 偏见风险**：一致性评分依赖于特定的 VLM（如 Gemini），可能存在模型固有的审美或逻辑偏见。
*   **动态物体缺失**：目前的动作序列主要侧重于相机控制，对场景中复杂动态物体（如行人、车辆）的交互评估较少。

（完）
