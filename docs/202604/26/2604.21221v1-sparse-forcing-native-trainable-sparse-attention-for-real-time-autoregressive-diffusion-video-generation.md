---
title: "Sparse Forcing: Native Trainable Sparse Attention for Real-time Autoregressive Diffusion Video Generation"
title_zh: Sparse Forcing：用于实时自回归扩散视频生成的原生可训练稀疏注意力
authors: "Boxun Xu, Yuming Du, Zichang Liu, Siyu Yang, Ziyang Jiang, Siqi Yan, Rajasi Saha, Albert Pumarola, Wenchen Wang, Peng Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21221v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 自回归扩散视频生成与时空记忆
tldr: 针对自回归视频扩散模型在长视频生成中的质量与延迟挑战，本文提出 Sparse Forcing 范式。该方法基于注意力集中于持久显著块的观察，设计了可训练的原生稀疏机制来动态压缩和更新 KV 缓存，并结合高效的 PBSA GPU 内核。实验证明，该方法在显著提升长视频生成质量的同时，大幅降低了显存占用并加快了推理速度，为实时长视频生成提供了高效方案。
selection_source: fresh_fetch
motivation: 发现自回归扩散模型中的注意力天然集中在少数持久的显著视觉块上，且在滑动窗口内呈现局部块稀疏模式。
method: 提出一种可训练的稀疏机制来动态选择和保留关键视觉块，并开发了 PBSA 高效内核以加速稀疏注意力计算。
result: "在长视频生成上 VBench 评分显著提升，推理速度最高提升 1.27 倍，且 KV 缓存峰值占用降低了 42%。"
conclusion: Sparse Forcing 通过原生可训练稀疏性，实现了高质量、低延迟且显存高效的实时长视频自回归生成。
---

## 摘要
我们引入了 Sparse Forcing，这是一种用于自回归视频扩散模型的训练与推理范式，在提高长时程生成质量的同时降低了解码延迟。Sparse Forcing 的灵感源于自回归扩散推演中的一个经验观察：注意力集中在显著视觉块的一个持久子集上，在 KV 缓存中形成了隐式的时空记忆，并在滑动窗口内表现出局部结构化的块稀疏模式。基于这一观察，我们提出了一种可训练的原生稀疏机制，该机制学习压缩、保留和更新这些持久块，同时将每个局部窗口内的计算限制在动态选择的局部邻域内。为了使该方法在训练和推理中都能大规模应用，我们进一步提出了持久块稀疏注意力（PBSA），这是一种高效的 GPU 内核，可加速稀疏注意力和内存更新，从而实现低延迟、内存高效的解码。实验表明，在 5 秒文本生成视频任务中，Sparse Forcing 相比 Self-Forcing 将 VBench 评分提高了 0.26，同时实现了 1.11-1.17 倍的解码加速，并将峰值 KV 缓存占用降低了 42%。在更长时程的推演中，这种增益更加显著，在 20 秒和 1 分钟的生成任务中，VBench 评分分别提高了 0.68 和 2.74，加速比分别达到 1.22 倍和 1.27 倍。

## Abstract
We introduce Sparse Forcing, a training-and-inference paradigm for autoregressive video diffusion models that improves long-horizon generation quality while reducing decoding latency. Sparse Forcing is motivated by an empirical observation in autoregressive diffusion rollouts: attention concentrates on a persistent subset of salient visual blocks, forming an implicit spatiotemporal memory in the KV cache, and exhibits a locally structured block-sparse pattern within sliding windows. Building on this observation, we propose a trainable native sparsity mechanism that learns to compress, preserve, and update these persistent blocks while restricting computation within each local window to a dynamically selected local neighborhood. To make the approach practical at scale for both training and inference, we further propose Persistent Block-Sparse Attention (PBSA), an efficient GPU kernel that accelerates sparse attention and memory updates for low-latency, memory-efficient decoding. Experiments show that Sparse Forcing improves the VBench score by +0.26 over Self-Forcing on 5-second text-to-video generation while delivering a 1.11-1.17x decoding speedup and 42% lower peak KV-cache footprint. The gains are more pronounced on longer-horizon rollouts, delivering improved visual quality with +0.68 and +2.74 VBench improvements, and 1.22x and 1.27x speedups on 20-second and 1-minute generations, respectively.

---

## 论文详细总结（自动生成）

以下是对论文《Sparse Forcing: Native Trainable Sparse Attention for Real-time Autoregressive Diffusion Video Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：自回归（AR）视频扩散模型在生成长视频时面临三大瓶颈：
    1.  **计算复杂度**：全序列注意力机制随长度呈二次方增长。
    2.  **显存压力**：KV Cache 占用巨大（1分钟视频可达 45GB 左右）。
    3.  **曝光偏差（Exposure Bias）**：推理时依赖自身生成的错误预测，导致误差随时间累积，出现色彩漂移和形变。
*   **研究动机**：作者观察到视频生成中的注意力具有天然的**时空持久性**（集中在少数关键视觉块）和**局部块稀疏性**（滑动窗口内仅需关注邻域）。基于此，论文旨在设计一种既能减少计算/显存开销，又能通过结构化稀疏性抑制误差传播的训练与推理范式。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出 **Sparse Forcing**，将 KV 缓存分解为“持久记忆（Persistent Memory）”和“局部窗口（Local Window）”，并引入原生可训练的稀疏注意力机制。
*   **关键技术细节**：
    1.  **隐式时空记忆分解**：将记忆 $M$ 分为持久块集合 $P$（捕捉全局语义、主体身份）和局部窗口 $L$（捕捉近期细节）。
    2.  **块压缩与评分（Blockified Compression）**：将潜在张量划分为时空块，通过池化生成压缩代表向量，用于计算块间的粗粒度相关性评分。
    3.  **动态更新机制**：
        *   **Top-C 持久化**：根据评分从局部窗口移出的块中筛选最重要的 $C$ 个块存入持久记忆。
        *   **Top-K 局部稀疏**：在局部窗口内，每行查询仅关注评分最高的 $K$ 个块。
    4.  **PBSA 内核（Persistent Block-Sparse Attention）**：基于 ThunderKittens 开发的定制化 GPU 内核，支持非规则的稀疏模式和持久块的跨步传递，实现端到端加速。
    5.  **因果蒸馏训练**：使用分布匹配蒸馏（DMD）损失，在训练中引入动态缓存更新和稀疏注意力，消除训练与推理之间的不一致性。

### 3. 实验设计
*   **基础模型**：以 Wan2.1-T2V-1.3B 为骨干网络。
*   **数据集/场景**：使用 VidProM 提示词库（经过 LLM 扩展和过滤），涵盖多种风格和复杂场景。
*   **Benchmark**：**VBench**（包含 16 个维度，涵盖语义对齐、感知质量、时空一致性等）。
*   **对比方法**：
    *   **自回归模型**：Self-Forcing, CausVid, SkyReels-V2, MAGI-1。
    *   **非自回归/双向模型**：LTX-Video, Wan2.1 原型。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   训练步数：1200 步。
    *   Batch Size：64。
    *   优化器：AdamW。
    *   训练时长：文中未明确给出总小时数，但提到 PBSA 内核显著提高了训练效率。

### 5. 实验数量与充分性
*   **实验规模**：生成了 4,730 个视频（946 个提示词 × 5 个样本）进行 VBench 评估。
*   **实验维度**：
    *   **长度扩展性**：对比了 5秒、20秒、1分钟三种长度。
    *   **消融实验**：分别验证了持久记忆（P）、局部块稀疏（BSA）和持续预训练（Cont. Pretrain）的作用。
    *   **内核性能**：对比了 PBSA 与 FlashAttention-2 在不同稀疏度下的加速比。
*   **充分性评价**：实验设计较为全面，不仅有量化指标，还有长视频生成的定性对比（视觉一致性分析），且对比了当前最前沿的开源自回归视频模型，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **质量提升**：在 1 分钟长视频生成上，VBench 评分比 Self-Forcing 提升了 **2.74**，显著改善了长时程的色彩一致性和物体稳定性。
*   **效率提升**：相比全注意力机制，实现了 **1.11x - 1.27x** 的解码加速，且 **KV Cache 峰值显存降低了 42%**。
*   **内核优势**：PBSA 内核在特定稀疏配置下比 FlashAttention-2 快 **1.16x 到 11.11x**。
*   **稀疏性的双重作用**：稀疏性不仅是计算优化手段，更是一种正则化形式，通过限制误差传播路径，提高了长视频生成的鲁棒性。

### 7. 优点
*   **原生可训练性**：不同于事后剪枝，该方法将稀疏性融入训练，使模型学会了如何利用有限的缓存。
*   **软硬件协同**：不仅提出了算法，还开发了高效的 PBSA 内核，解决了稀疏注意力在现代 GPU 上难以获得实际加速的痛点。
*   **长视频稳定性**：通过持久记忆机制，有效缓解了自回归模型常见的“漂移”问题。

### 8. 不足与局限
*   **模型覆盖面**：主要在 Wan2.1-1.3B 这一种架构上进行了验证，尚未展示在更大规模（如 10B+ 参数）或其他架构（如 U-Net）上的泛化能力。
*   **分辨率限制**：实验主要集中在特定分辨率（如 896x512），更高分辨率下的表现有待观察。
*   **潜在伪影**：在某些配置下，运动平滑度（Motion Smoothness）和闪烁（Flickering）指标略有下降，说明极度稀疏可能引入轻微的时域不连续。

（完）
