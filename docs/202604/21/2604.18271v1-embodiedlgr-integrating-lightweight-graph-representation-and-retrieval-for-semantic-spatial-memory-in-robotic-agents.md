---
title: "EmbodiedLGR: Integrating Lightweight Graph Representation and Retrieval for Semantic-Spatial Memory in Robotic Agents"
title_zh: EmbodiedLGR：为机器人智能体集成轻量级图表示与检索以实现语义-空间记忆
authors: "Paolo Riva, Leonardo Gargani, Matteo Frosi, Matteo Matteucci"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18271v1"
tags: ["query:robot"]
score: 9.0
evidence: 具有语义空间记忆的VLM驱动机器人代理，用于复杂环境
tldr: 针对机器人智能体在复杂环境中高效构建和检索语义空间记忆的需求，本文提出 EmbodiedLGR-Agent。该架构结合视觉语言模型（VLM），通过轻量级图表示存储物体位置等底层信息，并利用检索增强生成（RAG）保留高层场景描述。实验表明，该方法在 NaVQA 数据集上实现了领先的推理速度和极具竞争力的准确率，并成功在物理机器人上实现了本地化部署。
selection_source: fresh_fetch
motivation: 机器人需要一种既能提供精确语义空间信息，又能满足人类交互实时性要求的内存表示与检索机制。
method: 提出一种混合构建检索架构，利用参数高效的 VLM 将物体位置存入语义图，同时结合 RAG 架构处理高层场景描述。
result: 在 NaVQA 数据集上取得了最快的推理和查询速度，同时保持了与当前最先进方法相当的准确性。
conclusion: EmbodiedLGR-Agent 证明了轻量级图表示与 VLM 结合在提升机器人实时交互和环境理解能力方面的有效性。
---

## 摘要
随着应用于机器人的智能体人工智能领域的发展，对能够高效构建和检索记忆及观测结果的智能体的需求日益增长。在复杂环境中运行的机器人必须构建记忆结构，通过利用当前操作上下文的记忆表示来实现有效的机器人交互。与机器人交互的人们可能期望具身智能体提供有关位置、事件或物体的信息，这要求智能体在类人推理时间内提供精确答案，以被视为具有响应性。我们提出了具身轻量级图检索智能体（EmbodiedLGR-Agent），这是一种由视觉语言模型（VLM）驱动的智能体架构，能够构建机器人操作环境的密集且高效的表示。EmbodiedLGR-Agent 通过提供一种基于参数高效 VLM 的混合构建-检索方法，直接解决了对环境高效记忆表示的需求。该方法在语义图中存储有关物体及其位置的低层级信息，同时利用传统的检索增强架构保留观测场景的高层级描述。EmbodiedLGR-Agent 在流行的 NaVQA 数据集上进行了评估，在具身智能体的推理和查询时间方面达到了最先进的性能，同时在全局任务上保持了与当前最先进方法相比具有竞争力的准确率。此外，EmbodiedLGR-Agent 已成功部署在实体机器人上，通过人机交互展示了在现实世界场景中的实用性，同时在本地运行视觉语言模型和构建-检索流水线。

## Abstract
As the world of agentic artificial intelligence applied to robotics evolves, the need for agents capable of building and retrieving memories and observations efficiently is increasing. Robots operating in complex environments must build memory structures to enable useful human-robot interactions by leveraging the mnemonic representation of the current operating context. People interacting with robots may expect the embodied agent to provide information about locations, events, or objects, which requires the agent to provide precise answers within human-like inference times to be perceived as responsive. We propose the Embodied Light Graph Retrieval Agent (EmbodiedLGR-Agent), a visual-language model (VLM)-driven agent architecture that constructs dense and efficient representations of robot operating environments. EmbodiedLGR-Agent directly addresses the need for an efficient memory representation of the environment by providing a hybrid building-retrieval approach built on parameter-efficient VLMs that store low-level information about objects and their positions in a semantic graph, while retaining high-level descriptions of the observed scenes with a traditional retrieval-augmented architecture. EmbodiedLGR-Agent is evaluated on the popular NaVQA dataset, achieving state-of-the-art performance in inference and querying times for embodied agents, while retaining competitive accuracy on the global task relative to the current state-of-the-art approaches. Moreover, EmbodiedLGR-Agent was successfully deployed on a physical robot, showing practical utility in real-world contexts through human-robot interaction, while running the visual-language model and the building-retrieval pipeline locally.

---

## 论文详细总结（自动生成）

### 论文总结：EmbodiedLGR - 机器人智能体的轻量级语义空间记忆架构

#### 1. 核心问题与研究动机
在复杂环境中运行的具身智能体（机器人）需要具备构建和检索记忆的能力，以响应人类关于物体位置、历史事件或空间布局的查询。现有的方法（如基于 RAG 的系统或 3D 场景图）往往存在以下问题：
*   **冗余性高**：简单地按时间顺序堆叠观测结果，导致向量数据库过于臃肿。
*   **计算开销大**：依赖重型视觉语言模型（VLM），难以在机器人边缘端实现实时推理。
*   **检索效率低**：缺乏针对空间和时间维度的优化，导致响应延迟无法满足类人交互的需求。
**研究动机**：开发一种既能保持高精度语义理解，又能实现极低延迟检索的轻量级记忆架构。

#### 2. 方法论
EmbodiedLGR-Agent 提出了一种**双层混合记忆结构**，结合了图表示的遍历效率和向量数据库的语义深度：

*   **核心思想**：将底层物体信息（标签、精确坐标、时间戳）存储在**语义记忆图（Memory Graph）**中，而将高层场景描述（全局视觉说明）存储在**向量数据库**中。
*   **关键技术细节**：
    *   **感知层**：使用轻量级 VLM（Florence-2）实时提取图像中的物体标签和场景描述。
    *   **记忆构建**：
        *   **图更新机制**：通过空间距离阈值（$\delta_P$）和语义相似度阈值（$\delta_E$）判断新观测到的物体是已知实体的更新还是新实体。若相似且距离近，则使用移动平均法更新坐标，避免重复建节点。
        *   **向量库填充**：存储由 VLM 生成的详细场景描述及其嵌入向量。
    *   **检索工具集**：为 LLM 智能体提供三类专用图检索工具（语义、位置、时间搜索）以及一个向量库检索工具（作为语义复杂查询的备选方案/Fallback）。
*   **算法流程**：机器人探索环境 $\rightarrow$ VLM 处理帧 $\rightarrow$ 更新语义图和向量库 $\rightarrow$ 用户提问 $\rightarrow$ LLM 根据查询复杂度选择工具 $\rightarrow$ 从图中快速检索或从向量库深度检索 $\rightarrow$ 生成回答。

#### 3. 实验设计
*   **数据集**：使用 **NaVQA** 数据集，该数据集专门用于评估机器人在导航任务中的视觉问答能力。
*   **Benchmark 与对比方法**：
    *   主要对比基准是 **ReMEmbR**（一种基于纯向量数据库检索的 SOTA 方法）。
    *   对比了不同规模的 VLM（Florence-2-base 和 Florence-2-large）。
*   **评估指标**：响应延迟（Latency）、回退比例（Fallback %，即不得不求助于向量库的频率）、空间准确率（25米误差内）和时间准确率（3分钟误差内）。

#### 4. 资源与算力
*   **仿真环境**：AMD Ryzen 7 3700X CPU, NVIDIA RTX 3060 (12GB) GPU, 32GB RAM。
*   **物理部署**：**Nvidia Jetson AGX Orin (64GB)**。
*   **模型细节**：
    *   VLM：Florence-2（0.22B 和 0.77B 参数），在 Jetson 上**本地运行**，无需量化。
    *   LLM：通过 API 调用 GPT-4o。
    *   嵌入模型：all-MiniLM-L6-v2。
*   **训练时长**：文中未提及模型训练，重点在于推理和实时记忆构建。

#### 5. 实验数量与充分性
*   **实验规模**：从 NaVQA 中抽取了 120 个问题进行定量评估。
*   **消融实验**：分别测试了“仅图记忆”、“仅向量库（ReMEmbR）”以及“图+向量库混合”三种配置。
*   **真实世界验证**：在大学校园内进行了 30 分钟的实地探索和交互测试，验证了系统在真实 ROS 2 环境下的可行性。
*   **评价**：实验设计较为客观，涵盖了从仿真指标到物理部署的完整链路，但 120 个问题的样本量对于大规模基准测试来说相对较小。

#### 6. 主要结论与发现
*   **效率提升**：仅使用图检索时，响应延迟比纯向量库方法**降低了约 50%**。
*   **准确性**：混合架构在空间准确率上优于纯向量库方法，因为图结构能更精确地关联物体与坐标。
*   **智能体行为**：随着 VLM 能力增强（从 base 到 large），智能体更倾向于直接从语义图中获取答案，回退到向量库的比例从 93.34% 降至 80.83%。
*   **本地化可行性**：证明了在边缘计算设备（Jetson Orin）上实时运行 VLM 驱动的记忆系统是完全可行的。

#### 7. 优点
*   **轻量化与实时性**：通过 Florence-2 和图结构的结合，显著降低了具身智能体的计算负担。
*   **去冗余**：图更新机制有效解决了传统 RAG 在机器人连续观测中产生的内存爆炸问题。
*   **混合检索策略**：灵活切换底层原子信息（图）和高层语义信息（向量库），优化了推理路径。
*   **实战导向**：提供了完整的 ROS 2 集成方案，并在物理机器人上成功演示。

#### 8. 不足与局限
*   **LLM 依赖性**：系统性能高度依赖 LLM 调用工具的准确性，偶尔会出现误判或错误的工具调用。
*   **VLM 粒度限制**：为了追求速度使用的轻量级 VLM 在描述极其复杂的场景细节时，不如大型模型（如 VILA 或 Qwen-VL）丰富。
*   **动态环境挑战**：虽然有位置更新机制，但在高度动态（物体频繁移动）的环境下的鲁棒性仍需进一步验证。
*   **样本量限制**：定量评估的问题数量较少，可能无法完全覆盖极端长尾场景。

（完）
