---
title: "Ego-InBetween: Generating Object State Transitions in Ego-Centric Videos"
title_zh: Ego-InBetween：生成第一人称视角视频中的物体状态转换
authors: "Mengmeng Ge, Takashi Isobe, Xu Jia, Yanan Sun, Zetong Yang, Weinong Wang, Dong Zhou, Dong Li, Huchuan Lu, Emad Barsoum"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17749v1"
tags: ["query:q11"]
score: 9.0
evidence: 在动作指令下生成视频中的物体状态转换
tldr: 本研究针对第一视角下的指令式视觉状态转换（EIVST）任务，提出了EgoIn框架。该任务旨在根据初始状态、目标状态及动作指令生成中间转换过程。EgoIn通过微调的TransitionVLM推理多步转换逻辑，并利用转换调节模块与物体感知辅助监督机制，确保生成视频在逻辑上符合指令且在视觉上保持物体外观一致。实验表明，该方法在人机与机器人交互场景中均表现优异。
selection_source: fresh_fetch
motivation: 旨在解决当前生成模型在第一视角下难以准确推理物体状态转换逻辑及维持物体外观一致性的难题。
method: 提出EgoIn框架，结合TransitionVLM进行状态推理，并采用转换调节模块和物体感知辅助监督来生成连贯视频。
result: 在多个人机及机器人交互数据集上的实验验证了EgoIn在生成语义明确且视觉连贯的转换序列方面的卓越性能。
conclusion: 该研究为第一视角下的动作建模提供了新思路，有效实现了受控且一致的物体状态转换视频生成。
---

## 摘要
理解物理变换过程对于人类认知和人工智能系统都至关重要，特别是在第一人称视角下，它是动作建模中连接人与机器的关键桥梁。我们将这一建模过程定义为“第一人称指令式视觉状态转换”（EIVST），即在简短的动作指令下，生成描述物体在初始状态与目标状态之间变换过程的中间帧。EIVST 给当前的生成模型带来了两个挑战：(1) 理解初始和目标状态的视觉场景，并从第一人称视角推理变换步骤；(2) 生成符合给定指令且连贯的中间过渡，同时在两个视觉状态之间保持物体外观的一致性。为了应对这些挑战，我们提出了 EgoIn 框架。该框架首先利用 TransitionVLM 推断两个给定状态之间的多步转换过程，TransitionVLM 在我们精心策划的数据集上进行了微调，以更好地适应此任务并减少幻觉信息。随后，它根据所提出的转换条件模块（Transition Conditioning module）生成的转换条件来生成一系列帧。此外，我们还引入了物体感知辅助监督（Object-aware Auxiliary Supervision），以在整个转换过程中保持一致的物体外观。在人-物交互和机器人-物体交互数据集上的广泛实验表明，EgoIn 在生成具有语义意义且视觉连贯的变换序列方面表现优异。

## Abstract
Understanding physical transformation processes is crucial for both human cognition and artificial intelligence systems, particularly from an egocentric perspective, which serves as a key bridge between humans and machines in action modeling. We define this modeling process as Egocentric Instructed Visual State Transition (EIVST), which involves generating intermediate frames that depict object transformations between initial and target states under a brief action instruction. EIVST poses two challenges for current generative models: (1) understanding the visual scenes of the initial and target states and reasoning about transformation steps from an egocentric view, and (2) generating a consistent intermediate transition that follows the given instruction while preserving object appearance across the two visual states. To address these challenges, we propose the EgoIn framework. It first infers the multi-step transition process between two given states using TransitionVLM, fine-tuned on our curated dataset to better adapt to this task and reduce hallucinated information. It then generates a sequence of frames based on transition conditions produced by the proposed Transition Conditioning module. Additionally, we introduce Object-aware Auxiliary Supervision to preserve consistent object appearance throughout the transition. Extensive experiments on human-object and robot-object interaction datasets demonstrate EgoIn's superior performance in generating semantically meaningful and visually coherent transformation sequences.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Ego-InBetween**，一个旨在生成第一人称视角（Egocentric）视频中物体状态转换过程的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：理解物理世界的演变过程（如物体如何从状态 A 变为状态 B）是人类认知的核心。对于具身智能（机器人、视觉助手）而言，仅预测未来帧是不够的，需要根据初始状态、目标状态和动作指令，补全中间复杂的物理变换过程。
*   **核心问题**：定义了**第一人称指令式视觉状态转换（EIVST）**任务。当前模型在处理此类任务时面临两大挑战：(1) 难以准确推理第一视角下的多步变换逻辑；(2) 难以在生成过程中保持物体外观的一致性以及动作与指令的精确对齐。

### 2. 方法论
EgoIn 采用“分而治之”的策略，分为**转换过程建模**和**条件视频生成**两个阶段：
*   **TransitionVLM（转换推理）**：基于 Qwen2.5-VL 7B，通过 LoRA 微调。它负责分析初始/目标帧及指令，推断出：(1) 被操作的物体；(2) 中间转换的具体步骤（K 个步骤）；(3) 每个步骤对应的时序范围。
*   **Transition Conditioning (TC) 模块**：
    *   **多模态语义状态编码**：利用 SQ-Former 将初始/目标帧的视觉特征与 VLM 生成的语义特征对齐，提取状态感知表示。
    *   **范围感知转换编码**：根据 VLM 预测的时序范围，利用高斯重采样机制对转换特征进行加权，生成帧级别的引导信号，注入 U-Net 的交叉注意力层。
*   **物体感知辅助监督 (OAS)**：在训练阶段，引入一个定位头（Localization Head），利用 SAM2 生成的掩码作为真值，监督模型学习操作物体的空间位置。这有助于在生成过程中保持物体外观一致性，且在推理阶段不增加额外开销。

### 3. 实验设计
*   **数据集**：涵盖了人-物交互和机器人操作四大数据集：**Epic100**（厨房场景）、**EgoFHO**（日常活动）、**DualArm**（双臂机器人）和 **Bridge**（单臂机器人）。
*   **对比方法**：对比了视频插值模型（FILM、TRF、GI）、视频转换模型（SEINE）以及基于文本引导的插值模型（DC-Interp）。为了公平起见，所有基准模型均在 EIVST 数据集上进行了微调。
*   **评价指标**：采用 VBench 协议，包括 FVD（视频质量）、VTQ（时序质量）、VTC（文视频一致性）和 VIC（图视频一致性）。

### 4. 资源与算力
*   **模型规模**：推理部分使用 7B 参数的 VLM；生成部分基于 DynamiCrafter。
*   **训练细节**：TC 模块先单独训练 20k 步（学习率 1e-4，Batch Size 64），随后与 U-Net 空间层联合微调 10k 步（学习率 2e-5，Batch Size 32）。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，但提到该框架基于“轻量级且显存友好”的 DynamiCrafter 构建。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的数据集上进行了广泛测试，每个数据集抽取 1000 个样本进行评估。
*   **消融实验**：非常充分。分别验证了 TransitionVLM、TC 模块、OAS 监督机制的有效性；探讨了转换步骤 $K$ 的取值影响；并对比了 VLM 微调前后对减少“幻觉”的效果。
*   **主观评价**：组织了 40 名参与者的用户研究，从推理合理性、指令对齐和运动质量三个维度进行打分，结果显示 EgoIn 具有显著优势。

### 6. 主要结论与发现
*   **推理的重要性**：直接使用基础 I2V 模型往往会忽略关键步骤，而通过 VLM 显式推理转换步骤和时序范围，能显著提升生成视频的逻辑合理性。
*   **时序对齐**：预测非均匀的时序范围比简单的均匀分布更能生成符合物理常识的动作。
*   **外观一致性**：引入物体定位的辅助任务能有效缓解扩散模型在生成复杂动作时常见的物体变形或消失问题。

### 7. 优点
*   **逻辑性强**：通过 VLM 引入了显式的物理变换推理，解决了传统生成模型“只看图不思考”的问题。
*   **可控性高**：TC 模块实现了帧级别的精细引导，使生成过程在时间和空间上都更加受控。
*   **即插即用**：OAS 机制在训练时增强模型能力，推理时无需额外掩码输入，保持了推理效率。

### 8. 不足与局限
*   **长程生成挑战**：对于涉及大幅度场景切换或视角剧烈变化的超长转换过程，模型仍难以保持多视角一致性。
*   **复杂环境适应性**：在背景极其复杂或存在大量遮挡的场景下，物体定位和外观保持的难度会显著增加。
*   **应用限制**：目前主要集中在第一人称视角，对于第三人称或其他视角的泛化能力尚未深入探讨。

（完）
