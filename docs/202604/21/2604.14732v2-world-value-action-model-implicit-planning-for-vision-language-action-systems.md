---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v2"
tags: ["query:robot"]
score: 10.0
evidence: 用于VLA系统隐式规划的世界-价值-动作模型
tldr: 本研究针对现有视觉-语言-动作（VLA）模型在长程任务中缺乏推理和评估能力的问题，提出了World-Value-Action (WAV) 模型。该模型通过学习未来轨迹的结构化潜空间表示，结合预测未来状态的世界模型和评估长期效用的价值函数，实现了隐式规划。理论分析表明，在潜空间进行推理能有效解决动作空间搜索中可行性概率随步数指数衰减的问题。实验证明，WAV在复杂长程任务和泛化能力上显著优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有的VLA模型多依赖直接动作预测，难以在复杂决策任务中进行长程推理和后果评估。
method: 提出WAV框架，通过世界模型预测未来状态并利用价值函数评估轨迹，在结构化潜空间中进行隐式规划。
result: 实验表明WAV在任务成功率、泛化能力和鲁棒性方面均优于现有最先进方法，特别是在长程和组合场景中表现突出。
conclusion: 潜空间推理能有效重塑搜索分布，是提升具身智能体长程决策效率和性能的关键路径。
---

## 摘要
视觉-语言-动作（VLA）模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，大多数现有方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作（WAV）模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习一种以视觉观测和语言指令为条件的未来轨迹结构化潜表示。一个学习到的世界模型负责预测未来状态，而轨迹价值函数则评估其长时程效用。动作生成随后被表述为在该潜空间中的推理过程，模型逐渐将概率质量集中在高价值且动力学可行的轨迹上。我们提供了一个理论视角，表明直接在动作空间中进行规划会面临随着时程增加，可行轨迹概率呈指数级衰减的问题。相比之下，潜空间推理将搜索分布重塑向可行区域，从而实现了高效的长时程决策。大量的仿真和真实世界实验表明，WAV 模型始终优于现有最先进的方法，在任务成功率、泛化能力和鲁棒性方面取得了显著提升，特别是在长时程和组合场景中。代码可在 https://github.com/Win-commit/WAV 获取。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios. Code is available at https://github.com/Win-commit/WAV.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **World-Value-Action (WAV)** 的新型视觉-语言-动作模型，旨在通过潜空间中的隐式规划提升具身智能体在复杂长程任务中的决策能力。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型大多采用“反应式”策略，即直接从观测映射到动作，缺乏对未来状态的推理和对动作后果的评估能力。
*   **研究动机**：在长程（Long-horizon）任务中，微小的动作偏差会随时间累积导致失败。虽然世界模型可以预测未来，但如何有效地利用预测信息来指导动作选择仍是一个难题。作者试图探索：规划是否可以作为一种“隐式推理”过程集成到生成式模型中，而不是作为一个独立的显式优化模块。

### 2. 方法论
WAV 模型由三个紧密耦合的模块组成，基于 **Flow Matching（流匹配）** 框架构建：
*   **核心思想**：将规划定义为潜空间（Latent Space）中的推理过程。模型不直接在动作空间搜索，而是学习未来轨迹的结构化潜表示，并根据“价值”和“动力学可行性”重塑分布。
*   **关键技术模块**：
    1.  **语言条件视频生成模块 (World Model)**：使用 Diffusion Transformer (DiT) 架构，根据指令和当前观测预测未来的视觉演变。
    2.  **轨迹价值模块 (Value Function)**：评估预测视频序列的长期效用（累积奖励），为规划提供导向。
    3.  **动作解码模块 (Action Decoder)**：融合视频特征和价值特征，生成最终的可执行动作。
*   **算法流程（迭代推理）**：
    *   在推理阶段，模型不只进行一次采样，而是采用类似 MPPI 的迭代过程。
    *   采样多组视频潜变量 $\to$ 生成预测视频 $\to$ 价值模块评估 $\to$ 筛选高价值的“精英”样本 $\to$ 更新潜变量分布 $\to$ 最终解码动作。
*   **理论支撑**：论文证明了“可行性诅咒”——在动作空间中，可行轨迹的概率随步数增加呈指数级衰减；而潜空间推理能将搜索集中在可行且高价值的区域。

### 3. 实验设计
*   **仿真实验**：在 **LIBERO** 基准测试集上进行，涵盖 Spatial（空间推理）、Object（物体泛化）、Goal（目标变更）和 Long（长程组合）四个子集。
*   **真实世界实验**：使用 **Piper 双臂机器人**，执行包括“整理碗筷”、“铺平毛巾”以及复杂的“打开抽屉-放入物体-关闭抽屉”等任务。
*   **对比方法**：包括 Diffusion Policy、Octo、OpenVLA、$\pi_0$、GR00T-N1 以及同样包含世界模型的 WorldVLA、GE-ACT 等 SOTA 模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100-SXM4-80GB GPU**。
*   **训练时长**：
    *   LIBERO 任务：全参数微调，视频模块训练 3 万步，价值/动作模块训练 4 万步，耗时约 **5 天**。
    *   真实世界任务：每个任务单独训练，视频和价值/动作模块各训练 3 万步，耗时约 **3 天**。
*   **模型规模**：总参数量约为 **22 亿 (2.2B)**。

### 5. 实验数量与充分性
*   **实验规模**：在仿真环境中测试了数十个任务；在真实世界中对每个任务进行了 15 次独立实验。
*   **消融实验**：非常详尽，涵盖了迭代次数 $K$、采样数量 $M/N$、平滑参数 $\alpha/\beta$ 以及精英样本比例等对性能的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还分析了推理效率与显存占用，并提供了定性的视频预测对比，客观地展示了隐式规划在减少误差累积方面的作用。

### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 平均得分达到 98.1，刷新了该榜单的纪录。
*   **长程任务优势**：在最难的 LIBERO-Long 任务中，WAV 相比无规划版本提升显著，证明了隐式规划对长程推理的必要性。
*   **真实世界鲁棒性**：在真实抽屉任务中，WAV 的成功率远超基线模型（从 35.6% 提升至 75.6%），表现出更强的空间感知和多步协调能力。

### 7. 优点
*   **理论与实践结合**：通过数学证明解释了潜空间规划的优越性，为 VLA 模型的设计提供了理论依据。
*   **模块化统一**：将预测、评估和执行整合在一个流匹配框架下，架构优雅且易于扩展。
*   **迭代优化机制**：引入迭代推理（Iterative Inference），使模型具备了类似人类“三思而后行”的能力。

### 8. 不足与局限
*   **计算开销**：由于引入了迭代采样和视频生成，推理延迟和显存占用随迭代次数增加而显著上升，可能限制其在极高频控制场景下的应用。
*   **奖励函数依赖**：价值模块的训练仍依赖于预定义的密集奖励函数（Dense Reward），这在某些完全未知的任务中可能难以获取。
*   **实时性挑战**：虽然论文提到了效率权衡，但在物理机器人上的闭环实时部署仍面临较大压力。

（完）
