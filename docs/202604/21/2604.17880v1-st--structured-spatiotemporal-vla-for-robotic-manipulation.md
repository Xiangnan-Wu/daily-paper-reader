---
title: "ST-$π$: Structured SpatioTemporal VLA for Robotic Manipulation"
title_zh: ST-π：用于机器人操纵的结构化时空 VLA 模型
authors: "Chuanhao Ma, Hanyu Zhou, Shihan Peng, Yan Li, Tao Gu, Luxin Yan"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17880v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人操作的结构化时空VLA模型
tldr: 针对现有VLA模型在精细时空操作中推理不明确的问题，本文提出了ST-π框架。该框架通过时空VLM生成包含子任务、空间和时间定位的块级动作提示，并结合时空动作专家进行双生成器引导，实现了从全局行为规划到局部动作控制的结构化建模。此外，研究还提供了一个带有时空标注的真实机器人数据集，实验证明了该方法在复杂操作任务中的优越性。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要依赖隐式的跨模态映射，难以处理具有明确时空边界的复杂序列化机器人操作任务。
method: 提出一种结构化框架，利用VLM生成块级动作提示进行全局规划，并设计时空动作专家通过双生成器引导预测步级动作参数。
result: 在真实世界机器人任务和模拟实验中，ST-π在处理精细时空操作方面表现出显著的性能提升。
conclusion: 结构化的时空建模能有效提升VLA模型在复杂机器人操作中的规划与控制能力。
---

## 摘要
视觉-语言-动作（VLA）模型在通用机器人任务上取得了巨大成功，但在细粒度时空操纵方面仍面临挑战。通常，现有方法主要将时空知识嵌入到视觉和动作表示中，并直接进行跨模态映射以进行步级动作预测。然而，这种时空推理在很大程度上仍是隐式的，难以处理具有明确时空边界的多个序列行为。在这项工作中，我们提出了 ST-π，一种用于机器人操纵的结构化时空 VLA 模型。我们的模型由两个关键设计引导：1）时空 VLM。我们将 4D 观测和任务指令编码到潜空间中，并将其输入到大语言模型（LLM）中，以生成一系列具有因果顺序的块级动作提示，这些提示由子任务、空间定位和时间定位组成。2）时空动作专家。在块级动作提示的约束下，我们设计了一种结构化双生成器引导，以联合建模空间依赖关系和时间因果关系，从而预测步级动作参数。在这个结构化框架内，VLM 显式地规划全局时空行为，而动作专家则进一步细化局部时空控制。此外，我们提出了一个具有结构化时空标注的真实世界机器人数据集用于微调。广泛的实验证明了我们模型的有效性。代码链接：https://github.com/chuanhaoma/ST-pi。

## Abstract
Vision-language-action (VLA) models have achieved great success on general robotic tasks, but still face challenges in fine-grained spatiotemporal manipulation. Typically, existing methods mainly embed spatiotemporal knowledge into visual and action representations, and directly perform a cross-modal mapping for step-level action prediction. However, such spatiotemporal reasoning remains largely implicit, making it difficult to handle multiple sequential behaviors with explicit spatiotemporal boundaries. In this work, we propose ST-$π$, a structured spatiotemporal VLA model for robotic manipulation. Our model is guided by two key designs: 1) Spatiotemporal VLM. We encode 4D observations and task instructions into latent spaces, and feed them into the LLM to generate a sequence of causally ordered chunk-level action prompts consisting of sub-tasks, spatial grounding and temporal grounding. 2) Spatiotemporal action expert. Conditioned on chunk-level action prompts, we design a structured dual-generator guidance to jointly model spatial dependencies and temporal causality, thus predicting step-level action parameters. Within this structured framework, the VLM explicitly plans global spatiotemporal behavior, and the action expert further refines local spatiotemporal control. In addition, we propose a real-world robotic dataset with structured spatiotemporal annotations for fine-tuning. Extensive experiments have been conducted to demonstrate the effectiveness of our model. Our code link: https://github.com/chuanhaoma/ST-pi.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **ST-$\pi$**，一种旨在解决机器人操纵中精细时空推理问题的结构化视觉-语言-动作（VLA）模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的 VLA 模型（如 OpenVLA）在处理长程（Long-horizon）且具有明确时空边界的任务（如复杂的家庭活动或组装）时表现不佳。
*   **研究动机**：现有方法通常将时空知识隐式地嵌入到视觉和动作表示中，缺乏对任务边界的显式建模。这导致模型在处理需要精确空间定位和时间节奏控制的序列化子任务时，容易出现轨迹不连贯或规划混乱。
*   **核心目标**：通过显式的结构化建模，在“块级规划（Chunk-level planning）”和“步级执行（Step-level execution）”两个层面引入时空约束，提升机器人操作的稳定性和精确度。

### 2. 方法论：核心思想与关键技术
ST-$\pi$ 框架由两个核心组件组成：

*   **时空视觉语言模型 (ST-VLM) —— 负责“规划”**：
    *   **4D 表示构建**：整合多视图视频流、几何特征（来自几何编码器）和时间戳（傅里叶位置编码），形成统一的 4D 观测表示。
    *   **结构化任务分解**：将高层指令分解为一系列“块级动作提示（Action Prompts）”。每个提示包含：**语义意图**（做什么）、**空间定位**（在哪里做，3D 边界框）和**时间持续量**（做多久）。
    *   **因果建模**：利用块级因果注意力机制，确保子任务之间具有明确的逻辑先后顺序。

*   **时空动作专家 (ST-AE) —— 负责“执行”**：
    *   **双生成器引导**：设计了**空间运动生成器**（利用双向注意力捕捉全局轨迹结构）和**时间运动生成器**（利用因果注意力确保步骤间的时序一致性）。
    *   **流匹配 (Flow Matching)**：基于流匹配生成动作块，通过时间相关的融合策略，在生成早期侧重空间塑形，后期侧重时间细化，从而生成平滑且符合物理逻辑的轨迹。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：在 **LIBERO**（长程任务基准）和 **SIMPLER**（真实到仿真的评估基准）上进行测试。
    *   **真实世界**：构建了 **STAR** 数据集，包含 30 个操纵任务（物体识别、序列目标、长程任务），共 300k 个交互步骤，并带有详细的子任务时空标注。
*   **对比方法**：对比了 OpenVLA、Octo、SpatialVLA、TraceVLA、4D-VLA、$\pi_{0.5}$ 和 CogACT 等当前最先进的 VLA 模型。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 台 NVIDIA RTX PRO 6000 GPU**。
*   **训练细节**：采用三阶段训练方案（表示对齐、行为规划学习、机器人任务微调）。
    *   阶段 1 批大小为 64，学习率 2e-5。
    *   阶段 2 和 3 批大小为 32，学习率 1e-5。
*   **模型基础**：VLM 主干基于 PaliGemma (3B)，几何编码器基于 DINOv2，动作专家基于 Gemma-300M。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从仿真到真实世界的全方位评估。在 LIBERO 的四个子套件和 SIMPLER 的两个协议下进行了大量实验。
*   **消融实验**：针对结构化框架的有效性、高层注意力结构（因果 vs 双向）、观测模态（2D vs 3D vs 4D）以及任务分解的粒度（子任务数量 $K$）进行了详细的消融研究。
*   **评价指标**：使用了成功率（SR）和完成时间（CT），评估维度客观且全面。

### 6. 主要结论与发现
*   **显式建模的优越性**：ST-$\pi$ 在所有基准测试中均优于现有模型，尤其是在长程任务中，成功率提升显著。
*   **4D 表示的重要性**：引入几何特征和时间上下文（4D）比单纯的 2D 或 3D 观测能显著提高操作的稳定性。
*   **双生成器的协同**：空间生成器负责轨迹平滑，时间生成器负责速度一致性，两者结合能产生最自然的机器人动作。
*   **分解粒度**：适度的任务分解（如 $K=3$）能平衡规划的灵活性与稳定性，过度分解反而可能引入噪声。

### 7. 优点与亮点
*   **结构化创新**：将模糊的端到端映射转变为“语义+空间+时间”的显式提示词，增强了模型的可解释性和可控性。
*   **双系统设计**：ST-VLM 负责宏观规划，ST-AE 负责微调控制，这种分层架构符合人类操作逻辑。
*   **高质量数据集**：STAR 数据集的发布为社区提供了宝贵的带有时空标注的真实机器人数据。

### 8. 不足与局限
*   **序列化假设**：模型假设任务是可以线性分解的序列，难以处理需要并行操作或具有复杂分支结构的逻辑任务。
*   **计算开销**：由于引入了 4D 融合和双生成器，推理时的计算复杂度可能高于传统的单帧 VLA 模型。
*   **标注依赖**：训练 ST-VLM 需要详细的子任务边界和时空标注，这增加了数据准备的成本。

（完）
