---
title: "AutoWorld: Scaling Multi-Agent Traffic Simulation with Self-Supervised World Models"
title_zh: AutoWorld：利用自监督世界模型扩展多智能体交通仿真
authors: "Mozhgan Pourkeshavatz, Tianran Liu, Nicholas Rhinehart"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28963v1"
tags: ["query:robot"]
score: 8.0
evidence: 用于自动驾驶交通模拟的自监督世界模型
tldr: AutoWorld 提出了一种利用无标注 LiDAR 数据扩展多智能体交通模拟的方法。它通过自监督学习构建世界模型，从占据栅格表示中提取场景上下文，并结合级联行列式点过程（DPP）提升采样多样性。该方法在 WOSAC 榜单排名第一，证明了利用海量无标注数据提升模拟真实性的有效性，为低成本扩展交通模拟性能提供了新路径。
selection_source: fresh_fetch
motivation: 现有的交通模拟器过度依赖昂贵的标注数据，难以利用海量无标注传感器数据来提升模拟的真实感和规模。
method: 提出 AutoWorld 框架，利用无标注 LiDAR 占据表示学习世界模型，并通过级联行列式点过程和运动感知潜空间监督来生成多样化的多智能体轨迹。
result: 在 WOSAC 基准测试中排名第一，且实验证明模拟性能随无标注数据的增加而持续提升。
conclusion: 该研究展示了通过自监督世界模型利用无标注数据扩展交通模拟的可行性，显著降低了对人工标注的依赖。
---

## 摘要
多智能体交通仿真是开发和测试自动驾驶系统的核心。近期的驱动型仿真器已取得显著成果，但严重依赖于来自标注轨迹或语义注释的有监督学习，这使得提升其性能的成本十分高昂。与此同时，虽然可以大规模收集海量的无标注传感器数据，但在现有的交通仿真框架中，这些数据在很大程度上仍未得到利用。这提出了一个关键问题：如何利用无标注数据来提升交通仿真的性能？在这项工作中，我们提出了 AutoWorld，这是一个利用从 LiDAR 数据的无标注占据（occupancy）表示中学习到的世界模型的交通仿真框架。给定世界模型样本，AutoWorld 构建了一个由粗到精的预测场景上下文，作为多智能体运动生成模型的输入。为了提升样本多样性，AutoWorld 使用级联行列式点过程（Determinantal Point Process）框架来引导世界模型和运动模型的采样过程。此外，我们设计了一个运动感知的潜空间监督目标，增强了 AutoWorld 对场景动力学的表示能力。在 WOSAC 基准测试上的实验表明，根据主要的真实性元指标（RMM），AutoWorld 在排行榜上排名第一。我们进一步证明，随着无标注 LiDAR 数据的加入，仿真性能持续提升，并通过消融实验研究了各组件的有效性。我们的方法为在无需额外标注的情况下提升交通仿真的真实性铺平了道路。我们的项目主页包含额外的可视化效果和发布的源代码。

## Abstract
Multi-agent traffic simulation is central to developing and testing autonomous driving systems. Recent data-driven simulators have achieved promising results, but rely heavily on supervised learning from labeled trajectories or semantic annotations, making it costly to scale their performance. Meanwhile, large amounts of unlabeled sensor data can be collected at scale but remain largely unused by existing traffic simulation frameworks. This raises a key question: How can a method harness unlabeled data to improve traffic simulation performance? In this work, we propose AutoWorld, a traffic simulation framework that employs a world model learned from unlabeled occupancy representations of LiDAR data. Given world model samples, AutoWorld constructs a coarse-to-fine predictive scene context as input to a multi-agent motion generation model. To promote sample diversity, AutoWorld uses a cascaded Determinantal Point Process framework to guide the sampling processes of both the world model and the motion model. Furthermore, we designed a motion-aware latent supervision objective that enhances AutoWorld's representation of scene dynamics. Experiments on the WOSAC benchmark show that AutoWorld ranks first on the leaderboard according to the primary Realism Meta Metric (RMM). We further show that simulation performance consistently improves with the inclusion of unlabeled LiDAR data, and study the efficacy of each component with ablations. Our method paves the way for scaling traffic simulation realism without additional labeling. Our project page contains additional visualizations and released code.

---

## 论文详细总结（自动生成）

### 论文总结：AutoWorld —— 利用自监督世界模型扩展多智能体交通仿真

#### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的数据驱动型交通仿真器（如基于扩散模型或自回归模型的方法）高度依赖于**有标注的轨迹数据**或**语义注释**。这种有监督学习模式在扩展规模时成本极高，因为人工标注传感器数据既耗时又昂贵。
*   **核心问题**：如何利用海量的、易于获取的**无标注传感器数据**（如 LiDAR 点云）来提升交通仿真的真实感和多样性？
*   **整体含义**：论文提出了 **AutoWorld** 框架，旨在打破对标注数据的依赖，通过自监督学习构建“世界模型”来捕捉环境动力学，并以此引导多智能体行为生成，实现仿真性能随数据规模的自动增长。

#### 2. 论文提出的方法论
AutoWorld 采用两阶段架构，结合了自监督世界模型和条件运动生成：
*   **核心思想**：首先从无标注 LiDAR 数据中学习一个预测未来场景占据情况（Occupancy）的世界模型，然后将该模型的预测作为上下文，引导扩散模型生成具体的智能体轨迹。
*   **关键技术细节**：
    1.  **运动感知潜空间监督（Motion-Aware Latent Supervision）**：在训练世界模型时，针对占据栅格中静态物体多、动态物体少的类别不平衡问题，通过计算时间序列上的占据变化生成权重图，强制模型关注动态区域，无需语义标签即可学习场景动力学。
    2.  **世界模型引导的仿真**：利用流匹配（Flow Matching）预测未来潜空间占据序列，并构建“预测场景上下文”（Predictive Scene Context），通过交叉注意力机制为运动生成模型提供由粗到精的导航约束。
    3.  **级联行列式点过程（Cascaded DPP）**：在推理阶段，分别在世界模型采样（场景层级）和运动模型采样（行为层级）应用 DPP 引导。通过最大化样本集的行列式值，在保持高质量（真实感）的同时，强制样本之间产生排斥力，从而生成多样化的仿真结果。

#### 3. 实验设计
*   **数据集**：使用 **Waymo Open Dataset (WOD)** 训练世界模型（利用其无标注序列），使用 **Waymo Open Motion Dataset (WOMD)** 训练运动生成模块。
*   **Benchmark**：**WOSAC (Waymo Open Sim Agents Challenge)**，这是目前公认的最权威的闭环行为生成基准。
*   **对比方法**：与排行榜上的顶尖方法对比，包括 InfGen、LLM2AD、SMART-R1、DecompGAIL、UniMotion 等。
*   **评价指标**：主要指标为真实性元指标（RMM），涵盖运动学（Kinematic）、交互性（Interactive）和地图遵从性（Map-based）。

#### 4. 资源与算力
*   **算力支持**：论文提到使用了 **Google TPU Research Cloud (TRC)** 和 **NVIDIA 学术授权计划**提供的资源，以及加拿大数字研究联盟的计算资源。
*   **训练细节**：
    *   VAE 编码器训练了 100 个 epoch（Batch size 16）。
    *   世界模型（流匹配）训练了 100 个 epoch，随后使用运动感知监督微调了 50 个 epoch（Batch size 8）。
*   **明确说明**：文中未给出具体的 GPU/TPU 型号数量及总训练时长（小时数）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **排行榜测试**：在 WOSAC 完整测试集上进行了提交。
    *   **消融实验**：在 2% 的验证集切片上进行了详尽的消融（符合该领域通用做法），涵盖了采样策略（N×M 分配）、多样性机制、场景上下文的作用等。
    *   **扩展性实验**：测试了从 1x 到 5x 不同规模的无标注数据对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。通过对比“有/无语义监督”的世界模型，证明了自监督方法的有效性；通过数据规模曲线，验证了该方法具备“Scaling Law”属性。

#### 6. 论文的主要结论与发现
*   **性能领先**：AutoWorld 在 WOSAC 排行榜上排名**第一**（RMM 0.7865），尤其在交互性指标上表现优异。
*   **可扩展性**：仿真性能随无标注 LiDAR 数据的增加而**持续提升**，证明了世界模型作为预训练任务的价值。
*   **多样性与真实感的平衡**：级联 DPP 采样能在不破坏物理约束的前提下，显著提升仿真场景的多样性。
*   **自监督可行性**：运动感知监督使无标注训练的效果逼近甚至在某些指标上超过了依赖语义标签的模型。

#### 7. 优点
*   **范式创新**：成功将“世界模型”引入交通仿真，实现了从“纯监督轨迹学习”向“半监督场景动力学引导”的转变。
*   **数据高效**：能够挖掘海量廉价的无标注数据，解决了自动驾驶仿真中高质量标注数据稀缺的痛点。
*   **采样策略**：提出的级联 DPP 是一种无需重新训练的推理技巧，具有很强的通用性和灵活性。

#### 8. 不足与局限
*   **计算开销**：两阶段生成（流匹配 + 扩散模型）加上 DPP 引导，在推理时的计算延迟可能较高，难以满足超大规模实时仿真的需求。
*   **占据表示限制**：基于占据栅格（Voxel）的表示受限于分辨率，可能在处理远距离小物体或精细几何结构时存在信息损失。
*   **拓扑覆盖**：定性分析显示，虽然能覆盖大部分路径，但在极复杂的道路拓扑（如多路口选择）上，采样仍可能无法完全覆盖所有合法的拓扑路径。

（完）
