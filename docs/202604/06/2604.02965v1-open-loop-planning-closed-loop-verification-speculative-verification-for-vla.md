---
title: "Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA"
title_zh: 开环规划，闭环验证：针对 VLA 的投机验证
authors: "Zihua Wang, Zhitao Lin, Ruibo Li, Yu Zhang, Xu Yang, Siya Mi, Xiu-Shen Wei"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02965v1"
tags: ["query:robot"]
score: 10.0
evidence: 针对VLA控制与规划的投机验证框架
tldr: 针对视觉-语言-动作（VLA）模型推理成本高且开环执行鲁棒性差的问题，本文提出SV-VLA框架。该框架将重型VLA模型的高效长程规划与轻量级验证器的闭环在线监控相结合。重型模型作为宏观规划器生成动作序列及上下文，而轻量级验证器则根据实时观测判断是否需要重新规划。这种“投机验证”机制在保留动作分块效率的同时，显著增强了模型在动态环境中的可靠性与鲁棒性。
selection_source: fresh_fetch
motivation: 传统的VLA动作分块技术虽能减少计算量，但因缺乏实时反馈，容易受环境干扰并产生误差累积。
method: 采用重型VLA进行低频宏观规划，并引入轻量级验证器根据当前观测和规划上下文实时触发按需重规划。
result: 实验结果显示，SV-VLA在动态任务中成功结合了分块预测的高效性与闭环控制的稳定性。
conclusion: 该框架为平衡具身智能大模型的推理效率与执行可靠性提供了一种有效的解决方案。
---

## 摘要
视觉-语言-动作（VLA）模型作为具身控制的大型基础模型，在操作任务中展现出强大的性能。然而，其性能是以高昂的推理成本为代价的。为了提高效率，最近的方法采用了动作分块（action chunking），即预测一系列未来动作以进行开环执行。尽管开环执行在减少计算量方面行之有效，但由于缺乏闭环反馈，它对环境变化非常敏感，且容易产生误差累积。为了解决这一局限性，我们提出了 SV-VLA（针对 VLA 控制的投机验证），这是一个将高效的开环长程规划与轻量级闭环在线验证相结合的框架。具体而言，SV-VLA 使用重型 VLA 作为低频宏观规划器，生成动作分块及规划上下文，而轻量级验证器则根据最新观测结果持续监控执行过程。在当前观测和规划上下文的约束下，验证器将规划的动作与闭环参考动作进行比较，并仅在必要时触发重新规划。实验表明，SV-VLA 结合了分块预测的高效性与闭环控制的鲁棒性，能够在动态环境中实现高效且可靠的基于 VLA 的控制。代码已开源：https://github.com/edsad122/SV-VLA。

## Abstract
Vision-Language-Action (VLA) models, as large foundation models for embodied control, have shown strong performance in manipulation tasks. However, their performance comes at high inference cost. To improve efficiency, recent methods adopt action chunking, which predicts a sequence of future actions for open-loop execution. Although effective for reducing computation, open-loop execution is sensitive to environmental changes and prone to error accumulation due to the lack of close-loop feedback. To address this limitation, we propose Speculative Verification for VLA Control (SV-VLA), a framework that combines efficient open-loop long-horizon planning with lightweight closed-loop online verification. Specifically, SV-VLA uses a heavy VLA as a low-frequency macro-planner to generate an action chunk together with a planning context, while a lightweight verifier continuously monitors execution based on the latest observations. Conditioned on both the current observation and the planning context, the verifier compares the planned action against a closed-loop reference action and triggers replanning only when necessary. Experiments demonstrate that SV-VLA combines the efficiency of chunked prediction with the robustness of closed-loop control, enabling efficient and reliable VLA-based control in dynamic environments. Code is available: https://github.com/edsad122/SV-VLA.

---

## 论文详细总结（自动生成）

以下是对论文《Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA》的结构化深度分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然强大，但参数量巨大导致推理延迟极高，难以实现高频闭环控制。
*   **研究背景**：
    *   **动作分块（Action Chunking）**：目前主流做法是预测一段动作序列并开环执行，虽提高了效率，但无法应对环境动态变化，且存在严重的误差累积。
    *   **投机采样（Speculative Decoding）的局限**：在 NLP 中，验证过程可以并行完成；但在机器人领域，未来的验证依赖于执行动作后的新观测，传统的投机采样无法直接应用于需要实时反馈的具身任务。
*   **整体含义**：本文旨在提出一种“规划与验证解耦”的框架，既保留大模型长程规划的高效性，又引入轻量级的实时闭环监控。

### 2. 方法论：SV-VLA 框架
*   **核心思想**：**“昂贵但低频的规划” + “廉价但高频的验证”**。
*   **关键技术细节**：
    1.  **宏观规划器（Macro-Planner）**：使用重型 VLA 模型（如 OpenVLA），在规划边界提取当前观测，输出一个动作分块（Action Chunk）以及一个**规划上下文特征（Context Feature $F_0$）**。
    2.  **轻量级验证器（Verifier）**：由微型视觉骨干（如 ViT-Tiny）和浅层 MLP 组成。它在每个控制步运行，输入当前的实时观测 $I_t$ 和来自重型模型的 $F_0$，预测一个“参考动作”。
    3.  **基于偏差的重规划机制（Deviation-based Replanning）**：
        *   计算验证器预测的参考动作与原计划动作之间的 L1 距离。
        *   若偏差超过预设阈值 $\tau$，则判定原计划失效，立即丢弃剩余分块并触发重型 VLA 进行重规划。
        *   若偏差在范围内，则继续执行原计划，享受分块带来的高效率。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 机器人操作基准测试，包含三个子任务套件：
    *   **LIBERO-Goal**：考察目标达成能力。
    *   **LIBERO-Spatial**：考察空间关系处理。
    *   **LIBERO-Object**：考察对不同物体的操作。
*   **对比方法（Baselines）**：
    *   **BASE (K=8)**：短分块开环基准（高频调用大模型，较准但极慢）。
    *   **BASE (K=64)**：长分块开环基准（低频调用大模型，快但极易失败）。
    *   **Speculative Decoding (SpecVLA)**：传统的投机采样方法。

### 4. 资源与算力
*   **训练阶段**：使用了 **4 张 NVIDIA A100 GPU**。
*   **推理/验证阶段**：在 **1 张 NVIDIA V100 GPU** 上进行在线仿真测试，以确保公平的延迟对比。
*   **超参数**：使用 AdamW 优化器，学习率 $8 \times 10^{-4}$，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 LIBERO 的三大类任务，进行了大量的在线 Rollout 测试。
*   **消融实验**：非常充分，分别验证了：
    *   **上下文特征 $F_0$ 的必要性**：证明了来自大模型的全局意图对验证器的引导作用。
    *   **实时观测 $I_t$ 的必要性**：证明了闭环反馈是性能提升的核心。
    *   **重规划机制的有效性**：对比了直接执行验证器动作（不重规划）的效果。
    *   **阈值 $\tau$ 的敏感性分析**：探讨了效率与鲁棒性之间的权衡。
*   **客观性**：通过推理时间分解（Macro-Planner 耗时 1.373s vs Verifier 耗时 0.081s）客观展示了加速比的来源。

### 6. 主要结论与发现
*   **性能提升**：在长分块（K=64）设置下，SV-VLA 将平均成功率从 **79.5% 提升至 90.9%**。
*   **效率优势**：相比于短分块（K=8）基准，SV-VLA 实现了 **2.17 倍的加速**，同时保持了相近的成功率。
*   **鲁棒性**：定性分析显示，当机器人抓取失败时，SV-VLA 能通过验证器及时发现并触发重规划，而传统开环方法会继续执行错误动作导致任务彻底失败。

### 7. 优点（亮点）
*   **架构优雅**：巧妙地解决了投机采样在具身智能中“无法并行验证”的痛点，将其转化为“时序验证”。
*   **即插即用**：重型 VLA 模型和视觉编码器在训练验证器时保持冻结，适配成本极低。
*   **特征融合**：验证器不仅看当前图像，还利用了大模型的中间层特征（$F_0$），保证了验证过程与全局任务目标的一致性。

### 8. 不足与局限
*   **阈值依赖**：重规划阈值 $\tau$ 目前是手动设置的固定值，不同任务（如精细操作 vs 粗略移动）可能需要不同的阈值，缺乏自适应性。
*   **验证器能力上限**：验证器目前仅用于“触发重规划”，如果验证器本身能提供微小的局部动作修正（Local Correction），可能会进一步减少对重型模型的调用次数。
*   **环境覆盖**：实验主要在 LIBERO 仿真环境中完成，尚未在具有更复杂光照或物理干扰的真实世界场景中进行大规模验证。

（完）
