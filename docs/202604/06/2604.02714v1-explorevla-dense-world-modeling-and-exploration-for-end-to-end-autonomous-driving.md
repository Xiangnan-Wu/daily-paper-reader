---
title: "ExploreVLA: Dense World Modeling and Exploration for End-to-End Autonomous Driving"
title_zh: ExploreVLA：面向端到端自动驾驶的稠密世界建模与探索
authors: "Zihao Sheng, Xin Ye, Jingru Luo, Sikai Chen, Liu Ren"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02714v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于自动驾驶的稠密世界建模与VLA架构
tldr: 本研究针对端到端自动驾驶中VLA模型因模仿学习导致的分布外泛化性差的问题，提出了ExploreVLA框架。该框架通过统一的理解与生成架构，引入未来RGB和深度图像生成作为稠密世界建模目标，增强了模型的视觉几何表征。同时，利用预测不确定性作为内在奖励引导策略探索，并结合GRPO算法进行优化。实验表明，该方法在NAVSIM和nuScenes基准上达到了SOTA水平，显著提升了自动驾驶的规划能力。
selection_source: fresh_fetch
motivation: 传统的模仿学习限制了自动驾驶模型在未知场景下的探索能力，导致其在分布外场景中表现脆弱。
method: 提出一种结合RGB与深度图生成的稠密世界模型，并利用预测不确定性作为内在奖励，通过GRPO算法优化策略探索。
result: 在NAVSIM基准测试中取得了93.7的PDMS和88.8的EPDMS评分，刷新了当前最先进水平。
conclusion: 稠密世界建模与不确定性驱动的探索机制能有效提升端到端自动驾驶模型的鲁棒性和规划性能。
---

## 摘要
基于视觉-语言-动作（VLA）架构的端到端自动驾驶模型通过对专家演示进行行为克隆来学习驾驶策略，已展现出显著成果。然而，模仿学习本质上限制了模型仅能复制观察到的行为，而无法探索多样化的驾驶策略，导致其在面对新颖或分布外（OOD）场景时表现脆弱。强化学习（RL）通过支持超越专家分布的策略探索，提供了一种自然的补救方案。然而，通常在离线数据集上训练的 VLA 模型缺乏直接可观测的状态转移，因此需要一个学习到的世界模型来预测动作后果。在这项工作中，我们提出了一个统一的理解与生成框架，利用世界建模同时实现有意义的探索并提供稠密监督。具体而言，我们将轨迹预测扩展为包含未来 RGB 和深度图像生成的稠密世界建模目标，要求模型学习细粒度的视觉和几何表示，从而显著增强规划主干网络。除了作为监督信号外，该世界模型还进一步作为策略探索的内在奖励来源：其图像预测的不确定性自然地衡量了轨迹相对于训练分布的新颖性，其中高不确定性表示分布外场景，若这些场景是安全的，则代表了宝贵的学习机会。我们将这种探索信号整合到安全门控奖励中，并通过群体相对策略优化（GRPO）对策略进行优化。在 NAVSIM 和 nuScenes 基准测试上的实验证明了我们方法的有效性，在 NAVSIM 上实现了 93.7 的 SOTA PDMS 分数和 88.8 的 EPDMS 分数。代码和演示将在 https://zihaosheng.github.io/ExploreVLA/ 公开。

## Abstract
End-to-end autonomous driving models based on Vision-Language-Action (VLA) architectures have shown promising results by learning driving policies through behavior cloning on expert demonstrations. However, imitation learning inherently limits the model to replicating observed behaviors without exploring diverse driving strategies, leaving it brittle in novel or out-of-distribution scenarios. Reinforcement learning (RL) offers a natural remedy by enabling policy exploration beyond the expert distribution. Yet VLA models, typically trained on offline datasets, lack directly observable state transitions, necessitating a learned world model to anticipate action consequences. In this work, we propose a unified understanding-and-generation framework that leverages world modeling to simultaneously enable meaningful exploration and provide dense supervision. Specifically, we augment trajectory prediction with future RGB and depth image generation as dense world modeling objectives, requiring the model to learn fine-grained visual and geometric representations that substantially enrich the planning backbone. Beyond serving as a supervisory signal, the world model further acts as a source of intrinsic reward for policy exploration: its image prediction uncertainty naturally measures a trajectory's novelty relative to the training distribution, where high uncertainty indicates out-of-distribution scenarios that, if safe, represent valuable learning opportunities. We incorporate this exploration signal into a safety-gated reward and optimize the policy via Group Relative Policy Optimization (GRPO). Experiments on the NAVSIM and nuScenes benchmarks demonstrate the effectiveness of our approach, achieving a state-of-the-art PDMS score of 93.7 and an EPDMS of 88.8 on NAVSIM. The code and demo will be publicly available at https://zihaosheng.github.io/ExploreVLA/.

---

## 论文详细总结（自动生成）

### ExploreVLA：面向端到端自动驾驶的稠密世界建模与探索

#### 1. 核心问题与研究动机
*   **核心问题**：当前的端到端自动驾驶模型（如 VLA 架构）主要依赖**模仿学习（Behavior Cloning）**。这种范式存在两个致命缺陷：
    1.  **分布脆弱性**：模型只能复制专家行为，缺乏探索能力，在面对分布外（OOD）或新颖场景时表现极差。
    2.  **监督信号稀疏**：仅使用文本描述和轨迹坐标作为目标，无法捕捉驾驶场景中复杂的空间几何和细粒度视觉特征。
*   **研究背景**：强化学习（RL）虽能引导探索，但在自动驾驶中缺乏直接可观测的状态转移。因此，需要一个强大的“世界模型”来预测动作后果，并提供更丰富的学习信号。

#### 2. 方法论：统一理解与生成框架
*   **核心思想**：将自动驾驶任务转化为一个“理解+生成”的统一过程。模型不仅预测轨迹，还生成未来的 RGB 图像和深度图。
*   **关键技术细节**：
    *   **统一架构**：基于 Show-o 骨干网络，结合 Phi-1.5 (LLM) 和 MAGVIT-v2 (图像离散化标记器)，实现文本、图像和动作标记的统一处理。
    *   **稠密世界建模**：通过预测未来 $F$ 帧的 RGB 和深度图像，为模型提供像素级和几何级的稠密监督梯度，增强其对道路拓扑和物体距离的感知。
    *   **不确定性驱动的探索**：利用世界模型对未来图像预测的**信息熵（Entropy）**作为内在奖励。高熵意味着该轨迹产生的后果是模型未曾见过的（新颖性），从而鼓励模型在保证安全的前提下探索新策略。
    *   **安全门控奖励与 GRPO**：设计了一个复合奖励函数，只有当轨迹满足安全阈值（PDMS 分数）时，才会发放探索奖金。使用**群体相对策略优化（GRPO）**算法，通过采样多组候选轨迹并进行相对排名来优化策略。

#### 3. 实验设计
*   **数据集与场景**：
    *   **NAVSIM (v1 & v2)**：目前最先进的非反应式自动驾驶模拟基准，侧重于闭环指标评估。
    *   **nuScenes**：标准的开环规划评估数据集。
*   **Benchmark 与对比方法**：
    *   对比了包括 TransFuser、Hydra-MDP、DriveSuprim、AutoVLA、DriveVLA-W0 等在内的多种 SOTA 模型。
    *   评估指标包括 PDMS（预测驾驶员模型分数）、EPDMS（扩展 PDMS）、L2 误差和碰撞率。

#### 4. 资源与算力
*   **硬件环境**：实验在 **4 台 NVIDIA H200 GPU** 上运行。
*   **训练细节**：
    *   **第一阶段（模仿学习）**：预训练 10 个 epoch，有监督微调（SFT）15 个 epoch。
    *   **第二阶段（强化学习）**：使用 LoRA（秩为 32）进行 5 个 epoch 的后训练优化。
    *   **参数设置**：GRPO 组大小 $G=8$，学习率在 $3 \times 10^{-5}$ 到 $3 \times 10^{-6}$ 之间。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准上进行了详尽测试，并提供了大量的消融实验。
*   **消融实验**：分别验证了 RGB 生成、深度生成、PDMS 奖励以及图像探索奖励对最终性能的贡献。
*   **充分性评价**：实验设计较为充分且客观。通过对比“Best-of-N”策略和单次预测结果，展示了模型在不同配置下的稳定性。定性分析部分展示了模型如何纠正模仿学习中的典型错误（如闯红灯、碰撞风险），增强了说服力。

#### 6. 主要结论与发现
*   **性能突破**：ExploreVLA 在 NAVSIM v1 上达到了 **93.7 PDMS**，在 v2 上达到了 **88.8 EPDMS**，均刷新了 SOTA 纪录。
*   **世界模型的作用**：稠密监督（尤其是深度信息）显著提升了规划主干网络的表征能力。
*   **探索的价值**：基于不确定性的内在奖励能有效引导模型发现专家演示之外的成功策略，显著降低了安全关键场景下的失败率。

#### 7. 优点与亮点
*   **创新性探索机制**：巧妙地将世界模型的预测不确定性转化为 RL 的内在奖励，解决了自动驾驶中“如何定义有意义的探索”这一难题。
*   **多模态融合**：通过引入深度图生成，弥补了视觉模型在空间几何感知上的短板。
*   **高效优化**：采用 GRPO 算法，避免了传统 PPO 复杂的价值函数估计，提高了后训练阶段的效率。

#### 8. 不足与局限
*   **视角受限**：目前仅使用了前视单目摄像头，缺乏 360 度环视感知，这在处理侧向碰撞或复杂路口时可能存在盲区。
*   **计算开销**：尽管使用了 LoRA，但生成未来多帧图像和深度图在推理时会增加额外的计算负担。
*   **模拟器局限**：NAVSIM 是非反应式模拟器，虽然指标相关性强，但仍无法完全替代真实的闭环交互环境。

（完）
