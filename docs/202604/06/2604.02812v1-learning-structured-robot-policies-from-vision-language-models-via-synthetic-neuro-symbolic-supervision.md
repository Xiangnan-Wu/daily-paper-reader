---
title: Learning Structured Robot Policies from Vision-Language Models via Synthetic Neuro-Symbolic Supervision
title_zh: 通过合成神经符号监督从视觉语言模型中学习结构化机器人策略
authors: "Alessandro Adami, Tommaso Tubaldo, Marco Todescato, Ruggero Carli, Pietro Falco"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02812v1"
tags: ["query:robot"]
score: 8.0
evidence: 将视觉语言模型 (VLM) 专门化用于结构化机器人策略
tldr: 针对端到端机器人策略缺乏可解释性的问题，本文提出一种神经符号方法，利用视觉语言模型（VLM）从多模态输入中合成可执行的行为树策略。通过自动化流水线生成大规模合成数据集进行监督学习，使模型能够生成结构化且可解释的控制逻辑。实验证明，该方法在真实机器人任务中表现出色，为多模态决策提供了一种透明且模块化的替代方案。
selection_source: fresh_fetch
motivation: 现有的端到端视觉运动策略黑盒化严重且难以分析，限制了其在安全关键型机器人场景中的应用。
method: 提出一种神经符号框架，利用自动化流水线生成合成的多模态指令-策略数据集，训练VLM合成结构化的行为树策略。
result: 在两种机器人机械臂上的真实实验表明，完全基于合成数据训练的结构化策略能够成功迁移至物理系统并有效执行。
conclusion: 基础模型可以被适配为生成可解释的结构化机器人策略，为多模态机器人决策提供了一种优于不透明端到端方法的新途径。
---

## 摘要
视觉语言模型（VLM）最近在将多模态观测映射到机器人行为方面展现出了强大的能力。然而，目前大多数方法依赖于端到端的视觉运动策略，这些策略仍然是不透明且难以分析的，限制了它们在安全关键型机器人应用中的使用。相比之下，经典的机器人系统通常依赖于结构化的策略表示，这些表示提供了可解释性、模块化和反应式执行。本研究探讨了如何使基础模型专门化，以生成基于多模态感知的结构化机器人策略，从而架起高维学习与符号控制之间的桥梁。我们提出了一种神经符号方法，其中 VLM 根据视觉观测、自然语言指令和结构化系统规范合成可执行的行为树（Behavior Tree）策略。为了在无需人工标注的情况下实现可扩展的监督，我们引入了一个自动化流水线，该流水线生成了一个合成多模态数据集，其中包含领域随机化的场景以及由基础模型生成的指令-策略示例。在两台机器人机械臂上进行的真实世界实验表明，完全从合成监督中学习到的结构化策略能够成功迁移到物理系统。结果表明，基础模型可以经过调整以产生可解释且结构化的机器人策略，为多模态机器人决策提供了一种替代不透明端到端方法的新方案。

## Abstract
Vision-language models (VLMs) have recently demonstrated strong capabilities in mapping multimodal observations to robot behaviors. However, most current approaches rely on end-to-end visuomotor policies that remain opaque and difficult to analyze, limiting their use in safety-critical robotic applications. In contrast, classical robotic systems often rely on structured policy representations that provide interpretability, modularity, and reactive execution. This work investigates how foundation models can be specialized to generate structured robot policies grounded in multimodal perception, bridging high-dimensional learning and symbolic control. We propose a neuro-symbolic approach in which a VLM synthesizes executable Behavior Tree policies from visual observations, natural language instructions, and structured system specifications. To enable scalable supervision without manual annotation, we introduce an automated pipeline that generates a synthetic multimodal dataset of domain-randomized scenes paired with instruction-policy examples produced by a foundation model. Real-world experiments on two robotic manipulators show that structured policies learned entirely from synthetic supervision transfer successfully to physical systems. The results indicate that foundation models can be adapted to produce interpretable and structured robot policies, providing an alternative to opaque end-to-end approaches for multimodal robot decision making.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Structured Robot Policies from Vision-Language Models via Synthetic Neuro-Symbolic Supervision》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的机器人视觉运动策略大多采用“端到端”的黑盒模型，虽然表现强大，但缺乏可解释性、难以分析且难以施加安全约束，这限制了其在安全关键型任务中的应用。
*   **研究动机**：传统的机器人控制依赖于**行为树（Behavior Trees, BTs）**等结构化表示，具有模块化、可解释和反应式执行的优点。本文旨在结合视觉语言模型（VLM）的高维感知能力与符号控制的逻辑透明性，探索如何让基础模型生成可执行的结构化策略。
*   **整体含义**：提出一种神经符号（Neuro-Symbolic）框架，通过合成数据监督，使模型学会从视觉和指令中直接合成符合逻辑规范的行为树，从而替代不透明的端到端动作输出。

### 2. 方法论
*   **核心思想**：利用大型基础模型作为“教师”，自动生成大规模合成数据集（包含图像、指令和对应的行为树），随后微调一个较小的开源VLM（Pixtral-12B），使其掌握生成结构化机器人策略的能力。
*   **关键技术细节**：
    *   **自动化流水线**：使用MuJoCo物理引擎生成1万个随机桌面场景，提取对象元数据（位姿、颜色等）。
    *   **合成监督**：利用Gemini模型根据场景元数据和图像生成自然语言指令及对应的JSON格式行为树。
    *   **结构化约束**：在生成行为树时强制执行三项原则：**反应式守护**（动作前必有条件检查）、**空间偏移**（自动计算接近位姿的z轴偏移）和**扁平化层级**（防止逻辑过深导致执行错误）。
    *   **原子基元分解**：将复杂任务分解为`MovePose`、`OpenGripper`、`is_grasped`等原子动作和条件检查。
*   **算法流程**：输入（RGB图像 + 自然语言指令 + 系统规范/元数据） $\rightarrow$ 微调后的VLM $\rightarrow$ 输出（JSON格式的可执行行为树） $\rightarrow$ 机器人解释器执行。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：10,000个由MuJoCo生成的桌面操作场景。
    *   **真实场景**：在物理实验室环境下，使用随机摆放的几何体（立方体、圆柱体等）进行零样本（Zero-Shot）迁移测试。
*   **Benchmark与对比方法**：
    *   **对比模型**：GPT-5（文中指代未来/高性能版本）、Gemini 3 Flash、原始Pixtral-12B。
    *   **评估指标**：任务成功率（TSR）、JSON合法率（JVR）、键值错误率（KER）、树深度、推理延迟等。
*   **消融实验**：针对LoRA秩（Rank）、微调模块（仅Attention或包含MLP）、上下文窗口长度、学习率、数据集规模等进行了17组变体实验。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A40 GPU (48 GiB VRAM)**。
*   **训练细节**：
    *   采用8位量化（8-bit Paged AdamW）和Bfloat16精度。
    *   训练时长：主模型（A1）训练约 **15.12小时**。
    *   上下文窗口限制为4400 tokens，以防止显存溢出。
*   **推理延迟**：平均生成一个完整行为树的时间约为60秒左右（取决于模型变体）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：共17组（A1-A17），系统地分析了超参数对逻辑合成的影响，实验设计非常详尽。
    *   **真实验证**：在两台不同的工业机器人（**Franka Emika Panda** 和 **UR5e**）上进行了验证。
*   **充分性评价**：实验设计较为充分且客观。通过跨平台（Panda vs UR5e）的验证证明了策略的硬件无关性；通过消融实验找出了VLM合成逻辑树的关键瓶颈（如MLP层对符号推理的重要性）。

### 6. 主要结论与发现
*   **合成数据有效性**：完全在合成数据上训练的模型，能够成功实现“零样本逻辑迁移”，在真实世界图像中准确识别空间关系并生成正确策略。
*   **模型规模与能力**：经过专门微调的12B参数模型在结构化合成任务上可以媲美甚至超越更大规模的闭源模型（如GPT-5），因为后者在零样本下难以严格遵守复杂的JSON-BT架构规范。
*   **结构化先验**：模型不仅是记忆模板，还学会了空间算术（如自动计算物体的上方偏移位姿）。

### 7. 优点
*   **无需人工标注**：通过自动化流水线解决了机器人领域高质量标注数据匮乏的问题。
*   **高可解释性**：输出的行为树可以被人类阅读、审计，并在执行前进行语法验证，极大地提升了安全性。
*   **硬件无关性**：生成的符号策略不依赖特定机器人的逆运动学，同一套逻辑可直接运行在不同品牌的机械臂上。

### 8. 不足与局限
*   **语义泛化瓶颈（Pattern Backsliding）**：对于训练集中未出现的高级语义指令（如“交换两个物体的位置”），模型虽然能识别物体，但无法自主推导出需要中转点的复杂逻辑。
*   **功能接地间隙（Functional Grounding Gap）**：模型对新工具（如从夹爪换成吸盘）的物理特性缺乏零样本理解，容易在错误的逻辑顺序下触发动作。
*   **推理延迟**：目前生成一个行为树需要约1分钟，对于需要极高实时响应的动态环境可能存在挑战。

（完）
