---
title: "ScoRe-Flow: Complete Distributional Control via Score-Based Reinforcement Learning for Flow Matching"
title_zh: ScoRe-Flow：通过基于分数的强化学习实现流匹配的全分布控制
authors: "Xiaotian Qiu, Lukai Chen, Jinhao Li, Qi Sun, Cheng Zhuo, Guohao Dai"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10962v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于机器人控制和强化学习微调的流匹配策略
tldr: Flow Matching (FM) 在机器人控制中表现出色，但受限于模仿学习的数据质量。本文提出 ScoRe-Flow，一种基于得分的强化学习微调方法。该方法利用速度场推导出的得分函数来调节漂移项，并结合可学习的方差预测，实现了对随机转换均值和方差的解耦控制。实验表明，ScoRe-Flow 在 D4RL 任务中收敛速度提升 2.4 倍，在操作任务中显著提高了成功率。
selection_source: fresh_fetch
motivation: 现有的流匹配强化学习方法仅靠噪声注入进行探索，效率较低且难以充分利用演示数据的先验分布。
method: 通过速度场计算得分函数来调节漂移项，并结合学习到的方差预测，实现对随机策略分布的精确控制。
result: "在 D4RL 任务上收敛速度比现有 SOTA 快 2.4 倍，在 Robomimic 和 Franka Kitchen 任务中成功率提升高达 5.4%。"
conclusion: ScoRe-Flow 通过得分函数增强了流匹配策略的探索稳定性与效率，是提升具身智能系统性能的有效微调方案。
---

## 摘要
流匹配 (Flow Matching, FM) 策略已成为机器人控制的高效骨干架构，其提供的快速且具有表现力的动作生成为近期的大规模具身智能系统奠定了基础。然而，通过模仿学习训练的 FM 策略继承了演示数据的局限性；要超越次优行为，需要进行强化学习 (RL) 微调。近期的方法通过可学习的噪声注入将确定性流转换为随机微分方程 (SDE)，从而实现探索和可计算的似然度，但当演示数据已经提供强先验时，这种仅靠噪声的控制可能会损害训练效率。我们观察到，通过分数函数（即对数密度的梯度）调节漂移项，可以将探索引导至高概率区域，从而提高稳定性。该分数可以从速度场中获得闭式表达式，无需辅助网络。基于此，我们提出了 ScoRe-Flow，这是一种基于分数的 RL 微调方法，它将漂移调节与学习到的方差预测相结合，实现了对随机转移的均值和方差的解耦控制。实验表明，在 D4RL 运动任务中，ScoRe-Flow 的收敛速度比基于流的现有最先进 (SOTA) 方法快 2.4 倍，在 Robomimic 和 Franka Kitchen 操纵任务中，成功率最高提升了 5.4%。

## Abstract
Flow Matching (FM) policies have emerged as an efficient backbone for robotic control, offering fast and expressive action generation that underpins recent large-scale embodied AI systems. However, FM policies trained via imitation learning inherit the limitations of demonstration data; surpassing suboptimal behaviors requires reinforcement learning (RL) fine-tuning. Recent methods convert deterministic flows into stochastic differential equations (SDEs) with learnable noise injection, enabling exploration and tractable likelihoods, but such noise-only control can compromise training efficiency when demonstrations already provide strong priors. We observe that modulating the drift via the score function, i.e., the gradient of log-density, steers exploration toward high-probability regions, improving stability. The score admits a closed-form expression from the velocity field, requiring no auxiliary networks. Based on this, we propose ScoRe-Flow, a score-based RL fine-tuning method that combines drift modulation with learned variance prediction to achieve decoupled control over the mean and variance of stochastic transitions. Experiments demonstrate that ScoRe-Flow achieves 2.4x faster convergence than flow-based SOTA on D4RL locomotion tasks and up to 5.4% higher success rates on Robomimic and Franka Kitchen manipulation tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ScoRe-Flow** 的新型强化学习（RL）微调框架，旨在优化用于机器人控制的流匹配（Flow Matching, FM）策略。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：流匹配（FM）因其比扩散模型（Diffusion Models）更快的采样速度（通常快 4-10 倍）和极强的表达能力，正成为大规模机器人系统的核心。
*   **问题**：FM 策略通常通过模仿学习（IL）训练，受限于演示数据的质量。虽然 RL 微调可以超越演示者，但 FM 的确定性 ODE 动力学导致动作似然度难以定义，无法直接应用策略梯度方法。
*   **动机**：现有方法（如 ReinFlow）通过注入可学习噪声将 ODE 转换为 SDE，但这种“仅噪声”的控制只扰动位置而不调节动力学，导致探索效率低下。作者观察到，利用“分数函数”（Score Function）调节漂移项（Drift）可以引导策略向高概率区域探索，从而提高稳定性和收敛速度。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用分数函数与速度场之间的对偶关系，实现对随机转移过程的**均值（通过分数调节漂移）和方差（通过预测网络调节噪声）的解耦控制**。
*   **关键技术细节**：
    *   **闭式分数推导**：基于线性流路径的特性，推导出分数函数 $s_t$ 的闭式表达式：$s_t(a) = \frac{t v_\theta - a}{1-t}$。这意味着**无需额外辅助网络**即可获得分数信息，计算开销极低。
    *   **解耦控制 SDE**：构建新的随机微分方程：$da_t = [v_\theta(t, a_t, s) + \alpha_{scaled}(t) \cdot s_t(a_t)] dt + \sigma_\phi(t, a_t, s) dW_t$。
    *   **分数调度器 ($\alpha_\psi$)**：一个轻量级 MLP，用于调节分数引导的强度，并带有硬性的时间衰减约束（$1-t$），以防止在 $t \to 1$ 时分数发散导致数值不稳定。
    *   **方差预测器 ($\sigma_\phi$)**：独立预测每个步骤的探索噪声强度。
    *   **算法流程**：在采样过程中，通过 Euler-Maruyama 方案离散化 SDE，计算每一步的均值和方差，从而得到完整的轨迹对数似然，进而使用 PPO 算法进行全参数微调。

### 3. 实验设计
*   **数据集/场景**：
    *   **D4RL Locomotion**：Hopper, Walker2d, Ant, Humanoid（测试高效步态学习）。
    *   **Robomimic**：PickPlaceCan, Square, Transport（基于视觉的高精度操作）。
    *   **Franka Kitchen**：多任务顺序操作（测试长程决策和稀疏奖励下的表现）。
*   **Benchmark 与对比方法**：
    *   **ReinFlow (R/S)**：目前流匹配 RL 微调的最先进方法（SOTA）。
    *   **DPPO**：基于扩散策略的 PPO 微调方法。
    *   **FQL**：流 Q 学习（离线方法）。
    *   **Score-SDE**：本文的消融版本（均值与方差耦合，不使用学习的方差）。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA RTX 3090 GPU** 上进行（单卡顺序测量），对于复杂的 Robomimic Transport 任务使用了 **A100 GPU**。
*   **渲染**：使用 EGL 后端加速 GPU 渲染。
*   **时长**：在 D4RL 任务中，ScoRe-Flow 达到 90% 性能的平均时间仅为 **0.16 小时**（约 10 分钟），而 DPPO 平均需要 3.48 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从状态输入到视觉输入、从稠密奖励到稀疏奖励、从单臂到双臂协作的多种任务。
*   **充分性**：
    *   所有实验均运行了 **3 到 5 个随机种子**，并提供了均值和标准差。
    *   进行了详细的**消融实验**，验证了分数项、调度器、解耦控制以及采样步数（K）对性能的影响。
    *   使用了 Welch's t-test 进行统计显著性检验，证明了结果的客观性。

### 6. 主要结论与发现
*   **收敛速度**：在相同采样步数下，ScoRe-Flow 的收敛速度比 ReinFlow 快 **2.4 倍**。
*   **性能提升**：在 Robomimic 和 Franka Kitchen 任务中，成功率比现有方法高出 **5.4%**。
*   **稳定性**：分数引导的漂移调节显著增强了训练稳定性，防止策略漂移到低密度区域。
*   **效率**：相比扩散模型（DPPO），由于采样步数极少（通常 K=4），实现了高达 **21.9 倍** 的墙钟时间加速。

### 7. 优点
*   **理论优雅**：利用分数-速度对偶性，无需训练额外的分数估计网络，降低了复杂度。
*   **解耦控制**：首次在流匹配 RL 中实现了均值和方差的独立调节，提供了更精细的分布控制。
*   **即插即用**：可以轻松集成到现有的预训练流匹配模型中，且计算开销几乎可以忽略不计。

### 8. 不足与局限
*   **路径依赖**：闭式分数表达式依赖于**线性插值路径**和高斯源分布。如果使用非线性流（如黎曼流匹配）或非高斯先验，该公式将失效，需要额外的分数估计。
*   **超参数敏感性**：虽然比耦合方法更稳健，但仍需调节分数调度器和方差预测器的初始范围。
*   **应用范围**：目前主要验证了机器人操纵和运动任务，在更广泛的决策任务（如语言模型推理）中的表现尚待探索。

（完）
