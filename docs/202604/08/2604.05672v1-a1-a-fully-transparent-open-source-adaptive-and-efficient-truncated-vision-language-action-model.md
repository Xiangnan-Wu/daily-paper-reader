---
title: "A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model"
title_zh: A1：一个完全透明、开源、自适应且高效的截断式视觉-语言-动作模型
authors: "Kaidong Zhang, Jian Zhang, Rongtao Xu, Yu Sun, Shuoshuo Xue, Youpeng Wen, Xiaoyu Guo, Minghao Guo, Weijia Liufu, Liu Zihou, Kangyi Ji, Yangsong Zhang, Jiarun Zhu, Jingzhi Liu, Zihang Li, Ruiyi Chen, Meng Cao, Jingming Zhang, Shen Zhao, Xiaojun Chang, Feng Zheng, Ivan Laptev, Xiaodan Liang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05672v1"
tags: ["query:robot"]
score: 10.0
evidence: 完全开源且高效的截断式VLA模型
tldr: A1是一个开源透明的视觉-语言-动作（VLA）模型框架，旨在解决现有模型推理成本高、延迟大的问题。它通过预训练VLM提供先验，并引入预算感知的自适应推理方案，包括中间层动作一致性监测触发的提前终止机制和跨层截断流匹配技术。在多个仿真和真实机器人基准测试中，A1在显著降低计算开销的同时，实现了超越pi0等主流模型的SOTA性能。
selection_source: fresh_fetch
motivation: 现有的VLA模型由于庞大的骨干网络和复杂的动作生成头，导致推理延迟高且计算成本昂贵，难以在普通硬件上实现实时控制。
method: 提出了一种自适应推理方案，通过监测中间层动作一致性实现提前终止，并利用跨层截断流匹配技术减少去噪迭代次数。
result: "在RoboChallenge等多个基准测试中，A1以更低的推理成本（最高减少76.6%计算量）实现了29.00%的平均成功率，优于pi0和RDT-1B等基准模型。"
conclusion: A1证明了通过优化推理流程和利用VLM中间层信息，可以在不牺牲操作成功率的前提下大幅提升VLA模型的效率和实时性。
---

## 摘要
视觉-语言-动作（VLA）模型已成为开放世界机器人操控的一种强大范式，但其实际部署往往受限于成本：十亿级参数的 VLM 主干网络以及基于迭代扩散/流（diffusion/flow-based）的动作头会产生高延迟和高计算量，使得在通用硬件上进行实时控制变得昂贵。我们提出了 A1，这是一个完全开源且透明的 VLA 框架，旨在实现低成本、高吞吐量的推理，且不牺牲操控成功率。我们的方法利用预训练的 VLM 为动作生成提供隐式的可操作性（affordance）先验。我们发布了完整的训练栈（包括训练代码、数据/数据处理流水线、中间检查点和评估脚本），以实现端到端的复现。除了单独优化 VLM，A1 还通过引入一种预算感知的自适应推理方案来针对整个推理流水线，该方案能同时加速主干网络和动作头。具体而言，我们通过监测 VLM 中间层的动作一致性来触发提前终止，并提出了层间截断流匹配（Inter-Layer Truncated Flow Matching），该方法可以在不同层之间热启动去噪过程，从而以显著减少的有效去噪迭代次数实现精确的动作生成。在仿真基准测试（LIBERO、VLABench）和真实机器人（Franka、AgiBot）上，A1 在显著降低推理成本的同时（例如，流匹配推理的每回合延迟降低高达 72%，主干网络计算量减少高达 76.6%，且性能下降极小），实现了最先进的成功率。在 RoboChallenge 上，A1 实现了 29.00% 的平均成功率，优于包括 pi0 (28.33%)、X-VLA (21.33%) 和 RDT-1B (15.00%) 在内的基准模型。

## Abstract
Vision--Language--Action (VLA) models have emerged as a powerful paradigm for open-world robot manipulation, but their practical deployment is often constrained by \emph{cost}: billion-scale VLM backbones and iterative diffusion/flow-based action heads incur high latency and compute, making real-time control expensive on commodity hardware. We present A1, a fully open-source and transparent VLA framework designed for low-cost, high-throughput inference without sacrificing manipulation success; Our approach leverages pretrained VLMs that provide implicit affordance priors for action generation. We release the full training stack (training code, data/data-processing pipeline, intermediate checkpoints, and evaluation scripts) to enable end-to-end reproducibility. Beyond optimizing the VLM alone, A1 targets the full inference pipeline by introducing a budget-aware adaptive inference scheme that jointly accelerates the backbone and the \emph{action head}. Specifically, we monitor action consistency across intermediate VLM layers to trigger early termination, and propose Inter-Layer Truncated Flow Matching that warm-starts denoising across layers, enabling accurate actions with substantially fewer effective denoising iterations. Across simulation benchmarks (LIBERO, VLABench) and real robots (Franka, AgiBot), A1 achieves state-of-the-art success rates while significantly reducing inference cost (e.g., up to 72% lower per-episode latency for flow-matching inference and up to 76.6% backbone computation reduction with minor performance degradation). On RoboChallenge, A1 achieves an average success rate of 29.00%, outperforming baselines including pi0(28.33%), X-VLA (21.33%), and RDT-1B (15.00%).

---

## 论文详细总结（自动生成）

这是一份关于论文《A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然在开放世界泛化上表现出色，但部署成本极高。其瓶颈在于：1) 数十亿参数的 VLM 主干网络推理慢；2) 扩散或流匹配（Flow Matching）动作头需要多次迭代去噪，导致高延迟。
*   **核心目标**：开发一个完全开源、透明且高效的 VLA 框架，在保证操作成功率的同时，通过自适应推理大幅降低计算开销，实现低成本硬件上的实时控制。

### 2. 核心方法论
A1 模型由 VLM 主干（基于 Molmo-7B）和动作头（FM 或 MLP）组成，其核心创新在于**预算感知的自适应推理方案**：
*   **动作一致性提前终止（Early-Exit）**：
    *   在推理时，模型不再强制运行 VLM 的所有层（共 28 层）。
    *   通过监测中间层输出的动作块（Action Chunk）是否趋于稳定（计算相邻层动作的差异值 $\Delta$），若差异低于预设阈值 $\eta_i$，则提前停止主干网络的计算。
*   **层间截断流匹配（Inter-Layer Truncated Flow Matching, ITFM）**：
    *   针对流匹配动作头，传统方法每层都要从随机噪声开始多次迭代。
    *   ITFM 提出“热启动”机制：每层仅进行极少量去噪步数（如 $\delta=2$），并将当前层的输出作为下一层的初始值，实现了去噪过程在 VLM 层间的连续演进，极大地减少了总迭代次数。
*   **多出口训练（Multi-exit Training）**：在训练阶段，对 VLM 的所有中间层输出的动作进行监督，确保每一层都具备预测动作的能力。

### 3. 实验设计
*   **数据集**：
    *   **开源数据**：DROID, AgiBot, RoboCOIN, RoboMind, GM-100, RoboChallenge 等。
    *   **自有数据**：收集了 15,951 条涵盖 ARX, Franka, UR5, AgiBot 等机器人的真实轨迹。
*   **基准测试（Benchmarks）**：
    *   **仿真**：LIBERO（考察长程、空间、物体操作）、VLABench。
    *   **真实世界**：在 Franka, AgiBot, 武界（WuJie）, Dobot 四种平台上进行 7 类任务测试。
    *   **综合评测**：RoboChallenge Table30（包含 30 个真实机器人任务）。
*   **对比方法**：$\pi_0$、$\pi_{0.5}$、OpenVLA、X-VLA、RDT-1B、Octo 等主流 VLA 模型。

### 4. 资源与算力
*   **模型规格**：主干为 Molmo-7B（Qwen2-7B + SigLIP），动作头为 Qwen3-400M。
*   **训练配置**：预训练阶段 Batch Size 为 1024，总步数 200K；微调阶段根据任务不同设置 Batch Size（32-128）。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长，但详细记录了推理时的 GFLOPs（如 A1-FM 正常推理约 11.13 TFLOPs）。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验包含数千次试验；真实世界实验涵盖 4 个机器人平台，收集了超过 3000 条轨迹，每个任务测试 10 次。
*   **消融实验**：针对提前终止的阈值 $c$、去噪步数 $\delta$、热启动机制的有效性进行了详尽的对比。
*   **充分性评价**：实验设计非常全面，不仅覆盖了标准仿真库，还通过 RoboChallenge 这一大规模真实机器人基准证明了其泛化性。对比对象包含了目前最顶尖的开源和半开源模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：在 RoboChallenge 上以 29.00% 的成功率位列开源模型前列，优于 $\pi_0$ (28.33%) 和 RDT-1B (15.00%)。
*   **极高效率**：通过提前终止机制，在仅损失极小性能的情况下，可减少高达 **76.6%** 的主干网络计算量。
*   **推理加速**：引入 ITFM 后，流匹配推理的每回合延迟降低了 **72%**（从 37.8s 降至 10.5s）。
*   **冗余性发现**：实验证明 VLM 的深层计算在许多简单动作中是冗余的，模型能自适应地在复杂操作时进入深层，简单操作时早停。

### 7. 优点
*   **全栈透明**：承诺开源模型权重、训练代码、数据处理流水线和评估脚本，极大地推动了社区的复现工作。
*   **端到端优化**：不同于只优化 VLM 或只优化动作头的方案，A1 实现了主干与动作头的联合加速。
*   **部署友好**：通过大幅降低延迟，使得 7B 规模的 VLA 模型在普通硬件上进行实时闭环控制成为可能。

### 8. 不足与局限
*   **数据依赖**：预训练仍依赖于带标签的可操作性（Affordance）数据，这限制了数据规模的进一步自动化扩张。
*   **模仿学习通病**：主要基于模仿学习，存在复合误差问题，在极高精度要求的任务中可能仍需强化学习辅助。
*   **硬件延迟**：虽然模型推理变快了，但云端服务器与本地机器人之间的网络延迟仍是实际部署中影响流畅度的因素。

（完）
