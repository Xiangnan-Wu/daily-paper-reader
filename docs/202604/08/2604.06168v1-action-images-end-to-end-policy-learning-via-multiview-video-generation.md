---
title: "Action Images: End-to-End Policy Learning via Multiview Video Generation"
title_zh: 动作图像：通过多视图视频生成的端到端策略学习
authors: "Haoyu Zhen, Zixian Gao, Qiao Sun, Yilin Zhao, Yuncong Yang, Yilun Du, Tsun-Hsuan Wang, Yi-Ling Qiao, Chuang Gan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06168v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人策略学习的统一世界动作模型
tldr: Action Images 提出了一种将机器人策略学习转化为多视图视频生成的统一世界动作模型。该方法不使用低维动作 token，而是将 7 自由度动作转化为像素级、可解释的“动作图像”，使视频骨干网络能直接作为零样本策略。该模型支持视频-动作联合生成和标注，在 RLBench 和真实场景中表现出卓越的零样本成功率和生成质量，为策略学习提供了新路径。
selection_source: fresh_fetch
motivation: 现有的世界动作模型依赖独立的动作模块或非像素对齐的表示，难以充分利用预训练视频模型的知识并限制了跨环境迁移。
method: 将机器人动作转化为多视图像素级“动作图像”，通过多视图视频生成框架实现端到端的策略学习与预测。
result: 在 RLBench 和真实世界评估中，该模型实现了最强的零样本成功率，并显著提升了视频与动作联合生成的质量。
conclusion: 将动作表示为可解释的像素级图像是提升机器人策略学习泛化能力和生成质量的一种极具前景的方法。
---

## 摘要
世界动作模型 (WAMs) 已成为机器人策略学习的一个极具前景的方向，因为它们可以利用强大的视频骨干网络来建模未来状态。然而，现有方法通常依赖于独立的动作模块，或者使用非像素对齐 (pixel-grounded) 的动作表示，这使得充分利用视频模型的预训练知识变得困难，并限制了在不同视角和环境之间的迁移。在这项工作中，我们提出了 Action Images，这是一种统一的世界动作模型，将策略学习表述为多视图视频生成。我们没有将控制编码为低维 token，而是将 7 自由度 (7-DoF) 机器人动作转化为可解释的动作图像：即在 2D 像素中对齐并显式跟踪机械臂运动的多视图动作视频。这种像素对齐的动作表示允许视频骨干网络本身充当零样本 (zero-shot) 策略，而无需独立的策略头或动作模块。除了控制之外，同一个统一模型还支持在共享表示下的视频-动作联合生成、动作条件视频生成以及动作标注。在 RLBench 和真实世界评估中，我们的模型实现了最强的零样本成功率，并比之前的视频空间世界模型提高了视频-动作联合生成的质量，这表明可解释的动作图像是通往策略学习的一条极具前景的路径。

## Abstract
World action models (WAMs) have emerged as a promising direction for robot policy learning, as they can leverage powerful video backbones to model the future states. However, existing approaches often rely on separate action modules, or use action representations that are not pixel-grounded, making it difficult to fully exploit the pretrained knowledge of video models and limiting transfer across viewpoints and environments. In this work, we present Action Images, a unified world action model that formulates policy learning as multiview video generation. Instead of encoding control as low-dimensional tokens, we translate 7-DoF robot actions into interpretable action images: multi-view action videos that are grounded in 2D pixels and explicitly track robot-arm motion. This pixel-grounded action representation allows the video backbone itself to act as a zero-shot policy, without a separate policy head or action module. Beyond control, the same unified model supports video-action joint generation, action-conditioned video generation, and action labeling under a shared representation. On RLBench and real-world evaluations, our model achieves the strongest zero-shot success rates and improves video-action joint generation quality over prior video-space world models, suggesting that interpretable action images are a promising route to policy learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Action Images: End-to-End Policy Learning via Multiview Video Generation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人世界模型（World Models）在视频预测上表现出色，但难以将其预测能力转化为强大的策略泛化能力。
*   **研究动机**：作者认为视频泛化与策略泛化之间存在鸿沟，原因在于动作（Action）通常被表示为低维 token 或非空间对齐的潜变量，这导致模型无法充分利用预训练视频骨干网络的空间推理知识。
*   **整体含义**：提出将 7 自由度（7-DoF）动作转化为“动作图像”（Action Images），使动作与观测图像在同一像素空间内对齐，从而将策略学习统一为多视图视频生成任务。

### 2. 方法论
*   **核心思想**：将机器人动作编码为可解释的、像素对齐的多视图视频流，使视频骨干网络无需额外策略头即可直接输出控制指令。
*   **关键技术细节**：
    *   **动作转图像（Action-as-Images）**：将 7-DoF 动作（位置、姿态、夹爪状态）转换为 3 个语义 3D 点（位置点、法线点、向上点）。
    *   **多视图渲染**：利用相机参数将 3D 点投影到 2D 像素平面，渲染为 RGB 高斯热图。红色通道代表位置，绿色代表法线，蓝色通道同时编码“向上点”和夹爪开合度（在低响应区域注入信号）。
    *   **统一模型训练**：基于预训练的 Wan 2.2 视频生成模型，将观测视频和动作视频在时间轴上拼接。采用多掩码（Multi-mask）策略，支持联合生成、动作条件生成、动作标注等多种任务。
    *   **动作解码**：通过主视图热图锚点结合侧视图射线投射（Ray Casting），从生成的 2D 热图中还原 3D 语义点，进而恢复 7-DoF 连续动作。

### 3. 实验设计
*   **数据集与场景**：
    *   **RLBench**：用于仿真环境下的任务评估（包括领域内和零样本任务）。
    *   **DROID & BridgeV2**：提供大规模真实世界机器人操作数据。
    *   **真实世界测试**：在 xArm 机器人上针对未见过的物体和环境进行零样本评估。
*   **对比方法（Baselines）**：
    *   **VLA 模型**：如 $\pi_{0.5}$、MolmoAct。
    *   **扩散策略**：MV-Policy（多视图扩散策略）。
    *   **世界模型**：TesserAct、Cosmos-Policy（这些模型通常使用非像素对齐的动作表示）。

### 4. 资源与算力
*   **算力投入**：使用了 **32 台 NVIDIA A100 GPU** 进行训练。
*   **训练细节**：采用 DeepSpeed ZeRO 优化，混合精度训练，总计训练了 **100,000 个优化步数**。
*   **推理优化**：通过并行化、缓存和 `torch.compile` 等技术，将视频骨干网络的推理速度提升至最高 71 FPS。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 RLBench 的 10+ 种任务、真实世界 5 种以上复杂场景。
*   **实验维度**：
    *   **零样本策略评估**：验证跨环境泛化能力。
    *   **联合生成质量**：评估视频与动作预测的准确性（PSNR, SSIM, FVD 等指标）。
    *   **消融与扩展实验**：包括动作条件视频生成、视频到动作的标注任务。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还深入分析了生成质量和跨模态标注能力，实验结果在统计上具有说服力。

### 6. 主要结论与发现
*   **像素对齐是关键**：将动作表示为图像显著提升了策略的零样本泛化能力，尤其是在未见过的物体和环境中。
*   **统一表示的优势**：同一个模型在不改变架构的情况下，能够同时胜任控制、预测和标注任务，证明了视频空间是机器人学习的通用表征空间。
*   **多视图一致性**：多视图动作图像能有效解决单视图下的深度歧义，提高 3D 动作还原的精度。

### 7. 优点
*   **架构优雅**：消除了独立的策略头，使视频模型本身成为策略，极大简化了系统复杂性。
*   **可解释性强**：动作以热图形式呈现，人类可以直接观察模型“计划”移动到哪里。
*   **泛化性强**：通过像素级对齐，能够更好地继承大规模视频预训练模型的空间常识。

### 8. 不足与局限
*   **开环限制**：目前主要展示的是开环（Open-loop）执行结果，尚未完全实现高频闭环控制。
*   **推理延迟**：尽管经过优化，大型视频生成模型的实时推理对于某些高动态任务仍具挑战。
*   **数据依赖**：模型性能高度依赖于高质量的相机标定数据，对于标定缺失或噪声严重的真实数据处理能力有待进一步验证。

（完）
