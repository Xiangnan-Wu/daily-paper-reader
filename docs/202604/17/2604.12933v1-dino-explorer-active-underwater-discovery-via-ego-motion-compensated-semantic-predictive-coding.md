---
title: "DINO-Explorer: Active Underwater Discovery via Ego-Motion Compensated Semantic Predictive Coding"
title_zh: DINO-Explorer：通过自我运动补偿的语义预测编码实现主动水下发现
authors: "Yuhan Jin, Nayari Marie Lessa, Mariela De Lucas Alvarez, Melvin Laux, Lucas Amparo Barbosa, Frank Kirchner, Rebecca Adam"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12933v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于自主智能体预测短时序列的动作条件循环预测器
tldr: 针对水下自主航行器（AUV）被动记录导致的高价值瞬时事件遗漏问题，本文提出DINO-Explorer框架。该框架利用DINOv3基础模型的语义潜空间，结合动作条件的循环预测器和自我运动补偿模块，生成连续的语义“惊喜”信号。通过主动感知和异步事件筛选，该系统在显著降低遥测带宽的同时，能精准捕捉并优先传输具有科学价值的新奇事件，实现了高效的水下主动探索。
selection_source: fresh_fetch
motivation: 传统水下航行器多为被动数据记录器，难以实时识别并捕捉具有高科学价值的瞬时新奇事件。
method: 提出一种基于DINOv3语义潜空间的预测编码框架，通过动作条件预测和光流补偿来消除自我运动干扰，提取环境新奇度信号。
result: "实验显示该系统在减少48.2%遥测带宽的情况下，保留了78.8%的人工验证事件，并将误报率降低了45.5%。"
conclusion: DINO-Explorer通过语义预测和运动补偿实现了高效的主动感知，为带宽受限环境下的自主科学发现提供了强有力的技术支撑。
---

## 摘要
海洋生态系统的退化使得持续且具有科学选择性的水下监测变得至关重要。然而，大多数自主水下航行器（AUV）仅作为被动数据记录器运行，捕获详尽的视频供离线审查，且经常错过具有高科学价值的瞬态事件。向主动感知的转变需要一种因果性的在线信号，该信号能够在抑制由机动引起的视觉变化的同时，突出显著现象。我们提出了 DINO-Explorer，这是一个由连续语义惊奇信号驱动的新颖性感知框架。该框架在冻结的 DINOv3 基础模型的潜空间内运行，利用轻量级的动作条件循环预测器来预测短时域的语义演化。一个受传出拷贝启发的模块利用全局池化光流来抵消自身引起的视觉变化，而不会抑制真实的外部环境新颖性。我们在不同遥测约束下的异步事件分选这一下游任务上评估了该信号。结果表明，DINO-Explorer 提供了一种鲁棒且带宽高效的注意力机制。在固定工作点下，该系统保留了 78.8% 的发现后经人类审查员共识确认的事件，触发确认率为 56.8%，有效地提取了与任务相关的现象。至关重要的是，相对于未补偿的惊奇信号基准，自我运动调节抑制了 45.5% 的误报。在回放侧的帕累托消融研究中，DINO-Explorer 在验证的峰值 F1 分数与遥测带宽前沿上表现出稳健的优势，在选定的工作点将遥测带宽降低了 48.2%，同时保持了 62.2% 的峰值 F1 分数，成功地将数据传输集中在经人类验证的新颖性事件周围。

## Abstract
Marine ecosystem degradation necessitates continuous, scientifically selective underwater monitoring. However, most autonomous underwater vehicles (AUVs) operate as passive data loggers, capturing exhaustive video for offline review and frequently missing transient events of high scientific value. Transitioning to active perception requires a causal, online signal that highlights significant phenomena while suppressing maneuver-induced visual changes. We propose DINO-Explorer, a novelty-aware perception framework driven by a continuous semantic surprise signal. Operating within the latent space of a frozen DINOv3 foundation model, it leverages a lightweight, action-conditioned recurrent predictor to anticipate short-horizon semantic evolution. An efference-copy-inspired module utilizes globally pooled optical flow to discount self-induced visual changes without suppressing genuine environmental novelty. We evaluate this signal on the downstream task of asynchronous event triage under variant telemetry constraints. Results demonstrate that DINO-Explorer provides a robust, bandwidth-efficient attention mechanism. At a fixed operating point, the system retains 78.8% of post-discovery human-reviewer consensus events with a 56.8% trigger confirmation rate, effectively surfacing mission-relevant phenomena. Crucially, ego-motion conditioning suppresses 45.5% of false positives relative to an uncompensated surprise signal baseline. In a replay-side Pareto ablation study, DINO-Explorer robustly dominates the validated peak F1 versus telemetry bandwidth frontier, reducing telemetry bandwidth by 48.2% at the selected operating point while maintaining a 62.2% peak F1 score, successfully concentrating data transmission around human-verified novelty events.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DINO-Explorer** 的框架，旨在提升自主水下航行器（AUV）在复杂、高噪声环境下的主动感知与科学发现能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：传统 AUV 多作为“被动记录器”，捕获海量视频供离线审查。然而，水下环境存在光散射、高浊度、海雪干扰以及带宽受限等问题，导致高价值的瞬时科学事件（如生物出现、生境转换）极易被淹没在冗余数据中。
*   **核心挑战**：
    1.  如何在视觉退化的水下图像中定义“语义新颖性”？
    2.  如何区分“环境真实变化”与“机器人自身运动引起的视觉变化”？
    3.  如何生成因果、紧凑的在线信号，以引导注意力并减少遥测带宽？
*   **核心目标**：开发一种受生物启发（预测编码与传出拷贝）的感知框架，通过语义预测误差来识别新颖事件，并补偿自我运动以减少误报。

### 2. 方法论：核心思想与技术细节
DINO-Explorer 结合了基础模型、循环预测和运动补偿技术：
*   **语义潜空间表示**：使用冻结的 **DINOv3** 视觉基础模型作为编码器。将输入帧映射为高维特征图，通过全局平均池化（GAP）压缩为 1D 全局语义向量 $z_t$。这种抽象能有效过滤局部噪声（如海雪、光斑）。
*   **语义预测编码（Predictive Coding）**：利用轻量级的 **GRU（循环神经网络）** 作为内部世界模型。它根据历史语义状态预测下一时刻的语义变化 $\Delta \hat{z}_t$。
*   **自我运动补偿（Efference Copy）**：
    *   使用 **RAFT 光流估计器** 计算全局池化光流，作为机器人运动的代理信号。
    *   将运动向量与语义向量拼接，输入到动作条件 GRU 中。这使得模型能够“预见”由自身机动引起的视觉变化，从而在计算惊奇值时将其抵消。
*   **惊奇信号生成**：计算观测值 $z_t$ 与预测值 $\hat{z}_t$ 之间的混合损失（均方误差 + 余弦相似度）。该信号 $S_t$ 既用于触发事件，也用于在线自监督学习以适应环境漂移。
*   **下游触发策略**：对惊奇信号进行因果高斯平滑和自适应阈值处理，提取局部极大值作为事件提案。

### 3. 实验设计
*   **数据集**：使用了在北海（Borkum Riffgrund）采集的 **178.2 分钟** 720p 水下视频，涵盖沙地、礁石、底栖生物及高浊度场景。
*   **基准方法（Benchmarks）**：
    1.  **Direct $\Delta z_t$**：直接计算相邻帧的 DINO 潜空间差异（非预测性基准）。
    2.  **Naive Surprise**：未进行运动补偿的预测模型。
    3.  **Uniform Periodic**：固定时间间隔的均匀采样（内容无关基准）。
*   **评估指标**：峰值 F1 分数、人类共识保留率（SPCR）、触发确认率（TCR）、带宽节省率（BSR）和潜空间能量保留率（LER）。
*   **人类验证协议**：邀请 15 名审查员进行两阶段验证（盲注 + 模型引导验证），以建立客观的“地面真值”。

### 4. 资源与算力
*   **硬件平台**：实验在 **NVIDIA RTX A6000** GPU 上运行。
*   **运行性能**：
    *   平均延迟：**227.2 ms**。
    *   吞吐量：**4.40 FPS**。
*   **训练细节**：模型首先在连续水下素材上预训练，部署期间通过自监督反向传播进行实时在线优化。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了帕累托前沿（Pareto sweep）分析，通过改变灵敏度参数 $\alpha$ 测试了整个“事件质量 vs. 带宽”的权衡曲线，而非单一阈值。
*   **充分性评价**：实验设计较为充分。通过 15 人的大规模人类研究解决了新颖性定义的客观性问题；消融实验清晰地展示了运动补偿的贡献；对比实验涵盖了从简单差异到均匀采样的多种基准。

### 6. 主要结论与发现
*   **性能优异**：在选定工作点，DINO-Explorer 减少了 **48.2%** 的遥测带宽，同时保持了 **62.2%** 的峰值 F1 分数。
*   **运动补偿的价值**：自我运动补偿模块成功抑制了 **45.5%** 的误报（相对于未补偿模型）。
*   **事件捕获能力**：系统保留了 **78.8%** 的人类共识事件，证明了其作为主动发现工具的有效性。
*   **鲁棒性**：即使在北海这种极具挑战性的高浊度环境中，基于基础模型的语义预测依然比像素级方法更稳健。

### 7. 优点与亮点
*   **生物启发式设计**：将神经科学中的“传出拷贝”概念转化为实用的机器人运动补偿模块，构思巧妙。
*   **基础模型赋能**：利用 DINOv3 的强大特征表示能力，解决了水下视觉噪声大的痛点。
*   **带宽友好**：针对 AUV 实际部署中的通信瓶颈，提供了切实可行的遥测压缩方案。
*   **自适应性**：通过在线自监督更新，模型能够处理非平稳的水下背景变化。

### 8. 不足与局限
*   **空间细节丢失**：由于采用了全局平均池化（GAP），系统目前只能识别“发生了惊奇事件”，但无法指出图像中具体哪个位置或哪个物体引起了惊奇。
*   **算力需求较高**：4.4 FPS 的处理速度对于高速航行器或嵌入式平台（如 Jetson）来说仍显不足，需要进一步的模型压缩或蒸馏。
*   **依赖 GPU**：目前验证基于 RTX A6000，尚未在资源受限的 AUV 嵌入式硬件上完成实时闭环测试。

（完）
