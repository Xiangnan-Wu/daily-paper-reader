---
title: "Prompt Relay: Inference-Time Temporal Control for Multi-Event Video Generation"
title_zh: Prompt Relay：多事件视频生成的推理时时序控制
authors: "Gordon Chen, Ziqi Huang, Ziwei Liu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10030v1"
tags: ["query:q7"]
score: 9.0
evidence: 多事件视频生成的时序控制
tldr: 针对视频扩散模型在生成多事件视频时存在的语义纠缠和时序控制不足问题，本文提出 Prompt Relay。这是一种即插即用的推理时方法，无需修改架构或增加计算开销。通过在交叉注意力机制中引入惩罚项，使不同时间段仅关注对应的提示词，从而实现对事件发生时间、持续时长和顺序的精确控制，显著提升了文本与视频的时序对齐质量。
selection_source: fresh_fetch
motivation: 现有模型在处理包含多个事件的长提示词时，容易出现语义混淆且缺乏对事件发生时机和持续时间的精确控制。
method: 在推理阶段通过修改交叉注意力机制引入惩罚项，确保视频的每个时间片段仅与其分配的特定提示词进行交互。
result: 该方法在不增加额外计算成本的情况下，有效消除了语义干扰，显著提高了多事件视频生成的文本对齐度和视觉连贯性。
conclusion: Prompt Relay 为实现电影级视频合成中的精细化时序控制提供了一种高效、无需训练的即插即用解决方案。
---

## 摘要
视频扩散模型在生成高质量视频方面取得了显著进展。然而，这些模型难以表现现实世界视频中多个事件的时序演进，且缺乏显式机制来控制语义概念出现的时间、持续的时长以及多个事件发生的顺序。这种控制对于电影级视频合成尤为重要，因为连贯的叙事取决于事件之间精确的时机、时长和转换。当使用单个段落式提示词来描述一系列复杂事件时，模型通常会出现语义纠缠，即针对视频不同时刻设计的概念会相互渗透，导致文本-视频对齐效果不佳。为了解决这些局限性，我们提出了 Prompt Relay，这是一种推理阶段的即插即用方法，能够在多事件视频生成中实现细粒度的时序控制，且无需修改架构或增加额外的计算开销。Prompt Relay 在交叉注意力机制中引入了一种惩罚项，使每个时间段仅关注其分配的提示词，从而允许模型一次只表现一个语义概念，进而改善时序提示词对齐，减少语义干扰，并提升视觉质量。

## Abstract
Video diffusion models have achieved remarkable progress in generating high-quality videos. However, these models struggle to represent the temporal succession of multiple events in real-world videos and lack explicit mechanisms to control when semantic concepts appear, how long they persist, and the order in which multiple events occur. Such control is especially important for movie-grade video synthesis, where coherent storytelling depends on precise timing, duration, and transitions between events. When using a single paragraph-style prompt to describe a sequence of complex events, models often exhibit semantic entanglement, where concepts intended for different moments in the video bleed into one another, resulting in poor text-video alignment. To address these limitations, we propose Prompt Relay, an inference-time, plug-and-play method to enable fine-grained temporal control in multi-event video generation, requiring no architectural modifications and no additional computational overhead. Prompt Relay introduces a penalty into the cross-attention mechanism, so that each temporal segment attends only to its assigned prompt, allowing the model to represent one semantic concept at a time and thereby improving temporal prompt alignment, reducing semantic interference, and enhancing visual quality.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prompt Relay: Inference-Time Temporal Control for Multi-Event Video Generation》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：现有的视频扩散模型在生成高质量视频方面表现出色，但在处理包含多个连续事件的长提示词时存在缺陷。
*   **核心问题**：模型缺乏显式的时序控制机制，无法精确规定某个语义概念何时出现、持续多久以及事件间的先后顺序。这导致了“语义纠缠”（Semantic Entanglement）现象，即不同时间段的描述内容相互渗透，造成视频内容混乱或时序错位。
*   **整体含义**：本文旨在开发一种无需训练、即插即用的方法，在推理阶段实现对多事件视频生成的细粒度时序控制，以支持电影级的叙事合成。

### 2. 论文提出的方法论
*   **核心思想**：通过修改交叉注意力（Cross-Attention）机制，引入一种**时序路由（Temporal Routing）**机制，确保视频的特定帧仅对与其对应的文本提示词片段产生强响应。
*   **关键技术细节**：
    *   **注意力惩罚项（Penalty Term）**：在交叉注意力分数计算中引入惩罚项 $C(Q, K)$。当视频查询（Query）的时间索引与文本键（Key）所属的时间段不匹配时，该项会增大，从而抑制不相关的注意力。
    *   **边界注意力衰减（Boundary-Attention Decay）**：采用软性高斯惩罚函数而非硬掩码（Hard Masking）。在预设的时间窗口内不设惩罚，在窗口外则随时间距离平滑衰减。
    *   **平滑过渡机制**：通过在段落边界处允许相邻提示词的“协同激活”（Co-activation），为模型提供一个过渡缓冲区，使场景转换更加自然，避免了硬切换带来的视觉伪影或主体突变。
    *   **全局提示词锚定**：引入一个全局提示词来提供持久的上下文（如角色身份、环境风格），以解决局部提示词切换可能导致的一致性漂移问题。

### 3. 实验设计
*   **基础模型（Backbone）**：Wan2.2-T2V-A14B。
*   **对比方法（Benchmarks）**：包括 Sora (Storyboard)、Kling 2.6、Veo 3.1 以及原始的 Wan 2.2。
*   **测试场景**：构建了 20 个复杂的多事件测试场景（由 ChatGPT 生成），涵盖显式场景转换、多角色互动和复杂相机轨迹，每个场景包含 3-6 个时序事件。
*   **评估指标**：由于时序语义难以量化，主要采用**人类偏好研究**，从“时序提示词对齐度”、“转换自然度”和“视觉质量”三个维度进行评分（1-5 分）。

### 4. 资源与算力
*   **算力需求**：论文强调该方法是**推理时（Inference-time）**方法，**无需任何训练**，且**不增加额外的计算开销**。
*   **具体配置**：文中未明确列出生成视频所使用的具体 GPU 型号或数量，但提到其基于 14B 参数规模的 Wan2.2 模型进行推理。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 4 种当前最先进的商业/开源视频生成模型。
    *   进行了 20 组多样化的多事件场景实验。
    *   邀请了 30 名参与者进行主观评估。
    *   **消融实验**：针对衰减函数中的窗口参数 $w$ 和衰减阈值 $\epsilon$ 进行了详细测试，并对比了“硬掩码”与“软衰减”的效果。
*   **充分性评价**：实验设计较为客观，通过与顶级商业模型（如 Sora, Kling）的对比证明了其在时序控制上的优越性。虽然样本量（20个场景）不算巨大，但覆盖了多事件生成的典型难点。

### 6. 论文的主要结论与发现
*   **时序对齐显著提升**：Prompt Relay 有效解决了语义纠缠问题，确保事件按预定顺序和时长发生。
*   **视觉质量改善**：通过减少交叉注意力空间中的不必要竞争，模型能更专注于当前语义，从而提升了画面的清晰度和稳定性。
*   **软过渡的必要性**：实验证明，相比于直接切断注意力的硬掩码，平滑的边界衰减能显著减少转换时的闪烁和形变。

### 7. 优点
*   **高效性**：无需训练，无需修改模型架构，零额外计算成本，极易集成到现有扩散模型中。
*   **通用性**：作为一种即插即用方法，理论上适用于任何基于注意力机制的视频生成模型。
*   **控制精度**：提供了对视频叙事节奏（何时开始、持续多久）的精确控制。

### 8. 不足与局限
*   **依赖基础模型**：最终的视觉保真度仍受限于底层模型（如 Wan2.2）的上限，无法通过该方法弥补模型本身的纹理生成能力。
*   **一致性风险**：如果局部提示词之间描述冲突且缺乏有效的全局提示词，视频中的角色或物体可能在转换时出现身份漂移。
*   **评估主观性**：主要依赖人工评分，缺乏自动化的时序对齐量化评估指标（这也是该领域的共同挑战）。

（完）
