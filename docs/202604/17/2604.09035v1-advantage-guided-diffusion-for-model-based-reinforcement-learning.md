---
title: Advantage-Guided Diffusion for Model-Based Reinforcement Learning
title_zh: 用于基于模型强化学习的优势引导扩散
authors: "Daniele Foffano, Arvid Eriksson, David Broman, Karl H. Johansson, Alexandre Proutiere"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09035v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于基于模型的强化学习的扩散世界模型
tldr: 针对扩散世界模型在强化学习中存在的短视问题，本文提出优势引导扩散（AGD-MBRL）。该方法利用优势函数引导逆向扩散过程，使生成的轨迹向高长期回报方向偏移。通过引入SAG和EAG两种引导机制，在不改变训练目标的前提下，显著提升了MuJoCo任务中的样本效率和最终性能，证明了优势感知引导在解决短视问题上的有效性。
selection_source: fresh_fetch
motivation: 现有的扩散模型引导方式要么忽略价值信息，要么因仅关注即时奖励而导致在短预测周期内表现短视。
method: 提出AGD-MBRL，通过Sigmoid或指数优势引导机制调整逆向扩散过程，使采样集中于具有更高长期优势的轨迹。
result: 在MuJoCo控制任务中，该方法的样本效率和最终回报显著优于PolyGRAD、基于奖励引导的Diffuser及PPO等基准。
conclusion: 优势感知引导是解决扩散模型MBRL中短视问题的一种简单且有效的手段，能在不增加训练复杂度的前提下实现策略改进。
---

## 摘要
使用自回归世界模型的基于模型强化学习（MBRL）面临复合误差问题，而扩散世界模型通过联合生成轨迹段来缓解这一问题。然而，现有的扩散引导要么仅基于策略（丢弃了价值信息），要么基于奖励（在扩散时界较短时会表现出短视性）。我们提出了用于 MBRL 的优势引导扩散（AGD-MBRL），它利用智能体的优势估计来引导反向扩散过程，使采样集中在预期能产生超出生成窗口更高长期回报的轨迹上。我们开发了两种引导方式：(i) Sigmoid 优势引导 (SAG) 和 (ii) 指数优势引导 (EAG)。我们证明，通过 SAG 或 EAG 引导的扩散模型允许我们对轨迹进行重加权采样，其权重随状态-动作优势的增加而增加——在标准假设下，这意味着策略的改进。此外，我们表明，与无引导的扩散模型相比，AGD-MBRL 生成的轨迹遵循改进后的策略（即具有更高的价值）。AGD 通过引导状态分量同时保持动作生成受策略约束，与 PolyGRAD 风格的架构无缝集成，且无需更改扩散训练目标。在 MuJoCo 控制任务（HalfCheetah、Hopper、Walker2D 和 Reacher）上，AGD-MBRL 的样本效率和最终回报优于 PolyGRAD、在线 Diffuser 风格的奖励引导以及无模型基准（PPO/TRPO），在某些情况下提升幅度达到 2 倍。这些结果表明，优势感知引导是解决扩散模型 MBRL 中短时界短视问题的一种简单且有效的补救措施。

## Abstract
Model-based reinforcement learning (MBRL) with autoregressive world models suffers from compounding errors, whereas diffusion world models mitigate this by generating trajectory segments jointly. However, existing diffusion guides are either policy-only, discarding value information, or reward-based, which becomes myopic when the diffusion horizon is short. We introduce Advantage-Guided Diffusion for MBRL (AGD-MBRL), which steers the reverse diffusion process using the agent's advantage estimates so that sampling concentrates on trajectories expected to yield higher long-term return beyond the generated window. We develop two guides: (i) Sigmoid Advantage Guidance (SAG) and (ii) Exponential Advantage Guidance (EAG). We prove that a diffusion model guided through SAG or EAG allows us to perform reweighted sampling of trajectories with weights increasing in state-action advantage-implying policy improvement under standard assumptions. Additionally, we show that the trajectories generated from AGD-MBRL follow an improved policy (that is, with higher value) compared to an unguided diffusion model. AGD integrates seamlessly with PolyGRAD-style architectures by guiding the state components while leaving action generation policy-conditioned, and requires no change to the diffusion training objective. On MuJoCo control tasks (HalfCheetah, Hopper, Walker2D and Reacher), AGD-MBRL improves sample efficiency and final return over PolyGRAD, an online Diffuser-style reward guide, and model-free baselines (PPO/TRPO), in some cases by a margin of 2x. These results show that advantage-aware guidance is a simple, effective remedy for short-horizon myopia in diffusion-model MBRL.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **AGD-MBRL（优势引导扩散的基于模型强化学习）** 的新框架，旨在解决扩散世界模型在生成轨迹时的“短视”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景：** 在基于模型的强化学习（MBRL）中，传统的自回归世界模型（一步步预测）容易产生**复合误差**。扩散模型（Diffusion Models）通过一次性生成整段轨迹缓解了这一问题。
*   **核心问题：** 现有的扩散引导机制存在缺陷：
    *   **仅基于策略的引导（如 PolyGRAD）：** 忽略了价值函数信息，无法主动探索高回报区域。
    *   **基于奖励的引导（如 Diffuser）：** 具有**短视性（Myopia）**。由于扩散生成的轨迹窗口通常较短，仅靠窗口内的累积奖励无法反映窗口之外的长期价值。
*   **研究动机：** 利用强化学习中的**优势函数（Advantage Function）**来引导扩散过程。优势函数包含了对未来长期回报的估计（通过价值函数），从而引导模型生成更有利于策略改进的高质量轨迹。

### 2. 方法论
*   **核心思想：** 在扩散模型的反向去噪过程中，利用优势函数的梯度来偏移采样分布，使生成的轨迹向“优势值更高”的方向靠拢。
*   **关键技术细节：**
    *   **两种引导机制：**
        1.  **Sigmoid 优势引导 (SAG)：** 将步优化的概率建模为优势函数的 Sigmoid 变换。这是一种保守的加权方式，能防止优势函数估计过高导致的偏差。
        2.  **指数优势引导 (EAG)：** 采用能量模型形式，以优势函数的指数作为权重。这种方式更激进，能更强烈地将采样推向高优势区域。
    *   **理论证明：** 论文证明了使用 SAG 或 EAG 引导的采样等价于从一个**已改进的策略**中进行重采样，符合策略改进定理（Policy Improvement Theorem）。
    *   **算法流程：**
        1.  在真实环境中收集数据，训练扩散世界模型。
        2.  在去噪采样时，计算当前轨迹段的优势函数梯度。
        3.  利用梯度修正去噪均值（Classifier Guidance 技巧）。
        4.  使用生成的高优势轨迹通过 Actor-Critic 算法更新策略。

### 3. 实验设计
*   **实验场景：** 使用了经典的 **MuJoCo** 连续控制任务：`HalfCheetah`（半猎豹）、`Hopper`（单足跳）、`Walker2D`（双足行走）和 `Reacher`（触及器）。
*   **基准方法（Benchmarks）：**
    *   **PolyGRAD：** 基础的扩散 MBRL 方法（仅策略引导）。
    *   **Online Diffuser：** 作者改编的在线版 Diffuser（基于奖励引导）。
    *   **无模型基准：** PPO 和 TRPO。
*   **对比维度：** 样本效率（Sample Efficiency）和最终收敛回报（Final Return）。

### 4. 资源与算力
*   **算力来源：** 论文提到使用了瑞典国家超级计算基础设施（NAISS）的资源。
*   **具体细节：** 文中**未明确列出**具体的 GPU 型号、数量或具体的训练时长。但提到所有实验均在 150 万（1.5M）环境交互步数内完成。

### 5. 实验数量与充分性
*   **实验规模：** 在 4 个不同的 MuJoCo 环境上进行了测试。
*   **充分性评估：** 
    *   实验包含了与最先进的扩散 MBRL（PolyGRAD）以及经典无模型算法（PPO/TRPO）的对比。
    *   提供了带有标准差（Shaded areas）的训练曲线，证明了结果的统计显著性。
    *   **不足：** 实验环境主要集中在标准的 MuJoCo 任务上，缺乏更复杂、高维或稀疏奖励环境的验证。

### 6. 主要结论与发现
*   **性能提升：** AGD-MBRL 在多数任务中显著优于 PolyGRAD 和基于奖励引导的方法，在 `HalfCheetah` 任务中性能提升甚至达到 2 倍。
*   **解决短视：** 实验证明，相比于仅看当前窗口奖励的引导，优势引导能有效利用长期价值信息，使生成的“想象”轨迹质量更高。
*   **稳定性：** 优势引导（尤其是 SAG）在训练过程中表现出更好的稳定性，减少了性能回退的频率。

### 7. 优点与亮点
*   **理论完备：** 为优势引导提供了数学证明，将其与策略改进定理联系起来，使其不仅是一个启发式技巧，更具有理论支撑。
*   **即插即用：** 该方法不需要改变扩散模型的训练目标，只需在推理（采样）阶段加入梯度引导，易于集成到现有的扩散 MBRL 架构中。
*   **有效解决短视：** 巧妙地利用价值函数（Value Function）作为“未来”的代理，弥补了短时界扩散生成的局限性。

### 8. 不足与局限
*   **计算开销：** 扩散模型本身的迭代去噪过程非常耗时，虽然论文提升了样本效率，但**推理时间成本**（Wall-clock time）仍然是扩散模型类方法的通用瓶颈。
*   **依赖价值函数精度：** 引导的效果高度依赖于 RL 智能体学习到的优势函数/价值函数的准确性。如果价值函数估计偏差较大，引导可能会将模型带偏。
*   **环境覆盖面：** 实验仅限于连续控制任务，未在离散动作空间或具有复杂视觉输入的任务中进行验证。

（完）
