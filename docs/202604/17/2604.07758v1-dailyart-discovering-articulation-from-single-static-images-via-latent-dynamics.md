---
title: "DailyArt: Discovering Articulation from Single Static Images via Latent Dynamics"
title_zh: DailyArt：通过潜在动力学从单张静态图像中发现关节结构
authors: "Hang Zhang, Qijian Tian, Jingyu Gong, Daoguo Dong, Xuhong Wang, Yuan Xie, Xin Tan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07758v1"
tags: ["query:q10"]
score: 9.0
evidence: 用于具身智能和世界模型中发现关节结构的潜动力学
tldr: DailyArt 针对从单张静态闭合状态图像中推断物体关节参数的难题，提出了一种基于合成推理的新框架。该方法不依赖特定模板或多视图，而是先通过生成模型合成物体的最大开启状态以暴露运动线索，再通过对比观测状态与合成状态来估计所有关节参数。实验证明其在关节估计和部件级新状态合成方面表现优异，为具身智能理解复杂物体提供了新思路。
selection_source: fresh_fetch
motivation: 从单张闭合状态图像推断关节参数极具挑战，因为关键运动线索常被遮挡，且现有方法多依赖多状态观测或显式先验。
method: 提出一种合成介导的推理方法，先合成物体的最大开启状态以暴露关节线索，再利用集合预测公式从状态差异中同步估计所有关节参数。
result: 在关节估计任务上取得了强劲性能，并能基于估计的关节实现高质量的部件级新状态合成。
conclusion: DailyArt 证明了通过生成式合成来辅助几何推理的有效性，为单图关节物体理解提供了无需模板的新方案。
---

## 摘要
关节物体对于具身智能和世界模型至关重要，但从单张闭合状态的图像中推断其运动学特征仍然具有挑战性，因为关键的运动线索往往被遮挡。现有方法要么需要多状态观测，要么依赖于显式的部件先验、检索或其他辅助输入，而这些输入部分暴露了待推断的结构。在这项工作中，我们提出了 DailyArt，它将单张静态图像中的关节估计建模为一个由合成介导的推理问题。DailyArt 并非直接从严重遮挡的观测中回归关节，而是首先在相同相机视角下合成一个最大程度张开的关节状态以暴露关节线索，然后根据观测状态与合成状态之间的差异估计全套关节参数。通过使用集合预测公式，DailyArt 可以同时恢复所有关节，且在测试时无需特定物体的模板、多视图输入或显式的部件标注。以估计的关节为条件，该框架进一步支持部件级的新状态合成作为下游能力。大量实验表明，DailyArt 在关节估计方面取得了强大的性能，并支持以关节为条件的部件级新状态合成。项目页面见 https://rangooo123.github.io/DaliyArt.github.io/。

## Abstract
Articulated objects are essential for embodied AI and world models, yet inferring their kinematics from a single closed-state image remains challenging because crucial motion cues are often occluded. Existing methods either require multi-state observations or rely on explicit part priors, retrieval, or other auxiliary inputs that partially expose the structure to be inferred. In this work, we present DailyArt, which formulates articulated joint estimation from a single static image as a synthesis-mediated reasoning problem. Instead of directly regressing joints from a heavily occluded observation, DailyArt first synthesizes a maximally articulated opened state under the same camera view to expose articulation cues, and then estimates the full set of joint parameters from the discrepancy between the observed and synthesized states. Using a set-prediction formulation, DailyArt recovers all joints simultaneously without requiring object-specific templates, multi-view inputs, or explicit part annotations at test time. Taking estimated joints as conditions, the framework further supports part-level novel state synthesis as a downstream capability. Extensive experiments show that DailyArt achieves strong performance in articulated joint estimation and supports part-level novel state synthesis conditioned on joints. Project page is available at https://rangooo123.github.io/DaliyArt.github.io/.

---

## 论文详细总结（自动生成）

### DailyArt: 通过潜在动力学从单张静态图像中发现关节结构 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
关节物体（如冰箱、笔记本电脑、折叠椅）的感知是具身人工智能（Embodied AI）和世界模型的核心。然而，从**单张静态闭合状态图像**中推断其运动学结构（关节类型、轴线、运动范围）极具挑战，因为关键的运动线索（如铰链、滑轨）通常被表面遮挡。
*   **现有局限**：传统方法要么需要多状态观测（视频或图像对），要么依赖于显式的先验（如部件掩码、CAD 模板、检索库或人工提示）。
*   **研究动机**：人类可以通过想象物体如何移动来推断其结构。论文提出 **DailyArt**，旨在打破“需要先验才能估计关节，需要关节才能合成运动”的循环依赖，通过“合成介导推理”实现无先验的关节发现。

#### 2. 方法论：核心思想与技术流程
DailyArt 将关节估计重新表述为一个**合成介导的推理问题**，分为三个阶段：

*   **Stage I：新状态合成（发现线索）**
    *   **核心思想**：在不指定具体部件的情况下，将闭合状态图像 ($t=0$) 合成为最大开启状态 ($t=1$)。
    *   **技术细节**：使用冻结的 DINOv2 作为编码器提取语义特征，通过 VAE 解码器重建图像。利用自适应层归一化（AdaLN）将运动索引 $t$ 注入潜在空间，生成暴露隐藏关节线索的图像。
*   **Stage II：3D 感知的关节估计（几何推理）**
    *   **核心思想**：对比闭合与合成开启状态的差异，在 3D 空间中定位关节。
    *   **技术细节**：利用视觉几何变换器（VGGT）将图像对提升为 3D 点图。通过计算两状态间的位移提取“运动种子（Motion Seeds）”，并使用基于 Transformer 的集合预测（Set-prediction）架构，一次性回归所有关节的参数（类型、原点 $o$、轴向 $a$、运动范围 $\vartheta$）。
*   **Stage III：关节条件下的状态合成（下游应用）**
    *   **核心思想**：将 Stage II 估计的关节参数作为显式条件反馈给合成模型。
    *   **技术细节**：实现部件级的可控运动生成，允许用户指定特定关节进行不同程度（$t \in [0,1]$）的开启。

#### 3. 实验设计
*   **数据集**：
    *   **PartNet-Mobility**：包含 12 类关节物体（洗碗机、折叠椅、眼镜、笔记本等），2.7k 训练样本，347 个测试物体。
    *   **AKB-48**：用于**零样本（Zero-shot）**性能评估的真实世界关节物体数据集。
    *   **Objaverse-XL**：用于预训练解码器的通用 3D 资产。
*   **Benchmark 与对比方法**：
    *   **状态合成对比**：DragAPart, PartRM, Puppet-Master, LARM。
    *   **关节估计对比**：URDFormer, Singapo, Articulate-Anything, PhysX-Anything。
*   **评估指标**：
    *   合成质量：PSNR, SSIM, LPIPS, CLIP-T, FVD。
    *   估计精度：轴角度误差、原点距离、运动范围误差及综合成功率（Success Rate）。

#### 4. 资源与算力
*   **硬件配置**：使用了由 **8 张 NVIDIA H200 GPU (每张 140GB 显存)** 组成的集群。
*   **训练时长**：
    *   Stage I（表示对齐）：20k epochs。
    *   Stage II（关节估计）：500 epochs。
    *   Stage III（条件合成）：1k epochs。
*   **推理效率**：单次前向传播总耗时约 **0.45s**（合成 280ms + 估计 170ms），具备交互式应用潜力。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了详尽测试，涵盖了合成数据和真实世界跨域数据。
*   **消融实验**：
    1.  **合成的必要性**：对比了“直接回归”与“合成介导”，证明合成步骤使成功率从 44.2% 提升至 68.4%。
    2.  **3D 提升的作用**：证明 3D 几何约束比纯 2D 图像对编码更能减少几何歧义。
    3.  **合成策略**：对比了直接合成与序列生成，证明了直接合成的效率和准确性优势。
*   **客观性**：所有定量结果均取 5 次随机种子的平均值，并与当前最先进的（SOTA）基础模型（如 Articulate-Anything）进行了公平对比。

#### 6. 主要结论与发现
*   **合成即推理**：通过生成模型“想象”物体的开启状态，可以显著降低单图关节估计的难度。
*   **性能领先**：在 PartNet-Mobility 上，DailyArt 的综合成功率达到 68.4%，比最强基线（PhysX-Anything）高出 5.6%。
*   **泛化能力**：在未见过的真实物体（AKB-48）上表现出良好的零样本迁移能力，证明了其学习的是通用的运动学先验而非简单的类别记忆。

#### 7. 优点（亮点）
*   **无需先验**：测试时不需要掩码、CAD 模型或人工提示，仅需单张 RGB 图像。
*   **端到端流程**：实现了从感知（估计）到生成（合成）的闭环。
*   **3D 几何集成**：巧妙地结合了预训练的几何模型（VGGT），使 2D 生成与 3D 运动学约束相结合。

#### 8. 不足与局限
*   **误差传播**：Stage II 的估计精度高度依赖于 Stage I 合成图像的质量，若合成出现伪影，会直接导致关节预测失败。
*   **极端视角与遮挡**：当物体背面朝向相机或运动完全不可见时，3D 提升模块可能失效。
*   **状态假设**：模型假设物体具有明确的“闭合”和“最大开启”状态，对于没有明确边界的连续运动物体可能适用性较差。
*   **工业场景限制**：目前主要针对家用物体，对于结构极其复杂的工业机械关节支持有限。

（完）
