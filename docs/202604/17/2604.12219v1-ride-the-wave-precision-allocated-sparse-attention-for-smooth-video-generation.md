---
title: "Ride the Wave: Precision-Allocated Sparse Attention for Smooth Video Generation"
title_zh: 乘风破浪：面向平滑视频生成的精准分配稀疏注意力
authors: "Wentai Zhang, Ronghui Xi, Shiyao Peng, Jiayu Huang, Haoran Luo, Zichen Tang, Haihong E"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12219v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于平滑视频生成的稀疏注意力机制
tldr: 针对视频扩散Transformer在推理时计算开销巨大且稀疏注意力易导致画面闪烁的问题，本文提出了一种无需训练的框架PASA。该框架通过曲率感知的动态预算分配、硬件对齐的分组近似以及随机选择偏置机制，在显著提升推理速度的同时，有效消除了时间不一致性，实现了高效率且平滑的视频生成。
selection_source: fresh_fetch
motivation: 现有的稀疏注意力方法因采用静态模式和确定性路由，容易导致视频生成过程中出现严重的视觉闪烁和计算资源分配不均。
method: PASA引入了曲率感知的动态预算机制、硬件对齐的分组近似以及随机选择偏置，通过弹性分配计算资源和软化选择边界来优化注意力机制。
result: 实验证明，PASA在显著加速主流视频扩散模型推理的同时，能够生成结构稳定且视觉平滑的视频序列。
conclusion: PASA通过精准的资源调度和概率路由机制，成功解决了高效视频生成中的计算负担与时间一致性之间的矛盾。
---

## 摘要
视频扩散 Transformer（Video Diffusion Transformers）彻底改变了高保真视频生成，但面临自注意力机制带来的巨大计算负担。虽然稀疏注意力提供了一种极具前景的加速方案，但现有方法由于静态稀疏模式和确定性块路由，经常引发严重的视觉闪烁。为解决这些局限性，我们提出了精准分配稀疏注意力（Precision-Allocated Sparse Attention, PASA），这是一个专为高效且时间平滑的视频生成而设计的无需训练的框架。首先，我们实现了一种曲率感知的动态预算机制。通过分析不同时间步长下的生成轨迹加速度，我们弹性地分配精确计算预算，以确保在关键语义转换期间进行严格的高精度处理。其次，我们用硬件对齐的分组近似取代了全局同质化估计，在保持峰值计算吞吐量的同时，成功捕捉到了细粒度的局部变化。最后，我们在注意力路由机制中引入了随机选择偏差。这种概率方法软化了僵化的选择边界并消除了选择振荡，有效地根除了导致时间闪烁的局部计算饥饿问题。对领先视频扩散模型的大量评估表明，PASA 在实现显著推理加速的同时，能够持续生成极其流畅且结构稳定的视频序列。

## Abstract
Video Diffusion Transformers have revolutionized high-fidelity video generation but suffer from the massive computational burden of self-attention. While sparse attention provides a promising acceleration solution, existing methods frequently provoke severe visual flickering caused by static sparsity patterns and deterministic block routing. To resolve these limitations, we propose Precision-Allocated Sparse Attention (PASA), a training-free framework designed for highly efficient and temporally smooth video generation. First, we implement a curvature-aware dynamic budgeting mechanism. By profiling the generation trajectory acceleration across timesteps, we elastically allocate the exact-computation budget to secure high-precision processing strictly during critical semantic transitions. Second, we replace global homogenizing estimations with hardware-aligned grouped approximations, successfully capturing fine-grained local variations while maintaining peak compute throughput. Finally, we incorporate a stochastic selection bias into the attention routing mechanism. This probabilistic approach softens rigid selection boundaries and eliminates selection oscillation, effectively eradicating the localized computational starvation that drives temporal flickering. Extensive evaluations on leading video diffusion models demonstrate that PASA achieves substantial inference acceleration while consistently producing remarkably fluid and structurally stable video sequences.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PASA (Precision-Allocated Sparse Attention)** 的新框架，旨在解决视频生成模型中计算效率与画面平滑度之间的矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视频扩散 Transformer (Video DiT) 在处理高分辨率或长视频时，自注意力机制的计算量随序列长度呈平方级增长（$O(n^2)$），成为推理瓶颈。
*   **研究动机**：现有的稀疏注意力方法（如选择性丢弃或粗糙补偿）存在三大局限：
    1.  **静态稀疏模式**：在所有去噪步骤中采用统一的稀疏度，忽略了生成轨迹中不同阶段对计算精度的需求差异。
    2.  **确定性路由导致的闪烁**：固定的块选择机制会导致相邻帧之间出现“选择振荡”，表现为视觉上的时间闪烁。
    3.  **全局近似的同质化**：为了硬件效率使用全局统计量补偿，导致局部纹理（如背景）模糊。

### 2. 方法论
PASA 是一个**无需训练 (Training-free)** 的框架，包含三个核心模块：
*   **曲率感知动态预算 (Curvature-Aware Dynamic Budgeting)**：
    *   基于流匹配 (Flow Matching) 理论，通过计算相邻时间步预测速度场（Velocity Field）的 L1 距离来衡量生成轨迹的“曲率”。
    *   在语义构建剧烈的阶段（高曲率）分配更多精确计算预算，在平稳阶段（低曲率）增加稀疏度，实现总计算量不变下的精度重分配。
*   **随机选择偏置 (Stochastic Selection Bias)**：
    *   在块评分函数中注入受控的随机噪声，使原本处于边缘、容易被忽略的背景或边界块有概率获得精确计算。
    *   这种“概率路由”软化了选择边界，有效消除了因确定性选择导致的局部计算饥饿和时间闪烁。
*   **硬件对齐的分组一阶近似 (Grouped 1st-Order Approximation)**：
    *   不再使用单一的全局统计量进行泰勒展开补偿，而是将 Token 块划分为多个硬件对齐的组（如每组 32 块）。
    *   在组内共享补偿统计量，既保留了局部表征的细粒度，又通过 Triton 算子优化确保了 GPU 的高吞吐量。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型**：Wan 2.1 (1.3B 和 14B 版本)、HunyuanVideo (13B)。
    *   **基准测试**：使用 **VBench** 评估视觉质量和时间一致性；使用 PSNR、SSIM、LPIPS 评估与全注意力（Full Attention）基准的相似度。
*   **对比方法**：
    *   **SVG2**（基于选择/丢弃的代表性方法）。
    *   **PISA**（基于泰勒展开补偿的代表性方法，PASA 的基础）。
*   **测试场景**：涵盖 480P 和 720P 分辨率的文本生成视频 (T2V)。

### 4. 资源与算力
*   **硬件环境**：实验在 **8 台 NVIDIA H800 GPU** 上运行。
*   **软件环境**：CUDA 12.8, Python 3.12.9, Triton 3.4.0。
*   **训练时长**：由于是 **无需训练 (Training-free)** 的方法，该研究不涉及模型训练，仅涉及推理优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流大模型上进行了定量对比。
    *   进行了详细的**增量消融实验**和**减量消融实验**（针对动态预算、随机偏置、分组近似三个模块）。
    *   使用了 10 个由 GPT 生成的多样化提示词进行轨迹校准。
*   **充分性与客观性**：实验不仅关注 PSNR 等像素级指标，还引入了 VBench 这种专门针对视频生成质量的综合指标，并提供了推理延迟（Latency）的实测数据，评估维度较为全面、客观。

### 6. 主要结论与发现
*   **性能提升**：PASA 在保持 85% 极高稀疏度的前提下，PSNR 和 SSIM 显著优于 PISA 和 SVG2。
*   **加速效果**：在 HunyuanVideo-13B 上实现了高达 **2.36 倍** 的推理加速。
*   **平滑度突破**：在时间闪烁（Temporal Flickering）和运动平滑度（Motion Smoothness）指标上，PASA 甚至在某些模型上超越了全注意力基准，证明了随机路由对消除伪影的有效性。

### 7. 优点
*   **即插即用**：无需任何微调或训练，可直接应用于现有的 DiT 模型。
*   **硬件友好**：通过分组近似和 Triton 算子实现，充分利用了 GPU 的异步加载和计算特性。
*   **理论支撑强**：结合了流匹配轨迹分析和泰勒展开误差分析，方法论具有较强的数学解释性。

### 8. 不足与局限
*   **校准依赖**：动态预算曲线需要预先使用少量提示词进行离线校准（虽然论文称 10 个提示词即可，但不同领域的泛化性有待验证）。
*   **硬件限制**：分组近似的优化高度依赖于特定的 GPU 架构（如 Hopper 架构）和 Triton 编译器，在其他硬件（如移动端或旧款 GPU）上的加速效果可能打折扣。
*   **长视频挑战**：虽然在 5 秒视频上表现优异，但在极长视频生成中，累积的近似误差是否会影响长期结构稳定性仍需进一步研究。

（完）
