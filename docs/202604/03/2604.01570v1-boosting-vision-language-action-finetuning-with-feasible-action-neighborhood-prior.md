---
title: Boosting Vision-Language-Action Finetuning with Feasible Action Neighborhood Prior
title_zh: 利用可行动作邻域先验增强视觉-语言-动作模型微调
authors: "Haochen Niu, Kanyu Zhang, Shuyu Yin, Qinghai Guo, Peilin Liu, Fei Wen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01570v1"
tags: ["query:robot"]
score: 9.0
evidence: 通过可行动作邻域先验增强机器人操作中的VLA微调
tldr: 针对机器人操作中存在“可行动作邻域（FAN）”这一特性（即多个相近动作均可达成目标），本文提出了一种FAN引导的正则化方法。该方法通过引入高斯先验，使VLA模型的输出分布在动作空间中更加平滑且呈单峰分布，从而显著提升了模型在微调阶段的样本效率和泛化能力，在多种任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的VLA训练方法忽略了机器人操作中动作的容错性，导致模型在微调时样本效率低且泛化性差。
method: 引入一种基于FAN的高斯正则化项，通过约束模型输出分布与动作几何空间对齐，促进预测结果在最优动作附近的局部平滑性。
result: 在有监督微调和强化微调实验中，该方法显著提高了任务成功率，并展现出更强的分布外泛化能力。
conclusion: 利用物理操作中的动作容错性作为先验知识，是实现高效且鲁棒的VLA模型适配的一种原则性且实用的方法。
---

## 摘要
在现实世界的机器人操作中，状态通常允许一个近乎等效动作的邻域。也就是说，对于每个状态，存在一个可行动作邻域（FAN）而非单一的正确动作，在该邻域内的运动所产生的进展是难以区分的。然而，现有的 VLA 训练方法直接继承自语言任务的设置，未能利用 FAN 特性，从而导致泛化能力差和样本效率低。为了解决这一局限性，我们引入了一种 FAN 引导的正则化器，通过塑造模型的输出分布使其与 FAN 的几何结构对齐。具体而言，我们引入了一个高斯先验，旨在促进在首选方向和幅度周围产生局部平滑且单峰的预测。在强化微调（RFT）和监督微调（SFT）的广泛实验中，我们的方法在样本效率以及分布内和分布外（OOD）场景的成功率方面均取得了显著提升。通过与物理操作固有的动作容差相对齐，FAN 引导的正则化为实现高样本效率且具泛化性的 VLA 适配提供了一种有原则且实用的方法。

## Abstract
In real-world robotic manipulation, states typically admit a neighborhood of near-equivalent actions. That is, for each state, there exist a feasible action neighborhood (FAN) rather than a single correct action, within which motions yield indistinguishable progress. However, prevalent VLA training methodologies are directly inherited from linguistic settings and do not exploit the FAN property, thus leading to poor generalization and low sample efficiency. To address this limitation, we introduce a FAN-guided regularizer that shapes the model's output distribution to align with the geometry of FAN. Concretely, we introduce a Gaussian prior that promotes locally smooth and unimodal predictions around the preferred direction and magnitude. In extensive experiments across both reinforced finetuning (RFT) and supervised finetuning (SFT), our method achieves significant improvement in sample efficiency, and success rate in both in-distribution and out-of-distribution (OOD) scenarios. By aligning with the intrinsic action tolerance of physical manipulation, FAN-guided regularization provides a principled and practical method for sample-efficient, and generalizable VLA adaptation.

---

## 论文详细总结（自动生成）

### 论文总结：利用可行动作邻域先验增强视觉-语言-动作模型微调

#### 1. 论文的核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型通常采用自回归语言模型的架构，将连续动作空间离散化为“动作 Token”。现有的微调方法（如 SFT 和 RFT）直接继承了自然语言处理的训练范式，将动作视为互斥的离散类别。
*   **研究动机**：物理世界与语言逻辑存在本质区别。在机器人操作中，动作具有**固有容差（Tolerance）**和**近等效性**。对于某一状态，往往存在一个**可行动作邻域（Feasible Action Neighborhood, FAN）**，在该邻域内的动作都能产生几乎相同的任务进展。
*   **核心问题**：忽略 FAN 特性会导致模型在小样本微调时陷入“独热码（One-hot）”式的过拟合，表现为概率分布过于尖锐，导致泛化能力差且强化学习探索效率低下。

#### 2. 论文提出的方法论
论文提出了一种 **FAN 引导的正则化方法**，旨在将模型的输出分布与物理动作空间的几何结构对齐。
*   **核心思想**：通过引入一个高斯先验（Gaussian Prior），强制模型在预测最优动作的同时，赋予其邻域动作合理的概率权重，从而实现局部平滑和单峰分布。
*   **关键技术细节**：
    *   **FAN-SFT（有监督微调）**：在标准交叉熵损失基础上增加 KL 散度正则项。目标分布是以当前预测最优动作为均值、以策略自身方差为动态协方差的高斯分布。
    *   **FAN-PPO（强化微调）**：在 PPO 算法的策略损失中加入 KL 约束。为了保证训练稳定性，RFT 阶段使用固定方差的高斯分布作为锚点。
    *   **理论支撑**：论文证明了受限优化问题的闭式解（Proposition 1），揭示了新策略是旧策略、高斯先验与 Q 值加权指数项之间的几何插值。

#### 3. 实验设计
*   **数据集与场景**：
    *   **ManiSkill**：使用 `PutOnPlateInScene25Main-v3` 任务，包含 25 种多样的拾取-放置原语。
    *   **LIBERO**：涵盖 LIBERO-Spatial, Object, Goal, Long 四个子集。
    *   **OOD 评估**：设计了 15 种扰动，分为视觉（如桌子纹理、噪声）、语义（如未见物体、指令变化）和执行（如机器人初始位姿、物体中途移位）三大类。
*   **Benchmark 与对比方法**：
    *   **基座模型**：OpenVLA（单步动作）和 OpenVLA-OFT（动作块/Action Chunking）。
    *   **对比算法**：标准 SFT、标准 PPO、RL4VLA、UniVLA、TriVLA、WorldVLA 等。
    *   **真机实验**：在 JAKA 7-DoF 机械臂上进行空间扰动下的拾取放置测试。

#### 4. 资源与算力
论文明确列出了算力配置：
*   **GPU 型号**：NVIDIA A100 (80GB)。
*   **数量与配置**：
    *   **SFT**：使用 2 到 4 张 A100，Batch Size 为 32-48，LoRA Rank 为 32。
    *   **RFT**：使用 1 张 A100，训练 390-650 个 Episode。
*   **训练时长**：虽然未给出具体小时数，但提到 FAN-PPO 达到 90% 成功率所需的步数仅为基准方法的 **1/3**。

#### 5. 实验数量与充分性
实验设计非常详尽且具有说服力：
*   **多范式覆盖**：同时验证了 SFT 和 RFT 两种微调路径。
*   **多架构验证**：针对单步预测和 Chunking 预测模型均有效。
*   **消融与敏感性分析**：对正则化系数 $\alpha$、高斯方差 $\sigma$ 进行了详尽的参数扫射，并对比了“标签平滑（Label Smoothing）”和“熵最大化（Entropy Maximization）”等替代方案。
*   **客观性**：通过 15 种 OOD 任务和真机实验，证明了性能提升并非来自对特定数据集的过拟合，而是来自更强的泛化能力。

#### 6. 主要结论与发现
*   **性能提升**：在 ManiSkill SFT 任务中，ID 成功率提升 11.7%，OOD 提升 5.2%；在 RFT 任务中，OOD 成功率提升显著（部分任务超过 10%）。
*   **样本效率**：FAN 正则化显著加速了强化学习的收敛过程，极大地提高了样本利用率。
*   **空间泛化**：在 LIBERO 和真机实验中，模型展现出极强的空间鲁棒性，能够处理训练集中未出现的物体或容器位姿偏移。
*   **分布形态**：实验证实，更宽、更平滑的动作分布（即更大的隐式 FAN）与模型的泛化性能呈正相关。

#### 7. 优点（亮点）
*   **物理一致性**：首次形式化定义了“可行动作邻域（FAN）”，弥补了语言模型训练目标与物理操作容差之间的鸿沟。
*   **即插即用**：无需修改 VLA 模型架构或推理逻辑，仅通过修改损失函数即可实现，具有极强的通用性。
*   **理论与实践结合**：既有严谨的数学推导，又有直观的策略分布可视化和真机验证。

#### 8. 不足与局限
*   **先验形式单一**：目前主要采用各向同性的高斯分布，虽然简单有效，但对于某些具有复杂几何约束的任务（如狭窄缝隙插入），FAN 的形状可能高度非对称或多峰，简单高斯分布可能不是最优近似。
*   **超参数依赖**：正则化系数 $\alpha$ 和方差 $\sigma$ 对性能有一定影响，在不同任务间可能需要微调。
*   **任务覆盖面**：实验主要集中在拾取-放置类操作，对于更复杂的长程逻辑推理或高频动态平衡任务的适用性尚待进一步验证。

（完）
