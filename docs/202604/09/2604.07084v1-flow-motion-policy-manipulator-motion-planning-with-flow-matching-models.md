---
title: "Flow Motion Policy: Manipulator Motion Planning with Flow Matching Models"
title_zh: 流运动策略：基于流匹配模型的机械臂运动规划
authors: "Davood Soleymanzadeh, Xiao Liang, Minghui Zheng"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07084v1"
tags: ["query:robot"]
score: 8.0
evidence: 使用流匹配的机器人机械臂神经运动规划器
tldr: 针对现有端到端神经运动规划器生成路径单一且缺乏推理优化的问题，本文提出了Flow Motion Policy (FMP)。该方法利用流匹配模型的随机生成特性，捕捉规划数据集中的多模态分布。通过在推理阶段生成多个候选路径并进行碰撞检测，FMP实现了高效的“N选一”采样优化。实验表明，该方法在规划成功率和效率上均优于传统采样及现有神经规划方法，展示了随机生成策略在复杂环境避障中的潜力。
selection_source: fresh_fetch
motivation: 现有的端到端神经运动规划器通常只能生成单一路径，且难以在推理阶段通过优化来提升规划的成功率。
method: 利用流匹配模型构建随机生成策略，通过建模可行路径的概率分布来实现多模态路径生成与推理时的最佳采样。
result: 在基准测试中，Flow Motion Policy 在规划成功率和计算效率上均显著优于传统的采样算法及现有的神经运动规划模型。
conclusion: 研究证明了随机生成模型在端到端运动规划中的有效性，通过推理时的多路径采样优化能显著增强机械臂的避障性能。
---

## 摘要
最近，开环端到端神经运动规划器被提出，用于改进机器人机械臂的运动规划。这些方法能够直接根据传感器观测进行规划，而无需在规划过程中依赖特权碰撞检测器。然而，许多现有方法在不同运行中针对给定工作空间仅生成单一路径，且未利用其开环结构进行推理时优化。为解决这一局限性，我们引入了流运动策略（Flow Motion Policy），这是一种用于机器人机械臂的开环端到端神经运动规划器，它利用流匹配方法的随机生成公式来捕捉规划数据集固有的多模态性。通过对可行路径的分布进行建模，流运动策略实现了高效的推理时 $N$ 选 1（best-of-$N$）采样。该方法生成多个端到端候选路径，在规划后评估其碰撞状态，并执行第一个无碰撞的解。我们将流运动策略与具有代表性的基于采样的运动规划方法和神经运动规划方法进行了基准测试。评估结果表明，流运动策略提高了规划成功率和效率，突显了随机生成策略在端到端运动规划和推理时优化中的有效性。实验评估视频可通过此链接获取。

## Abstract
Open-loop end-to-end neural motion planners have recently been proposed to improve motion planning for robotic manipulators. These methods enable planning directly from sensor observations without relying on a privileged collision checker during planning. However, many existing methods generate only a single path for a given workspace across different runs, and do not leverage their open-loop structure for inference-time optimization. To address this limitation, we introduce Flow Motion Policy, an open-loop, end-to-end neural motion planner for robotic manipulators that leverages the stochastic generative formulation of flow matching methods to capture the inherent multi-modality of planning datasets. By modeling a distribution over feasible paths, Flow Motion Policy enables efficient inference-time best-of-$N$ sampling. The method generates multiple end-to-end candidate paths, evaluates their collision status after planning, and executes the first collision-free solution. We benchmark the Flow Motion Policy against representative sampling-based and neural motion planning methods. Evaluation results demonstrate that Flow Motion Policy improves planning success and efficiency, highlighting the effectiveness of stochastic generative policies for end-to-end motion planning and inference-time optimization. Experimental evaluation videos are available via this \href{https://zh.engr.tamu.edu/wp-content/uploads/sites/310/2026/03/FMP-Website.mp4}{link}.

---

## 论文详细总结（自动生成）

这是一份关于论文《Flow Motion Policy: Manipulator Motion Planning with Flow Matching Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是解决现有端到端神经运动规划器（Neural Motion Planners）的**单一性**与**缺乏推理优化**的问题。
*   **背景**：传统的采样算法（如 RRT）虽然可靠但计算缓慢（碰撞检测占 90% 时间）；现有的神经规划器虽然速度快，但大多是确定性的，即针对同一环境只能生成一条路径。
*   **动机**：如果第一条生成的路径发生碰撞，确定性模型就会失败。作者希望引入一种**随机生成式模型**，能够捕捉路径分布的多模态性，从而在推理阶段通过生成多个候选路径（Best-of-N 采样）并并行检测碰撞，找到第一个可行解，显著提升规划成功率。

### 2. 核心方法论
论文提出了 **Flow Motion Policy (FMP)**，其核心思想是将运动规划建模为基于**流匹配（Flow Matching）**的生成任务。
*   **核心思想**：利用流匹配框架构建连续归一化流（CNF），学习从高斯噪声到可行路径增量序列的映射。
*   **关键技术细节**：
    *   **输入编码**：使用 PointNet++ 提取机器人和环境的点云特征，使用 MLP 编码当前位姿和目标位姿。
    *   **特征融合**：采用 Transformer Encoder 作为骨干网络，将上述特征作为 Token 进行全局注意力融合。
    *   **流匹配头**：使用 MLP 预测向量场，通过欧拉积分（Euler Solver）将噪声转化为关节增量序列。
    *   **推理优化（Best-of-N Sampling）**：在推理时并行生成 $N$ 条候选路径，利用 GPU 加速的碰撞检测器（如 cuRobo）评估，执行第一个无碰撞的路径。

### 3. 实验设计
*   **数据集/场景**：使用基于 LLM 生成的多种复杂工作空间，包括 TableTop（桌面）、Box（箱子）、Bins（料仓）和三类不同难度的 Shelf（货架）任务。
*   **Benchmark（基准方法）**：
    *   **传统采样算法**：Bi-RRT, BIT*。
    *   **神经采样器**：MPNets, SIMPNet, GAIDE。
    *   **端到端确定性模型**：PerFACT。
    *   **生成式变体**：将 FMP 的头结构替换为 GMM（参考 Neural MP）或 Diffusion（扩散模型）进行对比。
*   **真实世界验证**：在 UR5e 机械臂上进行了实机部署，测试了料仓和货架等场景。

### 4. 资源与算力
*   **硬件**：论文明确指出实验是在 **NVIDIA RTX 4080 GPU** 上进行的。
*   **训练/推理细节**：使用了 PyTorch 框架。虽然未详细说明总训练时长，但提供了模型参数量对比：FMP 基础模型仅 **1.4M** 参数，远小于 PerFACT (4.15M) 和 Neural MP (20M)。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 种仿真任务和 30 组真实世界实验。
*   **消融实验**：非常充分。作者对比了不同的策略头（MLP, U-Net, Transformer, DiT）、不同的生成框架（Flow vs. Diffusion）、不同的推理步数（Euler steps）以及采样数量 $N$ 对成功率和耗时的影响。
*   **客观性**：通过与多种 SOTA 方法在相同指标（成功率、规划时间）下对比，实验设计较为公平，且包含了实机测试，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能优越**：FMP 在保持极高推理速度的同时，成功率接近或超过了复杂的采样算法。
*   **效率优势**：相比扩散模型（Diffusion），流匹配模型在达到相似成功率的情况下，推理速度显著更快（因为所需的去噪/积分步数更少）。
*   **采样红利**：Best-of-N 采样能显著提升性能。例如在实机测试中，N=100 时的成功率（86.7%）远高于 N=1 时的成功率（33.4%）。
*   **轻量化**：FMP 证明了较小的模型参数量（1.4M）配合高效的生成算法即可处理复杂的 6-DOF 避障规划。

### 7. 优点
*   **高效性**：利用流匹配的特性减少了推理迭代次数，比扩散模型更适合实时机器人应用。
*   **多模态捕捉**：能够生成多样化的路径，有效解决了神经规划器容易陷入局部死胡同的问题。
*   **端到端闭环潜力**：虽然是开环规划，但其高效性为未来实现高频闭环反应式规划奠定了基础。

### 8. 不足与局限
*   **静态环境假设**：目前的模型主要针对静态障碍物，未显式处理动态障碍物。
*   **路径质量**：生成的路径虽然无碰撞，但可能在平滑度或局部最优性上不如专门的轨迹优化算法，可能需要后处理。
*   **泛化限制**：在极其复杂的货架场景下，如果基础策略无法生成多样化的候选路径，Best-of-N 采样的提升效果会受限。

（完）
