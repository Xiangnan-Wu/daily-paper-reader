---
title: "OccSim: Multi-kilometer Simulation with Long-horizon Occupancy World Models"
title_zh: OccSim：基于长时程占用世界模型的多公里级仿真
authors: "Tianran Liu, Shengwen Zhao, Mozhgan Pourkeshavarz, Weican Li, Nicholas Rhinehart"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28887v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于长时程自动驾驶模拟的占据图世界模型
tldr: OccSim是首个基于占据世界模型的3D自动驾驶模拟器，旨在解决传统模拟对预录日志和高精地图的依赖。它仅需初始帧和动作序列，即可稳定生成超过3000帧、跨越4公里的超长时序3D占据地图，生成长度比现有SOTA提升80倍以上。通过W-DiT生成静态环境和Layout Generator生成动态代理，OccSim能合成大规模多样化数据，显著提升下游感知模型的零样本泛化能力。
selection_source: fresh_fetch
motivation: 传统自动驾驶模拟高度依赖预录日志或高精地图，限制了生成规模和开放式场景的构建。
method: 提出由W-DiT静态世界模型和布局生成器组成的架构，通过引入刚性变换实现超长时序的稳定生成。
result: "实验显示OccSim生成的仿真数据可使4D语义占据预测模型在未见数据上达到74%的零样本性能，远超传统模拟器。"
conclusion: OccSim证明了基于占据世界模型的大规模长时序仿真在提升自动驾驶感知系统泛化性方面的巨大潜力。
---

## 摘要
数据驱动的自动驾驶仿真长期以来受限于对预录制驾驶日志或空间先验（如高精地图）的严重依赖。这种根本性的依赖严重限制了可扩展性，将开放式生成能力局限在现有已采集数据集的有限规模内。为了突破这一瓶颈，我们提出了 OccSim，这是首个由占用世界模型驱动的 3D 仿真器。OccSim 摆脱了对连续日志或高精地图的需求；仅以单个初始帧和一系列未来自车动作为条件，它就能稳定生成超过 3,000 个连续帧，从而能够为仿真持续构建跨度超过 4 公里的大规模 3D 占用地图。这代表着在稳定生成长度上，相比之前的最先进占用世界模型提升了 80 倍以上。OccSim 由两个模块驱动：基于 W-DiT 的静态占用世界模型和布局生成器（Layout Generator）。W-DiT 通过在架构设计中显式引入已知的刚性变换，处理静态环境的超长时程生成；而布局生成器则根据合成的道路拓扑，在动态前景中填充交互式智能体。通过这些设计，OccSim 可以合成海量且多样化的仿真流。广泛的实验证明了其下游实用性：直接从 OccSim 收集的数据可以预训练 4D 语义占用预测模型，在未见数据上实现高达 67% 的零样本性能，比之前的基于资产的仿真器高出 11%。当将 OccSim 数据集规模扩大到 5 倍时，零样本性能提升至约 74%，而相比基于资产的仿真器的提升则扩大到 22.1%。

## Abstract
Data-driven autonomous driving simulation has long been constrained by its heavy reliance on pre-recorded driving logs or spatial priors, such as HD maps. This fundamental dependency severely limits scalability, restricting open-ended generation capabilities to the finite scale of existing collected datasets. To break this bottleneck, we present OccSim, the first occupancy world model-driven 3D simulator. OccSim obviates the requirement for continuous logs or HD maps; conditioned only on a single initial frame and a sequence of future ego-actions, it can stably generate over 3,000 continuous frames, enabling the continuous construction of large-scale 3D occupancy maps spanning over 4 kilometers for simulation. This represents an >80x improvement in stable generation length over previous state-of-the-art occupancy world models. OccSim is powered by two modules: W-DiT based static occupancy world model and the Layout Generator. W-DiT handles the ultra-long-horizon generation of static environments by explicitly introducing known rigid transformations in architecture design, while the Layout Generator populates the dynamic foreground with reactive agents based on the synthesized road topology. With these designs, OccSim can synthesize massive, diverse simulation streams. Extensive experiments demonstrate its downstream utility: data collected directly from OccSim can pre-train 4D semantic occupancy forecasting models to achieve up to 67% zero-shot performance on unseen data, outperforming previous asset-based simulator by 11%. When scaling the OccSim dataset to 5x the size, the zero-shot performance increases to about 74%, while the improvement over asset-based simulators expands to 22.1%.

---

## 论文详细总结（自动生成）

### OccSim：基于长时程占用世界模型的多公里级仿真论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的自动驾驶仿真器面临“真实性-多样性-交互性”的权衡难题：几何仿真器（如 CARLA）缺乏传感器真实感，而数据驱动方法通常高度依赖预录制的驾驶日志或高精地图（HD Maps），限制了其在未见场景中的扩展能力。此外，现有的占用世界模型（Occupancy World Models）在长时程生成上表现极差，通常在 50 帧左右就会出现结构崩溃或漂移。
**OccSim 的核心目标**是构建一个**独立于日志**、**长时程稳定**且**具备 3D 一致性**的占用世界模型驱动仿真器。它仅需单帧初始观察和自车动作序列，即可生成数公里的连续 3D 环境。

#### 2. 方法论：核心思想与关键技术
OccSim 采用静态环境与动态代理耦合生成的策略，包含以下核心组件：
*   **W-DiT (Warp-DiT) 静态世界模型**：
    *   **核心思想**：利用 3D 空间的几何刚性。不同于传统模型让网络隐式学习相机运动，W-DiT 显式引入刚性变换矩阵（SE(3) 等变性）来对前一帧的潜在特征（Latent）进行重采样（Warping）。
    *   **掩码注入调节（Mask-injected Conditioning）**：通过几何推导确定可见区域掩码，并结合随机掩码强制模型学习场景补全而非简单的复制。
    *   **SNR 加权感知损失**：在连续潜在空间训练的同时，将去噪后的特征投影回离散占用空间计算交叉熵损失，并根据信噪比（SNR）动态加权，提升几何边界的清晰度。
*   **地图融合与拓扑提取**：
    *   使用基于关键帧的融合算法将单帧局部占用图整合为全球地图。
    *   通过形态学骨架化提取道路拓扑图，并利用“双探测过滤”机制识别可扩展的支路。
*   **布局生成器（Layout Generator）**：
    *   基于潜在流匹配（Latent Flow Matching）训练一个 DiT-S 模型，根据生成的静态道路结构预测合理的初始交通流布局（位置、状态）。
*   **代理控制**：集成 2D 智能驾驶模型（2D-IDM）和 A* 路径规划，实现智能体的闭环交互仿真。

#### 3. 实验设计
*   **数据集**：Occ3D-nuScenes（用于静态生成对比）、UniOcc-Waymo 和 UniOcc-nuScenes（用于统一语义类别的仿真实验）。
*   **对比方法**：
    *   **静态生成**：OccWorld, Occllama, DOME, COME, OccFM, InfiniCubes 等。
    *   **仿真效用**：与基于资产的仿真器（如 UniOcc-CARLA）进行对比。
*   **评估指标**：
    *   **真实性**：2D/3D-FID, KID, MMD。
    *   **多样性**：Vendi Score, 语义 IoU 多样性得分。
    *   **下游效用**：在 4D 语义占用预测任务上的零样本（Zero-shot）迁移性能。

#### 4. 资源与算力
论文明确列出了训练所需的算力资源：
*   **W-DiT 训练**：使用 4 台 **NVIDIA Blackwell RTX Pro 6000** GPU，训练 200 个 epoch，耗时约 **100 GPU 小时**。
*   **布局生成器训练**：使用 8 台 **NVIDIA A100 (80G)** GPU，训练 500 个 epoch，耗时约 **160 GPU 小时**。
*   **推理效率**：在 RTX 4090 上，单帧生成时间约为 1.47 秒，优于部分 SOTA 模型。

#### 5. 实验数量与充分性
*   **实验规模**：进行了长达 3000 帧（约 4 公里）的超长时程生成实验，相比之前模型（约 50 帧）提升了 80 倍。
*   **消融实验**：针对感知损失、双头注入结构、SNR 加权等核心设计进行了详细的消融分析。
*   **多样性测试**：通过 10 个随机种子测试了模型在不同轨迹（直线、环路、转弯）下的表现。
*   **充分性评价**：实验设计较为全面，不仅关注生成质量，还通过下游任务（4D 预测）验证了仿真数据的实际价值，证明了实验的客观性和实用性。

#### 6. 主要结论与发现
*   **长时程稳定性**：W-DiT 成功解决了占用世界模型的崩溃问题，支持数千帧的稳定生成。
*   **仿真效用显著**：使用 OccSim 生成的数据预训练模型，其零样本性能比 CARLA 数据高出 11%-22%。当数据量扩大 5 倍时，性能可达真实数据训练效果的 74%。
*   **解耦生成的有效性**：静态环境与动态布局的解耦使得模型能够生成逻辑自洽且多样化的交通场景。

#### 7. 优点与亮点
*   **突破性长度**：首次实现多公里级别的占用图仿真，生成长度具有代际提升。
*   **日志独立性**：摆脱了对 HD Map 和连续日志的依赖，真正实现了“从单帧开启无限世界”。
*   **几何启发式架构**：将刚性变换显式引入 DiT 架构，比纯隐式学习更符合物理规律。
*   **下游闭环验证**：通过 4D 预测任务证明了生成数据能有效缓解 Sim-to-Real 差距。

#### 8. 不足与局限
*   **启发式融合**：地图融合和车道线提取仍部分依赖启发式算法（如骨架化），而非完全端到端学习。
*   **潜在空间假设**：W-DiT 依赖于潜在空间的旋转不变性，重采样操作可能引入微小的插值误差。
*   **数据规模限制**：受限于语义占用标注数据的稀缺（仅约 10 万帧），模型在极端复杂场景下的表现仍有提升空间。
*   **代理行为简单**：目前主要使用 IDM 控制代理，虽然兼容学习型策略，但原生代理行为的复杂性尚不及真实人类。

（完）
