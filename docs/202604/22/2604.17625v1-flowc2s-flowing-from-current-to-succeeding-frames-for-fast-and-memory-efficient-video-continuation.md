---
title: "FlowC2S: Flowing from Current to Succeeding Frames for Fast and Memory-Efficient Video Continuation"
title_zh: FlowC2S：从当前帧流向后续帧，实现快速且显存高效的视频续写
authors: "Hovhannes Margaryan, Quentin Bammey, Christian Sandor"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17625v1"
tags: ["query:q12"]
score: 9.0
evidence: 视频延续与未来帧合成
tldr: 本研究提出FlowC2S，一种高效的视频续写方法。通过微调预训练的文本生成视频流模型，该方法学习当前帧与后续帧之间的向量场。核心创新包括利用相邻视频块作为固有最优耦合以简化流路径，以及引入目标反演技术增强视觉保真度。相比传统方法，FlowC2S将模型输入维度减半，在仅需5次推理的情况下，在FID和FVD指标上超越了现有最先进技术，实现了更快速且节省显存的视频生成。
selection_source: fresh_fetch
motivation: 旨在解决现有视频续写方法在生成速度和内存效率方面的局限性。
method: 通过引入固有最优耦合和目标反演技术，直接学习当前帧到后续帧的向量场映射并减少输入维度。
result: 在LTXV和Wan模型上微调后，仅需5步推理即可在FID和FVD指标上达到SOTA水平。
conclusion: FlowC2S通过优化流路径和减少计算开销，为实现快速、低显存消耗的高质量视频续写提供了有效方案。
---

## 摘要
本文介绍了一种生成快速且显存高效的视频续写的新方法。我们的方法被称为 FlowC2S，它通过微调预训练的文本到视频流模型，来学习当前视频块与后续视频块之间的向量场。两个关键的设计选择至关重要：首先，我们引入了固有最优耦合，在训练过程中利用时间上相邻的视频块作为真实最优耦合的实际代理，从而产生更平直的流；其次，我们结合了目标反演，将目标块的反演潜变量注入到输入表示中，以增强对应关系并提高视觉保真度。通过直接从当前帧流向后续帧，而不是采用常见的将当前帧与噪声结合来生成视频续写的方式，我们将模型输入的维度降低了一半。该方法基于 LTXV 和 Wan 进行微调，在 FID 和 FVD 的定量评估中超越了现有最先进水平，且仅需五次神经函数评估。

## Abstract
This paper introduces a novel methodology for generating fast and memory-efficient video continuations. Our method, dubbed FlowC2S, fine-tunes a pre-trained text-to-video flow model to learn a vector field between the current and succeeding video chunks. Two design choices are key. First, we introduce inherent optimal couplings, utilizing temporally adjacent video chunks during training as a practical proxy for true optimal couplings, resulting in straighter flows. Second, we incorporate target inversion, injecting the inverted latent of the target chunk into the input representation to strengthen correspondences and improve visual fidelity. By flowing directly from current to succeeding frames, instead of the common combination of current frames with noise to generate a video continuation, we reduce the dimensionality of the model input by a factor of two. The proposed method, fine-tuned from LTXV and Wan, surpasses the state-of-the-art scores across quantitative evaluations with FID and FVD, with as few as five neural function evaluations.

---

## 论文详细总结（自动生成）

### FlowC2S：从当前帧流向后续帧，实现快速且显存高效的视频续写

#### 1. 论文的核心问题与整体含义（研究动机和背景）
视频续写（Video Continuation）旨在给定一段视频序列的基础上，生成逻辑连贯、视觉一致的后续帧。现有的主流方法（如基于扩散模型或流匹配模型）通常将“已知帧”作为条件，并结合“高斯噪声”来生成后续内容。这种设计存在两个主要痛点：
*   **显存开销大**：模型需要同时处理条件帧和等尺寸的噪声张量，导致输入维度翻倍。
*   **推理速度慢**：通常需要大量的神经函数评估（NFE）步数才能获得高质量结果，难以满足 AR/VR 等实时应用的需求。

**FlowC2S** 的核心动机是改变生成范式，直接学习从“当前帧分布”到“后续帧分布”的映射，从而省去噪声输入并加速收敛。

#### 2. 论文提出的方法论
FlowC2S 通过微调预训练的文本生成视频（T2V）流模型，构建了一个直接的向量场映射。

*   **核心思想**：将流匹配（Flow Matching）的初始分布 $p_{init}$ 从标准高斯分布替换为当前视频块的分布。模型学习的是一个从当前帧 $x_0$ 直接“流向”后续帧 $x_1$ 的速度场。
*   **关键技术细节**：
    1.  **固有最优耦合（Inherent Optimal Couplings）**：在流匹配中，源和目标的配对方式决定了流轨迹的平直度。论文提出将同一视频中时间相邻的两个块视为“天然的最优耦合”。这种方法无需复杂的数学计算，就能提供极佳的结构先验，使流轨迹更直，从而在推理时仅需极少步数（如 5 步）即可生成高质量图像。
    2.  **目标反演（Target Inversion, TI）**：为了增强视觉保真度，作者利用 RF-Solver 反演技术，将目标块的潜变量信息注入输入表示中。这有助于模型学习残差变换，减少生成过程中的色彩偏移和细节丢失。
    3.  **输入维度减半**：由于不再需要输入噪声张量，模型的输入维度降低了 50%，显著降低了峰值显存占用。

#### 3. 实验设计
*   **数据集**：
    *   **OpenVid-1M**：选取了 400K 高质量视频子集进行训练。
    *   **NuScenes**：用于验证模型在自动驾驶场景下的泛化能力。
*   **Benchmark 与对比方法**：
    *   **对比对象**：包括世界模型（Vista, GEM）和自回归 T2V 模型（LTXVCondition, CausVid）。
    *   **评估指标**：FID（图像质量）、FVD（视频连贯性）、LPIPS（缝隙平滑度）、VBench（综合一致性）以及 GPU 显存占用和 NFE 步数。

#### 4. 资源与算力
论文明确记录了实验所用的算力资源：
*   **GPU 型号**：NVIDIA H100 (80GB)。
*   **算力规模**：
    *   基于 **LTXV (2B)** 骨干：使用 28 台 H100。
    *   基于 **Wan (1.3B)** 骨干：使用 56 台 H100（以匹配 Batch Size）。
*   **训练参数**：总 Batch Size 为 14,336，训练步数为 1406 步，采用 AdamW 优化器。

#### 5. 实验数量与充分性
作者进行了多维度的实验，体现了较高的客观性：
*   **跨骨干网络验证**：在 LTXV 和 Wan 两个主流开源模型上均实现了性能提升。
*   **消融实验**：详细对比了“从头训练 vs 微调”、“有无最优耦合”、“有无目标反演”的效果，证明了各组件的必要性。
*   **推理步数实验**：测试了从 1 步到 40 步 NFE 的表现，证明了 5-10 步即可达到 SOTA。
*   **长视频泛化**：测试了模型在超出训练长度（如 113 帧）时的表现。
*   **用户主观研究**：邀请 15 名参与者对 20 组视频进行盲测，验证了 TI 技术的感知优势。

#### 6. 主要结论与发现
*   **性能领先**：FlowC2S 在 FID 和 FVD 指标上均优于现有最先进方法（如 Vista 和 CausVid）。
*   **极高效率**：在 LTXV 骨干上，相比原版条件生成方法，FlowC2S 减少了约 50% 的峰值显存，且推理速度快了 4 倍（仅需 5 步 NFE）。
*   **最优耦合有效性**：实验证实，时间相邻的视频块确实可以作为最优传输路径的有效代理，显著简化了生成任务。

#### 7. 优点（亮点）
*   **范式创新**：打破了“条件帧+噪声”的固有思维，通过改变初始分布实现了输入维度的本质压缩。
*   **数学动机强**：巧妙利用视频的时间连续性来近似最优耦合，理论与实践结合紧密。
*   **实用性极高**：大幅降低了视频生成的门槛，使得在嵌入式设备或 AR 硬件上运行高质量视频续写成为可能。

#### 8. 不足与局限
*   **场景切换处理能力弱**：由于训练数据剔除了场景剧烈切换的片段，模型难以生成具有突发性变化的视频内容。
*   **固定长度限制**：训练时使用了固定长度（17 或 41 帧），虽然能外推到 113 帧，但更长（如 129 帧以上）会出现明显的插值伪影和连贯性崩溃。
*   **缺乏显式控制**：目前仅支持基于文本和已知帧的续写，尚未集成相机轨迹、深度图等精细化的显式控制信号。

（完）
