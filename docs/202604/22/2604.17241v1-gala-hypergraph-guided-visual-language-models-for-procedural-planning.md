---
title: "GaLa: Hypergraph-Guided Visual Language Models for Procedural Planning"
title_zh: GaLa：用于程序化规划的超图引导视觉语言模型
authors: "Kun Wang, Yiming Li, Mingcheng Qu, Aqiang Zhang, Guang Yang, Tonghua Su"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17241v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于具身智能程序规划的视觉语言框架
tldr: 针对具身智能中的程序化规划任务，现有视觉语言模型（VLM）难以有效捕捉复杂的空间关系和深层语义。本文提出GaLa框架，通过超图表示法将物体建模为节点，并根据属性和功能语义构建区域级超边。结合TriView超图编码器和对比学习，GaLa能显式捕捉物体间的隐式语义关系和功能区域的层次结构，显著提升了复杂场景下的规划能力。
selection_source: fresh_fetch
motivation: 现有方法过度依赖VLM自身的推理能力，忽视了多模态输入中丰富的结构化语义信息和隐式空间关系。
method: 提出GaLa框架，利用超图建模物体及其功能区域，并通过TriView编码器在节点、区域及关联视图间通过对比学习强制执行语义一致性。
result: 在ActPlan1K和ALFRED基准测试中，GaLa在执行成功率、LCS和规划正确性方面均显著优于现有方法。
conclusion: 通过超图显式建模结构化语义关系，能有效增强视觉语言模型在复杂具身环境下的程序化规划性能。
---

## 摘要
编码在物体属性中的隐式空间关系和深层语义结构对于具身智能系统中的程序化规划至关重要。然而，现有方法往往过度依赖视觉语言模型（VLM）本身的推理能力，而忽视了可以从多模态输入中挖掘的丰富结构化语义信息。因此，模型难以在复杂场景中有效理解功能性空间关系。为了充分利用多模态数据中的隐式空间关系和深层语义结构，我们提出了 GaLa，一个用于多模态程序化规划的视觉语言框架。GaLa 引入了一种基于超图的表示方法，将图像中的物体实例建模为节点，并根据物体的属性和功能语义聚合物体来构建区域级超边。这种设计显式地捕捉了物体间的隐式语义关系以及功能区域的层级组织。此外，我们设计了一个三视图超图编码器（TriView HyperGraph Encoder），通过对比学习在节点视图、区域视图和节点-区域关联视图之间强制实现语义一致性，从而使超图语义能够更有效地注入到下游 VLM 推理中。在 ActPlan1K 和 ALFRED 基准测试上的广泛实验表明，GaLa 在执行成功率、LCS 和规划正确性方面显著优于现有方法。

## Abstract
Implicit spatial relations and deep semantic structures encoded in object attributes are crucial for procedural planning in embodied AI systems. However, existing approaches often over rely on the reasoning capabilities of vision language models (VLMs) themselves, while overlooking the rich structured semantic information that can be mined from multimodal inputs. As a result, models struggle to effectively understand functional spatial relationships in complex scenes. To fully exploit implicit spatial relations and deep semantic structures in multimodal data, we propose GaLa, a vision language framework for multimodal procedural planning. GaLa introduces a hypergraph-based representation, where object instances in the image are modeled as nodes, and region-level hyperedges are constructed by aggregating objects according to their attributes and functional semantics. This design explicitly captures implicit semantic relations among objects as well as the hierarchical organization of functional regions. Furthermore, we design a TriView HyperGraph Encoder that enforces semantic consistency across the node view, area view, and node area association view via contrastive learning, enabling hypergraph semantics to be more effectively injected into downstream VLM reasoning. Extensive experiments on the ActPlan1K and ALFRED benchmarks demonstrate that GaLa significantly outperforms existing methods in terms of execution success rate, LCS, and planning correctness.

---

## 论文详细总结（自动生成）

以下是对论文《GaLa: Hypergraph-Guided Visual Language Models for Procedural Planning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在具身智能（Embodied AI）领域，程序化规划要求智能体将高层指令分解为精确的动作序列。现有的视觉语言模型（VLM）在规划时主要依赖显式的几何信息（如边界框）或模型自身的推理能力，往往忽视了图像中蕴含的**隐式空间关系**（如“可放置于”、“被支撑”）和**深层语义结构**（如由多个物体构成的“厨房区”等功能区域）。这导致模型在复杂或反直觉场景中容易出现逻辑死锁、动作重复或规划失败。

### 2. 方法论：核心思想与关键技术
论文提出了 **GaLa** 框架，通过引入“超图”（Hypergraph）作为视觉与语言之间的中间语义抽象。
*   **超图语义构建（Step 1）**：
    *   **节点建模**：利用 YOLO-World 检测物体，提取位置、类别和属性描述作为超图节点。
    *   **超边构建**：利用 DBSCAN 空间聚类算法将物体分组，并调用大模型（LLM）根据组内物体属性推断区域语义（如“用餐区”），形成连接多个节点的超边。
    *   **反事实评分**：利用 LLM 对物体属性进行评分，识别环境中的异常状态。
*   **三视图超图编码器（Tri-View HyperGraph Encoder, Step 2）**：
    *   设计了三个互补视图：**节点视图**（物体级别）、**区域视图**（超边级别）和**全视图**（节点-区域关联）。
    *   采用**对比学习（InfoNCE Loss）**：通过对节点和超边进行随机掩码增强，强制模型在不同视图间保持语义一致性，确保结构化知识被有效编码。
*   **VLM 训练与推理（Step 3）**：
    *   将精炼后的超图知识（结构化文本）与原始图像、任务指令一同输入 VLM（以 InternVL3-8B 为底座）进行微调，预测下一步动作。

### 3. 实验设计
*   **数据集**：
    *   **ActPlan-1K**：包含正常活动和反事实（Counterfactual）活动，专门测试模型在非常规条件下的自适应规划能力。
    *   **ALFRED**：用于测试具身任务的零样本泛化性能。
*   **Benchmark（评估指标）**：
    *   **执行成功率（Exec.）**：生成的动作在模拟环境中可执行的比例。
    *   **LCS**：生成计划与参考计划的最长公共子序列相似度。
    *   **任务正确性（Corr.）**：最终是否完成了预定目标。
*   **对比方法**：包括闭源模型（GPT-4o, Gemini-Pro-1.5）、开源通用 VLM（LLaVA-OV, Qwen2-VL, InternVL 系列）以及专门的规划模型（Embodied-GPT, LLaPa）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA HGX H200 (141G)** GPU。
*   **训练配置**：训练时长为 30 个 epoch，采用 Adam 优化器，初始学习率为 2e-5，Batch Size 为 128。
*   **模型规模**：主干网络为 InternVL3-8B。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准数据集上进行了全面测试，涵盖了从 4.5B 到 8B 规模的十余种对比模型。
*   **消融实验**：针对超图结构、节点对比损失（HLn）、区域对比损失（HLg）和关联对比损失（HLm）分别进行了消融研究。
*   **充分性评价**：实验设计较为充分，不仅对比了通用模型，还对比了最新的专门化规划模型（如 LLaPa），并区分了正常与反事实场景，证明了方法在处理复杂逻辑时的鲁棒性。

### 6. 主要结论与发现
*   **超图的有效性**：显式建模物体间的层级关系和区域语义，能显著减少模型在模糊环境下的逻辑死锁。
*   **性能领先**：GaLa 在 ActPlan-1K 的反事实任务中 Correctness 达到 37.2%，在 ALFRED 上达到 50.1%，均刷新了 SOTA 记录。
*   **对比学习的作用**：三视图对比学习确保了超图中的结构化信息能被 VLM 充分吸收，而非仅仅作为冗余文本输入。

### 7. 优点
*   **结构化创新**：首次将图论中的超图结构引入 VLM 程序化规划，解决了物体孤立建模的问题。
*   **反事实推理**：通过超图捕捉到的深层语义，增强了模型在非常规场景（如物体出现在不该出现的地方）下的推理能力。
*   **即插即用**：该框架作为一种语义增强手段，理论上可以适配不同的 VLM 底座。

### 8. 不足与局限
*   **信息完整性风险**：由于 ActPlan-1K 提供的动作关联图像数量有限，构建的超图可能存在信息缺失，导致语义不完整。
*   **技术深度限制**：目前仅使用了基础的超图构造和对比学习，尚未深入探索更高级的图神经网络技术（如动态图演化）对规划的潜在提升。
*   **实时性挑战**：构建超图涉及聚类和多次 LLM 调用，在实时具身交互中可能存在延迟压力。

（完）
