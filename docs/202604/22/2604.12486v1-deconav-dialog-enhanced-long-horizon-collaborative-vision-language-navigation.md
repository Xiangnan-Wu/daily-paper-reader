---
title: "DeCoNav: Dialog enhanced Long-Horizon Collaborative Vision-Language Navigation"
title_zh: DeCoNav：对话增强的长程协作视觉语言导航
authors: "Sunyao Zhou, Yunzi Wu, Tianhang Wang, Xinhai Li, Guang Chen, Lizheng Liu, Chenjia Bai, Xuelong Li"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12486v1"
tags: ["query:robot"]
score: 9.0
evidence: 多机器人系统的协作视觉语言导航
tldr: 针对多机器人长程协作视觉语言导航（VLN）中现有方法缺乏实时同步和动态协调的问题，本文提出了DeCoNav框架。该框架采用去中心化架构，通过事件触发的对话机制让机器人交换语义状态，实现动态任务分配与重新规划。在DeCoNavBench上的实验表明，该方法显著提升了双机成功率（BSR），为复杂环境下的多机协作提供了高效的实时自适应方案。
selection_source: fresh_fetch
motivation: 现有的协作导航基准缺乏严格的同步执行机制，且静态协调策略无法应对任务执行中新出现的跨代理信息。
method: 提出一种去中心化框架，利用事件触发的对话进行语义状态交换，并结合动态任务分配与实时同步重规划。
result: "在包含176个场景的DeCoNavBench测试中，该方法将双机成功率（BSR）提升了69.2%。"
conclusion: 对话驱动的动态重规划机制能有效增强多机器人在长程协作导航任务中的实时适应能力和协作效率。
---

## 摘要
长程协作视觉语言导航（VLN）对于多机器人系统完成超出单个智能体能力的复杂任务至关重要。CoNavBench 迈出了第一步，引入了首个具有接力式多机器人任务、协作分类法以及基于图的生成和评估的长程协作 VLN 基准，用于模拟共享环境中的交接和汇合。然而，现有的基准和评估通常不强制在共享的世界时间线上进行严格同步的双机器人部署，且通常依赖于静态协调策略，无法在出现新的跨智能体证据时进行调整。我们提出了对话增强的长程协作视觉语言导航（DeCoNav），这是一个将事件触发的对话与动态任务分配和重新规划相结合的去中心化框架，用于实现实时、自适应的协调。在 DeCoNav 中，机器人在没有中央控制器的情况下通过对话交换紧凑的语义状态。当出现新证据、不确定性或冲突等信息性事件时，会触发对话以在同步执行下动态重新分配子目标并重新规划。在包含 176 个 HM3D 场景、1,213 个任务的 DeCoNavBench 中实现，DeCoNav 将双成功率（BSR）提高了 69.2%，证明了对话驱动、动态重新分配规划在多机器人协作中的有效性。

## Abstract
Long-horizon collaborative vision-language navigation (VLN) is critical for multi-robot systems to accomplish complex tasks beyond the capability of a single agent. CoNavBench takes a first step by introducing the first collaborative long-horizon VLN benchmark with relay-style multi-robot tasks, a collaboration taxonomy, along with graph-grounded generation and evaluation to model handoffs and rendezvous in shared environments. However, existing benchmarks and evaluations often do not enforce strictly synchronized dual-robot rollout on a shared world timeline, and they typically rely on static coordination policies that cannot adapt when new cross-agent evidence emerges. We present Dialog enhanced Long-Horizon Collaborative Vision-Language Navigation (DeCoNav), a decentralized framework that couples event-triggered dialogue with dynamic task allocation and replanning for real-time, adaptive coordination. In DeCoNav, robots exchange compact semantic states via dialogue without a central controller. When informative events such as new evidence, uncertainty, or conflicts arise, dialogue is triggered to dynamically reassign subgoals and replan under synchronized execution. Implemented in DeCoNavBench with 1,213 tasks across 176 HM3D scenes, DeCoNav improves the both-success rate (BSR) by 69.2%, demonstrating the effectiveness of dialogue-driven, dynamically reallocated planning for multi-robot collaboration.

---

## 论文详细总结（自动生成）

### DeCoNav：对话增强的长程协作视觉语言导航论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：长程协作视觉语言导航（VLN）要求多个机器人理解共享指令、同步行动并应对局部观测的局限性。
*   **研究背景与动机**：
    *   现有基准（如 CoNavBench）虽然提出了协作任务，但存在两个关键缺陷：一是评估时缺乏严格的**双机同步执行（Synchronized Rollout）**，导致忽略了真实的交互效应；二是协调策略通常是**静态的**，无法根据执行过程中新出现的证据（如某机器人提前发现目标）动态调整计划。
    *   论文旨在提出一种去中心化、可实时动态重规划的协作框架，以解决长程任务中的不确定性和效率问题。

#### 2. 方法论
DeCoNav 是一个去中心化框架，核心由三个耦合模块组成：
*   **语义视觉总线 (SVB)**：通信骨干。机器人不交换原始图像，而是发布紧凑的语义数据包（当前房间、观测到的关键物体、任务阶段状态、时间戳），在低带宽下保留关键协调信号。
*   **事件驱动对话重规划 (EDR)**：在线策略更新机制。对话仅由“信息性事件”（如阶段完成、首次发现目标、本地与伙伴证据冲突、停滞）触发。触发后，机器人通过对话交换语义，动态重新分配子目标优先级和接力责任。
*   **同步并行执行 (SPE)**：强制共享世界时钟。两台机器人在同一因果时间线上采取行动，确保评估的一致性。
*   **ROVE 数据生成管线**：为了确保基准测试的可靠性，提出了 ROVE 管线，通过 **RTSA**（三阶段房间类型语义对齐）和 **TriGate**（三重门目标验证：语义可见性、房间一致性、CLIP 识别度检查）来消除环境标注中的噪声。

#### 3. 实验设计
*   **数据集/场景**：基于 HM3D 场景构建了 **DeCoNavBench**。
    *   包含 176 个场景，1,213 个协作导航任务。
    *   平均每台机器人路径长度为 20.6 米，总计超过 205 万张图像。
*   **Benchmark 与对比方法**：
    *   对比了 **CoNavBench** 的基准模型。
    *   进行了**消融实验**：对比了“有无 DeCoNav 协调机制”在相同同步协议下的表现。
    *   **真实机器人实验**：使用两台宇树（Unitree）人形机器人，在非结构化办公环境中执行“跨房间物体运送”任务。

#### 4. 资源与算力
*   **数据标注算力**：在 RTSA 管线中使用了高性能视觉语言模型，包括 **GPT-5.2** 和 **Qwen3-VL-235B-A22B-Instruct** 进行房间分类。
*   **训练/推理算力**：论文**未明确说明**具体用于模型训练的 GPU 型号、数量及训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 1,213 个任务上进行了大规模仿真测试，样本量充足。
*   **充分性与客观性**：
    *   **消融实验**：通过固定执行协议仅改变协调机制，有效隔离了对话重规划的贡献。
    *   **数据质量**：通过 RTSA 将房间标注准确率从 68% 提升至 100%（经人工校验），确保了评估的公平性，避免了因标注噪声导致的性能误判。
    *   **真实场景验证**：通过人形机器人的实地部署，证明了该去中心化逻辑在异步通信和动态障碍环境下的鲁棒性。

#### 6. 主要结论与发现
*   **性能提升**：DeCoNav 在双机成功率（BSR）上比 CoNavBench 基准提升了 **69.2%**（绝对值提升 11%）。
*   **动态协调的价值**：实验证明，当机器人能够根据实时发现的证据（如路径封锁）自主协商交换子任务时，可以显著缩短总路径长度并提高成功率。
*   **同步评估的重要性**：强调了在共享时间线上进行同步执行是衡量协作质量的必要条件。

#### 7. 优点（亮点）
*   **动态性**：突破了以往协作导航中固定的角色分配，实现了基于事件的实时重规划。
*   **数据纯净度**：ROVE 管线极大地提高了 HM3D 数据的语义质量，为后续研究提供了更可靠的基准。
*   **部署一致性**：仿真中的去中心化逻辑与真实多机系统的分布式部署高度一致，具有很强的实用价值。

#### 8. 不足与局限
*   **模型依赖**：数据生成高度依赖于顶级 VLM（如 GPT-5.2），对于缺乏此类 API 访问权限的研究者来说，复现或扩展数据集成本较高。
*   **动作空间限制**：仿真实验仍基于离散动作空间（前进、左右转），与真实机器人连续控制之间仍存在差距。
*   **通信假设**：虽然使用了紧凑语义，但仍假设机器人之间存在可靠的（尽管是异步的）通信链路，未深入探讨极端弱网环境下的表现。

（完）
