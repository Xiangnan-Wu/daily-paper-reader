---
title: "Mask World Model: Predicting What Matters for Robust Robot Policy Learning"
title_zh: 掩码世界模型：预测关键信息以实现鲁棒的机器人策略学习
authors: "Yunfan Lou, Xiaowei Chi, Xiaojie Zhang, Zezhong Qian, Chengxuan Li, Rongyu Zhang, Yaoxu Lyu, Guoyu Song, Chuyao Fu, Haoxuan Xu, Pengwei Wang, Shanghang Zhang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19683v1"
tags: ["query:robot"]
score: 10.0
evidence: 利用扩散模型进行鲁棒机器人策略学习的世界模型
tldr: 针对视频生成预训练在机器人策略学习中容易过拟合背景和光照等无关因素的问题，本文提出了掩码世界模型（MWM）。该模型利用视频扩散架构预测语义掩码的演变而非像素，通过几何信息瓶颈过滤视觉噪声，捕捉核心物理动力学。实验证明，MWM在LIBERO和RLBench等基准测试及现实场景中表现优异，具有极强的鲁棒性和泛化能力。
selection_source: fresh_fetch
motivation: 传统的基于RGB视频预测的世界模型容易受到背景和光照等无关视觉因素的干扰，导致生成的控制策略脆弱且难以泛化。
method: 提出掩码世界模型（MWM），利用视频扩散架构预测语义掩码的演变，并将其与扩散策略头集成以实现端到端控制。
result: 在LIBERO、RLBench基准测试及真实世界实验中，MWM的表现显著优于现有的基于RGB的世界模型，并展现出对纹理丢失的强大韧性。
conclusion: 通过预测语义掩码而非像素，MWM能有效提取关键物理动力学信息并过滤视觉噪声，显著提升了机器人策略学习的稳健性。
---

## 摘要
源自大规模视频生成预训练的世界模型已成为通用机器人策略学习的一种极具前景的范式。然而，标准方法通常侧重于高保真 RGB 视频预测，这可能导致模型过拟合于无关因素，例如动态背景和光照变化。这些干扰降低了模型的泛化能力，最终导致控制策略不可靠且脆弱。为解决这一问题，我们引入了掩码世界模型（MWM），该模型利用视频扩散架构来预测语义掩码的演变而非像素。这种转变引入了几何信息瓶颈，迫使模型在过滤视觉噪声的同时，捕捉核心物理动力学和接触关系。我们将这种掩码动力学骨干网络与基于扩散的策略头无缝集成，以实现鲁棒的端到端控制。广泛的评估证明了 MWM 在 LIBERO 和 RLBench 仿真基准测试中的优越性，显著优于目前最先进的基于 RGB 的世界模型。此外，真实世界实验和鲁棒性评估（通过随机令牌剪枝）表明，MWM 展现出卓越的泛化能力以及对纹理信息丢失的强韧性。

## Abstract
World models derived from large-scale video generative pre-training have emerged as a promising paradigm for generalist robot policy learning. However, standard approaches often focus on high-fidelity RGB video prediction, this can result in overfitting to irrelevant factors, such as dynamic backgrounds and illumination changes. These distractions reduce the model's ability to generalize, ultimately leading to unreliable and fragile control policies. To address this, we introduce the Mask World Model (MWM), which leverages video diffusion architectures to predict the evolution of semantic masks instead of pixels. This shift imposes a geometric information bottleneck, forcing the model to capture essential physical dynamics and contact relations while filtering out visual noise. We seamlessly integrate this mask dynamics backbone with a diffusion-based policy head to enable robust end-to-end control. Extensive evaluations demonstrate the superiority of MWM on the LIBERO and RLBench simulation benchmarks, significantly outperforming the state-of-the-art RGB-based world models. Furthermore, real-world experiments and robustness evaluation (via random token pruning) reveal that MWM exhibits superior generalization capabilities and robust resilience to texture information loss.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Mask World Model (MWM)** 的新型机器人策略学习框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于视频生成预训练的世界模型（World Models）大多专注于预测高保真的 **RGB 像素**。然而，RGB 图像包含大量与控制任务无关的“视觉噪声”（如背景动态、光照变化、纹理等）。
*   **研究动机**：这种对像素细节的过度关注会导致模型过拟合，使得学习到的策略在面对环境变化（如换个桌布、光线变暗）时表现得非常脆弱。
*   **整体含义**：论文提出，机器人真正需要预测的是“与决策相关的动力学”。通过将预测目标从像素转向**语义掩码（Semantic Masks）**，可以引入一个“几何信息瓶颈”，迫使模型专注于物体身份、空间布局和接触关系，从而提升策略的鲁棒性和泛化能力。

### 2. 方法论：核心思想与技术细节
MWM 采用两阶段训练策略，将掩码动力学骨干网络与扩散策略头集成：

*   **核心思想**：在训练阶段利用语义掩码作为监督信号，但在推理阶段仅使用原始 RGB 图像，从而避免了对外部分割模型的依赖。
*   **关键技术细节**：
    1.  **掩码动力学骨干（Mask Dynamics Backbone）**：使用基于 **DiT (Diffusion Transformer)** 的架构。通过预训练的视频 VAE 将 RGB 帧和渲染后的掩码编码到统一的潜空间。
    2.  **条件流匹配（Conditional Flow Matching）**：在第一阶段，模型学习根据历史 RGB 帧预测未来的语义掩码潜变量。
    3.  **几何信息瓶颈**：掩码预测过滤掉了光照和纹理，只保留物理交互所需的几何结构。
    4.  **扩散策略头（Diffusion Policy Head）**：在第二阶段，策略头通过交叉注意力机制访问骨干网络生成的“预测特征库（Predictive Feature Bank）”，生成连续动作序列。
    5.  **架构优化**：引入了 3D RoPE（旋转位置编码）以处理压缩后的潜变量，并使用 AdaIN 风格的调制来增强时间步条件的稳定性。

### 3. 实验设计
*   **仿真基准测试**：
    *   **LIBERO**：包含 130 个任务，评估了 Spatial、Object、Goal 和 Libero-10 四个子集。
    *   **RLBench**：选择了 6 个代表性任务（如扫地、插花、浇水等）。
*   **对比方法**：包括 OpenVLA、CogACT、$\pi_0$、Cosmos+IDM 以及强基线 GE-ACT（基于 RGB 的世界模型）。
*   **现实场景**：使用 Franka 机器人完成 4 类任务（放面包、开抽屉、倒水、放书），并进行了 **OOD（分布外）泛化测试**（改变背景、光照、物体颜色）。
*   **鲁棒性测试**：通过**随机令牌剪枝（Random Token Pruning）**模拟计算受限或观察受损的情况。

### 4. 资源与算力
*   **硬件**：使用了由 **8 台 NVIDIA A100 (80GB)** 组成的 GPU 集群。
*   **训练时长**：
    *   **第一阶段（动力学预训练）**：约 3.5 天（30k 步）。
    *   **第二阶段（策略学习）**：每个任务套件约 1.5 天（18k 步）。
*   **软件框架**：使用了 DeepSpeed ZeRO-2 优化器。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从大规模仿真（数百个任务、数千次评估）到真实机器人的广泛实验。
*   **消融实验**：对比了 MWM 的不同变体（如直接解码掩码 vs. 使用预测特征），验证了特征耦合的有效性。
*   **充分性与公平性**：实验设计非常全面，不仅对比了成功率，还通过 AUC 指标量化了鲁棒性。所有基线方法在相同的数据和训练配方下进行对比，确保了实验的客观性和公平性。

### 6 主要结论与发现
*   **性能领先**：MWM 在 LIBERO 上的平均成功率达到 **98.3%**，在 RLBench 上达到 **68.3%**，显著优于基于 RGB 的模型（如 GE-ACT 的 30.8%）。
*   **泛化能力强**：在真实世界 OOD 测试中，MWM 的成功率保持在 42.1%，而 GE-ACT 仅为 12.5%。尤其在背景改变时，RGB 模型几乎崩溃，而 MWM 表现稳健。
*   **抗干扰性**：即使随机剪掉 50% 以上的视觉令牌，MWM 依然能保持较高的成功率，证明其捕捉到了关键的语义结构。

### 7. 优点
*   **创新的瓶颈设计**：通过语义掩码预测解决了视频世界模型中常见的“视觉干扰”问题。
*   **推理高效**：虽然训练需要掩码，但推理时是 **RGB-only**，不需要实时运行分割模型，降低了部署门槛。
*   **端到端集成**：将世界模型的预测特征直接馈送给扩散策略，实现了感知与控制的深度耦合。

### 8. 不足与局限
*   **训练数据依赖**：第一阶段训练需要高质量的语义掩码标注。虽然文中使用了自动标注工具（RoboEngine），但在极其复杂的非结构化环境中，掩码的获取成本和准确性仍是挑战。
*   **任务范围**：目前的实验主要集中在桌面操纵任务，对于需要极高精细纹理识别（如区分不同材质的柔软度）的任务，过度过滤 RGB 信息可能会带来负面影响。
*   **计算开销**：尽管推理时不需要分割模型，但基于 DiT 的骨干网络参数量较大，对实时控制频率（文中为 10Hz）有一定要求。

（完）
