---
title: "Curiosity-Critic: Cumulative Prediction Error Improvement as a Tractable Intrinsic Reward for World Model Training"
title_zh: Curiosity-Critic：将累积预测误差改进作为世界模型训练中一种易于处理的内在奖励
authors: "Vin Bhaskara, Haicheng Wang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18701v1"
tags: ["query:robot"]
score: 9.0
evidence: 基于预测误差的世界模型训练内在奖励
tldr: 针对世界模型训练中局部预测误差好奇心机制的局限性，本文提出 Curiosity-Critic。该方法将内在奖励定义为累积预测误差的改进量，并将其简化为当前预测误差与渐近误差基准之差。通过在线学习的 Critic 估计基准，该方法能有效区分可减少的认知不确定性和不可减少的偶然不确定性，在随机环境下显著提升了探索效率和模型精度。
selection_source: fresh_fetch
motivation: 传统的基于预测误差的好奇心奖励仅关注当前转换，忽略了世界模型在所有已访问转换上的累积误差改进。
method: 提出一种名为 Curiosity-Critic 的机制，通过学习一个 Critic 来在线估计状态转换的渐近误差基准，并将预测误差与该基准的差值作为内在奖励。
result: 在随机网格世界实验中，该方法在收敛速度和最终世界模型的准确性上均优于传统的预测误差和访问计数基准。
conclusion: Curiosity-Critic 提供了一种可处理的内在奖励形式，能有效分离认知与偶然不确定性，为世界模型训练提供了更优的探索策略。
---

## 摘要
基于局部预测误差的好奇心奖励关注当前的转移，而没有考虑世界模型在所有已访问转移上的累积预测误差。我们引入了 Curiosity-Critic，其内在奖励基于这一累积目标的改进，并证明它可以简化为一种易于处理的单步形式：即当前预测误差与当前状态转移的渐近误差基准之间的差值。我们通过一个与世界模型共同训练的学习型评论家（critic）在线估计该基准；通过回归单个标量，该评论家在世界模型饱和之前就能很好地收敛，从而在无需噪声底面先验知识的情况下，将探索重新导向可学习的转移。对于可学习的转移，该奖励较高，而对于随机转移，奖励会向基准坍缩，从而在线有效地分离了认知（可减少的）预测误差与偶然（不可减少的）预测误差。从 Schmidhuber (1991) 到学习特征空间变体，先前的预测误差好奇心公式都作为对应于该基准特定近似的特例出现。在随机网格世界上的实验表明，Curiosity-Critic 在收敛速度和最终世界模型准确性方面优于预测误差和访问计数基准。

## Abstract
Local prediction-error-based curiosity rewards focus on the current transition without considering the world model's cumulative prediction error across all visited transitions. We introduce Curiosity-Critic, which grounds its intrinsic reward in the improvement of this cumulative objective, and show that it reduces to a tractable per-step form: the difference between the current prediction error and the asymptotic error baseline of the current state transition. We estimate this baseline online with a learned critic co-trained alongside the world model; regressing a single scalar, the critic converges well before the world model saturates, redirecting exploration toward learnable transitions without oracle knowledge of the noise floor. The reward is higher for learnable transitions and collapses toward the baseline for stochastic ones, effectively separating epistemic (reducible) from aleatoric (irreducible) prediction error online. Prior prediction-error curiosity formulations, from Schmidhuber (1991) to learned-feature-space variants, emerge as special cases corresponding to specific approximations of this baseline. Experiments on a stochastic grid world show that Curiosity-Critic outperforms prediction-error and visitation-count baselines in convergence speed and final world model accuracy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Curiosity-Critic** 的新型内在奖励机制，旨在解决强化学习中世界模型（World Model）训练的效率与鲁棒性问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的基于“好奇心”的内在奖励（如 Schmidhuber 的预测误差模型）通常只关注单步预测误差。这导致了两个主要问题：
    1.  **忽略累积改进**：只看当前步，而不考虑该样本对模型在所有已访问数据上总误差的贡献。
    2.  **“嘈杂电视”问题（Noisy TV Problem）**：当环境中存在不可预测的随机噪声（偶然不确定性，Aleatoric Uncertainty）时，传统的预测误差奖励会使智能体被困在这些无意义的随机区域，因为误差永远无法降为零。
*   **核心目标**：提出一种理论上严谨且计算上可行的方法，将“累积预测误差的改进”作为奖励，从而有效区分“可学习的知识”和“不可减少的噪声”。

### 2. 方法论
*   **核心思想**：将内在奖励定义为：如果模型在当前转换上进行了训练，其在整个历史数据集上的总误差下降了多少。
*   **关键技术细节**：
    *   **数学简化**：通过数学推导（利用伸缩求和特性），作者证明了看似不可计算的“历史累积误差改进”在极限条件下可以简化为：**当前步的预测误差 - 该状态转换的渐近误差基准（即模型收敛后的剩余误差）**。
    *   **Curiosity-Critic 架构**：引入一个额外的神经评论家（Neural Critic）网络 $\phi$。
        *   **目标**：Critic 学习预测世界模型在更新后的“后验误差”（Post-update error）。
        *   **奖励公式**：$r_t = e_{before} - \phi(s_t, a_t)$。其中 $e_{before}$ 是世界模型更新前的误差，$\phi$ 是 Critic 预测的该状态下的噪声底面（Noise Floor）。
    *   **算法流程**：在每一步中，智能体观察转换 -> 计算更新前的误差 -> 更新世界模型 -> 计算更新后的误差 -> 使用更新后的误差作为标签训练 Critic -> 计算奖励并更新策略。

### 3. 实验设计
*   **场景设置**：一个 30x30 的随机网格世界（Stochastic Grid World）。
    *   **左半部分**：确定性区域，包含复杂的、可学习的像素模式。
    *   **右半部分**：随机区域，每一步都产生完全随机的噪声（模拟“嘈杂电视”）。
*   **Benchmark 与对比方法**：
    *   **Random**：随机探索。
    *   **Curiosity V1**：基于原始预测误差（Schmidhuber 1991）。
    *   **Curiosity V2**：基于单步学习进度（Schmidhuber 1991）。
    *   **Visitation Count**：基于访问次数的奖励。
    *   **Oracle Critic**：使用环境真实噪声水平作为基准的理想上限。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号或训练总时长。
*   **模型规模**：世界模型是一个两层 MLP（1024 隐藏单元），Critic 是一个更小的 MLP（128 隐藏单元）。实验在网格世界中进行，通常这类实验对算力要求较低，单机 CPU 或普通消费级 GPU 即可完成。

### 5. 实验数量与充分性
*   **实验规模**：所有实验均在 5 个独立随机种子下运行，并提供了均值和标准差。
*   **消融实验**：对比了“神经 Critic”（带泛化能力）和“表格化 Critic”（无泛化能力），证明了神经网络在估计噪声底面时的泛化优势。
*   **充分性评价**：在所设定的受控实验环境下，实验非常充分且客观。它精准地隔离了“噪声区分”这一核心变量。然而，实验目前仅限于 2D 网格世界，尚未在 Atari 或 VizDoom 等高维视觉环境中验证。

### 6. 主要结论与发现
*   **有效分离不确定性**：Curiosity-Critic 能够准确识别哪些误差是由于模型还没学会（认知不确定性），哪些是由于环境本身随机（偶然不确定性）。
*   **收敛速度快**：Critic 学习的是标量（误差值），比学习高维状态的世界模型收敛快得多（实验显示在 400 步左右即可准确估计噪声水平）。
*   **性能领先**：在最终世界模型的准确性和收敛速度上，该方法显著优于 V1、V2 和计数基准。V1 在噪声面前完全失效，而 V2 和计数方法虽然能收敛，但速度远慢于本项目方法。

### 7. 优点
*   **理论优雅**：将复杂的累积目标简化为简洁的单步差值，且证明了前人方法是该框架的特例。
*   **自校正机制**：Critic 与策略共同进化，如果 Critic 低估了噪声，智能体会反复访问该区域，进而迫使 Critic 修正估计，最终引导智能体离开。
*   **通用性**：该框架不依赖特定的误差度量（L2、KL 散度等均适用）。

### 8. 不足与局限
*   **环境复杂度有限**：目前的验证仅限于离散动作和 factored 状态空间的网格世界，缺乏在连续动作空间和原始像素输入下的表现数据。
*   **超参数敏感性**：虽然论文未详述，但 Critic 的学习率与世界模型更新频率的比例可能对稳定性有影响。
*   **偏差风险**：如果 Critic 在训练初期对某些区域产生严重的过高估计，可能会导致智能体过早放弃探索这些区域。

（完）
