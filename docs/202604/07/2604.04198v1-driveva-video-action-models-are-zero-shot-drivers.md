---
title: "DriveVA: Video Action Models are Zero-Shot Drivers"
title_zh: DriveVA：视频动作模型是零样本驾驶员
authors: "Mengmeng Liu, Diankun Zhang, Jiuming Liu, Jianfeng Cui, Hongwei Xie, Guang Chen, Hangjun Ye, Michael Ying Yang, Francesco Nex, Hao Cheng"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04198v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于未来视觉预测和动作序列的自动驾驶世界模型
tldr: DriveVA是一个新型自动驾驶世界模型，旨在解决现有模型在未知场景和传感器配置下泛化能力不足的问题。它利用预训练的大规模视频生成模型（DiT架构）的运动动力学先验，通过共享潜空间联合解码未来视频预测和动作序列。该方法实现了规划与场景演化的高度一致性，并在NAVSIM、nuScenes和Bench2drive等基准测试中展现了卓越的闭环性能和零样本跨域泛化能力。
selection_source: fresh_fetch
motivation: 现有的自动驾驶世界模型在跨数据集泛化以及视频预测与轨迹规划的一致性方面存在局限。
method: 提出一种基于DiT解码器的DriveVA模型，通过共享潜生成过程联合预测未来动作和视频，并结合视频延续策略提升长时一致性。
result: "在NAVSIM中取得90.9的PDM高分，并在nuScenes和Bench2drive的零样本测试中分别降低了约79%和53%的L2误差。"
conclusion: DriveVA证明了大规模视频生成模型具备强大的运动先验，能有效提升自动驾驶系统在复杂环境下的零样本驾驶和跨域泛化能力。
---

## 摘要
泛化是自动驾驶中的核心挑战，因为现实世界的部署要求在未见场景、传感器域和环境条件下具有鲁棒的性能。最近基于世界模型的规划方法在场景理解和多模态未来预测方面表现出强大的能力，但它们在跨数据集和传感器配置方面的泛化能力仍然有限。此外，它们松耦合的规划范式通常会导致视觉想象过程中视频与轨迹的一致性较差。为了克服这些局限性，我们提出了 DriveVA，这是一种新型自动驾驶世界模型，它在共享的潜空间生成过程中联合解码未来视觉预测和动作序列。DriveVA 从预训练良好的大规模视频生成模型中继承了关于运动动力学和物理合理性的丰富先验，以捕捉连续的时空演变和因果交互模式。为此，DriveVA 采用基于 DiT 的解码器来联合预测未来的动作序列（轨迹）和视频，从而实现规划与场景演变之间更紧密的对齐。我们还引入了一种视频延续策略，以增强长时外推的一致性。DriveVA 在 NAVSIM 挑战赛中实现了 90.9 PDM 分数的卓越闭环性能。广泛的实验还证明了 DriveVA 的零样本能力和跨域泛化能力。与最先进的基于世界模型的规划器相比，它在 nuScenes 上的平均 L2 误差和碰撞率分别降低了 78.9% 和 83.3%，在基于 CARLA v2 构建的 Bench2drive 上分别降低了 52.5% 和 52.4%。

## Abstract
Generalization is a central challenge in autonomous driving, as real-world deployment requires robust performance under unseen scenarios, sensor domains, and environmental conditions. Recent world-model-based planning methods have shown strong capabilities in scene understanding and multi-modal future prediction, yet their generalization across datasets and sensor configurations remains limited. In addition, their loosely coupled planning paradigm often leads to poor video-trajectory consistency during visual imagination. To overcome these limitations, we propose DriveVA, a novel autonomous driving world model that jointly decodes future visual forecasts and action sequences in a shared latent generative process. DriveVA inherits rich priors on motion dynamics and physical plausibility from well-pretrained large-scale video generation models to capture continuous spatiotemporal evolution and causal interaction patterns. To this end, DriveVA employs a DiT-based decoder to jointly predict future action sequences (trajectories) and videos, enabling tighter alignment between planning and scene evolution. We also introduce a video continuation strategy to strengthen long-duration rollout consistency. DriveVA achieves an impressive closed-loop performance of 90.9 PDM score on the challenge NAVSIM. Extensive experiments also demonstrate the zero-shot capability and cross-domain generalization of DriveVA, which reduces average L2 error and collision rate by 78.9% and 83.3% on nuScenes and 52.5% and 52.4% on the Bench2drive built on CARLA v2 compared with the state-of-the-art world-model-based planner.

---

## 论文详细总结（自动生成）

### 论文总结：DriveVA - 视频动作模型是零样本驾驶员

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：自动驾驶系统在现实部署中面临严峻的泛化挑战，需在未见场景、不同传感器配置和环境条件下保持鲁棒。
*   **核心问题**：
    1.  **泛化受限**：现有基于视觉语言动作（VLA）的模型多依赖静态图文对预训练，缺乏对时空演变和物理因果律的深度理解，导致跨数据集泛化能力差。
    2.  **一致性缺失**：现有的世界模型通常将视频预测与轨迹规划视为独立或松耦合的任务，导致“视觉想象”的内容与“实际规划”的动作之间存在偏差（即视频-轨迹不一致）。
*   **整体含义**：论文旨在探索大规模预训练视频生成模型是否能作为自动驾驶的通用基础，通过联合建模视频与动作，提升系统的零样本驾驶能力。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将未来视频预测和动作序列（轨迹）集成到一个**统一的共享潜生成过程**中。利用预训练视频模型（如 Wan2.2）捕获的物理世界先验，通过单一的 DiT（Diffusion Transformer）解码器同时输出视频潜变量和动作 Token。
*   **关键技术细节**：
    *   **统一生成框架**：输入包含历史帧潜变量、当前状态（速度）和可选的文本指令。模型通过 DiT 联合预测未来 $N$ 帧视频的潜变量和 $K$ 步动作。
    *   **流匹配（Flow Matching）**：采用条件流匹配技术，将生成过程建模为从噪声分布到目标数据分布的连续时间变换，相比传统扩散模型效率更高。
    *   **视频延续策略（Video Continuation）**：利用 3D 因果 VAE 编码历史观测，通过滑动窗口递归生成未来视频片段，确保长时预测的结构一致性。
    *   **双向交互**：在 DiT 内部，视频 Token 和动作 Token 通过自注意力机制进行双向交互，使动作生成显式受限于想象的未来场景演化。

#### 3. 实验设计
*   **数据集与场景**：
    *   **NAVSIM v1**：主要闭环评估基准，考察安全性、舒适性和进度。
    *   **nuScenes**：用于**零样本（Zero-shot）**跨数据集评估（在 NAVSIM 训练，直接在 nuScenes 测试）。
    *   **Bench2Drive (CARLA v2)**：用于**跨域（Real-to-Sim）**泛化评估。
*   **Benchmark 指标**：PDMS（综合得分）、L2 误差、碰撞率（CR）、舒适度等。
*   **对比方法**：
    *   传统端到端方法：UniAD, VADv2, DiffusionDrive 等。
    *   世界模型方法：PWM, DriveVLA-W0, Epona, LAW 等。

#### 4. 资源与算力
*   **硬件**：使用 **NVIDIA H20 GPU** 进行训练。
*   **训练配置**：
    *   第一阶段：Batch size 为 80，训练 20k 步。
    *   第二阶段：通过梯度累积实现有效 Batch size 640，微调 10k 步。
    *   学习率：$10^{-4}$，采用 AdamW 优化器。
*   **推理效率**：虽然模型巨大，但仅需 **2 步采样**即可达到近乎最优的闭环性能。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三大主流基准测试，包括闭环、开环及跨域场景。
    *   **消融实验**：针对视频损失、混合训练、视频延续策略、采样步数、预测时界、训练策略（从头训练 vs LoRA vs 全量微调）以及模型规模（5B vs 14B）进行了详尽测试。
    *   **外部验证**：创新性地使用 **DPVO（深度补丁视觉里程计）** 对生成的视频进行轨迹重建，定量验证了视频与轨迹的一致性。
*   **评价**：实验设计非常充分且客观。通过严苛的零样本设置（不进行目标域微调）证明了模型的真实泛化力，而非简单的过拟合。

#### 6. 主要结论与发现
*   **视频监督是关键**：视频级监督是规划性能提升的主要驱动力，而非辅助损失。移除视频损失会导致 PDMS 得分从 90.9 骤降至 71.4。
*   **联合解码的优越性**：统一生成框架显著减少了视频与动作之间的不匹配，使得规划动作更符合物理常识。
*   **零样本潜力**：DriveVA 在未见过的 nuScenes 数据集上，L2 误差和碰撞率比 SOTA 模型分别降低了 78.9% 和 83.3%，证明了大规模视频先验的强大迁移能力。
*   **仿真增强**：加入 CARLA 仿真数据混合训练能进一步提升模型在现实世界中的安全性指标（如 TTC）。

#### 7. 优点（亮点）
*   **强一致性**：通过共享潜空间解决了世界模型中长期存在的“想做不一”问题。
*   **卓越的泛化性**：展示了从现实到仿真、从一个城市到另一个城市的极强适应力。
*   **高效采样**：在流匹配框架下，仅需 2 步推理即可实现高质量规划，平衡了生成式模型的性能与速度。
*   **验证严谨**：引入视觉里程计（DPVO）作为第三方评估手段，增强了结论的可信度。

#### 8. 不足与局限
*   **计算开销**：尽管采样步数少，但基础模型（5B/14B）参数量巨大，对于车载嵌入式平台的实时部署仍有较高门槛。
*   **传感器局限**：目前主要基于前视摄像头，尚未完全扩展到全景环视或多模态传感器融合（如 LiDAR 原始点云）。
*   **长尾场景覆盖**：虽然泛化力强，但在极端罕见的因果逻辑（如复杂的交警手势）上的表现仍需进一步验证。
*   **单模态输入**：主要依赖视觉，对恶劣天气或极端光照下的鲁棒性可能受限于预训练视频数据集的分布。

（完）
