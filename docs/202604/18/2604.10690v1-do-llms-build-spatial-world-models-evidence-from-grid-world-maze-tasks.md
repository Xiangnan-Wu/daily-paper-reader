---
title: Do LLMs Build Spatial World Models? Evidence from Grid-World Maze Tasks
title_zh: 大语言模型是否构建了空间世界模型？来自网格世界迷宫任务的证据
authors: "Weijiang Li, Yilin Zhu, Rajarshi Das, Parijat Dube"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10690v1"
tags: ["query:robot"]
score: 9.0
evidence: 评估大语言模型中用于规划的空间世界模型
tldr: 本研究通过迷宫任务系统评估了主流大语言模型（如Gemini、GPT等）构建内部空间世界模型的能力。研究发现，模型在处理邻接表征时表现较好，但在视觉网格格式下性能大幅下降，且在连续空间计算中缺乏一致性。这表明LLM并未形成稳健的空间抽象，其推理高度依赖于特定的表征方式和提示词，对基础模型在空间规划领域的应用具有重要警示意义。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型是否真正构建了用于推理和规划的内部空间世界模型，而非仅依赖于表征模式。
method: 通过不同规模的迷宫任务，对比分析了多种LLM在邻接表征与视觉网格格式下的空间推理及路径规划表现。
result: 模型性能表现出极强的表征依赖性，且在处理连续空间问题时无法维持逻辑一致性，导致复杂任务下的表现崩溃。
conclusion: LLM并未发展出稳健的空间世界模型，其空间推理能力仅在特定表征和提示条件下有效，难以实现真正的空间抽象。
---

## 摘要
基础模型在多样化任务中表现出卓越性能，但其构建用于推理和规划的内部空间世界模型的能力仍不明确。我们通过迷宫任务系统地评估了大语言模型的空间理解能力，这是一种需要多步规划和空间抽象的受控测试环境。通过对 Gemini-2.5-Flash、GPT-5-mini、Claude-Haiku-4.5 和 DeepSeek-Chat 进行的全面实验，我们发现了空间推理中的显著差异，这挑战了关于大语言模型规划能力的假设。使用思维链提示，Gemini 在采用标记化邻接表示的小型迷宫（5x5 到 7x7 网格）上达到了 80-86% 的准确率，但在视觉网格格式下性能骤降至 16-34%，差异达 2-5 倍，这表明其空间推理依赖于表示方式，而非格式无关。我们通过序列邻近性问题和组合距离比较进一步探测了空间理解。尽管在推理轨迹中实现了 96-99% 的语义覆盖，模型仍无法利用这种理解进行一致的空间计算，这表明它们将每个问题视为独立的，而非构建累积的空间知识。我们基于迷宫求解任务的研究结果表明，大语言模型并未发展出鲁棒的空间世界模型，而是表现出特定于表示且依赖于提示的推理，仅在狭窄的条件下才能成功。这些结果对于在需要空间抽象的应用中部署基础模型具有重要意义。

## Abstract
Foundation models have shown remarkable performance across diverse tasks, yet their ability to construct internal spatial world models for reasoning and planning remains unclear. We systematically evaluate the spatial understanding of large language models through maze tasks, a controlled testing context requiring multi-step planning and spatial abstraction. Across comprehensive experiments with Gemini-2.5-Flash, GPT-5-mini, Claude-Haiku-4.5, and DeepSeek-Chat, we uncover significant discrepancies in spatial reasoning that challenge assumptions about LLM planning capabilities. Using chain-of-thought prompting, Gemini achieves 80-86% accuracy on smaller mazes (5x5 to 7x7 grids) with tokenized adjacency representations, but performance collapses to 16-34% with visual grid formats, which is a 2-5x difference, suggesting representation-dependent rather than format-invariant spatial reasoning. We further probe spatial understanding through sequential proximity questions and compositional distance comparisons. Despite achieving 96-99% semantic coverage in reasoning traces, models fail to leverage this understanding for consistent spatial computations, indicating that they treat each question independently rather than building cumulative spatial knowledge. Our findings based on the maze-solving tasks suggest that LLMs do not develop robust spatial world models, but rather exhibit representation-specific and prompting-dependent reasoning that succeeds only under narrow conditions. These results have critical implications for deploying foundation models in applications requiring spatial abstraction.

---

## 论文详细总结（自动生成）

这是一份关于论文《Do LLMs Build Spatial World Models? Evidence from Grid-World Maze Tasks》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是：**大语言模型（LLMs）是否真正构建了内部的“空间世界模型”（Spatial World Models）**，即一种能够进行系统推理和规划的抽象、因果表征，还是仅仅依赖于表征特定的启发式算法或表面模式匹配？
*   **背景**：随着 LLM 被广泛应用于机器人导航和自主系统，理解其空间智能的本质至关重要。
*   **动机**：现有的 NLP 基准测试可能通过记忆训练数据来完成，而迷宫任务需要结构化的拓扑理解、多步规划和组合泛化，是探测“世界模型”是否涌现的理想测试床。

### 2. 论文提出的方法论
研究者设计了一个系统性的评估框架，通过操纵输入格式、提示策略和任务复杂度来探测模型的空间能力。
*   **核心思想**：如果模型拥有真正的世界模型，其表现应当具有“格式不变性”（Format-invariance）和“知识一致性”（Consistency）。
*   **关键技术细节**：
    *   **任务分类**：
        1.  **迷宫导航（规划）**：寻找起点到终点的最短路径。
        2.  **序列推理（重用）**：连续询问四个关于同一迷宫的距离比较问题，其中 Q3 与 Q0 完全相同，测试模型是否重用之前的计算。
        3.  **组合距离比较**：通过比较角落到中心的距离，测试模型是否能将复杂问题分解并组合中间结果。
    *   **输入表征**：对比**邻接表（Adjacency List）**（纯逻辑连接）与**视觉网格（Visual Grid）**（2D 字符阵列）。
    *   **提示策略**：对比标准提示（Standard）、思维链（CoT）和事后推理（Post-hoc Reasoning）。

### 3. 实验设计
*   **数据集/场景**：使用 `maze-dataset` 库生成 5×5 到 9×9 不同尺寸的网格迷宫，每种尺寸包含 50 个测试用例。
*   **Benchmark**：自建的 **SpatialBench** 评估平台。
*   **对比模型**：Gemini-2.5-Flash, GPT-5-mini, Claude-Haiku-4.5, DeepSeek-Chat。
*   **评估指标**：路径准确率（Exact Path Accuracy）、编辑距离、路径有效性、语义覆盖率（Semantic Coverage）、ROUGE-L 以及 LLM-as-a-judge（使用 Llama-3.1-8b 作为裁判）。

### 4. 资源与算力
*   论文**未明确说明**具体的算力资源（如 GPU 型号、数量或训练时长）。
*   由于实验主要针对商业模型（Gemini, GPT, Claude, DeepSeek）的 API 进行推理评估，而非从头训练，因此算力消耗主要集中在 API 调用和推理生成上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流模型、5 种迷宫尺寸、2 种输入格式、3 种提示策略以及 0/3/5-shot 的上下文学习设置。
*   **充分性**：实验设计非常系统且控制变量严格。通过对比不同格式下的性能差异，以及分析重复问题下的推理轨迹，有效地隔离了“模式匹配”与“抽象推理”变量。
*   **客观性**：使用了自动化指标与 LLM 裁判相结合的方法，并提供了失败模式的定性分析（如模型倾向于忽略墙壁走直线距离），实验结果具有较高的说服力。

### 6. 论文的主要结论与发现
1.  **表征依赖性极强**：模型在邻接表格式下表现远好于视觉网格。例如 Gemini 在 5x5 邻接表上准确率 86%，但在视觉格式下仅为 34%。这表明模型依赖特定表征的启发式搜索，而非抽象的空间理解。
2.  **推理策略至关重要**：没有 CoT 引导时，模型几乎无法完成任务（如 Claude 在基础提示下几乎完全失败，但在 CoT 下恢复到 78%）。
3.  **缺乏知识重用**：在序列查询中，模型倾向于将重复的问题视为独立事件重新计算，而不是从已建立的内部模型中提取知识。
4.  **性能随规模崩溃**：随着迷宫尺寸增加，所有模型的性能均显著下降，显示出规划能力的脆弱性。

### 7. 优点
*   **多维度探测**：不仅关注任务成功率，还通过“重复问题”和“组合问题”深入分析推理过程的一致性。
*   **评估工具创新**：引入了语义覆盖和 LLM-as-a-judge 来量化推理质量，而不仅仅是看最终答案。
*   **开源贡献**：发布了 SpatialBench 平台，方便社区复现和扩展实验。

### 8. 不足与局限
*   **环境局限性**：实验仅限于 2D 网格迷宫，未涉及更复杂的 3D 空间、非欧几里得几何或连续空间导航。
*   **黑盒限制**：由于测试的是闭源模型，无法通过探测（Probing）或干预（Intervention）等机械解释性手段直接观察其内部神经元激活。
*   **上下文长度干扰**：在 k-shot 实验中，性能下降可能部分归因于上下文长度增加带来的干扰，而非空间推理能力的减弱。

（完）
