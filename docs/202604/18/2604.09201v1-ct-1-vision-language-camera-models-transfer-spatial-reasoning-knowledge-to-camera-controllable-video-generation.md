---
title: "CT-1: Vision-Language-Camera Models Transfer Spatial Reasoning Knowledge to Camera-Controllable Video Generation"
title_zh: CT-1：视觉-语言-相机模型将空间推理知识迁移至相机可控的视频生成
authors: "Haoyu Zhao, Zihao Zhang, Jiaxi Gu, Haoran Chen, Qingping Zheng, Pin Tang, Yeyin Jin, Yuang Zhang, Junqi Cheng, Zenghui Lu, Peng Shu, Zuxuan Wu, Yu-Gang Jiang"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09201v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 用于视频生成的视觉-语言-相机模型
tldr: 针对现有视频生成中相机控制不精确或依赖手动参数的问题，本文提出CT-1模型。该模型结合视觉语言模块与扩散Transformer，通过估计相机轨迹将空间推理知识迁移至视频生成。引入小波正则化损失优化轨迹学习，并构建了大规模数据集CT-200K。实验表明，CT-1显著提升了相机控制的准确性与视频质量，实现了更符合用户意图的自动化视频合成。
selection_source: fresh_fetch
motivation: 现有方法在相机控制上存在文本指令不精确或依赖繁琐手动参数的问题，限制了自动化视频生成的应用。
method: 提出CT-1模型，结合视觉语言模块与扩散Transformer，并利用小波正则化损失在频域内学习复杂的相机轨迹分布。
result: "通过在自建的CT-200K大规模数据集上训练，CT-1在相机控制准确率上比现有方法提升了25.7%。"
conclusion: CT-1成功桥接了空间推理与视频合成，为实现高精度、自动化的相机可控视频生成提供了新路径。
---

## 摘要
相机可控的视频生成旨在合成具有灵活且符合物理规律的相机运动的视频。然而，现有方法要么通过文本提示提供不精确的相机控制，要么依赖于耗费人力的手动相机轨迹参数，限制了它们在自动化场景中的应用。为解决这些问题，我们提出了一种新型的视觉-语言-相机模型，称为 CT-1（Camera Transformer 1），这是一种专门设计的模型，旨在通过准确估计相机轨迹，将空间推理知识迁移到视频生成中。CT-1 基于视觉-语言模块和扩散 Transformer 模型构建，在频域中采用基于小波的正则化损失，以有效学习复杂的相机轨迹分布。这些轨迹被集成到视频扩散模型中，以实现符合用户意图的空间感知相机控制。为了促进 CT-1 的训练，我们设计了一个专门的数据整理流水线，并构建了 CT-200K，这是一个包含超过 4700 万帧的大规模数据集。实验结果表明，我们的框架成功弥合了空间推理与视频合成之间的差距，生成了真实且高质量的相机可控视频，并将相机控制精度比现有方法提高了 25.7%。

## Abstract
Camera-controllable video generation aims to synthesize videos with flexible and physically plausible camera movements. However, existing methods either provide imprecise camera control from text prompts or rely on labor-intensive manual camera trajectory parameters, limiting their use in automated scenarios. To address these issues, we propose a novel Vision-Language-Camera model, termed CT-1 (Camera Transformer 1), a specialized model designed to transfer spatial reasoning knowledge to video generation by accurately estimating camera trajectories. Built upon vision-language modules and a Diffusion Transformer model, CT-1 employs a Wavelet-based Regularization Loss in the frequency domain to effectively learn complex camera trajectory distributions. These trajectories are integrated into a video diffusion model to enable spatially aware camera control that aligns with user intentions. To facilitate the training of CT-1, we design a dedicated data curation pipeline and construct CT-200K, a large-scale dataset containing over 47M frames. Experimental results demonstrate that our framework successfully bridges the gap between spatial reasoning and video synthesis, yielding faithful and high-quality camera-controllable videos and improving camera control accuracy by 25.7% over prior methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《CT-1: Vision-Language-Camera Models Transfer Spatial Reasoning Knowledge to Camera-Controllable Video Generation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的相机可控视频生成方法存在两极分化：要么依赖模糊的文本提示（控制不精确），要么依赖繁琐的手动相机轨迹参数（自动化程度低、人工成本高）。
*   **研究动机**：
    *   **空间推理缺失**：通用视频生成模型难以理解文本指令背后的复杂空间逻辑。
    *   **语义与像素的断层**：需要一个中间层来将高层用户意图（如“镜头缓慢拉近”）转化为低层物理轨迹。
    *   **数据匮乏**：缺乏大规模、高质量且带有精确相机运动描述的视频数据集。

### 2. 方法论
论文提出了 **CT-1 (Camera Transformer 1)**，这是一种视觉-语言-相机（VLC）模型，核心思想是**将相机轨迹预测视为一种条件分布建模任务**。

*   **核心架构**：
    *   **视觉-语言模块**：采用双分支视觉编码器（DINOv2 和 SigLIP）捕捉局部细节与全局语义，结合 LLaMA-2 语言模型。通过一个特殊的 `<CAM>`（相机上下文）Token 来聚合多模态信息。
    *   **扩散 Transformer (DiT)**：不直接回归单一轨迹，而是使用 DiT 学习相机轨迹的分布。轨迹被参数化为 $SE(3)$ 流形上的变换序列。
*   **关键技术细节**：
    *   **小波正则化损失 (WavReg Loss)**：在频域内对轨迹进行 Haar 离散小波变换。
        *   **原理**：将轨迹分解为低频（全局趋势）和高频（局部扰动）分量。
        *   **公式逻辑**：$L_{wav} = \lambda_a \|a_L - \hat{a}_L\|_1 + \sum \lambda_{dl} \|d_l - \hat{d}_l\|_1$，通过给低频分量分配更高权重，确保镜头运动的平滑性和物理稳定性。
*   **算法流程**：输入参考图和文本指令 $\rightarrow$ VLC 模块提取特征 $\rightarrow$ DiT 预测相机轨迹 $\rightarrow$ 解耦的视频生成模型（如 CameraNoise/Wan2.1）根据轨迹合成视频。

### 3. 实验设计
*   **数据集**：
    *   **CT-200K（自建）**：包含 20 万个视频剪辑，超过 4700 万帧。涵盖通用场景和需要空间推理的场景（如自我中心视角下的物体交互）。
    *   **评估集**：CameraBench100（涵盖 6 种典型相机运动）。
*   **对比方法**：
    *   **基于轨迹输入的模型**：MotionCtrl, CameraCtrl（配合 VLM 或自回归预测器）。
    *   **基于提示词输入的模型**：LTX-Video, CogVideoX, Wan2.1, Wan2.2。
*   **Benchmark**：主要使用 **Success Rate (成功率)**（由人类专家评估语义一致性）和 **VBench**（评估视频质量、美学、运动平滑度）。

### 4. 资源与算力
*   **训练算力**：使用了 **32 张 NVIDIA GPU**（具体型号未标明，推测为 A100 或 H100 级别）进行端到端训练。
*   **推理效率**：在单张 NVIDIA 设备上，CT-1 预测轨迹仅需约 22.8 秒，仅占整个视频生成管线耗时的 **5.3%**，内存占用约 28.9GB。
*   **模型规模**：提供了 Base (33M)、Large (130M) 和 Huge (458M) 三种参数规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在 CameraBench100 上进行了详尽的零样本（Zero-shot）对比。
    *   **消融实验**：涵盖了模型缩放（Scaling）、WavReg 损失权重 $\beta$、数据效率（通用 vs 推理数据）、以及不同轨迹建模方式（回归 vs 自回归 vs 扩散）的对比。
    *   **定性分析**：展示了在 OOD（分布外）场景、复杂推理场景、甚至自动驾驶场景（DrivingDoJo）下的泛化能力。
*   **充分性评价**：实验设计非常全面，不仅验证了模型本身的性能，还通过交叉验证（同一图多指令、同指令多图）证明了模型的鲁棒性。引入人类专家评估（User Study）弥补了自动指标在相机控制评价上的不足。

### 6. 主要结论与发现
*   **性能提升**：CT-1 在相机控制成功率上比现有最强方法（Wan2.2）提升了 **25.7%**。
*   **空间推理迁移**：证明了通过 VLC 模型可以有效地将空间推理知识从静态图像和文本迁移到动态视频生成中。
*   **频域优越性**：小波正则化（WavReg）比传统的时域平滑（如速度/加速度惩罚）更能保持运动趋势并减少抖动。
*   **Scaling Law**：相机轨迹预测同样遵循 Scaling Law，模型参数越大，复杂运动的建模能力越强。

### 7. 优点（亮点）
*   **模块化解耦**：将轨迹预测与视频生成解耦，使 CT-1 可以作为“插件”适配各种现有的可控视频生成模型（如 CameraCtrl, MotionCtrl）。
*   **高质量数据集**：CT-200K 的构建填补了领域空白，特别是其针对“空间推理”场景的设计。
*   **频域视角**：引入小波变换处理轨迹平滑度问题，是一个非常巧妙且符合物理直觉的设计。

### 8. 不足与局限
*   **评估主观性**：由于相机轨迹的“非唯一性”，实验高度依赖人工评估（Success Rate），缺乏一种公认的、完全自动化的精确度量指标。
*   **分辨率不匹配**：论文提到 CameraBench 的低分辨率输入与生成模型的高分辨率需求存在冲突，虽然通过重绘（Redraw）参考图缓解了问题，但这可能引入额外的偏差。
*   **多步推理限制**：虽然在简单推理场景表现良好，但在极长序列或极复杂的多目标交互推理中，轨迹的物理合理性仍有待进一步验证。

（完）
