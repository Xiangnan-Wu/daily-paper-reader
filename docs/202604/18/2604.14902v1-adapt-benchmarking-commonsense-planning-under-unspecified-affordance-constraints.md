---
title: "ADAPT: Benchmarking Commonsense Planning under Unspecified Affordance Constraints"
title_zh: ADAPT：未指定示能性约束下的常识规划基准测试
authors: "Pei-An Chen, Yong-Ching Liang, Jia-Fong Yeh, Hung-Ting Su, Yi-Ting Chen, Min Sun, Winston Hsu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14902v1"
tags: ["query:robot"]
score: 9.0
evidence: 具身智能体的常识规划与动作赋能基准
tldr: 本研究针对具身智能体在动态环境中忽视物体可操作性（Affordance）约束的问题，提出了DynAfford基准测试和ADAPT插件模块。DynAfford要求智能体在指令未明确约束的情况下感知环境并推断隐含前提；ADAPT则通过显式推理增强现有规划器的适应性。实验证明，该方法显著提升了智能体在复杂环境下的任务成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的具身智能体通常盲目执行指令，缺乏对动态环境中物体是否可操作（即Affordance约束）的感知与推理能力。
method: 引入了DynAfford基准测试用于评估动态约束下的规划，并开发了ADAPT模块，通过显式的可操作性推理增强现有规划器。
result: 实验显示ADAPT显著提高了任务成功率，且经过领域微调的视觉语言模型在可操作性推理上优于GPT-4o。
conclusion: 显式的可操作性推理是具身智能体在现实动态环境中实现稳健规划和任务执行的关键。
---

## 摘要
智能具身智能体不应仅仅简单地遵循指令，因为现实世界环境通常包含意外情况和异常。然而，现有方法通常侧重于直接执行指令，而未考虑目标对象是否真正可被操作，这意味着它们未能评估可用的示能性（affordances）。为解决这一局限性，我们引入了 DynAfford，这是一个在动态环境中评估具身智能体的基准，其中物体的示能性可能会随时间变化，且未在指令中明确说明。DynAfford 要求智能体感知物体状态，推断隐含的前置条件，并相应地调整其动作。为了实现这一能力，我们引入了 ADAPT，这是一个即插即用的模块，通过显式的示能性推理增强了现有的规划器。实验表明，集成 ADAPT 显著提高了在已知和未知环境中的鲁棒性和任务成功率。我们还展示了，作为示能性推理后端的领域自适应、经 LoRA 微调的视觉语言模型优于商业大语言模型（GPT-4o），这突显了任务对齐的示能性落地（grounding）的重要性。

## Abstract
Intelligent embodied agents should not simply follow instructions, as real-world environments often involve unexpected conditions and exceptions. However, existing methods usually focus on directly executing instructions, without considering whether the target objects can actually be manipulated, meaning they fail to assess available affordances. To address this limitation, we introduce DynAfford, a benchmark that evaluates embodied agents in dynamic environments where object affordances may change over time and are not specified in the instruction. DynAfford requires agents to perceive object states, infer implicit preconditions, and adapt their actions accordingly. To enable this capability, we introduce ADAPT, a plug-and-play module that augments existing planners with explicit affordance reasoning. Experiments demonstrate that incorporating ADAPT significantly improves robustness and task success across both seen and unseen environments. We also show that a domain-adapted, LoRA-finetuned vision-language model used as the affordance inference backend outperforms a commercial LLM (GPT-4o), highlighting the importance of task-aligned affordance grounding.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **ADAPT**，一个旨在提升具身智能体（Embodied Agents）在动态、未指定约束环境下常识规划能力的基准测试与方法框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的具身指令遵循（EIF）基准测试通常假设环境是静态的，且物体始终处于可操作状态（Affordance）。然而，现实世界充满意外（如微波炉被占用、抹布是脏的），人类能自然地推断出隐含的前置条件（如先洗抹布再放进抽屉），但目前的智能体往往只会盲目执行指令，导致在动态约束下频繁失败。
*   **核心问题**：如何让智能体在指令未明确说明的情况下，感知物体的动态示能性（Affordance）状态，并据此灵活调整规划？

### 2. 方法论
论文提出了两个核心贡献：**DynAfford 基准测试**和 **ADAPT 模块**。

*   **DynAfford 基准测试**：
    *   基于 AI2-THOR 模拟器构建，包含 2,628 个专家演示和 10,106 条自然语言标注。
    *   **动态示能性注入**：在任务初始化时随机设置物体状态为“不可用”（如：电器被占用、容器已使用、物体脏污），且这些约束不出现在指令中。
    *   **常识驱动的目标**：成功标准不仅是完成指令动作，还包括满足隐含的常识条件（如放入抽屉的必须是干净的布）。
*   **ADAPT 模块（即插即用）**：
    *   **阶段 I：示能性状态推理**：利用经过 **LoRA 微调的 LLaVA-1.5-7B** 模型。引入了**多模态上下文学习（MICL）**，输入包含三张图（可用参考图、不可用参考图、当前观察图），通过视觉对比判断物体当前是否可用。
    *   **阶段 II：适用性解决（Resolution）**：当检测到动作不可行时，调用大语言模型（LLM）进行常识推理，决定是“等待”（针对临时占用）还是执行“准备动作”（如清洗）。

### 3. 实验设计
*   **数据集/场景**：使用 AI2-THOR 2.0 模拟器，涵盖 57 个场景（厨房和浴室），分为训练集、验证集、测试可见（Seen）和测试不可见（Unseen）分片。
*   **对比方法**：
    *   **监督学习方法**：MOCA, FILM, CAPEAM。
    *   **少样本/大模型方法**：SayCan, LLM-Planner。
    *   **后端对比**：将 ADAPT 的后端从微调的 LLaVA 替换为商业模型 GPT-4o 进行对比。
*   **评估指标**：成功率（SR）、目标条件成功率（GC）以及路径长度加权后的 SR/GC（PLW SR/GC）。

### 4. 资源与算力
*   **硬件使用**：实验在 **NVIDIA RTX 3090 (24GB VRAM)** 上运行。
*   **训练细节**：使用 LoRA 对 LLaVA-1.5-7B 进行微调，训练 1 个 epoch，LoRA rank 为 64，学习率为 1e-5。
*   **推理效率**：ADAPT 仅在涉及动态物体时触发，平均每次调用推理耗时约 **0.2 秒**。

### 5. 实验数量与充分性
*   **实验规模**：在 2,076 个测试任务上进行了评估，涵盖了 6 种任务类型（拾取、放置、清洗、加热、冷却、堆叠）。
*   **消融实验**：针对 LoRA 微调和多模态上下文学习（MICL）分别做了消融，证明了两者对提升推理准确性的必要性。
*   **客观性**：通过对比“静态”和“动态”两种环境设置，清晰地隔离了示能性推理对性能的影响。同时引入了基于真值的启发式方法作为性能上限参考，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能提升显著**：集成 ADAPT 后，FILM 模型在测试不可见分片上的成功率提升了 **73.2%**（相对值），CAPEAM 的 PLW 指标也显著改善。
*   **领域自适应的重要性**：经过 LoRA 微调的 7B 模型在示能性判断上的准确率（平均 75.39%）**优于 GPT-4o**，说明通用的视觉理解在特定具身任务的细微状态感知上仍有不足。
*   **鲁棒性增强**：ADAPT 允许智能体在遇到障碍时保持长期目标的一致性，而不是盲目重试或彻底崩溃。

### 7. 优点与亮点
*   **即插即用**：ADAPT 不需要修改原有规划器的架构或重新训练底层控制器，具有极强的通用性。
*   **诊断性强**：DynAfford 填补了现有基准测试在“隐含前提推理”方面的空白，能有效诊断智能体在动态环境下的脆弱性。
*   **视觉对比机制**：通过 MICL 引入参考图，有效缓解了视觉模型在判断细微状态变化（如物体是否脏污）时的难度。

### 8. 不足与局限
*   **视角依赖**：由于使用单视角自我中心观察，ADAPT 容易受到遮挡和视角歧义的影响（如论文中提到的将洗碗机误认为微波炉的失败案例）。
*   **示能性类别有限**：目前仅关注占用、已使用、脏污三种类别，尚未涵盖更复杂的几何或结构性示能性约束。
*   **Sim-to-Real 差距**：实验完全在模拟器中进行，未考虑现实世界中的物理不确定性、安全约束或人类干预。

（完）
