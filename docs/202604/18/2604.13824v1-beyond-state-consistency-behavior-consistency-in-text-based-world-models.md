---
title: "Beyond State Consistency: Behavior Consistency in Text-Based World Models"
title_zh: 超越状态一致性：基于文本的世界模型中的行为一致性
authors: "Youling Huang, Guanqiao Chen, Junchi Yao, Lu Wang, Fangkai Yang, Chao Du, ChenZhuo Zhao, Pu Zhao, Qingwei Lin, Saravan Rajmohan, Dongmei Zhang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13824v1"
tags: ["query:robot"]
score: 9.0
evidence: 交互式智能体文本世界模型中的行为一致性
tldr: 针对文本世界模型中传统单步状态匹配指标无法准确反映智能体行为的问题，本文提出了一种行为对齐的训练范式。该方法引入了“行为一致性奖励（BehR）”指标，通过衡量参考智能体在预测状态与真实状态下动作概率的一致性来优化模型。实验证明，该方法在WebShop和TextWorld任务中显著提升了模型的长期对齐能力、离线评估准确性及在线规划性能。
selection_source: fresh_fetch
motivation: 传统的单步状态一致性指标不足以捕捉世界模型与真实环境之间的功能一致性，导致模型在支持智能体决策时表现不佳。
method: 提出行为一致性奖励（BehR），通过最小化参考智能体在预测状态与真实状态下动作似然度的差异来训练世界模型。
result: 实验显示BehR训练提升了长期对齐度，降低了离线评估的误报率，并在前瞻规划中取得了性能增益。
conclusion: 行为一致性是比单纯状态一致性更有效的训练目标，能显著增强文本世界模型在交互式任务中的实用性和可靠性。
---

## 摘要
世界模型已成为评估交互式智能体在在线规划和离线评估中所产生动作后果的关键组件。在基于文本的环境中，世界模型通常使用精确匹配（Exact Match）等单步指标进行评估和训练，旨在提高预测状态与真实世界状态之间的相似性，但此类指标已被证明不足以捕捉智能体的实际行为。为了解决这一问题，我们引入了一种新的行为对齐训练范式，旨在提高世界模型与真实环境之间的功能一致性。该范式专注于优化一个名为“行为一致性奖励”（Behavior Consistency Reward, BehR）的可处理步级指标，该指标衡量在冻结的参考智能体（Reference Agent）下，记录的下一步动作在真实状态与世界模型预测状态之间的似然度变化程度。在 WebShop 和 TextWorld 上的实验表明，基于 BehR 的训练在多种设置下改善了长期对齐效果，其中在 WebShop 中的提升最为明显，而在接近性能天花板的方案中变化较小，同时在四分之三的设置中保持或提高了单步预测质量。使用 BehR 训练的世界模型在离线代理评估中实现了更低的假阳性率，并在推理时前瞻规划中表现出虽小但令人鼓舞的提升。

## Abstract
World models have been emerging as critical components for assessing the consequences of actions generated by interactive agents in online planning and offline evaluation. In text-based environments, world models are typically evaluated and trained with single-step metrics such as Exact Match, aiming to improve the similarity between predicted and real-world states, but such metrics have been shown to be insufficient for capturing actual agent behavior. To address this issue, we introduce a new behavior-aligned training paradigm aimed at improving the functional consistency between the world model and the real environment. This paradigm focuses on optimizing a tractable step-level metric named Behavior Consistency Reward (BehR), which measures how much the likelihood of a logged next action changes between the real state and the world-model-predicted state under a frozen Reference Agent. Experiments on WebShop and TextWorld show that BehR-based training improves long-term alignment in several settings, with the clearest gains in WebShop and less movement in near-ceiling regimes, while preserving or improving single-step prediction quality in three of four settings. World models trained with BehR also achieve lower false positives in offline surrogate evaluation and show modest but encouraging gains in inference-time lookahead planning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond State Consistency: Behavior Consistency in Text-Based World Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**文本世界模型（Text-Based World Models）**的训练与评估问题。
*   **研究动机**：目前的世界模型主要追求“状态一致性”（State Consistency），即预测的文本状态与真实状态在字面上尽可能相似（如使用 Exact Match 或 BERTScore）。
*   **核心矛盾**：作者发现存在“指标反转”（Metric Inversion）现象——即使文本相似度极高，如果缺失了关键信息（如 WebShop 中漏掉目标商品），智能体也会决策失败；反之，即便文本差异大但保留了关键决策要素，智能体仍能成功。
*   **整体含义**：论文主张世界模型应追求**“功能一致性”**，即预测状态应诱导智能体做出与真实状态相同的决策，而非仅仅是文本相似。

### 2. 方法论：核心思想与技术细节
作者提出了一种**行为对齐的训练范式**，核心是**行为一致性奖励（BehR）**：
*   **核心思想**：利用一个冻结的**参考智能体（Reference Agent）**作为探针。如果预测状态保留了环境的功能性，那么参考智能体在预测状态下对“正确动作”的打分（似然度）应与在真实状态下一致。
*   **关键技术细节**：
    *   **BehR 公式**：$R_{beh} = \exp(-\text{coef} \cdot |\bar{\ell}_{pred} - \bar{\ell}_{real}|)$。其中 $\bar{\ell}$ 是参考智能体对记录动作的平均每 Token 对数似然度。该奖励在两者一致时达到最大值。
    *   **训练流程**：采用 **GRPO（Group Relative Policy Optimization）** 算法。世界模型针对同一历史生成多个候选状态，计算各自的 BehR，通过组内归一化奖励来更新模型参数。
    *   **行为锚定**：不要求模型模仿参考智能体，而是要求模型生成的“状态”能让参考智能体维持原有的决策偏好。

### 3. 实验设计
*   **数据集/场景**：
    *   **WebShop**：复杂的电商购物环境，涉及搜索、点击和属性选择，具有开放式状态空间。
    *   **TextWorld**：结构化的文字冒险游戏，强调逻辑约束和状态追踪。
*   **Benchmark 与对比方法**：
    *   **基线模型**：Word2World (W2W)，基于 SFT 训练的强力基线。
    *   **对比奖励函数**：F1 Reward（Token 级 F1 分数）、FactR（结构化事实准确率）。
    *   **评估智能体**：涵盖 Qwen3 (0.6B 到 32B)、GPT-4o 和 GPT-5，用于验证世界模型的泛化性。
*   **核心指标**：**Pairwise Consistency Ratio (CR pw)**，衡量在真实环境中成功的任务中，有多少在世界模型模拟下依然能成功。

### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA A100 (80GB) GPU**。
*   **训练时长**：代表性任务的训练大约需要 **2 天**。
*   **软件框架**：基于 VeRL 强化学习框架，使用 vLLM 作为推理后端，采用 FSDP 进行模型并行。

### 5. 实验数量与充分性
*   **实验规模**：作者在 2 个领域、2 种骨干网络（Qwen2.5-7B, LLaMA3.1-8B）、4 种主流评估智能体上进行了 **16 组** 核心配置对比。
*   **消融实验**：对比了不同奖励函数（F1, FactR, BehR）对性能的影响，验证了 BehR 的独特性。
*   **客观性与公平性**：实验采用了相同的训练语料和超参数设置进行受控对比；引入了跨家族智能体测试（如用 Qwen 训练的 WM 评估 GPT 智能体），增强了结论的普适性。

### 6. 主要结论与发现
*   **性能提升**：BehR 训练显著提升了任务级的 CR pw 指标，尤其在 WebShop 这种决策敏感的环境中。
*   **单步质量保持**：在提升行为一致性的同时，模型依然保持或提升了单步文本预测的准确率（EM）。
*   **校准效应**：BehR-WM 显著降低了弱智能体在离线评估中的“假阳性”率（从 42.5% 降至 9.5%），使评估结果更真实。
*   **规划增益**：在推理时前瞻规划（Lookahead Planning）中，使用 BehR 训练的模型作为模拟器能带来更好的性能增益。

### 7. 优点（亮点）
*   **视角创新**：首次系统性地提出并量化了文本世界模型中的“指标反转”问题，将训练目标从“文本重建”转向“行为保持”。
*   **指标实用**：BehR 作为一个密集、可处理的奖励信号，成功解决了黑盒智能体概率分布不可获取的难题。
*   **下游价值**：不仅提升了模型质量，还直接改善了离线评估的可靠性和在线规划的效果。

### 8. 不足与局限
*   **代理指标局限**：BehR 仅基于“单个记录动作”的似然度，而非完整的动作分布，可能无法完全捕捉复杂的决策边界。
*   **参考智能体依赖**：奖励质量受限于参考智能体的能力，虽然实验显示了跨家族泛化性，但仍存在潜在的偏置风险。
*   **天花板效应**：在 TextWorld 等逻辑相对简单的环境中，当基线已接近性能天花板时，BehR 带来的边际收益较小。
*   **计算开销**：相比 SFT，强化学习训练（尤其是涉及多次 LLM 似然度查询）需要更多的算力资源。

（完）
