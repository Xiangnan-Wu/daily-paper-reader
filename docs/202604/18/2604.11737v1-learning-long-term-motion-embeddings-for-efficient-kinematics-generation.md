---
title: Learning Long-term Motion Embeddings for Efficient Kinematics Generation
title_zh: 学习长期运动嵌入以实现高效运动学生成
authors: "Nick Stracke, Kolja Bauer, Stefan Andreas Baumann, Miguel Angel Bautista, Josh Susskind, Björn Ommer"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11737v1"
tags: ["query:q10"]
score: 9.0
evidence: 学习长期运动嵌入以实现高效的场景动力学
tldr: 针对视频生成模型在预测多种未来动态时效率低下的问题，本文提出了一种基于长期运动嵌入的高效运动生成框架。该方法通过从大规模轨迹数据中学习具有64倍时间压缩率的运动表示，并利用条件流匹配模型生成符合文本或空间指令的运动潜变量。该研究实现了长时、逼真运动的高效生成，在性能上超越了现有的视频模型和特定任务方法。
selection_source: fresh_fetch
motivation: 传统的视频合成模型在探索多种可能的未来场景动态时效率极低，难以实现快速的运动预测。
method: 通过学习高度压缩的运动嵌入并结合条件流匹配模型，在潜空间内根据文本或空间提示生成运动轨迹。
result: 该方法在生成长时且真实的运动分布方面，表现优于当前的先进视频生成模型和任务特定方法。
conclusion: 直接在压缩的运动嵌入空间建模场景动态，是实现高效、高质量长时运动预测的有效途径。
---

## 摘要
理解和预测运动是视觉智能的一个基本组成部分。尽管现代视频模型对场景动力学表现出强大的理解能力，但通过完整的视频合成来探索多种可能的未来仍然效率极低。我们通过直接在从追踪器模型获得的大规模轨迹中学习到的长期运动嵌入上进行操作，以高出几个数量级的效率对场景动力学进行建模。这使得能够高效生成符合通过文本提示或空间点击（spatial pokes）指定目标的长期、逼真的运动。为实现这一目标，我们首先学习了一个具有 64 倍时间压缩因子的极度压缩的运动嵌入。在该空间中，我们训练了一个条件流匹配（flow-matching）模型，以根据任务描述生成运动潜变量。由此产生的运动分布优于最先进的视频模型和专门的特定任务方法。

## Abstract
Understanding and predicting motion is a fundamental component of visual intelligence. Although modern video models exhibit strong comprehension of scene dynamics, exploring multiple possible futures through full video synthesis remains prohibitively inefficient. We model scene dynamics orders of magnitude more efficiently by directly operating on a long-term motion embedding that is learned from large-scale trajectories obtained from tracker models. This enables efficient generation of long, realistic motions that fulfill goals specified via text prompts or spatial pokes. To achieve this, we first learn a highly compressed motion embedding with a temporal compression factor of 64x. In this space, we train a conditional flow-matching model to generate motion latents conditioned on task descriptions. The resulting motion distributions outperform those of both state-of-the-art video models and specialized task-specific approaches.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ZipMo** 的框架，旨在通过学习长期运动嵌入（Long-term Motion Embeddings）来实现高效的运动学（Kinematics）生成。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代视频生成模型虽然能理解场景动力学，但由于将运动与外观（像素）耦合在一起，导致在探索多种可能的未来轨迹时效率极低（计算量巨大）。
*   **研究动机**：
    *   **计算冗余**：视频模型需要逐像素建模，而运动信息本质上比像素信息更稀疏、更紧凑。
    *   **多模态预测需求**：在机器人规划或场景理解中，需要快速生成多种合理的运动假设，现有视频模型无法满足实时性要求。
    *   **现有表示的局限**：光流（Flow）仅描述瞬时位移，轨迹（Tracks）虽紧凑但缺乏全局上下文且难以泛化。

### 2. 方法论
该论文提出了一个两阶段框架，核心思想是将稀疏轨迹压缩进一个连续的、可查询的潜空间。
*   **第一阶段：学习运动空间（Motion VAE）**
    *   **编码器**：将稀疏追踪器（如 TapNext）获得的轨迹与首帧图像特征（DINOv2）结合，通过 Transformer 编码为一个 $16 \times 16$ 的潜变量网格（Latent Grid）。
    *   **关键技术**：实现了 **64倍的时间压缩**。使用 3D 旋转位置编码（RoPE）来整合时间、空间位置和轨迹身份。
    *   **解码器**：采用类似 MAE 的设计，允许在首帧的任意空间位置进行查询，从而重建出密集的、上下文相关的运动轨迹。
*   **第二阶段：运动推理与规划（Motion Planner）**
    *   **核心算法**：在学习到的运动潜空间内训练一个**条件流匹配（Conditional Flow Matching）**模型。
    *   **条件机制**：支持文本指令（通过 BERT 嵌入）或空间点击（Pokes，即指定某点的终点位置）作为约束。
    *   **推理流程**：从高斯噪声开始，通过流匹配生成运动潜变量，再由 VAE 解码器转化为具体的运动轨迹。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用领域**：使用 KOALA-36M（1000万视频剪辑）进行大规模预训练，在 Pexels、DAVIS 和 PhysicsIQ 上评估。
    *   **机器人领域**：在 LIBERO 机器人基准测试（包含 LIBERO-10/90 等子集）上评估任务达成率。
*   **对比基准（Benchmarks）**：
    *   **视频模型**：Wan (14B)、Veo 3（Google）。
    *   **轨迹/运动模型**：Motion-I2V、Track2Act、ATM、Tra-MoE、WHN。
*   **评估指标**：Min MSE（反映真实性）、Mean MSE（反映多样性）、EPE（端点误差）、机器人任务成功率。

### 4. 资源与算力
论文详细列出了算力消耗：
*   **VAE 阶段**：3.4亿参数，使用 16 到 64 张 **Nvidia H200 GPU** 训练约 3 天。
*   **Planner 阶段**：5.3亿参数，同样使用 16 到 64 张 **Nvidia H200 GPU** 训练约 3 天。
*   **机器人策略头**：在 16 张 H200 上训练 12 小时。
*   **推理效率**：比视频模型快 **10,000 倍以上**（每秒可生成 2500 个时间步，而视频模型仅为 0.2）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从互联网视频到结构化机器人环境的跨领域测试，实验组数较多。
*   **消融实验**：
    *   测试了不同时间压缩倍率（2x 到 64x）的影响，发现高压缩率反而有助于学习语义运动。
    *   对比了不同追踪器（TapNext vs CoTracker3）的鲁棒性。
    *   验证了潜变量网格结构（Grid vs No Grid）的有效性。
*   **公平性**：在与视频模型对比时，采用了“时间对齐”（Time Matched）和“样本数对齐”（Sample Matched）两种方案，确保了对比的客观性。

### 6. 主要结论与发现
*   **压缩即学习**：极高的时间压缩（64x）不仅提升了效率，还迫使模型学习更高级的语义运动模式，提高了 kNN 检索准确率。
*   **性能领先**：在 LIBERO 机器人任务中，ZipMo 的成功率显著超过了 ATM 和 Tra-MoE 等专门的轨迹预测模型。
*   **效率飞跃**：在保持甚至超过 SOTA 视频模型运动质量的前提下，推理速度实现了量级上的突破。

### 7. 优点
*   **极度高效**：通过解耦运动与外观，极大地降低了预测未来动态的计算成本。
*   **灵活性强**：支持任意点的密集查询，且能处理文本和空间点击等多种交互输入。
*   **语义化**：学习到的运动空间具有良好的语义结构，能够区分不同的动作类别。

### 8. 不足与局限
*   **依赖追踪器质量**：模型性能受限于预训练时使用的追踪器（如 TapNext）的准确性，若追踪器在复杂遮挡下失效，会影响训练质量。
*   **缺乏像素生成**：该模型仅生成运动学轨迹，若需要最终的视频画面，仍需挂载额外的渲染器或视频合成模型。
*   **静态相机假设**：部分评估侧重于静态相机场景，在剧烈相机运动下的表现虽有测试（DAVIS），但仍是挑战。

（完）
