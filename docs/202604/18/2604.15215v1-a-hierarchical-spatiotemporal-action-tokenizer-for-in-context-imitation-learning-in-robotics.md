---
title: A Hierarchical Spatiotemporal Action Tokenizer for In-Context Imitation Learning in Robotics
title_zh: 一种用于机器人上下文内模仿学习的分层时空动作分词器
authors: "Fawad Javed Fateh, Ali Shah Ali, Murad Popattia, Usman Nizamani, Andrey Konin, M. Zeeshan Zia, Quoc-Huy Tran"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15215v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于机器人模仿学习的分层时空动作分词器
tldr: 本文提出了一种用于机器人上下文模仿学习的分层时空动作分词器（HiST-AT）。该方法通过两级矢量量化将动作映射为细粒度子簇和高层簇，并结合空间重构与时间戳预测来捕捉动作的时空特征。在多个仿真和真实机器人操作基准测试中，HiST-AT 显著优于非分层方法，达到了新的 SOTA 性能，证明了分层时空建模在复杂任务中的有效性。
selection_source: fresh_fetch
motivation: 旨在解决现有动作分词方法在上下文模仿学习中对复杂时空信息建模能力不足的问题。
method: 提出一种双层矢量量化架构，通过多级聚类同时恢复动作数据及其关联的时间戳信息。
result: 在多项仿真和真实机器人操作实验中，该方法均取得了当前最佳的性能表现。
conclusion: 分层时空动作分词是提升机器人上下文模仿学习效率和准确性的关键技术路径。
---

## 摘要
我们提出了一种用于上下文内模仿学习的新型分层时空动作分词器。我们首先提出了一种分层方法，该方法由两个连续层级的向量量化组成。具体而言，底层将输入动作分配给细粒度的子簇，而高层则进一步将细粒度子簇映射到簇。我们的分层方法优于非分层方法，同时主要通过重构输入动作来利用空间信息。此外，我们通过利用空间和时间线索扩展了该方法，形成了一个分层时空动作分词器，即 HiST-AT。具体来说，我们的分层时空方法在进行多级聚类的同时，能够恢复输入动作及其相关的时戳。最后，在多个仿真和真实机器人操作基准上的广泛评估表明，我们的方法在上下文内模仿学习中达到了新的最先进性能。

## Abstract
We present a novel hierarchical spatiotemporal action tokenizer for in-context imitation learning. We first propose a hierarchical approach, which consists of two successive levels of vector quantization. In particular, the lower level assigns input actions to fine-grained subclusters, while the higher level further maps fine-grained subclusters to clusters. Our hierarchical approach outperforms the non-hierarchical counterpart, while mainly exploiting spatial information by reconstructing input actions. Furthermore, we extend our approach by utilizing both spatial and temporal cues, forming a hierarchical spatiotemporal action tokenizer, namely HiST-AT. Specifically, our hierarchical spatiotemporal approach conducts multi-level clustering, while simultaneously recovering input actions and their associated timestamps. Finally, extensive evaluations on multiple simulation and real robotic manipulation benchmarks show that our approach establishes a new state-of-the-art performance in in-context imitation learning.

---

## 论文详细总结（自动生成）

### 论文总结：用于机器人上下文内模仿学习的分层时空动作分词器 (HiST-AT)

#### 1. 核心问题与研究动机
*   **核心问题**：在机器人上下文内模仿学习（In-Context Imitation Learning, ICIL）中，如何有效地对动作进行表征（Tokenization）是提升泛化能力的关键。现有的动作分词器通常采用扁平化聚类，且主要关注空间信息的重构，忽略了动作的**分层结构**（如复杂动作由简单子动作组成）和**时间连续性**。
*   **研究背景**：ICIL 允许机器人通过推理时提供的少量演示直接执行新任务而无需重新训练。然而，现有的方法在处理动作轨迹的平滑性和提取上下文相关的动作表征方面仍存在局限。

#### 2. 方法论
*   **核心思想**：提出 **HiST-AT**，通过**分层向量量化（Hierarchical VQ）**捕捉动作的宏观与微观结构，并结合**时空重构**任务来增强表征的鲁棒性。
*   **关键技术细节**：
    *   **分层聚类**：采用两级向量量化。底层（Codebook Z）将输入动作映射为细粒度的“子动作原语”；高层（Codebook A）将这些子动作进一步聚类为更抽象的动作单元。
    *   **时空重构**：模型不仅要重构输入的动作坐标（空间维度），还要预测动作对应的时间戳（时间维度）。这种自监督任务迫使模型学习动作随时间演变的动态特征。
    *   **Lipschitz 正则化**：在编码器中引入 Lipschitz 约束，通过限制权重的范数来确保潜在空间的平滑性，从而生成更平滑的机器人执行轨迹。
    *   **损失函数**：综合了量化损失（承诺损失和码本损失）、空间重构 MSE、时间重构 MSE 以及 Lipschitz 正则化损失。

#### 3. 实验设计
*   **数据集/场景**：
    *   **RoboCasa**：包含 7 种家庭任务（如开关抽屉、转动旋钮、插拔等）。
    *   **ManiSkill**：包含 3 种基础操作任务（取放方块、推方块、堆叠方块）。
    *   **真实世界**：在真实机器人操作基准上进行了验证（详细数据见补充材料）。
*   **对比方法（Baselines）**：
    *   基础架构：BC-Transformer, ACT, MCR。
    *   动作分词器：MLP, Discrete Binning (RT-1 风格), FAST, VQ-VAE, LFQ-VAE, 以及之前的 SOTA 方法 LipVQ-VAE。

#### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100 GPU**。
*   **训练时长**：
    *   RoboCasa 实验：训练 500K 次迭代。
    *   ManiSkill 实验：训练 30K 次迭代。
*   **模型规模**：为了公平对比，将 ACT 等复杂模型进行了缩减，使其参数量与 BC-Transformer 相当。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括标准成功率测试、跨数据集迁移测试（从 MimicGen 自动生成数据迁移到人类演示数据）、以及最具挑战性的 **Zero-shot（零样本）** 任务泛化测试。
*   **消融实验**：详细分析了分层聚类、时空重构各自的贡献，并探讨了码本大小（$\alpha K, K$）和时间损失权重（$\lambda_{temp}$）对性能的影响。
*   **评价**：实验设计全面，涵盖了仿真与现实、同分布与跨分布场景，对比基准涵盖了近年来主流的动作表征技术，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能提升**：HiST-AT 在 RoboCasa 上的平均成功率达到 59%（比前 SOTA 提升 6%），在 ManiSkill 上达到 67%（提升 5.3%）。
*   **泛化能力**：在零样本任务中，HiST-AT 的表现显著优于所有基准方法，证明了分层时空表征能更好地捕捉可迁移的动作模式。
*   **互补性**：实验证明分层聚类和时空重构是互补的，两者结合能产生最佳效果。

#### 7. 优点（亮点）
*   **结构创新**：首次将分层向量量化引入机器人动作分词领域，符合动作由粗到细的逻辑结构。
*   **时空融合**：通过引入时间戳预测任务，显式地解决了动作序列的时间相关性问题。
*   **鲁棒性强**：在跨数据集和零样本场景下的表现证明了该方法在处理分布偏移（Distribution Shift）时的优越性。

#### 8. 不足与局限
*   **超参数敏感性**：实验显示时间重构权重 $\lambda_{temp}$ 对结果有一定影响，过大或过小都会导致性能下降，可能需要针对不同任务进行微调。
*   **实时性说明不足**：虽然提到了推理过程，但未详细讨论分层量化带来的额外计算开销对实时控制频率的具体影响。
*   **真实世界细节**：正文对真实世界实验的描述较少，更多细节保留在补充材料中，削弱了正文对实机部署复杂性的展示。

（完）
