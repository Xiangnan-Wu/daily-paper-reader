---
title: "Rays as Pixels: Learning A Joint Distribution of Videos and Camera Trajectories"
title_zh: 以射线为像素：学习视频与相机轨迹的联合分布
authors: "Wonbong Jang, Shikun Liu, Soubhik Sanyal, Juan Camilo Perez, Kam Woh Ng, Sanskar Agrawal, Juan-Manuel Perez-Rua, Yiannis Douratsos, Tao Xiang"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09429v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 用于联合视频和相机轨迹生成的视频扩散模型
tldr: 本研究提出Rays as Pixels，一种将相机轨迹与视频内容联合建模的视频扩散模型。通过将相机参数转化为稠密射线像素（raxels），利用解耦自交叉注意力机制实现视频帧与相机位姿的同步去噪。该模型统一了位姿估计、联合生成及受控视频生成三大任务，在处理稀疏视图和模糊位姿时表现出极强的鲁棒性，并通过闭环自一致性测试验证了其预测的准确性。
selection_source: fresh_fetch
motivation: 针对相机位姿估计与视图合成任务长期分离、在稀疏或模糊场景下难以互补的问题，探索两者的联合分布学习。
method: 将相机表示为稠密射线像素，并利用视频扩散模型通过解耦自交叉注意力机制对视频和相机轨迹进行联合去噪。
result: 模型在位姿估计和相机控制视频生成上表现优异，且轨迹预测仅需极少的去噪步数即可实现与视频生成的自一致性。
conclusion: 通过联合学习视频与相机轨迹的分布，可以有效统一计算机视觉中的逆向感知与前向生成任务。
---

## 摘要
从图像中恢复相机参数以及从新视角渲染场景，长期以来在计算机视觉和图形学中被视为相互独立的任务。当图像覆盖稀疏或位姿存在歧义时，这种分离便不再适用，因为每个任务都需要另一个任务的输出。我们提出了“以射线为像素”（Rays as Pixels），这是一种视频扩散模型（VDM），用于学习视频和相机轨迹的联合分布。我们将每个相机表示为密集的射线像素（raxels），并通过解耦自交叉注意力（Decoupled Self-Cross Attention）机制，将其与视频帧共同进行去噪。单个训练好的模型可以处理三项任务：从视频预测相机轨迹、从输入图像联合生成视频和相机轨迹，以及根据输入图像沿目标相机轨迹生成视频。由于该模型既能从视频预测轨迹，又能根据其自身的预测生成视图，我们通过闭环自一致性测试对其进行了评估，证明了其正向和反向预测的一致性。值得注意的是，轨迹预测所需的去噪步骤远少于视频生成，甚至只需几个去噪步骤即可实现自一致性。我们报告了在位姿估计和相机控制视频生成方面的结果。

## Abstract
Recovering camera parameters from images and rendering scenes from novel viewpoints have long been treated as separate tasks in computer vision and graphics. This separation breaks down when image coverage is sparse or poses are ambiguous, since each task needs what the other produces. We propose Rays as Pixels, a Video Diffusion Model (VDM) that learns a joint distribution over videos and camera trajectories. We represent each camera as dense ray pixels (raxels) and denoise them jointly with video frames through Decoupled Self-Cross Attention mechanism. A single trained model handles three tasks: predicting camera trajectories from video, jointly generating video and camera trajectory from input images, and generating video from input images along a target camera trajectory. Because the model can both predict trajectories from a video and generate views conditioned on its own predictions, we evaluate it through a closed-loop self-consistency test, demonstrating that its forward and inverse predictions agree. Notably, trajectory prediction requires far fewer denoising steps than video generation, even a few denoising steps suffice for self-consistency. We report results on pose estimation and camera-controlled video generation.

---

## 论文详细总结（自动生成）

以下是对论文《Rays as Pixels: Learning A Joint Distribution of Videos and Camera Trajectories》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统计算机视觉将“相机位姿估计”（逆向过程）与“新视角合成/视频生成”（正向过程）视为独立任务。这种分离在图像覆盖稀疏或位姿模糊时会导致系统脆弱，因为这两个任务本质上是互补的。
*   **研究动机**：现有的受控视频生成模型通常将相机位姿视为固定的输入条件，依赖外部估计器（如COLMAP或DUSt3R）。如果估计器失败，生成模型也无法工作。
*   **整体含义**：本文提出了一种统一的视频扩散模型，通过学习视频帧与相机轨迹的**联合分布**，使单个模型能够同时胜任位姿估计、联合生成和受控生成三项任务。

### 2. 方法论
*   **核心思想**：将相机参数重新编码为“**射线像素（Raxels）**”。这是一种稠密的、像素级的地图，每个像素编码了对应相机射线的起点和方向。
*   **关键技术细节**：
    *   **Raxel 表示**：将射线方向 $d$ 和起点 $o$ 相加得到一个 3 通道向量（$d+o$）。由于其空间结构与 RGB 图像一致，可以使用预训练视频 VAE 的编码器将其压缩到潜空间。
    *   **联合去噪（Flow Matching）**：采用流匹配框架，将视频潜变量和射线潜变量拼接，在同一网络中共同去噪。
    *   **解耦自交叉注意力（Decoupled Self-Cross Attention）**：在 Transformer 块中设置并行分支，分别处理视频和射线。通过“模态内自注意力”保持各自的一致性，通过“模态间交叉注意力”实现两者间的对齐。
    *   **位姿恢复**：推理时，通过解码 Raxel 图像并使用 **Procrustes 算法**（正交普鲁克分析）进行刚性配准，从而恢复相机矩阵和焦距。

### 3. 实验设计
*   **数据集**：使用了大规模真实场景数据集 **RealEstate10K (Re10K)** 和 **DL3DV** 进行训练。
*   **评测基准（Benchmarks）**：
    *   **位姿估计**：在 Re10K、DL3DV-140 和 Tanks and Temples (T&T) 上测试旋转精度（mRRA@30）。
    *   **视频生成**：使用 FID（视觉质量）和 FVD（时间相干性）评估。
*   **对比方法**：
    *   **位姿估计对比**：VGGT（前馈式位姿估计器）。
    *   **视频生成对比**：MotionCtrl、VD3D、ViewCrafter、Wonderland、Kaleido 等。
*   **自一致性测试（Self-Consistency）**：通过“视频预测位姿 -> 位姿重新生成视频”的闭环测试，验证正反向预测的一致性。

### 4. 资源与算力
*   **模型规模**：基于 **Wan 2.1 14B** 文本生成视频模型构建，增加了 6B 参数的射线分支，总参数量达到 **20B**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。考虑到 20B 参数的全量微调，通常需要大规模 GPU 集群（如数百块 H100/A100）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大核心任务（位姿估计、联合生成、受控生成），并在三个主流数据集上进行了定量对比。
*   **消融实验**：针对 Raxel 表示（对比 Plücker 嵌入）、解耦注意力机制（DSCA）以及损失函数（余弦相似度损失）进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分，特别是引入了“闭环自一致性测试”，这在生成模型领域是一个非常客观且严苛的评价指标，证明了模型确实学习到了联合分布而非简单的条件映射。

### 6. 主要结论与发现
*   **联合学习的优势**：单个模型在位姿估计和视频生成上均达到了竞争水平，且两者在逻辑上是自洽的。
*   **去噪效率差异**：发现相机轨迹（射线）的收敛速度远快于视频内容，仅需 **2 个去噪步数** 即可获得高精度的位姿估计结果。
*   **表示的重要性**：实验证明 Raxel 表示优于 Plücker 嵌入，因为它能更好地利用预训练视频 VAE 的空间先验。

### 7. 优点
*   **统一性**：打破了感知（位姿估计）与生成（视图合成）的界限，实现了端到端的联合建模。
*   **鲁棒性**：不依赖外部位姿估计器，能够处理稀疏输入。
*   **灵活性**：支持多种推理模式，且对输入图像的时间位置（第一帧、中间帧或最后一帧）没有严格限制。

### 8. 不足与局限
*   **场景限制**：训练数据主要集中在静态场景，对于包含大量动态物体或剧烈相机运动的视频，泛化能力可能受限。
*   **时间分辨率**：受限于 VAE 的 4 倍时间压缩，恢复出的相机轨迹在极高频的时间尺度上可能存在平滑或分辨率不足的问题。
*   **控制维度**：目前主要侧重于相机控制，尚未整合文本描述等更高级的语义控制手段。

（完）
