---
title: "AGEL-Comp: A Neuro-Symbolic Framework for Compositional Generalization in Interactive Agents"
title_zh: AGEL-Comp：一种用于交互式智能体组合泛化的神经符号框架
authors: "Mahnoor Shahid, Hannes Rothe"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26522v1"
tags: ["query:robot"]
score: 9.0
evidence: 因果程序图作为交互式智能体的世界模型
tldr: 针对大语言模型智能体在交互环境中组合泛化能力不足的问题，本文提出 AGEL-Comp 神经符号框架。该框架通过动态因果程序图构建世界模型，利用归纳逻辑编程从反馈中合成知识，并结合 LLM 与神经定理证明器进行混合推理。实验表明，该方法在 Retro Quest 模拟环境中显著优于纯 LLM 模型，实现了可解释且具备组合结构的知识理解。
selection_source: fresh_fetch
motivation: 解决大语言模型智能体在复杂交互任务中因缺乏组合泛化能力而导致的鲁棒性不足问题。
method: 提出一种集成因果程序图、归纳逻辑编程引擎和神经定理证明器的神经符号架构，通过演绎-溯因循环更新知识。
result: 在 Retro Quest 模拟环境的组合泛化场景评估中，AGEL-Comp 的表现明显优于纯大语言模型。
conclusion: 该框架为构建具有显式、可解释且具备组合结构理解能力的智能体提供了一条原则性路径。
---

## 摘要
基于大语言模型（LLM）的智能体在组合泛化方面存在系统性缺陷，限制了其在交互式环境中的鲁棒性。本研究提出了 AGEL-Comp，这是一种神经符号 AI 智能体架构，旨在通过锚定智能体动作来解决这一挑战。AGEL-Comp 集成了三项核心创新：(1) 动态因果程序图（CPG）作为世界模型，将过程性和因果性知识表示为有向超图；(2) 归纳逻辑程序设计（ILP）引擎，从经验反馈中合成新的霍恩子句（Horn clauses），通过交互实现符号知识的锚定；(3) 混合推理核心，由 LLM 提出一组候选子目标，并由神经定理证明器（NTP）验证其逻辑一致性。这些组件共同构建了一个“演绎-溯因”（deduction--abduction）学习循环：使智能体能够演绎出计划，并溯因式地扩展其符号世界模型，同时神经自适应阶段使推理引擎与新知识保持一致。我们在 Retro Quest 模拟环境中提出了一套评估协议，用于探测组合泛化场景以评估 AGEL 智能体。研究结果清楚地表明，AGEL 模型的性能优于纯基于 LLM 的模型。我们的框架为开发能够对其世界建立显式、可解释且具有组合结构理解的智能体提供了一条原则性路径。

## Abstract
Large Language Model (LLM)-based agents exhibit systemic failures in compositional generalization, limiting their robustness in interactive environments. This work introduces AGEL-Comp, a neuro-symbolic AI agent architecture designed to address this challenge by grounding actions of the agent. AGEL-Comp integrates three core innovations: (1) a dynamic Causal Program Graph (CPG) as a world model, representing procedural and causal knowledge as a directed hypergraph; (2) an Inductive Logic Programming (ILP) engine that synthesizes new Horn clauses from experiential feedback, grounding symbolic knowledge through interaction; and (3) a hybrid reasoning core where an LLM proposes a set of candidate sub-goals that are verified for logical consistency by a Neural Theorem Prover (NTP). Together, these components operationalize a deduction--abduction learning cycle: enabling the agent to deduce plans and abductively expand its symbolic world model, while a neural adaptation phase keeps its reasoning engine aligned with new knowledge. We propose an evaluation protocol within the \texttt{Retro Quest} simulation environment to probe for compositional generalization scenarios to evaluate our AGEL agent. Our findings clearly indicate the better performance of our AGEL model over pure LLM-based models. Our framework presents a principled path toward agents that build an explicit, interpretable, and compositionally structured understanding of their world.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AGEL-Comp** 的神经符号框架，旨在解决大语言模型（LLM）智能体在交互式环境中面临的“组合泛化危机”。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 LLM 在自然语言任务中表现出色，但在作为交互式智能体时，往往表现出**组合泛化（Compositional Generalization）**能力的缺失。即它们难以将已知的原始组件（动作、概念）组合成全新的、系统性的方案，而是过度依赖统计模式匹配。
*   **研究背景**：这种局限性源于 LLM 的知识是“脱离实体”的，缺乏**锚定（Grounding）**——即抽象符号与现实世界因果后果之间的联系。当环境发生变化或出现未见过的组合场景时，纯神经模型往往会失效。
*   **整体含义**：论文提出通过“神经符号”结合的方式，为智能体构建一个显式的、可解释的、具备因果逻辑的世界模型，使其能从经验中学习规则并进行严密的逻辑推理。

### 2. 方法论：核心思想与关键技术
AGEL-Comp 框架通过**演绎-溯因（Deduction-Abduction）**学习循环来实现组合泛化，包含三个核心组件：
*   **因果程序图（CPG）作为世界模型**：将知识表示为有向超图，节点是谓词，边是霍恩子句（Horn clauses）。这种结构使复杂的程序依赖关系显式化。
*   **归纳逻辑编程（ILP）引擎**：
    *   **最小对比搜索（MCS）算法**：当智能体预测失败时，通过对比成功与失败的经验，定位导致失败的单一因果因素（因果归因）。
    *   **元解释学习（MIL）**：将定位到的因果因素泛化为通用的符号规则（如：从“火导致伤害”泛化为“所有有害物质都会导致伤害”），并更新到 CPG 中。
*   **混合推理核心（LLM + NTP）**：
    *   **LLM 作为规划器**：提出候选子目标。
    *   **神经定理证明器（NTP）作为验证器**：在连续向量空间中对 LLM 的提议进行可微的逻辑验证。只有符合 CPG 逻辑规则的计划才会被执行。
*   **学习流程**：智能体感知环境 -> LLM 提议计划 -> NTP 验证 -> 执行并获取反馈 -> 若失败则触发 ILP 学习新规则 -> 更新 CPG 和 NTP 嵌入。

### 3. 实验设计
*   **模拟环境**：开发了 **Retro Quest**，一个基于 Unity 的 2D 动作 RPG 游戏环境，包含怪物、NPC 和交互物体。
*   **评估协议**：设计了包含 10 个任务的课程学习，难度从简单到“极难”，专门探测智能体在处理歧义和新颖组合时的表现。
*   **对比基准（Benchmark）**：
    *   **MLLM-Agent (Baseline)**：标准的 ReAct 风格智能体，无显式世界模型。
    *   **AGEL-Comp (Full System)**：完整框架。
    *   **消融实验**：包括“无 NTP 验证器”版本和“无 ILP 学习器”版本。
*   **底层模型**：使用了 GPT-4o、Gemini Pro 2.5、LLaVA 1.6 和 DeepSeek v1 四种主流多模态模型作为认知核心。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练总时长。
*   **实现细节**：由于使用了 GPT-4o 和 Gemini 等闭源模型，部分推理依赖于 API 调用。开源模型（LLaVA, DeepSeek）的运行环境未详细标注，但提到 NTP 的训练涉及梯度优化和符号嵌入更新。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种不同的 LLM 后端，每种配置在 10 个任务上运行，并使用 3 个随机种子以确保结果的统计意义。
*   **充分性评价**：
    *   **充分性**：实验涵盖了从闭源到开源的多种模型，且通过消融实验清晰地展示了 NTP 和 ILP 各自的贡献，实验设计较为严谨。
    *   **客观性**：通过预注册实验协议（OSF）增加了研究的透明度。
    *   **公平性**：所有配置使用相同的感知输入和动作空间，对比具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：AGEL-Comp 在所有 LLM 后端上均实现了 **100% 的任务成功率**，而基准模型在困难任务中表现惨淡（甚至降至 0%）。
*   **零样本泛化**：AGEL-Comp 的“首次尝试成功率”远高于基准模型（60% vs 3.3%），证明其具备真正的组合推理能力而非试错。
*   **组件协同**：消融实验证明，仅有验证器（无学习）或仅有学习器（无验证）都无法达到最佳效果。逻辑推理（演绎）与经验学习（溯因）的结合是成功的关键。
*   **样本效率**：AGEL-Comp 表现出极高的样本效率，通常只需一次失败即可诱导出正确的规则。

### 7. 优点
*   **架构创新**：成功将符号逻辑的严密性与 LLM 的生成灵活性结合，解决了纯神经模型难以处理的组合泛化问题。
*   **可解释性**：CPG 提供的世界模型是显式的符号规则，人类可以直观地理解智能体学到了什么。
*   **鲁棒性**：通过 NTP 验证，有效减少了 LLM 产生看似合理但逻辑错误的计划（幻觉）。

### 8. 不足与局限
*   **计算开销**：NTP 的可微证明过程在大规模规则库下可能存在计算瓶颈（尽管文中提到通过约束搜索空间有所缓解）。
*   **环境限制**：目前仅在 2D 模拟环境中验证，现实世界中复杂的、噪声巨大的感知数据可能会对符号锚定（Grounding）造成挑战。
*   **规则冲突**：随着 CPG 规模增长，如何高效处理规则冲突和过时规则的撤回（Rule Retraction）仍需进一步研究。
*   **算力细节缺失**：缺乏具体的算力消耗报告，难以评估其在资源受限设备上的部署可行性。

（完）
