---
title: "World2VLM: Distilling World Model Imagination into VLMs for Dynamic Spatial Reasoning"
title_zh: World2VLM：将世界模型的想象力蒸馏至视觉语言模型以实现动态空间推理
authors: "Wanyue Zhang, Wenxiang Wu, Wang Xu, Jiaxin Luo, Helu Zhi, Yibin Huang, Shuo Ren, Zitao Liu, Jiajun Zhang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26934v1"
tags: ["query:robot"]
score: 9.0
evidence: 将生成式世界模型的空间想象力蒸馏到视觉语言模型中
tldr: 针对视觉语言模型（VLM）在动态空间推理方面的不足，本文提出 World2VLM 框架。该框架通过将生成式世界模型的空间想象能力蒸馏到 VLM 中，利用世界模型合成几何一致的未来视图，为前向和逆向空间推理提供结构化监督。实验表明，该方法在多个空间推理基准测试中显著提升了模型性能，且无需推理时的额外计算开销，实现了高效的空间能力内化。
selection_source: fresh_fetch
motivation: 现有的 VLM 难以理解运动带来的场景变化，且现有的推理时耦合世界模型的方法存在巨大的计算开销。
method: 通过世界模型根据相机轨迹合成几何一致的未来视图，并利用这些数据对 VLM 进行前向和逆向空间推理的两阶段蒸馏训练。
result: World2VLM 在 SAT-Real、VSI-Bench 和 MindCube 等多个基准测试中均优于基础模型和推理时生成方法。
conclusion: 世界模型不仅可以作为推理工具，更能作为高效的训练导师，帮助 VLM 学习并内化复杂的动态空间想象能力。
---

## 摘要
视觉语言模型 (VLM) 在静态视觉理解方面表现出强大的性能，但在动态空间推理方面仍面临挑战，这类推理需要想象场景在第一人称视角运动下如何演变。最近的研究通过使用合成数据扩展空间监督，或者在推理时将 VLM 与世界模型耦合来解决这一局限性。然而，前者通常缺乏对以运动为条件的决策状态转移的显式建模，而后者则会产生巨大的计算开销。在这项工作中，我们提出了 World2VLM，这是一个将生成式世界模型的空间想象力蒸馏到视觉语言模型中的训练框架。给定初始观测和参数化的相机轨迹，我们使用视角一致的世界模型来合成几何对齐的未来视图，并为前向（动作到结果）和逆向（结果到动作）空间推理推导出结构化监督。我们使用该流水线生成的紧凑数据集，通过两阶段方案对 VLM 进行后训练，并在多个空间推理基准测试上对其进行评估。World2VLM 在包括 SAT-Real、SAT-Synthesized、VSI-Bench 和 MindCube 在内的各种基准测试中，相比基础模型实现了持续的改进。它还优于测试时与世界模型耦合的方法，同时消除了对昂贵的推理时生成的需要。我们的结果表明，世界模型不仅可以作为推理时的工具，还可以作为有效的训练时教师，使 VLM 能够以可扩展且高效的方式内化空间想象力。

## Abstract
Vision-language models (VLMs) have shown strong performance on static visual understanding, yet they still struggle with dynamic spatial reasoning that requires imagining how scenes evolve under egocentric motion. Recent efforts address this limitation either by scaling spatial supervision with synthetic data or by coupling VLMs with world models at inference time. However, the former often lacks explicit modeling of motion-conditioned state transitions, while the latter incurs substantial computational overhead. In this work, we propose World2VLM, a training framework that distills spatial imagination from a generative world model into a vision-language model. Given an initial observation and a parameterized camera trajectory, we use a view-consistent world model to synthesize geometrically aligned future views and derive structured supervision for both forward (action-to-outcome) and inverse (outcome-to-action) spatial reasoning. We post-train the VLM with a two-stage recipe on a compact dataset generated by this pipeline and evaluate it on multiple spatial reasoning benchmarks. World2VLM delivers consistent improvements over the base model across diverse benchmarks, including SAT-Real, SAT-Synthesized, VSI-Bench, and MindCube. It also outperforms the test-time world-model-coupled methods while eliminating the need for expensive inference-time generation. Our results suggest that world models can serve not only as inference-time tools, but also as effective training-time teachers, enabling VLMs to internalize spatial imagination in a scalable and efficient manner.

---

## 论文详细总结（自动生成）

这是一份关于论文《World2VLM: Distilling World Model Imagination into VLMs for Dynamic Spatial Reasoning》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉语言模型（VLM）在静态图像理解上表现出色，但在**动态空间推理**（即想象场景在第一人称视角运动下如何演变的能力）方面存在显著短板。
*   **核心问题**：现有的改进方案要么依赖缺乏运动建模的静态合成数据，要么在推理时挂载昂贵的世界模型（World Model）。本文探讨：**能否将世界模型的“想象力”作为训练时的教师，直接蒸馏到 VLM 的参数中？**
*   **整体含义**：提出 World2VLM 框架，通过将世界模型生成的动作对齐视图转换内化到 VLM 中，使其在无需推理时调用外部模型的情况下，具备强大的动态空间推理能力。

### 2. 论文提出的方法论
核心思想是利用生成式世界模型作为“训练时教师”，通过两阶段后训练实现空间能力的内化。
*   **世界模型引导的转换构建**：
    *   给定初始观测图像和参数化的相机轨迹，利用视角一致的世界模型（如 SVC 或 HY-WorldPlay）合成几何对齐的未来视图。
    *   使用检测器-跟踪器提取对象元数据（如 Bounding Box），并将其序列化为文本。
*   **双向空间任务套件**：
    *   **逆向推理（Outcome-to-Action）**：给定两张图像，推断导致视角变化的相机动作（如平移距离、旋转角度）。
    *   **前向推理（Action-to-Outcome）**：给定初始图像和动作，预测结果（如物体的目标框、可见性判断）。
*   **两阶段后训练方案**：
    *   **第一阶段（SFT）**：在约 100k 条由世界模型生成的结构化数据上进行监督微调，涵盖 8 种具体的空间任务。
    *   **第二阶段（GRPO）**：使用组相对策略优化（GRPO）进行强化学习精调，通过任务感知的奖励函数（格式、数值精度、空间逻辑等）进一步提升模型输出的准确性和结构化程度。

### 3. 实验设计
*   **基础模型**：Qwen2.5-VL-7B-Instruct。
*   **数据集/场景**：训练数据源自 ScanNet（真实室内场景）和 MulSeT（模拟室内场景）。
*   **基准测试（Benchmarks）**：
    *   **SAT-Real & SAT-Synthesized**：评估第一人称视角下的空间适应能力。
    *   **VSI-Bench**：评估空间配置、测量估计和时空推理。
    *   **MindCube**：侧重于部分观测下的视角切换和心理模拟。
*   **对比方法**：
    *   基础 VLM 模型。
    *   **MindJourney 风格的推理时耦合方法**：在推理时调用世界模型生成候选视图并由 VLM 评分。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   采用 **LoRA**（Rank 256）进行参数高效微调。
    *   SFT 阶段使用 bfloat16 精度，最大序列长度 8192。
    *   GRPO 阶段在 1k 条平衡子集上进行，Actor/Critic 学习率分别为 $2 \times 10^{-6}$ 和 $1 \times 10^{-6}$。
*   **训练时长**：文中未明确给出总训练小时数，但提到 SFT 数据量为 100k，GRPO 为 1k 样本。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流空间推理基准上进行了全面测试，涵盖了真实与合成场景。
*   **消融实验**：
    *   **蒸馏方向**：对比了仅前向、仅逆向以及双向任务的效果，证明了双向监督的互补性。
    *   **数据来源**：对比了仅真实场景、仅模拟场景以及混合场景的效果。
    *   **教师模型**：对比了 SVC 和 HY-WorldPlay 两种不同的世界模型作为教师的效果。
*   **客观性**：实验设置了严格的基准测试隔离（Benchmark 样本不进入训练集），并与推理时开销巨大的 SOTA 方法进行了公平对比。

### 6. 论文的主要结论与发现
*   **性能提升**：World2VLM 在所有基准测试中均显著优于基础模型，平均分提升超过 15%。
*   **内化优于耦合**：训练时蒸馏的方法不仅在推理速度上远快于推理时耦合世界模型的方法，且在准确率上也表现更优。
*   **GRPO 的作用**：强化学习阶段能显著提升模型在数值精度（如旋转角度）和结构化输出（如 BBox 坐标）上的表现。
*   **跨教师通用性**：该框架不依赖于特定的世界模型，无论是基于扩散的 SVC 还是基于视频生成的 HY-WorldPlay 都能提供有效指导。

### 7. 优点
*   **高效性**：实现了“零推理开销”的空间想象力，部署时无需挂载庞大的生成模型。
*   **双向约束设计**：通过前向预测和逆向解释同时约束空间表征，有效减少了模型走“语言捷径”的可能性。
*   **任务全面性**：涵盖了从简单的动作估计到复杂的跨视图对象一致性判断等多种任务。

### 8. 不足与局限
*   **教师模型瓶颈**：VLM 的上限受限于世界模型的生成质量。如果世界模型产生几何畸变或伪影，会引入噪声监督。
*   **场景覆盖**：目前主要集中在室内场景（ScanNet/MulSeT），对于室外大场景或极端动态环境的泛化能力尚待验证。
*   **物体类别限制**：依赖于现有的检测器-跟踪器流水线，对于长尾类别或无固定形状物体的空间推理可能受限。

（完）
