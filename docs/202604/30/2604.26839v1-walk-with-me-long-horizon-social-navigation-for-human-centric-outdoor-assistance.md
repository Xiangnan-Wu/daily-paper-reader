---
title: "Walk With Me: Long-Horizon Social Navigation for Human-Centric Outdoor Assistance"
title_zh: 与我同行：面向以人为中心的户外辅助的长程社交导航
authors: "Lingfeng Zhang, Xiaoshuai Hao, Xizhou Bu, Yingbo Tang, Hongsheng Li, Jinghui Lu, Xiu-shen Wei, Jiayi Ma, Yu Liu, Jing Zhang, Hangjun Ye, Xiaojun Liang, Long Chen, Wenbo Ding"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26839v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于机器人导航与规划的视觉语言模型
tldr: 针对户外环境下机器人辅助人类导航的挑战，本文提出 Walk with Me 框架。该框架无需预构建高精地图，通过结合 GPS 和公共地图 API 的兴趣点，利用高级视觉语言模型（VLM）将自然语言指令转化为长程路径规划。系统采用分层架构：高级 VLM 负责语义目标定位与安全推理，低级视觉语言动作模型（VLA）负责具体动作执行。这种观察感知路由机制确保了机器人在复杂社交场景下的安全性与合规性，实现了实用的长程户外社交导航。
selection_source: fresh_fetch
motivation: 现有的户外导航方法高度依赖昂贵的高精地图，而学习类策略多局限于室内或短程场景，难以处理复杂的长程社交导航任务。
method: 提出一种分层框架，利用高级 VLM 进行语义目标定位与路径规划，并结合低级 VLA 模型执行具体动作，同时引入观察感知路由机制进行安全推理。
result: 成功实现了从高层自然语言指令到安全、长程且符合社交规范的户外导航行为的转化。
conclusion: Walk with Me 通过结合语义意图理解、无图长程规划和安全感知推理，为以人为中心的户外辅助机器人提供了切实可行的方案。
---

## 摘要
在开放世界户外环境中辅助人类，要求机器人将高层自然语言意图转化为安全、长程且符合社交规范的导航行为。现有的基于地图的方法依赖于昂贵的预构建高精地图，而基于学习的策略大多局限于室内和短程场景。为了弥补这一差距，我们提出了 Walk with Me，这是一个根据高层人类指令进行长程社交导航的无地图框架。Walk with Me 利用 GPS 上下文和来自公共地图 API 的轻量级候选兴趣点（POI）进行语义目的地定位和路径点提议。高层视觉语言模型（VLM）将抽象指令落实为具体目的地，并规划粗略的路径点序列。在执行过程中，一种观测感知的路由机制会确定底层视觉-语言-动作（VLA）策略是否能够处理当前情况，或者是否需要来自高层 VLM 的显式安全推理。常规路段由底层 VLA 执行，而复杂情况（如拥挤的路口）则会在不安全时触发高层推理和“停止并等待”行为。通过结合语义意图定位、无地图长程规划、安全感知推理和底层动作生成，Walk with Me 实现了面向以人为中心辅助的实用户外社交导航。

## Abstract
Assisting humans in open-world outdoor environments requires robots to translate high-level natural-language intentions into safe, long-horizon, and socially compliant navigation behavior. Existing map-based methods rely on costly pre-built HD maps, while learning-based policies are mostly limited to indoor and short-horizon settings. To bridge this gap, we propose Walk with Me, a map-free framework for long-horizon social navigation from high-level human instructions. Walk with Me leverages GPS context and lightweight candidate points-of-interest from a public map API for semantic destination grounding and waypoint proposal. A High-Level Vision-Language Model grounds abstract instructions into concrete destinations and plans coarse waypoint sequences. During execution, an observation-aware routing mechanism determines whether the Low-Level Vision-Language-Action policy can handle the current situation or whether explicit safety reasoning from the High-Level VLM is needed. Routine segments are executed by the Low-Level VLA, while complex situations such as crowded crossings trigger high-level reasoning and stop-and-wait behavior when unsafe. By combining semantic intent grounding, map-free long-horizon planning, safety-aware reasoning, and low-level action generation, Walk with Me enables practical outdoor social navigation for human-centric assistance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Walk With Me: Long-Horizon Social Navigation for Human-Centric Outdoor Assistance》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**户外长程社交导航**（Long-Horizon Social Navigation）问题。
*   **研究动机**：现有的机器人导航方法要么依赖昂贵且难以维护的高精地图（HD Map），要么局限于室内短程环境，难以处理户外开放世界中复杂的社交规则（如过马路、避开人群）以及人类抽象的自然语言指令（如“我想去散步”）。
*   **核心目标**：开发一个“无图”（Map-free）框架，能够将人类的高层意图转化为安全、长程且符合社交规范的导航行为，适用于最后一公里配送和导盲辅助等场景。

### 2. 方法论：核心思想与技术细节
论文提出了 **Walk with Me** 框架，其核心思想是**解耦“去哪里”的语义规划与“怎么走”的动作执行**。
*   **高层语义规划（High-Level Semantic Planning）**：
    *   **目的地定位**：利用高层视觉语言模型（VLM）结合当前 GPS 上下文，通过公共地图 API（如 Google/百度地图）搜索候选兴趣点（POI），将抽象指令（如“去散步”）落实为具体目的地。
    *   **路径点生成**：调用地图 API 的步行导航接口生成粗略的路径点序列。
*   **自适应双策略导航（Adaptive Dual-Policy Navigation）**：
    *   **观测感知路由机制**：系统实时判断当前场景是“常规”还是“复杂”。
    *   **高层 VLM 安全推理**：在复杂场景（如十字路口、拥挤地段）下，调用 VLM 进行显式安全推理，决定是否执行“停止并等待”（Stop-and-wait）。
    *   **底层 VLA 动作生成**：在常规路段，利用底层视觉-语言-动作模型（VLA）根据视觉观测、路径点和历史轨迹生成符合社交规范的局部运动轨迹。
*   **闭环执行**：结合 SLAM 位姿反馈和底层运动控制器，实现对预测轨迹的实时跟踪。

### 3. 实验设计
*   **实验场景**：在真实的户外环境中进行了两类应用测试：
    1.  **最后一公里配送**：如“将奶茶送到 B 栋”。
    2.  **导盲辅助**：如“我想去购物”。
*   **Benchmark 与对比方法**：
    *   **VLM 消融对比**：对比了 GPT-5、Claude-Sonnet、Gemini-3-Flash、Qwen3-VL-8B、RoboBrain 2.0 和 MiMo-Embodied。
    *   **VLA 消融对比**：对比了 GNM、ViNT、NoMaD、CityWalker 和 SocialNav。
*   **评价指标**：主要使用**成功率（Success Rate, SR）**，即机器人无需人工干预到达目的地的比例。

### 4. 资源与算力
*   **硬件平台**：使用 Athena 2.0 Pro AGV 轮式机器人，配备 Intel RealSense D455 摄像头和 GPS 接收器。
*   **算力支持**：VLA 模型和高层 VLM 推理部署在远程服务器上，配备了 **NVIDIA H20 GPU**。
*   **训练细节**：文中未详细说明底层 VLA 的具体训练时长，但指出其利用了大规模具身智能数据集进行预训练。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **20 次端到端真实世界测试**（4 个场景，每个场景 5 次独立实验）。此外，针对 VLM 和 VLA 的效能分别进行了 **20 组消融实验**。
*   **充分性评价**：实验设计较为全面，涵盖了从语义理解到长程执行的全流程。通过对比多种闭源和开源的最先进模型，验证了框架的泛化能力。虽然 20 次真实测试的样本量在统计学上不算巨大，但在机器人实地长程导航测试中具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **整体表现**：Walk with Me 在真实户外任务中达到了 **60% 的平均成功率**。
*   **任务差异**：配送任务（SR 70%）表现优于导盲任务（SR 50%），后者对安全推理和意图理解的要求更高。
*   **模型协同**：实验证明，强大的底层 VLA（如 SocialNav）对于处理局部社交互动至关重要，而高层 VLM 的安全推理能有效降低路口等高风险区域的事故率。

### 7. 优点（亮点）
*   **无图化部署**：摆脱了对昂贵高精地图的依赖，利用现有的公共地图 API 实现了极低成本的户外扩展。
*   **分层架构设计**：巧妙地结合了 VLM 的逻辑推理能力和 VLA 的实时控制能力，解决了长程导航中的“语义鸿沟”问题。
*   **安全意识**：引入了显式的“停止并等待”机制，使机器人在面对红绿灯和复杂车流时表现得更像人类。

### 8. 不足与局限
*   **依赖外部服务**：系统高度依赖 GPS 信号和公共地图 API 的可用性，在信号遮挡或地图数据过时的区域可能失效。
*   **推理延迟**：依赖远程服务器（H20 GPU）进行推理，在网络环境不稳定的情况下可能存在控制延迟。
*   **长尾场景挑战**：在极端拥挤或高度动态的非结构化环境中，VLM 的判断准确率仍有提升空间。
*   **样本量限制**：真实世界实验虽然客观，但受限于场地和成本，覆盖的极端天气或特殊地形较少。

（完）
