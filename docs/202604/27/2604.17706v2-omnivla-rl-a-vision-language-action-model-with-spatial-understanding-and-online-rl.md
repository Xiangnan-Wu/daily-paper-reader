---
title: "OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL"
title_zh: OmniVLA-RL：一种具有空间理解和在线强化学习能力的视觉-语言-动作模型
authors: "Haoxiang Jie, Yaoyuan Yan, Xiangyu Wei, Kailin Wang, Hongjie Yan, Zhiyou Heng, Daocheng Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17706v2"
tags: ["query:robot"]
score: 10.0
evidence: 具有空间理解和强化学习能力的VLA模型
tldr: 针对现有视觉-语言-动作（VLA）模型在空间感知、多模态融合及强化学习稳定性方面的不足，本文提出OmniVLA-RL。该模型采用Mix-of-Transformers（MoT）架构整合推理、空间与动作专家，并引入Flow-GSPO算法，通过将流匹配重构为随机微分方程并结合分组分段策略优化，显著提升了动作精度与训练鲁棒性，在LIBERO等基准测试中表现优异。
selection_source: fresh_fetch
motivation: 解决现有VLA模型在空间感知不精确、多模态融合欠佳以及强化学习训练不稳定等核心局限性。
method: 提出Mix-of-Transformers架构以协同多专家模块，并结合Flow-GSPO算法优化动作生成与策略训练。
result: 在LIBERO和LIBERO-Plus基准测试中，OmniVLA-RL取得了出色的整体性能，并超越了现有的主流方法。
conclusion: OmniVLA-RL通过创新的架构设计与优化算法，有效克服了当前VLA模型的局限，提升了具身智能体的操作精度。
---

## 摘要
视觉-语言-动作（VLA）模型代表了具身智能领域的范式转变，然而现有框架往往面临空间感知不精确、多模态融合效果欠佳以及强化学习不稳定性等挑战。为了弥补这些缺陷，我们提出了 OmniVLA-RL，这是一种利用混合 Transformer（MoT）设计来协同整合推理、空间和动作专家的创新架构。此外，我们引入了 Flow-GSPO，它将流匹配重新表述为随机微分方程（SDE）过程，并将其与分组分段策略优化（GSPO）相结合，以增强动作精度和训练鲁棒性。在 LIBERO 和 LIBERO-Plus 基准测试上的广泛评估表明，OmniVLA-RL 取得了出色的整体性能，并超越了现有的主流方法，有效克服了当前 VLA 模型的根本局限性。

## Abstract
Visual-Language-Action (VLA) models represent a paradigm shift in embodied AI, yet existing frameworks often struggle with imprecise spatial perception, suboptimal multimodal fusion, and instability in reinforcement learning. To bridge these gaps, we propose OmniVLA-RL, a novel architecture that leverages a Mix-of-Transformers (MoT) design to synergistically integrate reasoning, spatial, and action experts. Furthermore, we introduce Flow-GSPO, which reformulates flow matching as a Stochastic Differential Equation (SDE) process and integrates it with Group Segmented Policy Optimization (GSPO) to enhance action precision and training robustness. Extensive evaluations on the LIBERO and LIBERO-Plus benchmarks demonstrate that OmniVLA-RL achieves decent overall performance and surpasses mainstream existing methods, effectively overcoming the fundamental limitations of current VLA models.

---

## 论文详细总结（自动生成）

以下是对论文《OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型在具身智能领域虽有潜力，但存在三大核心局限：
*   **空间感知不精确**：通用视觉语言模型（VLM）难以准确理解物体的3D位置和尺寸，影响了抓取和避障的精度。
*   **多模态融合效率低**：现有的早期或晚期融合方案仅在编码器或动作头层面修改，未能实现语义、空间和动作特征在模型核心层（Transformer）的深度交互。
*   **强化学习（RL）训练不稳定**：传统的PPO算法过于复杂，而新兴的GRPO在处理动作序列时容易出现训练崩溃或Token级别的偏差。

**研究动机**：提出一种统一的架构，既能增强空间理解能力，又能通过稳定的在线强化学习提升动作生成的泛化性和成功率。

### 2. 方法论：核心思想与关键技术
论文提出了 **OmniVLA-RL** 框架，其核心由两部分组成：

*   **混合专家 Transformer (MoT) 架构**：
    *   **推理专家 (Reasoning Expert)**：利用预训练 VLM（如 PaLiGemma）提取高层语义和指令信息。
    *   **空间专家 (Spatial Expert)**：引入 VGGT 编码器提取细粒度 3D 结构特征，并通过辅助监督头（重建点云、相机参数等）强化几何感知。
    *   **动作专家 (Action Expert)**：基于条件流匹配（Conditional Flow Matching, CFM）生成高保真动作轨迹。
    *   **块状因果注意力 (Block-wise Causal Attention)**：将空间和语义 Token 设为“全可见前缀”，而动作 Token 设为“因果后缀”。这确保了感知阶段能充分融合信息，同时防止动作生成的随机噪声干扰环境理解。

*   **Flow-GSPO 算法**：
    *   **随机流匹配**：将确定性的常微分方程（ODE）去噪过程重构为随机微分方程（SDE），为 RL 探索引入必要的随机性。
    *   **分组分段策略优化 (GSPO)**：以“动作块（Action Block）”为单位进行序列级优化，计算组内相对优势（Advantage），避免了 Token 级别优化带来的不稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估空间推理、物体操作和长程决策。
    *   **LIBERO-Plus**：更具挑战性的扩展版，包含多阶段组合任务（如开抽屉并取出特定物体）。
*   **对比方法**：
    *   主流 VLA 模型：Diffusion Policy, Octo, OpenVLA, SpatialVLA, CoT-VLA, $\pi_0$, $\pi_{0.5}$, F1 等。
    *   RL 基准：PPO 和 GRPO。
*   **训练流程**：分为空间感知预训练（Stage I）、动作生成预训练（Stage II）和在线 RL 微调（Stage III）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号（如 A100/H100）、GPU 数量以及总训练时长。
*   **超参数提及**：RL 阶段进行了 200 步更新，组大小 $G=8$，动作视野 $H=16$，去噪步数 $K=10$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的四个任务套件上进行了全面的性能排名对比。
    *   在 LIBERO-Plus 上进行了 RL 收敛速度和最终成功率的对比实验。
    *   针对核心组件（RL 算法类型、是否有空间专家）进行了消融实验。
*   **充分性评价**：实验设计较为充分，不仅对比了 SOTA 模型，还通过 LIBERO-Plus 验证了模型在复杂长程任务中的鲁棒性。消融实验清晰地展示了空间专家对性能的贡献（性能提升约 8.3%）。

### 6. 主要结论与发现
*   **性能领先**：OmniVLA-RL 在 LIBERO 基准上达到了 **97.6%** 的平均成功率，超越了所有对比模型。
*   **RL 稳定性**：Flow-GSPO 在收敛速度和最终性能上显著优于 PPO 和 GRPO，证明了动作块级别优化更适合连续动作轨迹。
*   **空间感知的关键性**：空间专家的引入显著提升了模型在处理小物体和复杂遮挡时的精度。

### 7. 优点（亮点）
*   **深度融合**：通过 MoT 架构让空间、语义和动作在 Transformer 内部深度耦合，而非简单的特征拼接。
*   **算法创新**：成功将流匹配（Flow Matching）与 GSPO 结合，解决了生成式模型在强化学习中难以兼顾随机性与稳定性的难题。
*   **注意力机制优化**：Block-wise Causal Attention 有效隔离了感知与生成的干扰，保证了环境表征的纯净。

### 8. 不足与局限
*   **缺乏真机验证**：所有实验均在仿真环境（Simulation）中完成，未探讨“从仿真到现实（Sim-to-Real）”的迁移挑战。
*   **长程规划瓶颈**：虽然在长程任务表现优异，但模型仍缺乏一个专门的“世界模型”来预测环境演变，限制了其在极高复杂度任务中的推理能力。
*   **硬件细节缺失**：未披露训练所需的具体算力资源，不便于其他研究者评估复现成本。

（完）
