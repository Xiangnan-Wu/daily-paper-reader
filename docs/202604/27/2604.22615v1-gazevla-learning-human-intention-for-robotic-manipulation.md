---
title: "GazeVLA: Learning Human Intention for Robotic Manipulation"
title_zh: "GazeVLA: 学习人类意图以实现机器人操控"
authors: "Chengyang Li, Kaiyi Xiong, Yuan Xu, Lei Qian, Yizhou Wang, Wentao Zhu"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22615v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人操作和人类意图转移的GazeVLA框架
tldr: 针对具身智能模型对大规模机器人演示数据的依赖问题，本文提出 GazeVLA 框架。该方法将人类注视（Gaze）作为意图的代理，通过在大规模第一视角人类数据集上预训练来捕捉意图与动作的协同关系，并利用思维链推理在推理时先预测意图再执行动作。实验证明该方法在长程、精细任务及少样本场景下均达到 SOTA 水平，有效弥合了人机具身鸿沟。
selection_source: fresh_fetch
motivation: 旨在通过引入人类意图（以注视为表征）来弥合人类与机器人之间的具身鸿沟，降低对大规模机器人数据的依赖。
method: 在大规模人类数据集上预训练以捕捉意图与动作的协同，并采用思维链推理范式，在执行动作前显式预测注视点。
result: 在仿真和真实世界的长程、精细操作任务及少样本基准测试中，该方法均显著优于现有基线并达到 SOTA 性能。
conclusion: 显式建模并转移人类意图是提升机器人操控泛化能力、鲁棒性及学习效率的有效途径。
---

## 摘要
具身基础模型在机器人操控领域取得了重大突破，但它们仍然严重依赖大规模的机器人演示数据。尽管最近的研究探索了利用人类数据来减轻这种依赖，但由于人类与机器人之间固有的具身鸿沟（embodiment gap），如何有效地提取可迁移的知识仍然是一个重大挑战。我们认为，人类行为背后的意图可以作为弥合这一鸿沟的强大中间表示。在本文中，我们介绍了一种新型框架，该框架显式地学习并迁移人类意图，以促进机器人操控。具体而言，我们通过注视（gaze）来建模意图，因为注视自然地先于物理动作，并可作为人类意图的可观测代理。我们的模型首先在大规模第一视角（egocentric）人类数据集上进行预训练，以捕捉人类意图及其与动作的协同作用，随后在少量的机器人和人类数据上进行微调。在推理过程中，该模型采用思维链（Chain-of-Thought）推理范式，在执行动作之前按顺序预测意图。在仿真和真实世界环境、长程（long-horizon）和精细任务，以及少样本（few-shot）和鲁棒性基准测试下的广泛评估表明，我们的方法始终优于强基准模型，具有更好的泛化能力，并达到了当前最先进的性能。

## Abstract
Embodied foundation models have achieved significant breakthroughs in robotic manipulation, yet they still depend heavily on large-scale robot demonstrations. Although recent works have explored leveraging human data to alleviate this dependency, effectively extracting transferable knowledge remains a significant challenge due to the inherent embodiment gap between human and robot. We argue that the intention underlying human actions can serve as a powerful intermediate representation for bridging this gap. In this paper, we introduce a novel framework that explicitly learns and transfers human intention to facilitate robotic manipulation. Specifically, we model intention through gaze, as it naturally precedes physical actions and serves as an observable proxy for human intent. Our model is first pretrained on a large-scale egocentric human dataset to capture human intention and its synergy with action, followed by finetuning on a small set of robot and human data. During inference, the model adopts a Chain-of-Thought reasoning paradigm, sequentially predicting intention before executing the action. Extensive evaluations in simulation and real-world settings, across long-horizon and fine-grained tasks, and under few-shot and robustness benchmarks, show that our method consistently outperforms strong baselines, generalizes better, and achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《GazeVLA: Learning Human Intention for Robotic Manipulation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前具身智能（Embodied AI）模型的发展受限于**机器人演示数据的稀缺性与高昂采集成本**。虽然利用大规模人类视频数据是一种潜在的解决方案，但由于人类与机器人之间存在显著的**具身鸿沟（Embodiment Gap）**（如肢体结构、动作空间不同），直接迁移知识非常困难。
本文的核心动机在于：**人类意图（Intention）**是跨越具身鸿沟的桥梁。作者提出将**人类注视（Gaze）**作为意图的显式代理（Proxy），因为注视通常先于动作发生并聚焦于任务关键区域。通过学习“为什么做”而非仅仅是“做什么”，可以实现更高效的跨具身知识迁移。

### 2. 方法论：VLIA 模型
*   **核心思想**：构建一个“视觉-语言-意图-动作”（VLIA）模型，采用**思维链（CoT）推理范式**，即在生成动作指令前，先显式地预测人类/机器人的意图（注视点坐标）。
*   **关键技术细节**：
    *   **架构设计**：采用 **PaliGemma** 作为骨干网络（包含 SigLIP 视觉编码器和 Gemma-2B 语言模型）。动作生成部分由一个基于**条件流匹配（Conditional Flow Matching）**的 Action Expert 负责。
    *   **意图建模**：将注视点（Gaze）离散化为 2D 图像坐标 Token。模型首先通过自回归方式预测意图 Token，随后将其作为 KV Cache 调节 Action Expert 生成连续动作序列。
    *   **训练流程**：
        1.  **预训练**：在包含 1.5 亿帧的第一视角人类视频数据集上训练，学习意图与动作的协同关系。
        2.  **后训练（微调）**：在少量机器人数据和人类演示数据上联合训练。即使机器人数据没有注视标注，模型也能通过人类数据的先验知识迁移意图预测能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：整合了 Ego4D、Ego-Exo4D 等 13 个大型第一视角人类数据集。
    *   **仿真实验**：使用 **AV-ALOHA** 基准测试，包含 6 个复杂任务（如穿针、插槽等），并设置了分布外（OOD）的光照和干扰物测试。
    *   **真实世界**：在 **ALOHA 双臂机器人**和 **Unitree G1 人形机器人**（配备灵巧手）上进行测试。
*   **对比方法**：包括 LFA（基于 ACT）、Diffusion Policy (DP)、H-RDT（人类增强模型）以及最新的大规模预训练模型 **$\pi_{0.5}$**。

### 4. 资源与算力
*   **算力消耗**：论文明确指出，预训练阶段使用了 **8 张 NVIDIA A800 GPU**。
*   **训练时长**：总计消耗了 **1,344 GPU 小时**（20k 训练步数，Batch Size 为 2048）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真环境进行了 6 类任务的 ID（分布内）和 OOD（分布外）评估，每种方法测试 100 次。
    *   真实世界涵盖了夹爪操控和灵巧手操控（如键盘打字、拧螺丝），每项任务进行 20 次测试。
    *   **消融实验**：针对 CoT 推理链的有效性、人类数据的作用、预训练与微调的贡献做了详细对比。
*   **客观性评价**：实验设计较为充分，涵盖了从仿真到现实、从简单夹爪到复杂灵巧手的多维度验证，且对比了当前最强的基座模型（如 $\pi_{0.5}$），结果具有较高的说服力。

### 6. 主要结论与发现
*   **意图引导的有效性**：显式预测注视点能显著提升动作的精确度，尤其是在精细操作（如穿针）中。
*   **跨具身迁移成功**：模型能够将在人类数据上学到的意图理解能力成功迁移到机器人硬件上，即使机器人训练数据中缺失意图标注。
*   **泛化能力增强**：相比于直接模仿动作，基于意图的 CoT 范式在面对新物体、新背景和光照变化时表现出更强的鲁棒性，OOD 性能提升显著（相对提升约 22%）。

### 7. 优点与亮点
*   **新颖的视角**：将注视点作为意图的显式表征，为解决具身鸿沟提供了清晰的中间层。
*   **推理范式创新**：引入意图-动作思维链，使 VLA 模型具备了初步的“思考”过程，增强了逻辑一致性。
*   **强大的泛化性**：通过大规模人类视频预训练，模型在少样本（Few-shot）场景下表现优异，降低了对机器人数据的依赖。

### 8. 不足与局限
*   **预训练数据偏差**：人类注视点存在强烈的“中心偏差”，虽然文中采用了数据增强缓解，但在某些极端视角下可能仍存在预测偏差。
*   **动作空间对齐**：目前尚未在预训练阶段显式地将人类动作空间与机器人动作空间进行潜空间对齐，这可能限制了底层技能的直接迁移。
*   **实时性挑战**：自回归预测意图 Token 增加了推理延迟，对于极高频的实时控制可能存在压力。

（完）
