---
title: "ScenarioControl: Vision-Language Controllable Vectorized Latent Scenario Generation"
title_zh: ScenarioControl：视觉-语言可控的矢量化潜空间场景生成
authors: "Lili Gao, Yanbo Xu, William Koch, Samuele Ruffino, Luke Rowe, Behdad Chalaki, Dmitriy Rivkin, Julian Ost, Roger Girgis, Mario Bijelic, Felix Heide"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17147v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 用于自主智能体的矢量化潜空间视觉语言可控场景生成
tldr: ScenarioControl是首个支持视觉-语言控制的矢量化驾驶场景生成框架。它能根据文本或图像提示，在矢量化潜空间中合成包含地图、动态交通参与者及传感器观测的真实3D场景。通过创新的跨全局控制机制，该方法实现了对道路布局和交通状况的精细控制，并支持长时程、时间一致的场景生成，为自动驾驶仿真提供了高质量的数据支持。
selection_source: fresh_fetch
motivation: 现有的驾驶场景生成方法缺乏灵活的多模态控制手段，难以根据具体的文本或图像指令生成精确且真实的矢量化场景。
method: 提出一种在矢量化潜空间中联合表示道路与智能体的生成模型，并结合跨注意力机制与轻量级全局上下文分支实现多模态控制。
result: 实验表明，该方法在控制遵循度、生成保真度以及长时程一致性方面均优于现有技术，并发布了带文本标注的矢量化地图数据集。
conclusion: ScenarioControl通过多模态控制显著提升了驾驶场景生成的灵活性与真实性，为自动驾驶系统的开发与评估提供了强有力的工具。
---

## 摘要
我们介绍了 ScenarioControl，这是首个用于学习型驾驶场景生成的视觉-语言控制机制。给定文本提示或输入图像，ScenarioControl 可以合成多样且逼真的 3D 场景演变——包括地图、随时间变化的交互式参与者 3D 框、行人、驾驶基础设施以及自车摄像头观测。该方法在矢量化潜空间中生成场景，该空间共同表示道路结构和动态智能体。为了将多模态控制与稀疏的矢量化场景元素联系起来，我们提出了一种跨全局控制机制，该机制将交叉注意力与轻量级全局上下文分支相结合，在保持真实感的同时，实现了对道路布局和交通状况的细粒度控制。该方法从场景中不同参与者的视角生成时间一致的场景演变，支持驾驶场景的长时程延续。为了便于训练和评估，我们发布了一个包含与矢量化地图结构对齐的文本标注的数据集。大量实验验证了 ScenarioControl 的控制遵循度和保真度在所有实验中均优于所有测试方法。项目网页：https://light.princeton.edu/ScenarioControl

## Abstract
We introduce ScenarioControl, the first vision-language control mechanism for learned driving scenario generation. Given a text prompt or an input image, Scenario-Control synthesizes diverse, realistic 3D scenario rollouts - including map, 3D boxes of reactive actors over time, pedestrians, driving infrastructure, and ego camera observations. The method generates scenes in a vectorized latent space that represents road structure and dynamic agents jointly. To connect multimodal control with sparse vectorized scene elements, we propose a cross-global control mechanism that integrates crossattention with a lightweight global-context branch, enabling fine-grained control over road layout and traffic conditions while preserving realism. The method produces temporally consistent scenario rollouts from the perspectives different actors in the scene, supporting long-horizon continuation of driving scenarios. To facilitate training and evaluation, we release a dataset with text annotations aligned to vectorized map structures. Extensive experiments validate that the control adherence and fidelity of ScenarioControl compare favorable to all tested methods across all experiments. Project webpage: https://light.princeton.edu/ScenarioControl

---

## 论文详细总结（自动生成）

这是一份关于论文《ScenarioControl: Vision-Language Controllable Vectorized Latent Scenario Generation》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：自动驾驶系统的评估需要大量罕见且关键的安全场景（如逆行、复杂路口冲突），但现实采集的数据中这类场景极少。
*   **核心问题**：现有的模拟器要么是基于规则的（缺乏真实感和多样性），要么是生成式的但缺乏直观的控制手段（通常依赖已有的布局，无法通过自然语言或单张图像直接定义新场景）。
*   **整体含义**：本文提出了 **ScenarioControl**，这是首个支持**视觉-语言（Vision-Language）控制**的矢量化潜空间驾驶场景生成框架。它允许用户通过一段话或一张行车记录仪照片，生成包含地图拓扑、动态障碍物及其后续行为演变的完整3D场景。

### 2. 论文提出的方法论
*   **核心思想**：在矢量化潜空间中联合建模道路结构和动态智能体，并通过一种创新的“跨全局控制机制”将密集的多模态信号（文本/图像）注入到稀疏的矢量化元素中。
*   **关键技术细节**：
    *   **矢量化潜空间扩散模型（LDM）**：使用Transformer架构的自动编码器将场景元素（车道线、智能体状态）编码为潜变量，并在其上进行扩散生成。特别加入了高度（$z$）和物体高度（$h$）信息，以支持精确的传感器仿真。
    *   **跨全局控制机制（Cross-Global Control）**：包含两个并行分支：
        1.  **交叉注意力分支**：用于捕捉细粒度的控制（如特定位置的车辆）。
        2.  **轻量级全局上下文分支**：通过一组学习到的潜代币（Latent Tokens）聚合全局意图，提高生成全局一致性。
    *   **多模态调节**：
        *   **文本**：通过预训练文本编码器提取特征。
        *   **图像**：结合视觉骨干网络特征和深度估计图，通过位置编码增强空间对齐。
    *   **碰撞惩罚损失（Collision Penalty）**：在训练中加入正则项，减少生成场景中智能体之间的重叠。
    *   **视频生成管线**：将生成的矢量化场景投影回相机空间，利用LoRA微调的视频扩散模型（如Wan 2.2）生成照片级真实的视频。

### 3. 实验设计
*   **数据集**：
    *   **nuPlan**：主要训练和评估集。作者利用GPT-4o-mini为50万个场景生成了高质量的文本描述，构建了首个大规模视觉-语言矢量化地图数据集。
    *   **Waymo Open Motion Dataset**：用于验证模型的跨数据集泛化能力。
*   **Benchmark与对比方法**：
    *   对比了 **Scenario Dreamer**（无条件/布局条件生成）和 **TrafficGen**（仅智能体放置）。
    *   在控制机制上，对比了拼接（Concatenation）、全交叉注意力、门控注意力、线性注意力等多种主流注意力变体。
*   **评估指标**：智能体准确率（AP）、碰撞率、控制遵循度（CCS、SPG、CSC，用于衡量生成结果与控制信号的相关性和敏感度）。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的GPU型号、数量及总训练时长。
*   **模型规模**：提到了视频生成部分使用了50亿参数（5B）的Wan 2.2模型进行LoRA适配。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了50万组带标注的数据进行训练，规模庞大。
    *   进行了详尽的消融实验（表3），对比了9种不同的注意力机制，验证了“跨全局控制”的优越性。
    *   进行了跨数据集（nuPlan到Waymo）的泛化实验。
*   **充分性与公平性**：实验涵盖了从全局布局到局部智能体精度的多维度评估，对比基准选择合理，且通过定量指标（如AP和碰撞率）客观地展示了模型在控制力与真实性之间的平衡。

### 6. 论文的主要结论与发现
*   **控制有效性**：ScenarioControl在文本和图像条件下的控制遵循度显著优于现有方法，能够准确生成符合描述的复杂路口和交通状况。
*   **泛化能力**：模型展现了极强的跨数据集迁移能力，在未见过的Waymo数据集上表现优异。
*   **一致性**：通过矢量化表示作为中介，模型能够生成在不同视角下保持空间一致性的长时程视频演变。
*   **碰撞抑制**：引入碰撞惩罚损失和智能体数量预测分支（$f_{count}$）能显著提升生成场景的物理合理性。

### 7. 优点
*   **首创性**：填补了矢量化场景生成中缺乏自然语言和图像直观控制的空白。
*   **多模态融合**：提出的跨全局控制机制有效解决了密集特征（图像/文本）与稀疏矢量（点/线）之间的对齐难题。
*   **实用性**：生成的场景直接兼容现有的自动驾驶仿真堆栈，且支持多视角传感器仿真。
*   **资源贡献**：发布了包含50万条文本标注的大规模数据集，对社区有重要价值。

### 8. 不足与局限
*   **遮挡处理**：在单张图像条件化生成时，对于图像遮挡区域或视野外区域的补全仍存在一定的随机性，虽然合理但未必完全符合真实物理世界的所有细节。
*   **计算开销**：虽然矢量化生成很快，但后续的照片级视频渲染（5B模型）可能需要较高的计算资源，难以实现超实时的交互式编辑。
*   **复杂交互建模**：虽然支持长时程演变，但在极端复杂的交通参与者博弈（如多车强行加塞）中的行为真实性仍有提升空间。

（完）
