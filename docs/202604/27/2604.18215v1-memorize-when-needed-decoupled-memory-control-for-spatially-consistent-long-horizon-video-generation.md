---
title: "Memorize When Needed: Decoupled Memory Control for Spatially Consistent Long-Horizon Video Generation"
title_zh: 按需记忆：用于空间一致长时视频生成的解耦记忆控制
authors: "Yanjun Guo, Zhengqiang Zhang, Pengfei Wang, Xinyue Liang, Zhiyuan Ma, Lei Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18215v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 空间一致的长程视频生成
tldr: 本研究针对长程视频生成中的空间一致性问题，提出了一种解耦内存控制框架。传统方法将内存建模与生成过程耦合，导致场景重访时内容不一致且削弱了新场景的生成能力。该方法通过独立的轻量级内存分支学习历史空间信息，利用混合内存表示和相机感知门控机制，仅在需要时引入记忆。该方案不仅显著降低了训练成本，还在保持生成质量的同时，实现了卓越的空间一致性和新场景探索能力。
selection_source: fresh_fetch
motivation: 现有长程视频生成方法将内存建模与生成耦合，导致场景重访时不一致且限制了对新区域的生成能力。
method: 提出解耦框架，利用轻量级内存分支、混合内存表示及相机感知门控机制，实现按需进行空间一致性调节。
result: 实验表明，该方法在保持极高数据效率的同时，在视觉质量和空间一致性方面均达到了当前最先进水平。
conclusion: 解耦内存控制是实现长程视频生成中空间一致性与生成灵活性平衡的有效途径。
---

## 摘要
空间一致的长时视频生成旨在沿预定义的摄像机轨迹保持时间与空间的一致性。现有方法大多将记忆建模与视频生成耦合在一起，导致在场景重访时内容不一致，且在探索新区域时生成能力下降，即使在大量标注数据上进行训练也是如此。为了解决这些局限性，我们提出了一个将记忆条件与生成分离的解耦框架。我们的方法在显著降低训练成本的同时，增强了空间一致性，并保留了探索新场景的生成能力。具体而言，我们采用了一个轻量级且独立的记忆分支，从历史观测中学习精确的空间一致性。我们首先引入了一种混合记忆表示，以从生成的帧中捕捉互补的时间和空间线索，然后利用逐帧交叉注意力机制，确保每一帧仅以空间相关性最强的历史信息为条件，并将其注入生成模型以确保空间一致性。在生成新场景时，提出了一种摄像机感知的门控机制来调节记忆模块与生成模块之间的交互，仅在存在有意义的历史参考时才启用记忆条件。与现有方法相比，我们的方法具有极高的数据效率，实验表明，我们的方法在视觉质量和空间一致性方面均达到了最先进的性能。

## Abstract
Spatially consistent long-horizon video generation aims to maintain temporal and spatial consistency along predefined camera trajectories. Existing methods mostly entangle memory modeling with video generation, leading to inconsistent content during scene revisits and diminished generative capacity when exploring novel regions, even trained on extensive annotated data. To address these limitations, we propose a decoupled framework that separates memory conditioning from generation. Our approach significantly reduces training costs while simultaneously enhancing spatial consistency and preserving the generative capacity for novel scene exploration. Specifically, we employ a lightweight, independent memory branch to learn precise spatial consistency from historical observation. We first introduce a hybrid memory representation to capture complementary temporal and spatial cues from generated frames, then leverage a per-frame cross-attention mechanism to ensure each frame is conditioned exclusively on the most spatially relevant historical information, which is injected into the generative model to ensure spatial consistency. When generating new scenes, a camera-aware gating mechanism is proposed to mediate the interaction between memory and generation modules, enabling memory conditioning only when meaningful historical references exist. Compared with the existing method, our method is highly data-efficient, yet the experiments demonstrate that our approach achieves state-of-the-art performance in terms of both visual quality and spatial consistency.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **"Memorize When Needed" (MWN)** 的解耦记忆控制框架，旨在解决长程视频生成中的空间一致性与生成质量之间的矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在长程视频生成（尤其是涉及摄像机往返、场景重访时）中，如何既能保持与历史场景的空间一致性，又不损害模型在探索新区域时的生成能力（视觉质量和多样性）。
*   **研究动机**：现有方法通常将“记忆建模”与“视频生成”耦合在同一个网络中。这导致了两个痛点：
    1.  **性能权衡**：为了维持一致性，模型往往会牺牲生成质量，导致结构畸变。
    2.  **效率低下**：在生成全新场景时，强制引入不相关的历史记忆会干扰生成，且全量注意力计算开销巨大。
    3.  **数据依赖**：耦合架构通常需要海量带标注的 3D 数据进行微调。

### 2. 方法论
MWN 的核心思想是**“解耦”**与**“按需注入”**：
*   **解耦架构**：冻结预训练的视频生成主干网络（如 Wan2.1 或 CogVideoX），仅添加一个轻量级的**独立记忆分支**。
*   **混合记忆表示 (Hybrid Memory Representation)**：
    *   **时间记忆 ($K_t$)**：通过视频编码器提取连续帧的动态特征，捕捉运动模式。
    *   **空间记忆 ($K_s$)**：提取单帧的精细特征，提供清晰的几何参考。
*   **逐帧交叉注意力 (Per-frame Cross-Attention)**：利用掩码机制，让当前帧的 Latent 仅查询与其空间最相关的历史帧特征，减少计算量并提高对齐精度。
*   **相机感知门控机制 (Camera-aware Gating)**：
    *   通过计算当前相机位姿与历史位姿的视场角（FOV）重叠度和距离，生成相关性得分。
    *   **逻辑**：当得分高于阈值（重访旧场景）时，开启门控注入记忆；当得分低（探索新场景）时，关闭记忆分支，完全依赖主干网络的生成先验。
*   **训练策略**：采用“伪循环（Pseudo-Loops）”数据增强和“历史丢弃（History Dropout）”策略，使模型在没有昂贵 3D 标注的情况下也能学习鲁棒的记忆控制。

### 3. 实验设计
*   **数据集**：主要使用 **RealEstate10K**（室内外真实场景）。
*   **测试场景**：设计了四种挑战性轨迹：360°全景旋转、重复重访、随机循环插入、带偏移的返回。
*   **Benchmark 与对比方法**：
    *   **无显式记忆方法**：AC3D, DFoT, SEVA。
    *   **带记忆方法**：VMem, WorldPlay (及其蒸馏版 WorldPlay-d)。
*   **评价指标**：
    *   **一致性**：PSNR, SSIM, LPIPS。
    *   **视频质量**：美学质量 (AQ), 图像质量 (IQ), 运动平滑度 (MS), 动态程度 (DD)。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A800 GPU**。
*   **训练细节**：训练迭代次数为 **10K iterations**，有效 Batch Size 为 16。
*   **效率优势**：
    *   **参数量**：记忆分支仅占主干网络参数的约 2.2%（以 CogVideoX 为例，约 128M）。
    *   **计算量**：推理时的记忆 TFLOPs 比 WorldPlay 低 1000 倍以上。
    *   **数据量**：仅需 14K 样本即可达到 SOTA，而对比方法需 320K 样本。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了定量对比、定性视觉对比、分布外（OOD）泛化性测试以及详尽的消融实验。
*   **充分性评价**：实验设计非常充分。不仅在标准数据集上测试，还专门针对“重访”和“探索”两个维度分别评估，并引入了 55 个野外（In-the-wild）图像进行泛化性验证。消融实验验证了混合记忆表示和门控机制的必要性，逻辑严密且客观。

### 6. 主要结论与发现
*   **解耦是有效的**：通过冻结主干网络，MWN 成功保留了强大的生成先验，避免了长程生成中的内容崩溃。
*   **按需记忆是关键**：门控机制解决了“过度依赖记忆”导致的伪影问题，使模型在探索新区域时表现更自然。
*   **高效率**：MWN 在极低的数据和算力需求下，在空间一致性指标（PSNR/SSIM）上大幅领先现有 SOTA 方法（如 PSNR 提升了约 5dB）。

### 7. 优点
*   **极高的数据效率**：无需复杂的 3D 重建或海量标注数据，利用通用视频即可训练。
*   **即插即用潜力**：解耦设计使其理论上可以适配不同的预训练视频生成模型。
*   **平衡性极佳**：在保持长程一致性的同时，视频的单帧质量和动态感未受明显损害。

### 8. 不足与局限
*   **几何依赖性**：记忆检索依赖于 FOV 计算，在存在复杂遮挡（Occlusions）的场景下，几何规则可能失效，导致一致性下降。
*   **相机控制精度**：长程生成的质量高度依赖于外部相机控制模块的精度，若位姿估计有误，记忆注入可能会产生错位。
*   **360° 挑战**：尽管表现优于他法，但在极端的 360° 旋转回起始点时，所有模型仍存在一定的性能退化。

（完）
