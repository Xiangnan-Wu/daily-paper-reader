---
title: "SYMBOLIZER: Symbolic Model-free Task Planning with VLMs"
title_zh: SYMBOLIZER：基于 VLM 的无符号模型任务规划
authors: "Sami Azirar, Zlatan Ajanovic, Hermann Blum"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17830v1"
tags: ["query:robot"]
score: 9.0
evidence: 将VLM与经典规划结合用于机器人任务规划
tldr: 针对传统任务与运动规划（TAMP）中符号模型难以构建的问题，本文提出 SYMBOLIZER 框架。该方法结合了视觉语言模型（VLM）的零样本视觉理解能力与经典规划的启发式搜索。通过 VLM 将图像转换为基于谓词的符号状态，并利用领域无关的启发式搜索进行长程推理，无需预定义的动作模型。实验表明，该方法在多个基准测试中达到了 SOTA 性能，显著提升了规划的泛化性与效率。
selection_source: fresh_fetch
motivation: 传统 TAMP 依赖难以手工构建的符号模型，而现有 VLM 虽具备视觉理解力但规划能力有限，且现有集成方案缺乏通用性。
method: 利用 VLM 将图像映射为基于提升谓词的符号状态，并采用目标计数和基于宽度的领域无关启发式搜索进行无模型任务规划。
result: 在 ProDG 和 ViPlan 基准测试中取得 SOTA 结果，性能优于直接使用 VLM 规划，并与使用 VLM 启发式的方法相当。
conclusion: 结合 VLM 的状态感知与领域无关的符号搜索，可以有效解决具有大规模组合状态空间的跨领域长程规划问题。
---

## 摘要
传统的任务与运动规划（TAMP）系统依赖于用于运动规划的物理模型和用于任务规划的离散符号模型。尽管物理模型通常是现成的，但符号模型（由符号状态解释和动作模型组成）必须经过精心的人工设计或从标注数据中学习。这一过程既耗费资源，又将解决方案限制在特定领域，从而限制了可扩展性和适应性。另一方面，视觉语言模型（VLM）表现出理想的零样本视觉理解能力（归功于其在异构数据上的广泛训练），但其规划能力仍然有限。因此，将 VLM 与经典规划相结合，用于 TAMP 问题中的长程推理，具有巨大的潜力。该方向近期的工作仍缺乏通用性，且依赖于手工设计的特定任务解决方案，例如预先描述所有可能的物体，或使用符号动作模型。我们提出了一个能够很好地泛化到未见问题实例的框架。该方法仅需要描述物体间关系的提升谓词（lifted predicates），并利用 VLM 将其从图像中锚定（ground），从而获取符号状态。规划是通过使用目标计数（goal-count）和基于宽度（width-based）启发式的领域无关启发式搜索完成的，无需动作模型。在 VLM 锚定的状态空间上进行的符号搜索优于直接基于 VLM 的规划，且与使用 VLM 衍生启发式的方法表现相当。这表明领域无关搜索可以有效解决具有大规模组合状态空间的跨领域问题。我们对该方法进行了广泛评估，并在 ProDG 和 ViPlan 基准测试中取得了最先进的结果。

## Abstract
Traditional Task and Motion Planning (TAMP) systems depend on physics models for motion planning and discrete symbolic models for task planning. Although physics model are often available, symbolic models (consisting of symbolic state interpretation and action models) must be meticulously handcrafted or learned from labeled data. This process is both resource-intensive and constrains the solution to the specific domain, limiting scalability and adaptability. On the other hand, Visual Language Models (VLMs) show desirable zero-shot visual understanding (due to their extensive training on heterogeneous data), but still achieve limited planning capabilities. Therefore, integrating VLMs with classical planning for long-horizon reasoning in TAMP problems offers high potential. Recent works in this direction still lack generality and depend on handcrafted, task-specific solutions, e.g. describing all possible objects in advance, or using symbolic action models. We propose a framework that generalizes well to unseen problem instances. The method requires only lifted predicates describing relations among objects and uses VLMs to ground them from images to obtain the symbolic state. Planning is performed with domain-independent heuristic search using goal-count and width-based heuristics, without need for action models. Symbolic search over VLM-grounded state-space outperforms direct VLM-based planning and performs on par with approaches that use a VLM-derived heuristic. This shows that domain-independent search can effectively solve problems across domains with large combinatorial state spaces. We extensively evaluate on extensively evaluate our method and achieve state-of-the-art results on the ProDG and ViPlan benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《SYMBOLIZER: Symbolic Model-free Task Planning with VLMs》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：传统的任务与运动规划（TAMP）依赖于手工设计的符号模型（如 PDDL 动作定义和谓词），这限制了系统在非结构化环境中的泛化能力。
*   **核心挑战**：虽然视觉语言模型（VLM）具备强大的零样本视觉理解能力，但在处理长程（Long-horizon）推理和复杂组合逻辑时容易产生幻觉且规划能力不足。
*   **研究目标**：提出一个无需手工编写符号动作模型、仅依赖 VLM 进行状态感知、并结合经典启发式搜索进行规划的通用框架。

### 2. 方法论
*   **核心思想**：将 VLM 定位为“符号传感器”，将原始观测（图像或文本）映射为结构化的符号状态，随后利用物理模拟器作为黑盒转移模型，通过领域无关的启发式搜索寻找最优动作序列。
*   **关键技术细节**：
    *   **结构化输出约束**：为了避免 VLM 产生非法符号或命名不一致，SYMBOLIZER 将领域谓词定义为 Python 类。通过强制 VLM 生成符合特定 Schema 的 JSON 对象，确保了状态表示的类型安全和逻辑严密性。
    *   **符号锚定（Grounding）**：分为物体提取、谓词识别和目标解析三个阶段。利用上下文学习（In-context Learning）引导 VLM 识别物体间的空间和逻辑关系。
    *   **无模型规划**：不要求预定义的 PDDL 动作效果。规划器在模拟器中进行搜索，利用“目标计数（Goal-count）”作为主启发式函数，并引入“宽度搜索（Width-based Search）”作为打破僵局的机制，以鼓励探索新状态。
    *   **黑盒转移**：利用模拟器执行动作并获取下一状态的观测，再由 VLM 重新锚定，形成闭环。

### 3. 实验设计
*   **实验场景**：
    *   **自定义领域**：Blocksworld（积木世界）、Hanoi（汉诺塔）、Kitchen-Worlds（厨房场景）。
    *   **模拟环境**：PDDLGym（2D 图像）、PyBullet（3D 物理渲染）。
    *   **外部基准测试**：**ProDG**（包含真实图像的烹饪和汉诺塔任务）和 **ViPlan**（光照逼真的复杂家居任务）。
*   **对比方法**：
    *   **ViLaIn**：现有的基于 VLM 的 PDDL 生成框架。
    *   **ViPlan**：将 VLM 作为规划器或验证器的基准方法。
    *   **Dream-VL**：结合 VLM 锚定与学习型世界模型的规划方法。
    *   **消融实验**：直接 VLM 规划（无搜索）、VLM 引导搜索（使用 VLM 作为启发式）。

### 4. 资源与算力
*   **模型使用**：实验使用了 Gemini 3.1 Pro、Gemini 3.1 Flash Lite 和 Mistral Small 2503。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法主要依赖于现成的 VLM API 进行推理（零样本或少样本提示），且规划是在模拟器中进行的启发式搜索，因此其核心开销在于 VLM 的 API 调用成本和模拟器的运行时间，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2D 仿真到 3D 渲染再到真实图像的多个维度。在 ProDG 和 ViPlan 等标准基准上进行了详尽测试。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅评估了最终的规划成功率，还深入分析了物体提取、谓词识别和目标解析的 F1 分数。通过对比不同参数规模的模型（Pro vs. Flash vs. Mistral），验证了框架对不同能力水平 VLM 的适配性。

### 6. 主要结论与发现
*   **搜索胜过直接推理**：在 VLM 锚定的状态空间上进行符号搜索，其表现远优于直接让 VLM 输出规划指令。
*   **结构化约束的必要性**：通过 Python 类约束输出，显著减少了语法错误和逻辑幻觉，使得即使是较小的模型（如 Mistral Small）也能在复杂任务中表现出色。
*   **领域无关性**：SYMBOLIZER 在未见过的模拟器和任务中表现出极强的泛化能力，无需针对特定领域进行 Prompt 工程。
*   **性能领先**：在 ProDG 和 ViPlan 基准测试中均达到了 SOTA（最先进）水平，尤其在长程任务中优势明显。

### 7. 优点
*   **通用性强**：无需手工编写复杂的 PDDL 动作模型，降低了 TAMP 系统的应用门槛。
*   **鲁棒性高**：通过将感知（VLM）与推理（搜索）解耦，利用经典算法的完备性弥补了神经网络的随机性。
*   **零样本/少样本友好**：仅需极少量的上下文示例即可迁移到新领域。

### 8. 不足与局限
*   **依赖模拟器**：作为“无模型”规划，它高度依赖物理模拟器作为状态转移的来源，在缺乏精确模拟器的现实场景中可能受限。
*   **目标解析瓶颈**：实验显示，从纯文本指令中准确解析出符号化目标（Goal Grounding）仍是目前的性能瓶颈，尤其是在缺乏视觉上下文的情况下。
*   **静态谓词难题**：对于一些无法通过单张图像观察到的静态属性（如物体的固定属性），VLM 仍存在识别困难。
*   **实时性挑战**：频繁调用 VLM API 进行状态锚定可能会带来较高的延迟，限制了其在高速动态环境中的应用。

（完）
