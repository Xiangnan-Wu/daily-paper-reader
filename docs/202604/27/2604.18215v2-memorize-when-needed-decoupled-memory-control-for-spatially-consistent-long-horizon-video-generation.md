---
title: "Memorize When Needed: Decoupled Memory Control for Spatially Consistent Long-Horizon Video Generation"
title_zh: 按需记忆：用于空间一致长程视频生成的解耦记忆控制
authors: "Yanjun Guo, Zhengqiang Zhang, Pengfei Wang, Xinyue Liang, Zhiyuan Ma, Lei Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18215v2"
tags: ["query:q7"]
score: 9.0
evidence: 空间一致的长程视频生成
tldr: 针对长视频生成中空间一致性差和新场景生成能力受限的问题，本文提出了一种解耦的存储控制框架。该框架将存储调节与生成过程分离，通过轻量级存储分支学习历史观测的空间一致性，并利用混合存储表示和相机感知门控机制，在需要时引入历史参考。该方法不仅显著降低了训练成本，还在保持生成能力的同时实现了卓越的空间一致性和视觉质量。
selection_source: fresh_fetch
motivation: 现有方法将存储建模与视频生成耦合，导致场景重访时内容不一致且在探索新区域时生成能力下降。
method: 提出一种解耦框架，利用轻量级存储分支、混合存储表示及相机感知门控机制，实现按需引入空间相关的历史信息。
result: 实验表明，该方法在数据效率极高的情况下，在视觉质量和空间一致性方面均达到了当前最先进的水平。
conclusion: 通过解耦存储与生成，可以有效解决长视频生成中的空间一致性难题，并提升模型对新场景的探索能力。
---

## 摘要
空间一致的长程视频生成旨在沿着预定义的相机轨迹保持时间与空间的一致性。现有方法大多将记忆建模与视频生成耦合在一起，导致在场景重访时内容不一致，且在探索新区域时生成能力下降，即使在大量标注数据上进行训练也是如此。为了解决这些局限性，我们提出了一个将记忆条件与生成分离的解耦框架。我们的方法在显著降低训练成本的同时，增强了空间一致性，并保留了探索新场景的生成能力。具体而言，我们采用了一个轻量级的独立记忆分支，从历史观测中学习精确的空间一致性。我们首先引入了一种混合记忆表示，以捕捉生成帧中互补的时间和空间线索，然后利用逐帧交叉注意力机制，确保每一帧仅以空间相关性最强的历史信息为条件，并将其注入生成模型以确保空间一致性。在生成新场景时，我们提出了一种相机感知的门控机制来调节记忆模块与生成模块之间的交互，仅在存在有意义的历史参考时才启用记忆条件。与现有方法相比，我们的方法具有极高的数据效率，实验表明，我们的方法在视觉质量和空间一致性方面均达到了最先进的性能。

## Abstract
Spatially consistent long-horizon video generation aims to maintain temporal and spatial consistency along predefined camera trajectories. Existing methods mostly entangle memory modeling with video generation, leading to inconsistent content during scene revisits and diminished generative capacity when exploring novel regions, even trained on extensive annotated data. To address these limitations, we propose a decoupled framework that separates memory conditioning from generation. Our approach significantly reduces training costs while simultaneously enhancing spatial consistency and preserving the generative capacity for novel scene exploration. Specifically, we employ a lightweight, independent memory branch to learn precise spatial consistency from historical observation. We first introduce a hybrid memory representation to capture complementary temporal and spatial cues from generated frames, then leverage a per-frame cross-attention mechanism to ensure each frame is conditioned exclusively on the most spatially relevant historical information, which is injected into the generative model to ensure spatial consistency. When generating new scenes, a camera-aware gating mechanism is proposed to mediate the interaction between memory and generation modules, enabling memory conditioning only when meaningful historical references exist. Compared with the existing method, our method is highly data-efficient, yet the experiments demonstrate that our approach achieves state-of-the-art performance in terms of both visual quality and spatial consistency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Memorize When Needed: Decoupled Memory Control for Spatially Consistent Long-Horizon Video Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
长程视频生成（Long-horizon Video Generation）的核心挑战在于维持**空间一致性**，尤其是在相机轨迹发生“场景重访”（即回到之前观察过的位置）时。
*   **研究动机**：现有的长视频生成方法通常将“记忆建模”与“视频生成”耦合在一起。这种耦合导致了两个主要问题：
    1.  **生成能力受损**：模型过度依赖历史记忆，导致在探索新区域时无法生成高质量、多样化的内容。
    2.  **训练成本高昂**：需要大规模的标注数据进行全量微调。
*   **核心问题**：如何在不牺牲预训练模型生成能力的前提下，以低成本实现长程视频的空间一致性？

### 2. 方法论：核心思想与关键技术
论文提出了一个**解耦记忆控制框架**，其核心思想是“按需记忆（Memorize When Needed）”。

*   **解耦架构**：冻结预训练的视频生成骨干网络（如 Wan2.1 或 CogVideoX），仅训练一个轻量级的辅助记忆分支。
*   **相机感知门控机制（Camera-aware Gating）**：
    *   通过计算当前相机位姿与历史位姿的视场（FOV）重叠得分和空间距离，动态决定是否激活记忆注入。
    *   当探索新场景（无重叠）时，门控关闭，依靠骨干网络的先验生成内容；当重访旧场景（高重叠）时，门控开启，注入历史记忆以确保一致性。
*   **混合记忆表示（Hybrid Memory Representation）**：
    *   **连续时间特征（Kt）**：捕捉运动模式和动态。
    *   **离散空间特征（Ks）**：提供精确的静态几何参考。
*   **逐帧交叉注意力（Per-frame Cross-attention）**：
    *   利用掩码策略，使当前帧的潜在特征仅与空间上最相关的历史帧进行交互，实现精确的帧级对齐并降低计算开销。
*   **训练策略**：
    *   **伪循环合成**：通过重排现有视频帧模拟“前进-后退”的重访轨迹，无需昂贵的3D渲染数据。
    *   **历史丢弃（History Dropout）**：训练时随机掩盖历史参考，增强模型在无记忆辅助下的独立生成能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **RealEstate10K**：用于训练和基础测试。
    *   **Out-of-Distribution (OOD)**：收集了55张互联网及AI生成的图像，测试模型的泛化能力。
    *   **复杂轨迹测试**：设计了360°全景旋转、重复重访、随机循环插入、空间偏移返回等极端轨迹。
*   **Benchmark（评估指标）**：
    *   **一致性指标**：PSNR、SSIM、LPIPS（对比重访帧与原始帧）。
    *   **视频质量指标**：美学质量（AQ）、图像质量（IQ）、运动平滑度（MS）、动态程度（DD）。
*   **对比方法**：
    *   **无记忆方法**：AC3D, DFoT, SEVA。
    *   **有记忆方法**：VMem, WorldPlay（包括其蒸馏版本 WorldPlay-d）。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A800 GPU** 进行训练。
*   **训练细节**：
    *   训练迭代次数：10K iterations。
    *   有效 Batch Size：16。
    *   学习率：1 × 10⁻⁴。
*   **计算效率**：论文强调了其轻量化特性。相比 WorldPlay，其记忆分支的计算量（FLOPs）降低了超过 **1000倍**，总推理成本接近于仅运行骨干网络。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括定量对比（Tab 1, 3, 4）、定性视觉对比（Fig 3, A1, A2）、消融实验（Tab 5, Fig 4）以及计算效率分析（Tab 2）。
*   **充分性与公平性**：
    *   实验覆盖了从标准数据集到野外（OOD）数据的迁移测试，验证了泛化性。
    *   通过在两种不同的骨干网络（Wan2.1-14B 和 CogVideoX-5B）上实现，证明了框架的通用性。
    *   对比实验均在相同设置下进行，并引入了复杂的几何轨迹，评估较为客观、严谨。

### 6. 主要结论与发现
*   **性能领先**：在重访一致性指标（PSNR/SSIM）上显著优于现有方法（如 PSNR 提升了约 5dB）。
*   **生成质量平衡**：通过门控机制，模型在保持重访一致性的同时，在探索新场景时依然能维持极高的图像质量和动态效果。
*   **极高的数据效率**：仅需 14K 样本即可达到优于使用 320K 样本训练的 WorldPlay 的效果。

### 7. 优点（亮点）
*   **解耦设计的优雅性**：通过冻结骨干网络，极大地降低了训练门槛，并保护了预训练模型的生成先验。
*   **按需记忆的灵活性**：相机感知门控解决了“一致性”与“创造力”之间的固有矛盾。
*   **计算与数据双高效**：对显存和计算资源友好，且不依赖昂贵的3D标注数据。

### 8. 不足与局限
*   **几何检索依赖性**：记忆检索依赖于 FOV 重叠等几何规则，在存在严重遮挡（Occlusions）的复杂场景下，检索精度可能下降，导致一致性受损。
*   **相机控制精度**：长程生成的最终效果高度依赖于底层相机控制模块的精度，若相机控制出现漂移，记忆注入的效果也会打折扣。
*   **360°全景挑战**：实验显示，在极端的360°旋转任务中，所有方法（包括本项目）仍存在明显的性能下降，这仍是该领域的开放难题。

（完）
