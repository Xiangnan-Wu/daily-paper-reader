---
title: "SpaMEM: Benchmarking Dynamic Spatial Reasoning via Perception-Memory Integration in Embodied Environments"
title_zh: SpaMEM：通过具身环境中的感知-记忆整合评估动态空间推理
authors: "Chih-Ting Liao, Xi Xiao, Chunlei Meng, Zhangquan Chen, Yitong Qiao, Weilin Zhou, Tianyang Wang, Xu Zheng, Xin Cao"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22409v1"
tags: ["query:robot"]
score: 9.0
evidence: 通过动作条件场景变换进行空间信念演化
tldr: 针对多模态大模型在具身环境中难以维持长期空间一致性的挑战，本文提出了SpaMEM基准测试。该基准通过动作序列驱动的场景变换，评估模型在感知与记忆集成下的动态空间推理能力。SpaMEM包含大规模物理仿真数据集和三层任务体系，涵盖原子感知、时序推理及端到端视觉信念维护，揭示了模型在视觉记忆维持和坐标一致性定位方面的显著短板。
selection_source: fresh_fetch
motivation: 现有多模态模型在静态空间推理上表现尚可，但在需要根据动作序列持续更新空间信念的具身动态环境中表现不佳。
method: 构建了一个包含千万级多模态图像的大规模数据集，并设计了从原子感知到端到端视觉信念维护的三层分级诊断任务体系。
result: 实验表明模型在坐标一致性定位上存在瓶颈，且从文本状态追踪切换到纯视觉流维护时性能大幅下降，显示出对符号化信息的强依赖。
conclusion: SpaMEM为具身智能的空间推理提供了细粒度的诊断标准，强调了显式状态表示和长期视觉记忆机制的重要性。
---

## 摘要
多模态大语言模型（MLLMs）在静态视觉-空间推理方面取得了进展，但在具身设置中往往难以保持长程空间一致性。在这些设置中，模型必须根据环境变化下的第一人称视角观测不断修正其信念。我们推出了 SpaMEM（基于动作序列的空间记忆），这是一个大规模诊断基准，通过长交互周期内受动作约束的场景变换（生成、放置、移除）来隔离空间信念演化的机制。SpaMEM 构建于一个具有物理基础的数据集之上，包含跨四种模态（RGB、深度、实例、语义分割）的 10,601,392 张高保真图像，这些图像采集自 1,000 个程序化生成的房屋中的 25,000 多个交互序列。我们将具身空间推理形式化为一个包含 15 个诊断任务的三层级体系：第一层级衡量单次观测中的原子级空间感知；第二层级利用理想的文本状态历史来排除感知噪声，从而探测时间推理能力；第三层级则要求在相同的任务维度下，从原始视觉流中进行端到端的信念维护。我们进一步评估了短期（逐步）更新和长期（情节性）重建。对代表性开源视觉语言模型（VLM）系列的基准测试揭示了一个一致的堆叠瓶颈：坐标一致的接地（grounding）仍然是一个难以逾越的上限，而从第二层级到第三层级的剧烈性能下降暴露了明显的符号脚手架依赖，即模型在基于文本的记录方面表现良好，但难以维持稳健的视觉记忆。SpaMEM 提供了一个细粒度的诊断标准，并激发了对状态表示、信念修正和长程情节整合等显式机制的研究。

## Abstract
Multimodal large language models (MLLMs) have advanced static visual--spatial reasoning, yet they often fail to preserve long-horizon spatial coherence in embodied settings where beliefs must be continuously revised from egocentric observations under environmental change. We introduce SpaMEM (Spatial Memory from Action Sequences), a large-scale diagnostic benchmark that isolates the mechanics of spatial belief evolution via action-conditioned scene transformations (spawn, place, remove) over long interaction horizons. SpaMEM is built on a physically grounded dataset with 10,601,392 high-fidelity images across four modalities (RGB, depth, instance, semantic segmentation), collected from 25,000+ interaction sequences in 1,000 procedurally generated houses. We formalize embodied spatial reasoning as a three-level hierarchy with 15 diagnostic tasks: Level 1 measures atomic spatial perception from single observations; Level 2 probes temporal reasoning with oracle textual state histories to factor out perceptual noise; and Level 3 requires end-to-end belief maintenance from raw visual streams under the same task dimensions. We further evaluate both short-term (step-wise) updates and long-term (episodic) reconstruction. Benchmarking representative open-source VLM families reveals a consistent stacked bottleneck: coordinate-consistent grounding remains a hard ceiling, and the sharp collapse from Level 2 to Level 3 exposes a pronounced symbolic scaffolding dependency, where models succeed with text-based bookkeeping but struggle to sustain robust visual memory. SpaMEM provides a granular diagnostic standard and motivates explicit mechanisms for state representation, belief revision, and long-horizon episodic integration.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SpaMEM**，一个旨在评估多模态大语言模型（MLLMs）在具身环境中动态空间推理能力的基准测试。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态模型虽然在静态图像的空间推理上表现出色，但在具身智能（Embodied AI）场景下，面对持续变化的视觉流和动作序列时，难以维持长期的空间一致性。
*   **研究动机**：
    *   **静态偏见**：现有基准多依赖静态布局，模型常利用语义先验（如“杯子通常在桌上”）而非真正的几何推理。
    *   **感知与记忆耦合**：当模型失败时，无法区分是初始感知错误还是记忆更新失败。
    *   **动态环境缺失**：缺乏对物体被移动、增加或移除等动态场景下空间信念修正能力的评估。

### 2. 方法论：SpaMEM 框架
SpaMEM 通过“动作序列驱动的空间记忆”来隔离空间信念演化的机制。
*   **核心思想**：将具身空间推理拆解为**更新模式（Update Mode）**和**回答模式（Answer Mode）**。模型需根据动作（生成 spawn、放置 place、移除 remove）不断更新其内部的“信念图（Belief Graph）”。
*   **三层分级诊断体系**：
    *   **Level 1 (L1) 原子感知**：评估单帧图像下的物体识别、2D 定位、深度估计、相对方向和计数。
    *   **Level 2 (L2) 符号记忆**：提供理想的文本状态历史，排除感知噪声，纯粹测试模型在已知历史信息下的逻辑更新和时序推理能力。
    *   **Level 3 (L3) 动态整合**：端到端挑战，模型仅凭原始视觉流（RGB/RGB-D）和时间戳来维护空间信念。
*   **探测范式**：分为**短期（逐步）更新**评估和**长期（情节性）重建**评估，以诊断模型是局部遗忘还是全局崩溃。

### 3. 实验设计
*   **数据集与场景**：
    *   基于 **ProcTHOR-10K** 环境，程序化生成了 1,000 个房屋。
    *   包含 25,000 多个交互序列，总计 **10,601,392 张高保真图像**。
    *   涵盖四种模态：RGB、深度图、实例分割、语义分割。
    *   涉及 103 种物体类型和 22 种容器（Receptacles）。
*   **Benchmark 任务**：包含 15 个诊断任务，如语义识别（SOR）、视觉接地（VGL）、轨迹追踪（STT）、事件检测（SED）等。
*   **对比方法**：评估了三个主流开源 VLM 家族：
    *   **InternVL 系列** (2, 2.5, 3)
    *   **LLaVA 系列** (NeXT, OneVision)
    *   **Qwen-VL 系列** (2-VL, 2.5-VL, 3-VL)

### 4. 资源与算力
*   **数据生成**：使用 Qwen2.5-7B-Instruct 作为推理引擎生成交互动作，并在 AI2-THOR 模拟器中并行执行。
*   **算力说明**：文中提到使用了 GPU 加速服务器处理集中式 LLM 推理以减少显存碎片化，并采用多进程并行加速数据采集。但**未明确列出**评估这些大型 VLM 模型所消耗的具体 GPU 总时数或硬件集群规模（如 A100 数量）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 15 个任务维度上对 8 种以上的模型进行了全面测试，涵盖了 RGB 和 RGB-D 两种输入模态。
*   **充分性**：实验设计非常详尽，不仅有宏观的性能评分，还深入到了物体类别、容器类型对性能影响的细粒度分析。
*   **客观性**：通过 L1-L3 的分层设计，成功实现了感知错误与记忆错误的解耦，评估逻辑严密且公平。

### 6. 主要结论与发现
*   **静态到动态的剧烈退化**：模型在处理连续视频流时性能大幅下降（如 InternVL3 的 F1 从 0.36 降至 0.13）。
*   **符号脚手架依赖**：模型在有文本历史（L2）时表现尚可，但在纯视觉（L3）下“视觉世界模型”会迅速崩溃，显示其缺乏内在的视觉记忆机制。
*   **时空失调（Space-Time Dissonance）**：模型能大致记住事件发生的先后顺序，但完全无法将其映射到准确的坐标空间（定位精度接近于零）。
*   **几何先验无效性**：提供显式的深度信息（RGB-D）对提升长期空间整合能力的帮助微乎其微，瓶颈在于表征能力而非感知输入。

### 7. 优点与亮点
*   **分层诊断设计**：这是该基准最大的亮点，能够精准定位模型是在哪个环节（感知、短期记忆、长期整合）出了问题。
*   **动态场景构建**：通过动作条件改变环境，打破了模型对静态语义先验的依赖，真正测试几何与逻辑推理。
*   **大规模高质量数据**：千万级的多模态图像和详尽的元数据标注为具身智能研究提供了宝贵资源。

### 8. 不足与局限
*   **模拟器局限**：虽然使用了高保真模拟器，但与真实物理世界（Real-to-Sim）之间仍存在差距，如光影变化、物理碰撞的复杂性。
*   **输出接口限制**：目前的评估仍受限于 VLM 的文本输出接口，无法直接探测其内部的潜在表征（Latent Representations）。
*   **动作空间有限**：仅涵盖了生成、放置、移除三种基本动作，尚未涉及更复杂的具身操作（如推、拉、旋转物体）。

（完）
