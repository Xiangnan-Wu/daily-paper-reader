---
title: "OccDirector: Language-Guided Behavior and Interaction Generation in 4D Occupancy Space"
title_zh: "OccDirector: 4D 占用空间中语言引导的行为与交互生成"
authors: "Zhuding Liang, Tianyi Yan, Dubing Chen, Jiasen Zheng, Huan Zheng, Cheng-zhong Xu, Yida Wang, Kun Zhan, Jianbing Shen"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22240v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于自动驾驶模拟的生成式世界模型
tldr: OccDirector是一个创新的4D占用空间生成框架，旨在通过自然语言指令驱动自动驾驶场景中的复杂多智能体交互。它摆脱了对几何先验的依赖，利用VLM驱动的时空MMDiT和历史前缀锚定策略，实现了长时程的一致性生成。此外，研究者还推出了包含8.5万条多级语言标注的OccInteract数据集及评估基准，显著提升了行为编排的灵活性与真实性。
selection_source: fresh_fetch
motivation: 现有的4D占用生成模型过度依赖刚性几何条件或简单文本，难以实现复杂的时空语义理解和多智能体交互编排。
method: 采用VLM驱动的时空MMDiT架构，并结合历史前缀锚定策略，将自然语言脚本直接映射为物理上合理的4D体素动态。
result: 通过在包含8.5万条多级标注的新数据集OccInteract上实验，证明了该方法在生成质量和指令遵循能力上均达到领先水平。
conclusion: OccDirector成功实现了从外观合成到语言驱动行为编排的范式转变，为自动驾驶仿真提供了更具语义深度的生成方案。
---

## 摘要
生成式世界模型日益依赖 4D 占用（occupancy）来实现逼真的自动驾驶模拟。然而，现有的生成框架依赖于僵化的几何条件（如显式轨迹）或简单的属性级文本，无法编排复杂且具有时序性的多智能体交互。为了弥补这一语义-时空鸿沟，我们提出了 OccDirector，这是一个仅以自然语言为条件生成 4D 占用动态的开创性框架。OccDirector 充当“场景导演”，在不需要几何先验的情况下，将自然语言脚本映射为符合物理规律的体素动态。在技术上，它采用了由视觉语言模型（VLM）驱动的时空 MMDiT，并配备了历史前缀锚定策略，以确保长时程交互的一致性。此外，我们推出了 OccInteract-85k，这是一个独特标注了多级语言指令的新型数据集：涵盖了从静态布局到复杂多智能体行为的各种指令，并附带了一个基于 VLM 的新型评估基准。大量实验表明，OccDirector 实现了最先进的生成质量和前所未有的指令遵循能力，成功地将范式从外观合成转向了语言驱动的行为编排。

## Abstract
Generative world models increasingly rely on 4D occupancy for realistic autonomous driving simulation. However, existing generation frameworks depend on rigid geometric conditions (e.g., explicit trajectories) or simplistic attribute-level text, failing to orchestrate complex, sequential multi-agent interactions. To address this semantic-spatiotemporal gap, we propose OccDirector, a pioneering framework that generates 4D occupancy dynamics conditioned solely on natural language. Operating as a ``scenario director'', OccDirector maps natural language scripts into physically plausible voxel dynamics without requiring geometric priors. Technically, it employs a VLM-driven Spatio-Temporal MMDiT equipped with a history-prefix anchoring strategy to ensure long-horizon interaction consistency. Furthermore, we introduce OccInteract-85k, a novel dataset uniquely annotated with multi-level language instructions: ranging from static layouts to intricate multi-agent behaviors, alongside a novel VLM-based evaluation benchmark. Extensive experiments demonstrate that OccDirector achieves state-of-the-art generation quality and unprecedented instruction-following capabilities, successfully shifting the paradigm from appearance synthesis to language-driven behavior orchestration.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **OccDirector**，一个旨在通过自然语言指令直接生成 4D 占用空间（Occupancy Space）动态的生成式世界模型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：自动驾驶（AD）模拟需要高保真且可控的世界模型。虽然 4D 占用（时空体素网格）是描述动态环境的理想表示，但现有的生成方法（如 OccSora, DynamicCity）高度依赖**刚性几何条件**（如预定义的 BEV 布局或精确的车辆轨迹）。
*   **核心挑战**：存在“**语义-时空鸿沟**”。自然语言描述的是高层逻辑（如“超车”、“紧急制动”），而 4D 占用生成需要体素级的物理一致性和精确的时空演化。现有的文本编码器（如 CLIP）在处理复杂交互逻辑时表现得像“词袋”，缺乏组合推理能力。
*   **整体含义**：OccDirector 实现了从“外观合成”到“语言驱动行为编排”的范式转变，允许用户像导演一样通过脚本控制多智能体交互，而无需手动绘制几何路径。

### 2. 方法论
*   **核心思想**：利用视觉语言模型（VLM）作为语义理解核心，结合可扩展的时空 Transformer 架构，将文本脚本直接映射为物理上合理的体素流。
*   **关键技术细节**：
    *   **联合潜在表示**：使用预训练的 **Occ-VAE** 将 4D 占用数据压缩为紧凑的潜在网格；使用 **VLM（Qwen3-VL）** 提取富含过程语义的文本特征。
    *   **Token Refiner**：一个轻量级的双向 Transformer，用于弥合 VLM 特征与空间布局之间的模态鸿沟。
    *   **时空 MMDiT 架构**：采用多模态扩散 Transformer（MMDiT），通过**时空分离注意力（STSA）**处理海量体素 Token。它先进行帧内空间联合注意力（文本-空间对齐），再进行跨帧时间自注意力（动力学传播）。
    *   **历史前缀锚定策略（History-Prefix Anchoring）**：在训练中随机替换噪声历史帧为真实帧；在推理时通过每一步的强制锚定，确保生成的未来序列与给定的历史上下文严格一致，防止长时程漂移。
    *   **算法流程**：基于流匹配（Flow Matching）框架，模型学习回归从高斯噪声到真实潜在分布的向量场。

### 3. 实验设计
*   **数据集：OccInteract-85k**。研究者构建了一个包含 8.5 万个实例的大规模数据集，分为三个层次：
    1.  **Static**：静态布局描述（道路拓扑）。
    2.  **Env**：环境交互（如在路口转弯）。
    3.  **Agent**：多智能体交互（如紧急避让、切入），包含 VLM 预测的危险程度评分。
*   **Benchmark**：提出了一个基于 VLM 的自动评估基准，使用 GPT-5.2、Gemini-3.0-pro 等作为裁判，从场景完整性、结构正确性和指令遵循度三个维度评分。
*   **对比方法**：对比了 DOME、COME、DynamicCity 等 SOTA 几何驱动模型，以及将 DynamicCity 适配为文本驱动（+CLIP/T5）的基准。

### 4. 资源与算力
*   **硬件支持**：文中提到在 **NVIDIA A100 (80GB)** 上进行推理资源缩放测试。
*   **训练细节**：Batch size 为 32，训练迭代次数为 10 万次。
*   **效率**：由于采用了 STSA 架构，显存占用随帧数线性增长，且对文本长度（L）不敏感（即使 L 增加到 2048，显存增加也小于 0.1%）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量实验**：评估了单帧质量（IS, FID, KID）和视频质量（FVD, Precision, Recall）。
    *   **消融实验**：针对 STSA 架构、Token Refiner 的必要性、历史锚定比例、CFG 尺度等做了详尽测试。
    *   **鲁棒性测试**：使用了三种不同的顶级 VLM 作为裁判，证明了评估结果的一致性。
    *   **用户研究**：邀请 10 名参与者对 30 个样本进行主观评分，结果与 VLM 评分高度相关（相关系数 0.88）。
*   **充分性评价**：实验设计非常全面，涵盖了从底层图像指标到高层语义对齐的各个维度，对比实验和消融实验均体现了客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：OccDirector 在视频生成质量（FVD 275）上显著优于最强基准（FVD 388），提升幅度达 29.1%。
*   **语义对齐**：传统的文本编码器（CLIP/T5）在 4D 任务中存在严重的模式崩溃（Recall 极低），而 OccDirector 能精准理解“否定句”（如“没有其他车辆”）和复杂的相对运动逻辑（如“切入”）。
*   **一致性**：历史前缀锚定策略是实现长时程、因果逻辑一致的交互生成的关键。

### 7. 优点
*   **范式创新**：摆脱了对轨迹工程的依赖，极大地降低了自动驾驶仿真场景设计的门槛。
*   **架构优越**：STSA 解决了 4D 数据计算量巨大的问题，同时保持了深度的跨模态交互。
*   **数据贡献**：OccInteract-85k 数据集填补了语言-4D 占用配对数据的空白，且自动化标注流程具有高度的可扩展性。

### 8. 不足与局限
*   **数据偏差**：受限于原始数据集（如 nuScenes），生成的场景可能继承传感器盲区或自车体素缺失等伪影。
*   **长时程限制**：虽然有锚定策略，但在极长序列（如超过 8 秒）生成中，物理一致性的累积误差仍是一个挑战。
*   **语义粒度**：目前仅支持 11 类语义标签，尚无法捕捉更细微的状态（如转向灯、刹车灯或行人的手势）。
*   **裁判偏见**：基于 VLM 的评估虽然高效，但不同 VLM 模型之间存在主观评分标准的差异（如 GPT-5.2 明显比 Gemini 更严格）。

（完）
