---
title: "DynamicRad: Content-Adaptive Sparse Attention for Long Video Diffusion"
title_zh: DynamicRad：面向长视频扩散模型的内容自适应稀疏注意力
authors: "Yongji Long, Shijun Liang, Jintao Li, Yun Li"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20470v1"
tags: ["query:q7"]
score: 9.0
evidence: 长视频扩散模型的稀疏注意力机制
tldr: 针对长视频扩散模型中静态稀疏掩码易丢失复杂动态信息的局限，本文提出DynamicRad。该方法基于径向局部先验，引入双模式策略（静态比例与动态阈值），并结合离线贝叶斯优化和语义运动路由，实现内容自适应的稀疏注意力。在HunyuanVideo等模型上，DynamicRad在保持甚至提升生成质量的同时，实现了1.7至2.5倍的推理加速，显著优化了长视频生成的效率与质量平衡。
selection_source: fresh_fetch
motivation: 传统的静态稀疏掩码在处理复杂视频动态时容易丢失关键的长程信息，限制了长视频扩散模型的效率与质量。
method: 提出一种基于径向局部先验的统一稀疏注意力范式，通过双模式策略和轻量级语义运动路由实现内容自适应的注意力选择。
result: "在HunyuanVideo和Wan2.1上实现了超过80%的有效稀疏度，推理速度提升1.7至2.5倍，且在长序列设置下质量接近或优于全量注意力。"
conclusion: DynamicRad通过离线优化与动态路由的结合，成功推向了视频扩散模型效率与质量的帕累托前沿。
---

## 摘要
利用视频扩散模型中天然的时空能量衰减为提升效率提供了途径，但仅依赖僵化的静态掩码在复杂动态场景中存在丢失关键远程信息的风险。为解决这一问题，我们提出了 DynamicRad，这是一种统一的稀疏注意力范式，它将自适应选择建立在径向局部性先验之上。DynamicRad 引入了一种双模式策略：用于速度优化执行的静态比例（static-ratio）模式和用于质量优先过滤的动态阈值（dynamic-threshold）模式。为了在不产生在线搜索开销的情况下确保鲁棒性，我们集成了一个离线贝叶斯优化（BO）流水线，并结合了一个语义运动路由。这一轻量级投影模块以极低的运行时开销将提示词嵌入映射到最优稀疏方案。与在线分析方法不同，我们的离线 BO 在基于物理的代理任务上优化注意力重构误差（MSE），从而确保快速收敛。在 HunyuanVideo 和 Wan2.1-14B 上的实验表明，DynamicRad 推动了效率-质量的帕累托前沿，在超过 80% 的有效稀疏度下实现了 1.7 倍至 2.5 倍的推理加速。在某些长序列设置下，动态模式甚至达到或超过了全量基准，而掩码感知 LoRA 进一步提升了长时程的一致性。代码可在 https://github.com/Adamlong3/DynamicRad 获取。

## Abstract
Leveraging the natural spatiotemporal energy decay in video diffusion offers a path to efficiency, yet relying solely on rigid static masks risks losing critical long-range information in complex dynamics. To address this issue, we propose \textbf{DynamicRad}, a unified sparse-attention paradigm that grounds adaptive selection within a radial locality prior. DynamicRad introduces a \textbf{dual-mode} strategy: \textit{static-ratio} for speed-optimized execution and \textit{dynamic-threshold} for quality-first filtering. To ensure robustness without online search overhead, we integrate an offline Bayesian Optimization (BO) pipeline coupled with a \textbf{semantic motion router}. This lightweight projection module maps prompt embeddings to optimal sparsity regimes with \textbf{minimal runtime overhead}. Unlike online profiling methods, our offline BO optimizes attention reconstruction error (MSE) on a physics-based proxy task, ensuring rapid convergence. Experiments on HunyuanVideo and Wan2.1-14B demonstrate that DynamicRad pushes the efficiency--quality Pareto frontier, achieving \textbf{1.7$\times$--2.5$\times$ inference speedups} with \textbf{over 80\% effective sparsity}. In some long-sequence settings, the dynamic mode even matches or exceeds the dense baseline, while mask-aware LoRA further improves long-horizon coherence. Code is available at https://github.com/Adamlong3/DynamicRad.

---

## 论文详细总结（自动生成）

这是一份关于论文《DynamicRad: Content-Adaptive Sparse Attention for Long Video Diffusion》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着视频扩散模型（如 Sora、HunyuanVideo）向长时程、高分辨率演进，自注意力机制的计算复杂度随 Token 数量呈**二次方增长**，成为推理性能的主要瓶颈。
*   **现有方案的局限：** 静态稀疏注意力（如 Radial Attention）虽然硬件友好，但在处理复杂动态视频时容易丢失关键的长程依赖；而全动态稀疏方案（如 SVG）往往引入额外的在线计算开销或在长视频设置下表现不稳定。
*   **研究目标：** 提出一种既能保持硬件执行效率，又能根据视频内容动态调整稀疏模式的统一框架，以突破长视频生成的效率-质量帕累托前沿。

### 2. 方法论
DynamicRad 结合了径向局部性先验与内容自适应选择，核心组件包括：
*   **双模式选择策略：**
    *   **静态比例模式 (Static-ratio)：** 预设保留比例，适用于追求极致吞吐量的场景。
    *   **动态阈值模式 (Dynamic-threshold)：** 利用轻量级的注意力 Logit 代理评分进行过滤，优先保证生成质量。
*   **径向邻域划分：** 基于时空距离定义衰减窗口 $w(i,j)$。引入长程因子 $\lambda$ 控制远距离帧的采样频率，将注意力集中在能量较高的区域。
*   **语义运动路由 (Semantic Motion Router)：** 这是一个轻量级的 MLP 模块，它重用预计算的文本提示词嵌入（Prompt Embedding），预测视频的运动强度（低、中、高），并从离线查找表中检索最优的稀疏配置参数。
*   **离线贝叶斯优化 (BO)：** 为了避免昂贵的在线搜索，在基于物理特征漂移的代理任务上预先优化注意力重构误差（MSE），使搜索过程在 15 分钟内即可完成。
*   **掩码感知 LoRA (Mask-Aware LoRA)：** 针对受稀疏化影响敏感的投影层进行微调，补偿高稀疏度下的信息损失，增强时空一致性。

### 3. 实验设计
*   **基座模型：** HunyuanVideo (241帧) 和 Wan2.1-14B (145帧)。
*   **数据集：** 使用 OpenVid-1M 的子集进行微调和评估。
*   **Benchmark：** 
    *   **质量指标：** VBench（涵盖时空一致性 S.C.、审美质量 A.Q.、图像质量 I.Q.）和 VisionReward（人类偏好对齐）。
    *   **效率指标：** 稀疏度 (%)、推理延迟 (Latency)、加速比 (Speedup)。
*   **对比方法：** PowerAttention (PA)、SVG、Radial Attention 以及原始 Dense 基准。

### 4. 资源与算力
*   **硬件环境：** 主要使用 **NVIDIA H100 GPU**。
*   **算力配置：** 
    *   标准长度视频生成使用单张 H100。
    *   超长序列（HunyuanVideo-241f，超过 20 万 Token）使用 **8×H100 集群**进行序列并行推理。
*   **训练/搜索耗时：** 
    *   语义路由训练：在单张 H100 上耗时小于 1 小时。
    *   离线 BO 搜索：约 15 分钟完成。
    *   Mask-Aware LoRA：在 OpenVid-1M 子集上微调 1000 步。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流的大规模 DiT 架构（HunyuanVideo 和 Wan2.1）上进行了验证，涵盖了从标准长度（72/120帧）到长时程（145/241帧）的多种设置。
*   **消融实验：** 详细对比了配置参数（$\lambda, \gamma$ 等）、双模式切换、离线预测器以及 LoRA 模块对性能的影响。
*   **客观性：** 引入了微观基准测试（Micro-Benchmark）来量化路由和评分模块的毫秒级开销，证明了加速效果并非牺牲了大量的辅助计算。实验设计较为全面，对比了当前最先进的训练无关（Training-free）稀疏方案，具有较高的说服力。

### 6. 主要结论与发现
*   **显著加速：** 在保持 80% 以上有效稀疏度的前提下，实现了 **1.7× 至 2.54×** 的推理加速。
*   **质量保持甚至提升：** 动态模式在长序列设置下的表现（如 VisionReward 指标）有时甚至优于全量注意力基准，这可能归功于稀疏机制过滤了无关的噪声 Token。
*   **内容自适应的必要性：** 实验证明低运动场景允许极高稀疏度（>86%），而高运动场景需要更密集的长程连接，DynamicRad 的路由机制能有效识别并匹配这些需求。

### 7. 优点
*   **效率与灵活性的平衡：** 通过双模式设计，用户可以根据需求在“极速”和“高质量”之间切换。
*   **极低开销的自适应：** 语义路由仅需 <2ms 的 MLP 推理，且离线 BO 避免了复杂的在线 Profiling。
*   **通用性强：** 能够无缝集成到不同的视频 DiT 架构中，且支持 FlashInfer 等高性能算子。

### 8. 不足与局限
*   **极端运动挑战：** 在极高动态场景（如第一人称赛车）下，局部块稀疏模式可能仍会产生轻微伪影。
*   **语义歧义：** 路由机制依赖文本提示词，若提示词描述模糊或视频中途发生剧烈运动性质改变，全局单一的稀疏配置可能不是最优解。
*   **硬件依赖：** 性能增益依赖于特定的块大小（Block Size），在非服务器级 GPU 上可能需要重新校准查找表。

（完）
