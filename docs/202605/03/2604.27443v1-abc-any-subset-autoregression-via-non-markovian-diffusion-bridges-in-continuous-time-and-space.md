---
title: "ABC: Any-Subset Autoregression via Non-Markovian Diffusion Bridges in Continuous Time and Space"
title_zh: ABC：通过连续时空中的非马尔可夫扩散桥实现任意子集自回归
authors: "Gabe Guo, Thanawat Sornwanee, Lutong Hao, Elon Litman, Stefano Ermon, Jose Blanchet"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27443v1"
tags: ["query:q12"]
score: 9.0
evidence: 通过扩散桥进行视频预测和未来帧合成
tldr: ABC 提出了一种在连续时空中通过非马尔可夫扩散桥实现的任意子集自回归模型。针对现有扩散模型在处理视频、天气等连续随机过程时无法捕捉物理时间结构相似性、噪声注入不敏感及难以处理任意观测子集等问题，ABC 通过连续 SDE 建模，使生成过程紧贴物理时间，并利用路径依赖的条件机制实现了对历史或未来任意状态子集的灵活调节，显著提升了生成质量。
selection_source: fresh_fetch
motivation: 现有扩散模型在生成连续随机过程时，难以捕捉物理时间上的结构相似性，且无法灵活处理任意观测子集的条件生成。
method: 提出 ABC 模型，利用连续 SDE 追踪真实物理时间和状态，并通过路径空间上的测度变换实现非马尔可夫扩散桥和路径依赖的评分匹配。
result: 在视频生成和天气预报等多个领域的实验表明，ABC 在处理不规则采样和任意子集条件生成任务上优于现有竞争方法。
conclusion: ABC 通过将扩散过程与物理时间对齐，为连续时空下的复杂随机过程建模提供了一种更具物理合理性和灵活性的新框架。
---

## 摘要
基于部分观测（例如首帧和末帧）生成连续时间、连续空间的随机过程（例如视频、天气预报）是一项基础性挑战。现有方法（如扩散模型）存在关键局限性：(1) 从噪声到数据的演化过程无法捕捉物理时间相近状态之间的结构相似性，且在低步长机制下积分不稳定；(2) 注入的随机噪声对物理过程流逝的时间不敏感，导致动力学过程不正确；(3) 它们忽略了对任意状态子集（如不规则采样的时刻、未来观测值）的条件约束。我们提出了 ABC：通过连续时空中的非马尔可夫扩散桥实现的任意子集自回归模型。关键在于，我们使用一个连续的随机微分方程（SDE）对该过程进行建模，其时间变量和中间状态追踪真实时间和过程状态。这具有可证明的优势：(1) 生成未来状态的起点是已经接近的前一状态，而非无信息的噪声；(2) 随机噪声的注入随物理流逝时间而缩放，从而促进具有相似时间相邻状态的物理合理动力学。我们通过路径空间上的测度变换推导出 SDE 动力学，从而产生另一个优势：(3) 对状态历史和/或未来的任意子集进行路径依赖的条件约束。为了学习这些动力学，我们推导出了去噪分数匹配的路径和时间依赖扩展。我们的实验表明，ABC 在包括视频生成和天气预报在内的多个领域优于竞争方法。

## Abstract
Generating continuous-time, continuous-space stochastic processes (e.g., videos, weather forecasts) conditioned on partial observations (e.g., first and last frames) is a fundamental challenge. Existing approaches, (e.g., diffusion models), suffer from key limitations: (1) noise-to-data evolution fails to capture structural similarity between states close in physical time and has unstable integration in low-step regimes; (2) random noise injected is insensitive to the physical process's time elapsed, resulting in incorrect dynamics; (3) they overlook conditioning on arbitrary subsets of states (e.g., irregularly sampled timesteps, future observations). We propose ABC: Any-Subset Autoregressive Models via Non-Markovian Diffusion Bridges in Continuous Time and Space. Crucially, we model the process with one continual SDE whose time variable and intermediate states track the real time and process states. This has provable advantages: (1) the starting point for generating future states is the already-close previous state, rather than uninformative noise; (2) random noise injection scales with physical time elapsed, encouraging physically plausible dynamics with similar time-adjacent states. We derive SDE dynamics via changes-of-measure on path space, yielding another advantage: (3) path-dependent conditioning on arbitrary subsets of the state history and/or future. To learn these dynamics, we derive a path- and time-dependent extension of denoising score matching. Our experiments show ABC's superiority to competing methods on multiple domains, including video generation and weather forecasting.

---

## 论文详细总结（自动生成）

以下是对论文《ABC: Any-Subset Autoregression via Non-Markovian Diffusion Bridges in Continuous Time and Space》的结构化总结：

### 1. 核心问题与研究动机
论文关注的是**连续时间、连续空间随机过程**（如视频、天气预报、金融走势）的生成建模。
*   **现有局限：** 传统的扩散模型（Diffusion Models）通常将生成过程视为从纯噪声到数据的映射，忽略了物理时间上的状态连续性（相邻时刻状态应接近）。
*   **时间尺度失真：** 现有扩散桥模型无论物理时间跨度多大，都使用固定的辅助时间（0到1）和噪声计划，导致动力学不准确（如视频闪烁）。
*   **条件约束僵化：** 难以处理不规则采样的数据或非因果（Non-causal）约束（例如同时根据第一帧和最后一帧预测中间帧）。
*   **核心目标：** 提出一种能够处理**任意观测子集**、且生成过程的 SDE 时间与**物理时间**完全对齐的自回归模型。

### 2. 方法论
ABC 模型的核心思想是将随机过程建模为一个单一的、连续的随机微分方程（SDE），其状态演化直接对应物理时间的流逝。
*   **核心公式：** 建立一个受控 SDE：$dX_t = [-a(t)X_t + \sigma(t)^2 s(t, X_t; X_{0:i^*})]dt + \sigma(t)dB_t$。其中 $s$ 是路径依赖的分数函数（Score function）。
*   **Doob h-变换：** 利用测度变换（Change-of-measure）技术，通过 Doob h-变换将一个数据无关的基过程转变为能够匹配目标数据有限维分布的生成过程。
*   **非马尔可夫动力学：** 漂移项（Drift）不仅取决于当前状态，还取决于已观测到的路径历史（$X_{0:i^*}$），这使得模型能够捕捉长程依赖。
*   **路径依赖评分匹配：** 提出了一种新的训练目标，将去噪评分匹配（Denoising Score Matching）扩展到非马尔可夫和时间敏感的核函数上，支持对任意时刻子集的条件训练。
*   **任意子集推理：** 通过 Cross-Attention Transformer 架构，模型可以同时接收过去和未来的观测点作为输入，实现非因果的插值生成。

### 3. 实验设计
*   **数据集：**
    *   **CelebV-HQ：** 高质量人脸视频（17.8万训练集）。
    *   **Sky-Timelapse：** 天空延时摄影视频（1.7万训练集）。
    *   **SEVIR VIL：** 气象雷达回波数据集，用于天气预报。
*   **对比方法（Baselines）：**
    *   **DDBM / PFI：** 传统的扩散桥模型（固定噪声计划，忽略物理时间缩放）。
    *   **DDPM / SMLD：** 标准的噪声到数据扩散模型。
*   **评估指标：** FVD（视频时间一致性）、FID（单帧质量）、MAE/RMSE（天气预测精度）、CSI（关键成功指数）。

### 4. 资源与算力
*   **GPU 型号与数量：** 训练主要在 **4块或8块 NVIDIA A100 (80GB)** 上进行。
*   **训练时长：** 每个主要实验运行大约需要 **36小时**。
*   **模型规模：** 基础模型基于 DiT-B 架构，约 2.02 亿参数；扩展实验中使用了约 5.94 亿参数的中型模型。

### 5. 实验数量与充分性
*   **实验组数：** 论文进行了多维度的实验，包括非因果预测（每隔 K 帧采样）、因果预测（前缀生成）、不同帧率（16帧 vs 32帧）以及不同 SDE 步数（250 vs 500步）。
*   **消融实验：** 针对路径依赖条件（全路径 vs 马尔可夫简化）、波动率超参数（$\sigma$ 敏感度）、模型缩放（Scaling）以及是否有布朗桥（Brownian Bridge）修正项进行了详细消融。
*   **客观性：** 实验涵盖了视觉和气象两个截然不同的领域，对比基线涵盖了当前最先进的扩散桥和流匹配方法，实验设计较为公平且充分。

### 6. 主要结论与发现
*   **时间自适应波动率至关重要：** 证明了让噪声注入随物理时间缩放（ABC 的核心特性）能显著提升生成质量，避免了基线模型中常见的闪烁和轨迹错误。
*   **数据到数据优于噪声到数据：** 在连续过程建模中，从前一状态出发（Data-to-Data）比从纯噪声出发更稳定且效率更高。
*   **非马尔可夫优势：** 引入路径历史信息能显著提升长视频生成的连贯性。
*   **ABC 的通用性：** 模型在处理不规则采样和非因果约束时表现出极强的灵活性，且在标准因果预测任务中也达到了帕累托最优。

### 7. 优点
*   **理论严谨：** 基于随机分析和测度变换理论，为连续时间自回归提供了坚实的数学基础。
*   **物理一致性：** 解决了扩散模型中长期存在的“辅助时间”与“物理时间”脱节的问题。
*   **高度灵活：** 能够处理任意观测子集，是目前处理不规则采样时间序列的理想框架。
*   **生成质量：** 在保持单帧质量的同时，大幅提升了视频的时间一致性（FVD 指标领先）。

### 8. 不足与局限
*   **计算开销：** 尽管使用了 FlashAttention，但 Transformer 关注全路径历史在处理极长序列时仍可能面临内存压力。
*   **采样方案：** 目前主要依赖 SDE 离散化采样，未来可能需要更高效的 ODE-SDE 混合采样方案。
*   **应用范围：** 虽然在视频和天气上表现良好，但在更高维或更复杂的物理模拟（如 3D 流体）上的表现尚待验证。

（完）
