---
title: "Dreaming Across Towns: Semantic Rollout and Town-Adversarial Regularization for Zero-Shot Held-Out-Town Fixed-Route Driving in CARLA"
title_zh: 跨城镇之梦：CARLA 中零样本留出城镇固定路线驾驶的语义展开与城镇对抗正则化
authors: "Feeza Khan Khanzada, Jaerock Kwon"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27994v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于驾驶的 Dreamer 风格潜世界模型智能体
tldr: 本研究针对自动驾驶智能体在未见环境（CARLA模拟器中的新城镇）中性能下降的问题，提出了一种基于Dreamer潜世界模型的改进方法。通过引入多步语义预测和城镇对抗正则化，增强了模型对环境结构的泛化能力。在无导航指令和地图输入的零样本迁移测试中，该方法显著提升了在未见城镇中的固定路线驾驶成功率。
selection_source: fresh_fetch
motivation: 解决学习型驾驶智能体在部署到未见过的城镇环境时性能大幅下降的泛化难题。
method: 在Dreamer框架基础上，增加了未来视觉语义嵌入的多步预测损失和基于循环潜状态的城镇对抗监督。
result: 在CARLA的零样本迁移评估中，该模型在未见城镇的平均驾驶成功率优于其他Dreamer系列方法。
conclusion: 语义预测监督与城镇对抗正则化的结合能有效提升受控环境下自动驾驶智能体的跨场景泛化性能。
---

## 摘要
学习型驾驶智能体在部署到未见环境时性能往往会下降。本文研究了 CARLA 模拟器中该问题的一个受限实例：闭环固定路线驾驶智能体从 Town05 和 Town06 到未见过的 Town03 和 Town04 的零样本迁移。该研究通过将天气固定为 ClearNoon 并移除交通流和行人，从而隔离了结构性的城镇偏移。我们基于 Dreamer 风格的隐世界模型智能体，并增加了两个仅在训练阶段使用的辅助损失：沿想象展开（imagined rollouts）对未来视觉语义嵌入的多步预测，以及对循环隐状态语义投影的城镇对抗监督。因果上下文特征约束语义展开预测器，而 Actor 和 Critic 则保留标准的控制特征。策略不接收导航指令、路线折线、目标位姿或地图输入；参考路线仅由环境用于计算奖励、进度、成功和终止。在评估的留出城镇中，所提出的模型在包含的 Dreamer 系列方法中实现了最高的平均成功率。次要的安全和车道保持指标在不同城镇之间表现不一。这些结果支持一个受限的结论：在这种受控的固定天气 CARLA 设置中，语义展开监督结合城镇对抗正则化提高了留出城镇的平均路线完成率。

## Abstract
Learned driving agents often degrade when deployed in unseen environments. This paper studies a deliberately bounded instance of that problem in the CARLA simulator: zero-shot transfer of a closed-loop fixed-route driving agent from Town05 and Town06 to unseen Town03 and Town04. The study isolates structural town shift by keeping weather fixed to ClearNoon and removing traffic and pedestrians. We build on a Dreamer-style latent world-model agent and add two training-only auxiliary losses: multi-horizon prediction of future visual-semantic embeddings along imagined rollouts and town-adversarial supervision on a semantic projection of the recurrent latent state. A causal context feature conditions the semantic rollout predictor, while the actor and critic retain the standard control feature. The policy receives no navigation command, route polyline, goal pose, or map input; the reference route is used only by the environment for reward, progress, success, and termination. Across the evaluated held-out towns, the proposed model achieves the highest mean success rate among the included Dreamer-family methods. Secondary safety and lane-keeping metrics are mixed across towns. These results support a bounded conclusion: in this controlled fixed-weather CARLA setting, semantic rollout supervision combined with town-adversarial regularization improves mean held-out-town route completion.

---

## 论文详细总结（自动生成）

这篇论文探讨了自动驾驶智能体在未见环境中的泛化问题，特别是在 CARLA 模拟器中实现跨城镇的零样本迁移。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：深度强化学习训练的驾驶智能体在部署到未见过的环境（如新城镇）时，性能往往会大幅下降。这主要是因为模型过度拟合了训练城镇的特定布局和车道拓扑。
*   **核心问题**：如何在不使用目标域数据进行微调的情况下，提高智能体在未见城镇中的“零样本”迁移能力？
*   **研究背景**：研究者设定了一个受限的实验环境，通过固定天气（ClearNoon）并移除交通流和行人，专门隔离并研究“城镇结构偏移”（Structural Town Shift）对驾驶性能的影响。

### 2. 方法论
论文在 **DreamerV3**（一种基于潜世界模型的强化学习框架）的基础上，引入了两个关键的训练期辅助任务：
*   **核心思想**：通过增强世界模型对未来语义的预测能力，并强制学习城镇无关的特征，来提升泛化性。
*   **关键技术细节**：
    *   **语义展开（Semantic Rollout）**：在想象的潜空间轨迹中，要求模型预测未来多个时间步的视觉语义嵌入。这些嵌入由预训练且冻结的 **OpenCLIP** 图像编码器提供，作为语义监督信号。
    *   **城镇对抗正则化（Town-Adversarial Regularization）**：在潜状态的语义投影层添加一个**梯度反转层（GRL）**和城镇分类器。通过对抗训练，迫使语义特征分支丢弃能够识别特定城镇的信息，从而学习到更通用的驾驶语义。
    *   **因果上下文特征（Causal Context Feature）**：使用编码器嵌入的指数移动平均（EMA）来捕捉历史上下文，并将其作为语义预测器的输入，以保持在线推理的因果性。
    *   **无指令驾驶**：策略不依赖导航指令或地图，仅通过环境奖励引导进行固定路线的循迹。

### 3. 实验设计
*   **数据集/场景**：使用 CARLA 模拟器。
    *   **训练集**：Town05 和 Town06。
    *   **测试集（留出城镇）**：Town03 和 Town04。
*   **Benchmark**：固定路线的闭环驾驶任务，评估指标包括成功率（Success Rate）、行驶距离、每公里碰撞数、每公里压线数等。
*   **对比方法**：
    *   **Dreamer-Std**：标准 DreamerV3。
    *   **Dreamer-Aug**：仅使用图像增强（亮度、对比度、噪声等）。
    *   **Dreamer-DANN**：在基础特征上应用对抗训练。
    *   **多种消融变体**：如仅使用语义展开、仅使用架构改变而不加损失函数、直接使用语义特征作为策略输入等（共 10 种变体）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到了总训练步数为 500,000 步，Batch Size 为 64，重放序列长度为 8，想象步长为 5。每个模型使用 3 个随机种子独立训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 种不同的模型变体进行了对比，每个变体在 2 个留出城镇上进行测试，每个城镇每个种子运行 64 个评估回合。
*   **充分性与公平性**：
    *   实验设计较为充分，包含了详尽的消融实验（如测试不同 GRL 强度、不同输入特征等）。
    *   所有对比方法共享相同的模拟器设置、动作空间和奖励函数，确保了比较的公平性。
    *   使用了 3 个随机种子并报告了均值和标准差，增强了结果的可信度。

### 6. 主要结论与发现
*   **性能提升**：提出的 **Sem+Adv-Aux** 模型在未见城镇的平均成功率最高。在 Town04 上达到了 85.62% 的成功率，显著优于标准 Dreamer（33.44%）。
*   **协同效应**：语义展开监督与城镇对抗正则化结合的效果优于单一机制。单纯的图像增强或对抗训练不足以解决复杂的结构偏移。
*   **策略输入选择**：发现将语义分支作为“辅助正则化项”效果最好，而直接将策略输入改为语义特征（st）反而会导致性能下降。
*   **驾驶风格**：该方法倾向于更稳健、速度稍慢的驾驶风格，从而换取更高的路线完成率。

### 7. 优点
*   **解耦研究**：通过固定天气和移除动态障碍物，清晰地揭示了城镇拓扑结构对模型泛化的挑战。
*   **语义先验利用**：巧妙地利用 OpenCLIP 作为冻结的语义特征提取器，避免了昂贵的端到端语义分割标注。
*   **辅助任务设计**：辅助任务仅在训练时存在，不增加推理时的计算负担，具有很强的实用性。

### 8. 不足与局限
*   **场景简化**：实验环境过于理想化（无雨、无车、无行人），无法代表真实的城市驾驶复杂性。
*   **任务局限**：属于“无指令循迹”，即智能体不知道目的地，只是在固定路线上跑。这与实际的“导航到目的地”任务有差距。
*   **泛化范围**：仅在 4 个城镇间测试，样本量较小，尚不清楚该结论是否能推广到更多样化的城镇或真实世界道路。
*   **指标权衡**：虽然成功率提升，但在某些次要指标（如特定城镇的压线率）上并未全面超越所有基准方法。

（完）
