---
title: Can Tabular Foundation Models Guide Exploration in Robot Policy Learning?
authors: "Buqing Ou, Frederike Dümbgen"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27667v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于机器人策略学习和探索的表格基础模型
tldr: 针对机器人高维连续控制中局部优化易陷入局部最优且全局搜索成本高的问题，本文提出TFM-S3方法。该方法将局部更新与间歇性全局搜索相结合，利用SVD构建低维策略子空间，并引入预训练表格基础模型（TFM）作为代理模型预测收益，从而在有限的采样成本下实现大规模候选筛选。实验证明，该方法在多个基准任务上显著提升了收敛速度和最终性能。
selection_source: fresh_fetch
motivation: 旨在解决机器人连续控制中局部优化方法对初始化敏感以及全局搜索采样成本过高的问题。
method: 提出TFM-S3框架，通过SVD构建策略子空间，并利用预训练表格基础模型作为代理进行低成本的全局探索与筛选。
result: 在连续控制基准测试中，TFM-S3在相同采样预算下比TD3和种群算法具有更快的收敛速度和更高的最终性能。
conclusion: 研究证明预训练基础模型可以作为强大的代理工具，有效提升机器人连续控制策略学习的样本效率。
---

## Abstract
Policy optimization in high-dimensional continuous control for robotics remains a challenging problem. Predominant methods are inherently local and often require extensive tuning and carefully chosen initial guesses for good performance, whereas more global and less initialization-sensitive search methods typically incur high rollout costs. We propose TFM-S3, a tabular hybrid local-global method for improving global exploration in robot policy learning with limited rollout cost. We interleave high-frequency local updates with intermittent rounds of global search. In each search round, we construct a dynamically updated low-dimensional policy subspace via SVD and perform iterative surrogate-guided refinement within this space. A pretrained tabular foundation model predicts candidate returns from a small context set, enabling large-scale screening with limited rollout cost. Experiments on continuous control benchmarks show that TFM-S3 consistently accelerates early-stage convergence and improves final performance compared to TD3 and population-based baselines under an identical rollout budget. These results demonstrate that foundation models are a powerful new tool for creating sample-efficient policy learning methods for continuous control in robotics.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何利用预训练的表格基础模型（Tabular Foundation Models, TFM）来引导机器人策略学习中的全局探索。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在高维连续控制任务中，传统的深度强化学习（RL）面临两难境地：基于梯度的局部优化方法（如 TD3）容易陷入局部最优且对初始化敏感；而全局搜索方法（如进化策略）虽然探索性强，但采样成本（Rollout Cost）极高。
*   **研究动机**：
    *   神经网络策略参数空间巨大（数万维），直接进行全局搜索存在“维度灾难”。
    *   有效的策略更新通常存在于低维结构化流形中。
    *   传统的代理模型（如高斯过程）在稀疏数据和高维空间下表现不佳，而预训练的表格基础模型（如 TabPFN）具有强大的小样本泛化能力，可作为高效的性能预测器。

### 2. 方法论（TFM-S3 框架）
论文提出了 **TFM-S3**（Tabular Foundation Model–guided Subspace Search），其核心思想是将局部梯度更新与间歇性的子空间全局搜索相结合：
*   **动态子空间构建**：利用最近 $Q$ 个步长的策略梯度快照构建梯度矩阵，通过奇异值分解（SVD）提取前 $r$ 个左奇异向量，定义一个动态演化的低维策略子空间。
*   **表格基础模型作为代理**：使用预训练的 **TabPFN-v2** 作为代理模型。它不需要针对特定环境调参，仅需少量的真实采样数据（Context Set）即可预测子空间内其他候选策略的预期收益。
*   **迭代细化搜索**：
    1.  在子空间内随机采样少量点进行真实环境评估，构建初始上下文集。
    2.  利用 TFM 预测大量候选点（如 256 个）的收益并进行排名。
    3.  仅对预测表现最好的候选点进行真实环境评估（Rollout），并将其加入上下文集。
    4.  重复上述过程 $T$ 次，最后将找到的最佳参数投影回原高维空间更新策略。

### 3. 实验设计
*   **实验场景**：使用了 MuJoCo 物理引擎中的三个经典连续控制基准任务：`HalfCheetah-v5`、`Ant-v5` 和 `Humanoid-v5`。
*   **基准算法（Benchmarks）**：
    *   **TD3**：标准的局部梯度优化算法。
    *   **Random Search TD3**：在相同子空间内进行随机搜索，保持与 TFM-S3 相同的采样预算（每轮 32 次评估）。
    *   **TFM-S3-TD3 (One Shot)**：消融实验版本，不进行迭代细化，仅进行一次代理预测。
*   **实验设置**：总训练预算为 100 万步，每个实验重复 5 个随机种子。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到 TFM 使用了 TabPFN-v2，子空间维度 $r$ 固定为 15，搜索周期 $M$ 为 1 万步，搜索激活前的预热期为 15 万步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单到复杂（Humanoid）的三个代表性任务，每个任务 5 个种子，提供了均值和标准差。
*   **充分性分析**：
    *   **对比公平性**：所有对比方法在相同的采样预算（Rollout Budget）下运行，确保了性能提升来自于搜索策略的优化而非更多的环境交互。
    *   **消融实验**：通过 One-shot 版本的对比，证明了“迭代细化”机制的必要性。
    *   **预测准确性分析**：专门分析了 TFM 的排名一致性（Spearman 相关系数）和决策质量，验证了基础模型在强化学习闭环中的有效性。

### 6. 主要结论与发现
*   **性能提升**：TFM-S3 在所有任务中均显著加速了早期收敛，并提升了最终的策略性能。
*   **样本效率**：在相同的环境交互次数下，利用 TFM 引导的搜索比纯随机搜索更有效。
*   **基础模型的可靠性**：实验证明 TabPFN 即使在只有 32 个样本的情况下，也能在 15 维子空间内提供高质量的排名预测（63.5% 的预测最优选处于真实排名的前 20%）。
*   **稳定性**：相比于 vanilla TD3，TFM-S3 在不同随机种子间的表现更加稳定（方差更小）。

### 7. 优点
*   **即插即用**：作为一种模块化增强手段，可以集成到任何基于梯度的 RL 算法（如 SAC, PPO）中。
*   **无需调参**：利用预训练模型的归纳偏置，避免了传统代理模型（如 GP）复杂的核函数选择和超参数调节。
*   **动态适应**：通过 SVD 动态更新子空间，使全局搜索能够紧跟策略优化的轨迹。

### 8. 不足与局限
*   **预热期依赖**：在训练初期（策略极不稳定时），TFM 的预测准确性较低，因此需要较长的预热期（15 万步）后才能激活。
*   **计算开销**：虽然节省了环境采样成本，但引入了 SVD 计算和 Transformer 推理的额外计算开销（文中未详细量化此部分时间成本）。
*   **子空间维度限制**：子空间维度 $r$ 是手动固定的（$r=15$），对于极度复杂的任务，可能需要更自适应的维度选择机制。
*   **环境覆盖**：目前仅在 MuJoCo 模拟环境下验证，尚未在具有高观测噪声或真实机器人硬件上进行测试。

（完）
