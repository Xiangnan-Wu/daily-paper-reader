---
title: "HERMES++: Toward a Unified Driving World Model for 3D Scene Understanding and Generation"
title_zh: HERMES++：迈向用于 3D 场景理解与生成的统一驾驶世界模型
authors: "Xin Zhou, Dingkang Liang, Xiwu Chen, Feiyang Tan, Dingyuan Zhang, Hengshuang Zhao, Xiang Bai"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28196v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于3D场景理解和未来几何预测的统一驾驶世界模型
tldr: HERMES++ 是一款统一的驾驶世界模型，旨在解决现有模型在 3D 场景理解与未来几何预测之间的脱节问题。它通过 BEV 表示将多视图空间信息与大语言模型（LLM）结合，并引入 LLM 增强查询和时空链接机制，实现了语义理解与物理模拟的协同。该模型在 3D 场景理解和未来点云预测任务上均优于专门化模型，为自动驾驶提供了更全面的环境模拟能力。
selection_source: fresh_fetch
motivation: 现有的驾驶世界模型往往侧重于生成而忽视理解，且大语言模型缺乏预测未来几何演变的能力，导致语义解释与物理模拟之间存在断层。
method: 提出一种集成 BEV 表示、LLM 增强查询、当前至未来链接以及联合几何优化策略的统一框架，实现理解与生成的协同。
result: 在多个基准测试中表现出色，在未来点云预测和 3D 场景理解任务上均超越了现有的专门化方法。
conclusion: HERMES++ 成功弥合了语义理解与物理模拟的差距，证明了统一模型在处理复杂驾驶环境动态方面的有效性。
---

## 摘要
驾驶世界模型通过模拟环境动力学，成为自动驾驶的一项关键技术。然而，现有方法主要集中在未来场景生成，往往忽略了全面的 3D 场景理解。相反，虽然大语言模型（LLMs）展示了令人印象深刻的推理能力，但它们缺乏预测未来几何演变的能力，导致语义解释与物理模拟之间存在显著差距。为了弥补这一差距，我们提出了 HERMES++，这是一个在单一框架内集成 3D 场景理解和未来几何预测的统一驾驶世界模型。我们的方法通过协同设计解决了这些任务的不同需求。首先，BEV（鸟瞰图）表示将多视图空间信息整合为与 LLM 兼容的结构。其次，我们引入了 LLM 增强的世界查询，以促进理解分支的知识迁移。第三，设计了一个“当前到未来”链接（Current-to-Future Link）来弥合时间跨度，使几何演变以语义上下文为条件。最后，为了增强结构完整性，我们采用了联合几何优化策略，将显式几何约束与隐式潜变量正则化相结合，使内部表示与几何感知先验保持一致。在多个基准测试上的广泛评估验证了我们方法的有效性。HERMES++ 表现出色，在未来点云预测和 3D 场景理解任务中均优于专门的方法。模型和代码将在 https://github.com/H-EmbodVis/HERMESV2 公开发布。

## Abstract
Driving world models serve as a pivotal technology for autonomous driving by simulating environmental dynamics. However, existing approaches predominantly focus on future scene generation, often overlooking comprehensive 3D scene understanding. Conversely, while Large Language Models (LLMs) demonstrate impressive reasoning capabilities, they lack the capacity to predict future geometric evolution, creating a significant disparity between semantic interpretation and physical simulation. To bridge this gap, we propose HERMES++, a unified driving world model that integrates 3D scene understanding and future geometry prediction within a single framework. Our approach addresses the distinct requirements of these tasks through synergistic designs. First, a BEV representation consolidates multi-view spatial information into a structure compatible with LLMs. Second, we introduce LLM-enhanced world queries to facilitate knowledge transfer from the understanding branch. Third, a Current-to-Future Link is designed to bridge the temporal gap, conditioning geometric evolution on semantic context. Finally, to enforce structural integrity, we employ a Joint Geometric Optimization strategy that integrates explicit geometric constraints with implicit latent regularization to align internal representations with geometry-aware priors. Extensive evaluations on multiple benchmarks validate the effectiveness of our method. HERMES++ achieves strong performance, outperforming specialist approaches in both future point cloud prediction and 3D scene understanding tasks. The model and code will be publicly released at https://github.com/H-EmbodVis/HERMESV2.

---

## 论文详细总结（自动生成）

这篇论文提出了 **HERMES++**，一个旨在统一自动驾驶中 **3D 场景理解**（语义层面）与**未来几何预测**（物理层面）的驾驶世界模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的自动驾驶世界模型存在“理解”与“生成”的脱节。生成式模型（如预测未来点云）虽能模拟物理演变，但缺乏语义推理能力；而基于大语言模型（LLM）的驾驶模型虽擅长场景解释，却无法预测精确的未来 3D 几何变化。
*   **核心目标**：构建一个统一框架，使语义推理能够指导几何演变，同时几何预测能为语言生成提供物理支撑，从而实现更可靠、可解释的自动驾驶系统。

### 2. 方法论
HERMES++ 采用了一个多阶段协同设计的架构：
*   **BEV 视觉分词器 (Visual Tokenizer)**：将多视图图像转换为鸟瞰图（BEV）表示，通过下采样和展平操作，将其转化为 LLM 可处理的视觉 Token，既保留了空间结构又兼顾了计算效率。
*   **LLM 增强的世界查询 (World Queries)**：在 LLM 输入序列中插入可学习的查询向量。这些查询通过因果注意力机制吸收 LLM 内部的世界知识和当前场景的语义上下文。
*   **当前至未来链接 (Current-to-Future Link)**：
    *   **文本注入 (Textual Injection)**：将 LLM 处理后的文本嵌入作为条件信号，直接调制生成过程。
    *   **自我运动调制 (Ego Modulation)**：根据未来车辆运动参数动态调整特征分布，实现运动与场景动力学的解耦。
*   **联合几何优化策略 (Joint Geometric Optimization)**：
    *   **显式约束**：对渲染出的点云进行 L1 深度损失监督。
    *   **隐式正则化**：引入预训练的几何特征提取器，通过余弦相似度损失 ($L_{cos}$) 和 Gram 矩阵损失 ($L_{gram}$) 约束潜空间特征，确保其符合 3D 几何先验，消除透视投影带来的伪影。

### 3. 实验设计
*   **数据集**：
    *   **nuScenes**：用于几何表示学习和点云预测。
    *   **OmniDrive-nuScenes**：用于场景描述和视觉问答（VQA）。
    *   **nuScenes-QA & DriveLM**：验证空间感知和逻辑推理的泛化能力。
    *   **NuInteract**：用于视觉-语言的初步对齐预训练。
*   **对比方法**：
    *   **生成专家模型**：ViDAR, DriveX, 4D-Occ 等。
    *   **理解专家模型**：LLaVA-OV, OmniDrive, ORION 等。
*   **评价指标**：Chamfer Distance (CD) 衡量几何精度；CIDEr, METEOR, ROUGE-L 衡量文本理解质量。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到了三个训练阶段（对齐、精调、统一训练），总 Batch Size 在 32 到 128 之间，训练轮数（Epochs）从 3 到 36 不等。使用了 LoRA 技术对 LLM 进行高效微调。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流自动驾驶数据集上进行了测试，涵盖了生成、理解、推理和规划（Planning）四种任务。
*   **消融实验**：非常充分。针对 BEV 分辨率、几何优化策略的各组件（$L_{cos}, L_{gram}$）、链接模块的功能（文本注入、运动调制）、查询初始化方式以及模型规模（0.8B 到 3.8B）均做了详细对比。
*   **客观性**：通过与多种 SOTA（当前最佳）专家模型对比，证明了统一模型在不使用辅助监督（如 3D 检测框）的情况下，依然能超越专门化模型，实验设计客观公平。

### 6. 主要结论与发现
*   **统一胜过单一**：在单一框架内同时训练理解和生成任务，可以产生正向协同效应，提升两者的性能。
*   **BEV 是最佳接口**：相比直接输入多视图图像 Token，BEV 表示能更好地保持空间拓扑，是连接 LLM 与 3D 物理世界的理想桥梁。
*   **语义引导有效性**：通过文本注入，LLM 的推理结果能显著降低未来几何预测的误差。
*   **隐式正则化至关重要**：仅靠点云渲染监督不足以学习到完美的潜空间表示，隐式几何约束能有效消除“射线状”伪影。

### 7. 优点
*   **架构优雅**：成功将 LLM 的认知能力与 3D 几何的模拟能力无缝集成。
*   **高性能与低监督**：不依赖昂贵的 3D 检测或地图标注，仅靠自监督几何预测和指令微调即可达到 SOTA。
*   **可扩展性**：实验证明模型性能随参数量增加（从 1.8B 到 3.8B）持续提升，具备基础模型（Foundation Model）的潜力。

### 8. 不足与局限
*   **实时性挑战**：虽然 BEV 压缩了 Token，但引入 LLM 后的推理延迟可能仍难以满足高频自动驾驶控制的需求（文中未详细讨论推理耗时）。
*   **模态局限**：目前主要关注点云生成，尚未扩展到全景视频生成等更直观的视觉模态。
*   **长尾场景覆盖**：尽管在标准数据集表现优异，但在极端天气或罕见交通冲突中的鲁棒性仍需进一步验证。

（完）
