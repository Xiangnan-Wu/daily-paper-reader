---
title: "PhyCo: Learning Controllable Physical Priors for Generative Motion"
title_zh: PhyCo：学习生成式运动的可控物理先验
authors: "Sriram Narayanan, Ziyu Jiang, Srinivasa Narasimhan, Manmohan Chandraker"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28169v1"
tags: ["query:q16"]
score: 10.0
evidence: 使用具有可控先验的视频扩散模型进行物理世界模拟
tldr: PhyCo 针对视频扩散模型在物理一致性（如碰撞、摩擦、材质反应）方面的不足，提出了一种学习可控物理先验的框架。通过构建包含10万个物理仿真视频的大规模数据集，并结合物理监督的微调与视觉语言模型（VLM）引导的奖励优化，PhyCo 实现了无需推理时仿真的物理可控视频生成，显著提升了生成视频的物理真实感与可解释性。
selection_source: fresh_fetch
motivation: 现有的视频扩散模型在生成过程中难以保持物理一致性，且缺乏对摩擦、弹力和力等底层物理属性的精确控制。
method: 通过大规模物理仿真数据集微调扩散模型，并利用 ControlNet 注入物理属性图，结合 VLM 引导的奖励优化提供微分反馈。
result: 在 Physics-IQ 基准测试中，PhyCo 显著提升了物理真实感，并在人类评估中展现出对物理属性更清晰、更忠实的控制能力。
conclusion: 该研究证明了通过学习物理先验可以实现无需在线仿真的、可扩展且物理一致的可控视频生成。
---

## 摘要
现代视频扩散模型在外观合成方面表现出色，但在物理一致性方面仍面临挑战：物体漂移、碰撞缺乏真实的弹性回馈，且材料响应很少与其底层属性相匹配。我们提出了 PhyCo，这是一个将连续、可解释且具有物理依据的控制引入视频生成的框架。我们的方法整合了三个关键组件：(i) 一个包含超过 10 万个高保真模拟视频的大规模数据集，其中摩擦力、恢复系数、变形和力在各种场景中系统地变化；(ii) 使用以像素对齐的物理属性图为条件的 ControlNet，对预训练扩散模型进行物理监督微调；以及 (iii) VLM 引导的奖励优化，其中经过微调的视觉语言模型通过针对性的物理查询评估生成的视频，并提供可微分的反馈。这种结合使生成模型能够通过物理属性的变化产生物理一致且可控的输出，且在推理时无需任何模拟器或几何重建。在 Physics-IQ 基准测试中，PhyCo 相比强基线模型显著提高了物理真实性，人类研究也证实了其对物理属性的控制更加清晰和忠实。我们的结果展示了一条通往物理一致、可控的生成式视频模型的可扩展路径，这些模型能够泛化到合成训练环境之外。

## Abstract
Modern video diffusion models excel at appearance synthesis but still struggle with physical consistency: objects drift, collisions lack realistic rebound, and material responses seldom match their underlying properties. We present PhyCo, a framework that introduces continuous, interpretable, and physically grounded control into video generation. Our approach integrates three key components: (i) a large-scale dataset of over 100K photorealistic simulation videos where friction, restitution, deformation, and force are systematically varied across diverse scenarios; (ii) physics-supervised fine-tuning of a pretrained diffusion model using a ControlNet conditioned on pixel-aligned physical property maps; and (iii) VLM-guided reward optimization, where a fine-tuned vision-language model evaluates generated videos with targeted physics queries and provides differentiable feedback. This combination enables a generative model to produce physically consistent and controllable outputs through variations in physical attributes-without any simulator or geometry reconstruction at inference. On the Physics-IQ benchmark, PhyCo significantly improves physical realism over strong baselines, and human studies confirm clearer and more faithful control over physical attributes. Our results demonstrate a scalable path toward physically consistent, controllable generative video models that generalize beyond synthetic training environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PhyCo** 的框架，旨在为生成式视频模型引入可控且符合物理规律的先验知识。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
当前的视频生成模型（如扩散模型）虽然在视觉外观和纹理合成上表现惊人，但在**物理一致性**方面存在显著缺陷：物体经常出现非自然的漂移、碰撞后缺乏真实的弹性回馈、材质反应与其实际物理属性不符。
*   **研究动机**：现有的物理增强方法要么依赖于推理时的复杂仿真器（限制了速度和泛化性），要么仅提供粗略的语义引导。PhyCo 旨在通过学习，使模型能够直接根据摩擦力、恢复系数等物理参数生成连续、可控且物理一致的运动，且在推理时无需仿真器。

### 2. 方法论
PhyCo 采用了一个两阶段的训练流程：
*   **大规模物理仿真数据集构建**：利用 Kubric 和 PyBullet 生成了超过 10 万个高保真视频，涵盖 8 种场景（如滑行、碰撞、变形体掉落等）。每个视频都标注了精确的物理属性：摩擦力（Friction）、恢复系数（Restitution）、变形参数（Deformation）和外力（Force）。
*   **物理监督微调（Stage 1）**：
    *   使用 **ControlNet** 架构对预训练的视频扩散模型（Cosmos-Predict2）进行微调。
    *   **核心思想**：将物理属性编码为“空间对齐的物理属性图”（以圆形 Blob 形式表示物体位置及其对应的物理参数值），作为 ControlNet 的输入。
*   **VLM 引导的奖励优化（Stage 2）**：
    *   引入一个经过微调的视觉语言模型（VLM，如 Qwen2.5-VL）作为“物理评论家”。
    *   **流程**：模型生成视频后，VLM 针对物理行为提出特定问题（如“物体是否向受力方向移动？”）。利用 VLM 输出的 Logits 计算可微分的奖励信号，通过反向传播进一步优化 ControlNet，增强物理属性的忠实度。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：自建的 100K+ 物理仿真视频集。
    *   **测试集**：Physics-IQ 基准测试（包含固体力学、流体力学、光学、磁学、热力学五个领域）以及 25 个真实世界视频。
*   **对比方法**：包括 Cosmos-Predict2、CogVideoX-I2V、SVD-XT、LTX-Video 等基础模型，以及专门针对物理控制的方法如 Force-Prompting 和 VLIPP。
*   **评估指标**：Physics-IQ 分数、FVMD（运动一致性度量）、人类偏好研究（2AFC）、物理属性预测误差。

### 4. 资源与算力
论文详细列出了训练所需的算力资源：
*   **第一阶段（ControlNet 微调）**：使用 **4 张 NVIDIA H100 GPU**，每个属性分支训练约 10,000 步，耗时约 **0.5 天**。
*   **第二阶段（VLM 奖励优化）**：使用 **8 张 NVIDIA H200 GPU**，训练 100 个迭代，耗时约 **70 分钟**。峰值显存占用约为 115 GB。
*   **VLM 微调**：使用 4 张 H100 GPU 训练 200 个迭代。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的定量评估（Physics-IQ 全集）、定性对比、消融实验（验证 VLM 奖励的作用）以及人类主观评价（16 名参与者，39 组视频对比）。
*   **充分性与公平性**：实验涵盖了从合成数据到真实世界场景的泛化测试，并对比了多种最先进的开源模型。通过在不同架构（如 Wan2.2）上进行微调，验证了该方法作为一种通用物理先验学习路径的有效性，实验设计较为客观全面。

### 6. 主要结论与发现
*   **物理真实感提升**：PhyCo 在 Physics-IQ 基准测试中显著优于所有基线模型，尤其在固体力学和热力学方面表现突出。
*   **精确可控性**：通过调整输入属性图，模型能实现对摩擦力、弹力等参数的连续控制，且生成的运动轨迹与物理预期高度一致。
*   **强泛化能力**：尽管只在简单几何体的仿真数据上训练，模型能泛化到复杂物体（如蹦床上的女孩、艺术风格场景），并支持多种物理属性的组合控制。

### 7. 优点（亮点）
*   **端到端可控性**：首次在视频生成中实现了对多种底层物理参数（而非仅仅是运动轨迹）的连续、定量控制。
*   **无需推理仿真**：将物理规律内化在模型权重中，推理速度快，摆脱了对几何重建和物理求解器的依赖。
*   **VLM 反馈机制**：创新性地利用微调后的 VLM 提供物理一致性奖励，有效弥补了扩散损失函数在捕捉高层物理逻辑方面的不足。

### 8. 不足与局限
*   **物理近似而非精确模拟**：生成的动力学仍是真实物理的近似，对于极复杂的交互（如流固耦合、多体关节运动）建模尚不完善。
*   **守恒定律约束**：模型并未在架构上强制执行动量或能量守恒，偶尔会出现细微的物理偏差。
*   **算力需求高**：第二阶段的 VLM 引导训练对显存和算力要求极高（需 H200 级别设备），限制了其在大规模参数模型上的快速迭代。

（完）
