---
title: "AIM: Intent-Aware Unified world action Modeling with Spatial Value Maps"
title_zh: AIM：基于空间价值图的意图感知统一世界动作建模
authors: "Liaoyuan Fan, Zetian Xu, Chen Cao, Wenyao Zhang, Mingqi Yuan, Jiayu Chen"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11135v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人控制的意图感知统一世界动作模型
tldr: "针对预训练视频模型在机器人动作生成中存在的结构性失配问题，本文提出了AIM模型。AIM通过引入显式的空间价值图作为中间接口，将视觉动态演化与操作意图对齐。该模型基于混合Transformer架构，利用意图因果注意力机制引导动作生成，并通过自蒸馏强化学习优化动作头。在RoboTwin 2.0基准测试中，AIM实现了94.0%的成功率，显著提升了长程及接触敏感任务的表现。"
selection_source: fresh_fetch
motivation: 现有的统一世界动作模型难以直接从视频表征中解码可靠动作，因为视频模型关注场景演化而动作生成需要明确的交互意图和空间推理。
method: 提出一种意图感知的统一模型，通过预测空间价值图来编码任务相关的交互结构，并结合混合Transformer架构与自蒸馏强化学习进行训练。
result: "在RoboTwin 2.0基准测试中达到94.0%的平均成功率，在长程和接触敏感型任务上表现尤为突出，大幅超越了现有基线模型。"
conclusion: 显式的空间意图建模是连接视觉世界建模与机器人控制的有效桥梁，能够显著增强世界模型在复杂操作任务中的可靠性。
---

## 摘要
预训练视频生成模型为机器人控制提供了强大的先验，但现有的统一世界动作模型在没有大量机器人特定训练的情况下，仍难以解码出可靠的动作。我们将这一局限性归因于结构性失配：虽然视频模型捕捉了场景的演变方式，但动作生成需要对交互位置和潜在的操作意图进行显式推理。我们提出了 AIM，一种意图感知的统一世界动作模型，通过显式的空间接口弥合了这一差距。AIM 并非直接从未来的视觉表示中解码动作，而是预测一个对齐的空间价值图，该图编码了与任务相关的交互结构，从而实现了面向控制的未来动力学抽象。AIM 基于预训练视频生成模型，在共享的混合 Transformer（mixture-of-transformers）架构中对未来观测和价值图进行联合建模。它采用意图因果注意力，仅通过价值表示将未来信息路由到动作分支。我们进一步提出了一个自蒸馏强化学习阶段，该阶段冻结视频和价值分支，并利用源自投影价值图响应的稠密奖励以及稀疏的任务级信号，仅优化动作头。为了支持训练和评估，我们构建了一个包含 3 万条操作轨迹的仿真数据集，其中包含同步的多视图观测、动作和价值图标注。在 RoboTwin 2.0 基准测试上的实验表明，AIM 达到了 94.0% 的平均成功率，显著优于之前的统一世界动作基准。值得注意的是，在长时程和接触敏感的操作任务中，这种提升更为显著，证明了显式空间意图建模作为视觉世界建模与机器人控制之间桥梁的有效性。

## Abstract
Pretrained video generation models provide strong priors for robot control, but existing unified world action models still struggle to decode reliable actions without substantial robot-specific training. We attribute this limitation to a structural mismatch: while video models capture how scenes evolve, action generation requires explicit reasoning about where to interact and the underlying manipulation intent. We introduce AIM, an intent-aware unified world action model that bridges this gap via an explicit spatial interface. Instead of decoding actions directly from future visual representations, AIM predicts an aligned spatial value map that encodes task-relevant interaction structure, enabling a control-oriented abstraction of future dynamics. Built on a pretrained video generation model, AIM jointly models future observations and value maps within a shared mixture-of-transformers architecture. It employs intent-causal attention to route future information to the action branch exclusively through the value representation. We further propose a self-distillation reinforcement learning stage that freezes the video and value branches and optimizes only the action head using dense rewards derived from projected value-map responses together with sparse task-level signals. To support training and evaluation, we construct a simulation dataset of 30K manipulation trajectories with synchronized multi-view observations, actions, and value-map annotations. Experiments on RoboTwin 2.0 benchmark show that AIM achieves a 94.0% average success rate, significantly outperforming prior unified world action baselines. Notably, the improvement is more pronounced in long-horizon and contact-sensitive manipulation tasks, demonstrating the effectiveness of explicit spatial-intent modeling as a bridge between visual world modeling and robot control.

---

## 论文详细总结（自动生成）

以下是对论文《AIM: Intent-Aware Unified world action Modeling with Spatial Value Maps》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的“统一世界动作模型”（Unified World Action Models）虽然能利用预训练视频生成模型的强大先验，但在解码可靠的机器人动作时仍面临困难。
*   **研究动机**：作者认为这源于一种**结构性失配**。视频模型关注场景如何演变（“看起来像什么”），而机器人控制需要明确推理“在哪里交互”以及“操作意图是什么”。直接从稠密的未来视觉表征中提取稀疏的动作信号非常困难，因为视觉表征中充斥着大量与控制无关的外观细节。
*   **整体含义**：提出一种意图感知的模型 AIM，通过引入**空间价值图（Spatial Value Maps）**作为中间接口，将视觉预测与动作生成解耦并对齐，从而提升控制的可靠性。

### 2. 方法论
AIM 的核心思想是在预测未来视频的同时，预测一个对齐的空间价值图，并以此引导动作生成。
*   **核心架构**：基于预训练视频生成模型 **Wan2.2-TI2V-5B**，采用**混合 Transformer（Mixture-of-Transformers）**架构。视频分支预测未来 RGB 帧，价值分支预测空间价值图，动作分支预测连续控制向量。
*   **空间价值图（SVM）**：作为显式接口，SVM 编码了任务相关的交互结构（如抓取点、放置区域）。它与 RGB 帧空间对齐，将复杂的视觉动力学抽象为面向控制的先验。
*   **意图因果注意力（Intent-Causal Attention）**：设计了一种特殊的注意力掩码。未来视频 token 可以查看历史和指令；未来价值 token 可以查看未来视频；而**动作 token 只能通过未来价值 token 获取未来信息**，被禁止直接访问未来 RGB token。这强制模型通过“意图”路径进行推理。
*   **两阶段训练流程**：
    1.  **阶段 I（监督学习）**：联合训练未来帧生成、价值图估计和动作预测。
    2.  **阶段 II（自蒸馏强化学习）**：冻结视频和价值分支，仅使用 **GRPO 算法**优化动作头。奖励函数结合了环境稀疏奖励和基于预测价值图的稠密奖励（即动作落点是否在价值图的高亮区域）。

### 3. 实验设计
*   **数据集**：构建了一个大规模仿真数据集，包含 **30,000 条操作轨迹**，涵盖同步的多视图视频、动作序列及自动生成的价值图标注（基于物理引擎的接触检测）。
*   **基准测试（Benchmark）**：在 **RoboTwin 2.0** 基准上进行评估，包含 50 个操作任务，分为 **Easy** 和 **Hard** 两种设置。
*   **对比方法**：对比了多个强力基线模型，包括：
    *   VLA 模型：$\pi_{0}$、$\pi_{0.5}$、X-VLA。
    *   世界动作模型：Motus、Fast-WAM、GigaWorld-Policy、LingBot-VA。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **模型规模**：提到视频骨干网络基于 5B 参数的 Wan2.2 模型，动作头深度相同但隐藏层宽度较小，这暗示了该模型对显存和计算资源有较高要求。

### 5. 实验数量与充分性
*   **实验规模**：在 50 个不同的仿真任务上进行了测试，每个任务都有 Easy/Hard 两种配置，实验覆盖面较广。
*   **消融实验**：通过对比“Stage 1”（仅监督学习）和完整 AIM 模型，验证了 RL 后训练的效果。
*   **充分性评价**：实验设计较为充分，涵盖了从简单拾取到复杂的长程、接触敏感型任务（如“打开笔记本电脑”、“旋转二维码”）。对比基线均为当前领域内最前沿的模型，评估标准客观。

### 6. 主要结论与发现
*   **性能领先**：AIM 在 RoboTwin 2.0 上实现了 **94.0% (Easy)** 和 **92.1% (Hard)** 的平均成功率，显著优于所有外部基线。
*   **空间意图的重要性**：在接触敏感和多阶段任务中提升最明显，证明了显式空间价值图能有效弥合视觉预测与物理控制之间的鸿沟。
*   **自蒸馏 RL 的有效性**：利用模型自身预测的价值图作为稠密奖励，可以在不破坏预训练视频先验的前提下，显著提升动作头的闭环执行能力。

### 7. 优点（亮点）
*   **架构创新**：通过意图因果注意力机制，巧妙地在统一生成框架中实现了“感知-意图-动作”的逻辑流。
*   **可解释性**：预测的价值图为动作生成提供了直观的视觉解释，用户可以看到机器人“想”去哪里交互。
*   **高效推理**：支持 KV 缓存（KV Caching），提高了长程任务的自回归生成效率。

### 8. 不足与局限
*   **仿真依赖**：所有实验和数据均在 RoboTwin 仿真环境中完成，**缺乏真实世界（Real-to-Sim）的迁移验证**。
*   **标注成本**：虽然仿真中可以自动生成价值图，但在真实世界数据集中获取高质量的空间价值图标注（Affordance Map）可能非常昂贵或困难。
*   **算力门槛**：基于 5B 参数的视频扩散模型进行联合推理，其实时部署性能可能受限，且训练成本较高。

（完）
