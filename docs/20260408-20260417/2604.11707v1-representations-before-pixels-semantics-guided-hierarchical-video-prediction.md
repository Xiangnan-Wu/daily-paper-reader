---
title: "Representations Before Pixels: Semantics-Guided Hierarchical Video Prediction"
title_zh: 表征先于像素：语义引导的分层视频预测
authors: "Efstathios Karypidis, Spyros Gidaris, Nikos Komodakis"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11707v1"
tags: ["query:q12"]
score: 9.0
evidence: 用于自动驾驶的分层视频预测
tldr: 针对自动驾驶等复杂场景中视频预测的语义一致性挑战，本文提出Re2Pix分层预测框架。该方法将预测任务分解为语义表示预测和视觉合成两个阶段，先在视觉大模型的特征空间预测未来场景结构，再利用潜在扩散模型渲染图像。通过引入嵌套丢弃和混合监督策略，有效解决了自回归预测中的训练-测试偏差问题，显著提升了视频的语义一致性、感知质量和训练效率。
selection_source: fresh_fetch
motivation: 传统的直接像素预测方法在复杂动态环境中难以同时维持高视觉保真度和长期的场景语义一致性。
method: 采用分层架构，先预测冻结视觉大模型的特征表示以捕捉场景动力学，再以此为条件通过潜在扩散模型合成像素。
result: 在自动驾驶基准测试上，该方法在语义一致性、感知质量和训练效率方面均显著优于现有的强扩散模型基线。
conclusion: “语义优先”的设计通过解耦动力学建模与外观生成，为实现高质量且语义连贯的视频预测提供了有效方案。
---

## 摘要
准确的未来视频预测需要高视觉保真度和一致的场景语义，特别是在自动驾驶等复杂的动态环境中。我们提出了 Re2Pix，这是一个分层视频预测框架，它将预测分解为两个阶段：语义表征预测和表征引导的视觉合成。我们的方法不是直接预测未来的 RGB 帧，而是首先在冻结的视觉基础模型的特征空间中预测未来的场景结构，然后以这些预测的表征为条件，利用潜在扩散模型渲染出逼真的图像帧。这种分解使模型能够首先关注场景动态，然后再关注外观生成。一个关键挑战源于训练期间可用的真实值（ground-truth）表征与推理时使用的预测表征之间的训练-测试不匹配。为了解决这个问题，我们引入了两种条件策略：嵌套丢弃（nested dropout）和混合监督（mixed supervision），从而提高了对不完美自回归预测的鲁棒性。在具有挑战性的驾驶基准测试上的实验表明，与强大的扩散基准模型相比，所提出的语义优先设计显著提高了时间语义一致性、感知质量和训练效率。我们在 https://github.com/Sta8is/Re2Pix 提供了实现代码。

## Abstract
Accurate future video prediction requires both high visual fidelity and consistent scene semantics, particularly in complex dynamic environments such as autonomous driving. We present Re2Pix, a hierarchical video prediction framework that decomposes forecasting into two stages: semantic representation prediction and representation-guided visual synthesis. Instead of directly predicting future RGB frames, our approach first forecasts future scene structure in the feature space of a frozen vision foundation model, and then conditions a latent diffusion model on these predicted representations to render photorealistic frames. This decomposition enables the model to focus first on scene dynamics and then on appearance generation. A key challenge arises from the train-test mismatch between ground-truth representations available during training and predicted ones used at inference. To address this, we introduce two conditioning strategies, nested dropout and mixed supervision, that improve robustness to imperfect autoregressive predictions. Experiments on challenging driving benchmarks demonstrate that the proposed semantics-first design significantly improves temporal semantic consistency, perceptual quality, and training efficiency compared to strong diffusion baselines. We provide the implementation code at https://github.com/Sta8is/Re2Pix

---

## 论文详细总结（自动生成）

这是一份关于论文《Representations Before Pixels: Semantics-Guided Hierarchical Video Prediction》（Re2Pix）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶等复杂动态场景中，视频预测要求同时具备**高视觉保真度**和**长期的语义一致性**。目前的端到端扩散模型（如在 VAE 潜在空间直接预测）往往将场景动力学（语义）与外观细节（像素）纠缠在一起，导致预测过程中出现物体身份漂移、结构崩溃或闪烁伪影。
**研究动机**：探索是否可以将语义预测与视觉合成显式分离。通过“先预测语义结构，再渲染像素细节”的分层方式，降低模型学习难度，提高预测的逻辑连贯性。

### 2. 方法论：核心思想与关键技术
Re2Pix 将视频预测分解为两个阶段：
*   **阶段 1：高层语义预测**
    *   使用冻结的视觉基础模型（VFM，如 DINOv2）提取上下文帧的语义特征。
    *   采用一个轻量级的**掩码特征 Transformer（Masked Feature Transformer）**，在特征空间内以自回归方式预测未来帧的语义表征。
*   **阶段 2：语义引导的视觉生成**
    *   使用潜在扩散模型（Latent Diffusion Model, DiT 架构）生成未来帧。
    *   **核心创新（鲁棒性策略）**：为了解决训练时使用真实特征（GT）而推理时使用预测特征（带噪声）导致的“训练-测试不匹配”问题，提出了：
        1.  **嵌套丢弃（Nested Dropout）**：在训练时随机截断 PCA 降维后的特征通道，迫使模型不依赖细微特征，增强对预测误差的容忍度。
        2.  **混合监督（Mixed Supervision）**：以 90% 真实特征和 10% 预测特征的比例混合训练，让生成器提前适应不完美的输入。
*   **融合方式**：采用早期融合（Early Fusion），将 VFM 特征与 VAE 潜在变量在输入层进行通道级拼接或求和。

### 3. 实验设计
*   **数据集**：Cityscapes（城市街景）、nuScenes（自动驾驶多模态）、CoVLA（大规模驾驶视频）以及用于零 shot 泛化测试的 KITTI。
*   **基准模型（Benchmarks）**：
    *   标准扩散模型基线（Baseline）。
    *   表征对齐方法：REPA 和 VideoREPA。
    *   大规模预训练系统：Vista 和 Cosmos-Predict-2（在相同设置下微调）。
*   **评估指标**：
    *   **语义一致性**：mIoU（分割准确率）、AbsRel/$\delta_1$（深度估计准确率）。
    *   **生成质量**：FID（空间真实感）、FVD（时间连贯性）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练时长**：
    *   单数据集实验（如 Cityscapes）：约 **7 小时**（40k 次迭代）。
    *   多数据集联合训练：约 **28 小时**（120k 次迭代）。
*   **模型规模**：扩散 Transformer 约为 8 亿参数（800M），整体框架（含 VFM 和 VAE）约 11 亿参数。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。涵盖了从单数据集训练到大规模多数据集联合训练，并进行了跨数据集的零样本（Zero-shot）泛化测试。
*   **消融实验**：详细探讨了嵌套丢弃、混合监督比例、PCA 组件数量、以及不同 VFM 编码器（DINOv2 vs SigLIP-2）对结果的影响。
*   **客观性**：针对扩散模型的随机性，进行了 3 次独立运行并报告了均值和标准差，确保了结果的公平性。

### 6. 主要结论与发现
*   **性能领先**：Re2Pix 在语义一致性和图像质量上均显著优于 REPA 等强基线。
*   **训练加速**：相比传统扩散模型，Re2Pix 在达到相同 FID/FVD 水平时实现了 **7 倍的训练加速**，在语义指标上实现了 **14 倍的加速**。
*   **分层优势**：显式建模语义结构能有效防止物体在长序列预测中的“身份丢失”。
*   **鲁棒性**：嵌套丢弃和混合监督是弥合分层架构中预测误差的关键。

### 7. 优点
*   **架构优雅**：解耦了“逻辑预测”与“图像渲染”，符合人类视觉认知的逻辑。
*   **效率极高**：通过引入 VFM 的先验知识，大幅降低了扩散模型从头训练的收敛时间。
*   **泛化性强**：在未见过的 KITTI 数据集上表现出优异的零样本预测能力。
*   **即插即用**：不依赖特定的 VFM，具有良好的模块化特性。

### 8. 不足与局限
*   **领域局限**：实验主要集中在自动驾驶（驾驶视角）领域，尚未在更通用的视频生成（如自然场景、人类动作）中验证。
*   **误差累积**：虽然有鲁棒性策略，但第一阶段自回归预测的特征误差在极长序列中仍可能累积，影响最终渲染。
*   **依赖预训练模型**：性能上限受限于所选用的 VFM（如 DINOv2）的表征能力。

（完）
