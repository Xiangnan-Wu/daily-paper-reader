---
title: "Vision-Language-Action Model, Robustness, Multi-modal Learning, Robot Manipulation"
title_zh: 视觉-语言-动作模型、鲁棒性、多模态学习与机器人操作
authors: "Yuhan Xie, Yuping Yan, Yunqi Zhao, Handing Wang, Yaochu Jin"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10055v1"
tags: ["query:robot"]
score: 10.0
evidence: 机器人操作中视觉-语言-动作（VLA）模型的鲁棒性
tldr: 针对视觉-语言-动作（VLA）模型在多模态扰动下鲁棒性差且任务保真度易受损的问题，本文提出STRONG-VLA框架。该框架采用解耦微调策略，通过第一阶段的课程学习逐步增强模型对视觉和文本噪声的抵抗力，并在第二阶段通过干净数据重新对齐以恢复执行精度。实验证明，该方法在多个VLA架构上显著提升了任务成功率，并具备良好的跨架构泛化能力和实机应用潜力。
selection_source: fresh_fetch
motivation: 现有的VLA模型在面对视觉和语言噪声时表现脆弱，且传统的联合训练方法会导致鲁棒性与任务保真度之间的优化冲突。
method: 提出一种解耦微调框架，先通过由易到难的多模态扰动课程学习获取鲁棒性，再利用干净数据进行任务对齐以恢复性能。
result: "在LIBERO基准测试中，该方法使OpenVLA、pi0等模型的任务成功率在已知和未知扰动下均获得显著提升（最高达16.49%）。"
conclusion: 解耦优化是提升具身智能模型多模态鲁棒性的有效途径，为构建稳健的机器人控制系统提供了简单且原则性的框架。
---

## 摘要
尽管视觉-语言-动作（VLA）模型在具身任务中表现出强劲的性能，但在多模态扰动下仍然非常脆弱，视觉损坏和语言噪声共同引发的分布偏移会降低任务级的执行效果。现有的鲁棒性方法通常依赖于使用扰动数据进行联合训练，将鲁棒性视为一个静态目标，这导致了鲁棒性与任务保真度之间的优化冲突。在这项工作中，我们提出了 STRONG-VLA，这是一个解耦的微调框架，它明确地将鲁棒性获取与任务对齐细化分离开来。在第一阶段，模型被暴露于难度递增的多模态扰动课程中，从而在受控的分布偏移下实现渐进式鲁棒性学习。在第二阶段，模型与干净的任务分布重新对齐，以在保持鲁棒性的同时恢复执行保真度。我们进一步建立了一个全面的基准测试，包含跨越文本和视觉模态的 28 种扰动类型，这些扰动源于传感器噪声、遮挡和指令损坏等现实来源。在 LIBERO 基准上的广泛实验表明，STRONG-VLA 在多种 VLA 架构上一致地提高了任务成功率。在 OpenVLA 上，我们的方法在已知扰动下实现了高达 12.60% 的提升，在未知扰动下实现了 7.77% 的提升。值得注意的是，在 OpenVLA-OFT（+14.48% / +13.81%）和 pi0（+16.49% / +5.58%）上观察到了类似或更大的改进，证明了强大的跨架构泛化能力。在 AIRBOT 机器人平台上的真实世界实验进一步验证了其在实际应用中的有效性。这些结果强调了解耦优化对于多模态鲁棒性的重要性，并将 STRONG-VLA 确立为一个简单且原则性的鲁棒具身控制框架。

## Abstract
Despite their strong performance in embodied tasks, recent Vision-Language-Action (VLA) models remain highly fragile under multimodal perturbations, where visual corruption and linguistic noise jointly induce distribution shifts that degrade task-level execution. Existing robustness approaches typically rely on joint training with perturbed data, treating robustness as a static objective, which leads to conflicting optimization between robustness and task fidelity. In this work, we propose STRONG-VLA, a decoupled fine-tuning framework that explicitly separates robustness acquisition from task-aligned refinement. In Stage I, the model is exposed to a curriculum of multimodal perturbations with increasing difficulty, enabling progressive robustness learning under controlled distribution shifts. In Stage II, the model is re-aligned with clean task distributions to recover execution fidelity while preserving robustness. We further establish a comprehensive benchmark with 28 perturbation types spanning both textual and visual modalities, grounded in realistic sources of sensor noise, occlusion, and instruction corruption. Extensive experiments on the LIBERO benchmark show that STRONG-VLA consistently improves task success rates across multiple VLA architectures. On OpenVLA, our method achieves gains of up to 12.60% under seen perturbations and 7.77% under unseen perturbations. Notably, similar or larger improvements are observed on OpenVLA-OFT (+14.48% / +13.81%) and pi0 (+16.49% / +5.58%), demonstrating strong cross-architecture generalization. Real-world experiments on an AIRBOT robotic platform further validate its practical effectiveness. These results highlight the importance of decoupled optimization for multimodal robustness and establish STRONG-VLA as a simple yet principled framework for robust embodied control.

---

## 论文详细总结（自动生成）

以下是对论文《STRONG-VLA: Decoupled Robustness Learning for Vision–Language–Action Models under Multimodal Perturbations》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$）在具身智能领域表现出色，但在现实部署中面临传感器噪声、指令模糊和环境遮挡等**多模态扰动**。
*   **核心问题**：现有的鲁棒性提升方法通常采用“联合训练”（同时使用干净和扰动数据），这会导致**优化冲突**：干净数据要求模型对细微信号敏感，而扰动数据要求模型对噪声保持不变性。这种冲突限制了模型在保持任务精度（保真度）的同时获取鲁棒性的能力。
*   **研究目标**：提出一种解耦的微调框架，使模型在不牺牲原始任务性能的前提下，增强对复杂多模态扰动的抵抗力。

### 2. 方法论：STRONG-VLA 框架
*   **核心思想**：将鲁棒性获取与任务对齐优化在时间上**解耦**，分为两个阶段进行。
*   **关键技术细节**：
    *   **第一阶段：课程驱动的鲁棒性获取（Stage I）**：
        *   **难度量化**：定义标量指标 $d(\delta) \in [0, 1]$。文本难度基于噪声Token占比；视觉难度基于像素偏差（光度）、遮挡比例（信息丢失）或变换幅度（几何位移）。
        *   **分层课程调度**：遵循“先文本硬化，后视觉硬化”的顺序。每个子阶段分为“热身、爬坡、硬化”三个阶段，扰动注入概率和难度上限随训练进程逐渐增加。
    *   **第二阶段：任务对齐细化（Stage II）**：
        *   使用**干净数据**对第一阶段得到的模型进行保守微调。目的是纠正第一阶段可能引入的分布偏差，恢复模型对精确任务信号的敏感度。
*   **算法流程**：初始化参数 $\theta_0 \rightarrow$ 在扰动分布 $P_1$ 下进行课程学习得到 $\theta_1 \rightarrow$ 在干净分布 $D_{clean}$ 下进行重对齐得到最终参数 $\theta^*$。

### 3. 实验设计
*   **数据集与场景**：使用 **LIBERO** 机器人操作基准测试，涵盖 Spatial（空间）、Objects（物体）、Goal（目标）和 Long（长程）四个任务套件。
*   **基准测试（Benchmark）**：构建了包含 **28 种扰动类型**的全面基准，分为：
    *   **文本模态**：对抗注入（如角色欺骗）、语言损坏（如乱码）、语义漂移、上下文干扰。
    *   **视觉模态**：光度退化（如高斯噪声）、物理遮挡、几何偏移（如旋转、平移）、动态伪影。
*   **对比方法**：
    *   **原始模型（Raw Models）**：OpenVLA-7B, OpenVLA-OFT, $\pi_0$。
    *   **鲁棒性基准方法**：BYOVLA, RobustVLA。
    *   **消融变体**：联合训练（Joint training）、无课程学习版本、无第二阶段版本。

### 4. 资源与算力
*   **硬件设备**：使用 **NVIDIA L40S GPU** 进行实验。
*   **训练配置**：
    *   采用 LoRA（秩 $r=32$）进行参数高效微调。
    *   单卡 Batch Size 为 4，梯度累积 4 步（有效 Batch Size 为 16）。
    *   使用 `bfloat16` 混合精度训练。
*   **训练时长**：第一阶段 50k 步，第二阶段 8k 步。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流 VLA 架构上进行了验证，涵盖了 28 种扰动下的任务成功率（TSR）评估。
*   **充分性评价**：
    *   **多维度验证**：不仅测试了“见过”的扰动，还测试了“未见过”的扰动（Zero-shot 鲁棒性）以及多模态组合扰动。
    *   **消融实验**：系统验证了阶段化优化和课程调度的必要性。
    *   **实机实验**：在 **AIRBOT Play** 机械臂上进行了真实场景验证（抓取并放置方块）。
    *   **客观性**：对比基准时采用了相同的扰动参数和任务实例，确保了公平性。

### 6. 主要结论与发现
*   **性能提升**：STRONG-VLA 在所有架构上均显著提升了 TSR。在 OpenVLA 上，已知扰动提升达 12.60%，未知扰动提升 7.77%；在 $\pi_0$ 上提升甚至高达 16.49%。
*   **解耦优势**：实验证明解耦训练优于联合训练，能有效缓解鲁棒性与性能之间的折中。
*   **模态差异**：文本鲁棒性的泛化能力较强，而**视觉鲁棒性在面对未见过的扰动类型时仍表现出局限性**。
*   **多模态协同**：模型能够捕捉跨模态的互补鲁棒性，在视觉和文本同时受损时表现稳健。

### 7. 优点
*   **简单且原则性强**：通过解耦优化这一简单直观的策略解决了复杂的梯度冲突问题。
*   **基准全面**：28 种扰动类型非常贴近现实物理世界的传感器失效和人为指令错误。
*   **跨架构通用性**：在基于 Transformer 的 OpenVLA 和基于 Flow 的 $\pi_0$ 上均有效，证明了方法的普适性。

### 8. 不足与局限
*   **视觉泛化瓶颈**：对于未见过的视觉扰动（如特定的几何畸变），模型的零样本泛化能力仍有待提高。
*   **人工设计依赖**：扰动分类和难度量化指标仍依赖人工设计，可能无法完全覆盖所有现实世界的不确定性。
*   **实机规模有限**：真实世界实验虽然验证了可行性，但在任务多样性和平台覆盖面上规模较小。

（完）
