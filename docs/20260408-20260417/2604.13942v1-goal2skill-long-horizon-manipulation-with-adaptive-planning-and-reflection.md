---
title: "Goal2Skill: Long-Horizon Manipulation with Adaptive Planning and Reflection"
title_zh: Goal2Skill：具有自适应规划与反思的长程操控
authors: "Zhen Liu, Xinyu Ning, Zhe Hu, Xinxin Xie, Weize Li, Zhipeng Tang, Chongyu Wang, Zejun Yang, Hanlin Wang, Yitong Liu, Zhongzhu Pu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13942v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于操作任务的视觉-语言-动作 (VLA) 系统
tldr: 针对现有VLA系统在长程、依赖记忆的任务中表现脆弱的问题，本文提出了Goal2Skill框架。该框架采用双系统架构，将高层语义推理（VLM代理负责任务分解、验证与纠错）与底层运动执行（VLA控制器负责扩散式动作生成）解耦。通过构建规划与执行的闭环，实现了具备记忆感知、自适应重规划和在线恢复能力的具身操控，在RMBench基准测试中显著超越了现有基线模型。
selection_source: fresh_fetch
motivation: 现有的端到端VLA策略在处理具有部分可观测性、遮挡和多阶段依赖的长程任务时，缺乏持久记忆和显式的错误恢复能力。
method: 提出一种双系统框架，结合基于VLM的高层规划器进行任务分解与反思，以及基于扩散模型的底层执行器进行精确的视觉运动控制。
result: "在RMBench任务中，该框架将平均成功率从最强基线的9.8%大幅提升至32.4%。"
conclusion: 实验证明，结构化记忆和闭环恢复机制对于提升具身智能在复杂长程操控任务中的鲁棒性和成功率至关重要。
---

## 摘要
最近的视觉-语言-动作（VLA）系统在具身操控中展现了强大的能力。然而，大多数现有的 VLA 策略依赖于有限的观测窗口和端到端动作预测，这使得它们在具有部分可观测性、遮挡和多阶段依赖的长程、依赖记忆的任务中表现脆弱。此类任务不仅需要精确的视觉运动控制，还需要持久记忆、自适应任务分解以及从执行失败中显式恢复的能力。为了解决这些局限性，我们提出了一种用于长程具身操控的双系统框架。该框架显式地将高层语义推理与底层运动执行分离。高层规划器实现为基于视觉语言模型（VLM）的智能体模块，负责维护结构化任务记忆，并执行目标分解、结果验证和错误驱动的修正。底层执行器实例化为基于 VLA 的视觉运动控制器，通过以保持几何特征的过滤观测为条件的扩散动作生成来执行每个子任务。这两个系统共同构成了规划与执行之间的闭环，实现了记忆感知推理、自适应重新规划和鲁棒的在线恢复。在代表性 RMBench 任务上的实验表明，所提出的框架显著优于代表性基准模型，平均成功率达到 32.4%，而最强基准模型仅为 9.8%。消融研究进一步证实了结构化记忆和闭环恢复对于长程操控的重要性。

## Abstract
Recent vision-language-action (VLA) systems have demonstrated strong capabilities in embodied manipulation. However, most existing VLA policies rely on limited observation windows and end-to-end action prediction, which makes them brittle in long-horizon, memory-dependent tasks with partial observability, occlusions, and multi-stage dependencies. Such tasks require not only precise visuomotor control, but also persistent memory, adaptive task decomposition, and explicit recovery from execution failures. To address these limitations, we propose a dual-system framework for long-horizon embodied manipulation.   Our framework explicitly separates high-level semantic reasoning from low-level motor execution. A high-level planner, implemented as a VLM-based agentic module, maintains structured task memory and performs goal decomposition, outcome verification, and error-driven correction. A low-level executor, instantiated as a VLA-based visuomotor controller, carries out each sub-task through diffusion-based action generation conditioned on geometry-preserving filtered observations. Together, the two systems form a closed loop between planning and execution, enabling memory-aware reasoning, adaptive replanning, and robust online recovery. Experiments on representative RMBench tasks show that the proposed framework substantially outperforms representative baselines, achieving a 32.4% average success rate compared with 9.8% for the strongest baseline. Ablation studies further confirm the importance of structured memory and closed-loop recovery for long-horizon manipulation.

---

## 论文详细总结（自动生成）

以下是对论文《Goal2Skill: Long-Horizon Manipulation with Adaptive Planning and Reflection》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型虽然在短程任务中表现出色，但在处理**长程（Long-Horizon）**、**依赖记忆**的任务时非常脆弱。这些任务通常具有部分可观测性、遮挡和复杂的多阶段依赖关系。
*   **核心挑战**：纯反应式（Reactive）策略缺乏持久的任务上下文维护、缺乏中间状态的验证机制，且无法从执行失败中显式恢复。
*   **整体含义**：论文提出了 **Goal2Skill** 框架，旨在通过“大脑（高层规划）”与“小脑（底层执行）”的分离，将长程操控转化为一个包含规划、执行、验证和修正的闭环过程。

### 2. 方法论
该框架由两个核心系统组成，形成闭环控制：
*   **高层规划器（High-Level Planner）**：
    *   **核心思想**：基于视觉语言模型（VLM）进行语义推理。
    *   **任务分解**：将全局目标 $G$ 分解为结构化子任务元组 $\tau_k$，包含指令、预设/后置条件、时间限制、空间约束 $B_k$（用于过滤干扰物）和技能索引。
    *   **结构化记忆管理**：维护由**情节历史（Episodic History）**、**工作记忆（Working Memory）**和**错误寄存器（Error Register）**组成的记忆状态 $M_t$。
    *   **反思引擎（Reflection Engine）**：当验证器检测到失败时，VLM 会诊断原因并决定是“重试”、“调整参数”还是“重新规划”。
*   **底层执行器（Low-Level Executor）**：
    *   **几何感知感知**：利用规划器提供的空间约束 $B_k$ 生成掩码，过滤掉与任务无关的干扰区域，保留几何特征。
    *   **扩散技能库**：基于扩散策略（Diffusion Policy）实现视觉运动控制，根据过滤后的图像和指令生成连续动作序列。

### 3. 实验设计
*   **数据集与场景**：使用了 **RMBench** 基准测试中的 5 个代表性任务。
    *   **M(1) 任务**（低记忆需求）：观察并抓取、重排积木。
    *   **M(n) 任务**（高记忆需求）：电池尝试、积木排序尝试、按按钮。
*   **对比基准（Baselines）**：
    *   非预训练策略：**DP** (Diffusion Policy), **ACT**。
    *   预训练 VLA 策略：**Pi0.5**, **X-VLA**。
*   **评估指标**：任务成功率（SR），每个任务评估 100 个 Rollout 回合。

### 4. 资源与算力
*   **训练细节**：论文提到针对每个任务使用了 50 个专家演示进行训练，优化步骤为 **30k steps**。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个任务上进行了 100 次独立测试，样本量足以支撑统计意义。
*   **消融实验**：
    *   **记忆消融**：分别验证了情节历史（$H_t$）和工作记忆（$W_t$）对长程任务的影响。
    *   **恢复机制消融**：验证了验证器（$\Phi_{verify}$）和反思引擎（$\Phi_{reflect}$）在应对执行失败时的作用。
*   **充分性评价**：实验设计较为全面，特别是在 M(n) 任务上的对比充分展示了该方法在处理复杂依赖关系时的优势。对比方法涵盖了当前主流的端到端 VLA 架构，具有较高的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：Goal2Skill 的平均成功率达到 **32.4%**，远超最强基线（X-VLA, 9.8%）。
*   **记忆的重要性**：在记忆密集型 M(n) 任务中，该框架优势更明显（38.7% vs 9.0%），证明了结构化记忆是处理长程任务的关键。
*   **闭环恢复的价值**：显式的验证和反思机制显著提升了鲁棒性，使机器人能够在初次尝试失败后通过调整参数或重规划完成任务。

### 7. 优点（亮点）
*   **解耦设计**：成功将高层语义推理与底层精细动作控制分离，降低了单一模型的学习难度。
*   **主动记忆机制**：记忆不再是简单的存储，而是作为验证、诊断和恢复的主动基质。
*   **几何过滤感知**：通过高层规划引导底层视觉关注点，有效解决了复杂环境中的干扰物问题。

### 8. 不足与局限
*   **任务覆盖面**：虽然在 RMBench 上表现优异，但实验仅限于 5 个任务，尚未在更大规模、更多样化的真实世界场景中验证。
*   **实时性挑战**：引入 VLM 进行高层规划和反思可能会带来额外的推理延迟，论文未详细讨论系统在实时交互中的响应速度。
*   **技能库依赖**：底层执行器依赖于预定义的技能库，对于技能库之外的全新动作可能缺乏泛化能力。

（完）
