---
title: "Grounded Forcing: Bridging Time-Independent Semantics and Proximal Dynamics in Autoregressive Video Synthesis"
title_zh: Grounded Forcing：在自回归视频合成中桥接时间无关语义与近端动态
authors: "Jintao Chen, Chengyu Bai, Junjun Hu, Xinda Xue, Mu Xu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06939v2"
tags: ["query:q12"]
score: 9.0
evidence: 结合语义锚点和近端动力学的自回归视频合成
tldr: 自回归视频生成在长视频合成中面临语义遗忘、视觉漂移和交互控制失效等挑战。本文提出 Grounded Forcing 框架，通过双内存 KV 缓存解耦局部动态与全局语义，利用双参考 RoPE 注入抑制位置外推导致的漂移，并采用非对称邻近重缓存实现平滑的指令切换。该框架有效弥合了时间无关语义与近端动态，显著提升了长视频生成的连贯性与稳定性，为交互式长视频合成奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决自回归视频生成中因上下文限制、位置外推和指令切换导致的语义遗忘、视觉漂移及控制力丧失问题。
method: 提出 Grounded Forcing 框架，通过双内存 KV 缓存、双参考 RoPE 注入和非对称邻近重缓存三大协同机制稳定生成过程。
result: 实验结果表明，该方法在长程视频生成中显著增强了语义一致性和视觉稳定性。
conclusion: Grounded Forcing 通过将生成过程锚定在稳定的语义核心上，为实现高质量、可交互的长视频合成提供了有效路径。
---

## 摘要
自回归视频合成为无限时长生成提供了一条充满前景的路径，但从根本上受到三个交织挑战的阻碍：上下文限制导致的语义遗忘、位置外推引起的视觉漂移，以及交互式指令切换过程中的可控性丧失。现有方法通常孤立地处理这些问题，限制了长期连贯性。我们提出了 Grounded Forcing，这是一个通过三个连锁机制桥接时间无关语义和近端动态的新型框架。首先，为了解决语义遗忘问题，我们提出了一种双重记忆 KV 缓存（Dual Memory KV Cache），将局部时间动态与全局语义锚点解耦，确保了长期的语义连贯性和身份稳定性。其次，为了抑制视觉漂移，我们设计了双参考 RoPE 注入（Dual-Reference RoPE Injection），将位置嵌入限制在训练流形内，同时使全局语义具有时间不变性。第三，为了解决可控性问题，我们开发了非对称近端重缓存（Asymmetric Proximity Recache），通过近端加权缓存更新，促进提示词转换过程中的平滑语义继承。这些组件协同工作，将生成过程系留在稳定的语义核心上，同时兼顾灵活的局部动态。大量实验表明，Grounded Forcing 显著增强了长程一致性和视觉稳定性，为交互式长视频合成奠定了坚实基础。

## Abstract
Autoregressive video synthesis offers a promising pathway for infinite-horizon generation but is fundamentally hindered by three intertwined challenges: semantic forgetting from context limitations, visual drift due to positional extrapolation, and controllability loss during interactive instruction switching. Current methods often tackle these issues in isolation, limiting long-term coherence. We introduce Grounded Forcing, a novel framework that bridges time-independent semantics and proximal dynamics through three interlocking mechanisms. First, to address semantic forgetting, we propose a Dual Memory KV Cache that decouples local temporal dynamics from global semantic anchors, ensuring long-term semantic coherence and identity stability. Second, to suppress visual drift, we design Dual-Reference RoPE Injection, which confines positional embeddings within the training manifold while rendering global semantics time-invariant. Third, to resolve controllability issues, we develop Asymmetric Proximity Recache, which facilitates smooth semantic inheritance during prompt transitions via proximity-weighted cache updates. These components operate synergistically to tether the generative process to stable semantic cores while accommodating flexible local dynamics. Extensive experiments demonstrate that Grounded Forcing significantly enhances long-range consistency and visual stability, establishing a robust foundation for interactive long-form video synthesis.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Grounded Forcing**，一个旨在解决自回归（AR）视频生成中长程一致性与交互控制问题的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
自回归视频生成是实现无限时长视频合成和实时交互模拟的关键技术，但目前面临“三位一体”的挑战：
*   **语义遗忘（Semantic Forgetting）：** 由于 KV Cache 窗口限制，模型会丢失初始帧定义的身份和场景信息。
*   **视觉漂移（Visual Drift）：** 随着生成步数增加，位置编码（RoPE）超出训练分布（OOD），导致画面崩溃。
*   **可控性丧失（Controllability Loss）：** 在动态切换提示词（Prompt Switching）时，简单的缓存刷新会导致画面突变（语义冲击）或忽略新指令。
论文的动机在于通过解耦“时间无关的全局语义”与“近端的局部动态”，使生成过程既能锚定核心特征，又能灵活响应交互指令。

### 2. 论文提出的方法论
核心思想是 **Grounded Forcing** 框架，包含三个协同工作的关键技术：
*   **双重记忆 KV 缓存（Dual Memory KV Cache）：**
    *   将缓存分为 **局部时间记忆（LTM）** 和 **全局一致性记忆（GCM）**。
    *   LTM 存储最近的滑动窗口帧，负责捕捉高频运动。
    *   GCM 存储稀疏的关键帧锚点。引入了**多样性感知更新策略**：通过计算新帧与现有锚点的余弦相似度，仅当新帧具有高“重要性”（语义新颖）且现有锚点存在“冗余”时才进行替换，确保 GCM 覆盖演进的语义空间。
*   **双参考 RoPE 注入（Dual-Reference RoPE Injection）：**
    *   **GCM（时间不变）：** 为全局锚点注入固定的位置索引（$t=0$），使其成为位置无关的语义参考。
    *   **LTM（相对上下文）：** 将局部窗口的索引映射回模型训练时的安全范围（如 $[0, 21]$），防止位置外推导致的视觉崩溃。
*   **非对称近端重缓存（Asymmetric Proximity Recache, APR）：**
    *   在提示词切换时，不采用全量刷新，而是根据时间邻近度进行加权更新。
    *   **公式逻辑：** 距离当前生成点越近的帧，新提示词的权重 $\alpha_t$ 越高，以确保即时响应；距离越远的帧保留更多旧缓存，以维持身份继承，从而实现平滑过渡。

### 3. 实验设计
*   **基础模型：** Wan2.1-T2V-1.3B。
*   **数据集/场景：** 
    *   从 **MovieGenBench** 随机抽取 100 个提示词，生成 5s、60s、120s、240s 的视频。
    *   使用 **Qwen3-Max** 生成 50 组交互式提示词列表（每组含 6 个子提示词），用于测试交互能力。
*   **Benchmark：** 使用 **VBench** 评估指标，包括主体一致性、背景一致性、运动平滑度、审美质量等 7 个维度。
*   **对比方法：** 包括 LongLive、Rolling Forcing、Infinity-RoPE 等当前最先进的自回归长视频生成模型。

### 4. 资源与算力
*   **硬件：** 使用了 **32 张 NVIDIA H20 GPU**。
*   **训练时长：** 采用两阶段训练。
    *   第一阶段：在 5s 剪辑上训练 1200 步，确保基础稳定性。
    *   第二阶段：在长序列和动态切换场景下训练 1000 步，增强长程一致性和交互控制。
*   **推理：** 支持实时流式合成。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从短程（5s）到极长程（240s/4分钟）的定量评估，并针对交互场景做了专门测试。
*   **消融实验：** 针对 Dual Memory、DR-RoPE 和 APR 三个核心组件分别进行了剔除实验，验证了各模块对主体一致性和背景稳定性的贡献。
*   **用户研究：** 邀请 10 名参与者对 50 组结果进行 5 分制 Likert 量表评分。
*   **评价：** 实验设计较为全面，对比了多种主流 AR 方案，且覆盖了单提示词长生成和多提示词交互切换两种核心场景，实验结果具有较强的说服力和客观性。

### 6. 论文的主要结论与发现
*   **解耦的必要性：** 证明了将静态语义锚点与动态运动特征分离是解决长视频漂移的关键。
*   **性能领先：** 在 240s 的超长视频生成中，Grounded Forcing 在主体一致性（0.9163）和背景一致性（0.9265）上显著优于基线方法。
*   **交互平滑性：** APR 机制有效消除了提示词切换时的“语义冲击”，使模型能够处理具有语言依赖性的连续指令（如“一个人在走”到“然后他跑起来”）。

### 7. 优点
*   **系统性解决：** 并非单一技巧，而是从内存管理、位置编码和缓存更新三个维度系统性地解决了 AR 生成的顽疾。
*   **训练与推理结合：** 既有结构创新（双内存），又考虑了训练策略的对齐，使得模型在长程生成下非常鲁棒。
*   **多样性更新：** GCM 的更新算法考虑了语义冗余，比简单的“保留第一帧”更具适应性。

### 8. 不足与局限
*   **模型规模限制：** 实验主要基于 1.3B 参数的模型，尚未在大规模参数模型（如 10B+）上验证其扩展性。
*   **计算开销：** 虽然支持实时生成，但双内存管理和 APR 的重缓存计算相比简单滑动窗口会增加一定的推理复杂度和内存占用。
*   **场景覆盖：** 尽管在 MovieGenBench 上表现良好，但在极度复杂的转场或大幅度相机运动下的表现仍有待进一步探索。

（完）
