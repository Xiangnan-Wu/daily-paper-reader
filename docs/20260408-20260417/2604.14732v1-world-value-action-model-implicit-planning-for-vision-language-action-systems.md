---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于VLA系统隐式规划的世界-价值-动作模型
tldr: 本文针对视觉-语言-动作（VLA）模型在长程任务中缺乏推理能力的问题，提出了World-Value-Action (WAV) 模型。该模型通过学习结构化潜空间表示，结合世界模型预测未来和价值函数评估，将动作生成转化为隐式规划过程。理论分析表明，相比直接在动作空间搜索，潜空间推理能更有效地定位可行轨迹。实验证明WAV在复杂长程任务中的成功率和泛化性显著优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有VLA模型多依赖直接动作预测，缺乏对长程轨迹的推理和后果评估能力，难以应对复杂的决策任务。
method: 提出WAV模型，利用世界模型预测未来状态并结合价值函数，在结构化潜空间中通过隐式规划生成动作。
result: 实验结果显示，WAV在模拟和现实实验中的任务成功率、泛化性和鲁棒性均优于SOTA方法，尤其擅长长程组合任务。
conclusion: 在潜空间进行隐式规划能有效重塑搜索分布并提升决策效率，是增强具身智能体长程任务能力的有效范式。
---

## 摘要
视觉-语言-动作（VLA）模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，大多数现有方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作（WAV）模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习一种以视觉观测和语言指令为条件的未来轨迹结构化潜表示。一个学习到的世界模型负责预测未来状态，而轨迹价值函数则评估其长时程效用。动作生成随后被表述为在该潜空间中的推理过程，模型逐渐将概率质量集中在高价值且动力学可行的轨迹上。我们提供了一个理论视角，表明直接在动作空间中进行规划会面临可行轨迹概率随时间跨度增加而呈指数级衰减的问题。相比之下，潜空间推理将搜索分布重塑向可行区域，从而实现了高效的长时程决策。大量的仿真和真实世界实验表明，WAV 模型始终优于现有最先进的方法，在任务成功率、泛化能力和鲁棒性方面取得了显著提升，特别是在长时程和组合场景中。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型大多依赖于直接的动作预测（反应式控制），缺乏对长程（Long-horizon）轨迹的推理能力和对动作后果的评估机制。
*   **研究背景**：虽然世界模型可以预测未来，但如何将其有效地整合进动作决策过程仍是难题。直接在动作空间进行规划会面临“可行性诅咒”——随着规划步数增加，可行轨迹在海量动作空间中的占比呈指数级衰减。
*   **整体含义**：本文提出了 **WAV（World-Value-Action）** 模型，通过在结构化的潜空间（Latent Space）进行隐式规划，将预测未来、评估价值和生成动作统一在一个框架内。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将动作生成视为潜空间中的推理过程。模型不直接搜索动作，而是通过迭代优化未来视觉轨迹的潜分布，使其向“动力学可行”且“高任务价值”的区域集中。
*   **关键模块**：
    1.  **语言条件视频生成模块**：基于 Diffusion Transformer (DiT)，根据指令和当前观测预测未来视觉演变。
    2.  **轨迹价值模块**：评估预测视频序列的长程效用（累积奖励）。
    3.  **动作解码模块**：结合视频特征和价值特征，解码出具体的机器人控制动作。
*   **算法流程（隐式规划）**：
    *   采用类似 MPPI（模型预测路径积分）的迭代推理。
    *   **初始化**：从高斯噪声中采样视频和价值的潜变量。
    *   **迭代优化**：通过世界模型生成未来视频，利用价值模块评分，筛选出“精英”样本（Top-K），并据此更新潜变量的分布（均值和方差）。
    *   **最终决策**：在优化后的分布中采样，生成最终动作。
*   **训练技术**：采用三阶段的 **Flow Matching（流匹配）** 训练策略，依次训练视频生成、价值评估和动作解码。

### 3. 实验设计
*   **仿真场景**：使用 **LIBERO** 基准测试，包含四个子集：Spatial（空间推理）、Object（物体泛化）、Goal（目标条件）和 Long（长程组合任务）。
*   **现实场景**：使用双臂 Piper 机器人执行复杂任务，如整理碗筷（Bowl organization）、铺平毛巾（Towel flattening）和长程抽屉任务（打开抽屉-放入物体-关闭抽屉）。
*   **对比方法**：
    *   **传统 VLA**：Diffusion Policy, Octo, OpenVLA, $\pi_0$ 等。
    *   **带世界模型的 VLA**：WorldVLA, DreamVLA, UniVLA, GE-ACT 等。

### 4. 资源与算力
*   **硬件环境**：使用 8 台 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：
    *   **LIBERO 任务**：全参数微调，视频模块需 3 万步，价值/动作模块需 4 万步，总计约 **5 天**。
    *   **现实世界任务**：每个任务单独训练，总计约 **3 天**。
*   **推理效率**：文中记录了不同迭代次数（K）下的推理时间，K=3 时在性能与速度间达到较好平衡。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真**：在 LIBERO 的数十个任务上进行了全面评测。
    *   **现实世界**：每个任务进行 15 次独立实验，对比了成功率。
    *   **消融实验**：针对迭代次数 $K$、采样数 $M/N$、平滑参数 $\alpha/\beta$、精英选择比例 $K_1/K_2$ 等进行了详细测试。
*   **充分性评价**：实验设计较为充分且客观。不仅有端到端的成功率对比，还通过理论分析（引理 4.1 和命题 4.2）支撑了为何潜空间规划优于动作空间规划，并提供了视频生成和价值预测的定性可视化分析。

### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 榜单上取得了 **98.1%** 的平均成功率，刷新了 SOTA 记录，尤其在长程任务（Long）上优势明显。
*   **现实泛化**：在真实机器人实验中，WAV 的平均成功率（75.6%）远高于基准模型 GE-ACT（35.6%），证明了隐式规划能有效减少长程操作中的误差累积。
*   **理论验证**：实验证实了迭代推理（K > 0）能显著提升表现，验证了“规划即推理”的有效性。

### 7. 优点（亮点）
*   **理论与实践结合**：通过数学证明解释了 VLA 模型在长程任务中的失效原因（可行性概率指数衰减），并给出了潜空间重塑分布的解决方案。
*   **模块化统一框架**：将预测、评估和执行解耦又通过潜空间推理有机统一，架构设计优雅。
*   **鲁棒性强**：通过迭代优化，模型能够纠正初始预测的偏差，对复杂环境的适应性更好。

### 8. 不足与局限
*   **计算开销**：由于涉及多次迭代采样和 DiT 推理，模型的推理延迟和显存占用高于传统的反应式 VLA 模型，难以实现极高频的闭环控制。
*   **部署限制**：目前主要在特定任务上微调，尚未展示在大规模开放域指令下的实时零样本（Zero-shot）表现。
*   **数据依赖**：价值函数的训练依赖于高质量的奖励标注或规则定义的奖励函数，这在某些极其复杂的非结构化任务中可能难以获取。

（完）
