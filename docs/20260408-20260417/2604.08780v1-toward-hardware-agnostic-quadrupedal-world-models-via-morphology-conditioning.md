---
title: Toward Hardware-Agnostic Quadrupedal World Models via Morphology Conditioning
title_zh: 迈向通过形态条件化实现硬件无关的四足机器人世界模型
authors: "Mohamad H. Danesh, Chenhao Li, Amin Abyaneh, Anas Houssaini, Kirsty Ellis, Glen Berseth, Marco Hutter, Hsiu-Chin Lin"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08780v1"
tags: ["query:robot"]
score: 10.0
evidence: 通过形态调节实现硬件无关的四足机器人世界模型
tldr: 针对当前四足机器人世界模型受限于特定硬件、泛化性差的问题，本文提出了一种硬件无关的四足世界模型（QWM）。通过引入物理形态编码器和奖励归一化器，将机器人工程规格显式作为生成动力学的条件，实现了环境动力学与机器人形态的解耦。该模型作为神经模拟器，支持在不同形态的四足机器人间进行零样本控制迁移，是迈向通用形态感知世界模型的重要一步。
selection_source: fresh_fetch
motivation: 现有的世界模型过度拟合特定机器人的运动学和动力学属性，导致硬件变更时需从头训练，缺乏跨平台的通用性。
method: 提出显式形态调节方案，利用物理形态编码器整合机器人规格，并结合奖励归一化器来解耦环境动力学与机器人形态。
result: 成功实现了在多种四足机器人形态间的零样本泛化控制，使模型能够作为跨形态的神经模拟器运行。
conclusion: 该研究证明了通过形态调节可构建具备插值能力的通用四足世界模型，为实现硬件无关的机器人学习提供了新路径。
---

## 摘要
世界模型有望带来机器人领域的范式转变，即智能体只需学习一次其环境的底层物理规律，即可实现高效的规划和行为学习。然而，当前的世界模型通常是锁定硬件的“专家型”模型：由于运动学和动力学特性的不匹配，在波士顿动力 Spot 机器人上训练的模型在宇树 Unitree Go1 上会遭遇灾难性的失败，因为模型过拟合于特定的具身约束，而非捕捉通用的运动动力学。因此，执行器动力学或肢体长度的微小变化都需要从头开始训练一个新模型。在这项工作中，我们迈出了建立可泛化四足机器人世界模型（QWM）框架的一步，该框架将环境动力学与机器人形态解耦。我们解决了隐式系统辨识的局限性，即若将静态物理属性（如质量或肢体长度）视为从运动历史中推断的隐变量，会产生适应滞后，从而损害零样本的安全性和效率。相反，我们显式地将生成动力学建立在机器人的工程规格条件之上。通过集成物理形态编码器和奖励归一化器，我们使该模型能够作为一个神经模拟器，具备跨形态泛化的能力。这种能力开启了跨多种具身形式的零样本控制。我们首次提出了一种能够实现足式运动新形态零样本泛化的世界模型。尽管我们仔细研究了该方法的局限性（QWM 在四足形态家族中作为分布有界的插值器运行，而非通用的物理引擎），但这项工作代表了迈向足式运动形态条件化世界模型的重要一步。

## Abstract
World models promise a paradigm shift in robotics, where an agent learns the underlying physics of its environment once to enable efficient planning and behavior learning. However, current world models are often hardware-locked specialists: a model trained on a Boston Dynamics Spot robot fails catastrophically on a Unitree Go1 due to the mismatch in kinematic and dynamic properties, as the model overfits to specific embodiment constraints rather than capturing the universal locomotion dynamics. Consequently, a slight change in actuator dynamics or limb length necessitates training a new model from scratch. In this work, we take a step towards a framework for training a generalizable Quadrupedal World Model (QWM) that disentangles environmental dynamics from robot morphology. We address the limitations of implicit system identification, where treating static physical properties (like mass or limb length) as latent variables to be inferred from motion history creates an adaptation lag that can compromise zero-shot safety and efficiency. Instead, we explicitly condition the generative dynamics on the robot's engineering specifications. By integrating a physical morphology encoder and a reward normalizer, we enable the model to serve as a neural simulator capable of generalizing across morphologies. This capability unlocks zero-shot control across a range of embodiments. We introduce, for the first time, a world model that enables zero-shot generalization to new morphologies for locomotion. While we carefully study the limitations of our method, QWM operates as a distribution-bounded interpolator within the quadrupedal morphology family rather than a universal physics engine, this work represents a significant step toward morphology-conditioned world models for legged locomotion.

---

## 论文详细总结（自动生成）

这是一份关于论文《Toward Hardware-Agnostic Quadrupedal World Models via Morphology Conditioning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的机器人世界模型（World Models, WMs）通常是“硬件锁定”的专家模型。由于不同机器人（如波士顿动力的 Spot 与宇树的 Go1）在运动学和动力学属性上存在巨大差异，模型往往过拟合于特定硬件，导致在不同形态间迁移时会发生灾难性失败。
*   **研究动机**：
    *   **数据瓶颈**：每更换或升级一次硬件（如改变肢体长度或电机）都需要重新收集数百万样本进行训练，成本极高。
    *   **隐式辨识的局限**：传统方法依赖从运动历史中隐式推断物理属性，这会产生“适应滞后”，在零样本迁移时存在安全风险。
    *   **通用性需求**：旨在构建一个能解耦“环境动力学”与“机器人形态”的通用四足世界模型（QWM），使其能像大语言模型处理不同语境一样，适应不同的机器人硬件。

### 2. 方法论
核心思想是**显式形态调节（Explicit Morphology Conditioning）**，将机器人的工程规格直接作为动力学模型的输入。

*   **物理形态编码器 (PME)**：从机器人的 USD 或 URDF 文件中提取 11 维归一化特征向量 $\mu$，涵盖：
    *   **运动学与拓扑**：肢体长度、膝盖配置（X型或狗型）。
    *   **几何结构**：支撑多边形的纵横比。
    *   **动力学**：总质量的对数缩放、躯干质量占比。
    *   **执行器**：扭矩密度（最大扭矩与重量之比）。
*   **形态条件化世界模型**：
    *   基于 **DreamerV3** 架构，改进了循环状态空间模型（RSSM）。
    *   **双塔编码器**：分别处理高频本体感受数据和静态形态向量 $\mu$，防止静态信号被动态数据淹没。
    *   **循环状态注入**：将 $\mu$ 直接注入循环神经网络（GRU）的每个时间步，使隐藏状态 $h_t$ 无需记忆静态属性，专注于追踪动态状态。
*   **自适应奖励归一化 (ARN)**：针对不同机器人奖励尺度差异巨大的问题（如 Spot 奖励约 350，ANYmal 约 25），利用分位数 EMA 动态缩放奖励，确保多机器人联合训练时的梯度稳定性。

### 3. 实验设计
*   **实验场景**：基于 NVIDIA Isaac Lab 开发了 **Hetero-Isaac** 框架，支持在单个 GPU 批次中并行模拟不同形态的机器人。
*   **机器人队列**：包含 ANYmal (B, C, D)、Unitree (A1, Go1, Go2, B2) 和 Boston Dynamics Spot。
*   **对比方法 (Benchmarks)**：
    *   **模型强化学习**：PPO, PME-PPO（带形态编码的 PPO）, BoT-PPO（身体变压器）。
    *   **世界模型**：原生 DreamerV3, PWM, TWISTER。
*   **评估维度**：多形态掌握能力、长程动力学预测精度、零样本泛化（插值与外推）、真机部署表现。

### 4. 资源与算力
*   **仿真环境**：使用了 NVIDIA Isaac Lab，支持 4,096 个并行环境。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但根据 DreamerV3 的典型需求和 36M 步的训练量，推测使用了高性能工作站或集群（如 A100 级别）。
*   **推理速度**：真机部署时，推理管线在机器人车载电脑上以 50Hz 的频率实时运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 种不同规格的机器人上进行了联合训练。
    *   进行了详尽的**消融实验**（共 8 组变体），验证了 PME、ARN 以及形态注入位置的必要性。
    *   进行了 10 次重复的真机实验（每组 60 秒）以获取定量追踪误差。
*   **充分性评价**：实验设计非常全面。不仅涵盖了仿真内部的对比，还通过“留一法”验证了**插值泛化**（如在 A1/Go2 上练，在 Go1 上测）和**外推泛化**（测试极端的 B2 型号），并最终完成了跨品牌的真机验证，客观地展示了模型的优缺点。

### 6. 主要结论与发现
*   **零样本迁移成功**：QWM 能够作为“物理适配器”，仅通过更换形态向量 $\mu$，即可让冻结的策略在从未见过的机器人（Go1, ANYmal-D）上实现稳定行走。
*   **解耦效果显著**：通过可视化发现，随机状态 $z_t$ 变得与形态无关，仅捕捉瞬时运动特征，而形态信息被成功隔离在循环状态 $h_t$ 中。
*   **泛化边界**：QWM 在形态空间的“插值”表现极佳，但在面对极端的“外推”形态（如质量大出数倍的 B2）时仍会失败，说明模型本质上是一个分布有界的插值器。

### 7. 优点与亮点
*   **架构创新**：通过显式注入工程参数，解决了世界模型在机器人领域长期存在的硬件过拟合问题。
*   **训练稳定性**：ARN 机制有效地解决了异构机器人任务中奖励函数不统一导致的梯度支配问题。
*   **实用性强**：无需任何真机微调或“热身”期即可部署，极大地降低了新硬件的适配成本。

### 8. 不足与局限
*   **外推能力有限**：模型高度依赖训练分布的覆盖范围，对于超出分布太远的形态（Outliers）缺乏鲁棒性。
*   **感知局限**：目前主要基于本体感受（盲走），尚未整合视觉信息，无法处理复杂地形的几何感知。
*   **任务单一**：实验主要集中在行走（Locomotion），尚未验证该框架在操纵（Manipulation）或其他复杂技能上的通用性。
*   **算力信息缺失**：未提供具体的训练资源消耗，不便于其他研究者评估复现成本。

（完）
