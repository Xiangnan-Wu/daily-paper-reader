---
title: "Lyra 2.0: Explorable Generative 3D Worlds"
title_zh: Lyra 2.0：可探索的生成式 3D 世界
authors: "Tianchang Shen, Sherwin Bahmani, Kai He, Sangeetha Grama Srinivasan, Tianshi Cao, Jiawei Ren, Ruilong Li, Zian Wang, Nicholas Sharp, Zan Gojcic, Sanja Fidler, Jiahui Huang, Huan Ling, Jun Gao, Xuanchi Ren"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13036v1"
tags: ["query:q14"]
score: 9.0
evidence: 生成摄像机控制的视频以模拟场景漫游进行 3D 创建
tldr: Lyra 2.0 针对生成式 3D 场景重建中的长程一致性问题，提出了一个可探索的大规模 3D 世界生成框架。该方法通过维护每帧 3D 几何信息进行信息路由以解决空间遗忘，并利用自增强历史训练来纠正时间漂移。这使得模型能够生成长路径、高一致性的视频轨迹，进而通过前馈重建技术恢复出高质量、可实时渲染的 3D 场景。
selection_source: fresh_fetch
motivation: 现有的视频生成模型在生成长路径 3D 场景时，面临空间遗忘导致的重访区域失真和时间漂移导致的误差累积问题。
method: 利用 3D 几何引导的信息路由检索历史帧以维持空间一致性，并结合自增强历史训练来教导模型纠正合成过程中的漂移误差。
result: 该框架能够生成显著更长且具备 3D 一致性的视频轨迹，从而支持高质量、大规模复杂 3D 环境的可靠重建。
conclusion: Lyra 2.0 通过解决空间遗忘和时间漂移，为构建持久且可自由探索的生成式 3D 世界提供了有效的技术路径。
---

## 摘要
视频生成的最新进展为 3D 场景创建开启了一种新范式：生成模拟场景漫游的受相机控制的视频，然后通过前馈重建技术将其提升至 3D。这种生成式重建方法结合了视频模型的视觉保真度和创作能力，并能生成可用于实时渲染和模拟的 3D 输出。扩展到大型复杂环境需要跨越长相机轨迹、大视角变化和位置重访的 3D 一致性视频生成，而当前的视频模型在这些场景下性能会迅速下降。现有的长时程生成方法从根本上受到两种退化形式的限制：空间遗忘和时间漂移。随着探索的进行，先前观察到的区域会超出模型的时间上下文，迫使模型在重访时产生结构幻觉。同时，自回归生成会随时间累积微小的合成误差，逐渐扭曲场景的外观和几何结构。我们提出了 Lyra 2.0，这是一个用于大规模生成持久且可探索的 3D 世界的框架。为了解决空间遗忘问题，我们维护逐帧 3D 几何，并仅将其用于信息路由——检索相关的历史帧并建立与目标视角的密集对应关系——同时依靠生成先验进行外观合成。为了解决时间漂移问题，我们使用自增强历史进行训练，使模型接触到其自身的退化输出，从而教会它纠正漂移而非传播漂移。这些方法共同实现了显著更长且具有 3D 一致性的视频轨迹，我们利用这些轨迹微调前馈重建模型，从而可靠地恢复高质量的 3D 场景。

## Abstract
Recent advances in video generation enable a new paradigm for 3D scene creation: generating camera-controlled videos that simulate scene walkthroughs, then lifting them to 3D via feed-forward reconstruction techniques. This generative reconstruction approach combines the visual fidelity and creative capacity of video models with 3D outputs ready for real-time rendering and simulation. Scaling to large, complex environments requires 3D-consistent video generation over long camera trajectories with large viewpoint changes and location revisits, a setting where current video models degrade quickly. Existing methods for long-horizon generation are fundamentally limited by two forms of degradation: spatial forgetting and temporal drifting. As exploration proceeds, previously observed regions fall outside the model's temporal context, forcing the model to hallucinate structures when revisited. Meanwhile, autoregressive generation accumulates small synthesis errors over time, gradually distorting scene appearance and geometry. We present Lyra 2.0, a framework for generating persistent, explorable 3D worlds at scale. To address spatial forgetting, we maintain per-frame 3D geometry and use it solely for information routing -- retrieving relevant past frames and establishing dense correspondences with the target viewpoints -- while relying on the generative prior for appearance synthesis. To address temporal drifting, we train with self-augmented histories that expose the model to its own degraded outputs, teaching it to correct drift rather than propagate it. Together, these enable substantially longer and 3D-consistent video trajectories, which we leverage to fine-tune feed-forward reconstruction models that reliably recover high-quality 3D scenes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lyra 2.0: Explorable Generative 3D Worlds》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的 3D 场景创建正转向一种新范式——“生成式重建”，即利用视频扩散模型生成相机控制的漫游视频，再将其转化为 3D 表示。然而，在生成大规模、复杂环境（如跨房间或长街道）时，现有模型面临两大核心挑战：
    *   **空间遗忘（Spatial Forgetting）**：当相机移动并重访旧区域时，由于历史帧超出了模型有限的上下文窗口，模型会产生结构幻觉，导致全局不一致。
    *   **时间漂移（Temporal Drifting）**：自回归生成过程中微小的合成误差会随时间累积，导致颜色偏移和几何扭曲。
*   **核心目标**：开发一个能够生成持久、可大规模探索且具备 3D 一致性的生成式 3D 世界框架。

### 2. 论文提出的方法论
Lyra 2.0 采用“检索-生成-更新”的循环机制，核心思想是**将几何跟踪与像素合成解耦**。
*   **抗遗忘机制（空间存储）**：
    *   **3D 缓存（3D Cache）**：维护逐帧的深度图和下采样点云，不进行全局融合以避免误差累积。
    *   **几何感知检索**：根据目标视角的可见性得分，从缓存中检索最相关的 5 帧历史帧作为“空间插槽”。
    *   **规范坐标变形（Canonical Coordinate Warping）**：通过变形规范坐标图（而非 RGB 图像）来建立密集 3D 对应关系，引导模型进行外观合成，避免直接引入渲染伪影。
*   **抗漂移机制（时间一致性）**：
    *   **FramePack 上下文压缩**：对不同远近的历史帧采用变长补丁化（Patchification），在固定 Token 预算下延长时程。
    *   **自增强训练（Self-Augmentation Training）**：在训练时随机让模型接触自身生成的、带噪声的预测结果（而非纯真值），教会模型在推理时纠正漂移误差。
*   **3D 重建与蒸馏**：
    *   使用微调后的 **Depth Anything v3 (DAv3)** 进行前馈 3D 高斯泼溅（3DGS）重建。
    *   通过 **DMD (分布匹配蒸馏)** 将模型从 35 步采样压缩至 4 步，实现交互式生成。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DL3DV（包含 10,000 个真实场景的长视频剪辑）。
    *   **测试集**：DL3DV-Evaluation（域内）和 Tanks and Temples（跨域泛化）。
*   **Benchmark 与指标**：
    *   标准指标：SSIM, LPIPS, FID。
    *   世界模型指标（WorldScore）：主观质量、风格一致性、相机控制力。
    *   几何指标：重投影误差（Reprojection Error）。
*   **对比方法**：包括 GEN3C, Yume1.5, CaM, VMem, SPMem, HY-WorldPlay 等具备存储机制的相机控制视频生成模型。

### 4. 资源与算力
*   **硬件**：使用了 **64 台 NVIDIA GB200 GPU**。
*   **训练细节**：
    *   训练迭代次数：7,000 次。
    *   Batch Size：64。
    *   学习率：3×10⁻⁵。
*   **推理效率**：全模型（35 步）在单台 GB200 上生成 80 帧需 194 秒；蒸馏后的 DMD 模型（4 步）仅需 **15 秒**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（涵盖 7 种基准方法）和定性可视化。
*   **消融实验**：针对全局点云 vs 逐帧缓存、显式融合 vs 学习融合、FramePack 的必要性、自增强训练的作用等关键组件做了 5 组详细消融。
*   **充分性评价**：实验设计非常充分且客观。它不仅在标准视觉指标上取胜，还引入了专门针对长视频一致性的重投影误差和风格一致性指标，证明了其在长程生成上的真实优势。

### 6. 论文的主要结论与发现
*   **解耦是关键**：将 3D 几何仅作为“信息路由”信号，而让扩散模型负责外观合成，能有效平衡几何约束与视觉保真度。
*   **自增强训练有效性**：通过在训练中引入推理时的误差分布，显著提升了模型在长序列生成中的鲁棒性。
*   **3D 一致性转化**：视频生成的一致性提升直接导致了后续 3DGS 重建质量的飞跃，减少了浮点噪声（Floaters）。

### 7. 优点
*   **长程持久性**：成功解决了重访场景时的“幻觉”问题，支持长达数百帧的 3D 一致性探索。
*   **高质量重建**：生成的视频可直接转化为高质量的 3D 高斯和表面网格，具备模拟就绪（Simulation-Ready）的特性。
*   **交互性**：提供了实时交互 GUI，并支持通过 DMD 蒸馏实现快速生成。

### 8. 不足与局限
*   **静态环境限制**：目前主要针对静态场景，无法处理动态物体（如行人、车辆）。
*   **光度不一致性**：受限于训练数据（DL3DV），生成的视频可能存在曝光变化，影响 3DGS 重建的稳定性。
*   **数据偏差**：模型性能仍高度依赖于训练数据的分布，对于极端罕见的场景可能表现欠佳。

（完）
