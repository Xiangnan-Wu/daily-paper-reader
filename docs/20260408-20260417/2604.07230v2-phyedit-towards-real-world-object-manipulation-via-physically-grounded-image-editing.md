---
title: "PhyEdit: Towards Real-World Object Manipulation via Physically-Grounded Image Editing"
title_zh: PhyEdit：通过物理驱动的图像编辑实现真实世界物体操控
authors: "Ruihang Xu, Dewei Zhou, Xiaolong Shen, Fan Ma, Yi Yang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07230v2"
tags: ["query:robot"]
score: 9.0
evidence: 用于交互式世界模型的物理接地操作
tldr: 针对现有图像编辑模型在物体操控中缺乏物理准确性（如缩放和定位错误）的问题，本文提出 PhyEdit 框架。该框架通过引入显式几何模拟作为 3D 感知视觉引导，并结合 2D-3D 联合监督，显著提升了物体操控的物理准确性。此外，研究者发布了包含深度标注的 RealManip-10K 数据集和 ManipEval 评测基准。实验证明，PhyEdit 在 3D 几何精度和操控一致性上优于现有开源及闭源模型。
selection_source: fresh_fetch
motivation: 现有生成模型因缺乏 3D 几何和透视投影机制，导致在图像编辑中难以实现精确的物体空间缩放和定位。
method: 提出 PhyEdit 框架，利用显式几何模拟作为即插即用的 3D 先验引导，并结合 2D-3D 联合监督提升物理一致性。
result: 实验表明 PhyEdit 在 3D 几何准确性和操控一致性方面均优于现有最先进模型，包括强力的闭源模型。
conclusion: 通过将显式 3D 几何引导与生成模型结合，可以有效实现符合物理规律的真实世界物体操控图像编辑。
---

## 摘要
在图像编辑中实现物理准确的物体操控，对于其在交互式世界模型中的潜在应用至关重要。然而，现有的视觉生成模型往往无法实现精确的空间操控，导致物体的缩放和定位不准确。这一局限性主要源于缺乏整合 3D 几何和透视投影的显式机制。为了实现精确操控，我们开发了 PhyEdit，这是一个利用显式几何模拟作为上下文 3D 感知视觉引导的图像编辑框架。通过将这种即插即用的 3D 先验与 2D-3D 联合监督相结合，我们的方法有效地提高了物理准确性和操控一致性。为了支持该方法并评估性能，我们提出了一个真实世界数据集 RealManip-10K，用于 3D 感知物体操控，包含成对图像和深度标注。我们还提出了 ManipEval，这是一个具有多维指标的基准测试，用于评估 3D 空间控制和几何一致性。广泛的实验表明，我们的方法在 3D 几何准确性和操控一致性方面均优于现有方法，包括强大的闭源模型。

## Abstract
Achieving physically accurate object manipulation in image editing is essential for its potential applications in interactive world models. However, existing visual generative models often fail at precise spatial manipulation, resulting in incorrect scaling and positioning of objects. This limitation primarily stems from the lack of explicit mechanisms to incorporate 3D geometry and perspective projection. To achieve accurate manipulation, we develop PhyEdit, an image editing framework that leverages explicit geometric simulation as contextual 3D-aware visual guidance. By combining this plug-and-play 3D prior with joint 2D--3D supervision, our method effectively improves physical accuracy and manipulation consistency. To support this method and evaluate performance, we present a real-world dataset, RealManip-10K, for 3D-aware object manipulation featuring paired images and depth annotations. We also propose ManipEval, a benchmark with multi-dimensional metrics to evaluate 3D spatial control and geometric consistency. Extensive experiments show that our approach outperforms existing methods, including strong closed-source models, in both 3D geometric accuracy and manipulation consistency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PhyEdit** 的框架，旨在解决图像编辑中物体操控的物理准确性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的扩散模型（如 Flux, GPT-4o 等）在 2D 图像编辑上表现出色，但在涉及 3D 空间操控（如将物体向远处移动）时，往往无法遵循物理规律（如透视投影导致的缩放变化），容易出现比例失调、位置偏移或几何不一致。
*   **核心问题**：如何让生成模型具备 3D 几何感知能力，从而在真实世界场景中实现精确的、符合物理规律的物体操控（平移、缩放、遮挡处理）。

### 2. 方法论
PhyEdit 框架结合了显式几何模拟与生成式渲染，核心包含三个部分：
*   **3D 变换模块（3D Transformation Module）**：
    *   利用 3D 基础模型预测源图像的深度和相机位姿。
    *   **流程**：将目标物体从 2D 像素“反投影”（Unproject）到 3D 空间，根据用户指令进行 3D 位移，再重新“投影”（Proj）回 2D 平面，生成一个**几何预览图（Preview Image）**。
*   **DiT 去噪骨干网络（Backbone）**：
    *   采用基于 Transformer 的扩散模型（DiT），将上述“几何预览图”作为额外的视觉引导条件输入，为模型提供明确的空间位置和缩放先验。
*   **2D-3D 联合监督（Joint Supervision）**：
    *   **2D 损失**：标准的潜空间流匹配损失（Flow Matching Loss）。
    *   **3D 损失**：引入**深度空间监督**。通过解码生成的图像并计算其与真实深度图之间的尺度不变对数损失（SILog Loss），强制模型生成的物体在深度维度上保持准确。

### 3. 实验设计
*   **数据集**：构建了 **RealManip-10K**，包含 10,000 对真实世界的图像对，涵盖了显著的深度变化、遮挡关系和多物体交互，并附带精确的深度标注和 3D 坐标。
*   **评测基准（Benchmark）**：提出 **ManipEval**，包含 200 个测试案例，从五个维度评估：
    1.  **2D 空间精度**（DIoU, Mask IoU）。
    2.  **深度准确性**（AbsRel, $\delta_{1.25}$）。
    3.  **3D 操控精度**（Chamfer 距离, 质心距离）。
    4.  **图像质量与一致性**（RA-DINO, DeQA）。
    5.  **物理合理性**（基于 VLM 的 Phys-VLM 评分）。
*   **对比方法**：对比了 5 类方法，包括基于拖拽的方法（GoodDrag）、显式操控方法（Move-and-Act）、3D 感知编辑（OBJect 3DIT）以及强大的闭源商业模型（Nano Banana Pro/Gemini, GPT-Image-1.5, Qwen-Image-2.0-Pro）。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA RTX PRO 6000 GPU**。
*   **训练细节**：总 Batch Size 为 64，训练步数为 10,000 步。
*   **优化器**：AdamW，学习率为 $1 \times 10^{-4}$。
*   **微调技术**：使用 LoRA（Rank=128）对 DiT 的自注意力层进行微调。

### 5. 实验数量与充分性
*   **实验规模**：论文在 200 个复杂的真实场景（涉及 320 个物体）上进行了详尽测试，涵盖单物体和多物体操控。
*   **消融实验**：针对深度监督方法（潜空间 vs 像素空间）、参考图的作用进行了对比实验。
*   **充分性评价**：实验设计非常全面，不仅对比了开源模型，还直接挑战了目前最强的闭源商业模型。通过引入 3D 几何指标（如 Chamfer 距离），比传统的 2D 指标更能客观反映物理操控的准确性。

### 6. 主要结论与发现
*   **性能领先**：PhyEdit 在所有 3D 几何相关指标上均显著优于现有方法，甚至在 3D 精度上超越了最强的商业模型（如 Nano Banana Pro）。
*   **深度监督的重要性**：像素级的深度监督比单纯的潜空间监督更能有效提升小物体和复杂深度变化的生成质量。
*   **视觉引导的必要性**：仅靠文本指令难以让模型理解精确的 3D 意图，显式的 3D 变换预览图是实现高精度操控的关键。

### 7. 优点
*   **即插即用**：3D 变换模块不改变生成模型的核心架构，易于迁移到不同的 DiT 模型。
*   **物理接地**：通过显式的投影几何解决了生成模型长期存在的“缩放幻觉”问题。
*   **高质量资源**：提供的 RealManip-10K 数据集填补了真实世界 3D 操控图像数据的空白。

### 8. 不足与局限
*   **提示词控制力**：在处理非几何属性（如颜色、纹理变化）的自由文本指令时，控制力仍有提升空间。
*   **极端场景失效**：在物体距离相机极近、场景极度拥挤或物体发生剧烈非刚性变形时，模型可能会产生伪影。
*   **推理开销**：由于需要运行 3D 基础模型进行预处理和深度预测，推理流程比纯 2D 编辑模型更复杂。

（完）
