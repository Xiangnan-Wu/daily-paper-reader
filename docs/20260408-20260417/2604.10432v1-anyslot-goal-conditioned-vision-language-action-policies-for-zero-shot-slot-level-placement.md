---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人操作的任务目标条件视觉-语言-动作策略
tldr: AnySlot是一个针对零样本槽位级物体放置任务的视觉-语言-动作（VLA）框架。针对现有VLA模型在处理复杂指令和高精度放置时的不足，AnySlot通过引入显式空间视觉目标（场景标记）作为中间表示，将任务解耦为高层槽位选择和底层目标条件控制。此外，研究者还推出了包含九类任务的SlotBench基准测试。实验证明，AnySlot在零样本槽位放置任务中显著优于现有基准模型，实现了亚厘米级的执行精度。
selection_source: fresh_fetch
motivation: 现有的单体VLA策略在处理需要高精度和复杂语义理解的槽位级放置任务时，难以兼顾可靠的槽位定位与精确的动作执行。
method: 提出一种分层框架，先将语言指令转化为显式的场景标记作为视觉目标，再由目标条件VLA策略引导机器人完成精确放置。
result: 在SlotBench基准测试中，AnySlot在零样本槽位级放置任务上的表现显著优于传统的扁平化VLA模型和模块化定位方法。
conclusion: 通过引入中间视觉目标表示来解耦语义理解与空间控制，AnySlot有效提升了机器人策略在复杂放置任务中的准确性和鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略来说仍然是一个重大挑战。插槽级任务既需要可靠的插槽定位，也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，这是一个通过引入显式空间视觉目标作为语言定位与控制之间的中间表示，从而降低组合复杂性的框架。AnySlot 通过生成场景标记将语言转化为显式的视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到目前缺乏针对此类高精度任务的基准测试，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置方面显著优于扁平化 VLA 基准模型和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

这是一份关于论文《AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）策略在处理“插槽级放置”（Slot-level placement）任务时表现不佳。这类任务要求机器人根据复杂的组合语言指令（如“放在左下角第二个格子里”），在密集的候选槽位中识别目标，并实现亚厘米级的操作精度。
*   **研究背景**：
    *   **扁平化策略（Flat Policies）**：直接将指令映射到动作，但在处理复杂空间推理时能力不足，且难以兼顾语义准确性和几何精度。
    *   **模块化策略（Modular Policies）**：通常将指令转化为坐标点，但坐标点丢失了槽位的几何边界信息，且视觉语言模型（VLM）的坐标回归往往不够精确。

### 2. 方法论
AnySlot 提出了一个两阶段的框架，核心思想是**将语言指令转化为显式的空间视觉目标（Visual Goal）**，以此作为推理与控制之间的中间表示。
*   **第一阶段：高层目标构建（High-level Goal Construction）**
    *   使用现成的图像生成模型（如 Nano-Banana）根据语言指令在主视图图像中生成一个“蓝色球体”标记。
    *   利用深度图和相机内参将该标记中心反投影到 3D 世界空间，形成一个 3D 锚点。
    *   将该 3D 锚点重新投影到所有相机视角（如手眼相机、侧边相机），生成视图一致的视觉覆盖层（Overlays）。
*   **第二阶段：底层目标条件策略（Low-level Goal-Conditioned Policy）**
    *   采用连续生成式 VLA 策略（基于 $\pi_{0.5}$，使用 PaliGemma-3B 骨干网络）。
    *   策略输入包含带有视觉标记的多视图图像，指令被简化为固定的模板（如“根据蓝球引导移动物体”），从而将策略的注意力集中在视觉目标的执行上，而非复杂的语义解析。

### 3. 实验设计
*   **基准测试（SlotBench）**：论文引入了专门设计的 SlotBench，包含 9 类挑战任务：序数推理、尺寸比较、高度比较、距离推理、组合关系、逻辑否定、模糊语言、功能性推理（Affordance）和世界知识。
*   **实验场景**：在 SAPIEN 仿真环境中构建，涉及多种托盘几何形状、物体姿态随机化以及复杂的干扰项。
*   **对比方法**：
    *   **扁平化 VLA**：OpenVLA、$\pi_0$、$\pi_{0.5}$、Diffusion Policy。
    *   **模块化方法**：RoboPoint、HAMSTER、AnyPlace。
    *   **Oracle 基准**：由人类专家提供完美的高层目标，以测试底层控制器的性能上限。

### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA H200 GPU**。
*   **训练细节**：对 $\pi_{0.5}$ 策略进行了 20,000 步的全量微调，Batch Size 为 64。
*   **数据集**：使用合成数据集 $D_{syn}$ 进行训练，包含随机化的物体姿态和槽位尺寸，并带有模拟生成的视觉引导球。

### 5. 实验数量与充分性
*   **实验规模**：每个任务类别进行 50 次试验，总计进行了数百次独立测试。
*   **消融实验**：
    *   对比了不同的高层定位模块（Molmo、GPT-4o、Gemini 1.5 Pro 等 VLM vs. Nano-Banana 等图像生成器）。
    *   对比了不同的底层策略架构（Diffusion Policy vs. $\pi_{0.5}$）。
    *   验证了合成数据集 $D_{syn}$ 对训练的重要性。
*   **充分性评价**：实验覆盖了从语义理解到物理执行的多个维度，对比了当前最先进的多种模型，并包含了真实世界的机器人验证，实验设计较为客观、公平且充分。

### 6. 主要结论与发现
*   **解耦的优越性**：通过视觉目标将“推理”与“控制”解耦，AnySlot 在 SlotBench 上的平均成功率接近 90%，远超扁平化 VLA（通常低于 20%）。
*   **视觉目标优于坐标点**：图像生成模型提供的空间标记比 VLM 回归的坐标点更具鲁棒性，能提供更好的几何上下文。
*   **零样本能力**：AnySlot 展现了极强的零样本泛化能力，能够处理训练中未见过的复杂逻辑指令和槽位布局。

### 7. 优点（亮点）
*   **创新的中间表示**：引入“场景标记”作为视觉目标，巧妙地解决了 VLA 模型在精确空间定位上的短板。
*   **高质量基准**：SlotBench 填补了机器人领域缺乏高精度、复杂语义放置任务基准的空白。
*   **视图一致性**：通过 3D 锚点重投影技术，确保了多摄像头系统在执行任务时的一致性。

### 8. 不足与局限
*   **推理延迟**：依赖外部图像生成模型会引入额外的计算开销，导致推理延迟较高（Nano-Banana 约需 11-16 秒），难以实现完全实时的闭环调整。
*   **依赖感知精度**：系统的准确性高度依赖于相机标定和深度图的质量，在深度信息缺失或标定误差较大的环境下可能失效。
*   **任务范围限制**：目前主要集中在“放置”这一特定动作，对于更复杂的长程操作（Long-horizon tasks）尚待验证。

（完）
