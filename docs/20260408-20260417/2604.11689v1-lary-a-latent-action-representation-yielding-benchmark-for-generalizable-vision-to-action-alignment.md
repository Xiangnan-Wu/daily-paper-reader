---
title: "LARY: A Latent Action Representation Yielding Benchmark for Generalizable Vision-to-Action Alignment"
title_zh: LARY：一个用于可泛化视觉到动作对齐的潜在动作表示生成基准
authors: "Dujun Nie, Fengjiao Chen, Qi Lv, Jun Kuang, Xiaoyu Li, Xuezhi Cao, Xunliang Cai"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11689v1"
tags: ["query:robot"]
score: 10.0
evidence: VLA 模型的潜在动作表示
tldr: 针对视觉-语言-动作（VLA）模型中显式动作数据短缺的问题，本文提出了LARY基准，用于评估从视频中提取的潜动作表示在语义理解和机器人控制方面的能力。通过包含百万级视频和多模态轨迹的大规模数据集，研究发现通用视觉基础模型在动作对齐上优于专门的具身模型，且潜空间比像素空间更适合物理动作对齐。
selection_source: fresh_fetch
motivation: 旨在解决如何从大规模无标注人类视频中提取有效的潜动作表示，以克服VLA模型训练中动作数据匮乏的挑战。
method: 提出了LARY基准框架，通过涵盖151类动作、百万级视频及多具身运动轨迹的大规模数据集，统一评估高层语义动作和底层机器人控制。
result: 实验表明通用视觉基础模型在动作表征上优于专门的具身模型，且基于潜空间的视觉表征比像素空间更契合物理动作空间。
conclusion: 研究强调了语义级抽象在视觉到动作映射中的核心作用，证明了通用视觉知识对物理控制具有内在的促进价值。
---

## 摘要
虽然显式动作数据的短缺限制了视觉-语言-动作（VLA）模型的发展，但人类动作视频提供了一个规模庞大且无需标注的数据源。利用大规模人类视频数据集的一个关键挑战在于将视觉信号转化为独立于本体（ontology-independent）的表示，即潜在动作（latent actions）。然而，潜在动作表示从视觉观测中推导出鲁棒控制的能力尚未得到严格评估。我们提出了 LARY（Latent Action Representation Yielding）基准，这是一个统一的框架，用于在高级语义动作（做什么）和低级机器人控制（怎么做）两个层面上评估潜在动作表示。该精心策划的数据集包含超过 100 万个视频（1,000 小时），涵盖 151 个动作类别，以及跨越不同具身智能体和环境的 62 万个图像对和 59.5 万条运动轨迹。我们的实验揭示了两个关键见解：（i）在没有任何动作监督的情况下训练的通用视觉基础模型，其表现始终优于专门的具身潜在动作模型；（ii）基于潜空间的视觉空间在本质上比基于像素的空间能更好地与物理动作空间对齐。这些结果表明，通用视觉表示固有地编码了用于物理控制的动作相关知识，且语义层面的抽象是比像素级重建更有效的视觉到动作路径。

## Abstract
While the shortage of explicit action data limits Vision-Language-Action (VLA) models, human action videos offer a scalable yet unlabeled data source. A critical challenge in utilizing large-scale human video datasets lies in transforming visual signals into ontology-independent representations, known as latent actions. However, the capacity of latent action representation to derive robust control from visual observations has yet to be rigorously evaluated. We introduce the Latent Action Representation Yielding (LARY) Benchmark, a unified framework for evaluating latent action representations on both high-level semantic actions (what to do) and low-level robotic control (how to do). The comprehensively curated dataset encompasses over one million videos (1,000 hours) spanning 151 action categories, alongside 620K image pairs and 595K motion trajectories across diverse embodiments and environments. Our experiments reveal two crucial insights: (i) General visual foundation models, trained without any action supervision, consistently outperform specialized embodied latent action models. (ii) Latent-based visual space is fundamentally better aligned to physical action space than pixel-based space. These results suggest that general visual representations inherently encode action-relevant knowledge for physical control, and that semantic-level abstraction serves as a fundamentally more effective pathway from vision to action than pixel-level reconstruction.

---

## 论文详细总结（自动生成）

### LARY：一个用于可泛化视觉到动作对齐的潜在动作表示生成基准

#### 1. 论文的核心问题与整体含义
论文的核心问题在于：**如何评估和提升从大规模无标注视频中提取的“潜在动作（Latent Actions）”表示的质量。**
在具身智能（Embodied AI）领域，带动作标签的机器人数据极度匮乏，而人类视频数据规模巨大但缺乏显式动作标注。现有的视觉-语言-动作（VLA）模型尝试将视频转化为“潜在动作”来学习控制逻辑，但缺乏一个**解耦的、标准化的基准**来直接衡量这些表示的有效性。LARY 基准旨在填补这一空白，评估潜在动作在“语义理解（做什么）”和“物理控制（怎么做）”两个维度的表现。

#### 2. 论文提出的方法论
论文提出了 **LARY (Latent Action Representation Yielding) 基准框架**，其核心思想和技术细节包括：
*   **层次化评估协议：**
    *   **高层语义探测（Semantic Probing）：** 通过分类任务评估模型区分“原子动作”（如：向上移动、关闭夹持器）和“复合行为”（如：抓取、放置、搅拌）的能力。
    *   **底层物理映射（Physical Mapping）：** 通过回归任务评估模型从潜在表示中重建机器人末端执行器轨迹（如 7-DoF/16-DoF 动作）的能力。
*   **自动化数据引擎：** 利用大语言模型（如 Doubao-1.5-pro-vision）对海量原始视频进行自动时间分割、语义对齐和一致性检查，构建了统一分类体系下的多具身数据集。
*   **通用潜在动作模型（General LAMs）：** 提出一种新范式，将冻结的通用视觉基础模型（如 DINOv3, SigLIP 2）作为编码器，结合 VQ-VAE 架构训练潜在动作表示，以利用互联网规模的视觉先验。

#### 3. 实验设计
*   **数据集与场景：**
    *   **规模：** 超过 120 万个视频剪辑（1000+ 小时）、62 万个图像对、59.5 万条运动轨迹。
    *   **多样性：** 涵盖 151 个动作类别，涉及 11 种机器人平台（如 Franka, AgiBot G1）及人类视角，包含模拟和真实环境（厨房、工厂、商店等）。
*   **对比方法（11 种模型）：**
    *   **具身 LAMs：** LAPA, UniVLA, villa-X。
    *   **通用语义编码器：** DINOv3, V-JEPA 2。
    *   **生成式像素编码器：** Wan2.2 VAE, FLUX.2-dev VAE。
    *   **论文提出的 General LAMs：** 基于 DINOv2/v3, SigLIP 2, MAGVIT2 改造的模型。

#### 4. 资源与算力
*   **算力设备：** 使用了 **8 台 NVIDIA H800 (141GB) GPU**。
*   **训练细节：** General LAMs 在包含 200 万帧的内部数据集上训练了 **100,000 个步数（steps）**。输入图像分辨率统一为 224x224。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大规模的跨模型对比，涵盖了分类（原子/复合、人类/机器人）和回归（不同机器人平台）多项任务。
*   **消融实验：** 针对量化瓶颈进行了详细消融，包括码本大小（Codebook Size）、序列长度（Sequence Length）和潜在维度（Latent Dimension）。
*   **充分性与公平性：** 实验通过冻结权重、统一投影维度和多头优化策略确保了不同架构模型之间的公平对比。引入了长尾分布分析和注意力热图可视化，增强了实验的客观性和深度。

#### 6. 论文的主要结论与发现
*   **通用视觉模型胜过专用模型：** 未经动作监督训练的通用视觉基础模型（如 V-JEPA 2, DINOv3）在动作表征上始终优于专门的具身潜在动作模型。
*   **潜空间优于像素空间：** 基于潜在特征（Latent-based）的视觉空间比基于像素（Pixel-based）的空间能更好地与物理动作空间对齐。
*   **语义抽象是关键：** 语义层面的抽象是从视觉到动作映射的更有效路径，而非简单的像素级重建。
*   **量化稳定性：** 发现适中的码本大小和较长的序列长度对于维持潜在动作空间的稳定性和高利用率至关重要。

#### 7. 优点
*   **解耦评估：** 首次将潜在动作表示的质量与下游策略性能解耦，提供了直接的测量标准。
*   **数据规模巨大：** 构建了目前已知规模最大、覆盖具身类型最全的潜在动作评估数据集。
*   **自动化流程：** 开发的自动化数据引擎解决了具身数据标注不一致和边界模糊的痛点。
*   **见解深刻：** 揭示了通用视觉先验在机器人控制中的巨大潜力，为未来 VLA 模型的设计指明了方向。

#### 8. 不足与局限
*   **机器人数据性能差距：** 虽然 General LAMs 在人类动作上表现优异，但在特定机器人动作上的表现仍受限于机器人相关训练数据的规模和多样性。
*   **精细动作挑战：** 对于极高频或极精细的指尖操作（如微小的手指关节运动），目前的视觉编码器和回归模型仍面临挑战。
*   **应用限制：** 评估主要集中在末端执行器的宏观位移，尚未完全覆盖所有复杂的接触力学或多模态反馈（如触觉）。

（完）
