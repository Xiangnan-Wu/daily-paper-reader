---
title: "WOMBET: World Model-based Experience Transfer for Robust and Sample-efficient Reinforcement Learning"
title_zh: WOMBET：基于世界模型的经验迁移，用于鲁棒且高效采样的强化学习
authors: "Mintae Kim, Koushil Sreenath"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08958v2"
tags: ["query:robot"]
score: 10.0
evidence: 基于世界模型的机器人强化学习经验迁移
tldr: 针对机器人强化学习中数据采集成本高且风险大的挑战，本文提出WOMBET框架，通过联合生成与利用先验数据实现高效经验迁移。该方法在源任务中学习世界模型，利用不确定性惩罚规划生成高质量离线数据，并在目标任务中通过自适应采样进行在线微调。理论分析证明了该目标的下界性质，实验表明其在连续控制任务中显著提升了采样效率和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的离线到在线强化学习通常依赖固定的先验数据集，缺乏对如何主动生成可靠数据以优化迁移过程的研究。
method: 通过在源任务中构建世界模型，结合不确定性惩罚规划生成并筛选高质量轨迹，随后在目标任务中利用自适应采样进行平滑微调。
result: 在连续控制基准测试中，WOMBET在采样效率和最终任务表现上均显著优于现有的强基线算法。
conclusion: 联合优化数据生成与迁移机制是实现鲁棒且样本高效的强化学习的有效途径。
---

## 摘要
机器人领域中的强化学习（RL）通常受限于数据收集的成本和风险，这促使了从源任务到目标任务的经验迁移。离线到在线（Offline-to-online）强化学习利用先验数据，但通常假设数据集是给定的且固定的，并未解决如何生成可靠的迁移数据的问题。我们提出了“基于世界模型的经验迁移”（WOMBET），这是一个联合生成并利用先验数据的框架。WOMBET 在源任务中学习一个世界模型，并通过不确定性惩罚规划生成离线数据，随后筛选出具有高回报和低认知不确定性的轨迹。随后，它在目标任务中利用离线和在线数据之间的自适应采样进行在线微调，实现了从先验驱动的初始化到任务特定适配的平稳过渡。我们证明了不确定性惩罚目标提供了真实回报的下界，并推导出了捕捉分布失配和近似误差的有限样本误差分解。实验表明，在连续控制基准测试中，WOMBET 相比强基准模型提高了采样效率和最终性能，证明了联合优化数据生成与迁移的优势。

## Abstract
Reinforcement learning (RL) in robotics is often limited by the cost and risk of data collection, motivating experience transfer from a source task to a target task. Offline-to-online RL leverages prior data but typically assumes a given fixed dataset and does not address how to generate reliable data for transfer. We propose \textit{World Model-based Experience Transfer} (WOMBET), a framework that jointly generates and utilizes prior data. WOMBET learns a world model in the source task and generates offline data via uncertainty-penalized planning, followed by filtering trajectories with high return and low epistemic uncertainty. It then performs online fine-tuning in the target task using adaptive sampling between offline and online data, enabling a stable transition from prior-driven initialization to task-specific adaptation. We show that the uncertainty-penalized objective provides a lower bound on the true return and derive a finite-sample error decomposition capturing distribution mismatch and approximation error. Empirically, WOMBET improves sample efficiency and final performance over strong baselines on continuous control benchmarks, demonstrating the benefit of jointly optimizing data generation and transfer.

---

## 论文详细总结（自动生成）

这篇论文提出了 **WOMBET**（World Model-based Experience Transfer），旨在解决机器人强化学习中数据采集成本高、风险大以及离线数据质量不可控的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在机器人任务中实现高效且鲁棒的经验迁移？
*   **研究背景**：
    *   **在线 RL** 采样效率低且在现实世界中存在安全风险。
    *   **离线 RL** 依赖于给定的静态数据集，无法解决数据来源和质量问题。
    *   **离线到在线（Offline-to-Online）RL** 虽然结合了两者的优点，但通常假设离线数据是现成的，且在微调阶段容易遇到分布偏移（Distributional Shift）导致的价值估计爆炸。
*   **动机**：受生物系统利用内部“世界模型”重用过去经验的启发，作者希望建立一个能**主动生成**高质量迁移数据并能**平滑过渡**到新任务的统一框架。

### 2. 方法论
WOMBET 的核心思想是**联合优化数据生成与迁移过程**，主要包含以下关键技术：
*   **基于世界模型的数据生成**：在源任务中学习一个概率集成世界模型（Probabilistic Ensemble World Model）。利用该模型进行不确定性惩罚的 MPC（模型预测控制）规划，生成合成轨迹。
*   **双准则过滤（Dual-Criterion Filtering）**：为了确保迁移数据的可靠性，WOMBET 仅保留满足以下两个条件的轨迹：
    1.  **高回报**：确保数据具有任务相关性。
    2.  **低认知不确定性**：通过模型集成方差衡量，剔除模型预测不可靠的区域，从而抑制模型偏差。
*   **自适应混合数据训练**：在目标任务的在线微调阶段，算法动态调整离线数据（源任务生成）与在线数据（目标任务采集）的采样比例 $\alpha$。该比例基于 **TD 误差（时序差分误差）** 自动调节：当 TD 误差大（模型不稳）时增加离线数据权重以稳定训练；当误差减小时增加在线数据权重以促进适配。
*   **理论保证**：论文证明了不确定性惩罚的目标函数是真实回报的一个下界，并推导出了包含分布失配和有限样本近似误差的性能边界。

### 3. 实验设计
*   **实验场景**：基于 **MuJoCo** 物理引擎的连续控制基准测试（如 HalfCheetah, Hopper, Walker2d 等）。
*   **对比基准（Benchmarks）**：
    *   **在线 RL**：SAC, PPO, TD3（评估采样效率）。
    *   **离线 RL**：BC, IQL, CQL, MOPO, COMBO（评估零样本迁移能力）。
    *   **离线到在线 RL**：RLPD, AWAC 等（评估迁移与微调性能）。
*   **评估指标**：归一化回报（Normalized Return）随交互步数的变化曲线。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 LayerNorm 稳定 Critic 网络，以及集成模型（Ensemble）的数量等超参数，但缺乏硬件环境的详细描述。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个不同的 MuJoCo 环境中进行了测试。
    *   所有实验均使用了 **5 个随机种子**，并绘制了标准差阴影区域。
    *   进行了详细的**消融实验**，分别验证了“自适应采样”和“双准则过滤”中各组件（仅回报过滤 vs 仅不确定性过滤）的贡献。
*   **充分性评价**：实验设计较为全面，涵盖了从零开始训练、纯离线迁移以及离线到在线微调等多个维度的对比，能够客观反映算法在不同阶段的优势。

### 6. 主要结论与发现
*   **采样效率显著提升**：WOMBET 在达到相同性能时，所需的真实环境交互次数通常不到标准在线 RL 算法的一半。
*   **迁移鲁棒性**：通过双准则过滤生成的数据比原始模型生成的全量数据更具迁移价值，能有效避免微调初期的性能崩溃。
*   **自适应机制的必要性**：动态调整离线/在线数据比例比固定比例（如 50/50）具有更好的收敛速度和最终性能。

### 7. 优点（亮点）
*   **闭环框架**：打破了“给定离线数据集”的强假设，将数据生成纳入迁移学习框架。
*   **理论与实践结合**：不仅提出了算法，还通过 Lipschitz 连续性等假设给出了性能下界的理论证明。
*   **稳定性设计**：引入 LayerNorm 和集成最小值（Ensemble-min）目标，有效缓解了离线到在线切换时的价值过估计问题。

### 8. 不足与局限
*   **环境假设**：实验主要集中在模拟环境（MuJoCo），尚未在具有复杂视觉输入或高度非线性动力学的真实机器人硬件上验证。
*   **源/目标任务差异限制**：虽然处理了奖励和初始状态的差异，但如果源任务与目标任务的动力学（Dynamics）差异过大，基于源任务模型生成的数据可能失效。
*   **超参数敏感性**：双准则过滤中的阈值（$u_{th}$ 和 $J_{th}$）以及自适应采样的增益参数可能需要针对不同任务进行精细调优。

（完）
