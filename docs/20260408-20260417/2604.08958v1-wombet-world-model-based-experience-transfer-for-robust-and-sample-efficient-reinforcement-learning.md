---
title: "WOMBET: World Model-based Experience Transfer for Robust and Sample-efficient Reinforcement Learning"
title_zh: WOMBET：用于鲁棒且样本高效强化学习的基于世界模型的经验迁移
authors: "Mintae Kim, Koushil Sreenath"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08958v1"
tags: ["query:robot"]
score: 10.0
evidence: 基于世界模型的机器人经验迁移
tldr: 针对机器人强化学习中数据采集成本高且风险大的挑战，本文提出WOMBET框架，旨在实现从源任务到目标任务的高效经验迁移。该框架利用世界模型通过不确定性惩罚规划自主生成高质量离线数据，并结合自适应采样策略在目标任务中进行在线微调。研究不仅提供了理论上的误差界限证明，还在连续控制基准测试中验证了其在提升样本效率和任务性能方面的显著优势。
selection_source: fresh_fetch
motivation: 现有的离线到在线强化学习通常假设数据集固定，缺乏一种能同时生成可靠先验数据并有效迁移至新任务的统一机制。
method: WOMBET通过在源任务学习世界模型并利用不确定性惩罚规划生成、筛选轨迹，随后在目标任务中通过自适应采样实现从先验引导到任务适配的平滑过渡。
result: 实验结果显示，WOMBET在多个连续控制基准任务上的样本效率和最终性能均显著优于现有的强基线方法。
conclusion: 通过联合优化数据生成与迁移过程，WOMBET证明了利用世界模型生成高质量先验数据能有效增强强化学习的稳健性与学习效率。
---

## 摘要
机器人领域中的强化学习（RL）通常受限于数据采集的成本和风险，这促使了从源任务到目标任务的经验迁移。离线到在线（Offline-to-online）强化学习利用先验数据，但通常假设数据集是给定的且固定的，并未解决如何生成可靠迁移数据的问题。我们提出了“基于世界模型的经验迁移”（WOMBET），这是一个联合生成并利用先验数据的框架。WOMBET 在源任务中学习一个世界模型，并通过不确定性惩罚规划生成离线数据，随后筛选出具有高回报和低认知不确定性的轨迹。接着，它在目标任务中进行在线微调，通过在离线和在线数据之间进行自适应采样，实现了从先验驱动的初始化到任务特定适配的平稳过渡。我们证明了不确定性惩罚目标提供了真实回报的下界，并推导出了捕捉分布失配和近似误差的有限样本误差分解。实验表明，在连续控制基准测试中，WOMBET 相比强基准方法提高了样本效率和最终性能，证明了联合优化数据生成与迁移的优势。

## Abstract
Reinforcement learning (RL) in robotics is often limited by the cost and risk of data collection, motivating experience transfer from a source task to a target task. Offline-to-online RL leverages prior data but typically assumes a given fixed dataset and does not address how to generate reliable data for transfer. We propose \textit{World Model-based Experience Transfer} (WOMBET), a framework that jointly generates and utilizes prior data. WOMBET learns a world model in the source task and generates offline data via uncertainty-penalized planning, followed by filtering trajectories with high return and low epistemic uncertainty. It then performs online fine-tuning in the target task using adaptive sampling between offline and online data, enabling a stable transition from prior-driven initialization to task-specific adaptation. We show that the uncertainty-penalized objective provides a lower bound on the true return and derive a finite-sample error decomposition capturing distribution mismatch and approximation error. Empirically, WOMBET improves sample efficiency and final performance over strong baselines on continuous control benchmarks, demonstrating the benefit of jointly optimizing data generation and transfer.

---

## 论文详细总结（自动生成）

这篇论文提出了 **WOMBET**（World Model-based Experience Transfer），旨在解决机器人强化学习中数据采集成本高、风险大以及现有“离线到在线”迁移学习中数据集固定且质量不可控的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在机器人强化学习中，既能安全、高效地从源任务（Source Task）生成高质量的先验数据，又能将这些数据有效地迁移到目标任务（Target Task）中进行快速适配？
*   **研究动机**：
    *   **在线 RL** 样本效率低，且在现实机器人上进行探索存在安全风险。
    *   **离线 RL** 依赖于给定的静态数据集，无法解决数据从何而来以及数据与新任务不匹配的问题。
    *   **现有迁移方法** 通常假设离线数据是现成的，忽略了数据生成过程与迁移过程的联合优化。

### 2. 核心方法论
WOMBET 提出了一个联合优化数据生成与迁移的统一框架，其核心流程如下：
*   **基于世界模型的数据生成**：在源任务中学习一个概率集成世界模型（Probabilistic Ensemble World Model），利用该模型进行不确定性惩罚的 MPC（模型预测控制）规划。
*   **双准则过滤（Dual-Criterion Filtering）**：为了确保生成的离线数据集 $D_S$ 的可靠性，框架通过两个指标筛选轨迹：**高回报**（确保任务相关性）和**低认知不确定性**（确保模型预测的准确性，减少模型偏差）。
*   **自适应混合数据训练**：在目标任务的在线微调阶段，算法动态调整离线数据 $D_S$ 和在线数据 $D_T$ 的采样比例。
    *   **自适应逻辑**：基于评论家网络（Critic）的 TD 误差。当 TD 误差较大（欠拟合）时，增加离线数据比例以稳定训练；随着在线表现提升，逐渐转向在线数据以减少分布偏移。
*   **理论保证**：论文证明了不确定性惩罚目标函数是真实回报的下界，并推导出了包含分布失配和有限样本近似误差的性能界限。

### 3. 实验设计
*   **实验场景**：使用了多种 **MuJoCo** 连续控制基准任务（如 HalfCheetah, Hopper, Walker2d, Ant 等）。
*   **对比基准（Benchmarks）**：
    *   **在线 RL 算法**：SAC, PPO, TD3（评估样本效率）。
    *   **离线 RL 算法**：BC, IQL, CQL, MOPO, COMBO（评估零样本迁移能力）。
    *   **离线到在线 RL 算法**：将上述离线算法在 WOMBET 生成的数据上预训练后进行微调。
*   **消融实验**：验证了“双准则过滤”和“自适应采样策略”对最终性能的贡献。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 LayerNorm 稳定 Critic 网络，以及集成模型包含 5-7 个神经网络，这暗示了该方法相比纯模型无关（Model-free）方法会有更高的计算开销，尤其是在 MPC 规划阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的环境任务上进行了测试，每个实验均使用 **5 个随机种子**并标注了标准差。
*   **充分性**：实验设计较为全面，不仅对比了最先进的基线方法，还通过消融实验深入探讨了每个核心组件的必要性。
*   **公平性**：在对比“离线到在线”基线时，所有方法均使用 WOMBET 生成的相同数据集进行初始化，确保了对比的重点在于“迁移机制”而非“数据质量”。

### 6. 主要结论与发现
*   **样本效率显著提升**：WOMBET 在达到相同性能时，所需的真实环境交互次数通常不到标准在线 RL 算法的一半。
*   **迁移鲁棒性**：通过不确定性惩罚和双准则过滤，WOMBET 生成的数据能有效避免模型偏差（Model Bias），在目标任务中表现出比传统离线 RL 更强的适配能力。
*   **自适应采样的重要性**：动态调整数据比例比固定比例（如 50/50）能更有效地平衡偏差与方差，防止在线微调初期的性能崩溃。

### 7. 优点
*   **闭环设计**：将数据生成、筛选、迁移和微调整合在一个迭代优化的框架内，解决了离线数据“从哪来”的问题。
*   **理论与实践结合**：不仅有直观的算法设计，还提供了严谨的误差分解和性能下界证明。
*   **稳定性强**：引入 LayerNorm 和集成最小值目标，有效缓解了离线到在线切换时的值函数爆炸问题。

### 8. 不足与局限
*   **计算开销**：基于世界模型的 MPC 规划和集成模型训练在计算上比纯 Model-free 方法更昂贵。
*   **任务相似性假设**：虽然论文讨论了迁移，但实验主要集中在动力学相同、奖励或初始状态略有差异的任务上。对于动力学差异巨大的极端任务迁移，其表现尚待验证。
*   **超参数敏感性**：不确定性惩罚系数 $\lambda$ 和过滤阈值可能需要针对不同任务进行精细调整。

（完）
