---
title: "DA-PTQ: Drift-Aware Post-Training Quantization for Efficient Vision-Language-Action Models"
title_zh: DA-PTQ：面向高效视觉-语言-动作模型的漂移感知后训练量化
authors: "Siyuan Xu, Tianshi Wang, Fengling Li, Lei Zhu, Heng Tao Shen"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11572v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人的高效视觉-语言-动作模型
tldr: 针对视觉-语言-动作模型（VLA）在机器人部署中面临的显存和计算压力，本文提出了DA-PTQ量化框架。研究发现传统PTQ在序列控制中会产生时间误差累积，导致运动漂移。DA-PTQ通过跨空间表示补偿和运动驱动的混合精度分配，有效缓解了多模态表示与动作空间之间的失真，并在低比特设置下实现了接近全精度的性能，显著提升了VLA在资源受限平台上的部署效率。
selection_source: fresh_fetch
motivation: 传统量化方法在VLA序列控制中会产生时间误差累积，导致机器人执行轨迹出现严重的运动漂移。
method: 提出DA-PTQ框架，包含跨空间表示补偿以减少模态间失真，以及运动驱动的混合精度分配以优化轨迹误差。
result: 实验表明DA-PTQ显著降低了运动漂移，在低比特量化下达到了与全精度模型相当的性能。
conclusion: DA-PTQ为VLA模型在资源受限机器人平台上的高效部署提供了一种有效的后量化解决方案。
---

## 摘要
视觉-语言-动作模型（VLAs）在具身智能领域展现出巨大潜力，但由于其极高的内存和计算需求，在资源受限的机器人上部署仍面临挑战。虽然后训练量化（PTQ）提供了一种高效的解决方案，但直接将 PTQ 应用于 VLAs 往往会导致序列控制过程中的性能严重下降。我们发现时间误差累积是一个关键因素，即视觉-语言到动作接口处的量化扰动被逐渐放大，导致执行轨迹中出现运动学漂移。为了解决这一问题，我们提出了漂移感知后训练量化（DA-PTQ），它将量化建模为序列决策过程中的漂移感知优化问题。DA-PTQ 包含两个组件：（1）跨空间表示补偿，旨在减轻多模态表示与动作空间之间的结构化失真，以提高动作一致性；（2）运动驱动的混合精度分配，通过最小化轨迹级运动误差来分配位宽。大量实验表明，DA-PTQ 显著减少了运动学漂移，并在低比特设置下实现了与全精度模型相当的性能，从而使 VLAs 在资源受限的机器人平台上进行实际部署成为可能。

## Abstract
Vision-Language-Action models (VLAs) have demonstrated strong potential for embodied AI, yet their deployment on resource-limited robots remains challenging due to high memory and computational demands. While Post-Training Quantization (PTQ) provides an efficient solution, directly applying PTQ to VLAs often results in severe performance degradation during sequential control. We identify temporal error accumulation as a key factor, where quantization perturbations at the vision-language-to-action interface are progressively amplified, leading to kinematic drift in executed trajectories. To address this issue, we propose Drift-Aware Post-Training Quantization (DA-PTQ), which formulates quantization as a drift-aware optimization problem over sequential decision processes. DA-PTQ consists of two components: (1) Cross-Space Representation Compensation, which mitigates structured distortions between multimodal representations and action space to improve action consistency, and (2) Motion-Driven Mixed-Precision Allocation, which assigns bit-widths by minimizing trajectory-level motion errors. Extensive experiments show that DA-PTQ significantly reduces kinematic drift and achieves comparable performance to full-precision models under low-bit settings, enabling practical deployment of VLAs on resource-limited robotic platforms.

---

## 论文详细总结（自动生成）

这是一份关于论文《DA-PTQ: Drift-Aware Post-Training Quantization for Efficient Vision-Language-Action Models》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作模型（VLA）虽然在具身智能任务中表现出色，但其巨大的参数量（如 7B 以上）导致在资源受限的机器人硬件上部署困难。
*   **研究动机**：传统的后训练量化（PTQ）方法在处理静态任务（如图像分类、文本生成）时效果良好，但在机器人序列控制任务中，量化引入的微小扰动会在“感知-动作”接口处被时间步累积并经由机器人动力学放大，导致**运动学漂移（Kinematic Drift）**，最终使机器人无法完成任务。
*   **整体含义**：本文旨在开发一种无需重新训练的量化框架，通过显式建模和抑制序列执行中的误差累积，实现 VLA 模型的高效压缩与稳定部署。

### 2. 方法论：DA-PTQ 框架
DA-PTQ 将量化视为一个“漂移感知”的优化问题，核心思想包含两个关键技术：

*   **跨空间表示补偿 (Cross-Space Representation Compensation, CSRC)**：
    *   **原理**：量化会扭曲视觉-语言特征到动作空间的映射。CSRC 通过校准数据集提取全精度和量化后的激活统计量（均值、方差、协方差）。
    *   **技术细节**：利用轻量级的仿射变换和低秩矩阵更新（基于 SVD 分解）来对齐量化前后的特征分布。这些补偿参数在推理前会被“折叠”进权重中，因此**不增加任何推理开销**。
*   **运动驱动的混合精度分配 (Motion-Driven Mixed-Precision Allocation, DA-MPA)**：
    *   **原理**：并非所有模型层对最终轨迹误差的影响都一致。
    *   **技术细节**：引入**结构化雅可比矩阵（Structural Jacobian）**作为代理指标，评估动作向量中每个维度（平移、旋转、夹持）对长程轨迹漂移的贡献。根据漂移敏感度得分，对关键层保留高精度（如 BF16），对非敏感层进行激进量化（如 W4），从而在压缩率和控制精度间取得平衡。

### 3. 实验设计
*   **数据集与场景**：
    *   **校准集**：从 BridgeData V2 中抽取 512 条代表性轨迹。
    *   **评估环境**：使用 SimplerEnv 仿真基准。
*   **Benchmark 与机器人**：
    *   **WidowX 机器人**：评估域内（In-domain）任务，如“放勺子到毛巾上”、“放胡萝卜到盘子里”。
    *   **Google Robot**：评估跨域（Cross-domain）零样本泛化能力，包括视觉匹配和变体聚合任务。
*   **对比方法**：
    *   **CogACT (FP)**：全精度基准模型。
    *   **VLA-Cache**：NeurIPS'25 提出的推理加速方法（仅缓存，不量化）。
    *   **QuantVLA**：CVPR'26 提出的 VLA 量化基准方法。

### 4. 资源与算力
*   **硬件环境**：实验部署在 **NVIDIA RTX 5090 GPU** 上。
*   **算力需求**：由于是 PTQ（后训练量化）框架，该方法**无需模型微调或重训练**。主要的计算开销集中在校准阶段（前向传播收集统计量和计算雅可比矩阵），通常在单卡上数小时内即可完成。文中强调了其“训练免除（Training-free）”的特性，适合快速部署。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个不同的机器人平台（WidowX 和 Google Robot）。
    *   进行了多项操纵任务的成功率测试，并对比了内存减少量和推理加速比。
    *   **消融实验**：分别验证了 CSRC 和 DA-MPA 的独立贡献，证明了两者结合能达到帕累托最优。
    *   **定性分析**：提供了 7 自由度动作曲线的平滑度可视化。
*   **充分性评价**：实验设计较为充分，不仅验证了性能提升，还通过跨机器人实验证明了量化参数的泛化能力。对比基准包含了最新的同类研究，评价指标（成功率、显存、速度）全面且客观。

### 6. 主要结论与发现
*   **性能保持**：DA-PTQ 在 W4A8（权重 4bit，激活 8bit）设置下，成功率接近全精度模型，显著优于传统量化方法。
*   **效率提升**：相比全精度模型，DA-PTQ 实现了约 **42.5% 的显存减少**和 **54.8% 的推理加速**。
*   **漂移抑制**：通过雅可比矩阵引导的精度分配，有效消除了低比特量化常出现的动作高频振荡和轨迹偏离现象。

### 7. 优点与亮点
*   **漂移感知建模**：首次将机器人运动学中的雅可比矩阵引入量化敏感度分析，解决了具身智能特有的时间误差累积问题。
*   **零推理开销**：补偿机制通过代数折叠实现，在提升精度的同时不牺牲推理速度。
*   **强泛化性**：在 BridgeData 上校准的参数可以直接应用于不同型号的机器人（如 Google Robot），表现出良好的鲁棒性。

### 8. 不足与局限
*   **动力学简化**：雅可比矩阵的推导基于一定的运动学简化（如小角假设），在处理极度非线性或存在复杂接触动力学（如揉捏物体）的任务时，其误差估计可能不够准确。
*   **校准集依赖**：虽然表现出一定的泛化性，但量化效果仍受限于校准数据的分布，若部署环境与校准环境差异巨大，可能仍需重新校准。
*   **硬件限制**：虽然针对资源受限平台设计，但 4-bit 量化的实际加速效果高度依赖于底层硬件对低比特算术指令的支持（如 Tensor Core 的优化）。

（完）
