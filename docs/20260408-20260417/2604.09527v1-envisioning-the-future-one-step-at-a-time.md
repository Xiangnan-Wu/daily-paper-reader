---
title: "Envisioning the Future, One Step at a Time"
title_zh: 步进式预见未来
authors: "Stefan Andreas Baumann, Jannik Wiese, Tommaso Martorella, Mahdi M. Kalayeh, Björn Ommer"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09527v1"
tags: ["query:q12"]
score: 9.0
evidence: 将开放集未来场景动力学预测为轨迹上的逐步推理
tldr: 针对复杂场景演化预测中计算开销大、难以处理多模态不确定性的问题，本文提出了一种基于稀疏点轨迹的自回归扩散模型。该方法摒弃了传统的稠密视频生成，转而通过逐步推理预测场景中点的运动轨迹，能够从单张图像快速生成数千种物理合理的未来可能。研究还引入了OWM基准测试，实验证明该方法在保持高预测精度的同时，采样速度比稠密模拟器快几个数量级，为大规模探索未来假设提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的稠密视频预测方法在外观细节上消耗了过多计算资源，限制了对长程、多模态未来场景的高效探索。
method: 提出一种自回归扩散模型，通过对稀疏点轨迹进行逐步推理来建模场景动态，并显式捕捉随时间增长的不确定性。
result: 该方法在预测精度上媲美或超越了稠密模拟器，同时实现了数个数量级的采样速度提升，支持快速生成数千个多样化的未来轨迹。
conclusion: 通过将预测重点从稠密像素转向稀疏动力学轨迹，本研究为可扩展且实用的开放集未来场景预测提供了新路径。
---

## 摘要
准确预测复杂多样场景的演化，需要模型能够表征不确定性、沿长交互链进行模拟，并高效探索多种可能的未来。然而，现有的大多数方法依赖于稠密视频或潜空间预测，将大量容量消耗在稠密外观上，而非场景中点的底层稀疏轨迹。这使得对未来假设的大规模探索成本高昂，并在长程、多模态运动至关重要时限制了性能。我们通过将开放集未来场景动力学的预测建模为稀疏点轨迹上的步进式推理来解决这一问题。我们的自回归扩散模型通过短促且局部可预测的转换来推进这些轨迹，显式地建模了不确定性随时间的增长。这种以动力学为中心的表征能够从单张图像快速推演数千个多样的未来，并可选择性地受初始运动约束引导，同时保持物理合理性和长程一致性。我们进一步引入了 OWM，这是一个基于多样化野外视频的开放集运动预测基准，用于评估真实世界不确定性下预测轨迹分布的准确性和变异性。我们的方法在预测准确性上达到或超过了稠密模拟器，同时实现了高出几个数量级的采样速度，使开放集未来预测既具可扩展性又具实用性。项目主页：http://compvis.github.io/myriad。

## Abstract
Accurately anticipating how complex, diverse scenes will evolve requires models that represent uncertainty, simulate along extended interaction chains, and efficiently explore many plausible futures. Yet most existing approaches rely on dense video or latent-space prediction, expending substantial capacity on dense appearance rather than on the underlying sparse trajectories of points in the scene. This makes large-scale exploration of future hypotheses costly and limits performance when long-horizon, multi-modal motion is essential. We address this by formulating the prediction of open-set future scene dynamics as step-wise inference over sparse point trajectories. Our autoregressive diffusion model advances these trajectories through short, locally predictable transitions, explicitly modeling the growth of uncertainty over time. This dynamics-centric representation enables fast rollout of thousands of diverse futures from a single image, optionally guided by initial constraints on motion, while maintaining physical plausibility and long-range coherence. We further introduce OWM, a benchmark for open-set motion prediction based on diverse in-the-wild videos, to evaluate accuracy and variability of predicted trajectory distributions under real-world uncertainty. Our method matches or surpasses dense simulators in predictive accuracy while achieving orders-of-magnitude higher sampling speed, making open-set future prediction both scalable and practical. Project page: http://compvis.github.io/myriad.

---

## 论文详细总结（自动生成）

这是一份关于论文《Envisioning the Future, One Step at a Time》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：智能的核心在于预见未来并据此指导行为。然而，现有的视频生成模型或潜空间世界模型在预测未来时面临巨大的“视觉税”（Visual Tax）——它们将绝大部分计算资源消耗在生成高分辨率的像素外观上，而非底层的运动逻辑。
*   **核心问题**：如何在复杂、随机的开放世界中，高效地模拟成千上万种可能的未来轨迹，以支持长程推理和反事实探索（如：如果我这样击球，球会怎么滚）？
*   **整体含义**：论文提出了一种名为 **Myriad** 的模型，主张“预见未来不需要画出每一帧”。通过将预测目标从稠密像素转向稀疏点轨迹，实现了在保持物理一致性的同时，采样速度提升数个数量级。

### 2. 方法论
*   **核心思想**：将未来预测建模为单张图像上稀疏点轨迹的**自回归扩散过程**。
*   **关键技术细节**：
    *   **自回归表征**：模型按时间步（Step-wise）分解预测任务，每一步预测点的位移增量（$\Delta x$）。这种步进式推理能更好地处理复杂的多次交互（如台球碰撞）。
    *   **运动 Token 构造**：每个 Token 融合了三种信息：外观（起始点特征）、局部上下文（当前点特征）和身份标识（随机轨迹 ID，用于区分多个目标）。
    *   **快速推理块（Fast Reasoning Blocks）**：设计了并行 Transformer 结构，将自注意力、交叉注意力和前馈网络融合，并利用 KV 缓存技术，极大提升了推理吞吐量。
    *   **流匹配后验（Flow Matching Head）**：使用流匹配（FM）代替传统的 GMM 或分类头来建模运动分布，能更准确地捕捉多模态的不确定性。
    *   **尺度级联（Scale Cascade）**：针对运动幅度的长尾分布（有时微动，有时剧烈），引入多尺度缩放机制，增强了模型对不同尺度运动的建模稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **OWM (Open-World Motion)**：论文新提出的基准，包含95个野外视频，涵盖行人、动物、车辆等多样化运动。
    *   **物理诊断集**：使用 PhysicsIQ 和 Physion 的子集，评估模型对刚体碰撞等物理规律的理解。
    *   **Billiard (台球)**：在模拟环境中测试模型通过“预见未来”进行动作规划的能力。
*   **对比方法**：对比了 SVD 1.1、Wan2.2、CogVideo-X 1.5、MAGI-1 等最先进的稠密视频生成模型。
*   **Benchmark 逻辑**：采用 **Best-of-N**（N次采样中最接近真实值的）和 **Best-within-5min**（在5分钟计算预算内能达到的最高精度）作为核心指标。

### 4. 资源与算力
*   **硬件配置**：使用了 **16 张 Nvidia H200 GPU**。
*   **训练时长**：在 10M 规模的视频剪辑数据集上训练，约 **20 小时** 即可收敛。
*   **模型规模**：参数量为 **665M**，远小于动辄数十亿参数的视频生成模型。

### 5. 实验数量与充分性
*   **实验覆盖面**：实验涵盖了从野外开放场景到受控物理环境，再到闭环规划任务（台球），维度全面。
*   **消融实验**：对后验建模方式（FM vs GMM）、尺度级联、并行推理块的效率等核心组件进行了详尽的消融对比。
*   **客观性**：引入“时间预算限制”下的精度对比非常客观，直接揭示了稀疏模型在实际应用（如实时规划）中的巨大优势。

### 6. 主要结论与发现
*   **效率飞跃**：在相同的计算预算下，Myriad 的采样速度比视频模型快 **2000 倍**以上，能够探索数千种未来可能性。
*   **精度领先**：尽管不生成像素，但在轨迹预测精度上，Myriad 在 OWM 和物理基准测试中均达到或超过了顶级视频生成模型。
*   **规划能力**：在台球规划任务中，Myriad 凭借高吞吐量的模拟能力，在动作选择的准确率上显著优于所有基线模型。
*   **不确定性校准**：模型输出的标准差与实际预测误差高度相关，证明其对“不确定性”的建模是可靠的。

### 7. 优点
*   **避开视觉税**：通过稀疏化表征，解决了未来预测中计算开销过大的核心痛点。
*   **支持分支探索**：极高的速度使得“反事实推理”（探索多种如果...会怎样）变得实用。
*   **物理一致性强**：步进式推理有效模拟了物体间的链式反应和碰撞逻辑。
*   **即插即用**：支持用户通过简单的“Poke”（初始推力）来引导预测。

### 8. 不足与局限
*   **相机假设**：目前主要针对**静态相机**场景，对于带有剧烈自机运动（Ego-motion）的场景处理能力有限。
*   **伪标签依赖**：训练依赖于现有的点跟踪器（如 TapNext）生成的伪标签，可能会继承跟踪器的错误或偏差。
*   **语义缺失**：由于不生成像素，模型无法捕捉物体的形变（如流体、布料）或外观细节的变化。

（完）
