---
title: "Robotic Manipulation is Vision-to-Geometry Mapping ($f(v) \\rightarrow G$): Vision-Geometry Backbones over Language and Video Models"
title_zh: 机器人操作是视觉到几何的映射 ($f(v) \rightarrow G$)：优于语言和视频模型的视觉-几何骨干网络
authors: "Zijian Song, Qichang Li, Jiawei Zhou, Zhenlong Yuan, Tianshui Chen, Liang Lin, Guangrun Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12908v1"
tags: ["query:robot"]
score: 9.0
evidence: 对机器人操作控制中VLA和视频模型的评述
tldr: 机器人操纵本质上是视觉到几何的映射。本文针对现有VLA和视频模型缺乏3D几何先验的问题，提出了VGA（Vision-Geometry-Action）模型。该模型放弃了传统的语言或视频骨干网络，转而采用预训练的3D世界模型作为核心，通过视觉-几何-动作的直接映射实现精准控制。实验证明，VGA在模拟环境和真实场景的零样本视角泛化中均优于π0.5等顶尖模型，展示了原生3D表示在物理智能中的潜力。
selection_source: fresh_fetch
motivation: 现有的视觉语言或视频模型依赖2D语义或像素先验，难以满足机器人操纵对精确3D几何空间关系的本质需求。
method: 提出VGA模型，利用预训练的3D世界模型作为骨干网络，并结合渐进式体积调制模块和联合训练策略，将视觉输入直接映射为物理动作。
result: 在仿真基准测试中超越了π0.5和GeoVLA，并在真实世界部署中展现出卓越的零样本跨视角泛化能力。
conclusion: 采用原生3D表示而非语言或2D视频先验，是实现具有泛化能力的物理智能的一种极具前景的方向。
---

## 摘要
机器人操作的核心是一个视觉到几何映射（$f(v) \rightarrow G$）的问题。物理动作从根本上由 3D 位置和空间关系等几何属性定义。因此，我们认为通用机器人控制的基础应该是视觉-几何骨干网络，而非广泛采用的视觉-语言或视频模型。传统的 VLA 和视频预测模型依赖于在大规模 2D 图像-文本或时序像素数据上预训练的骨干网络。尽管这些模型有效，但它们的表示很大程度上受语义概念或 2D 先验的影响，这在本质上与物理操作所需的精确 3D 几何特性不一致。基于这一见解，我们提出了视觉-几何-动作（VGA）模型，该模型直接在预训练的原生 3D 表示上进行动作生成。具体而言，VGA 使用预训练的 3D 世界模型取代了传统的语言或视频骨干网络，建立了无缝的视觉到几何映射，将视觉输入直接转化为物理动作。为了进一步增强几何一致性，我们引入了渐进式体积调制模块并采用了联合训练策略。大量的实验验证了我们方法的有效性。在仿真基准测试中，VGA 的表现优于包括 $π_{0.5}$ 和 GeoVLA 在内的顶级 VLA 基线模型，证明了其在精确操作方面的优越性。更重要的是，VGA 在真实世界的部署中对未见过的视角表现出卓越的零样本泛化能力，持续优于 $π_{0.5}$。这些结果表明，基于原生 3D 表示进行操作——而非通过语言或 2D 视频先验进行转换——是实现通用物理智能的一个极具前景的方向。

## Abstract
At its core, robotic manipulation is a problem of vision-to-geometry mapping ($f(v) \rightarrow G$). Physical actions are fundamentally defined by geometric properties like 3D positions and spatial relationships. Consequently, we argue that the foundation for generalizable robotic control should be a vision-geometry backbone, rather than the widely adopted vision-language or video models. Conventional VLA and video-predictive models rely on backbones pretrained on large-scale 2D image-text or temporal pixel data. While effective, their representations are largely shaped by semantic concepts or 2D priors, which do not intrinsically align with the precise 3D geometric nature required for physical manipulation. Driven by this insight, we propose the Vision-Geometry-Action (VGA) model, which directly conditions action generation on pretrained native 3D representations. Specifically, VGA replaces conventional language or video backbones with a pretrained 3D world model, establishing a seamless vision-to-geometry mapping that translates visual inputs directly into physical actions. To further enhance geometric consistency, we introduce a Progressive Volumetric Modulation module and adopt a joint training strategy. Extensive experiments validate the effectiveness of our approach. In simulation benchmarks, VGA outperforms top-tier VLA baselines including $π_{0.5}$ and GeoVLA, demonstrating its superiority in precise manipulation. More importantly, VGA exhibits remarkable zero-shot generalization to unseen viewpoints in real-world deployments, consistently outperforming $π_{0.5}$. These results highlight that operating on native 3D representations-rather than translating through language or 2D video priors-is a highly promising direction for achieving generalizable physical intelligence.

---

## 论文详细总结（自动生成）

这篇论文由中山大学、阿里巴巴等机构的研究团队发表，提出了一种全新的机器人操纵范式。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型和视频预测模型主要依赖于在 2D 图像-文本或像素序列上预训练的骨干网络。这些模型虽然语义理解能力强，但缺乏对物理操纵至关重要的 **3D 几何空间推理能力**。
*   **研究动机**：作者认为机器人操纵本质上是“视觉到几何的映射”（$f(v) \rightarrow G$）。物理动作（如抓取、定向）是由 3D 位置、旋转和空间关系定义的。现有的 2D 骨干网络在处理 3D 任务时存在“3D-2D-3D”的信息瓶颈，导致泛化性差且空间精度不足。

### 2. 论文提出的方法论：VGA (Vision-Geometry-Action)
*   **核心思想**：放弃传统的语言或视频骨干网络，直接采用预训练的 **3D 世界模型（VGGT）** 作为骨干网络，建立原生的视觉到几何映射。
*   **关键技术细节**：
    *   **视觉-几何骨干网络 (VGGT)**：利用预训练的视觉几何接地 Transformer，通过交替注意力机制（帧内局部注意力和跨帧全局注意力）提取原生 3D 表示。
    *   **渐进式体积调制 (PVM)**：在骨干网络和动作头之间引入该模块，通过多层交叉注意力机制，将丰富的几何结构信息逐步注入动作生成过程。
    *   **联合训练与解耦推理**：在训练阶段，模型同时预测机器人动作和 3D 属性（如深度图、相机参数）；在推理阶段，关闭几何预测分支，仅保留动作分支以确保实时性（约 10Hz）。
    *   **多模态对齐**：通过 Qwen-GTE 编码语言指令，并与视觉、机器人本体感受数据在统一的 Token 空间内融合。

### 3. 实验设计
*   **仿真实验**：
    *   **数据集/场景**：使用 **LIBERO** 基准测试，包含四个子集：LIBERO-Spatial（空间关系）、Object（物体泛化）、Goal（任务组合）和 Long（长程任务）。
    *   **对比方法**：包括顶尖 VLA 模型（$\pi_{0.5}$、OpenVLA、Octo）、3D 感知模型（SpatialVLA、GeoVLA）以及视频动作模型（WorldVLA、UniMimic）。
*   **真实世界实验**：
    *   **场景**：使用 Franka Panda 机械臂，执行抓取方块、按按钮、叠方块等任务。
    *   **测试维度**：包括分布内测试和**零样本跨视角泛化测试**（在训练中未见过的相机视角下执行任务）。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：最长的训练任务在 **60 GPU 小时** 内完成。
*   **参数量**：模型总参数约 3.6B，通过 LoRA 微调，可训练参数约为 **500M**。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验涵盖了 40 个任务，每个任务进行 50 次随机试验（共 2000 次 rollout）；真实世界实验每个任务进行 20 次试验。
*   **消融实验**：针对 LoRA 秩、PVM 模块、联合训练策略、骨干网络初始化等进行了详细的对比。
*   **充分性与公平性**：实验设计遵循了学术界标准协议，对比了 2024-2026 年间几乎所有主流的基线模型，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **3D 骨干网络的优越性**：VGA 在 LIBERO 平均成功率上达到 98.1%，超过了所有对比的 VLA 和视频模型。
*   **空间智能的提升**：原生 3D 表示能显著提高空间精准操纵的能力，尤其是在需要精确对齐的任务中。
*   **强大的跨视角泛化**：在真实世界中，VGA 在未见过的新视角下的表现优于 $\pi_{0.5}$（成功率高出 6%），证明了 3D 几何先验能有效弥合感知-动作鸿沟。

### 7. 优点
*   **范式创新**：首次明确提出并验证了“视觉-几何-动作”路径优于“视觉-语言-动作”路径。
*   **高效性**：无需额外的 3D 传感器（如深度相机）即可在推理时获得 3D 感知能力。
*   **鲁棒性**：通过联合训练引入几何监督，使模型学习到的特征更具物理意义，而非简单的 2D 模式匹配。

### 8. 不足与局限
*   **语义推理限制**：由于骨干网络侧重几何而非大规模语言模型，在处理需要复杂常识推理的指令（如“把方块放在泰勒·斯威夫特的照片上”）时可能不如传统 VLA。
*   **长程任务挑战**：在 LIBERO-Long 子集上的表现虽强，但相比某些专门优化时序的模型，提升幅度较小，说明其长程时序依赖建模仍有优化空间。
*   **数据依赖**：虽然支持零样本视角泛化，但仍需要一定量的专家演示数据进行微调。

（完）
