---
title: "WM-DAgger: Enabling Efficient Data Aggregation for Imitation Learning with World Models"
title_zh: WM-DAgger：利用世界模型实现模仿学习的高效数据聚合
authors: "Anlan Yu, Zaishu Chen, Peili Song, Zhiqing Hong, Haotian Wang, Desheng Zhang, Tian He, Yi Ding, Daqing Zhang"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11351v1"
tags: ["query:robot"]
score: 10.0
evidence: 利用世界模型合成机器人模仿学习中的分布外恢复数据
tldr: 针对模仿学习中复合误差导致的分布外（OOD）状态问题，本文提出WM-DAgger框架。该框架利用世界模型在无需人类干预的情况下自动合成OOD恢复数据，通过纠偏动作合成与一致性引导过滤机制，有效克服了世界模型的幻觉问题。在多项真实机器人任务中，该方法仅需极少数演示即可显著提升策略的鲁棒性与成功率。
selection_source: fresh_fetch
motivation: 传统的DAgger框架因高度依赖人类持续参与提供反馈，严重限制了模仿学习在处理分布外状态时的扩展性。
method: 利用世界模型合成任务相关的纠偏动作，并通过将合成帧与专家演示实景锚定来过滤物理不合理的轨迹。
result: "在真实机器人实验中表现出色，仅需5次演示即可在软袋推移任务中实现93.3%的成功率。"
conclusion: WM-DAgger证明了通过世界模型进行自动数据聚合是解决模仿学习误差累积问题并提升数据效率的有效方案。
---

## 摘要
模仿学习是训练机器人策略的一种强大范式，但其性能受限于复合误差：微小的策略偏差可能将机器人带入训练集中未见的分布外（OOD）状态，在这些状态下，策略可能会产生更大的误差，最终导致失败。虽然数据聚合（DAgger）框架试图解决这一问题，但其对人类持续参与的依赖严重限制了可扩展性。在本文中，我们提出了 WM-DAgger，这是一个高效的数据聚合框架，它利用世界模型合成 OOD 恢复数据，而无需人类参与。具体而言，我们专注于使用手眼机械臂且仅有少样本演示的操作任务。为了避免合成误导性数据并克服世界模型固有的幻觉问题，我们的框架引入了两个关键机制：（1）纠正动作合成模块，生成面向任务的恢复动作以防止误导性监督；（2）一致性引导过滤模块，通过将合成的终端帧锚定到专家演示中相应的真实帧，剔除物理上不合理的轨迹。我们在多个真实世界机器人任务上对 WM-DAgger 进行了广泛验证。结果表明，我们的方法显著提高了成功率，在仅有五次演示的情况下，在软袋推移任务中实现了 93.3% 的成功率。源代码已在 https://github.com/czs12354-xxdbd/WM-Dagger 公开。

## Abstract
Imitation learning is a powerful paradigm for training robotic policies, yet its performance is limited by compounding errors: minor policy inaccuracies could drive robots into unseen out-of-distribution (OOD) states in the training set, where the policy could generate even bigger errors, leading to eventual failures. While the Data Aggregation (DAgger) framework tries to address this issue, its reliance on continuous human involvement severely limits scalability. In this paper, we propose WM-DAgger, an efficient data aggregation framework that leverages World Models to synthesize OOD recovery data without requiring human involvement. Specifically, we focus on manipulation tasks with an eye-in-hand robotic arm and only few-shot demonstrations. To avoid synthesizing misleading data and overcome the hallucination issues inherent to World Models, our framework introduces two key mechanisms: (1) a Corrective Action Synthesis Module that generates task-oriented recovery actions to prevent misleading supervision, and (2) a Consistency-Guided Filtering Module that discards physically implausible trajectories by anchoring terminal synthesized frames to corresponding real frames in expert demonstrations. We extensively validate WM-DAgger on multiple real-world robotic tasks. Results that our method significantly improves success rates, achieving a 93.3\% success rate in soft bag pushing with only five demonstrations. The source code is publicly available at https://github.com/czs12354-xxdbd/WM-Dagger.

---

## 论文详细总结（自动生成）

# WM-DAgger: 利用世界模型实现模仿学习的高效数据聚合

### 1. 核心问题与研究动机
*   **核心问题**：模仿学习（Imitation Learning）在实际应用中面临**复合误差（Compounding Errors）**问题。即机器人微小的执行偏差会导致其进入训练集未覆盖的“分布外（OOD）”状态，由于缺乏在这些状态下的指导，误差会迅速累积导致任务失败。
*   **研究背景**：传统的数据聚合框架（如 DAgger）需要人类专家持续参与，实时纠正机器人的错误并提供新标签，这在人力成本和可扩展性上存在巨大瓶颈。
*   **研究目标**：利用生成式**世界模型（World Models）**作为“虚拟专家”，自动合成高质量的 OOD 恢复数据，从而在无需人类干预的情况下增强策略的鲁棒性。

### 2. 方法论
WM-DAgger 框架主要由以下三个核心组件构成：
*   **手眼动作条件世界模型 (EAC-WM)**：
    *   基于 NVIDIA Cosmos-Predict2.5 基础模型，针对手眼相机（Eye-in-Hand）配置进行了优化。
    *   **Action2Image 模块**：将低维的机器人动作（平移、旋转、夹持器状态）投影为高维的像素级几何运动场（Motion Fields），解决了稀疏动作向量与高维视觉特征之间的维度失配问题。
    *   **训练策略**：结合了无目标的探索数据（Play Data）和少量的专家演示数据（Task Data），通过整流流（Rectified Flow）框架进行微调。
*   **纠正动作合成模块 (Corrective Action Synthesis)**：
    *   在专家轨迹的基础上，通过随机采样偏离方向来生成 OOD 状态。
    *   **方向约束机制**：强制合成的恢复动作与专家原始动作的夹角大于 120°，确保生成的动作是“朝向目标恢复”而非“背离目标”，避免产生误导性的监督信号。
*   **一致性引导过滤模块 (Consistency-Guided Filtering)**：
    *   针对世界模型易产生的“幻觉”（如物体变形、物理逻辑错误）问题，该模块对比合成轨迹的终端帧与专家演示中相同位姿的真实帧。
    *   利用 **DINOv2** 提取特征并计算余弦相似度，剔除视觉或物理上不一致的低质量样本，确保训练数据的保真度。

### 3. 实验设计
*   **实验场景**：涵盖了四类具有挑战性的真实世界任务：
    1.  **软袋推移 (Soft Bag Pushing)**：涉及可变形物体和复杂的物理交互。
    2.  **取放任务 (Pick-and-Place)**：测试对已知和未知物体的泛化能力。
    3.  **选票插入 (Ballot Insertion)**：高精度、接触密集的任务。
    4.  **毛巾折叠 (Towel Folding)**：复杂的 6-DoF 空间轨迹和严重的形变处理。
*   **Benchmark 与对比方法**：
    *   **Standard BC**：标准行为克隆（无数据聚合）。
    *   **DMD (Diffusion Meets DAgger)**：目前最先进的基于扩散模型的数据合成方法。

### 4. 资源与算力
*   **硬件平台**：
    *   **训练**：使用了 **4 张 NVIDIA L20 GPU**。
    *   **推理/部署**：使用了 **1 张 NVIDIA L20 GPU**。
*   **数据量**：每个任务仅需约 5 分钟的 Play Data 和 20 次专家演示。
*   **训练时长**：文中未明确给出具体的训练小时数，但提到模型基于 2B 参数的基础模型进行微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个任务进行了多次在线测试（如软袋推移任务进行了 30 次独立运行）。
    *   **少样本测试**：对比了 1-shot 到 20-shot 不同演示数量下的性能。
    *   **消融实验**：分别验证了 Play Data、过滤模块、方向约束模块对最终成功率的影响。
    *   **数据缩放实验**：测试了合成样本数量（300 到 3000 个）与性能的相关性。
*   **评价**：实验设计较为全面，涵盖了从刚体到柔性体、从简单到高精度的多种维度，对比基准（DMD）具有代表性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在软袋推移任务中，WM-DAgger 在仅有 5 次演示时成功率达 **93.3%**，远超标准 BC（26.7%）和 DMD（40.0%）。
*   **数据效率**：即使在 1-shot（仅 1 次演示）的情况下，该方法也能实现 73.3% 的成功率，证明了世界模型在扩充训练分布方面的强大能力。
*   **泛化能力**：在取放任务中，对未见过的物体也表现出良好的泛化性，说明合成的多样化数据有助于策略学习通用特征而非死记硬背。
*   **物理一致性**：世界模型生成的连续帧在处理可变形物体（如毛巾、软袋）时，比单帧生成的扩散模型具有更好的物理连贯性。

### 7. 优点与亮点
*   **全自动闭环**：实现了从 OOD 状态发现到恢复动作合成、再到数据清洗的全自动化，极大降低了 DAgger 的应用门槛。
*   **几何感知增强**：通过 Action2Image 模块显式引入相机几何约束，显著提升了手眼相机视角下的生成质量。
*   **幻觉抑制**：创新性地利用终端帧一致性作为代理指标，有效解决了生成式模型在长程预测中的漂移问题。

### 8. 不足与局限
*   **硬件依赖**：依赖于高性能的预训练视频基础模型（如 Cosmos），对于算力受限的场景可能难以部署。
*   **任务复杂度限制**：目前主要针对平行夹持器。论文提到，对于**多指灵巧手（Dexterous Hands）**等高自由度操作，由于运动学拓扑复杂，世界模型在合成物理一致性画面上仍面临巨大挑战。
*   **实时性**：虽然数据合成是离线的，但世界模型的训练和大规模轨迹生成仍需要可观的时间成本。

（完）
