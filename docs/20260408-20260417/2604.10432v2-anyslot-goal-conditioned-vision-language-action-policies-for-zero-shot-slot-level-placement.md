---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v2"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人操作的目标条件视觉-语言-动作策略
tldr: 针对视觉语言动作（VLA）策略在处理复杂指令下的高精度插槽级放置任务（如亚厘米级精度）存在的挑战，本文提出了AnySlot框架。该框架通过引入显式空间视觉目标作为中间表示，将语言指令转化为场景标记，并利用目标条件VLA策略执行任务。这种分层设计解耦了高层语义定位与底层精确控制。此外，研究者还推出了SlotBench基准测试。实验证明AnySlot在零样本插槽级放置任务中显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的单体VLA策略在处理需要亚厘米级精度和复杂语义理解的插槽级放置任务时，难以兼顾可靠的插槽定位与精确的动作执行。
method: 提出AnySlot框架，通过生成场景标记将语言指令转化为显式视觉目标，并采用目标条件VLA策略实现高层语义定位与底层精确控制的解耦。
result: 在包含九类任务的SlotBench基准测试中，AnySlot在零样本插槽级放置任务上的表现显著优于传统的扁平化VLA模型和模块化定位方法。
conclusion: 通过引入中间视觉目标来解耦语义理解与动作执行，能有效提升机器人策略在复杂、高精度放置任务中的泛化能力和操作鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略来说仍然是一个重大挑战。插槽级任务既需要可靠的插槽定位，也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，该框架通过引入显式空间视觉目标作为语言定位与控制之间的中间表示，从而降低了组合复杂性。AnySlot 通过生成场景标记将语言转化为显式的视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到目前缺乏针对此类高精度任务的基准测试，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置任务中显著优于扁平化 VLA 基线模型和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

### AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略总结

#### 1. 论文的核心问题与整体含义
论文关注的是机器人操作中的**插槽级放置（Slot-level Placement）**任务。这类任务要求机器人根据复杂的组合语言指令（如“将方块放入第二大的插槽”），在密集的候选位置中识别出精确的目标插槽，并实现亚厘米级的放置精度。
*   **研究动机**：现有的单体式（Flat）VLA策略在处理复杂空间推理时能力不足，且难以兼顾语义理解与高精度控制；而传统的模块化方法（VLM预测坐标+运动规划）由于坐标预测过于粗糙且丢失了几何上下文，导致在未见过的布局中表现不佳。
*   **核心目标**：通过解耦“高层语义推理”与“底层运动控制”，实现零样本（Zero-shot）下的高精度插槽级放置。

#### 2. 论文提出的方法论：AnySlot 框架
AnySlot 采用两阶段的分层设计，引入**显式空间视觉目标**作为中间表示：
*   **第一阶段：高层目标构建（视觉定位）**
    *   利用现成的图像生成模型（如 Nano-Banana）根据语言指令在机器人头部相机的图像中生成一个“蓝色球体”标记。
    *   通过深度图回投影（Back-projection）和相机标定，将该 2D 标记提升为 3D 空间锚点。
    *   将该 3D 锚点重新投影到所有相机视角（如头部相机、手腕相机），生成视图一致的视觉目标覆盖图（Overlays）。
*   **第二阶段：底层目标条件化策略（执行控制）**
    *   使用目标条件化的 VLA 策略（基于 $\pi_{0.5}$，采用 PaliGemma-3B 主干网络和流匹配动作专家）。
    *   策略输入包含带有蓝色球体标记的多视图图像，指令被固定为简单的“移动物体到蓝色球体引导的插槽”。
    *   这种设计让底层策略专注于视觉目标的追踪与精确对准，而无需处理复杂的语言逻辑。

#### 3. 实验设计
*   **基准测试（SlotBench）**：论文推出了专门的仿真基准 SlotBench，包含 9 类挑战：序数推理、尺寸比较、高度推理、距离推理、组合关系、逻辑否定、模糊语言、功能性推理（Affordance）和世界知识。
*   **对比方法**：
    *   **扁平化策略（Flat）**：Diffusion Policy, OpenVLA, $\pi_0, \pi_{0.5}$。
    *   **模块化策略（Modular）**：RoboPoint, HAMSTER, AnyPlace。
    *   **Oracle 基准**：由人类专家提供完美的目标定位，以测试底层策略的性能上限。
*   **评估指标**：成功率（SR）、指令准确率（IA，定位误差 < 0.02m）、粗略准确率（CA）。

#### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA H200 GPU**。
*   **训练细节**：底层策略 $\pi_{0.5}$ 在合成数据集 $D_{syn}$ 上进行了全参数微调，训练步数为 **20,000 步**，Batch Size 为 64。
*   **推理延迟**：文中提到高层图像生成模块存在一定延迟（如 Nano-Banana 2 约为 11 秒，GPT-5.2 Think 约为 133 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：每种方法在 9 类任务中各进行 50 次试验。
*   **消融实验**：
    *   对比了不同的高层定位模块（多种 VLM 与图像生成模型）。
    *   对比了不同的底层策略架构（Diffusion Policy vs VLA）。
    *   验证了合成数据集 $D_{syn}$ 对训练的重要性。
    *   对比了域内（In-domain）微调的扁平化策略。
*   **充分性评价**：实验覆盖了从语义推理到几何控制的多个维度，对比基线涵盖了当前主流的 VLA 模型，实验设计较为客观、公平。

#### 6. 论文的主要结论与发现
*   **解耦是关键**：将复杂的语言推理转化为显式的视觉目标，能显著降低策略的学习难度。
*   **图像生成优于坐标回归**：相比于 VLM 输出的离散坐标，图像生成模型提供的视觉标记保留了更多几何上下文，对底层策略更友好。
*   **AnySlot 性能卓越**：在 SlotBench 的所有任务中，AnySlot 达到了近 90% 的平均成功率，而扁平化 VLA 模型在复杂推理任务中几乎全部失败。
*   **泛化能力强**：AnySlot 在未见过的插槽布局和指令下表现出极强的零样本迁移能力。

#### 7. 优点
*   **创新性**：首次提出将图像生成模型作为 VLA 策略的高层定位器，解决了坐标表示精度不足的问题。
*   **可解释性**：生成的蓝色球体标记为机器人的决策提供了直观的视觉解释。
*   **高精度**：通过 3D 重建和多视图投影，确保了跨视角的一致性和亚厘米级的操作精度。
*   **基准贡献**：SlotBench 填补了机器人领域缺乏复杂空间推理放置任务基准的空白。

#### 8. 不足与局限
*   **推理延迟**：依赖外部大型图像生成模型，导致单次任务的初始化时间较长（秒级），难以满足实时性要求极高的场景。
*   **依赖深度信息**：第一阶段的目标构建高度依赖准确的深度图和相机标定，在透明物体或强反光环境下可能失效。
*   **任务范围**：目前主要集中在单步放置任务，对于长程（Long-horizon）或需要连续推理的任务尚待验证。

（完）
