---
title: "VAG: Dual-Stream Video-Action Generation for Embodied Data Synthesis"
title_zh: VAG：用于具身数据合成的双流视频-动作生成
authors: "Xiaolei Lang, Yang Wang, Yukun Zhou, Chaojun Ni, Kerui Li, Jiagang Zhu, Tianze Liu, Jiajun Lv, Xingxing Zuo, Yun Ye, Guan Huang, Xiaofeng Wang, Zheng Zhu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09330v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人世界动作模型的双流视频-动作生成
tldr: 针对具身智能领域高质量示教数据获取成本高的问题，本文提出VAG框架。VAG采用基于流匹配的双流架构，在视觉和语言引导下同步生成视频及其对应的动作轨迹。通过同步去噪分支和自适应3D池化技术，VAG有效解决了视频与动作间的对齐难题，克服了传统两阶段方法效率低和误差累积的缺陷。实验证明，VAG生成的合成数据能显著提升下游策略的泛化性能，为具身数据合成提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 现有世界模型缺乏动作对齐，且两阶段生成流程存在效率低和误差累积的问题，限制了其在具身数据合成中的应用。
method: 提出一种基于流匹配的双流框架，通过同步去噪和自适应3D池化机制实现视频与动作的联合生成与跨模态对齐。
result: 在仿真和真实世界任务中，VAG生成了高质量且可执行的视频-动作对，并有效提升了预训练策略的泛化能力。
conclusion: VAG证明了联合生成视频与动作的有效性，是解决机器人学习中数据稀缺问题并实现具身数据合成的实用工具。
---

## 摘要
最近，在规模化人类遥操作数据上训练的机器人基础模型取得了进展，使机器人能够执行日益复杂的现实世界任务。然而，由于收集特定任务的演示数据既昂贵又费力，扩展这些系统仍然具有挑战性。合成数据（尤其是生成的视频）提供了一个充满前景的方向，但现有的世界模型（WMs）并不直接适用于策略学习，因为它们不提供成对的动作轨迹。世界-动作（WA）模型通过在输出视觉结果的同时预测动作，部分解决了这一问题，但往往缺乏强大的视频-动作对齐；而先生成视频再推断动作的两阶段流水线则会引入低效和误差累积。为了解决这些局限性，我们提出了 VAG，这是一个基于流匹配（flow-matching）的统一双流框架，能够在视觉和语言条件的约束下联合生成视频和动作。通过同步两个分支的去噪过程，并使用自适应 3D 池化机制将紧凑的全局视频上下文传输到动作分支，VAG 提高了生成过程中的跨模态一致性。在模拟和真实世界设置中，VAG 都能产生具有竞争力的预测质量且对齐的视频-动作对，支持可执行的轨迹回放，并提供有用的合成预训练数据以提高下游策略的泛化能力，这表明了其作为具身数据合成的实用世界-动作模型的潜力。

## Abstract
Recent advances in robot foundation models trained on large-scale human teleoperation data have enabled robots to perform increasingly complex real-world tasks. However, scaling these systems remains difficult because collecting task-specific demonstrations is expensive and labor-intensive. Synthetic data, especially generated videos, offer a promising direction, but existing World Models (WMs) are not directly suitable for policy learning since they do not provide paired action trajectories. World-Action (WA) models partially address this by predicting actions with visual outputs, yet often lack strong video-action alignment, while two-stage pipelines that generate video first and then infer actions introduce inefficiency and error accumulation. To address these limitations, we propose VAG, a unified flow-matching-based dual-stream framework that jointly generates video and action under visual and language conditioning. By synchronizing denoising in both branches and using an adaptive 3D pooling mechanism to transfer compact global video context to the action branch, VAG improves cross-modal consistency during generation. Across both simulated and real-world settings, VAG produces aligned video-action pairs with competitive prediction quality, supports executable trajectory replay, and provides useful synthetic pretraining data that improves downstream policy generalization, indicating its potential as a practical world-action model for embodied data synthesis.

---

## 论文详细总结（自动生成）

这是一份关于论文《VAG: Dual-Stream Video-Action Generation for Embodied Data Synthesis》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：机器人基础模型的扩展受限于高质量人类遥操作数据的获取成本。虽然现有的“世界模型”（World Models）可以生成逼真的视频，但缺乏配对的动作轨迹；而现有的“世界-动作模型”或两阶段流水线（先生成视频再提取动作）存在视频-动作对齐差、效率低和累积误差大的问题。
*   **整体含义**：本文提出了 **VAG**（Dual-Stream Video-Action Generation），旨在通过一个统一的生成框架，根据初始图像和语言指令，同步生成高保真且严格对齐的视频序列与动作轨迹，从而为机器人策略训练提供高质量的合成数据。

### 2. 方法论
*   **核心思想**：采用基于**流匹配（Flow-Matching）**的统一双流架构，将视频生成和动作生成整合进同一个去噪流程中，实现跨模态的同步与对齐。
*   **关键技术细节**：
    *   **双流架构**：包含视频分支（基于 Diffusion Transformer, DiT）和动作分支（基于 1D U-Net）。
    *   **同步去噪**：两个分支在推理时共享相同的去噪步数和噪声强度，确保时间轴上的严格同步。
    *   **自适应 3D 池化（Adaptive 3D Pooling）**：这是连接两路分支的关键桥梁。在每个去噪步骤中，将视频分支预测的“干净”潜变量（latent）通过 3D 池化压缩成全局嵌入向量（Embedding），作为条件输入动作分支，引导动作的生成。
    *   **条件控制**：模型同时接收文本指令（通过 T5-XXL 编码）和初始帧图像作为前缀条件。

### 3. 实验设计
*   **数据集/场景**：
    *   **AgiBot 数据集**：大规模真实世界机器人操作数据（G1 双足/双臂机器人）。
    *   **LIBERO 数据集**：模拟环境下的基准测试，涵盖空间、对象、目标等多种任务。
    *   **自采集数据集**：使用 Agilex Cobot Magic 双臂机器人采集的真实数据。
*   **Benchmark 与对比方法**：
    *   **视频生成对比**：SVD、Wan2.2、Cosmos-Predict2。
    *   **动作预测对比**：对比了两阶段基线（VAG-Video + ResNet/AnyPos 动作提取器）。
    *   **下游应用测试**：将 VAG 生成的数据用于预训练 $\pi_{0.5}$ (VLA 模型)，观察其在真实世界任务中的泛化表现。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   训练时长/迭代：在 AgiBot 数据集上训练了 40,000 次迭代。
    *   Batch Size：每张 GPU 的 batch size 为 1。
    *   推理设置：推理时采用 35 步去噪。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从模拟器（LIBERO）到真实世界（AgiBot 及自研机器人）的跨度。
*   **充分性评价**：实验设计较为全面。不仅评估了生成质量（FVD, FID, LPIPS 等指标），还通过“轨迹回放”验证了动作的可执行性，并通过下游 VLA 模型的性能提升验证了合成数据的实用价值。对比实验涵盖了当前主流的视频生成模型和动作提取方案，具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **对齐性更强**：VAG 在视频-动作一致性上优于两阶段方法，欧氏距离（ED）更小，成功率（SR）更高。
*   **提升泛化性**：使用 VAG 合成的数据进行预训练，使下游 VLA 模型在真实世界任务中的成功率从 35% 提升至 **55%**（绝对提升 20%），尤其在面对未见过的物体位置或颜色时表现出更强的鲁棒性。
*   **具备策略潜力**：VAG 生成的动作轨迹可以直接在真实机器人上回放并完成任务，表明其本身具备作为“世界-动作策略”的潜力。

### 7. 优点
*   **统一性**：将视频和动作生成整合在单一的前向传播中，避免了多阶段模型的低效。
*   **高效对齐**：通过自适应 3D 池化实现轻量化但有效的跨模态特征传递。
*   **实用性强**：直接证明了合成数据对提升现有机器人模型泛化能力的显著作用。

### 8. 不足与局限
*   **单向引导**：目前仅实现了视频特征引导动作生成，动作分支尚未反向作用于视频分支，可能浪费了潜在的控制信号。
*   **模型容量限制**：动作分支仍使用较小的 U-Net，未来可能需要升级为 DiT 以匹配更大规模的数据。
*   **任务覆盖面**：虽然在操作任务上表现良好，但在更复杂、长程或涉及多物体交互的极端场景下的表现仍有待进一步验证。

（完）
