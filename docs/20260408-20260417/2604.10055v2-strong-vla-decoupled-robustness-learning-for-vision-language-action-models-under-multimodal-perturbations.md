---
title: "STRONG-VLA: Decoupled Robustness Learning for Vision-Language-Action Models under Multimodal Perturbations"
title_zh: STRONG-VLA：多模态扰动下视觉-语言-动作模型的解耦鲁棒性学习
authors: "Yuhan Xie, Yuping Yan, Yunqi Zhao, Handing Wang, Yaochu Jin"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10055v2"
tags: ["query:robot"]
score: 10.0
evidence: 视觉-语言-动作 (VLA) 模型的鲁棒性学习
tldr: 针对视觉-语言-动作（VLA）模型在多模态扰动下表现脆弱的问题，本文提出了STRONG-VLA框架。该框架采用解耦微调策略，通过第一阶段的课程学习逐步增强模型对视觉和文本噪声的鲁棒性，并在第二阶段利用干净数据恢复任务执行精度。实验表明，该方法在多个VLA架构上显著提升了任务成功率，并具有良好的跨架构泛化能力和实机部署效果。
selection_source: fresh_fetch
motivation: 现有的VLA模型在面对视觉和语言的多模态扰动时极易失效，且传统的联合训练方法会导致鲁棒性与任务保真度之间的优化冲突。
method: 提出一种解耦微调框架，通过课程学习逐步引入多模态扰动进行鲁棒性训练，随后利用干净数据进行任务重对齐以恢复性能。
result: 在LIBERO基准测试中，该方法显著提升了OpenVLA和pi0等模型的任务成功率，在已知和未知扰动下均表现出极强的泛化能力。
conclusion: 解耦优化是提升具身智能模型多模态鲁棒性的有效途径，STRONG-VLA为构建稳健的机器人控制系统提供了一个简单且原则性的框架。
---

## 摘要
尽管视觉-语言-动作（VLA）模型在具身任务中表现出色，但在多模态扰动下仍表现出高度的脆弱性。视觉损坏和语言噪声共同引发的分布偏移会降低任务级的执行效果。现有的鲁棒性方法通常依赖于使用扰动数据进行联合训练，将鲁棒性视为静态目标，这导致了鲁棒性与任务保真度之间的优化冲突。在这项工作中，我们提出了 STRONG-VLA，这是一个将鲁棒性获取与任务对齐细化显式分离的解耦微调框架。在第一阶段，模型暴露于难度递增的多模态扰动课程中，从而在受控的分布偏移下实现渐进式鲁棒性学习。在第二阶段，模型与干净的任务分布重新对齐，以在保持鲁棒性的同时恢复执行保真度。我们进一步建立了一个全面的基准测试，包含跨越文本和视觉模态的 28 种扰动类型，这些扰动源于传感器噪声、遮挡和指令损坏等现实因素。在 LIBERO 基准上的广泛实验表明，STRONG-VLA 在多种 VLA 架构上一致地提高了任务成功率。在 OpenVLA 上，我们的方法在已知扰动下实现了高达 12.60% 的提升，在未知扰动下实现了 7.77% 的提升。值得注意的是，在 OpenVLA-OFT（+14.48% / +13.81%）和 pi0（+16.49% / +5.58%）上也观察到了类似或更大的改进，证明了强大的跨架构泛化能力。在 AIRBOT 机器人平台上的真实世界实验进一步验证了其在实际应用中的有效性。这些结果强调了多模态鲁棒性解耦优化的重要性，并将 STRONG-VLA 确立为一个简单且原则性的鲁棒具身控制框架。

## Abstract
Despite their strong performance in embodied tasks, recent Vision-Language-Action (VLA) models remain highly fragile under multimodal perturbations, where visual corruption and linguistic noise jointly induce distribution shifts that degrade task-level execution. Existing robustness approaches typically rely on joint training with perturbed data, treating robustness as a static objective, which leads to conflicting optimization between robustness and task fidelity. In this work, we propose STRONG-VLA, a decoupled fine-tuning framework that explicitly separates robustness acquisition from task-aligned refinement. In Stage I, the model is exposed to a curriculum of multimodal perturbations with increasing difficulty, enabling progressive robustness learning under controlled distribution shifts. In Stage II, the model is re-aligned with clean task distributions to recover execution fidelity while preserving robustness. We further establish a comprehensive benchmark with 28 perturbation types spanning both textual and visual modalities, grounded in realistic sources of sensor noise, occlusion, and instruction corruption. Extensive experiments on the LIBERO benchmark show that STRONG-VLA consistently improves task success rates across multiple VLA architectures. On OpenVLA, our method achieves gains of up to 12.60% under seen perturbations and 7.77% under unseen perturbations. Notably, similar or larger improvements are observed on OpenVLA-OFT (+14.48% / +13.81%) and pi0 (+16.49% / +5.58%), demonstrating strong cross-architecture generalization. Real-world experiments on an AIRBOT robotic platform further validate its practical effectiveness. These results highlight the importance of decoupled optimization for multimodal robustness and establish STRONG-VLA as a simple yet principled framework for robust embodied control.

---

## 论文详细总结（自动生成）

以下是对论文《STRONG-VLA: Decoupled Robustness Learning for Vision-Language-Action Models under Multimodal Perturbations》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型在具身智能领域表现强劲，但在现实部署中极易受到多模态扰动（如视觉传感器噪声、遮挡、模糊的语言指令）的影响。
*   **核心矛盾**：现有的鲁棒性训练通常采用“联合训练”（同时使用干净和扰动数据），这会导致优化冲突——干净数据要求模型对细微信号敏感，而扰动数据要求模型对噪声保持不变性。这种冲突限制了模型在任务保真度与鲁棒性之间的平衡。
*   **整体含义**：本文旨在通过“解耦”优化过程，使模型既能获得强大的抗干扰能力，又能保持高精度的任务执行能力。

### 2. 方法论
*   **核心思想**：提出 **STRONG-VLA** 框架，将鲁棒性获取与任务对齐显式分离，分为两个阶段进行微调。
*   **关键技术细节**：
    *   **第一阶段：课程驱动的鲁棒性获取**。模型暴露于难度递增的扰动中。
        *   **难度量化**：定义标量 $d(\delta) \in [0, 1]$。文本难度基于噪声标记占比；视觉难度基于像素偏差（光度）、遮挡比例（物理遮挡）或位移幅度（几何变换）。
        *   **分层课程**：先进行文本硬化（Stage I-T），再进行视觉硬化（Stage I-V），通过三个阶段（热身、提升、硬化）逐渐增加扰动概率和强度。
    *   **第二阶段：任务对齐细化**。使用干净数据对第一阶段得到的模型进行保守微调，旨在恢复模型对任务相关信号的敏感度，重新锚定到名义任务分布。
*   **算法流程**：
    1.  初始化预训练 VLA 参数。
    2.  执行 Stage I：在扰动分布下最小化动作预测损失，学习跨模态的不变性表示。
    3.  执行 Stage II：在干净分布下微调，恢复执行精度。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 机器人操作基准测试，涵盖 Spatial（空间）、Objects（物体）、Goal（目标）和 Long（长程）四个任务套件。
*   **基准测试（Benchmark）**：构建了包含 **28 种扰动类型** 的基准，分为文本（对抗注入、语言损坏、上下文干扰、语义漂移）和视觉（光度退化、物理遮挡、几何偏移、动态伪影）两大类。
*   **对比方法**：
    *   **原始模型**：OpenVLA-7B、OpenVLA-OFT、$\pi_0$。
    *   **鲁棒性基准**：BYOVLA（运行时干预）、RobustVLA（强化学习后训练）。
*   **实机验证**：在 AIRBOT Play 机械臂上进行“抓取方块并放置”的真实场景实验。

### 4. 资源与算力
*   **硬件型号**：使用 **NVIDIA L40S GPUs**。
*   **训练配置**：
    *   采用 LoRA（秩 $r=32$）进行参数高效微调。
    *   Stage I 训练 50,000 步，Stage II 训练 8,000 步。
    *   有效 Batch Size 为 16（单卡 4，梯度累积 4），使用 bfloat16 混合精度。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同的 VLA 架构上进行了验证，涵盖了从 7B 参数的大模型到流式动作模型（$\pi_0$）。
*   **充分性**：
    *   测试了“已知扰动”和“未知扰动”（Zero-shot 鲁棒性）。
    *   进行了详细的消融实验（验证了阶段式优化、课程学习、解耦训练的必要性）。
    *   包含了单模态与多模态复合扰动的对比。
*   **客观性**：所有对比方法均在相同的任务实例和扰动参数下运行，确保了评估的公平性。

### 6. 主要结论与发现
*   **性能提升**：STRONG-VLA 在所有架构上均显著优于原始模型。在 $\pi_0$ 上，任务成功率在已知扰动下提升了 **16.49%**。
*   **解耦优势**：相比于混合数据的联合训练，解耦训练能更好地缓解鲁棒性与性能之间的权衡。
*   **模态差异**：文本鲁棒性的泛化能力较强，而视觉鲁棒性在面对未见过的几何变换或严重遮挡时仍面临挑战。
*   **多模态挑战**：当视觉和文本同时受损时，任务难度呈指数级增加，而 STRONG-VLA 在此类极端情况下的表现尤为突出。

### 7. 优点
*   **简单且原则性强**：通过两阶段微调解决了复杂的优化冲突问题，易于集成到现有的 VLA 训练流程中。
*   **基准全面**：28 种扰动类型非常贴近现实世界中的传感器失效和人为指令错误。
*   **跨架构泛化**：证明了该方法不仅适用于特定模型，对不同规模和结构的 VLA 均有效。

### 8. 不足与局限
*   **视觉泛化瓶颈**：对于未见过的视觉扰动（如特定的几何畸变），模型的零样本泛化能力仍有待提高。
*   **人工设计依赖**：课程学习的难度指标和扰动算子仍依赖人工设计，未来可能需要更自动化的生成方式。
*   **实机规模有限**：虽然进行了真机实验，但任务场景相对单一，在大规模复杂环境下的表现需进一步验证。

（完）
