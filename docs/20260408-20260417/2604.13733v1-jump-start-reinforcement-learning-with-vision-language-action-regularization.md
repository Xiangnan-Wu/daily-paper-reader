---
title: Jump-Start Reinforcement Learning with Vision-Language-Action Regularization
title_zh: 利用视觉-语言-动作正则化加速强化学习
authors: "Angelo Moroncelli, Roberto Zanetti, Marco Maccarini, Loris Roveda"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13733v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人操作的视觉-语言-动作 (VLA) 模型
tldr: 针对强化学习在长程任务中探索效率低和视觉语言模型（VLA）推理速度慢的问题，本文提出VLAJS方法。该方法将VLA作为临时的动作引导源，通过方向性动作一致性正则化改进PPO算法，在训练初期引导智能体探索。VLA引导采用稀疏触发并随时间衰减，使RL智能体能结合VLA的推理能力与RL的高频控制优势。实验证明，VLAJS在多项机器人操纵任务中显著提升了采样效率，并成功实现了真机部署。
selection_source: fresh_fetch
motivation: 强化学习在处理稀疏奖励的长程任务时探索效率低下，而现有的VLA模型虽具备通用推理能力但难以直接用于高频精确控制。
method: 提出VLAJS框架，在PPO算法中引入方向性动作一致性正则化，利用VLA提供的稀疏、可衰减的动作建议来引导早期探索。
result: "在六项仿真任务中，VLAJS比PPO和蒸馏基准减少了超过50%的环境交互次数，并在真实机器人上实现了零样本迁移和鲁棒控制。"
conclusion: 通过将VLA的宏观引导与RL的微观控制相结合，VLAJS有效解决了复杂操纵任务中的冷启动问题，提升了学习效率和泛化能力。
---

## 摘要
强化学习 (RL) 能够实现机器人操作的高频闭环控制，但由于探索效率低下和信用分配困难，将其扩展到具有稀疏或不完善奖励的长程任务仍然具有挑战性。视觉-语言-动作 (VLA) 模型利用大规模多模态预训练来提供通用的任务级推理，但目前的局限性阻碍了它们在快速且精确的操作中的直接应用。在本文中，我们提出了视觉-语言-动作引导启动 (VLAJS)，这是一种将稀疏的 VLA 指导与同策略 RL 相结合的方法，旨在提高探索和学习效率。VLAJS 将 VLA 视为高层动作建议的瞬态来源，在训练早期引导探索并改善信用分配，同时保留了 RL 基于状态的高频控制。我们的方法通过方向性动作一致性正则化增强了近端策略优化 (PPO)，在训练早期使 RL 智能体的动作与 VLA 指导实现软对齐，而无需强制执行严格模仿、不需要演示数据，也不依赖于持续的教师查询。VLA 指导以稀疏方式应用并随时间退火，使智能体能够在线适应并最终超越指导策略。我们在仿真环境中的六个挑战性操作任务（提升、取放、销钉重定向、销钉插入、拨动和推移）上评估了 VLAJS，并在真实的 Franka Panda 机器人上验证了其中的子集。VLAJS 在样本效率方面始终优于 PPO 和蒸馏式基准方法，在多个任务中将所需的环境交互减少了 50% 以上。现实世界实验证明了零样本的从仿真到现实 (sim-to-real) 迁移，以及在杂乱环境、物体变化和外部扰动下的鲁棒执行。

## Abstract
Reinforcement learning (RL) enables high-frequency, closed-loop control for robotic manipulation, but scaling to long-horizon tasks with sparse or imperfect rewards remains difficult due to inefficient exploration and poor credit assignment. Vision-Language-Action (VLA) models leverage large-scale multimodal pretraining to provide generalist, task-level reasoning, but current limitations hinder their direct use in fast and precise manipulation. In this paper, we propose Vision-Language-Action Jump-Starting (VLAJS), a method that bridges sparse VLA guidance with on-policy RL to improve exploration and learning efficiency. VLAJS treats VLAs as transient sources of high-level action suggestions that bias early exploration and improve credit assignment, while preserving the high-frequency, state-based control of RL. Our approach augments Proximal Policy Optimization (PPO) with a directional action-consistency regularization that softly aligns the RL agent's actions with VLA guidance during early training, without enforcing strict imitation, requiring demonstrations, or relying on continuous teacher queries. VLA guidance is applied sparsely and annealed over time, allowing the agent to adapt online and ultimately surpass the guiding policy. We evaluate VLAJS on six challenging manipulation tasks: lifting, pick-and-place, peg reorientation, peg insertion, poking, and pushing in simulation, and validate a subset on a real Franka Panda robot. VLAJS consistently outperforms PPO and distillation-style baselines in sample efficiency, reducing required environment interactions by over 50% in several tasks. Real-world experiments demonstrate zero-shot sim-to-real transfer and robust execution under clutter, object variation, and external perturbations.

---

## 论文详细总结（自动生成）

### 论文总结：利用视觉-语言-动作正则化加速强化学习 (VLAJS)

#### 1. 核心问题与整体含义
*   **研究动机**：强化学习（RL）在机器人操作中具备高频闭环控制和精确性优势，但在处理**长程任务（Long-horizon）**或**奖励函数设计不完善（Suboptimal Reward）**时，面临探索效率极低和信用分配（Credit Assignment）困难的问题。
*   **背景**：视觉-语言-动作（VLA）模型（如 OpenVLA）拥有强大的语义推理和通用泛化能力，但其推理延迟高、控制频率低且缺乏精度。
*   **核心目标**：将 VLA 的宏观语义指导与 RL 的微观高频控制相结合，利用 VLA 作为“启动器”引导 RL 早期探索，从而解决 RL 的冷启动问题。

#### 2. 方法论
*   **核心思想 (VLAJS)**：将预训练的 VLA 模型视为一种**瞬态（Transient）且稀疏（Sparse）**的辅助指导源，而非持续的模仿对象。
*   **关键技术细节**：
    *   **稀疏查询与时间离散化**：为了降低计算开销，不在每个 RL 步查询 VLA，而是在每个 Rollout 中仅查询少量次数，并将 VLA 输出的动作在时间上插值分解到后续 $D$ 个控制步中。
    *   **方向性动作一致性损失 (Directional Loss)**：不使用传统的均方误差（MSE）强制匹配动作数值，而是使用**余弦相似度损失**。这只要求 RL 智能体的动作方向与 VLA 建议一致，允许智能体自主决定动作幅度和精度。
    *   **基于奖励趋势的跳跃启动调度**：引入自适应机制，根据奖励增长情况动态减少 VLA 查询频率并衰减损失权重。
    *   **永久停用机制**：一旦 RL 智能体的表现达到预设阈值（如平均奖励 > 3），则完全关闭 VLA 指导，让智能体自由探索并最终超越教师模型。

#### 3. 实验设计
*   **场景与数据集**：
    *   **仿真环境**：使用 ManiSkill 操纵基准，包含 6 个任务：取方块、取放方块、销钉重定向、销钉插入、拨动方块、推方块。
    *   **现实世界**：在 Franka Panda 机器人上进行验证，涉及抓取蔬菜、放置碗中、移动锅盖等任务。
*   **对比方法 (Benchmarks)**：
    *   **PPO**：标准的同策略强化学习算法。
    *   **Sparse RPD**：基于 MSE 损失的稀疏策略蒸馏方法。
    *   **VLAJS (RPD)**：消融实验，将方向性损失替换为 MSE 损失。
*   **测试维度**：分为“长程任务（步数放大 10 倍）”和“不完善奖励设计”两个主要用例。

#### 4. 资源与算力
*   **算力支持**：论文提到使用了瑞士国家超算中心（CSCS）的 **Alps 系统**进行计算。
*   **具体细节**：文中未明确给出具体的 GPU 型号数量或总训练时长，但强调了 VLA 推理的高昂成本是设计“稀疏查询”机制的主要原因。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个仿真任务上进行了测试，每个实验运行了 **6 个随机种子**并报告了 95% 置信区间。
*   **消融与鲁棒性**：
    *   对比了不同的 VLA 教师（OpenVLA, Octo）。
    *   测试了不同相机视角对 VLA 指导的影响。
    *   验证了在 VLA 未见过的分布外（OOD）任务上的表现。
*   **评价**：实验设计较为充分，涵盖了从算法组件分析到真机部署的全过程，统计学处理（置信区间、宏平均）保证了结果的客观性。

#### 6. 主要结论与发现
*   **效率提升**：VLAJS 在多个任务中将所需的**环境交互次数减少了 50% 以上**。
*   **超越教师**：通过瞬态指导，RL 智能体在训练后期能够摆脱 VLA 的局限，最终成功率和性能均超过了原始 VLA 模型。
*   **方向性损失的优越性**：相比于 MSE 损失，方向性损失在跳跃启动阶段更稳健，不会过度约束智能体的动作空间。
*   **零样本迁移**：仿真训练后的策略无需微调即可直接部署到真实机器人，且对环境扰动（如人手干扰）表现出比纯 VLA 模型更强的鲁棒性。

#### 7. 优点
*   **实用性强**：解决了 VLA 推理慢无法直接用于 RL 训练的痛点，通过稀疏查询实现了计算效率与学习效果的平衡。
*   **设计巧妙**：方向性损失（Directional Loss）的设计非常符合机器人控制逻辑——先学“往哪走”，再学“怎么走得准”。
*   **Sim-to-Real 成功率高**：展示了基于状态的 RL 策略在处理视觉干扰方面的天然优势。

#### 8. 不足与局限
*   **教师依赖**：虽然对 VLA 性能要求不高，但仍需要 VLA 能提供起码的正确方向引导；若 VLA 在某些极端任务中完全失效，该方法可能退化为普通 PPO。
*   **状态估计需求**：真机部署时依赖 YOLO 等外部检测器获取物体状态，增加了系统复杂性。
*   **启发式阈值**：停用指导的奖励阈值（如奖励 > 3）具有一定的启发性，在不同任务或不同奖励量纲下可能需要手动调整。

（完）
