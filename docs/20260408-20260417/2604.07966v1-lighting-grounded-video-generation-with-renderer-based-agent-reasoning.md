---
title: Lighting-grounded Video Generation with Renderer-based Agent Reasoning
title_zh: 基于渲染器智能体推理的受光照约束视频生成
authors: "Ziqi Cai, Taoyu Yang, Zheng Chang, Si Li, Han Jiang, Shuchen Weng, Boxin Shi"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07966v1"
tags: ["query:q7"]
score: 9.0
evidence: 基于扩散模型的场景可控视频生成框架
tldr: 针对视频生成中光照、布局和相机轨迹难以精确控制且相互耦合的问题，本文提出LiVER框架。该框架通过引入包含丰富3D属性的大规模数据集，利用渲染器生成的控制信号引导扩散模型，实现了场景要素的解耦控制。此外，还开发了场景智能体以实现从高层指令到3D信号的自动转换。LiVER在保持高保真度和时序一致性的同时，为电影制作等领域提供了精准的场景编辑能力。
selection_source: fresh_fetch
motivation: 现有视频扩散模型在光照、布局和相机轨迹等关键场景要素上缺乏精确且解耦的控制能力，限制了其在专业领域的应用。
method: 通过统一的3D表示生成渲染控制信号，并利用轻量化调节模块和渐进式训练策略将其集成到基础视频扩散模型中，同时引入场景智能体辅助推理。
result: 实验结果显示，LiVER在实现精确场景控制的同时，在画面真实感和时序一致性方面均达到了当前最先进的水平。
conclusion: LiVER通过结合3D场景属性与扩散模型，为可控视频生成提供了全新的范式，显著提升了视频创作的灵活性和专业性。
---

## 摘要
扩散模型在视频生成方面取得了显著进展，但其可控性仍然是一个主要限制。布局、光照和摄像机轨迹等关键场景要素通常相互纠缠或仅被微弱地建模，限制了它们在电影制作和虚拟制作等需要显式场景控制的领域中的应用。我们提出了 LiVER，这是一个用于场景可控视频生成的扩散框架。为实现这一目标，我们引入了一个新颖的框架，该框架以显式的 3D 场景属性为条件进行视频合成，并由一个包含物体布局、光照和摄像机参数密集标注的新型大规模数据集提供支持。我们的方法通过从统一的 3D 表示中渲染控制信号来解耦这些属性。我们提出了一个轻量级条件模块和一种渐进式训练策略，将这些信号集成到基础视频扩散模型中，确保了稳定的收敛和高保真度。我们的框架支持广泛的应用，包括底层 3D 场景完全可编辑的图像到视频和视频到视频合成。为了进一步提高易用性，我们开发了一个场景智能体，能够自动将高层用户指令转换为所需的 3D 控制信号。实验表明，LiVER 在实现精确、解耦的场景要素控制的同时，达到了最先进的照片级真实感和时间一致性，为可控视频生成树立了新标准。

## Abstract
Diffusion models have achieved remarkable progress in video generation, but their controllability remains a major limitation. Key scene factors such as layout, lighting, and camera trajectory are often entangled or only weakly modeled, restricting their applicability in domains like filmmaking and virtual production where explicit scene control is essential. We present LiVER, a diffusion-based framework for scene-controllable video generation. To achieve this, we introduce a novel framework that conditions video synthesis on explicit 3D scene properties, supported by a new large-scale dataset with dense annotations of object layout, lighting, and camera parameters. Our method disentangles these properties by rendering control signals from a unified 3D representation. We propose a lightweight conditioning module and a progressive training strategy to integrate these signals into a foundational video diffusion model, ensuring stable convergence and high fidelity. Our framework enables a wide range of applications, including image-to-video and video-to-video synthesis where the underlying 3D scene is fully editable. To further enhance usability, we develop a scene agent that automatically translates high-level user instructions into the required 3D control signals. Experiments show that LiVER achieves state-of-the-art photorealism and temporal consistency while enabling precise, disentangled control over scene factors, setting a new standard for controllable video generation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **LiVER** 的新型视频生成框架，旨在解决当前视频扩散模型在光照、布局和相机轨迹控制方面存在的“属性纠缠”和“物理不准确”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频生成模型（如 Sora、Wan2.2 等）虽然画面精美，但在专业创作（如电影制作）所需的**显式场景控制**上表现不足。具体表现为：光照、物体布局和相机运动往往耦合在一起，难以独立调节；且模型难以生成符合物理规律的复杂光照效果（如精确的阴影、高光反射和环境遮挡）。
*   **研究动机**：通过引入 3D 渲染器的物理先验，将光照、几何和相机参数解耦，从而实现高保真、可编辑且符合物理规律的视频合成。

### 2. 方法论：核心思想与技术细节
LiVER 的核心思想是**“以 3D 渲染为桥梁，连接高层指令与像素生成”**。
*   **渲染器智能体推理（Renderer-based Agent Reasoning）**：
    *   **场景智能体**：将用户文本解析为结构化场景图（Scene Graph），从库中检索 3D 资产并优化其空间布局。
    *   **相机智能体**：根据电影摄影术语（如“推拉镜头”、“环绕镜头”）规划平滑的 3D 相机轨迹。
    *   **光照设置**：解析文本中的氛围词（如“温暖”、“阴天”），匹配或生成 HDR 环境贴图。
*   **光照约束场景代理（Lighting-grounded Scene Proxy）**：
    *   利用 3D 引擎（Blender）渲染出多通道 2D 代理信号，包括：**漫反射（Diffuse）**、**粗糙 GGX（Rough GGX）**和**光泽 GGX（Glossy GGX）**。这些信号捕捉了不同频率的光照响应和材质反射。
*   **视频合成架构**：
    *   基于 **Wan2.2-5B** 基础模型，引入轻量级**条件编码器（Proxy Encoder）**和**适配器（Adapter）**。
    *   将 9 通道的场景代理信号（3 种渲染图 × RGB）注入视频潜空间，通过空间残差调制引导生成过程。
*   **三阶段训练策略**：
    1.  **条件路径训练**：冻结主干，仅训练编码器和适配器，学习初步控制。
    2.  **联合 LoRA 微调**：解冻 LoRA 层，优化语义对齐和视觉质量。
    3.  **光照多样性扩展**：混合真实与合成数据，增强模型对复杂光照现象的泛化能力。

### 3. 实验设计
*   **数据集 (LiVERSet)**：包含约 1.1 万个视频剪辑（81 帧，720×1280 分辨率）。
    *   **LiVER-Real**：真实视频，通过算法重建 3D 几何、相机位姿和 HDR 环境光。
    *   **LiVER-Syn**：合成视频，利用 PBR 材质和动态 HDR 环境光渲染，提供极高的光照多样性。
*   **对比基线 (Baselines)**：
    *   **CameraCtrl**（侧重相机控制）
    *   **MotionCtrl**（侧重运动控制）
    *   **VideoFrom3D**（基于 3D 几何生成视频）
*   **评估指标**：FVD/FID（质量）、CLIP（语义）、ATE/RPE（相机精度）、SI-MSE（光照误差）、mIoU（布局精度）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：总 Batch Size 为 16，训练步数约为 10 万步。
*   **优化器**：AdamW，学习率固定为 $1 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：在 1000 个视频的测试集上进行了定量评估。
*   **消融实验**：针对“合成数据的作用”和“阶段性训练策略”进行了对比，证明了合成 PBR 数据对光照控制的关键性。
*   **用户研究**：邀请 25 名参与者对 20 组视频进行盲测，涵盖视频质量、场景控制、相机控制和光照控制四个维度。
*   **评价**：实验设计较为全面，涵盖了从底层物理指标（位姿误差）到高层感知质量（用户偏好）的多个维度，对比方法具有代表性，实验结果客观。

### 6. 主要结论与发现
*   **解耦控制**：LiVER 成功实现了光照与几何的解耦，用户可以独立更改环境贴图而不影响物体形状。
*   **物理真实性**：通过引入 GGX 渲染通道，模型生成的金属反射、皮肤质感和阴影效果显著优于仅依赖文本或简单几何条件的模型。
*   **性能领先**：在所有定量指标（尤其是光照误差和布局精度）上均优于 SOTA 方法。

### 7. 优点与亮点
*   **创新的代理表示**：将复杂的 3D 物理属性简化为多通道 2D 渲染图，既保留了物理信息，又降低了扩散模型的学习难度。
*   **智能体集成**：通过 LLM 智能体降低了 3D 控制的门槛，使用户能通过自然语言驱动复杂的 3D 渲染管线。
*   **高质量数据集**：LiVERSet 为社区提供了稀缺的、带有密集光照和几何标注的视频数据。

### 8. 不足与局限
*   **几何依赖性**：由于初始 3D 重建可能较为粗糙，模型在处理精细几何细节时仍高度依赖文本提示词，可能导致几何一致性在极端情况下受损。
*   **推理开销**：虽然生成过程高效，但前端的 3D 检索、布局优化和渲染过程增加了整体管线的复杂度和预处理时间。
*   **应用限制**：目前主要针对静态场景或简单动态场景，对于具有复杂形变（如人体动作）的场景支持尚不明确。

（完）
