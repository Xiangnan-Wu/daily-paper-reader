---
title: "Matrix-Game 3.0: Real-Time and Streaming Interactive World Model with Long-Horizon Memory"
title_zh: Matrix-Game 3.0：具有长程记忆的实时流式交互式世界模型
authors: "Zile Wang, Zexiang Liu, Jiaxing Li, Kaichen Huang, Baixin Xu, Fei Kang, Mengyin An, Peiyu Wang, Biao Jiang, Yichen Wei, Yidan Xietian, Jiangbo Pei, Liang Hu, Boyi Jiang, Hua Xue, Zidong Wang, Haofeng Sun, Wei Li, Wanli Ouyang, Xianglong He, Yang Liu, Yangguang Li, Yahui Zhou"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08995v2"
tags: ["query:profile-2"]
score: 10.0
evidence: 具有长时记忆的交互式世界模型
tldr: Matrix-Game 3.0 是一款针对 720p 高清、实时且具备长程记忆的交互式世界模型。针对现有模型在长时一致性和生成效率上的不足，该研究通过构建大规模工业级数据引擎、引入基于残差预测与相机感知记忆检索的训练框架，以及采用多段自回归蒸馏与量化加速技术，实现了在 5B 参数下达到 40 FPS 的实时生成速度，并能维持分钟级的时空一致性，为工业级可部署世界模型提供了实践路径。
selection_source: fresh_fetch
motivation: 现有交互式视频生成模型难以同时兼顾长时一致性与高分辨率实时生成，限制了其在现实场景中的应用。
method: 结合工业级无限数据引擎、自纠错残差建模、相机感知记忆检索以及基于 DMD 的多段自回归蒸馏与模型压缩技术。
result: 在 720p 分辨率下，5B 模型实现了 40 FPS 的实时生成，并能保持分钟级序列的稳定一致性，2x14B 模型进一步提升了质量。
conclusion: Matrix-Game 3.0 证明了通过数据、架构与推理优化，可以构建出兼具长程记忆与实时性能的工业级交互式世界模型。
---

## 摘要
随着交互式视频生成技术的进步，扩散模型日益展现出其作为世界模型的潜力。然而，现有方法仍难以同时实现基于记忆的长期时间一致性和高分辨率实时生成，限制了其在现实场景中的应用。为此，我们提出了 Matrix-Game 3.0，这是一种专为 720p 实时长视频生成设计的记忆增强型交互式世界模型。在 Matrix-Game 2.0 的基础上，我们在数据、模型和推理方面引入了系统性的改进。首先，我们开发了一个升级版的工业级无限数据引擎，该引擎集成了基于虚幻引擎（Unreal Engine）的合成数据、从 AAA 级游戏中大规模自动采集的数据以及真实世界视频增强，从而大规模产出高质量的“视频-姿态-动作-提示词”四元组数据。其次，我们提出了一个针对长程一致性的训练框架：通过对预测残差建模并在训练期间重新注入不完美的生成帧，基础模型学会了自我修正；同时，相机感知的记忆检索与注入使基础模型能够实现长程时空一致性。第三，我们设计了一种基于分布匹配蒸馏（DMD）的多段自回归蒸馏策略，并结合模型量化和 VAE 解码器剪枝，实现了高效的实时推理。实验结果表明，Matrix-Game 3.0 使用 5B 模型在 720p 分辨率下可实现高达 40 FPS 的实时生成，同时在长达数分钟的序列中保持稳定的记忆一致性。扩展到 2x14B 模型进一步提升了生成质量、动态效果和泛化能力。我们的方法为实现工业级可部署的世界模型提供了一条切实可行的路径。

## Abstract
With the advancement of interactive video generation, diffusion models have increasingly demonstrated their potential as world models. However, existing approaches still struggle to simultaneously achieve memory-enabled long-term temporal consistency and high-resolution real-time generation, limiting their applicability in real-world scenarios. To address this, we present Matrix-Game 3.0, a memory-augmented interactive world model designed for 720p real-time longform video generation. Building upon Matrix-Game 2.0, we introduce systematic improvements across data, model, and inference. First, we develop an upgraded industrial-scale infinite data engine that integrates Unreal Engine-based synthetic data, large-scale automated collection from AAA games, and real-world video augmentation to produce high-quality Video-Pose-Action-Prompt quadruplet data at scale. Second, we propose a training framework for long-horizon consistency: by modeling prediction residuals and re-injecting imperfect generated frames during training, the base model learns self-correction; meanwhile, camera-aware memory retrieval and injection enable the base model to achieve long horizon spatiotemporal consistency. Third, we design a multi-segment autoregressive distillation strategy based on Distribution Matching Distillation (DMD), combined with model quantization and VAE decoder pruning, to achieve efficient real-time inference. Experimental results show that Matrix-Game 3.0 achieves up to 40 FPS real-time generation at 720p resolution with a 5B model, while maintaining stable memory consistency over minute-long sequences. Scaling up to a 2x14B model further improves generation quality, dynamics, and generalization. Our approach provides a practical pathway toward industrial-scale deployable world models.

---

## 论文详细总结（自动生成）

以下是对论文《Matrix-Game 3.0: Real-Time and Streaming Interactive World Model with Long-Horizon Memory》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的交互式视频生成模型（世界模型）难以同时满足**长程时空一致性**（Long-horizon Consistency）、**高分辨率**（720p）和**实时生成**（Real-time）这三个关键指标。
*   **研究背景**：虽然扩散模型在短视频生成上取得巨大进步，但在交互场景下，由于自回归生成的误差累积（Error Accumulation），模型往往在几十秒后出现内容漂移或崩溃。此外，维持长时记忆通常伴随巨大的计算开销，难以达到工业级部署要求的实时性。

### 2. 方法论：核心思想与关键技术
Matrix-Game 3.0 通过数据、建模和推理三位一体的协同设计解决上述问题：
*   **工业级无限数据引擎**：
    *   **Unreal-Gen**：基于虚幻引擎 5，通过 NavMesh 和 RL 代理自动探索，产出零对齐误差的“视频-姿态-动作”数据。
    *   **AAA 游戏采集**：自动化采集《GTA V》、《赛博朋克 2077》等大作数据，通过解耦架构实现 TB 级录制。
    *   **真实世界增强**：整合 DL3DV、RealEstate10K 等数据集，并统一使用 ViPE 进行姿态标注。
*   **误差感知的基础模型（Error-Aware Base Model）**：
    *   引入**误差缓冲池（Error Buffer）**：在训练中收集模型预测的残差，并将其重新注入到历史帧条件中，使模型学会自我纠错，增强对不完美上下文的鲁棒性。
*   **相机感知的长程记忆机制**：
    *   **统一 DiT 架构**：将检索到的记忆帧、近期历史帧和当前预测帧放入同一个 Self-Attention 空间，实现跨时空的特征交换。
    *   **相机感知检索**：根据相机位姿和视锥重叠度（Frustum Overlap）从历史池中精准检索相关帧。
    *   **扰动 RoPE**：引入头向扰动的旋转位置编码（Head-wise Perturbed RoPE），缓解长序列中的位置混淆。
*   **训练-推理对齐的蒸馏（DMD Distillation）**：
    *   采用多段自回归蒸馏策略，让学生模型在训练时模拟真实的几步采样（Few-step）推理过程，减少训练与推理之间的分布偏移。

### 3. 实验设计
*   **数据集/场景**：使用了约 480 万个视频剪辑，涵盖第一人称和第三人称视角，包括室内、城市、荒野、驾驶等多种复杂环境。
*   **Benchmark 与对比**：
    *   主要对比对象包括 Matrix-Game 2.0、Genie 系列、Wan2.2 等。
    *   **场景重访实验（Scene Revisitation）**：通过让相机在长序列后回到初始位置，测试模型是否能利用记忆恢复之前的场景细节。
*   **评估指标**：生成速度（FPS）、重建质量（PSNR/SSIM）、长时一致性（定性分析）。

### 4. 资源与算力
*   **推理算力**：采用异步部署方案，使用 **8 张 GPU 进行 DiT 推理 + 1 张 GPU 进行 VAE 解码**（支持 H 系列和 A 系列 GPU）。
*   **训练细节**：
    *   基础模型微调：50,000 步，学习率 $2 \times 10^{-5}$。
    *   蒸馏阶段：冷启动 600 步 + 多段训练 2,400 步。
    *   模型规模：提供 5B 参数（主打实时）和 28B MoE 参数（主打高质量泛化）两个版本。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了多组分钟级的长视频生成案例，涵盖了极其多样的游戏和现实场景。
*   **消融实验**：针对推理加速进行了详细消融，包括 **INT8 量化**、**VAE 剪枝（MG-LightVAE）** 和 **GPU 加速检索**。结果显示 GPU 检索对 FPS 的提升最为关键（从 6.6 FPS 提升至 40 FPS）。
*   **客观性**：实验不仅关注视觉质量，还通过量化指标（PSNR/SSIM）评估了 VAE 剪枝带来的损失，并提供了不同 GPU 架构下的性能对比，较为客观全面。

### 6. 主要结论与发现
*   **实时性突破**：5B 模型在 720p 分辨率下实现了 **40 FPS** 的生成速度，达到了真正的实时交互水平。
*   **记忆有效性**：通过相机感知的记忆检索，模型能够维持分钟级的时空一致性，在场景重访时能精准还原先前生成的细节。
*   **扩展效应**：将模型扩展至 28B MoE 规模后，动态表现、物理真实感和泛化能力有显著提升。

### 7. 优点（亮点）
*   **工业级闭环**：从数据自动生成到模型训练再到系统级推理优化，提供了一套完整的工业化落地路径。
*   **自纠错机制**：通过 Error Buffer 解决自回归漂移问题，思路巧妙且有效。
*   **推理效率极高**：通过 VAE 剪枝和异步 GPU 调度，解决了高分辨率视频生成的实时性瓶颈。

### 8. 不足与局限
*   **位姿依赖**：长程记忆高度依赖准确的相机位姿标注，对于缺乏位姿信息的野外视频，其表现可能受限。
*   **28B 模型实时性**：虽然 5B 模型达到了 40 FPS，但论文未明确说明 28B 模型在相同硬件下的实时性能，大模型可能仍面临延迟挑战。
*   **交互深度**：目前的交互主要集中在相机控制和基础动作，对于更复杂的物理交互（如物体破碎、复杂 NPC 互动）的模拟能力仍有待进一步验证。

（完）
