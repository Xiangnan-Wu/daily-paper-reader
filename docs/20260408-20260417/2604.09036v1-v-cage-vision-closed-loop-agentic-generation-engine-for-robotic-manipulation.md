---
title: "V-CAGE: Vision-Closed-Loop Agentic Generation Engine for Robotic Manipulation"
title_zh: V-CAGE：用于机器人操纵的视觉闭环智能体生成引擎
authors: "Yaru Liu, Ao-bo Wang, Nanyang Ye"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09036v1"
tags: ["query:robot"]
score: 9.0
evidence: 通过自主机器人数据合成来扩展 VLA 模型的智能体框架
tldr: "V-CAGE是一个用于机器人操作数据合成的智能体框架，旨在解决现有方法缺乏上下文感知和物理可行性的问题。它通过修复引导的场景构建确保布局合理且可达，并利用视觉语言模型（VLM）进行闭环验证以过滤失败轨迹。此外，该框架引入了感知驱动的压缩算法，在减少90%以上存储空间的同时保持训练效果。V-CAGE实现了端到端自动化，为大规模、高质量机器人数据集的生成提供了高效方案。"
selection_source: fresh_fetch
motivation: 现有的机器人数据生成方法缺乏上下文感知，常导致生成的场景在语义上不连贯或在物理上不可达。
method: 提出一种结合修复引导场景构建、VLM闭环验证及感知驱动压缩的自动化智能体合成框架。
result: "实现了语义结构化且运动可达的场景生成，并通过高效压缩在减少90%存储成本的同时保证了VLA模型的训练效果。"
conclusion: V-CAGE通过集成语义规划与视觉自验证，为大规模合成高质量、多样化的机器人操作数据集提供了可扩展的自动化方案。
---

## 摘要
扩展视觉-语言-动作（VLA）模型需要大规模的数据集，这些数据集必须在语义上连贯且在物理上可行。然而，现有的场景生成方法通常缺乏上下文感知能力，难以合成嵌入丰富语义信息的高保真环境，经常导致目标位置不可达，从而使任务过早失败。我们提出了 V-CAGE（视觉闭环智能体生成引擎），这是一个用于自主机器人数据合成的智能体框架。与传统的脚本化流水线不同，V-CAGE 作为一个具身智能体系统运行，利用基础模型将高层语义推理与底层物理交互联系起来。具体而言，我们引入了修复引导的场景构建（Inpainting-Guided Scene Construction），以系统地安排上下文感知布局，确保生成的场景既具有语义结构，又在运动学上可达。为了确保轨迹的正确性，我们将功能元数据与基于视觉语言模型的闭环验证机制相结合，充当视觉评论者，严格过滤掉静默失败并切断错误传播链。最后，为了克服大规模视频数据集的存储瓶颈，我们实现了一种感知驱动的压缩算法，在不损害下游 VLA 训练效果的前提下，实现了超过 90% 的文件大小缩减。通过集中语义布局规划和视觉自我验证，V-CAGE 实现了端到端流水线的自动化，从而能够高度可扩展地合成多样化、高质量的机器人操纵数据集。

## Abstract
Scaling Vision-Language-Action (VLA) models requires massive datasets that are both semantically coherent and physically feasible. However, existing scene generation methods often lack context-awareness, making it difficult to synthesize high-fidelity environments embedded with rich semantic information, frequently resulting in unreachable target positions that cause tasks to fail prematurely. We present V-CAGE (Vision-Closed-loop Agentic Generation Engine), an agentic framework for autonomous robotic data synthesis. Unlike traditional scripted pipelines, V-CAGE operates as an embodied agentic system, leveraging foundation models to bridge high-level semantic reasoning with low-level physical interaction. Specifically, we introduce Inpainting-Guided Scene Construction to systematically arrange context-aware layouts, ensuring that the generated scenes are both semantically structured and kinematically reachable. To ensure trajectory correctness, we integrate functional metadata with a Vision-Language Model based closed-loop verification mechanism, acting as a visual critic to rigorously filter out silent failures and sever the error propagation chain. Finally, to overcome the storage bottleneck of massive video datasets, we implement a perceptually-driven compression algorithm that achieves over 90\% filesize reduction without compromising downstream VLA training efficacy. By centralizing semantic layout planning and visual self-verification, V-CAGE automates the end-to-end pipeline, enabling the highly scalable synthesis of diverse, high-quality robotic manipulation datasets.

---

## 论文详细总结（自动生成）

以下是对论文《V-CAGE: Vision-Closed-Loop Agentic Generation Engine for Robotic Manipulation》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：扩展视觉-语言-动作（VLA）模型需要海量、语义连贯且物理可行的专家轨迹数据。
*   **现有瓶颈**：
    *   **缺乏上下文感知**：传统仿真数据生成多采用随机放置物体的脚本化流水线，导致场景缺乏语义逻辑（如碗放在地上而非桌上）或出现物理冲突（物体重叠、不可达）。
    *   **“静默失败”问题**：现有的“生成并执行”模式属于开环系统，无法识别执行过程中的隐性失败（如抓取时滑落），导致长程任务中错误累积，生成大量无效数据。
    *   **存储压力**：大规模视频数据集对存储空间要求极高，限制了数据规模的进一步扩展。

### 2. 方法论
V-CAGE 提出了一个具身智能体驱动的自动化合成框架，核心思想是将数据生成过程视为一个具备推理和自验证能力的智能体行为。
*   **修复引导的场景构建 (IGSC)**：
    1.  **资产选择与初步放置**：LLM 根据指令选择物体，并进行初步的无碰撞放置。
    2.  **布局规划与图像修复**：利用 LLM 生成语义布局描述，通过图像修复模型（Nano Banana）生成符合逻辑的目标图像。
    3.  **坐标恢复**：利用 Grounding DINO 和 DINOv2 进行特征匹配，将修复图像中的像素位置映射回 3D 世界坐标。
    4.  **碰撞优化**：使用 L-BFGS-B 算法微调物体位置，确保物理可行性。
*   **基于模板的子任务搜索**：利用预定义的代码模板和物体的功能元数据（如抓取点、放置点），自动合成可执行的原子操作脚本。
*   **VLM 闭环验证**：引入视觉语言模型（如 Gemini 3）作为“视觉评论者”，对每个子任务的执行结果进行视觉确认。若验证失败则立即中止并重新采样，确保轨迹的绝对正确性。
*   **感知驱动的动作感知压缩**：基于人类视觉感知指标（ColorVideoVDP），在保证 VLA 训练效果的前提下，通过关键帧提取和码率优化实现 >90% 的数据压缩。

### 3. 实验设计
*   **实验场景**：涵盖厨房、办公室、零售店和家庭环境。
*   **任务设置**：设计了四个复杂的长程任务：自动结账（AutoCheckout）、面包打包（PackBreads）、文具整理（PackStationery）、橱柜分类（SortToCabinet）。
*   **对比基准 (Benchmark)**：
    *   **模型**：使用 π0.5 VLA 模型作为评估对象。
    *   **对比维度**：预训练模型（零样本）vs. 原始数据微调 vs. 压缩数据微调。
    *   **消融实验**：对比了随机放置 vs. IGSC 布局，以及开环生成 vs. VLM 闭环验证。
*   **Sim-to-Real 验证**：在 ALOHA-AgileX 硬件平台上进行实机测试，验证仿真数据对真实世界策略的提升。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **软件环境**：使用了 SAPIEN 物理仿真后端，验证环节调用了 Gemini 3 API。

### 5. 实验数量与充分性
*   **实验规模**：
    *   每个长程任务生成了 100 条专家轨迹。
    *   消融实验覆盖了 5 类场景（浴室、橱柜、收银台、厨房、办公桌）中的数十个子任务。
    *   实机测试进行了 20 次试验。
*   **充分性评价**：实验设计较为全面，不仅验证了生成数据的有效性，还通过消融实验证明了 IGSC 和 VLM 验证模块的必要性。Sim-to-Real 的对比实验（仅真实数据 vs. 混合数据）有力地支持了该框架的实用价值。

### 6. 主要结论与发现
*   **数据质量显著提升**：经过 V-CAGE 数据微调后，π0.5 模型在复杂任务上的成功率从 0% 提升至最高 100%（文具整理任务）。
*   **闭环验证的必要性**：VLM 验证将数据纯度提升至 100%，有效剔除了开环生成中占比 20%-50% 的无效轨迹。
*   **压缩算法的高效性**：在文件大小缩减 93% 的情况下，VLA 模型的训练效果几乎没有损失（成功率波动在误差范围内）。
*   **增强现实表现**：在真实世界中，加入仿真数据训练使机器人成功率从 20% 提升至 55%，证明了数据的高保真度和迁移能力。

### 7. 优点
*   **智能体化思维**：将数据生成从“盲目脚本”转变为“有意识的推理与验证”，解决了合成数据中的语义逻辑缺失问题。
*   **闭环自纠错**：利用 VLM 解决“静默失败”，极大地提高了自动化数据采集的可靠性。
*   **实用性强**：感知压缩算法解决了大规模视频存储的痛点，对工业化数据生产具有参考价值。

### 8. 不足与局限
*   **计算成本**：基于拒绝采样的验证机制虽然保证了质量，但在任务极其复杂、原生成功率较低时，会浪费大量仿真算力和 VLM 调用成本。
*   **物体类型限制**：目前主要针对刚性物体和简单的关节物体（如抽屉），尚未探讨对柔性物体（如布料、液体）的生成支持。
*   **长程任务挑战**：实验显示随着任务步骤增加，策略成功率仍会出现明显下降，说明长程推理的复合错误仍是 VLA 模型的固有难题。

（完）
