---
title: "Describe-Then-Act: Proactive Agent Steering via Distilled Language-Action World Models"
title_zh: 先描述后行动：通过蒸馏语言-动作世界模型实现主动智能体引导
authors: "Massimiliano Pappa, Luca Romani, Valentino Sacco, Alessio Palma, Stéphane Lathuilière, Fabio Galasso, Xavier Alameda-Pineda, Indro Spinelli"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23149v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于主动智能体引导的蒸馏语言-动作世界模型
tldr: 本研究针对安全关键型智能体在执行动作前需预判后果的需求，指出传统视觉模拟世界模型存在高延迟问题。为此提出DILLO框架，将“模拟后行动”转变为“描述后行动”，通过跨模态蒸馏技术，让轻量级大语言模型学习从策略潜状态预测语义结果。该方法完全绕过视觉生成，实现了14倍的推理加速，并在MetaWorld和LIBERO基准测试中显著提升了任务成功率，为高效、主动的智能体引导提供了新方案。
selection_source: fresh_fetch
motivation: 现有的视觉世界模型在预测动作后果时推理延迟过高，限制了其在实时安全避错场景中的应用。
method: 提出DILLO模型，利用跨模态蒸馏将VLM的语义分析能力迁移至LLM，通过潜状态和动作直接预测文本形式的执行结果。
result: 该方法在MetaWorld和LIBERO任务中实现了14倍的推理加速，并使平均任务成功率提升了9.3个百分点。
conclusion: 实验证明语义层面的结果预测足以替代昂贵的视觉模拟，从而在保证实时性的同时有效引导智能体规避失败。
---

## 摘要
部署安全关键型智能体要求在执行动作之前预见其后果。虽然世界模型为这种主动预见提供了一种范式，但目前依赖视觉模拟的方法会产生极高的延迟，每步通常超过几秒钟。在这项工作中，我们挑战了视觉处理对于预防失败是必不可少的这一假设。我们表明，已训练策略的潜状态结合其计划动作，已经编码了足够的信息来预测动作结果，使得视觉模拟在预防失败方面变得多余。为此，我们引入了 DILLO（蒸馏语言-动作世界模型），这是一个快速引导层，将范式从“先模拟后行动”转变为“先描述后行动”。DILLO 通过跨模态蒸馏进行训练，其中特权视觉语言模型教师对离线轨迹进行标注，而以潜状态为条件的大语言模型学生学习预测语义结果。这创建了一条纯文本推理路径，完全绕过了沉重的视觉生成，比基准方法实现了 14 倍的加速。在 MetaWorld 和 LIBERO 上的实验表明，DILLO 能够生成下一状态的高保真描述并引导策略，使任务的平均回合成功率提升了 9.3 个百分点，最高提升达 15 个百分点。

## Abstract
Deploying safety-critical agents requires anticipating the consequences of actions before they are executed. While world models offer a paradigm for this proactive foresight, current approaches relying on visual simulation incur prohibitive latencies, often exceeding several seconds per step. In this work, we challenge the assumption that visual processing is necessary for failure prevention. We show that a trained policy's latent state, combined with its planned actions, already encodes sufficient information to anticipate action outcomes, making visual simulation redundant for failure prevention. To this end, we introduce DILLO (DIstiLLed Language-ActiOn World Model), a fast steering layer that shifts the paradigm from "simulate-then-act" to "describe-then-act." DILLO is trained via cross-modal distillation, where a privileged Vision Language Model teacher annotates offline trajectories and a latent-conditioned Large Language Model student learns to predict semantic outcomes. This creates a text-only inference path, bypassing heavy visual generation entirely, achieving a 14x speedup over baselines. Experiments on MetaWorld and LIBERO demonstrate that DILLO produces high-fidelity descriptions of the next state and is able to steer the policy, improving episode success rate by up to 15 pp and 9.3 pp on average across tasks.

---

## 论文详细总结（自动生成）

### 论文总结：Describe-Then-Act (DILLO)

#### 1. 核心问题与整体含义（研究动机和背景）
在部署安全关键型智能体（如机器人）时，预判动作后果至关重要。传统的**世界模型（World Models）**通常采用“视觉模拟”路径，即预测未来的图像或视频帧。然而，这种方式存在严重的**高延迟问题**（单步推理常需数秒），难以满足实时控制的需求。
本论文提出了**“潜状态充足性假设”（Latent Sufficiency Hypothesis）**：认为策略网络的内部潜状态（Latent State）已经编码了足够的任务关键信息（如物体几何、距离、接触动力学），无需通过昂贵的像素渲染即可预测动作结果。基于此，作者提出了 **DILLO** 框架，将范式从“先模拟后行动”转变为**“先描述后行动”**，旨在提供一种极低延迟、可解释的主动安全引导层。

#### 2. 方法论：核心思想与关键技术
DILLO 的核心是一个**蒸馏语言-动作世界模型**，其工作流程如下：
*   **核心思想**：通过跨模态知识蒸馏，将一个拥有“特权信息”的视觉语言模型（VLM）教师的预见能力，迁移到一个仅以潜状态为条件的轻量级大语言模型（LLM）学生身上。
*   **关键技术细节**：
    *   **输入**：当前策略的潜状态 $z_t$（由策略编码器提取）和计划执行的动作块 $a_{t:t+k}$。
    *   **输出**：自然语言行为预览 $d$（描述物理交互）和二进制裁决 $c$（Positive/Negative，判断动作是否推进任务）。
    *   **训练流程（三阶段课程学习）**：
        1.  **投影器对齐**：冻结 LLM，训练线性投影器将连续的潜状态和动作映射到 LLM 的嵌入空间。
        2.  **描述蒸馏**：使用 LoRA 微调 LLM，使其能够根据投影后的嵌入生成教师模型提供的自然语言描述。
        3.  **裁决优化**：引入交叉熵损失，优化模型输出“Positive”或“Negative”标签的准确性。
*   **推理机制（拒绝采样）**：在执行前，策略采样 $N$ 个候选动作块，DILLO 对其进行并行评估，仅允许被裁决为“Positive”的动作执行，否则触发回退机制。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **MetaWorld**：单任务强化学习场景（Soccer, Sweep-Into, Drawer-Open）。
    *   **LIBERO**：多任务模仿学习场景（包括 Goal, Object, Spatial 以及包含 10 任务和 90 任务的复杂套件）。
*   **Benchmark 与对比方法**：
    *   **Zero-Shot (ZS) / Few-Shot (FS)**：直接使用原始 LLM/VLM 根据当前观察预测结果。
    *   **Few-Shot Captioning (CAP)**：作为 Oracle 基准，允许模型看到真实的未来观察并进行描述。
    *   **Forewarn**：现有的基于视觉模拟的策略引导基准方法。
*   **评估指标**：T2O（文本到观察保真度）、基于 Qwen2.5 的语义评分、任务成功率（SR）、裁决准确率。

#### 4. 资源与算力
*   **算力支持**：论文提到使用了 NVIDIA 提供的 **DGX Spark** 集群进行大规模数据采集和模拟。
*   **推理硬件**：推理延迟测试在消费级显卡 **NVIDIA RTX 3090** 上完成。
*   **对比硬件**：基准方法 Forewarn 在企业级显卡 RTX A6000 上运行。
*   **训练时长**：文中未明确给出具体的训练总时长，但强调了推理的实时性（1B 模型约 0.26s，4B 模型约 0.38s）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单单任务到极其复杂的 90 任务迁移学习。
*   **数据多样性**：为了保证鲁棒性，训练数据不仅包含专家轨迹，还包含低水平策略的失败案例以及注入感知噪声的轨迹，以模拟真实部署中的协变量偏移。
*   **验证维度**：
    *   **自动评估**：通过 LLM Judge 进行语义一致性分析。
    *   **人类验证**：邀请 10 位机器人领域专家对 120 个回合进行盲测，评估描述的准确性和裁决的一致性。
*   **充分性评价**：实验设计较为全面，涵盖了保真度分析、消融实验（1B vs 4B 规模）和端到端性能提升测试，逻辑闭环清晰。

#### 6. 主要结论与发现
*   **潜状态确实充足**：DILLO 仅凭潜状态生成的描述在保真度上多次超过了能看到未来图像的 CAP 基准，证明了任务优化后的潜状态比原始像素更具预测性。
*   **显著加速**：相比视觉世界模型，DILLO 实现了约 **14 倍** 的推理加速，使实时主动引导成为可能。
*   **性能提升**：在 MetaWorld 和 LIBERO 上，DILLO 使任务成功率平均提升了 **9.3 个百分点**，最高提升 **15 个百分点**。
*   **高准确率**：在无视觉输入的情况下，其失败预测（裁决）的准确率达到了 **91.4%**。

#### 7. 优点
*   **高效实时**：绕过了高开销的图像生成，极大地降低了计算延迟。
*   **可解释性**：提供人类可读的文本预览，不仅能引导机器人，还能让监控人员理解机器人的“意图”。
*   **即插即用**：作为策略无关的包装层，可以适配不同的底层控制算法（RL 或 IL）。
*   **跨模态创新**：成功证明了将 VLM 的特权知识蒸馏到纯文本 LLM 路径的可行性。

#### 8. 不足与局限
*   **架构绑定**：由于潜状态是特定于策略架构的，DILLO 的投影器需要针对不同的策略模型重新训练/对齐。
*   **任务规模瓶颈**：在 LIBERO-90（90 个任务）这种极端多样化的场景下，提升效果相对有限，显示出模型容量或数据规模在极广分布下的挑战。
*   **依赖特权教师**：训练阶段仍需要能够访问模拟器底层状态（如 6DoF 位姿）的教师模型，在纯现实世界数据上的蒸馏可能受限于感知算法的精度。

（完）
