---
title: "TRAP: Hijacking VLA CoT-Reasoning via Adversarial Patches"
title_zh: TRAP：通过对抗性补丁劫持 VLA 的思维链推理
authors: "Zhengxian Huang, Wenjun Zhu, Haoxuan Qiu, Xiaoyu Ji, Wenyuan Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23117v1"
tags: ["query:robot"]
score: 9.0
evidence: 针对使用思维链推理的VLA模型的安全性和对抗性攻击研究
tldr: 本研究揭示了视觉-语言-动作（VLA）模型中思维链（CoT）推理的安全隐患。研究发现CoT推理对机器人动作生成有极强的主导权，据此提出了TRAP攻击框架。该框架利用对抗性补丁（如打印的杯垫）篡改中间推理过程，在不改变用户指令的情况下诱导机器人执行特定错误动作。实验在多种VLA架构和CoT范式下验证了攻击的有效性，并成功进行了实物测试，为VLA系统的安全性研究提供了新视角。
selection_source: fresh_fetch
motivation: 旨在探索VLA模型中CoT推理机制是否存在安全漏洞，以及攻击者是否能通过干扰推理过程来劫持机器人行为。
method: 提出TRAP框架，通过优化对抗性补丁来诱导错误的中间CoT推理，从而实现对机器人最终动作的定向劫持。
result: 在3种主流VLA架构和3种CoT范式上均验证了攻击的有效性，并成功在真实物理场景中通过打印补丁实现了攻击。
conclusion: CoT推理是VLA系统的一个新型攻击向量，亟需开发针对性的防御措施以确保机器人系统的安全性。
---

## 摘要
通过集成思维链（CoT）推理，视觉-语言-动作（VLA）模型在机器人操作中展现出强大的能力，特别是在提高泛化性和可解释性方面。然而，基于 CoT 的推理机制的安全性在很大程度上仍未得到探索。在本文中，我们展示了 CoT 推理为定向控制劫持引入了一种新的攻击向量——例如，在不修改用户指令的情况下，导致机器人错误地将刀而不是苹果递给人。我们首先提供了实证证据，表明即使 CoT 与输入指令在语义上不一致，它也会强烈地支配动作的生成。基于这一观察，我们提出了 TRAP，这是首个针对 CoT 推理 VLA 模型的定向对抗攻击框架。TRAP 使用对抗性补丁（例如放置在桌子上的杯垫）来破坏中间 CoT 推理并劫持 VLA 的输出。通过优化 CoT 对抗损失，TRAP 诱导产生特定的、由攻击者定义的行为。在 3 种主流 VLA 架构和 3 种 CoT 推理范式上的广泛评估验证了 TRAP 的有效性。值得注意的是，我们在现实场景中通过将补丁打印在纸上实现了该攻击。我们的研究结果强调了保障 VLA 系统中 CoT 推理安全性的紧迫需求。

## Abstract
By integrating Chain-of-Thought(CoT) reasoning, Vision-Language-Action (VLA) models have demonstrated strong capabilities in robotic manipulation, particularly by improving generalization and interpretability. However, the security of CoT-based reasoning mechanisms remains largely unexplored.   In this paper, we show that CoT reasoning introduces a novel attack vector for targeted control hijacking--for example, causing a robot to mistakenly deliver a knife to a person instead of an apple--without modifying the user's instruction. We first provide empirical evidence that CoT strongly governs action generation, even when it is semantically misaligned with the input instructions. Building on this observation, we propose TRAP, the first targeted adversarial attack framework for CoT-reasoning VLA models. TRAP uses an adversarial patch (e.g., a coaster placed on the table) to corrupt intermediate CoT reasoning and hijack the VLA's output. By optimizing the CoT adversarial loss, TRAP induces specific and adversary-defined behaviors. Extensive evaluations across 3 mainstream VLA architectures and 3 CoT reasoning paradigms validate the effectiveness of TRAP. Notably, we implemented the patch by printing it on paper in a real-world setting. Our findings highlight the urgent need to secure CoT reasoning in VLA systems.

---

## 论文详细总结（自动生成）

### 论文总结：TRAP —— 通过对抗性补丁劫持 VLA 的思维链推理

#### 1. 核心问题与整体含义（研究动机和背景）
随着视觉-语言-动作（VLA）模型集成思维链（CoT）推理，机器人在复杂任务中的泛化性和可解释性得到了显著提升。然而，这种中间推理机制也引入了新的安全风险。
*   **研究动机**：探索 CoT 推理是否会成为攻击者操纵机器人行为的新路径。
*   **核心发现**：研究者通过实验证明，CoT 在 VLA 模型的动作生成中起到了主导作用。即使输入指令是正确的（如“递给我苹果”），如果中间 CoT 被篡改（如推理出“目标是刀”），机器人最终会执行错误动作。
*   **整体含义**：本文提出了 **TRAP** 框架，证明了攻击者可以通过在物理环境中放置一个看似无害的“对抗性补丁”（如打印的杯垫），在不触碰用户指令的情况下，实现对机器人行为的定向劫持。

#### 2. 方法论：核心思想与关键技术
TRAP 的核心思想是通过优化一个对抗性补丁 $\delta$，诱导 VLA 模型产生攻击者预设的中间 CoT 序列，进而驱动模型生成特定的恶意动作。

*   **核心优化目标**：
    最小化联合损失函数：$\min_{\delta} \mathbb{E} [L_{cot} + \lambda L_{action}]$。
    *   **$L_{cot}$**：使用交叉熵损失，将生成的 CoT Token 序列对齐到攻击者的目标序列。
    *   **$L_{action}$**：针对不同 VLA 架构，离散动作采用交叉熵损失，连续动作（如扩散模型输出）采用均方误差（MSE）损失。
*   **物理鲁棒性技术**：
    *   **同态变换（Homography Transformation）**：模拟补丁在物理桌面上的几何形变。
    *   **颜色校准（Color Calibration）**：利用 MLP 模型学习数字图像与打印图像之间的色彩映射，解决“打印-摄取”过程中的色差问题。
    *   **变换期望（EoT）**：在优化过程中加入随机的光照、位置和角度变换，增强补丁在真实环境中的稳定性。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **仿真环境**：使用了 **SimplerEnv** 和 **LIBERO** 两个主流机器人基准测试平台。
    *   **任务设计**：涵盖了抓取（Pick）、放置（Place）和移动（Move）等 5 种典型操作任务。
*   **受害者模型（Benchmark）**：
    *   **MolmoACT**：集成式 VLA，采用离散动作 Token。
    *   **GraspVLA**：集成式 VLA，采用连续动作回归（物体中心 CoT）。
    *   **InstructVLA**：分层式 VLA，采用文本子任务分解作为 CoT。
*   **对比方法**：
    *   **Random Noise**：基准下限。
    *   **Action-Only Attack**：仅针对动作输出进行优化的传统攻击。
    *   **CoT-Only Attack**：仅针对推理过程的攻击（类似于大模型的越狱攻击）。

#### 4. 资源与算力
*   **硬件设备**：
    *   **补丁优化训练**：使用了一台 **NVIDIA H800 GPU**。
    *   **仿真评估**：使用了一台 **NVIDIA RTX 4090 GPU**。
*   **训练细节**：优化过程中像素更新步长设为 8/255，Batch Size 为 4。论文未明确给出每个补丁的具体训练时长，但提到最大迭代轮数为 80-100 轮。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   每个任务采样了 25 种不同的物体布局用于训练，10 种未见过的布局用于测试。
    *   总计进行了 175 次仿真 Rollouts。
    *   **真实世界实验**：在 Franka Panda 机器人臂上进行了 15 次独立试验。
*   **充分性评估**：实验设计较为全面，不仅验证了攻击成功率（ASR），还通过 **t-SNE 可视化**分析了表征空间的变化，并进行了**指令变体测试**（改写指令）、**通用补丁测试**（一个补丁针对多个指令）以及**迁移性测试**（跨模型版本）。实验覆盖了不同架构和推理范式，具有较强的客观性和说服力。

#### 6. 主要结论与发现
*   **CoT 的主导性**：在指令与 CoT 发生冲突时，VLA 模型倾向于遵循 CoT 的逻辑，这使得劫持推理过程比直接劫持动作更有效。
*   **TRAP 的有效性**：在所有测试模型上，TRAP 的攻击成功率显著优于 Action-Only 方法。在某些模型（如 GraspVLA）上，ASR 超过了 75%。
*   **泛化能力**：对抗性补丁对未见过的物体布局具有良好的泛化性，且在物理世界中依然保持有效（实测成功率约 33.3% 完全劫持，86.7% 部分劫持）。

#### 7. 优点（亮点）
*   **新颖的攻击向量**：首次识别并利用了 VLA 模型中 CoT 推理这一特定的安全漏洞。
*   **跨架构适用性**：证明了无论是离散还是连续动作空间，无论是集成还是分层架构，该攻击均有效。
*   **物理落地**：通过颜色校准和几何变换技术，成功实现了从数字空间到物理空间的攻击迁移，具有极高的现实威胁。

#### 8. 不足与局限
*   **任务复杂度有限**：目前的实验主要集中在短程（Short-horizon）的“抓取与放置”任务，尚未在需要复杂逻辑长程推理的任务中验证。
*   **隐蔽性不足**：生成的对抗性补丁通常具有明显的、不自然的纹理，容易被人类察觉。未来需要研究如何将攻击特征隐藏在自然物体（如真正的杯垫图案）中。
*   **防御研究尚浅**：论文虽然提出了检测 CoT 与指令一致性的防御思路，但未进行深入的防御实验验证。

（完）
