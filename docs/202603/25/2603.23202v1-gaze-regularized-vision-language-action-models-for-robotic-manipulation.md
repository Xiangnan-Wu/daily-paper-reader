---
title: Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation
title_zh: 机器人操纵中基于注视正则化的视觉-语言-动作模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23202v1"
tags: ["query:robot"]
score: 9.0
evidence: 视觉-语言-动作模型的视线正则化训练
tldr: 针对视觉-语言-动作（VLA）模型在精细操作任务中缺乏主动视觉注意力的问题，本文提出一种注视正则化训练框架。该方法通过KL散度将人类注视热图转化为补丁级分布，引导模型内部注意力对齐人类视觉模式。在不改变架构和推理开销的前提下，该框架显著提升了模型性能、收敛速度及鲁棒性，并增强了决策的可解释性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理精细操作时缺乏有效的视觉注意力分配机制，而人类注视数据蕴含了丰富的任务意图和规划信息。
method: 利用KL散度将时间聚合的人类注视热图转化为补丁级分布，以此正则化Transformer的注意力权重，使模型学习到人类的感知偏置。
result: "在多个操作基准测试中实现了4-12%的性能提升，且在光照变化和传感器噪声下表现出更强的鲁棒性。"
conclusion: 引入人类感知先验能有效加速机器人学习并提升任务表现，同时无需额外硬件即可增强系统的可解释性。
---

## 摘要
尽管视觉-语言-动作（VLA）模型取得了进展，但由于当前模型缺乏主动视觉注意力分配机制，机器人操纵在细粒度任务中仍面临挑战。人类注视自然地编码了意图、规划和执行模式，为引导机器人感知提供了强大的监督信号。我们提出了一种注视正则化训练框架，在不改变架构或增加推理开销的情况下，将 VLA 模型的内部注意力与人类视觉模式对齐。我们的方法将时间聚合的注视热图转换为 patch 级分布，并通过 KL 散度对 Transformer 的注意力进行正则化，在保持部署效率的同时，建立了对任务相关特征的归纳偏置。当集成到现有的 VLA 架构中时，我们的方法在各项操纵基准测试中实现了 4-12% 的提升。注视正则化模型在更少的训练步数下即可达到同等性能，并在光照变化和传感器噪声下保持鲁棒性。除了性能指标外，学习到的注意力模式还产生了反映人类策略的可解释可视化结果，增强了对机器人系统的信任。此外，我们的框架不需要眼动追踪设备，并可直接应用于现有数据集。这些结果表明，人类感知先验可以显著加速机器人学习，同时提高任务性能和系统可解释性。

## Abstract
Despite advances in Vision-Language-Action (VLA) models, robotic manipulation struggles with fine-grained tasks because current models lack mechanisms for active visual attention allocation. Human gaze naturally encodes intent, planning, and execution patterns -- offering a powerful supervisory signal for guiding robot perception. We introduce a gaze-regularized training framework that aligns VLA models' internal attention with human visual patterns without architectural modifications or inference-time overhead. Our method transforms temporally aggregated gaze heatmaps into patch-level distributions and regularizes the transformer's attention through KL divergence, creating an inductive bias toward task-relevant features while preserving deployment efficiency. When integrated into existing VLA architectures, our approach yields 4-12% improvements across manipulation benchmarks. The gaze-regularized models reach equivalent performance with fewer training steps and maintain robustness under lighting variations and sensor noise. Beyond performance metrics, the learned attention patterns produce interpretable visualizations that mirror human strategies, enhancing trust in robotic systems. Moreover, our framework requires no eye-tracking equipment and applies directly to existing datasets. These results demonstrate that human perceptual priors can significantly accelerate robot learning while improving both task performance and system interpretability.

---

## 论文详细总结（自动生成）

以下是对论文《Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在处理精细机器人操纵任务时表现不佳，主要原因是其感知方式是“被动”的。模型必须同时学习“看哪里”和“如何做”，这导致训练效率低、收敛慢，且在复杂环境下缺乏鲁棒性。
*   **研究背景**：人类在执行任务时会通过主动视觉注意力（注视）来过滤无关信息并锁定关键目标。人类注视中蕴含了任务意图、规划和执行的先验知识。
*   **整体含义**：本文旨在通过引入人类注视模式作为训练时的正则化信号，将 VLA 模型从“被动观察者”转变为“主动感知者”，从而提升其在复杂操纵任务中的性能和可解释性。

### 2. 方法论
*   **核心思想**：在训练阶段，利用预训练的注视预测模型生成合成注视热图，并强制 VLA 模型的内部注意力机制（Transformer Attention）与这些人类注视分布对齐。
*   **关键技术细节**：
    1.  **注视先验生成**：由于机器人数据集通常缺少眼动数据，研究者使用预训练的 **GLC（Global-Local Correlation）** 网络在视频帧上生成合成注视热图。
    2.  **时间聚合**：为了捕捉人类注视的预测性和稳定性，对连续多帧的热图进行加权平均聚合。
    3.  **补丁级分布转换**：将连续的热图投影到与 Transformer 视觉 Token 相同的网格上，转换为离散的概率分布 $G_{i,t}$。
    4.  **注意力正则化**：提取 VLA 模型最后一层视觉-语言交叉注意力的分布 $S_{i,t}$，计算其与注视分布 $G_{i,t}$ 之间的 **KL 散度（Kullback-Leibler Divergence）**。
*   **公式流程**：
    总损失函数定义为：$L_{total} = L_{action} + \lambda \cdot D_{KL}(G_{i,t} \parallel S_{i,t})$。其中 $L_{action}$ 是标准的动作预测损失（如流匹配损失），$\lambda$ 是正则化系数。
*   **推理阶段**：该方法是 **Training-only** 的，推理时不需要注视预测模型或眼动设备，不增加任何计算开销。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO Suite**：包括 Spatial（空间关系）、Object（物体识别）、Goal（目标导向）和 LIBERO-10（多任务泛化）。
    *   **ALOHA-Sim**：用于验证跨域泛化能力（如 Transfer Cube, Peg Insertion）。
    *   **真实世界实验**：在物理机器人上执行“放置方块”、“放置杯子”等任务。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Pi-0 和 OpenVLA。
    *   **对比实验**：对比了无正则化的基准模型、使用不同注视预测器（如 DeepGaze）的模型、以及使用均匀分布（Uniform）作为注意力的模型。
*   **鲁棒性测试**：在光照变化、传感器噪声、语言指令扰动和视角缺失（Missing Views）等条件下进行对比。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练规模**：实验记录了在 10k、20k 和 30k 训练步数下的性能表现，显示出注视正则化能显著加快收敛速度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 个主要的仿真基准测试和 1 个真实世界场景。
    *   进行了详尽的消融实验，包括正则化系数 $\lambda$ 的敏感度分析（发现弱正则化效果最好）。
    *   验证了在不同 VLA 架构（Pi-0 和 OpenVLA）上的通用性。
*   **充分性评价**：实验设计非常全面且客观。通过引入多种视觉和语言扰动，验证了模型不仅是“跑分”高，而且在非理想环境下更具鲁棒性。同时，通过 Top-k 重叠率定量分析了模型注意力与真实人类注视的对齐程度。

### 6. 主要结论与发现
*   **性能提升**：在 LIBERO 各项任务中，成功率普遍提升了 **4-12%**。例如在 LIBERO-Spatial 上从 85.9% 提升至 95.5%。
*   **收敛加速**：注视正则化模型在训练早期（如 20k 步）即可达到基准模型 30k 步甚至更高的水平，显著提高了样本效率。
*   **鲁棒性增强**：在光照变化和相机噪声下，性能差距进一步拉大，证明对齐人类注视能帮助模型锁定更稳定的语义特征。
*   **可解释性**：可视化结果显示，正则化后的模型注意力更集中在任务相关物体（如盘子、夹爪目标）上，而非背景，这增强了人类对机器人决策的信任。

### 7. 优点
*   **无缝集成**：不需要修改现有的 VLA 架构，仅作为辅助损失函数存在。
*   **零推理开销**：在部署阶段完全不增加计算负担，适合实时控制。
*   **无需额外硬件**：利用合成注视模型解决了机器人领域缺乏大规模眼动数据集的问题。
*   **模型无关性**：证明了该方法在 Pi-0 和 OpenVLA 等不同架构上均有效。

### 8. 不足与局限
*   **合成注视的偏差**：虽然验证了合成注视与真实注视的对齐性，但合成模型（GLC）本身可能存在预测偏差，若预测错误可能会误导模型。
*   **任务局限性**：目前的实验主要集中在短时程或中等复杂度的操纵任务，对于极长时程、需要复杂逻辑推理的任务，注视信号的引导作用是否依然如此显著尚待验证。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
