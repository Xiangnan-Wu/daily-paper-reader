---
title: "VLA-IAP: Training-Free Visual Token Pruning via Interaction Alignment for Vision-Language-Action Models"
title_zh: VLA-IAP：通过交互对齐实现视觉-语言-动作模型的免训练视觉 Token 剪枝
authors: "Jintao Cheng, Haozhe Wang, Weibin Li, Gang Wang, Yipu Zhang, Xiaoyu Tang, Jin Wu, Xieyuanli Chen, Yunhui Liu, Wei Zhang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22991v1"
tags: ["query:robot"]
score: 9.0
evidence: 视觉-语言-动作（VLA）模型的视觉标记剪枝
tldr: 针对视觉-语言-动作（VLA）模型推理成本高的问题，本文提出了一种无需训练的视觉标记剪枝方法VLA-IAP。该方法引入“交互优先”范式，通过几何先验保留关键结构锚点，并结合动态调度策略根据语义-运动对齐程度调整剪枝强度。实验表明，VLA-IAP在保持与原始模型相当性能的同时，显著提升了推理速度，并在多种仿真及真实机器人任务中展现出极强的泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉剪枝方法忽略了物理交互这一核心属性，导致在任务早期容易误删对操作至关重要的结构化区域。
method: 提出一种无需训练的交互对齐剪枝策略，利用几何先验保留结构锚点，并根据语义-运动对齐程度动态调整剪枝强度。
result: "在LIBERO基准测试中实现了97.8%的成功率和高达1.54倍的推理加速，性能与未剪枝的骨干模型相当。"
conclusion: VLA-IAP通过显式的交互对齐策略，在不牺牲性能的前提下有效降低了具身智能模型的推理开销，具有很强的实用价值。
---

## 摘要
视觉-语言-动作（VLA）模型迅速推动了具身智能的发展，使机器人能够执行复杂的指令驱动型任务。然而，随着模型容量和视觉上下文长度的增加，VLA 系统的推理成本已成为在资源受限平台上进行实际部署的主要瓶颈。现有的视觉 Token 剪枝方法主要依赖于语义显著性或简单的时序线索，忽略了连续的物理交互，而这是 VLA 任务的一个基本属性。因此，当前的方法往往会剪掉视觉上稀疏但在结构上对操作至关重要的区域，导致任务早期阶段的行为不稳定。为了克服这一问题，我们提出转向一种显式的“交互优先”范式。我们提出的免训练方法 VLA-IAP（交互对齐剪枝）引入了一种几何先验机制来保留结构锚点，并采用一种根据语义-运动对齐情况调整剪枝强度的动态调度策略。这实现了从保守到激进的过渡，确保了早期不确定阶段的鲁棒性，以及交互锁定后的高效性。大量实验表明，VLA-IAP 在 LIBERO 基准测试中实现了 97.8% 的成功率和 1.25 倍的加速，并且在保持与未剪枝骨干网络相当的性能下，加速比最高可达 1.54 倍。此外，该方法在多种模型架构、三个不同的仿真环境以及真实机器人平台上均表现出优越且一致的性能，验证了其强大的泛化能力和实际应用价值。我们的项目网站是：https://chengjt1999.github.io/VLA-IAP.github.io/。

## Abstract
Vision-Language-Action (VLA) models have rapidly advanced embodied intelligence, enabling robots to execute complex, instruction-driven tasks. However, as model capacity and visual context length grow, the inference cost of VLA systems becomes a major bottleneck for real-world deployment on resource-constrained platforms. Existing visual token pruning methods mainly rely on semantic saliency or simple temporal cues, overlooking the continuous physical interaction, a fundamental property of VLA tasks. Consequently, current approaches often prune visually sparse yet structurally critical regions that support manipulation, leading to unstable behavior during early task phases. To overcome this, we propose a shift toward an explicit Interaction-First paradigm. Our proposed \textbf{training-free} method, VLA-IAP (Interaction-Aligned Pruning), introduces a geometric prior mechanism to preserve structural anchors and a dynamic scheduling strategy that adapts pruning intensity based on semantic-motion alignment. This enables a conservative-to-aggressive transition, ensuring robustness during early uncertainty and efficiency once interaction is locked. Extensive experiments show that VLA-IAP achieves a \textbf{97.8\% success rate} with a \textbf{$1.25\times$ speedup} on the LIBERO benchmark, and up to \textbf{$1.54\times$ speedup} while maintaining performance \textbf{comparable to the unpruned backbone}. Moreover, the method demonstrates superior and consistent performance across multiple model architectures and three different simulation environments, as well as a real robot platform, validating its strong generalization capability and practical applicability. Our project website is: \href{https://chengjt1999.github.io/VLA-IAP.github.io/}{VLA-IAP.com}.

---

## 论文详细总结（自动生成）

以下是对论文《VLA-IAP: Training-Free Visual Token Pruning via Interaction Alignment for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型虽然在具身智能领域表现出色，但其庞大的视觉 Token 序列导致推理延迟极高（通常低于 5Hz），难以满足机器人实时闭环控制的需求。
*   **研究动机**：现有的视觉剪枝方法多遵循“感知优先”范式，依赖语义显著性或简单时序线索。这导致模型容易误删视觉上不显著但对物理操作至关重要的区域（如透明杯缘、细长手柄），在任务早期阶段引发行为不稳定。本文旨在提出一种“交互优先”的剪枝范式，确保在压缩 Token 的同时保留物理交互的关键结构。

### 2. 方法论：VLA-IAP 核心思想与技术细节
VLA-IAP 是一种**无需训练（Training-free）**的动态剪枝框架，核心技术包括：
*   **几何先验机制（Geometric Prior）**：利用轻量级的 Sobel 算子提取图像边缘梯度，独立于语义注意力显式地保留物体的物理轮廓（结构锚点）。这纠正了模型只关注语义丰富区域而忽略操作边界的偏差。
*   **语义-运动对齐模块**：
    *   **语义先验**：计算视觉特征与语言指令特征的互相关联。
    *   **运动先验**：通过视觉特征的**二阶时序差分**（近似加速度）来过滤相机平移等线性噪声，识别真正的物理运动区域，并结合历史累积和高斯平滑增强连通性。
*   **交互对齐动态策略**：计算语义掩码与运动掩码的交并比（IoU）。
    *   **保守模式（低 IoU）**：任务早期，采用“双弱排除”策略，仅剔除纯背景，确保不丢失潜在目标。
    *   **激进模式（高 IoU）**：当意图与运动对齐（交互锁定）时，收缩语义关注点并结合运动区域，实现极高比例的压缩。
*   **最终 Token 选择**：融合几何、语义、运动得分，通过索引收集操作生成精简的视觉序列输入 LLM。

### 3. 实验设计
*   **仿真基准测试**：
    *   **LIBERO**：评估终身学习下的空间、对象、目标和长程任务泛化性。
    *   **VLABench**：测试复杂的长程推理任务（如加调料、选扑克）。
    *   **CALVIN**：评估长程语言条件下的策略连续性。
*   **对比方法**：包括 FastV、SparseVLM、DivPrune、VLA-Cache、SpecPrune-VLA、EfficientVLA 等主流剪枝方法。
*   **骨干模型**：验证了在 OpenVLA-OFT、DreamVLA、$\pi_0$ 和 $\pi_{0.5}$ 等多种架构上的通用性。
*   **真实世界实验**：在自研的双臂机器人平台上执行简单任务、长程任务及双臂协同任务。

### 4. 资源与算力
*   **硬件环境**：实验运行在 Linux 系统上，使用单张 **NVIDIA A100 GPU**。
*   **算力特点**：由于该方法是**免训练**的，因此不需要额外的训练算力和时长，主要开销在于推理时的轻量级先验计算（如 Sobel 算子和特征差分）。
*   **推理效率**：在 50% 留存率下，CUDA 推理延迟从 98ms 降至约 72ms。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 3 个大型仿真基准和 3 类真实机器人任务，进行了不同 Token 留存率（70%、50%、30%）的对比。
*   **消融实验**：详细验证了时序剪枝、IoU 引导切换和边缘增强三个组件的贡献。
*   **客观性**：通过跨模型架构（Transformer-based, Diffusion-based）和跨环境的测试，证明了方法的鲁棒性。实验结果显示在极端压缩（仅保留 30% Token）下，VLA-IAP 的成功率远超其他感知优先的方法。

### 6. 主要结论与发现
*   **性能与速度双赢**：在 LIBERO 上实现了 97.8% 的成功率和 1.25x 加速；在保持原始性能的前提下，最高可实现 **1.54x 的推理加速**。
*   **交互优先的必要性**：实验证明，单纯依赖 LLM 的内部注意力图进行剪枝在复杂任务中会失效，因为 LLM 往往会关注无关背景。
*   **几何先验的作用**：显式引入边缘信息能有效防止在操作关键时刻丢失物体边界，从而提升操作精度。

### 7. 优点
*   **即插即用**：无需重新训练或微调模型，部署成本极低。
*   **物理扎根性强**：通过几何和运动先验，解决了 VLA 模型在剪枝过程中的“物理失真”问题。
*   **动态自适应**：能够根据任务阶段（探索 vs. 交互）自动调整剪枝强度，平衡了安全性和效率。

### 8. 不足与局限
*   **超参数依赖**：IoU 阈值、边缘权重等超参数可能需要针对不同环境进行微调以达到最优效果。
*   **极端环境挑战**：在光照剧烈变化或背景极度复杂的环境下，基于 Sobel 的边缘提取和二阶差分的运动检测可能会引入噪声。
*   **硬件加速限制**：虽然减少了 Token 数量，但引入的先验计算模块（如形态学操作）在某些嵌入式硬件上可能存在额外的优化空间。

（完）
