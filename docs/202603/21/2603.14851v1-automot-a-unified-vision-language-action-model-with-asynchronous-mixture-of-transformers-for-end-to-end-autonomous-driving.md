---
title: "AutoMoT: A Unified Vision-Language-Action Model with Asynchronous Mixture-of-Transformers for End-to-End Autonomous Driving"
title_zh: AutoMoT：一种基于异步 Mixture-of-Transformers 的端到端自动驾驶统一视觉-语言-动作模型
authors: "Wenhui Huang, Songyan Zhang, Qihang Huang, Zhidong Wang, Zhiqi Mao, Collister Chua, Zhan Chen, Long Chen, Chen Lv"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14851v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于端到端自动驾驶的VLA模型
tldr: AutoMoT是一个统一的视觉-语言-动作（VLA）模型，旨在解决端到端自动驾驶中推理与动作空间不匹配及推理延迟问题。它采用异步Transformer混合架构（MoT），通过联合注意力共享保留预训练VLM的推理能力，并支持不同频率的任务执行。实验证明其在多项基准测试中表现优异，并揭示了预训练VLM在自动驾驶中语义理解与动作规划的界限。
selection_source: fresh_fetch
motivation: 现有端到端自动驾驶系统在整合VLM时面临推理与动作分布不一致、预训练能力利用不足及推理延迟高等挑战。
method: 提出AutoMoT框架，利用具有联合注意力共享的混合Transformer架构，通过异步执行实现高效的快慢推理。
result: 在多个开环和闭环基准测试中，AutoMoT达到了与当前最先进方法相当的竞争性性能。
conclusion: 预训练VLM通过语义提示即可实现出色的场景理解，但在决策和轨迹规划等动作级任务上仍需针对性微调。
---

## 摘要
将视觉语言模型（VLMs）集成到端到端（E2E）自动驾驶（AD）系统中，在提升场景理解方面展现出巨大潜力。然而，现有的集成策略存在若干局限性：它们要么难以解决推理空间与动作空间之间的分布失配问题，要么未能充分利用预训练 VLM 的通用推理能力，或者在动作策略生成过程中产生显著的推理延迟，从而降低了驾驶性能。为了应对这些挑战，我们在本文中提出了 AutoMoT，这是一个将推理和动作生成统一在单个视觉-语言-动作（VLA）模型中的端到端自动驾驶框架。我们的方法利用了具有联合注意力共享的 Mixture-of-Transformer（MoT）架构，在保留预训练 VLM 通用推理能力的同时，通过在不同任务频率下的异步执行实现高效的快慢推理。在多个基准测试的开环和闭环设置下进行的广泛实验表明，AutoMoT 达到了与现有最先进方法相当的性能。我们进一步研究了预训练 VLM 在自动驾驶中的功能边界，探讨了何时需要针对自动驾驶进行专门的微调。结果表明，预训练 VLM 仅通过语义提示即可实现具有竞争力的多任务场景理解性能，而微调对于决策和轨迹规划等动作级任务仍然至关重要。演示视频和定性结果请参考项目主页。

## Abstract
Integrating vision-language models (VLMs) into end-to-end (E2E) autonomous driving (AD) systems has shown promise in improving scene understanding. However, existing integration strategies suffer from several limitations: they either struggle to resolve distribution misalignment between reasoning and action spaces, underexploit the general reasoning capabilities of pretrained VLMs, or incur substantial inference latency during action policy generation, which degrades driving performance. To address these challenges, we propose \OURS in this work, an end-to-end AD framework that unifies reasoning and action generation within a single vision-language-action (VLA) model. Our approach leverages a mixture-of-transformer (MoT) architecture with joint attention sharing, which preserves the general reasoning capabilities of pre-trained VLMs while enabling efficient fast-slow inference through asynchronous execution at different task frequencies. Extensive experiments on multiple benchmarks, under both open- and closed-loop settings, demonstrate that \OURS achieves competitive performance compared to state-of-the-art methods. We further investigate the functional boundary of pre-trained VLMs in AD, examining when AD-tailored fine-tuning is necessary. Our results show that pre-trained VLMs can achieve competitive multi-task scene understanding performance through semantic prompting alone, while fine-tuning remains essential for action-level tasks such as decision-making and trajectory planning. We refer to \href{https://automot-website.github.io/}{Project Page} for the demonstration videos and qualitative results.

---

## 论文详细总结（自动生成）

以下是对论文《AutoMoT: A Unified Vision-Language-Action Model with Asynchronous Mixture-of-Transformers for End-to-End Autonomous Driving》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：将视觉语言模型（VLM）集成到端到端（E2E）自动驾驶中已成为趋势，旨在利用其强大的场景理解和推理能力。
*   **核心问题**：现有集成方式存在三大局限：
    1.  **分布失配**：VLM 的推理空间与驾驶员的动作空间之间存在分布不一致。
    2.  **能力浪费**：微调 VLM 往往会限制其通用推理能力，甚至导致性能退化。
    3.  **推理延迟**：VLA 模型通常采用同步推理，高昂的计算开销导致动作生成延迟，难以满足实时驾驶需求。
*   **整体含义**：本文旨在构建一个统一的 VLA 框架，在保留预训练 VLM 通用智能的同时，通过异步架构解决实时性与动作精准度的问题。

### 2. 方法论
*   **核心思想**：提出 **AutoMoT** 框架，采用 **混合 Transformer（MoT）** 架构，将模型解耦为“理解专家”和“动作专家”，通过**层间联合注意力共享**实现信息融合，并支持**异步快慢推理**。
*   **关键技术细节**：
    *   **理解专家 (UE)**：采用冻结的 Qwen3-VL-4B 模型，负责高层语义理解和思维链（CoT）推理，保留通用知识。
    *   **动作专家 (AE)**：一个 1.6B 的 Transformer，从头训练以捕获驾驶领域的特定知识，负责生成元动作（Meta-actions）和轨迹规划。
    *   **联合注意力共享 (Joint Attention Sharing)**：AE 在计算注意力时，会跨任务访问 UE 生成的键值对（KV Cache），使动作决策建立在深层语义特征之上。
    *   **异步推理机制**：UE 以低频率运行（慢路径），更新 KV Cache；AE 以高频率运行（快路径），利用最新的 KV Cache 结合实时视觉输入生成动作。
    *   **动作细化器 (Action Refiner)**：集成了一个基于扩散模型（Diffusion-based）的规划头，对 AE 生成的粗略轨迹进行细化。

### 3. 实验设计
*   **数据集与场景**：
    *   **闭环评估**：CARLA Bench2Drive 排行榜。
    *   **开环评估**：nuScenes 数据集。
    *   **推理评估**：LingoQA、OmniDrive、CODA-LM 以及多个通用 VQA 数据集（ScienceQA, FigureQA 等）。
    *   **自建数据集**：NuSync（基于 nuScenes 标注的异步多帧决策数据集）和 PDM-Meta。
*   **对比方法**：包括 UniAD、VAD、DriveVLM、AutoVLA、SimLingo、ORION 等当前最先进的（SOTA）端到端模型。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练策略**：采用了全分片数据并行（FSDP）技术。
*   **训练细节**：学习率在 $1 \times 10^{-4}$ 到 $2 \times 10^{-5}$ 之间，AE 参数量约为 1.6B。文中未明确给出总训练时长，但提到了针对不同任务的微调过程。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从感知推理到开环规划，再到闭环驾驶的全流程测试。
*   **消融实验**：非常详尽，包括：
    1.  **VLM 冻结 vs 微调**：探讨了功能边界和灾难性遗忘问题。
    2.  **同步 vs 异步推理**：验证了异步机制在降低延迟方面的有效性。
    3.  **组件贡献**：分析了理解专家和决策目标对规划性能的影响。
    4.  **规划头对比**：对比了 MLP 头与 Diffusion 头的性能与稳定性。
*   **客观性**：通过在多个第三方基准测试（如 Bench2Drive）上取得 SOTA 结果，证明了实验的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：AutoMoT 在 Bench2Drive 闭环测试中取得了 87.34 的驾驶得分（DS），显著优于现有 VLM 增强方法。
*   **功能边界**：预训练 VLM 仅通过语义提示（Prompting）即可胜任场景理解，而**针对动作级任务（决策、规划）的微调是必不可少的**。
*   **异步优势**：异步推理机制将推理延迟降低了 **86.8%**（从 0.38s 降至 0.05s），而规划精度的损失微乎其微（仅 1.24%）。
*   **避免遗忘**：冻结 VLM 主干网络能有效防止在学习驾驶任务时出现通用推理能力的“灾难性遗忘”。

### 7. 优点（亮点）
*   **架构创新**：异步 MoT 架构巧妙地解决了 VLM 推理慢与自动驾驶实时性要求高之间的矛盾。
*   **实用性强**：通过 KV Cache 共享实现快慢路径结合，为 VLA 模型在车载嵌入式设备上的部署提供了可行方案。
*   **深入洞察**：对 VLM 在自动驾驶中“何时微调、微调什么”给出了明确的实证建议，具有很高的学术参考价值。

### 8. 不足与局限
*   **模型体量**：尽管有异步加速，但 4B+1.6B 的总参数量对于资源极度受限的边缘计算平台仍具挑战。
*   **随机性风险**：实验发现 Diffusion 规划头虽然上限更高，但在闭环测试中表现出较高的方差（不稳定性），这在安全敏感的自动驾驶领域是一个潜在风险。
*   **数据依赖**：性能高度依赖于高质量的元动作标注数据（如 NuSync），这类数据的获取成本较高。

（完）
