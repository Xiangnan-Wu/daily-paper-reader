---
title: Towards Generalizable Robotic Manipulation in Dynamic Environments
title_zh: 迈向动态环境下的泛化机器人操控
authors: "Heng Fang, Shangru Li, Shuhan Wang, Xuanyang Xi, Dingkang Liang, Xiang Bai"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15620v1"
tags: ["query:robot"]
score: 9.0
evidence: 评估并改进用于动态机器人操作的 VLA 模型
tldr: 针对视觉语言动作（VLA）模型在动态环境下操作能力不足的问题，本文提出了大规模动态操作数据集和基准测试 DOMINO，包含35个任务和超过11万条专家轨迹。同时，提出了一种感知动态的VLA架构PUMA，通过整合历史光流和世界查询来预测未来状态。实验证明PUMA在动态任务上表现优异，且动态数据训练能显著提升模型在静态任务中的泛化能力。
selection_source: fresh_fetch
motivation: 现有的VLA模型因缺乏动态数据集且过度依赖单帧观察，导致其在处理移动目标时缺乏必要的时空推理能力。
method: 引入了大规模动态操作数据集DOMINO，并提出PUMA架构，通过结合历史光流和专门的世界查询来隐式预测物体未来状态。
result: "PUMA在动态任务上的成功率比基准模型绝对提升了6.3%，刷新了当前最先进的性能记录。"
conclusion: 在动态数据上进行训练不仅能增强模型在动态环境中的表现，还能培养出更鲁棒的时空表征，从而提升其在静态任务中的迁移能力。
---

## 摘要
视觉-语言-动作（VLA）模型在静态操控中表现出色，但在包含移动目标的动态环境中却面临挑战。这种性能差距主要源于动态操控数据集的匮乏，以及主流 VLA 模型对单帧观测的依赖，这限制了它们的时空推理能力。为解决这一问题，我们推出了 DOMINO，这是一个用于泛化动态操控的大规模数据集和基准测试，包含 35 个具有层级复杂度的任务、超过 11 万条专家轨迹以及一个多维评估套件。通过全面的实验，我们系统地评估了现有 VLA 模型在动态任务上的表现，探索了增强动态感知能力的有效训练策略，并验证了动态数据的泛化性。此外，我们提出了 PUMA，一种具有动态感知能力的 VLA 架构。通过整合以场景为中心的历史光流和专门的世界查询（world queries）来隐式预测以物体为中心的未来状态，PUMA 将历史感知能力与短时预测相结合。结果表明，PUMA 达到了最先进的性能，在成功率上比基准模型提高了 6.3% 的绝对值。此外，我们证明了在动态数据上进行训练可以培养出鲁棒的时空表示，并能迁移到静态任务中。所有代码和数据均可在 https://github.com/H-EmbodVis/DOMINO 获取。

## Abstract
Vision-Language-Action (VLA) models excel in static manipulation but struggle in dynamic environments with moving targets. This performance gap primarily stems from a scarcity of dynamic manipulation datasets and the reliance of mainstream VLAs on single-frame observations, restricting their spatiotemporal reasoning capabilities. To address this, we introduce DOMINO, a large-scale dataset and benchmark for generalizable dynamic manipulation, featuring 35 tasks with hierarchical complexities, over 110K expert trajectories, and a multi-dimensional evaluation suite. Through comprehensive experiments, we systematically evaluate existing VLAs on dynamic tasks, explore effective training strategies for dynamic awareness, and validate the generalizability of dynamic data. Furthermore, we propose PUMA, a dynamics-aware VLA architecture. By integrating scene-centric historical optical flow and specialized world queries to implicitly forecast object-centric future states, PUMA couples history-aware perception with short-horizon prediction. Results demonstrate that PUMA achieves state-of-the-art performance, yielding a 6.3% absolute improvement in success rate over baselines. Moreover, we show that training on dynamic data fosters robust spatiotemporal representations that transfer to static tasks. All code and data are available at https://github.com/H-EmbodVis/DOMINO.

---

## 论文详细总结（自动生成）

### 迈向动态环境下的泛化机器人操控：DOMINO 与 PUMA 总结

#### 1. 核心问题与研究动机
论文关注的核心问题是**视觉-语言-动作（VLA）模型在动态环境（如物体移动、存在干扰）中表现不佳**。
*   **研究背景**：现有的 VLA 模型（如 OpenVLA、$\pi_0$）在静态任务中表现优异，但在面对移动目标时成功率骤降。
*   **动机**：作者认为这种局限性源于两个方面：一是缺乏大规模的动态操控数据集；二是现有模型大多依赖单帧观测，缺乏处理时空演变和预测未来运动的能力。

#### 2. 核心方法论
论文从“数据”和“架构”两个维度提出了解决方案：

*   **DOMINO 数据集与基准测试**：
    *   **核心思想**：构建一个可扩展、低成本的动态操控数据生成流水线。
    *   **关键技术**：采用“两阶段时空同步法”。首先在静态环境中进行“时间试运行”记录机器人执行时间，然后在动态执行阶段通过“运动学反向计算”确定物体的初始位置和轨迹，确保机器人动作与物体运动精确同步。
    *   **任务分级**：将动态难度分为三级：Level 1（匀速直线）、Level 2（高阶多项式曲线）、Level 3（随机且突发的动力学干扰）。

*   **PUMA 架构（Predictive Unified Manipulation Architecture）**：
    *   **核心思想**：将“历史感知”与“未来预测”相结合。
    *   **关键技术**：
        1.  **场景中心历史编码**：不直接堆叠原始历史帧，而是计算历史帧间的**光流图（Optical Flow）**，为模型提供显式的运动趋势。
        2.  **物体中心未来预测**：引入 $N$ 个可学习的**世界查询（World Queries）**。在训练阶段，利用 Grounded-SAM 提取目标物体的 DINO 特征作为监督信号，强制模型隐式预测物体未来的状态。
        3.  **损失函数**：总损失 $L_{total} = L_{action} + \lambda L_{world}$，其中 $L_{world}$ 是预测特征与真实未来特征之间的余弦相似度损失。

#### 3. 实验设计
*   **数据集/场景**：在基于 SAPIEN 引擎的 RoboTwin 2.0 环境中构建。包含 35 个动态任务，涵盖 5 种机器人形态（如 Aloha、Franka、UR5 等），总计超过 11.7 万条专家轨迹。
*   **Benchmark**：提出了 DOMINO@$\alpha$ 评价体系（$\alpha$ 代表物体最大速度），使用成功率（SR）和操控评分（MS，考虑路径完成度和碰撞惩罚）作为指标。
*   **对比方法**：对比了 ACT、OpenVLA、OpenVLA-OFT、RDT-1B、$\pi_0$、$\pi_{0.5}$、InternVLA-M1 等多种 SOTA 模型。

#### 4. 资源与算力
*   **训练算力**：所有 VLA 模型均在 **NVIDIA A100 GPU** 上进行端到端训练。
*   **数据生成与评估**：在 **NVIDIA RTX 系列 GPU** 上完成。
*   **训练细节**：采用 AdamW 优化器，基础学习率 $10^{-5}$，动作模型学习率 $10^{-4}$，训练 100 个 epoch（约 10 万步），每台设备 batch size 为 16。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模实验，包括 35 个任务的全量对比、不同动态等级（Level 1-3）的压力测试、以及跨形态的验证。
*   **消融实验**：针对历史光流 vs. 原始帧、有无未来预测任务、预测步长 $N$ 的影响进行了详细消融。
*   **公平性**：为了公平对比，作者在相同的 Qwen-VL 骨干网络下重新实现了多个基准模型（标注为 *），确保性能提升源于架构改进而非参数量差异。实验设计较为客观、全面。

#### 6. 主要结论与发现
*   **性能提升**：PUMA 在动态任务上取得了 17.2% 的成功率，比最强基准模型提升了 **6.3% 的绝对值**。
*   **动态数据的价值**：在动态数据上训练的模型在静态任务中表现出极强的**零次迁移（Zero-shot）能力**，甚至在某些任务上超过了纯静态训练的模型。这表明动态数据能帮助模型克服对静态位置的过拟合，学习到更本质的时空表征。
*   **预测的重要性**：单纯注入未来轨迹是不够的，必须结合历史上下文才能实现稳定的闭环控制。

#### 7. 优点与亮点
*   **数据生成创新**：提出的两阶段同步法解决了动态环境下专家演示采集难的问题，实现了自动化大规模生成。
*   **显式运动表征**：引入光流图作为输入，相比隐式学习，显著降低了模型理解物体运动趋势的难度。
*   **多维评估**：不仅看成功率，还引入了操控评分（MS），更细致地衡量了动作的平滑度和安全性。

#### 8. 不足与局限
*   **仿真与现实差距**：虽然在仿真中表现优异，但光流计算对环境光照和遮挡敏感，在真实世界复杂背景下的鲁棒性仍待验证。
*   **高阶动态挑战**：尽管 PUMA 优于基准，但在 Level 3（随机突发动力学）任务下的成功率依然较低，说明处理极端不可预测运动仍是难题。
*   **计算开销**：引入历史帧和光流计算增加了推理时的内存占用和预处理延迟。

（完）
