---
title: "AnoleVLA: Lightweight Vision-Language-Action Model with Deep State Space Models for Mobile Manipulation"
title_zh: AnoleVLA：基于深度状态空间模型的轻量级视觉-语言-动作模型，用于移动操作
authors: "Yusuke Takagi, Motonari Kambara, Daichi Yashima, Koki Seno, Kento Tokura, Komei Sugiura"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15046v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于操作的基于深度状态空间模型的轻量级VLA模型
tldr: 针对语言引导的机器人操作任务，传统基于Transformer的视觉-语言-动作（VLA）模型在资源受限环境下计算成本过高。本文提出AnoleVLA，这是一种利用深度状态空间模型（SSM）处理多模态序列的轻量化模型。该模型通过高效的序列建模生成机器人轨迹，在保持高性能的同时显著降低了推理开销，为移动机器人部署提供了高效方案。
selection_source: fresh_fetch
motivation: 传统的Transformer架构在资源受限的移动机器人平台上部署时，面临计算成本高和推理速度慢的挑战。
method: 提出AnoleVLA模型，采用深度状态空间模型（SSM）替代标准Transformer来高效处理视觉和文本输入并生成动作序列。
result: 在真实世界评估中，AnoleVLA的任务成功率比代表性大型VLA高出21个百分点，且推理速度快约三倍。
conclusion: 深度状态空间模型是实现轻量化、高性能机器人视觉-语言-动作建模的有效途径，特别适用于实时移动操作环境。
---

## 摘要
在本研究中，我们探讨了语言引导的机器人操作问题，即机器人需要根据视觉观察和自然语言指令来操纵各种物体。这一任务对于在人类环境中运行的服务机器人至关重要，且需要兼顾安全性、效率和任务通用性。尽管视觉-语言-动作模型（VLA）在该任务中展现出强大的性能，但由于标准 Transformer 骨干网络的计算开销，其在资源受限环境中的部署仍面临挑战。为了克服这一局限性，我们提出了 AnoleVLA，这是一种利用深度状态空间模型高效处理多模态序列的轻量级 VLA。该模型凭借其轻量且快速的序列状态建模能力来处理视觉和文本输入，使机器人能够高效地生成运动轨迹。我们在仿真和物理实验中对所提方法进行了评估。值得注意的是，在真实世界的评估中，AnoleVLA 的任务成功率比代表性的大规模 VLA 高出 21 个百分点，同时推理速度提升了约三倍。

## Abstract
In this study, we address the problem of language-guided robotic manipulation, where a robot is required to manipulate a wide range of objects based on visual observations and natural language instructions. This task is essential for service robots that operate in human environments, and requires safety, efficiency, and task-level generality. Although Vision-Language-Action models (VLAs) have demonstrated strong performance for this task, their deployment in resource-constrained environments remains challenging because of the computational cost of standard transformer backbones. To overcome this limitation, we propose AnoleVLA, a lightweight VLA that uses a deep state space model to process multimodal sequences efficiently. The model leverages its lightweight and fast sequential state modeling to process visual and textual inputs, which allows the robot to generate trajectories efficiently. We evaluated the proposed method in both simulation and physical experiments. Notably, in real-world evaluations, AnoleVLA outperformed a representative large-scale VLA by 21 points for the task success rate while achieving an inference speed approximately three times faster.

---

## 论文详细总结（自动生成）

### AnoleVLA：基于深度状态空间模型的轻量级视觉-语言-动作模型总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对**语言引导的机器人操作**任务，指出当前主流的视觉-语言-动作模型（VLA）虽然性能强大，但大多基于 Transformer 架构。Transformer 的自注意力机制随序列长度呈平方级计算复杂度增长，导致在资源受限的移动机器人平台上部署时，面临推理延迟高、显存占用大等瓶颈。为了实现安全、高效且具有通用性的机器人操作，研究者提出了一种名为 **AnoleVLA** 的轻量级模型，旨在通过更高效的序列建模架构替代传统的 Transformer，提升实时操作性能。

#### 2. 方法论：核心思想与关键技术
AnoleVLA 的核心思想是利用**深度状态空间模型（Deep SSM，具体为 Mamba）**替代 Transformer 作为骨干网络，利用其线性计算复杂度的优势处理多模态序列。
*   **架构组成**：
    *   **输入嵌入模块**：将机器人状态（当前位姿及增量）、视觉观察（通过预训练的 SigLIP 2 编码）和自然语言指令映射到统一的潜在空间。
    *   **Mamba 骨干网络**：由多个 Mamba 块堆叠而成，通过选择性循环机制动态更新隐藏状态，实现长上下文的高效融合。
    *   **动作头**：直接从骨干网络的最后一个隐藏状态生成连续的动作块（Action Chunk），无需像传统 VLA 那样进行动作离散化或自回归解码。
*   **两阶段训练策略**：
    *   **第一阶段**：使用速度损失（Velocity Loss，$L_1$ 损失）监督预测轨迹与真实轨迹的偏差。
    *   **第二阶段**：引入加速度损失（Acceleration Loss），通过监督动作序列的时间差分来优化轨迹的平滑度，减少机器人运动中的抖动。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：使用 Meta-World 基准测试，涵盖 50 种不同的物体操作任务（如移动、开关门等）。
    *   **物理实验**：使用丰田 HSR 移动机器人，在模拟家庭环境（WRS2020RS 标准）中执行“移动、抓取、打开、关闭、按压”五类任务。
*   **对比方法（Benchmark）**：
    *   **大规模模型**：$\pi_{0.5}$（3.0B 参数）。
    *   **同规模轻量级模型**：SmolVLA (0.45B)、VLA-Adapter (0.5B)、TinyVLA (0.42B)。

#### 4. 资源与算力
*   **硬件环境**：使用单块 **NVIDIA GeForce RTX 4090 (24GB)** GPU 和 Intel Core i9-14900F 处理器。
*   **训练开销**：总训练时长约为 **20 小时**（两阶段各 200k 步，共 400k 步）。
*   **推理性能**：推理速度约为 **216 ms/chunk**。模型参数量约为 **0.47B**，计算量为 91 GFLOPs。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖了 Meta-World 的全部任务等级（简单到极难）。
    *   物理实验对 5 类任务各进行了 20 次测试，并收集了 250 个真实演示样本进行微调。
    *   进行了**消融实验**，验证了加速度损失对性能的提升（提升约 4.7% 成功率）。
    *   进行了**错误分析**，将失败案例归纳为位置识别错误、抓取点预测错误和动作执行不完整三类。
*   **评价**：实验设计较为全面，涵盖了从仿真到现实的迁移，对比了多种主流轻量级架构，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能卓越**：在 Meta-World 仿真中，AnoleVLA 的平均成功率（67.85%）显著优于所有对比模型，包括规模更大的 $\pi_{0.5}$。
*   **现实表现强劲**：在真实机器人实验中，AnoleVLA 的平均成功率达到 63%，比最强的轻量级基准 SmolVLA 高出 11 个百分点，比 $\pi_{0.5}$ 高出 21 个百分点。
*   **推理高效**：推理速度比 $\pi_{0.5}$ 快约 **3 倍**，且显存占用更低，证明了 SSM 在实时机器人控制中的潜力。
*   **样本效率高**：在仅有少量（每项任务 50 个）真实演示数据的情况下，AnoleVLA 表现出比大型模型更强的适应能力。

#### 7. 优点与亮点
*   **架构创新**：率先在 VLA 模型中引入 Mamba 架构，解决了 Transformer 在长序列下的计算瓶颈。
*   **平滑控制**：通过两阶段训练和加速度损失，显著改善了机器人执行动作时的平滑度，这在接触密集型任务（如开抽屉）中至关重要。
*   **端到端连续动作**：避免了动作离散化带来的量化误差，简化了推理流程。

#### 8. 不足与局限
*   **空间推理能力有限**：错误分析显示，模型最主要的失败原因是“位置识别错误”，表明模型在精确提取物体空间坐标和理解复杂空间关系方面仍有提升空间。
*   **视觉编码器依赖**：虽然骨干网络很轻量，但仍依赖预训练的视觉编码器（SigLIP 2），视觉特征的质量直接决定了操作上限。
*   **泛化范围**：虽然在特定任务上表现优异，但对于未见过的极端环境或更复杂的长程逻辑任务，其泛化能力尚待进一步验证。

（完）
