---
title: "VectorWorld: Efficient Streaming World Model via Diffusion Flow on Vector Graphs"
title_zh: VectorWorld：基于矢量图扩散流的高效流式世界模型
authors: "Chaokang Jiang, Desen Zhou, Jiuming Liu, Kevin Li Sun"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17652v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于自动驾驶智能体的生成式世界模型
tldr: VectorWorld是一个针对自动驾驶闭环评估的高效流式世界模型。针对现有模型初始化不匹配、实时性差及长时仿真动力学失效等问题，该模型通过生成以自我为中心的矢量图瓦片，结合运动感知VAE、单步扩散流补全和物理对齐的NPC策略，实现了高保真、实时的长距离（1km+）闭环仿真。
selection_source: fresh_fetch
motivation: 现有生成式世界模型在闭环评估中面临初始化不匹配、采样延迟高以及长时仿真中动力学不可行性累积的问题。
method: 提出VectorWorld模型，利用运动感知VAE对齐初始化，采用单步扩散流进行实时矢量图补全，并引入物理对齐的ΔSim策略稳定长时仿真。
result: 在Waymo和nuPlan数据集上，该方法显著提升了地图结构保真度和初始化有效性，支持超过1公里的稳定实时闭环回放。
conclusion: VectorWorld通过结合矢量图生成与物理约束策略，为自动驾驶提供了一个高效、稳定且符合动力学要求的流式仿真环境。
---

## 摘要
自动驾驶策略的闭环评估需要超越日志回放（log replay）的交互式仿真。然而，现有的生成式世界模型在闭环中往往会退化，原因在于：(i) 无历史信息的初始化与策略输入不匹配；(ii) 多步采样延迟违反了实时性预算；(iii) 长时界下不断累积的运动学不可行性。我们提出了 VectorWorld，这是一种流式世界模型，能够在滚动执行（rollout）过程中增量生成以自车为中心的 $64 \mathrm{m}\times 64\mathrm{m}$ 车道-智能体矢量图瓦片（tiles）。VectorWorld 通过运动感知门控 VAE 产生与策略兼容的交互状态，从而使初始化与受历史信息约束的策略保持一致。它通过无求解器的一步掩码补全实现了实时外扩（outpainting），该补全采用边缘门控关系 DiT，并使用区间条件 MeanFlow 和基于 JVP 的大步长监督进行训练。为了稳定长时界滚动执行，我们引入了 $\Delta$Sim，这是一种符合物理规律的非自车（NPC）策略，具有离散-连续混合动作和可微运动学 Logit 整形。在 Waymo Open Motion 和 nuPlan 数据集上，VectorWorld 提高了地图结构的保真度和初始化的有效性，并支持稳定、实时的 $1\mathrm{km}+$ 闭环滚动执行。

## Abstract
Closed-loop evaluation of autonomous-driving policies requires interactive simulation beyond log replay. However, existing generative world models often degrade in closed loop due to (i) history-free initialization that mismatches policy inputs, (ii) multi-step sampling latency that violates real-time budgets, and (iii) compounding kinematic infeasibility over long horizons. We propose VectorWorld, a streaming world model that incrementally generates ego-centric $64 \mathrm{m}\times 64\mathrm{m}$ lane--agent vector-graph tiles during rollout. VectorWorld aligns initialization with history-conditioned policies by producing a policy-compatible interaction state via a motion-aware gated VAE. It enables real-time outpainting via solver-free one-step masked completion with an edge-gated relational DiT trained with interval-conditioned MeanFlow and JVP-based large-step supervision. To stabilize long-horizon rollouts, we introduce $Δ$Sim, a physics-aligned non-ego (NPC) policy with hybrid discrete--continuous actions and differentiable kinematic logit shaping. On Waymo open motion and nuPlan, VectorWorld improves map-structure fidelity and initialization validity, and supports stable, real-time $1\mathrm{km}+$ closed-loop rollouts (\href{https://github.com/jiangchaokang/VectorWorld}{code}).

---

## 论文详细总结（自动生成）

以下是对论文《VectorWorld: Efficient Streaming World Model via Diffusion Flow on Vector Graphs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对自动驾驶（AD）策略在闭环评估中面临的挑战，指出当前生成式世界模型在实际部署为仿真后端时存在三个核心瓶颈：
*   **初始化不匹配（Cold-start）：** 许多模型生成的是静态快照，而反应式策略通常需要历史运动信息作为输入，导致仿真开始时出现瞬态不稳定（如抖动）。
*   **采样延迟高：** 基于扩散模型的生成器通常需要多步迭代，难以满足流式外扩（Streaming Outpainting）的实时性要求。
*   **长时界漂移：** 微小的运动学违规会在长距离（公里级）仿真中累积，导致场景崩溃或不可行。

**VectorWorld** 的核心意义在于通过矢量图（Vector Graph）表示和高效的扩散流（Diffusion Flow）技术，构建了一个能够实时、稳定、长距离运行的流式世界模型。

### 2. 论文提出的方法论
VectorWorld 由三个相互耦合的关键组件组成：

*   **交互状态接口（Motion-aware Gated VAE）：**
    *   引入一个门控机制，根据智能体的运动状态选择性地编码历史轨迹。
    *   对于静止智能体抑制噪声，对于运动智能体保留动力学特征，从而为下游策略提供“热启动”输入，解决初始化不匹配问题。
*   **边缘门控关系 DiT（Edge-Gated Relational DiT）：**
    *   采用异构矢量图表示车道和智能体。
    *   在 Transformer 块中引入边缘条件偏置（Bias）和值门控（Value Gating），显式建模车道拓扑（如前驱/后继）和智能体-车道关系，确保生成结构的连贯性。
*   **区间条件 MeanFlow 与 JVP 监督：**
    *   为了实现**单步（One-step）**生成，采用 MeanFlow 框架预测区间平均速度。
    *   引入基于雅可比-向量积（JVP）的一阶修正项进行大步长监督，校准边界速度，使得模型在无求解器的情况下仅需一次前向计算即可完成高质量的地图外扩。
*   **$\Delta$Sim 物理对齐 NPC 策略：**
    *   结合离散 Token 和连续残差的混合动作空间。
    *   利用可微运动学代价函数对 Logit 进行整形（Shaping），并在训练中引入 DKAL 损失，强制 NPC 行为符合物理规律，抑制误差累积。

### 3. 实验设计
*   **数据集：** 使用了自动驾驶领域主流的 **Waymo Open Motion** 和 **nuPlan** 数据集。
*   **Benchmark 与对比方法：**
    *   **离线生成对比：** 与 SLEDGE (L/XL) 和 ScenDream (B/L) 等最先进的矢量化场景生成器进行对比。
    *   **闭环评估：** 使用 IDM（规则驱动）和 PPO（强化学习驱动）作为测试策略，评估仿真器的稳定性。
*   **评估指标：** 包括感知指标（FD）、地图结构指标（路由长度、终点距离 EPD）、安全性指标（碰撞率）以及闭环指标（成功率、进度、抖动值）。

### 4. 资源与算力
论文在附录 A.13 中明确列出了算力细节：
*   **GPU 型号与数量：** 使用了 **NVIDIA H20 GPU**。
    *   **VAE 训练：** 8 张 GPU，训练 60,000 步。
    *   **MeanFlow 生成器训练：** 8 张 GPU，训练 100,000 步。
    *   **$\Delta$Sim 策略训练：** 4 张 GPU，训练 10,000 步。
*   **推理效率：** 生成一个 $64\mathrm{m} \times 64\mathrm{m}$ 的瓦片仅需约 **6ms**（包含单步采样和解码），完全满足实时流式需求。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大规模的定量评估（数千个场景）和详尽的消融实验。
*   **消融实验：** 针对 VAE 门控、DiT 关系模块、MeanFlow 训练目标（JVP、双时间通道）以及 $\Delta$Sim 的各个组件均做了独立验证。
*   **客观性与公平性：** 引入了“跨后端验证”（Cross-backend evaluation），在 VectorWorld 中训练的策略被放到日志回放和其他仿真器中测试，证明了其训练价值而非仅仅是过拟合仿真器特性。实验设计考虑了长时界（1km+）的极端情况，具有较强的说服力。

### 6. 主要结论与发现
*   **单步生成的有效性：** 证明了通过 JVP 修正的 MeanFlow 可以在极低延迟下保持复杂的矢量图拓扑结构。
*   **初始化至关重要：** 带有运动历史的“交互状态”能显著降低闭环仿真初期的抖动，提高策略成功率（从 42% 提升至 78%）。
*   **长时稳定性：** 物理对齐的 NPC 策略是维持公里级仿真的关键，有效抑制了运动学违规的累积。
*   **训练价值：** 在 VectorWorld 中重新训练的 PPO 策略在压力测试中的成功率从 25% 提升至 56%，证明了该模型作为训练环境的潜力。

### 7. 优点
*   **极高的效率：** 6ms 的生成速度远超传统多步扩散模型，使实时流式仿真成为可能。
*   **结构保真度：** 通过边缘门控机制，解决了矢量化生成中常见的车道断裂和智能体错位问题。
*   **系统性解决闭环痛点：** 不仅仅关注生成质量，还从接口对齐、实时性和物理可行性三个维度系统性地解决了闭环仿真退化问题。

### 8. 不足与局限
*   **地图元素覆盖有限：** 目前主要建模车道中心线，尚未涵盖复杂的路缘、人行横道细节或精细的交通灯逻辑（虽然 nuPlan 实验有所涉及，但仍有提升空间）。
*   **微观分布差异：** 在某些细粒度指标（如 nuPlan 上的智能体间距 JSD）上，单步生成相比多步扩散模型仍存在微小差距。
*   **极端工况覆盖：** 尽管支持压力测试，但对于极端罕见的边缘案例（如多车连环碰撞后的物理模拟）可能仍依赖于简化的运动学模型。

（完）
