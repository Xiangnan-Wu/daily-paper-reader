---
title: "VLA-Thinker: Boosting Vision-Language-Action Models through Thinking-with-Image Reasoning"
title_zh: VLA-Thinker：通过以图思考推理增强视觉-语言-动作模型
authors: "Chaoyang Wang, Wenrui Bao, Sicheng Gao, Bingxin Xu, Yu Tian, Yogesh S. Rawat, Yunhao Ge, Yuzhang Shang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14523v1"
tags: ["query:robot"]
score: 10.0
evidence: 通过图像思考推理增强VLA模型
tldr: 针对现有视觉-语言-动作（VLA）模型在长程任务中仅依赖静态视觉输入和文本推理的局限，本文提出 VLA-Thinker 框架。该框架将感知建模为可动态调用的推理动作，通过 SFT 冷启动激活视觉思维链，并利用 GRPO 强化学习优化推理-动作轨迹。实验表明，该方法在 LIBERO 等基准测试中显著提升了机器人操作的成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 现有 VLA 模型将视觉输入视为静态上下文，限制了其在复杂任务中主动重新审视环境和解决歧义的能力。
method: 提出一种“带图思考”推理框架，通过 SFT 激活视觉思维链并结合 GRPO 强化学习来对齐完整的推理与动作轨迹。
result: "在 LIBERO 基准测试中达到 97.5% 的成功率，并在 RoboTwin 2.0 的长程机器人任务中表现出显著的性能提升。"
conclusion: VLA-Thinker 证明了将感知作为动态推理动作并结合强化学习，能有效增强具身智能模型在复杂环境下的决策能力。
---

## 摘要
视觉-语言-动作（VLA）模型在具身智能方面展现出了巨大的潜力，但现有的大多数方法依赖于基于文本的思维链推理，其中视觉输入被视为静态上下文。这限制了模型在长程任务中主动重新观察环境并消除歧义的能力。我们提出了 VLA-Thinker，这是一个“以图思考”推理框架，它将感知建模为一种可动态调用的推理动作。为了训练该系统，我们引入了一个两阶段训练流水线，包括：(1) 使用精选视觉思维链数据的 SFT 冷启动阶段，以激活结构化推理和工具使用行为；(2) 基于 GRPO 的强化学习，使完整的推理-动作轨迹与任务级成功保持一致。在 LIBERO 和 RoboTwin 2.0 基准测试上的广泛实验表明，VLA-Thinker 显著提升了操作性能，在 LIBERO 上实现了 97.5% 的成功率，并在长程机器人任务中取得了显著收益。项目与代码：https://cywang735.github.io/VLA-Thinker/。

## Abstract
Vision-Language-Action (VLA) models have shown promising capabilities for embodied intelligence, but most existing approaches rely on text-based chain-of-thought reasoning where visual inputs are treated as static context. This limits the ability of the model to actively revisit the environment and resolve ambiguities during long-horizon tasks. We propose VLA-Thinker, a thinking-with-image reasoning framework that models perception as a dynamically invocable reasoning action. To train such a system, we introduce a two-stage training pipeline consisting of (1) an SFT cold-start phase with curated visual Chain-of-Thought data to activate structured reasoning and tool-use behaviors, and (2) GRPO-based reinforcement learning to align complete reasoning-action trajectories with task-level success. Extensive experiments on LIBERO and RoboTwin 2.0 benchmarks demonstrate that VLA-Thinker significantly improves manipulation performance, achieving 97.5% success rate on LIBERO and strong gains across long-horizon robotic tasks. Project and Codes: https://cywang735.github.io/VLA-Thinker/ .

---

## 论文详细总结（自动生成）

以下是对论文《VLA-Thinker: Boosting Vision-Language-Action Models through Thinking-with-Image Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在引入思维链（CoT）推理时，大多局限于“基于文本”的范式。视觉观察通常被一次性编码为静态上下文，而推理仅在语言空间展开。
*   **研究动机**：这种设计限制了跨模态交互，导致模型在面对长程操作任务（Long-horizon manipulation）时，无法像人类一样主动重新审视环境以消除歧义或从中间错误中恢复。
*   **整体含义**：本文旨在打破静态视觉感知的限制，提出一种“以图思考”（Thinking-with-Image）的推理框架，将感知过程建模为推理过程中的一个显式、可动态调用的组件。

### 2. 方法论
*   **核心思想**：将感知视为一种“推理动作”（Reasoning Action）。模型在生成最终控制动作前，可以通过调用视觉工具（如 `ZOOM-IN` 放大特定区域）来主动获取任务相关的视觉信息，实现感知、推理与动作的交替协作。
*   **关键技术细节**：
    *   **多模态交替推理**：模型生成的轨迹包含：文本推理步骤（Thought）、感知工具调用（Tool Call）、返回的视觉证据（Visual Evidence）以及最终的环境动作（Action）。
    *   **两阶段训练流水线**：
        1.  **SFT 冷启动阶段**：利用强大的多模态大模型（如 Qwen3-VL-30B）合成高质量的具身 CoT 数据，包含结构化的推理步骤和工具调用模式，解决具身任务缺乏推理标注的问题。
        2.  **GRPO 强化学习阶段**：采用组相对策略优化（Group Relative Policy Optimization, GRPO），通过稀疏的任务成功奖励（Success Reward）和格式奖励（Format Reward）对完整的推理-动作轨迹进行因果对齐。
*   **算法流程**：模型接收指令和图像 -> 生成推理 -> 决定是否调用工具（如放大局部） -> 接收新视觉反馈 -> 继续推理 -> 输出最终动作。

### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：包含 Spatial（空间）、Object（物体）、Goal（目标）、Long（长程）四个子集，用于评估单臂操作。
    *   **RoboTwin 2.0**：一个具有强领域随机化的双臂协作基准测试，涵盖从短程（约 120 步）到超长程（约 550 步）的 12 个代表性任务。
*   **Benchmark 与对比方法**：
    *   对比了包括 OpenVLA, π0, DeepThinkVLA, CoT-VLA, RDT 等在内的多种最先进（SOTA）模型。
    *   基准模型（Backbone）选用 OpenVLA-OFT。

### 4. 资源与算力
*   **算力配置**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：完整的两阶段训练过程大约需要 **3 天**。
*   **参数设置**：SFT 阶段 Batch Size 为 64，学习率 $1 \times 10^{-5}$；RL 阶段 Batch Size 为 128，学习率 $2 \times 10^{-6}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 上对每个任务进行了 50 次随机初始条件的评估。
    *   在 RoboTwin 2.0 上对每个任务进行了 100 次随机初始条件的评估。
    *   进行了详细的消融实验，分别验证了 SFT 冷启动和 GRPO 强化学习对性能的贡献。
*   **充分性与客观性**：实验覆盖了单臂与双臂、短程与长程、静态与动态环境，对比方法涵盖了主流的 VLA 架构。通过训练曲线展示了成功率的提升与推理长度的优化过程，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能突破**：VLA-Thinker 在 LIBERO 基准测试中达到了 **97.5%** 的平均成功率，比基准模型 OpenVLA-OFT 提升了 **6.5%**。
*   **长程任务优势**：在 RoboTwin 2.0 的长程和超长程任务中，VLA-Thinker 的表现显著优于非推理模型和纯文本推理模型，证明了主动感知能有效减少长链条操作中的误差累积。
*   **推理行为演变**：通过 RL 训练，模型学会了“按需推理”。在任务简单时直接输出动作，在存在歧义或关键信息缺失时才调用视觉工具，从而优化了推理效率。

### 7. 优点（亮点）
*   **范式创新**：首次在 VLA 模型中实现了“以图思考”，将感知从被动输入转变为主动查询。
*   **训练策略高效**：结合了 SFT 的结构化先验和 GRPO 的轨迹级对齐，有效解决了稀疏奖励下的长程推理优化难题。
*   **鲁棒性强**：通过动态重新审视环境，模型展现出了更强的纠错能力和空间感知能力。

### 8. 不足与局限
*   **工具多样性有限**：目前仅验证了 `ZOOM-IN`（放大）这一种视觉工具，尚未探索物体分割、属性识别等更复杂的感知工具。
*   **计算开销**：由于引入了自回归推理过程，推理时间比 OpenVLA-OFT 增加了约 **19%**，在极高频控制场景下可能存在压力。
*   **幻觉风险**：作为基于预训练多模态大模型（MLLM）的系统，仍可能继承 MLLM 的幻觉问题，导致在视觉推理中引用错误的物体属性。

（完）
