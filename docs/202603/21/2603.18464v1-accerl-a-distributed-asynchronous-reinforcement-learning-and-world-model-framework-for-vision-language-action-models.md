---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:robot"]
score: 10.0
evidence: 将可训练的世界模型集成到 VLA 模型的强化学习中
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习过程中面临的计算效率低下、同步延迟以及数据获取困难等核心挑战。
method: 提出了一种完全异步且解耦的框架，通过物理隔离核心环节并集成即插即用的世界模型来生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现了吞吐量的超线性扩展，并展现出极高的样本效率和训练稳定性。
conclusion: AcceRL为大规模VLA模型的高效强化学习训练提供了一个高性能、可扩展且具备世界模型增强能力的系统级解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义
该论文旨在解决大规模**视觉-语言-动作（VLA）模型**在强化学习（RL）训练中面临的效率低下和数据获取困难的问题。
*   **研究动机**：传统的同步强化学习框架存在“长尾效应”，即系统必须等待最慢的模拟器步进或推理完成，导致昂贵的 GPU 资源大量闲置。
*   **核心挑战**：物理模拟器频率低、VLA 模型推理延迟高、以及大规模模型对数据的极度渴求。
*   **整体含义**：AcceRL 通过构建一个完全异步、解耦的分布式框架，并首次集成可训练的“世界模型”（World Model），实现了硬件利用率的最大化和样本效率的指数级提升。

### 2. 方法论
AcceRL 采用了双层异步机制和世界模型增强技术：
*   **宏观异步（训练与采样解耦）**：训练器（Trainer）与采样器（Rollout Worker）物理隔离。采样器持续向分布式经验回放池发送轨迹，训练器则独立采样进行梯度更新，消除全局同步屏障。
*   **微观异步（交互与推理解耦）**：采用“推理即服务”（Inference-as-a-Service）模式。采样器仅负责环境交互，将观测值发送至中央推理池，推理池通过动态批处理（Dynamic Batching）最大化 GPU 利用率。
*   **世界模型（WM）集成**：集成基于扩散模型（Diffusion-based）的观测模型和奖励模型。智能体可以在“想象”中进行虚拟采样，绕过缓慢的物理模拟器。
*   **关键算法优化**：
    *   **GIPO 算法**：使用高斯信任权重代替 PPO 的硬截断，缓解异步架构中的策略滞后（Policy Lag）问题。
    *   **价值重计算（Value Re-computation）**：在更新前使用最新参数重新评估状态价值，确保优势估计的准确性。
    *   **词表瘦身（Vocabulary Slimming）**：裁剪语言模型头，仅保留动作相关的 Token，降低显存占用并提升吞吐量。

### 3. 实验设计
*   **数据集/场景**：使用了 **LIBERO** 机器人操作基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **Benchmark**：以 OpenVLA-OFT（监督学习基准）为基础。
*   **对比方法**：对比了当前最先进的 VLA 强化学习框架，包括 **SimpleVLA-RL** 和 **RLinf-VLA**。

### 4. 资源与算力
*   **GPU 型号**：实验中明确提到了使用 **NVIDIA H200 GPU**。
*   **算力规模**：测试了从 1 到 7 个 Trainer GPU 的扩展性，以及多达 64 个采样工人的并行能力。
*   **训练时长**：文中未给出总训练小时数，但展示了收敛曲线：在世界模型辅助下，仅需约 10,000 步真实环境交互即可实现性能突破。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 LIBERO 的全套四个任务类别，并进行了详细的消融实验（包括价值重计算的影响、GIPO 与 PPO 的对比、吞吐量扩展性分析）。
*   **充分性与客观性**：实验设计较为充分，通过对比 SOTA 方法证明了其在长程任务中的优越性。系统层面的吞吐量测试展示了超线性扩展效果，证明了框架在工程上的高效性。

### 6. 主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中表现出极强的鲁棒性（成功率 99.1%）。
*   **超线性扩展**：得益于 ZeRO 优化和异步设计，系统吞吐量随 GPU 数量增加呈现超线性增长，GPU 利用率保持在 94% 以上。
*   **样本效率飞跃**：通过世界模型的“想象学习”，在线样本效率提升了 **200 倍（20,000%）**。
*   **算法稳定性**：GIPO 算法在异步环境下的收敛速度比标准 PPO 快约 7.5 倍。

### 7. 优点
*   **系统架构创新**：完全解耦的设计彻底解决了分布式 RL 中的“掉队者”问题和 GPU 空转问题。
*   **即插即用世界模型**：首次将可训练的扩散世界模型无缝集成到异步 RL 流水线中，极大地缓解了具身智能的数据瓶颈。
*   **针对性优化**：词表瘦身和 Token 级策略优化非常契合 VLA 模型的自回归特性，提升了数值稳定性。

### 8. 不足与局限
*   **模型支持范围**：目前主要针对 VLA 模型，尚未支持大规模语言模型（LLM）的全量对齐训练。
*   **系统复杂性**：异步架构和世界模型的引入增加了系统的实现难度和超参数调优的复杂性。
*   **硬件依赖**：虽然效率提升显著，但训练 7B 规模的 VLA 模型仍需高性能 GPU 集群支持，对中小型研究团队仍有门槛。

（完）
