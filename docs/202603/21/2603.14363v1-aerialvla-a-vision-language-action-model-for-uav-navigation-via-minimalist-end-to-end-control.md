---
title: "AerialVLA: A Vision-Language-Action Model for UAV Navigation via Minimalist End-to-End Control"
title_zh: AerialVLA：一种通过极简端到端控制实现无人机导航的视觉-语言-动作模型
authors: "Peng Xu, Zhengnan Deng, Jiayan Deng, Zonghua Gu, Shaohua Wan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14363v1"
tags: ["query:robot"]
score: 10.0
evidence: 通过端到端控制实现无人机导航的VLA模型
tldr: 本研究针对无人机视觉语言导航中存在的外部引导依赖和语义鸿沟问题，提出了AerialVLA框架。这是一种极简的端到端视觉-语言-动作模型，能将原始视觉观测和模糊指令直接映射为连续的物理控制信号。通过双视图感知策略、模糊定向提示机制以及集成了着陆信号的统一控制空间，该模型实现了真正的自主导航。实验证明，AerialVLA在已知和未知环境中均表现优异，显著提升了泛化能力。
selection_source: fresh_fetch
motivation: 现有无人机导航方法过度依赖密集外部引导和辅助检测器，限制了系统的自主性并产生了语义鸿沟。
method: 提出一种端到端VLA框架，结合双视图感知、模糊定向提示和统一的3自由度运动及着陆控制空间。
result: 在TravelUAV基准测试中取得SOTA性能，尤其在未知场景下的成功率达到领先基准的三倍。
conclusion: 极简且以自主性为中心的端到端范式比复杂的模块化系统具有更强的鲁棒性和泛化能力。
---

## 摘要
无人机（UAV）的视觉语言导航（VLN）要求在动态 3D 环境中进行复杂的视觉解释和连续控制。现有的分层方法依赖于密集的先验指导或辅助目标检测器，从而产生了语义鸿沟并限制了真正的自主性。我们提出了 AerialVLA，这是一个极简的端到端视觉-语言-动作框架，可将原始视觉观测和模糊语言指令直接映射为连续的物理控制信号。首先，我们引入了一种精简的双视图感知策略，在减少视觉冗余的同时保留了前向导航和精确落地的关键线索，这进一步促进了未来的仿真到现实迁移。为了恢复真正的自主性，我们部署了一种仅源自机载传感器的模糊方向提示机制，完全消除了对密集先验指导的依赖。最后，我们构建了一个统一的控制空间，将连续的 3 自由度（3-DoF）运动学指令与内在降落信号相结合，使智能体无需外部目标检测器即可实现精准降落。在 TravelUAV 基准测试上的大量实验表明，AerialVLA 在已知环境中达到了最先进的性能。此外，它在未知场景中表现出卓越的泛化能力，成功率接近领先基线模型的三倍，验证了以自主为中心的极简范式比复杂的模块化系统能捕获更稳健的视觉-运动表征。

## Abstract
Vision-Language Navigation (VLN) for Unmanned Aerial Vehicles (UAVs) demands complex visual interpretation and continuous control in dynamic 3D environments. Existing hierarchical approaches rely on dense oracle guidance or auxiliary object detectors, creating semantic gaps and limiting genuine autonomy. We propose AerialVLA, a minimalist end-to-end Vision-Language-Action framework mapping raw visual observations and fuzzy linguistic instructions directly to continuous physical control signals. First, we introduce a streamlined dual-view perception strategy that reduces visual redundancy while preserving essential cues for forward navigation and precise grounding, which additionally facilitates future simulation-to-reality transfer. To reclaim genuine autonomy, we deploy a fuzzy directional prompting mechanism derived solely from onboard sensors, completely eliminating the dependency on dense oracle guidance. Ultimately, we formulate a unified control space that integrates continuous 3-Degree-of-Freedom (3-DoF) kinematic commands with an intrinsic landing signal, freeing the agent from external object detectors for precision landing. Extensive experiments on the TravelUAV benchmark demonstrate that AerialVLA achieves state-of-the-art performance in seen environments. Furthermore, it exhibits superior generalization in unseen scenarios by achieving nearly three times the success rate of leading baselines, validating that a minimalist, autonomy-centric paradigm captures more robust visual-motor representations than complex modular systems.

---

## 论文详细总结（自动生成）

### AerialVLA：通过极简端到端控制实现无人机导航的视觉-语言-动作模型总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的无人机视觉语言导航（UAV-VLN）系统普遍依赖“双重拐杖”：一是**密集先验指导（Oracle Guidance）**，即在输入中直接提供精确的转向指令，使智能体沦为被动的指令跟随者；二是**外部目标检测器**（如 Grounding DINO），用于触发降落，导致感知与控制链路断裂。
*   **研究背景**：无人机在 3D 环境中面临 6 自由度（6-DoF）的复杂控制挑战。现有的模块化方法存在语义鸿沟、推理延迟高且在未知环境中泛化性差的问题。本文旨在通过一个极简的端到端视觉-语言-动作（VLA）模型，实现从原始视觉和模糊指令到物理控制信号的直接映射。

#### 2. 方法论
AerialVLA 框架基于 OpenVLA-7B（结合 Llama 2 与 SigLIP/DINOv2 视觉编码器），核心思想包括：
*   **极简双视图感知（Minimalist Dual-View Perception）**：放弃冗余的多摄像头阵列，仅融合**前视（Front）**和**下视（Down）**视图。通过垂直拼接形成 224x224 的图像输入，前视用于避障和目标识别，下视用于精确对准和降落。
*   **模糊定向提示（Fuzzy Directional Prompting）**：完全移除步进式先验指导，仅利用机载传感器（IMU/GPS）获取目标的大致相对方位，并将其映射为“在你右侧”、“正前方”等 7 种模糊语义提示。这迫使模型进行主动视觉寻址，而非被动跟随。
*   **数值动作标记化（Numerical Action Tokenization）**：将连续的 **3 自由度（3-DoF）**动作空间（前向位移 $\Delta x$、垂直位移 $\Delta z$、偏航角变化 $\Delta \psi$）离散化为 99 个 bin，并直接映射到 LLM 词表中的现有数字标记。这种方法利用了预训练模型对数值大小的理解，加速了收敛。
*   **内在降落策略**：将降落信号集成到控制空间中。当模型预测出 `LAND` 标记或接近零的位移时触发降落，无需外部检测器。
*   **几何一致性监督**：在训练阶段过滤掉因人类操作延迟导致的矛盾样本（如侧方有目标却直飞），确保模型学习到正确的避障与转向逻辑。

#### 3. 实验设计
*   **数据集与基准**：使用 **TravelUAV** 基准测试中的 UAV-Need-Help 任务，包含约 1.2 万条人类驾驶轨迹。
*   **测试场景**：分为三个子集：**Seen（已知环境）**、**Unseen Object（未知目标类别）**、**Unseen Map（未知地图/地理环境）**。
*   **对比方法**：
    *   启发式基线：随机动作、固定动作。
    *   混合 VLM 方法：CMA、TravelUAV-DA（带外部检测器的 SOTA）。
    *   通用基础模型：NavFoM、LongFly（具有时空编码能力的最新模型）。
*   **评估指标**：导航误差（NE）、成功率（SR）、先验成功率（OSR）、路径长度加权成功率（SPL）。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA RTX 4090 (24GB)** GPU。
*   **训练细节**：采用 LoRA 微调（秩 $r=64$），可训练参数占比约 2.98%。
*   **时长**：训练 5 个 epoch，耗时约 **35 小时**。
*   **推理效率**：总延迟为 0.38s，优于 TravelUAV 的 0.63s，显存占用约 17GB。

#### 5. 实验数量与充分性
*   **实验规模**：在 7,922 条轨迹上训练，在 3,005 条测试轨迹上验证。
*   **消融实验**：针对数据清洗（有无几何过滤）、感知视图数量（2 视图 vs 5 视图）、动作标记方式（数值标记 vs 自定义标记）进行了详细对比。
*   **充分性评价**：实验覆盖了从已知到完全未知的跨度，对比了当前领域内最强的基线模型。通过消融实验证明了“极简感知”和“数值标记”的有效性，实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **性能领先**：在已知环境中，AerialVLA 的成功率（SR）达到 47.96%，显著超过 LongFly（36.39%）。
*   **泛化能力极强**：在**未知地图（Unseen Map）**中，AerialVLA 的成功率（37.58%）是领先基线（11.27%）的 **3 倍以上**。
*   **自主性验证**：证明了移除密集先验指导后，端到端模型能通过主动视觉感知捕获更稳健的视觉-运动表征，而非简单的轨迹回归。

#### 7. 优点（亮点）
*   **端到端闭环**：实现了从像素到控制指令的真正统一，消除了模块化系统中的语义鸿沟。
*   **感知效率高**：双视图拼接策略在保留关键几何信息的同时，极大降低了计算开销。
*   **无需外部检测器**：内在降落机制提高了系统在开放世界中的鲁棒性。
*   **利用预训练知识**：通过数值标记化巧妙利用了 LLM 的数值推理能力。

#### 8. 不足与局限
*   **缺乏长期记忆**：由于采用反应式控制（Reactive Control）而非显式的历史记忆机制，在高度重复的城市结构中可能出现全局回溯困难。
*   **行为保守**：在极端分布外场景（如目标被茂密树冠严重遮挡）下，模型倾向于安全悬停，而非执行复杂的多阶段探索。
*   **仿真依赖**：目前主要在 AirSim 仿真环境中验证，尚未展示在真实物理环境中的迁移表现（Sim-to-Real）。

（完）
