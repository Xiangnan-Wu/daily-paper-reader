---
title: "DreamPlan: Efficient Reinforcement Fine-Tuning of Vision-Language Planners via Video World Models"
title_zh: DreamPlan：通过视频世界模型对视觉语言规划器进行高效强化微调
authors: "Emily Yue-Ting Jia, Weiduo Yuan, Tianheng Shi, Vitor Guizilini, Jiageng Mao, Yue Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16860v1"
tags: ["query:robot"]
score: 9.0
evidence: 通过视频世界模型对VLM规划器进行微调
tldr: DreamPlan 针对视觉语言模型（VLM）在机器人操纵任务中缺乏物理感知的问题，提出了一种利用视频世界模型进行强化微调的框架。该方法首先利用 VLM 收集探索性数据训练视频生成模型以模拟物理规律，随后在虚拟“想象”中通过 ORPO 算法对 VLM 规划器进行微调。这种方法有效结合了语义推理与物理接地，在无需大规模真实交互的情况下显著提升了复杂任务的成功率。
selection_source: fresh_fetch
motivation: 现有的 VLM 规划器缺乏对物理环境的深刻理解，且直接在现实世界中进行强化学习微调成本高昂且不安全。
method: 利用探索性数据训练动作条件视频生成模型作为世界模型，并在该虚拟环境中通过优势比策略优化（ORPO）对 VLM 进行微调。
result: 实验证明 DreamPlan 能够有效将物理和任务特定知识注入 VLM，显著提高了机器人在复杂操纵任务中的成功率。
conclusion: 通过视频世界模型进行虚拟微调是提升 VLM 规划器物理接地能力和任务性能的一种高效且安全的方法。
---

## 摘要
机器人操纵需要复杂的常识推理，这是大规模视觉语言模型（VLM）天然具备的能力。虽然 VLM 在作为零样本规划器方面展现出前景，但由于缺乏具身的物理理解，在部署于复杂的现实环境（尤其是可变形物体操纵等挑战性任务）时，往往会导致复合误差和较低的成功率。尽管强化学习（RL）可以使这些规划器适应特定的任务动力学，但通过现实世界交互直接微调 VLM 的成本极高、不安全且样本效率低下。为了克服这一瓶颈，我们提出了 DreamPlan，这是一个通过视频世界模型对 VLM 规划器进行强化微调的新型框架。DreamPlan 不依赖昂贵的物理展开，而是首先利用零样本 VLM 收集探索性交互数据。我们证明，这些次优数据足以训练一个动作条件视频生成模型，从而隐式地捕捉复杂的现实物理规律。随后，利用优势比策略优化（ORPO），VLM 规划器完全在该视频世界模型的“想象”中进行微调。通过利用这些虚拟展开，物理和任务特定的知识被高效地注入到 VLM 中。我们的结果表明，DreamPlan 弥合了语义推理与物理具身之间的差距，在无需大规模现实世界数据收集的情况下，显著提高了操纵成功率。项目主页：https://psi-lab.ai/DreamPlan/。

## Abstract
Robotic manipulation requires sophisticated commonsense reasoning, a capability naturally possessed by large-scale Vision-Language Models (VLMs). While VLMs show promise as zero-shot planners, their lack of grounded physical understanding often leads to compounding errors and low success rates when deployed in complex real-world environments, particularly for challenging tasks like deformable object manipulation. Although Reinforcement Learning (RL) can adapt these planners to specific task dynamics, directly fine-tuning VLMs via real-world interaction is prohibitively expensive, unsafe, and sample-inefficient. To overcome this bottleneck, we introduce DreamPlan, a novel framework for the reinforcement fine-tuning of VLM planners via video world models. Instead of relying on costly physical rollouts, DreamPlan first leverages the zero-shot VLM to collect exploratory interaction data. We demonstrate that this sub-optimal data is sufficient to train an action-conditioned video generation model, which implicitly captures complex real-world physics. Subsequently, the VLM planner is fine-tuned entirely within the "imagination" of this video world model using Odds Ratio Policy Optimization (ORPO). By utilizing these virtual rollouts, physical and task-specific knowledge is efficiently injected into the VLM. Our results indicate that DreamPlan bridges the gap between semantic reasoning and physical grounding, significantly improving manipulation success rates without the need for large-scale real-world data collection. Our project page is https://psi-lab.ai/DreamPlan/.

---

## 论文详细总结（自动生成）

### DreamPlan: 通过视频世界模型高效强化微调视觉语言规划器

#### 1. 核心问题与整体含义
论文针对的核心问题是：**视觉语言模型（VLM）虽然具备强大的语义推理能力，但缺乏对物理规律的直观理解（Physical Grounding）**。在处理如绳索、布料等可变形物体的复杂操纵任务时，零样本（Zero-shot）VLM 规划器往往无法准确预测动作产生的物理后果。此外，直接在现实世界中通过强化学习（RL）微调 VLM 存在成本高、不安全且样本效率极低的问题。

**DreamPlan** 的核心意义在于提出了一种**高效的离线强化学习框架**，利用视频生成模型作为“世界模型”来模拟物理反馈，使 VLM 能够在“想象”中学习物理规律，从而弥合语义推理与物理具身之间的鸿沟。

#### 2. 方法论
DreamPlan 包含三个关键阶段：
*   **探索性数据收集（Zero-shot Proposal）**：利用零样本 VLM 在真实环境中执行任务，收集包含动作和观察结果的轨迹数据。即使这些数据大多是失败或次优的，它们仍包含了丰富的因果信号。
*   **视频世界模型训练（World Model Learning）**：
    *   基于预训练的视频扩散模型（如 CogVideoX-5B）。
    *   **关键技术**：通过渲染机器人手臂的运动轨迹，并利用 **ControlNet** 架构将这些动作信号注入扩散模型，使其能够生成与动作一致的物体变形视频。
    *   **优化**：采用“仅物体（Object-Only）”预测策略，去除背景干扰，专注于建模复杂的变形动力学。
*   **世界模型引导的强化微调（RL Fine-Tuning）**：
    *   **Best-of-K 采样**：对于给定的状态，VLM 生成 K 个候选动作，世界模型预测每个动作的后果。
    *   **偏好对构建**：利用 GPT-4o 作为评估器，根据预测结果选出最优动作（正样本）和其余动作（负样本）。
    *   **ORPO 算法**：使用优势比策略优化（Odds Ratio Policy Optimization）直接对 VLM（如 Qwen3-VL-8B）进行微调，增加优选动作的概率，无需在优化循环中反复调用耗时的视频生成。

#### 3. 实验设计
*   **实验场景**：三个具有挑战性的真实世界可变形物体操纵任务：**绳索拉直（Rope Straightening）**、**布料折叠（Cloth Folding）**、**软玩具手臂复位（Toy Arm Repositioning）**。
*   **Benchmark 与对比方法**：
    *   **零样本基准**：Qwen3-VL (4B, 8B, 32B) 和 GPT-4o。
    *   **消融实验**：对比了全场景预测与仅物体预测的效果，以及 DreamPlan 与显式验证（采样后逐一验证）的效率对比。
*   **评估指标**：成功得分（0, 0.5, 1 分制），基于 10 次随机初始状态的试验平均值。

#### 4. 资源与算力
*   **硬件设备**：双臂 Franka FR3 机器人，RealSense D435i 摄像头。
*   **计算资源**：本地工作站配备单块 **NVIDIA RTX 5090 (32GB)** GPU。
*   **训练数据量**：通过自动化流水线收集了约 **2056 条轨迹**，耗时约 **4 小时**。
*   **推理效率**：微调后的模型推理仅需约 **1.12 秒**，而显式验证方法（N=8）需耗时 2605 秒。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种不同类型的可变形物体，每种任务进行 10 次独立试验。
*   **充分性**：实验设计较为全面，包含了定性视频对比、定量成功率对比以及针对核心组件（如视频预测质量、推理效率）的消融研究。
*   **客观性**：对比了当前最强的开源（Qwen3）和闭源（GPT-4o）模型，证明了即使是较小参数的模型（8B）在经过 DreamPlan 微调后也能超越参数量大得多的模型（32B）。

#### 6. 主要结论与发现
*   **物理知识注入成功**：DreamPlan 显著提升了 VLM 的操纵成功率，平均得分从 0.33 提升至 0.60（提升约 80%）。
*   **次优数据有效性**：证明了即使是 VLM 早期失败的尝试数据，也足以训练出一个能够捕捉物理规律的高质量视频世界模型。
*   **效率优势**：通过 ORPO 和离线微调，成功将昂贵的视频生成过程与策略更新解耦，实现了高效的具身智能进化。

#### 7. 优点
*   **高效性**：避免了昂贵的实时物理交互和在线视频生成，推理速度极快。
*   **物理接地**：通过视频扩散模型成功捕捉了传统模拟器难以建模的可变形物体动力学。
*   **通用性**：框架不依赖于特定的 VLM 架构，具有较强的迁移潜力。
*   **动作空间设计**：采用基于关键点的离散动作表示，降低了 VLM 的学习难度。

#### 8. 不足与局限
*   **任务覆盖范围**：虽然在可变形物体上表现优异，但实验任务种类（3 种）相对有限，尚未验证在更广泛、更通用场景下的泛化能力。
*   **依赖预处理**：系统依赖 SAM2 进行物体分割和掩码传播，若分割失败会影响后续流程。
*   **训练耗时**：虽然推理快，但训练阶段的视频生成（Best-of-K 采样）仍然需要较长时间（每个动作序列约 1 分钟）。
*   **评估器依赖**：偏好对的构建依赖于 GPT-4o 的视觉评估能力，可能存在评估偏差。

（完）
