---
title: "VGGT-World: Transforming VGGT into an Autoregressive Geometry World Model"
title_zh: VGGT-World：将 VGGT 转换为自回归几何世界模型
authors: "Xiangyu Sun, Shijie Wang, Fengyi Zhang, Lin Liu, Caiyan Jia, Ziying Song, Zi Huang, Yadan Luo"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12655v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于预测的自回归几何世界模型
tldr: VGGT-World 是一种新型几何世界模型，它摒弃了传统的视频生成方式，转而预测冻结的几何基础模型（VGGT）特征的演变。通过将 VGGT 的潜变量作为世界状态，并利用轻量级时间流变换器进行自回归预测，该模型解决了高维特征空间中的流匹配坍缩和曝光偏差问题。实验证明，该模型在深度预测任务上显著优于现有基准，且推理速度提升了 3.6-5 倍，仅需 0.43B 可训练参数。
selection_source: fresh_fetch
motivation: 传统世界模型侧重于光度细节生成，导致预测结果往往缺乏几何一致性且计算开销巨大。
method: 将冻结的 VGGT 特征作为世界状态，采用 z-预测参数化的流匹配技术和两阶段潜变量流强制课程学习进行自回归轨迹预测。
result: 在 KITTI、Cityscapes 和 TartanAir 等数据集上，深度预测性能显著超越强基线模型，且运行速度提升 3.6-5 倍。
conclusion: 研究证明冻结的几何基础模型特征是构建高效且具有 3D 一致性的世界模型的有效预测状态。
---

## 摘要
通过生成未来视频帧来预测场景演变的世界模型将其大部分容量用于光度细节，但由此产生的预测往往在几何上不一致。我们提出了 VGGT-World，这是一种几何世界模型，它完全避开了视频生成，转而预测冻结的几何基础模型（GFM）特征的时间演变。具体而言，我们将冻结的 VGGT 的潜标记（latent tokens）重新用作世界状态，并训练一个轻量级的时间流变换器（temporal flow transformer）来自回归地预测它们的未来轨迹。在这个高维（d=1024）特征空间中出现了两个技术挑战：(i) 标准的速度预测流匹配（flow matching）会发生崩溃，以及 (ii) 自回归推演（rollout）受到复合曝光偏差的影响。我们通过一种能够产生显著更高信噪比的纯净目标（z-prediction）参数化方法解决了第一个问题，并通过一种两阶段潜流强制（latent flow-forcing）课程解决了第二个问题，该课程使模型逐渐以其自身部分去噪的推演结果为条件。在 KITTI、Cityscapes 和 TartanAir 上的实验表明，VGGT-World 在深度预测方面显著优于最强的基准模型，同时运行速度快 3.6-5 倍，且仅包含 0.43B 个可训练参数，证明了冻结的 GFM 特征是 3D 世界建模中一种有效且高效的预测状态。

## Abstract
World models that forecast scene evolution by generating future video frames devote the bulk of their capacity to photometric details, yet the resulting predictions often remain geometrically inconsistent. We present VGGT-World, a geometry world model that side-steps video generation entirely and instead forecasts the temporal evolution of frozen geometry-foundation-model (GFM) features. Concretely, we repurpose the latent tokens of a frozen VGGT as the world state and train a lightweight temporal flow transformer to autoregressively predict their future trajectory. Two technical challenges arise in this high-dimensional (d=1024) feature space: (i) standard velocity-prediction flow matching collapses, and (ii) autoregressive rollout suffers from compounding exposure bias. We address the first with a clean-target (z-prediction) parameterization that yields a substantially higher signal-to-noise ratio, and the second with a two-stage latent flow-forcing curriculum that progressively conditions the model on its own partially denoised rollouts. Experiments on KITTI, Cityscapes, and TartanAir demonstrate that VGGT-World significantly outperforms the strongest baselines in depth forecasting while running 3.6-5 times faster with only 0.43B trainable parameters, establishing frozen GFM features as an effective and efficient predictive state for 3D world modeling.

---

## 论文详细总结（自动生成）

### VGGT-World：将 VGGT 转换为自回归几何世界模型 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的世界模型（如 Sora, Cosmos）主要通过生成未来视频帧来预测场景演变。然而，这类模型将大量计算资源浪费在光影、纹理等光度细节上，导致生成的几何结构往往不一致（如物体扭曲、深度塌陷）。
**核心问题**：如何在不依赖昂贵的视频生成（VAE 空间）的情况下，构建一个能够保持 3D 几何一致性且高效的预测模型？
**研究动机**：作者认为“几何基础模型”（GFM）如 VGGT 已经编码了丰富的 3D 结构信息。因此，与其预测像素，不如直接预测 GFM 特征在时间上的演变，从而实现“几何世界建模”。

#### 2. 方法论：核心思想与关键技术
VGGT-World 摒弃了视频 VAE，直接在冻结的 VGGT 特征空间中进行自回归预测。
*   **核心思想**：将冻结的 VGGT 编码器提取的潜标记（Latent Tokens）视为“世界状态”，使用轻量级的时间流变换器（Temporal Flow Transformer）预测这些状态的未来轨迹。
*   **关键技术细节**：
    *   **几何状态表示**：提取 VGGT 第 4 层的输出作为预测状态（维度 $d=1024$），该层保留了足够的几何细节且与后续解码器兼容。
    *   **z-预测流匹配（z-prediction Flow）**：在高维特征空间中，传统的速度预测（v-prediction）容易崩溃。作者改用直接预测“纯净目标”（z-prediction）的参数化方案，显著提高了信噪比。
    *   **两阶段潜流强制课程（Latent Flow-Forcing Curriculum）**：
        *   **第一阶段**：教师强制训练（Teacher-Forcing），建立基础向量场。
        *   **第二阶段**：轨迹一致性流强制，通过将模型自身生成的“部分去噪状态”与真值混合，逐步引入推理时的误差，缓解自回归推演中的曝光偏差。
    *   **联合解码**：将预测的未来特征与历史特征拼接，通过冻结的 VGGT 解码器一次性生成全序列的深度图、点云和相机位姿。

#### 3. 实验设计
*   **数据集/场景**：
    *   **KITTI & Cityscapes**：真实道路驾驶场景，用于评估深度预测。
    *   **TartanAir**：大型合成数据集，具有复杂的 6-DoF 相机运动，用于评估 3D 点云预测。
*   **Benchmark 与对比方法**：
    *   **2D 视频生成模型**：Cosmos (4B/12B), VISTA。
    *   **潜空间预测模型**：DINO-Foresight。
    *   **几何感知生成模型**：Aether, Gen3R, WVD。
*   **评估指标**：AbsRel（绝对相对误差）、$\delta_1$（深度准确率）、Chamfer Distance（倒角距离）、Accuracy、Completeness。

#### 4. 资源与算力
*   **硬件**：使用 **1 台 NVIDIA RTX Pro 6000 (96 GB)** GPU 进行训练。
*   **参数量**：可训练参数仅为 **0.43B**（相比之下，Cosmos 为 12B，Gen3R 为 9B），VGGT 主干网络（1.3B）保持冻结。
*   **训练细节**：采用 AdamW 优化器，学习率 $2 \times 10^{-4}$，Batch Size 在两阶段分别为 64 和 48。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流数据集上进行了广泛测试，涵盖了短、中、长期预测视野。
*   **消融实验**：针对 $z$-预测 vs $v$-预测、流强制课程学习（$\lambda$ 调度策略）、预测视野长度等关键组件做了详细的消融分析。
*   **客观性**：对比了从轻量级到超大规模（12B）的各类模型，并提供了推理速度（Latency）的量化对比，实验设计较为全面、公平。

#### 6. 主要结论与发现
*   **性能卓越**：在深度预测任务中，VGGT-World 将 AbsRel 降低了 21%-32%，显著优于最强的基线模型。
*   **高效性**：推理速度比 Cosmos 快 5 倍，比 Gen3R 快 3.6 倍。
*   **几何一致性**：通过预测几何特征而非像素，模型生成的点云结构更完整，避免了视频生成模型常见的“碎片化”问题。
*   **感知增强**：实验发现，预测未来有助于优化对“当前时刻”的几何感知（Completeness 提升），证明了预测能力的反馈作用。

#### 7. 优点（亮点）
*   **范式创新**：提出了“几何世界模型”概念，避开了计算昂贵的视频 VAE 训练。
*   **轻量且强大**：仅需少量可训练参数即可超越百亿级参数的通用视频模型。
*   **即插即用**：利用冻结的 GFM 特征，能够直接继承预训练模型的 3D 先验知识。
*   **稳定性高**：通过 $z$-预测和流强制课程学习，解决了高维潜空间自回归预测的稳定性难题。

#### 8. 不足与局限
*   **控制能力有限**：目前模型主要基于历史帧进行自回归推演，缺乏显式的动作（Action）或相机位姿（Pose）控制输入。
*   **数据覆盖**：虽然在驾驶和合成场景表现优异，但在更复杂的通用室内场景或动态交互场景下的泛化能力仍需进一步验证。
*   **依赖预训练模型**：模型的上限受限于冻结的 VGGT 特征质量，若 VGGT 本身在某些极端场景失效，预测也会随之受限。

（完）
