---
title: "Vega: Learning to Drive with Natural Language Instructions"
title_zh: Vega：利用自然语言指令学习驾驶
authors: "Sicheng Zuo, Yuxuan Li, Wenzhao Zheng, Zheng Zhu, Jie Zhou, Jiwen Lu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25741v2"
tags: ["query:robot"]
score: 10.0
evidence: 用于自动驾驶的统一视觉-语言-世界-动作模型
tldr: 本研究针对现有自动驾驶模型缺乏遵循多样化用户指令能力的局限，提出了Vega模型。首先构建了包含10万个场景的大规模指令驾驶数据集InstructScene。Vega采用统一的视觉-语言-世界-动作架构，利用自回归范式处理视觉和语言输入，并结合扩散模型生成未来预测和行驶轨迹。实验证明，Vega在规划性能和指令遵循方面表现优异，为实现智能化和个性化自动驾驶奠定了基础。
selection_source: fresh_fetch
motivation: 现有自动驾驶模型多将语言用于场景描述，缺乏遵循多样化用户指令以实现个性化驾驶的灵活性。
method: 构建了大规模指令数据集InstructScene，并提出结合自回归处理与扩散生成的统一模型Vega。
result: 实验结果显示Vega在规划性能上达到领先水平，并展现出极强的指令遵循能力。
conclusion: 该研究通过整合自然语言指令与多模态决策，为开发更智能、更个性化的自动驾驶系统开辟了新路径。
---

## 摘要
视觉-语言-动作（Vision-language-action）模型重塑了自动驾驶，将语言融入到决策过程中。然而，大多数现有流程仅利用语言模态进行场景描述或推理，缺乏遵循多样化用户指令以实现个性化驾驶的灵活性。为解决这一问题，我们首先构建了一个大规模驾驶数据集（InstructScene），包含约 100,000 个场景，并标注了多样化的驾驶指令及其对应的轨迹。随后，我们提出了一种统一的视觉-语言-世界-动作（Vision-Language-World-Action）模型 Vega，用于基于指令的生成与规划。我们采用自回归范式处理视觉输入（视觉）和语言指令（语言），并采用扩散范式生成未来预测（世界建模）和轨迹（动作）。我们通过联合注意力机制实现模态间的交互，并为不同模态使用独立的投影层以增强能力。大量实验表明，我们的方法不仅实现了卓越的规划性能，还展现出强大的指令遵循能力，为更智能、更个性化的驾驶系统铺平了道路。

## Abstract
Vision-language-action models have reshaped autonomous driving to incorporate languages into the decision-making process. However, most existing pipelines only utilize the language modality for scene descriptions or reasoning and lack the flexibility to follow diverse user instructions for personalized driving. To address this, we first construct a large-scale driving dataset (InstructScene) containing around 100,000 scenes annotated with diverse driving instructions with the corresponding trajectories. We then propose a unified Vision-Language-World-Action model, Vega, for instruction-based generation and planning. We employ the autoregressive paradigm to process visual inputs (vision) and language instructions (language) and the diffusion paradigm to generate future predictions (world modeling) and trajectories (action). We perform joint attention to enable interactions between the modalities and use individual projection layers for different modalities for more capabilities. Extensive experiments demonstrate that our method not only achieves superior planning performance but also exhibits strong instruction-following abilities, paving the way for more intelligent and personalized driving systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Vega**，一个旨在实现“指令驱动驾驶”的统一视觉-语言-世界-动作模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）驾驶模型大多仅将语言用于场景描述或逻辑推理，缺乏遵循多样化、个性化用户指令的能力（例如“超车以赶上绿灯”或“靠边停车”）。
*   **研究动机**：
    *   **从模仿到指令的转变**：传统模型倾向于模仿平均化的专家策略，无法处理开放式的自然语言指令。
    *   **监督信号稀疏**：高维视觉输入与低维动作输出之间存在巨大的信息鸿沟，导致模型难以学习复杂的映射关系。
*   **整体含义**：通过引入“世界模型”（未来图像预测）作为密集监督信号，Vega 实现了能够理解复杂指令并生成相应驾驶行为的智能体。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将驾驶任务建模为一个因果链：感知环境 $\rightarrow$ 接收指令 $\rightarrow$ 决定动作 $\rightarrow$ 观察结果。Vega 统一了自回归（AR）理解和扩散（Diffusion）生成两种范式。
*   **关键技术细节**：
    *   **InstructScene 数据集**：利用 Qwen2.5-VL 自动标注了约 10 万个场景，包含“过去观察+未来轨迹 $\rightarrow$ 驾驶指令”的配对数据。
    *   **混合架构（Integrated Transformer）**：
        *   **自回归部分**：用于处理视觉观察和文本指令的理解。
        *   **扩散部分**：用于生成未来图像（世界建模）和规划轨迹（动作预测）。
    *   **Mixture-of-Transformers (MoT)**：为不同模态（视觉、文本、动作）分配独立的 Transformer 参数层，既保持了全局因果注意力交互，又增强了各模态的处理能力。
    *   **密集监督**：通过预测未来图像，强制模型学习环境动力学，从而辅助动作规划。
    *   **训练目标**：联合优化动作预测损失（MSE）和未来图像生成损失（VAE Latent 上的 MSE）。

### 3. 实验设计
*   **数据集/场景**：
    *   **NAVSIM v1**：基于 OpenScene 的非反应式仿真基准。
    *   **NAVSIM v2**：包含反应式交通流的更真实仿真环境。
*   **Benchmark**：主要使用 PDMS（预测驾驶模型得分）和 EPDMS（扩展得分），涵盖碰撞率、道路合规性、舒适度等指标。
*   **对比方法**：包括 TransFuser、HydraMDP++、DiffusionDrive、DriveVLA-W0、AutoVLA 等 SOTA 自动驾驶模型。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   训练步数：200,000 步。
    *   学习率：2e-5，包含 2500 步预热。
    *   Batch Size：每张显卡为 1。
    *   模型规模：基于 Qwen2.5-7B 主干网络。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准（NAVSIM v1/v2）上进行了全面测试。
    *   **消融实验**：针对“未来帧预测的影响”、“图像-动作交织序列的长度”、“独立动作模块的必要性”以及“与纯 VLA 基准的对比”进行了 4 组核心消融实验。
    *   **定性分析**：展示了多组不同指令下模型生成不同轨迹和未来图像的可视化结果。
*   **充分性评价**：实验设计较为充分，不仅有量化指标对比，还通过可视化验证了模型在同一场景下遵循不同指令的灵活性，客观地证明了世界模型对规划的增益。

### 6. 主要结论与发现
*   **性能领先**：Vega 在 NAVSIM v2 上达到了 86.9 的 EPDMS，在使用 Best-of-N 策略时达到 89.4，超越了现有 SOTA 方法。
*   **世界模型的作用**：实验证明，加入未来图像预测任务能显著提升规划性能，因为图像提供了比动作标签更丰富的环境动力学信息。
*   **指令遵循能力**：Vega 能够根据“加速”、“减速”、“靠边”等不同指令生成物理可行且符合语义的轨迹和预测图像。

### 7. 优点
*   **统一性**：在一个 Transformer 框架内解决了理解、预测和规划问题，避免了模块化系统的累积误差。
*   **创新的 MoT 设计**：有效地解耦了不同模态的参数，解决了多模态联合训练时的干扰问题。
*   **数据集贡献**：开源了大规模指令驾驶数据集 InstructScene，填补了该领域的空白。

### 8. 不足与局限
*   **输入模态限制**：目前主要依赖单路前视摄像头，而许多 SOTA 模型使用了环视或激光雷达（LiDAR），这限制了其在复杂路口等场景的感知上限。
*   **计算开销**：基于 7B 规模的集成 Transformer 模型推理延迟可能较高，对于车载实时部署（Real-time deployment）具有挑战性。
*   **指标偏差**：论文提到 NAVSIM v1 的指标过于偏向保守策略，可能无法完全体现指令驱动带来的灵活性优势。

（完）
