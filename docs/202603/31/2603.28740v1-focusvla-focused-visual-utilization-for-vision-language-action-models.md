---
title: "FocusVLA: Focused Visual Utilization for Vision-Language-Action Models"
title_zh: FocusVLA：面向视觉-语言-动作模型的聚焦视觉利用
authors: "Yichi Zhang, Weihao Yuan, Yizhuo Zhang, Xidong Zhang, Jia Wan"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28740v1"
tags: ["query:robot"]
score: 9.0
evidence: 优化视觉-语言-动作模型中的视觉利用率
tldr: FocusVLA 针对视觉-语言-动作（VLA）模型中视觉信息利用不足及噪声干扰的问题，提出了聚焦视觉利用的新范式。通过引入模态级联注意力和聚焦注意力机制，该模型能够强制关注任务相关的视觉细节并抑制无关噪声。实验证明，FocusVLA 在模拟和真实机器人任务中显著提升了动作生成的质量、灵巧性及收敛速度。
selection_source: fresh_fetch
motivation: 现有 VLA 模型受架构偏置、视觉 Token 过多及任务无关噪声的影响，导致模型难以有效利用关键视觉细节来生成精确动作。
method: 提出了 FocusVLA 架构，包含用于消除快捷路径的模态级联注意力，以及用于动态筛选并增强任务相关视觉区域的聚焦注意力。
result: 在仿真和真实世界基准测试中，FocusVLA 显著提高了复杂操作任务的成功率，并大幅加快了模型的训练收敛。
conclusion: 研究表明 VLA 性能的提升关键在于如何有效利用视觉信息，而非仅仅依赖视觉表征质量的提高。
---

## 摘要
视觉-语言-动作（VLA）模型通过将策略建立在丰富的视觉-语言信息之上来改进动作生成。然而，当前的自回归策略受到三个瓶颈的限制：(1) 架构偏差导致模型忽视视觉细节，(2) 过多的视觉标记（tokens）使得注意力难以聚焦在正确的区域，以及 (3) 与任务无关的视觉信息引入了大量噪声——这些因素共同严重损害了动作的质量。在本文中，我们研究了如何有效地利用不同的视觉表示进行动作生成。为此，我们首先通过实验验证了上述问题，并表明 VLA 的性能主要受限于视觉信息的利用方式，而非视觉表示的质量。基于这些见解，我们提出了 FocusVLA，这是一种将模型注意力引导至任务相关视觉区域的新型范式，从而有效地连接视觉与动作。具体而言，我们首先提出了模态级联注意力（Modality Cascaded Attention）以消除捷径路径，从而迫使 VLA 模型依赖任务相关的视觉细节进行动作生成。此外，我们提出了聚焦注意力（Focus Attention），它动态选择任务相关的视觉补丁（patches）以控制信息量，同时显式调节其影响力以抑制任务无关的噪声。在模拟和真实世界机器人基准测试上的广泛实验表明，FocusVLA 不仅能有效利用视觉细节执行灵巧操作，还能在各种任务中显著提高性能并加速收敛。

## Abstract
Vision-Language-Action (VLA) models improve action generation by conditioning policies on rich vision-language information. However, current auto-regressive policies are constrained by three bottlenecks: (1) architectural bias drives models to overlook visual details, (2) an excessive number of visual tokens makes attention difficult to focus on the correct regions, and (3) task-irrelevant visual information introduces substantial noise - together severely impairing the quality of action. In this paper, we investigate how to effectively utilize different visual representations for action generation. To this end, we first empirically validate the above issues and show that VLA performance is primarily limited by how visual information is utilized, rather than by the quality of visual representations. Based on these insights, we introduce FocusVLA, a novel paradigm that directs the model's attention to task-relevant visual regions to effectively bridge vision to action. Specifically, we first propose Modality Cascaded Attention to eliminate shortcut pathways, thereby compelling VLA models to rely on task-relevant visual details for action generation. Furthermore, we propose Focus Attention, which dynamically selects task-relevant visual patches to control information quantity while explicitly modulating their influence to suppress task-irrelevant noise. Extensive experiments on both simulated and real-world robotic benchmarks demonstrate that FocusVLA not only effectively leverages visual details to perform dexterous manipulations, but also substantially improves performance and accelerates convergence across a variety of tasks.

---

## 论文详细总结（自动生成）

以下是对论文《FocusVLA: Focused Visual Utilization for Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自回归视觉-语言-动作（VLA）模型在处理需要精细感知和鲁棒操作的任务时表现不佳，存在明显的“视觉到动作”的鸿沟。
*   **研究动机**：作者发现 VLA 性能的瓶颈不在于视觉编码器的表征能力，而在于**视觉信息的利用效率**。具体存在三个瓶颈：
    1.  **架构偏置（Architectural Bias）**：混合注意力机制允许模型通过学习动作查询（Action Queries）的捷径来绕过具体的视觉细节。
    2.  **信息过载（Information Overload）**：过多的视觉 Token 分散了模型的注意力，使其难以聚焦关键操作区域。
    3.  **任务无关噪声（Task-Irrelevant Noise）**：环境背景信息引入了大量噪声，降低了信噪比，干扰了动作生成的准确性。

### 2. 方法论
FocusVLA 提出了一种引导模型关注任务相关视觉区域的新范式，核心技术包括：
*   **模态级联注意力（Modality Cascaded Attention）**：
    *   **核心思想**：取消传统的混合注意力，改为解耦的级联设计。
    *   **流程**：动作潜变量（Action Latent）依次通过自注意力、动作查询交叉注意力和视觉交叉注意力。这种顺序交互强制模型在进行动作推理前必须独立检索视觉信息，消除了架构捷径。
*   **聚焦注意力（Focus Attention）**：
    *   **补丁级聚焦（Patch-level Focus）**：利用交叉注意力分数进行 Top-K 筛选，剔除冗余和无关的视觉补丁，减少干扰。
    *   **通道级聚焦（Channel-level Focus）**：引入元素级门控机制（Element-wise Gate），动态抑制任务无关的特征通道，增强指令遵循能力。
*   **多视觉表征融合**：结合了 VLM 的语义特征、DINOv2+SigLIP 的空间特征以及 VGGT 的 3D 几何特征，通过级联结构实现互补。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程）四个任务套件，评估模型的泛化和长程推理能力。
    *   **RoboTwin 2.0**：一个大规模的双臂机器人仿真基准，包含 Easy（域内）和 Hard（域随机化）设置。
    *   **真实世界实验**：在 Realman 双臂机器人平台上进行，涉及抓取水果、叠杯子、放置方块等任务。
*   **对比方法**：包括 OpenVLA、OpenVLA-OFT、UniVLA、Spatial Forcing、VLA-Adapter-Pro、Pi0、Pi0.5、NORA-1.5、EVO-1、DP 等当前最先进的 VLA 和扩散策略模型。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **LIBERO 训练**：使用 4 台 NVIDIA A100 GPU。
    *   **RoboTwin 训练**：使用 8 台 NVIDIA A100 GPU。
*   **训练时长/步数**：
    *   LIBERO 单权重模型训练 100k 步；多权重模型根据任务不同训练 10k 至 80k 步。
    *   RoboTwin 训练 20k（Easy）至 100k（Hard）步。
*   **模型规模**：基于 Qwen2.5-0.5B，总可训练参数约为 3.427 亿（342.7M）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 LIBERO 的 40 个子任务、RoboTwin 的 7 类复杂操作任务以及真实世界测试。
*   **消融实验**：针对注意力模式（混合 vs 级联）、补丁筛选数量（128/256/384/512）、门控粒度（单参数/头级/元素级）以及视觉表征组合进行了系统对比。
*   **客观性**：通过初步的受控实验验证了三个瓶颈假设，确保了后续方法设计的针对性和科学性。实验涵盖了单任务和多任务设置，对比基准广泛，结果具有说服力。

### 6. 主要结论与发现
*   **利用胜过表征**：VLA 的性能主要受限于视觉信息的利用方式，而非视觉编码器的质量。即使是基础的视觉特征，只要利用得当，也能大幅提升性能。
*   **聚焦是关键**：通过级联注意力和 Token 剪枝实现的“聚焦”机制，不仅提升了操作精度（如在 RoboTwin 的精细操作任务中表现优异），还显著加快了训练收敛速度（在 LIBERO-Spatial 上实现 5 倍加速）。
*   **SOTA 性能**：FocusVLA 在 0.5B 参数规模下，性能超越了许多 7B 规模的模型（如 OpenVLA-OFT）。

### 7. 优点
*   **架构创新**：通过级联注意力从根本上解决了自回归 VLA 模型容易忽略视觉细节的结构性缺陷。
*   **效率与精度并重**：在减少视觉 Token 处理量的同时，通过精准筛选提升了动作生成的质量。
*   **训练高效**：相比基准模型 VLA-Adapter，实现了 1.5 倍至 5 倍的收敛加速。

### 8. 不足与局限
*   **VLM 内部利用**：目前的研究主要集中在策略网络（Policy）端对视觉信息的利用，尚未深入探讨 VLM 内部视觉处理的优化。
*   **规模化验证**：虽然在 0.5B 模型上效果显著，但尚未在大规模参数（如 7B 或更大）或更大规模的数据集上验证其扩展性。
*   **真实世界数据限制**：真实世界实验的成功率仍受限于训练数据的规模，且对极端环境变化（如背景颜色和纹理同时改变）的鲁棒性仍有提升空间。

（完）
