---
title: "HapticVLA: Contact-Rich Manipulation via Vision-Language-Action Model without Inference-Time Tactile Sensing"
title_zh: HapticVLA：无需推理时触觉传感的视觉-语言-动作模型实现丰富接触操作
authors: "Konstantin Gubernatorov, Mikhail Sannikov, Ilya Mikhalchuk, Egor Kuznetsov, Makar Artemov, Ogunwoye Faith Ouwatobi, Marcelino Fernando, Artem Asanov, Ziang Guo, Dzmitry Tsetserukou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15257v1"
tags: ["query:robot"]
score: 10.0
evidence: 无需推理时触觉感知的触觉感知视觉-语言-动作模型
tldr: "HapticVLA 提出了一种无需推理时触觉传感器的视觉-语言-动作模型，用于处理接触丰富的操作任务。该方法通过安全感知奖励加权流匹配（SA-RWFM）训练专家模型，并利用触觉蒸馏（TD）将触觉感知能力转移到常规 VLA 中。这使得机器人仅凭视觉和状态即可生成具备触觉意识的动作，在降低硬件成本的同时，在真实实验中取得了 86.7% 的成功率，优于带触觉反馈的基准模型。"
selection_source: fresh_fetch
motivation: 触觉传感器成本高且难以在不同机器人平台间复用，限制了具备触觉感知能力的 VLA 模型的广泛应用。
method: 采用两阶段法：先通过奖励加权流匹配训练具备触觉安全意识的专家，再将其触觉特征蒸馏至仅依赖视觉和状态输入的 VLA 模型中。
result: "在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，表现优于包括直接使用触觉反馈在内的多种基准模型。"
conclusion: 该研究证明了触觉感知能力可以离线学习并蒸馏至视觉模型中，从而在无需实时触觉硬件的情况下实现安全且精准的接触式操作。
---

## 摘要
触觉感知是视觉-语言-动作（VLA）架构的一项关键能力，因为它能在丰富接触的任务中实现灵巧且安全的操作。然而，对专用触觉硬件的依赖增加了成本，并降低了在不同机器人平台上的可复现性。我们认为，触觉感知操作可以离线学习，并在推理时无需直接触觉反馈即可部署。为此，我们提出了 HapticVLA，它分为两个紧密耦合的阶段：安全感知奖励加权流匹配（SA-RWFM）和触觉蒸馏（TD）。SA-RWFM 训练了一个流匹配动作专家，该专家结合了预计算的安全感知触觉奖励，对过大的抓取力和次优的抓取轨迹进行惩罚。TD 进一步将这种触觉感知能力转移到常规 VLA 中：我们从 SA-RWFM 教师模型中蒸馏出一个紧凑的触觉 token，并训练一个学生 VLA 从视觉和状态模态中预测该 token，从而在推理时无需车载触觉传感器即可生成触觉感知动作。这种设计在 VLA 中保留了丰富接触的触觉感知推理，同时在部署期间消除了对车载触觉传感器的需求。在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，始终优于基准 VLA，包括那些在推理期间提供直接触觉反馈的版本。

## Abstract
Tactile sensing is a crucial capability for Vision-Language-Action (VLA) architectures, as it enables dexterous and safe manipulation in contact-rich tasks. However, reliance on dedicated tactile hardware increases cost and reduces reproducibility across robotic platforms. We argue that tactile-aware manipulation can be learned offline and deployed without direct haptic feedback at inference. To this end, we present HapticVLA, which proceeds in two tightly coupled stages: Safety-Aware Reward-Weighted Flow Matching (SA-RWFM) and Tactile Distillation (TD). SA-RWFM trains a flow-matching action expert that incorporates precomputed, safety-aware tactile rewards penalizing excessive grasping force and suboptimal grasping trajectories. TD further transfers this tactile-aware capability into a conventional VLA: we distill a compact tactile token from the SA-RWFM teacher and train a student VLA to predict that token from vision and state modalities, enabling tactile-aware action generation at inference without requiring on-board tactile sensors. This design preserves contact-rich tactile-aware reasoning within VLA while removing the need for on-board tactile sensors during deployment. On real-world experiments, HapticVLA achieves a mean success rate of 86.7%, consistently outperforming baseline VLAs - including versions provided with direct tactile feedback during inference.

---

## 论文详细总结（自动生成）

### HapticVLA 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型主要依赖视觉感知，缺乏对力矩和触觉的理解，导致在处理易碎或柔软物体（如鸡蛋、塑料罐）时容易因用力过猛造成损坏。
*   **研究动机**：虽然引入触觉传感器能解决上述问题，但专用触觉硬件成本高昂、难以在不同机器人平台间复用。本文旨在研究：**能否在训练阶段利用触觉数据学习安全操作逻辑，但在推理部署阶段完全不依赖触觉传感器？**

#### 2. 方法论：核心思想与关键技术
HapticVLA 采用两阶段训练框架，将触觉知识从“专家”转移到“通用模型”：
*   **安全感知奖励加权流匹配（SA-RWFM）**：
    *   **触觉奖励计算**：基于触觉地图（Tactile Maps）计算平均力、峰值压力、压力集中度、抓取不对称性和滑动情况。定义了安全奖励函数，对过大压力、抓取不稳或物体滑动进行惩罚。
    *   **专家训练**：在离线数据集上，利用计算出的奖励值对训练样本进行加权。高奖励（操作安全且成功）的样本在流匹配（Flow Matching）损失函数中权重更高，从而训练出一个具备触觉安全意识的专家模型。
*   **触觉蒸馏（TD）**：
    *   **离线目标生成**：使用训练好的专家模型（输入包含视觉+状态+触觉）对全量数据集生成预测动作。
    *   **学生模型训练**：训练一个仅输入视觉和关节状态的学生 VLA。其训练目标是“混合目标”：50% 来源于人类演示的真实轨迹（Ground Truth），50% 来源于专家模型的预测输出。
    *   **架构对齐**：通过修改状态投影层，使学生模型在丢失触觉输入维度的情况下，仍能继承专家模型在潜在空间中的推理能力。

#### 3. 实验设计
*   **实验场景**：
    1.  **果酱罐取放**：处理可变形塑料罐。
    2.  **华夫饼取放**：处理极易碎的食品。
    3.  **鸡蛋取放**：极高精度的力控任务，需打开蛋盒并移动鸡蛋。
*   **硬件平台**：双臂 LeRobot SO-101 机器人，右臂配备高密度触觉阵列（单指 100 个触点）。
*   **数据集**：310 个真实世界序列（包含成功和故意失败的样本）+ 1000 个数字孪生（Isaac Sim）仿真序列。
*   **基准模型（Benchmarks）**：
    *   **SmolVLA (0.45B)**：基础模型。
    *   **X-VLA (0.9B)**：跨具身适配模型。
    *   **VLA-0**：在同类硬件上表现优异的大型 VLA。
    *   **消融实验**：对比了有/无触觉蒸馏、有/无异步推理等配置。

#### 4. 资源与算力
*   **推理设备**：NVIDIA Jetson Orin NX (16GB) 边缘计算电脑。
*   **训练细节**：论文提到使用了微调（Fine-tuning）策略，但**未明确说明**具体使用的训练 GPU 型号（如 A100/H100）、训练总时长或具体的算力消耗总量。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务对每个模型进行了 20 次独立测试，总计进行了数百次真实世界评估。
*   **充分性**：
    *   包含了针对触觉蒸馏模块的消融实验，验证了该模块对性能提升的贡献。
    *   对比了多种 SOTA（当前最佳）的小型 VLA 模型，证明了在特定接触任务上的优越性。
*   **客观性**：实验标准明确（如鸡蛋是否有裂纹、罐子是否永久变形），并使用了失败样本进行负反馈训练，实验设计较为严谨。

#### 6. 主要结论与发现
*   **性能卓越**：HapticVLA 在三项任务中达到了 **86.7%** 的平均成功率，远超基础 SmolVLA（约 40%）和其他基准模型（X-VLA 和 VLA-0 在此类高精度任务中几乎无法成功）。
*   **蒸馏的有效性**：通过蒸馏得到的“无传感器”模型，其表现甚至优于某些在推理时直接输入原始触觉信号的模型，说明模型成功内化了触觉推理逻辑。
*   **安全意识**：SA-RWFM 显著提升了模型对易碎物体的保护能力，尤其在鸡蛋取放任务中提升了 45% 的成功率。

#### 7. 优点与亮点
*   **硬件解耦**：最大的亮点在于“训练时有感，推理时无感”，极大降低了触觉感知机器人的部署门槛和成本。
*   **安全导向**：将触觉安全奖励引入流匹配框架，解决了传统 VLA 仅模仿动作而忽略物理交互力的问题。
*   **端到端部署**：模型轻量化，可在 Jetson Orin 等边缘设备上实时运行。

#### 8. 不足与局限
*   **数据规模较小**：真实世界数据仅 310 条，虽然有仿真数据补充，但对于复杂环境的泛化能力仍有待在大规模数据集上验证。
*   **任务局限性**：实验集中在取放任务，尚未验证在更复杂的接触任务（如擦拭、装配、工具使用）中的表现。
*   **异步推理偏差**：消融实验显示，异步推理模式下性能会有所下降，说明时间对齐和延迟对触觉敏感型任务仍是挑战。

（完）
