---
title: "vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models"
title_zh: vla-eval：视觉-语言-动作模型的统一评估框架
authors: "Suhwan Choi, Yunsung Lee, Yubeen Park, Chris Dongjoo Kim, Ranjay Krishna, Dieter Fox, Youngjae Yu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13966v1"
tags: ["query:robot"]
score: 8.0
evidence: 机器人视觉-语言-动作模型的统一评估框架
tldr: 针对视觉-语言-动作（VLA）模型评估中存在的脚本碎片化、依赖冲突和协议不统一等问题，本文推出了开源评估框架 vla-eval。该框架通过 WebSocket 协议和 Docker 隔离技术实现了模型推理与基准测试环境的解耦，支持 13 个基准测试和 6 种模型服务器。通过并行化技术，评估效率提升了 47 倍，并揭示了现有模型评估中隐藏的非标准化细节，为 VLA 领域提供了统一且高效的评估标准。
selection_source: fresh_fetch
motivation: 解决 VLA 模型评估中因各基准测试脚本独立维护导致的重复代码、依赖冲突及协议不规范问题。
method: 采用基于 WebSocket 和 Docker 的解耦架构，通过统一的接口实现模型推理与基准测试环境的隔离与交互。
result: 实现了 47 倍的评估吞吐量提升，并成功复现了已发表模型的性能，同时发现了影响结果的未记录参数和歧义语义。
conclusion: vla-eval 提供了一个高效、可复现且易于扩展的统一评估平台，有助于推动 VLA 模型的标准化研究。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用由各模型仓库独立维护的特定基准测试脚本进行评估，这导致了代码重复、依赖冲突以及协议规范不明确等问题。我们提出了 vla-eval，这是一个开源评估框架，通过基于 Docker 的环境隔离和 WebSocket msgpack 协议，实现了模型推理与基准测试执行的解耦。模型只需实现单个 predict() 方法即可完成集成；基准测试则通过四方法接口进行集成；完整的交叉评估矩阵可自动运行。完成一次完整的评估仅需两条命令：vla eval serve 和 vla eval run。该框架支持 13 个仿真基准测试和 6 个模型服务器。通过回合分片（episode sharding）和批处理推理实现的并行评估使吞吐量提升了 47 倍，在大约 18 分钟内即可完成 2000 个 LIBERO 回合。利用该基础设施，我们对一个已发表的 VLA 模型在三个基准测试上进行了可复现性审计，发现这三个基准测试均能紧密复现已发表的数值，同时也揭示了未记录的需求、模糊的终止语义以及可能在无形中扭曲结果的隐藏归一化统计数据。此外，我们还发布了一个 VLA 排行榜，汇总了 17 个基准测试中的 657 项已发表结果。框架、评估配置及所有复现结果均已公开。

## Abstract
Vision Language Action VLA models are typically evaluated using per benchmark scripts maintained independently by each model repository, leading to duplicated code, dependency conflicts, and underspecified protocols. We present vla eval, an open source evaluation harness that decouples model inference from benchmark execution through a WebSocket msgpack protocol with Docker based environment isolation. Models integrate once by implementing a single predict() method; benchmarks integrate once via a four method interface; the full cross evaluation matrix works automatically. A complete evaluation requires only two commands: vla eval serve and vla eval run. The framework supports 13 simulation benchmarks and six model servers. Parallel evaluation via episode sharding and batch inference achieves a 47x throughput improvement, completing 2000 LIBERO episodes in about 18 minutes. Using this infrastructure, we conduct a reproducibility audit of a published VLA model across three benchmarks, finding that all three closely reproduce published values while uncovering undocumented requirements ambiguous termination semantics and hidden normalization statistics that can silently distort results. We additionally release a VLA leaderboard aggregating 657 published results across 17 benchmarks. Framework, evaluation configs, and all reproduction results are publicly available.

---

## 论文详细总结（自动生成）

### vla-eval：视觉-语言-动作（VLA）模型的统一评估框架论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着视觉-语言-动作（VLA）模型的快速发展，机器人操作的仿真基准测试（Benchmarks）日益增多，但评估生态系统却高度碎片化。主要问题包括：
*   **重复劳动**：每个模型仓库都独立实现基准测试脚本，导致代码冗余且易随上游接口变化而失效。
*   **依赖冲突**：不同基准测试（如 LIBERO、ManiSkill2、CALVIN）对 Python 版本和物理引擎（robosuite, SAPIEN, PyBullet）的要求各异，难以在单一环境中并存。
*   **协议不规范**：论文常忽略随机种子、回合数、归一化统计数据等关键细节，导致结果难以复现。
*   **评估缓慢**：串行评估数千个回合耗时极长（如 LIBERO 需 14 小时），限制了研究效率。
**研究意义**：本文旨在建立一个类似语言模型领域 `lm-evaluation-harness` 的统一框架，实现模型与基准测试的解耦，提高评估的可复现性和效率。

#### 2. 论文提出的方法论
核心思想是**解耦模型推理与基准测试执行**，采用客户端-服务器架构：
*   **通信协议**：使用 WebSocket 配合 msgpack 二进制序列化进行数据交换，传输观测值（obs）、动作（action）及状态信息。
*   **环境隔离**：
    *   **基准测试端**：每个基准测试运行在独立的 Docker 容器中，预装所有依赖和资产（场景、纹理、机器人描述）。
    *   **模型服务端**：模型通过实现一个简单的 `predict()` 方法进行集成，利用 `uv run` 或 Docker 实现依赖隔离。
*   **并行化技术**：
    *   **回合分片（Episode Sharding）**：将评估任务分配到 $N$ 个并行的 Docker 容器中。
    *   **批处理推理（Batch Inference）**：模型服务器同时处理来自多个环境的请求。
*   **自动化流程**：通过 YAML 配置文件驱动，仅需 `vla-eval serve` 和 `vla-eval run` 两条命令即可完成交叉评估。

#### 3. 实验设计
*   **支持范围**：集成了 13 个主流仿真基准测试（包括 LIBERO, CALVIN, SimplerEnv, ManiSkill2 等）和 6 种先进 VLA 模型（OpenVLA, CogACT, $\pi_0$, GR00T N1 等）。
*   **可复现性审计**：使用 DB-CogACT 模型在三个关键基准测试上进行验证：
    *   **LIBERO**：4 个套件，共 2000 个回合。
    *   **CALVIN**：ABC $\rightarrow$ D 任务，1000 个链式序列。
    *   **SimplerEnv**：WidowX 任务，288 个回合（3 个种子）。
*   **对比基准**：将复现结果与已发表的论文原始数值进行对比。
*   **排行榜构建**：通过 AI 智能体（Claude Code）和人工审核，从 2685 篇论文中提取并标准化了 657 项结果，涵盖 17 个基准测试。

#### 4. 资源与算力
*   **硬件使用**：模型服务器运行在 **NVIDIA H100 GPU** 上。
*   **并行规模**：在 LIBERO 评估中使用了 50 个分片（Docker 容器），批处理大小 $B=16$。
*   **效率提升**：
    *   **LIBERO**：从 14 小时缩短至 **18 分钟**（47 倍加速）。
    *   **CALVIN**：从 8.6 小时缩短至 33 分钟（16 倍加速）。
    *   **SimplerEnv**：从 1.7 小时缩短至 8.5 分钟（12 倍加速）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了数千个回合的仿真实验，覆盖了从 6D 到 14D 的不同动作空间。
*   **充分性**：实验不仅验证了框架的性能提升，还深入探讨了导致复现偏差的底层原因（如终止语义、归一化参数）。
*   **客观性**：通过固定种子和版本化的 Docker 镜像确保了实验的公平性。排行榜的构建结合了 AI 提取与人工校验，并公开了所有协议细节。

#### 6. 论文的主要结论与发现
*   **复现性挑战**：即使是知名基准测试也存在未记录的细节。例如，SimplerEnv 的终止标志位存在语义歧义，CALVIN 依赖隐藏的训练集归一化统计数据。
*   **评估现状**：排行榜分析显示，**81% 的模型仅在单一基准测试上进行评估**，只有 0.6% 的模型评估超过 5 个基准测试，这限制了对模型通用能力的评估。
*   **性能一致性**：通过 `vla-eval` 复现的结果与原始论文数值误差基本在 $\pm 3\%$ 以内，证明了框架的可靠性。

#### 7. 优点
*   **极高效率**：通过并行化显著降低了 VLA 模型的评估门槛。
*   **标准化**：解决了长期困扰社区的依赖冲突和资产安装难题。
*   **透明度**：保存完整的评估配置（Docker 标签、种子、参数），确保“一键复现”。
*   **社区贡献**：提供了目前最全面的 VLA 性能排行榜和统一接口。

#### 8. 不足与局限
*   **场景局限**：目前仅支持仿真环境，未涵盖真实机器人评估。
*   **指标单一**：主要关注任务成功率，缺乏对运动质量、效率和安全性的深入度量。
*   **排行榜偏差**：排行榜数据源于论文提取而非独立重测，可能存在原始论文报告偏差。
*   **审计范围**：可复现性审计仅针对单一模型（DB-CogACT），未来需扩展到更多模型。

（完）
