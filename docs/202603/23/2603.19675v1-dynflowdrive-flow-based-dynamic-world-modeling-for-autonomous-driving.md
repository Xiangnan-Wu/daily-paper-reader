---
title: "DynFlowDrive: Flow-Based Dynamic World Modeling for Autonomous Driving"
title_zh: DynFlowDrive：用于自动驾驶的基于流的动态世界建模
authors: "Xiaolu Liu, Yicong Li, Song Wang, Junbo Chen, Angela Yao, Jianke Zhu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19675v1"
tags: ["query:robot"]
score: 10.0
evidence: 基于流的动态世界模型，用于自动驾驶和规划可靠性
tldr: 本研究针对自动驾驶中现有世界模型难以准确捕捉受轨迹约束的场景演化问题，提出了 DynFlowDrive。该模型利用修正流（Rectified Flow）公式构建流动力学潜空间世界模型，通过学习速度场来描述不同驾驶动作下的场景状态迁移。此外，引入了稳定性感知多模式轨迹选择策略。实验表明，该方法在 nuScenes 和 NavSim 基准上显著提升了规划可靠性，且不增加额外推理开销。
selection_source: fresh_fetch
motivation: 现有自动驾驶世界模型在捕捉受轨迹约束的场景演化方面存在局限，导致规划不可靠。
method: 提出基于流动力学的潜空间世界模型，利用修正流学习不同驾驶动作下的场景状态演化速度场。
result: 在 nuScenes 和 NavSim 基准测试中，该方法在不增加推理开销的情况下显著提升了多种驾驶框架的性能。
conclusion: DynFlowDrive 通过流动力学建模和稳定性感知轨迹选择，有效增强了自动驾驶系统的场景预测与规划能力。
---

## 摘要
最近，世界模型已被引入自动驾驶系统以提高规划的可靠性。现有方法通常通过外观生成或确定性回归来预测未来状态，这限制了它们捕捉受轨迹约束的场景演变的能力，并导致动作规划不可靠。为解决这一问题，我们提出了 DynFlowDrive，这是一种潜空间世界模型，利用基于流的动力学来建模不同驾驶动作下的世界状态转换。通过采用修正流（rectified flow）公式，该模型学习了一个描述场景状态在不同驾驶动作下如何变化的速度场，从而实现了对未来潜状态的渐进式预测。在此基础上，我们进一步引入了一种稳定性感知的多模态轨迹选择策略，该策略根据所引发的场景转换的稳定性来评估候选轨迹。在 nuScenes 和 NavSim 基准测试上的大量实验表明，该方法在不同的驾驶框架中均能实现一致的改进，且不引入额外的推理开销。源代码将在 https://github.com/xiaolul2/DynFlowDrive 公布。

## Abstract
Recently, world models have been incorporated into the autonomous driving systems to improve the planning reliability. Existing approaches typically predict future states through appearance generation or deterministic regression, which limits their ability to capture trajectory-conditioned scene evolution and leads to unreliable action planning. To address this, we propose DynFlowDrive, a latent world model that leverages flow-based dynamics to model the transition of world states under different driving actions. By adopting the rectifiedflow formulation, the model learns a velocity field that describes how the scene state changes under different driving actions, enabling progressive prediction of future latent states. Building upon this, we further introduce a stability-aware multi-mode trajectory selection strategy that evaluates candidate trajectories according to the stability of the induced scene transitions. Extensive experiments on the nuScenes and NavSim benchmarks demonstrate consistent improvements across diverse driving frameworks without introducing additional inference overhead. Source code will be abaliable at https://github.com/xiaolul2/DynFlowDrive.

---

## 论文详细总结（自动生成）

以下是对论文《DynFlowDrive: Flow-Based Dynamic World Modeling for Autonomous Driving》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的自动驾驶世界模型主要分为两类：**显式生成式**（生成像素或占据网格）和**隐空间回归式**。前者过度关注视觉细节（纹理、光照）而忽视了动力学，计算开销大；后者通常采用“单步回归”，将状态转移视为静态映射，无法捕捉场景随动作连续演化的动态过程。
*   **研究动机**：作者认为，有效的世界模型应将受轨迹约束的场景演化视为一个**连续动力系统**。例如，平滑减速和紧急刹车虽然最终位置可能相似，但其场景演化过程和安全性完全不同。因此，需要一种能建模连续转换过程的动态世界模型。

### 2. 方法论
*   **核心思想**：提出 **DynFlowDrive**，利用**修正流（Rectified Flow）**在隐空间内建模受动作约束的状态转移，通过学习“速度场”来描述场景状态随驾驶动作的变化。
*   **关键技术细节**：
    *   **轨迹规划范式**：采用标准的端到端架构，通过场景编码器提取特征，利用多模态轨迹解码器生成多个候选轨迹及其置信度得分。
    *   **流动力学隐空间世界模型**：
        *   使用预训练的 VAE 编码器提取稳定的隐空间特征。
        *   采用修正流公式，在当前帧隐状态和下一帧目标隐状态之间构建线性插值路径。
        *   训练一个基于 Transformer 的流模型 $F_\theta$，学习预测速度场 $v_\theta$，该速度场受当前场景和候选轨迹的共同约束。
    *   **稳定性感知多模态选择（Stability-aware Selection）**：
        *   在训练阶段，利用流模型模拟轨迹诱导的隐空间演化。
        *   通过计算演化过程中速度矢量的方向一致性（角度偏差）来衡量“稳定性”。
        *   结合轨迹误差、重构误差和动力学稳定性指标，选择最优轨迹模式来监督评分头。

### 3. 实验设计
*   **数据集与场景**：
    *   **nuScenes**：用于开环评估，包含 1000 个驾驶场景。
    *   **NavSim**：用于闭环评估，包含 103K 关键帧，提供更真实的模拟环境。
*   **Benchmark 与指标**：
    *   **nuScenes**：L2 位移误差（1s, 2s, 3s 平均值）和碰撞率（CR）。
    *   **NavSim**：PDM 得分（PDMS），综合考虑了无碰撞、可行驶区域合规性、舒适度等指标。
*   **对比方法**：包括感知类方法（UniAD, VAD, SparseDrive 等）和隐空间世界模型方法（LAW, SSR, WoTE, World4Drive 等）。

### 4. 资源与算力
*   **nuScenes 训练**：使用 **8 张 NVIDIA RTX 4090 GPU**，训练 12 个 epoch，每张 GPU 的 batch size 为 1。
*   **NavSim 训练**：训练 30 个 epoch，每张 GPU 的 batch size 为 16。
*   **推理效率**：文中强调，由于世界模型仅在训练阶段用于辅助轨迹选择和特征增强，**推理阶段不参与计算**，因此不会引入额外的推理开销（FPS 保持在 13.6-13.8 左右）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准（开环与闭环）上进行了全面测试。
*   **消融实验**：
    *   验证了动态世界模型（Dyn-WM）相对于静态模型（Static-WM）的优越性。
    *   测试了不同流积分步数（1, 3, 5, 10 步）对性能的影响。
    *   对比了不同的轨迹选择准则（仅 L2、增加重构、增加流稳定性）。
*   **充分性评价**：实验设计较为充分，涵盖了从组件有效性到长时预测准确性的多维度验证，对比对象包含了近年来的 SOTA 模型，结果具有客观性。

### 6. 主要结论与发现
*   **动态建模的必要性**：相比于静态回归，基于流的动态建模能显著降低长时（3s）预测误差和碰撞率。
*   **稳定性指标的价值**：引入流动力学稳定性作为轨迹选择标准，能有效筛选出物理上更合理、演化更平滑的驾驶动作。
*   **性能提升**：在 nuScenes 上，相比基准 SSR，L2 误差从 0.39m 降至 0.31m；在 NavSim 上达到了 88.7 的 PDMS 高分。

### 7. 优点
*   **连续性建模**：通过修正流将离散的状态预测转变为连续的速度场学习，更符合物理世界规律。
*   **零推理成本**：巧妙地将复杂的世界模型计算留在训练阶段，通过改进轨迹评分头来提升推理时的决策质量。
*   **通用性强**：可以无缝集成到现有的多种端到端自动驾驶框架（如基于 BEV 或非 BEV 的模型）中。

### 8. 不足与局限
*   **依赖预训练模型**：隐空间特征高度依赖于预训练的 VAE，若 VAE 无法捕捉某些关键语义，流模型的效果会受限。
*   **数值积分误差**：虽然流模型理论上是连续的，但在训练和评估稳定性时仍需离散化（如 Euler 积分），步数选择会影响数值稳定性。
*   **场景复杂度限制**：目前主要在标准数据集上验证，对于极端罕见的边缘案例（Corner Cases）或极高动态的复杂交互场景，流模型的泛化能力仍有待进一步观察。

（完）
