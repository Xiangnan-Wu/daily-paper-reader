---
title: "X-World: Controllable Ego-Centric Multi-Camera World Models for Scalable End-to-End Driving"
title_zh: X-World：面向可扩展端到端驾驶的可控自我中心多摄像头世界模型
authors: "Chaoda Zheng, Sean Li, Jinhao Deng, Zhennan Wang, Shijia Chen, Liqiang Xiao, Ziheng Chi, Hongbin Lin, Kangjie Chen, Boyang Wang, Yu Zhang, Xianming Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19979v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于自动驾驶的动作条件生成式世界模型
tldr: X-World是一个受动作驱动的多摄像头生成式世界模型，旨在解决自动驾驶端到端评估中实车测试成本高、场景覆盖有限的问题。它能根据历史视频和未来动作序列生成具有跨视角几何一致性和时间连贯性的多视角视频，并支持对交通参与者、道路元素及天气等外观的精细控制，为可扩展且可重复的自动驾驶评估提供了仿真基础。
selection_source: fresh_fetch
motivation: 现有的自动驾驶评估依赖高成本且难以复现的实车测试，亟需一种能够根据动作生成真实、可控且长时稳定的未来观测的模拟器。
method: 提出一种多视角潜空间视频生成器，通过动作序列、场景元素和文本提示词作为控制信号，显式增强跨视角几何一致性与时间连贯性。
result: 实验证明 X-World 在多视角生成质量、视角一致性、长时稳定性以及对动作和场景控制的遵循度方面均表现优异。
conclusion: X-World 为端到端自动驾驶提供了一个高保真、可控且可扩展的仿真平台，支持视频风格迁移和多样化的场景评测。
---

## 摘要
在自动驾驶的端到端时代，视觉-语言-动作（VLA）策略直接将原始传感器流映射为驾驶动作，可扩展且可靠的评估变得日益关键。然而，目前的评估流程仍然严重依赖真实世界的道路测试，这不仅成本高昂，且存在场景覆盖范围有限的偏差，且难以复现。这些挑战促使人们开发一种真实世界模拟器，它能够根据给定的动作生成逼真的未来观测结果，同时在长时程内保持可控性和稳定性。我们提出了 X-World，这是一种以动作为条件的、多摄像头生成式世界模型，可直接在视频空间中模拟未来观测。给定同步的多视图摄像头历史记录和未来的动作序列，X-World 会生成遵循指令动作的未来多摄像头视频流。为了确保场景推演的可复现性和可编辑性，X-World 进一步支持对动态交通参与者和静态道路元素的可选控制，并保留了用于外观级控制（如天气和时间）的文本提示接口。除了世界模拟之外，X-World 还能通过以外观提示为条件，在保留底层动作和场景动力学的同时实现视频风格迁移。X-World 的核心是一个多视图潜空间视频生成器，旨在显式地促进多种控制信号下的跨视图几何一致性和时间连贯性。实验表明，X-World 实现了高质量的多视图视频生成，具有：(i) 跨摄像头的强视图一致性，(ii) 长时程推演中稳定的时间动力学，以及 (iii) 具有严格动作遵循和对可选场景控制忠实遵循的高可控性。这些特性使 X-World 成为可扩展且可复现评估的实用基础。

## Abstract
Scalable and reliable evaluation is increasingly critical in the end-to-end era of autonomous driving, where vision--language--action (VLA) policies directly map raw sensor streams to driving actions. Yet, current evaluation pipelines still rely heavily on real-world road testing, which is costly, biased toward limited scenario coverage, and difficult to reproduce. These challenges motivate a real-world simulator that can generate realistic future observations under proposed actions, while remaining controllable and stable over long horizons. We present X-World, an action-conditioned multi-camera generative world model that simulates future observations directly in video space. Given synchronized multi-view camera history and a future action sequence, X-World generates future multi-camera video streams that follow the commanded actions. To ensure reproducible and editable scene rollouts, X-World further supports optional controls over dynamic traffic agents and static road elements, and retains a text-prompt interface for appearance-level control (e.g., weather and time of day). Beyond world simulation, X-World also enables video style transfer by conditioning on appearance prompts while preserving the underlying action and scene dynamics. At the core of X-World is a multi-view latent video generator designed to explicitly encourage cross-view geometric consistency and temporal coherence under diverse control signals. Experiments show that X-World achieves high-quality multi-view video generation with (i) strong view consistency across cameras, (ii) stable temporal dynamics over long rollouts, and (iii) high controllability with strict action following and faithful adherence to optional scene controls. These properties make X-World a practical foundation for scalable and reproducible evaluation.

---

## 论文详细总结（自动生成）

### X-World: 面向可扩展端到端驾驶的可控自我中心多摄像头世界模型论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：端到端自动驾驶（尤其是视觉-语言-动作 VLA 模型）的快速发展受限于评估手段。传统的模块化测试（如检测 AP、预测 ADE）无法衡量端到端系统的闭环表现，而实车路测成本高、场景覆盖不全且难以复现。
*   **研究动机**：需要一个能够根据驾驶动作生成真实、稳定、长时程且多视角一致的“真实世界模拟器”。该模拟器应支持交互式推演，允许对交通参与者和环境进行精细控制，从而为端到端策略提供可扩展的评估和在线强化学习（RL）训练环境。

#### 2. 方法论：核心思想与技术细节
X-World 基于视频扩散模型（Video Diffusion），构建了一个动作条件的潜空间视频生成器。
*   **核心架构**：
    *   **基础模型**：基于 WAN 2.2 (5B 参数) 的 DiT（Diffusion Transformer）架构。
    *   **高效压缩**：采用 3D 因果变分自编码器（VAE），实现 16× 空间压缩和 4× 时间压缩，显著降低计算开销。
    *   **视角-时间自注意力**：在多摄像头和多时间步的 Token 之间交替执行自注意力，确保跨视角的几何一致性和时间连贯性。
*   **多模态控制信号**：
    *   **自我动作（Ego-Action）**：输入速度、曲率、俯仰角等运动学序列，通过 Fourier 特征编码注入。
    *   **动态/静态元素**：通过检测模型提取目标框和车道线，利用 umT5 编码语义，通过解耦的交叉注意力（Cross-Attention）注入。
    *   **外观控制**：保留文本提示接口，用于控制天气、时间、地域风格。
*   **两阶段训练流程**：
    *   **阶段 I（双向训练）**：在 81 帧的短片段上进行双向生成训练，建立准确的可控性基础。
    *   **阶段 II（因果/少步训练）**：将模型转换为块状因果（Chunk-wise Causal）结构，采用自强制（Self-forcing）和分布匹配蒸馏（DMD）技术，实现 4 步快速去噪和长时程（Rolling KV Cache）稳定推演。

#### 3. 实验设计
*   **数据集**：使用小鹏汽车（XPeng）大规模真实驾驶数据，包含 7 路同步摄像头（覆盖 360 度）、12 FPS、10 秒片段。
*   **标注体系**：构建了自动标注流水线，包括 50 种环境标签、24 种静态标签、5 种动态目标标签及 21 种驾驶行为标签。
*   **评估场景**：
    *   **动作遵循**：测试左转、右转、换道等动作下的视频生成准确性。
    *   **场景编辑**：在固定背景下插入行人/骑行者，或修改道路拓扑。
    *   **风格迁移**：改变地域（如中国 vs 欧洲）或天气（晴天 vs 雨天）。
*   **对比基准**：主要展示了模型在长达 24 秒推演中的稳定性，以及与真实 Log 数据的对比。

#### 4. 资源与算力
*   **算力说明**：文中提到投入了“巨大的计算资源”（Substantial computational resources）用于全量数据的标注和模型迭代训练。
*   **具体细节**：**未明确说明**具体的 GPU 型号（如 H100/A100）、集群规模或具体的训练时长。考虑到 5B 参数规模及多视角视频生成任务，推测其算力需求极高。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了多组定性实验结果，涵盖了 24 秒长序列生成、多视角一致性对比、反事实（Counterfactual）动作推演以及复杂的场景编辑（如遮挡物后突然出现的骑行者）。
*   **充分性评价**：实验设计较为全面，涵盖了从底层生成质量到高层闭环评估的应用。通过消融实验（隐含在两阶段训练设计中）验证了因果生成对长时程稳定的必要性。
*   **客观性**：展示了模型在处理长尾场景（如极端天气、海外路标风格迁移）的能力，具有较强的工业应用说服力。

#### 6. 主要结论与发现
*   **高保真模拟**：X-World 能够生成物理上合理、视觉上逼真的多视角视频，且严格遵循驾驶动作指令。
*   **长时程稳定**：通过阶段 II 的因果训练和 Rolling KV Cache，模型在 24 秒以上的推演中不会出现严重的漂移或崩溃。
*   **闭环价值**：该模型可作为 VLA 2.0 策略的评估引擎，支持在虚拟环境中进行“反事实”测试（即如果当时没刹车会怎样），这是传统 Log-replay 无法实现的。

#### 7. 优点
*   **跨视角一致性强**：通过专门设计的注意力机制解决了多摄像头之间物体身份和几何位置错位的问题。
*   **流式交互设计**：块状因果生成支持低延迟的实时交互，使其能真正用于强化学习的在线训练。
*   **精细化控制**：不仅能控车，还能控人、控路、控天气，提供了极高的场景编辑自由度。

#### 8. 不足与局限
*   **感知依赖**：训练过程高度依赖高精度的感知模型来提供动态/静态元素的标签，感知误差可能会传播至生成模型。
*   **计算成本**：作为 5B 规模的扩散模型，尽管经过了少步蒸馏，但在实时闭环仿真中可能仍需极高的推理算力支持。
*   **偏差风险**：虽然支持风格迁移，但在完全未见过的极端地域或极端天气下，生成的真实性仍可能受限于训练数据的分布。

（完）
