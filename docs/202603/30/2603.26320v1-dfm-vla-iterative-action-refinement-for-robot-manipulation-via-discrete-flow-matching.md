---
title: "DFM-VLA: Iterative Action Refinement for Robot Manipulation via Discrete Flow Matching"
title_zh: DFM-VLA：通过离散流匹配实现机器人操纵的迭代动作细化
authors: "Jiayi Chen, Wenxuan Song, Shuai Chen, Jingbo Wang, Zhijun Li, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26320v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于机器人操作的离散流匹配VLA
tldr: 针对现有视觉-语言-动作（VLA）模型在离散动作生成中无法修正早期错误的问题，本文提出了DFM-VLA。该模型利用离散流匹配技术，通过建模动作序列的概率速度场实现迭代优化。结合两阶段解码策略，DFM-VLA在CALVIN和LIBERO等基准测试中显著优于自回归和扩散模型，在保持高推理效率的同时，显著提升了机器人复杂操作任务的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的离散VLA模型在生成动作Token后无法进行后续修正，导致早期生成的错误会持续影响最终的操作表现。
method: 引入离散流匹配技术来建模动作Token的概率速度场，并结合迭代细化与确定性验证的两阶段解码策略进行动作生成。
result: "在CALVIN和LIBERO基准测试中，DFM-VLA分别取得了4.44的平均成功长度和95.7%的成功率，性能显著优于自回归及扩散模型基准。"
conclusion: 通过离散流匹配实现的动作迭代细化机制，能有效解决离散VLA模型的生成瓶颈，是提升机器人操作性能的有效方案。
---

## 摘要
使用离散标记化方案编码动作的视觉-语言-动作（VLA）模型在机器人操纵中被日益广泛地采用，但现有的解码范式仍存在根本性的局限。无论是通过自回归 VLA 顺序解码动作，还是通过离散扩散 VLA 并行解码动作，一旦生成一个标记（token），它通常就是固定的，无法在后续迭代中修改，因此早期的标记错误无法在后期得到有效纠正。我们提出了 DFM-VLA，一种用于动作标记迭代细化的离散流匹配 VLA。DFM-VLA 建模了一个标记级的概率速度场，在细化迭代过程中动态更新完整的动作序列。我们研究了两种构建速度场的方法：辅助速度头公式和动作嵌入引导公式。我们的框架进一步采用了一种两阶段解码策略，包括一个迭代细化阶段，随后是用于稳定收敛的确定性验证。在 CALVIN、LIBERO 和真实世界操纵任务上的大量实验表明，DFM-VLA 在操纵性能上始终优于强大的自回归、离散扩散和连续扩散基准模型，同时保持了较高的推理效率。特别地，DFM-VLA 在 CALVIN 上实现了 4.44 的平均成功长度，在 LIBERO 上实现了 95.7% 的平均成功率，突显了通过离散流匹配进行动作细化在机器人操纵中的价值。我们的项目可在 https://chris1220313648.github.io/DFM-VLA/ 获取。

## Abstract
Vision--Language--Action (VLA) models that encode actions using a discrete tokenization scheme are increasingly adopted for robotic manipulation, but existing decoding paradigms remain fundamentally limited. Whether actions are decoded sequentially by autoregressive VLAs or in parallel by discrete diffusion VLAs, once a token is generated, it is typically fixed and cannot be revised in subsequent iterations, so early token errors cannot be effectively corrected later. We propose DFM-VLA, a discrete flow matching VLA for iterative refinement of action tokens. DFM-VLA~models a token-level probability velocity field that dynamically updates the full action sequence across refinement iterations. We investigate two ways to construct the velocity field: an auxiliary velocity-head formulation and an action-embedding-guided formulation. Our framework further adopts a two-stage decoding strategy with an iterative refinement stage followed by deterministic validation for stable convergence. Extensive experiments on CALVIN, LIBERO, and real-world manipulation tasks show that DFM-VLA consistently outperforms strong autoregressive, discrete diffusion, and continuous diffusion baselines in manipulation performance while retaining high inference efficiency. In particular, DFM-VLA achieves an average success length of 4.44 on CALVIN and an average success rate of 95.7\% on LIBERO, highlighting the value of action refinement via discrete flow matching for robotic manipulation. Our project is available \url{https://chris1220313648.github.io/DFM-VLA/}

---

## 论文详细总结（自动生成）

以下是对论文《DFM-VLA: Iterative Action Refinement for Robot Manipulation via Discrete Flow Matching》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的离散视觉-语言-动作（VLA）模型在生成动作标记（Token）时存在**“不可逆承诺”（Irreversible Commitment）**问题。
    *   **自回归（AR）模型**按顺序解码，一旦某个 Token 生成，后续无法更改。
    *   **离散扩散（DD）模型**虽然并行，但在早期迭代中产生的错误 Token 在后期往往被固定，难以有效修正。
*   **研究背景**：这种早期错误的累积会严重降低机器人执行长程任务的成功率。论文旨在引入**离散流匹配（Discrete Flow Matching, DFM）**技术，通过迭代细化（Refinement）机制，允许模型在推理过程中不断修正已生成的动作序列。

### 2. 方法论：核心思想与技术细节
DFM-VLA 的核心是将动作生成建模为一个在离散空间上的概率流过程。
*   **核心思想**：通过建模一个 Token 级的**概率速度场（Probability Velocity Field）**，在连续的时间步内动态更新整个动作序列，实现从噪声分布到目标动作分布的平滑转换。
*   **关键技术细节**：
    *   **速度场构建（两种变体）**：
        1.  **辅助速度头（Velocity Head）**：通过额外的预测头从模型隐藏状态直接预测 Token 替换的速度。
        2.  **动作嵌入引导（Embedding-guided）**：利用动作 Token 在嵌入空间中的语义距离来定义概率路径，推导出动力学最优速度。实验证明此方法收敛更快、性能更强。
    *   **两阶段解码策略**：
        1.  **迭代细化阶段**：利用 Euler 离散化采样，允许 Token 根据速度场进行随机“跳变”和修正。
        2.  **确定性验证阶段**：在最后几步切换为贪婪解码，确保动作序列稳定收敛。
    *   **自适应 KV 缓存**：针对迭代解码的特性，通过余弦相似度动态更新动作侧的缓存，将推理速度提升了 2.4 倍。

### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：评估长程、语言条件下的机器人操纵（ABCD→D 设置），重点考察错误累积情况。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，测试模型的跨任务泛化能力。
    *   **真实世界任务**：使用双臂 AgileX 机器人执行“协作提锅”、“蔬菜入锅”和“方块上盘”等任务。
*   **对比方法**：
    *   **自回归基准**：RT-1, Robo-Flamingo, UniVLA, OpenVLA, ReconVLA 等。
    *   **扩散模型基准**：π0-FAST, Dream-VLA, RDT（连续扩散）, Discrete Diffusion VLA 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   仿真环境训练步数为 20k 至 32k 步。
    *   真实世界任务每个任务训练 5k 步。
    *   学习率设为 1e-4，全局 Batch Size 为 32（8 GPU × 4 梯度累积）。
    *   模型初始化自预训练的机器人视频数据检查点。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验：CALVIN 进行了 1000 次 rollout，LIBERO 每个任务 50 次 rollout（共 40 个任务）。
    *   真实世界：每个任务 40 次测试。
    *   消融实验：针对超参数 $c$ 和 $\alpha$、解码步数分配（$T_{fine}$ vs $T_{val}$）、速度场构建方式、数据规模（10%、50%、100%）进行了详尽测试。
*   **充分性评价**：实验设计非常充分，涵盖了从仿真到现实、从短程到长程、从效率到精度的全方位对比，且消融实验逻辑严密，能够客观证明各组件的有效性。

### 6. 主要结论与发现
*   **性能领先**：DFM-VLA 在 CALVIN 上达到 **4.44** 的平均成功长度，在 LIBERO 上达到 **95.7%** 的成功率，均刷新了 SOTA 记录。
*   **修正能力**：实验证实 DFM 机制能有效修正早期解码错误，尤其在长程任务中表现出比 AR 和 DD 模型更强的鲁棒性。
*   **效率优势**：结合自适应 KV 缓存，DFM-VLA 在保持高性能的同时，推理延迟显著低于传统的自回归模型。
*   **数据效率**：在仅使用 10% 训练数据时，DFM-VLA 的表现依然大幅领先于 AR 和 DD 基准，显示出极高的数据利用率。

### 7. 优点（亮点）
*   **创新性地引入 DFM**：首次将离散流匹配引入 VLA 领域，解决了离散动作生成的“不可逆”痛点。
*   **嵌入引导机制**：巧妙利用 Token 嵌入空间的几何结构来引导动作细化，比单纯的掩码（Mask）机制更具语义合理性。
*   **两阶段解码**：平衡了生成过程中的“探索”（纠错）与“利用”（收敛稳定）。
*   **软硬件结合**：不仅在算法上创新，还通过自适应缓存优化了实际部署的推理效率。

### 8. 不足与局限
*   **离散化损失**：尽管表现优于部分连续扩散模型，但动作离散化（Tokenization）本身可能在极高精度的微操任务中存在量化误差。
*   **计算开销**：虽然比 AR 快，但相比于单步生成的模型，迭代细化仍需多次模型前向计算（文中默认为 16 步），在算力受限的嵌入式设备上仍有压力。
*   **依赖预训练**：模型性能高度依赖于底座 VLM 的预训练质量，对于完全未见过的物体或环境，其泛化边界仍有待进一步探索。

（完）
