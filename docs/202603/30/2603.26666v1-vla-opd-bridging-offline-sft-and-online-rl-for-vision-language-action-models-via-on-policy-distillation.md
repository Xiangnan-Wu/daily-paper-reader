---
title: "VLA-OPD: Bridging Offline SFT and Online RL for Vision-Language-Action Models via On-Policy Distillation"
title_zh: VLA-OPD：通过同策略蒸馏连接视觉-语言-动作模型的离线监督微调与在线强化学习
authors: "Zhide Zhong, Haodong Yan, Junfeng Li, Junjie He, Tianran Zhang, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26666v1"
tags: ["query:robot"]
score: 9.0
evidence: VLA模型的同策略蒸馏
tldr: 针对视觉-语言-动作（VLA）模型在部署时面临的离线微调分布偏移和在线强化学习效率低下的问题，本文提出了VLA-OPD框架。该框架通过在线策略蒸馏，利用专家教师对学生自生成轨迹进行密集的Token级监督。采用Reverse-KL目标函数，在纠正策略偏差的同时保留预训练能力，显著提升了模型的鲁棒性与采样效率。
selection_source: fresh_fetch
motivation: 传统的离线SFT存在分布偏移和灾难性遗忘，而在线RL受限于稀疏奖励和低采样效率，难以实现高效的VLA模型后训练。
method: 提出VLA-OPD框架，利用专家教师在学生生成的轨迹上提供密集监督，并结合Reverse-KL目标函数实现稳定的在线策略蒸馏。
result: 在LIBERO和RoboTwin2.0基准测试中，VLA-OPD在采样效率上优于RL，在鲁棒性上优于SFT，并有效缓解了灾难性遗忘。
conclusion: VLA-OPD成功桥接了SFT的效率与RL的鲁棒性，为VLA模型提供了一种稳定且高效的在线对齐与性能提升方案。
---

## 摘要
尽管预训练的视觉-语言-动作（VLA）模型在机器人操控中表现出令人印象深刻的泛化能力，但后训练对于确保部署期间的可靠性能仍然至关重要。然而，标准的离线监督微调（SFT）面临分布偏移和预训练能力灾难性遗忘的问题，而在线强化学习（RL）则受困于稀疏奖励和低样本效率。在本文中，我们提出了同策略 VLA 蒸馏（VLA-OPD），这是一个连接 SFT 的效率与 RL 的鲁棒性的框架。VLA-OPD 不依赖于稀疏的环境奖励，而是利用专家教师在学生自生成的轨迹上提供密集的、标记级（token-level）的监督。这使得能够对策略诱导状态进行主动纠错，同时通过温和的对齐保留预训练的通用能力。至关重要的是，我们通过逆向 KL（Reverse-KL）目标函数来构建 VLA-OPD。与导致模式覆盖（mode-covering）熵爆炸的标准正向 KL 或导致过早熵崩溃的硬交叉熵（Hard-CE）不同，我们的有界模式寻求（mode-seeking）目标通过过滤教师的认知不确定性并保持动作多样性，确保了稳定的策略学习。在 LIBERO 和 RoboTwin2.0 基准测试上的实验表明，VLA-OPD 相比 RL 显著提高了样本效率，相比 SFT 显著提高了鲁棒性，同时有效缓解了后训练过程中的灾难性遗忘。

## Abstract
Although pre-trained Vision-Language-Action (VLA) models exhibit impressive generalization in robotic manipulation, post-training remains crucial to ensure reliable performance during deployment. However, standard offline Supervised Fine-Tuning (SFT) suffers from distribution shifts and catastrophic forgetting of pre-trained capabilities, while online Reinforcement Learning (RL) struggles with sparse rewards and poor sample efficiency. In this paper, we propose On-Policy VLA Distillation (VLA-OPD), a framework bridging the efficiency of SFT with the robustness of RL. Instead of relying on sparse environmental rewards, VLA-OPD leverages an expert teacher to provide dense, token-level supervision on the student's self-generated trajectories. This enables active error correction on policy-induced states while preserving pre-trained general capabilities through gentle alignment. Crucially, we formulate VLA-OPD via a Reverse-KL objective. Unlike standard Forward-KL that induces mode-covering entropy explosion, or Hard-CE that causes premature entropy collapse, our bounded mode-seeking objective ensures stable policy learning by filtering out the teacher's epistemic uncertainty while maintaining action diversity. Experiments on LIBERO and RoboTwin2.0 benchmarks demonstrate that VLA-OPD significantly improves sample efficiency over RL and robustness over SFT, while effectively mitigating catastrophic forgetting during post-training.

---

## 论文详细总结（自动生成）

这篇论文提出了 **VLA-OPD**（Vision-Language-Action On-Policy Distillation），旨在解决视觉-语言-动作（VLA）模型在后训练（Post-training）阶段面临的效率与鲁棒性权衡问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：预训练的 VLA 模型虽然具有泛化性，但在具体任务部署时需要后训练。
*   **核心矛盾**：
    *   **离线监督微调（SFT）**：虽然收敛快、效率高，但存在“分布偏移”（训练在专家状态，测试在学生状态）和“灾难性遗忘”预训练能力的问题。
    *   **在线强化学习（RL）**：通过环境交互解决了分布偏移，但受限于“稀疏奖励”，导致采样效率极低且优化不稳定。
*   **研究目标**：寻找一种既能拥有 SFT 的高效率，又能具备 RL 鲁棒性的统一框架。

### 2. 方法论：VLA-OPD 框架
VLA-OPD 通过**同策略蒸馏（On-Policy Distillation）**连接了 SFT 和 RL，其核心流程分为三个阶段：
*   **阶段 1：同策略采样（Student Sampling）**：学生模型在环境中执行任务并生成轨迹。这使得模型能够暴露在自己诱导的状态分布下，特别是那些容易出错的“失败状态”。
*   **阶段 2：教师密集标注（Teacher Labeling）**：利用一个冻结的专家教师模型，对学生访问的每一个状态提供 Token 级的动作概率分布（Logits）。这提供了比环境奖励更密集的监督信号。
*   **阶段 3：逆向 KL 优化（Reverse-KL Optimization）**：
    *   **核心公式**：最小化 $D_{KL}(\pi_\theta || \pi_{tea})$，即最大化奖励 $r_t = -(\log \pi_\theta(a_t|s_t) - \log \pi_{tea}(a_t|s_t))$。
    *   **模式寻求（Mode-Seeking）**：论文指出，标准的**正向 KL** 会导致“熵爆炸”（被迫覆盖教师的所有不确定性），而**硬交叉熵（Hard-CE）**会导致“熵崩溃”（失去多样性）。**逆向 KL** 具有有界的模式寻求特性，能过滤教师的认知不确定性，同时保持动作多样性。
*   **组采样策略**：采用类似 GRPO 的组采样（Group Sampling）来估计策略梯度，降低方差。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，用于测试单臂操作。
    *   **RoboTwin2.0**：包含 4 个复杂的双臂协同任务。
*   **Benchmark 与对比方法**：
    *   **基准**：SimpleVLA-RL（作为教师）、OpenVLA-OFT（学生初始化）。
    *   **对比方法**：离线 SFT（Octo, OpenVLA, Nora）、在线 RL（GRPO）、以及不同对齐目标的消融版本。
*   **实验设置**：在 LIBERO 上使用极度匮乏的数据（仅 1 条演示轨迹进行 SFT 初始化），在 RoboTwin 上使用 1000 条轨迹初始化。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **超参数提及**：实验中使用了 Batch Size 为 64（消融实验为 32），组采样大小 $G=8$。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单臂和双臂、短时和长时任务，共计 8 个主要任务场景。
*   **消融实验**：针对对齐目标（Reverse-KL vs Forward-KL vs Hard-CE）和组采样大小 $G$ 做了详细对比。
*   **遗忘分析**：专门设计了“已见任务 vs 未见任务”的权衡实验，验证模型在提升目标任务性能时是否保留了通用能力。
*   **评价**：实验设计较为全面，通过对比不同数据量级下的表现，客观地证明了该方法在样本效率上的优势。

### 6. 主要结论与发现
*   **高效率**：在 LIBERO-Long 任务中，VLA-OPD 仅需 50 步即可达到 RL 基线 150 步的效果，实现 **3 倍加速**。
*   **高性能**：仅用 1 条演示轨迹训练的学生模型，通过蒸馏后性能可比肩使用 50 条轨迹训练的完整数据集模型。
*   **抗遗忘**：相比离线 SFT，VLA-OPD 在提升目标任务成功率的同时，对未见任务的性能下降极小，有效缓解了灾难性遗忘。
*   **稳定性**：逆向 KL 目标函数在 OOD（分布外）状态下表现出更健康的熵演变，避免了训练崩溃。

### 7. 优点
*   **理论深刻**：深入分析了不同 KL 散度方向对 VLA 模型熵动力学的影响，提出了“有界模式寻求”的见解。
*   **桥接能力强**：成功将 RL 的在线探索优势与 SFT 的密集监督优势结合，解决了 RL 奖励稀疏的痛点。
*   **实用性高**：在极低数据（1-traj）下表现强劲，降低了机器人数据采集的成本。

### 8. 不足与局限
*   **依赖专家教师**：该框架的前提是存在一个高性能的教师模型。如果对于某些新任务没有现成的专家，该方法将难以实施。
*   **算力细节缺失**：未提供具体的算力消耗数据，难以评估其在实际生产环境中的部署成本。
*   **教师偏差风险**：学生模型的上限受限于教师模型，如果教师模型在某些极端状态下存在偏差，学生可能会继承这些错误。

（完）
