---
title: Policy-Guided World Model Planning for Language-Conditioned Visual Navigation
title_zh: 策略引导的世界模型规划用于语言条件下的视觉导航
authors: "Amirhosein Chahe, Lifeng Zhou"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25981v1"
tags: ["query:robot"]
score: 10.0
evidence: 用于语言条件视觉导航的潜空间世界模型规划
tldr: 针对语言引导的视觉导航中长程规划难和世界模型动作初始化差的问题，本文提出PiJEPA框架。该框架结合了策略学习与潜空间世界模型规划，首先利用微调的通用策略生成动作分布，再以此引导MPPI在JEPA世界模型中进行规划。实验证明，该方法在真实导航任务中显著提升了目标到达率和指令遵循度。
selection_source: fresh_fetch
motivation: 现有的反应式策略难以处理长程规划，而世界模型在动作初始化时缺乏有效引导。
method: 提出PiJEPA框架，利用微调的Octo策略为JEPA世界模型中的MPPI规划提供动作先验分布。
result: 在真实世界导航任务中，PiJEPA在目标到达准确性和指令遵循保真度上均优于独立策略和无先验的世界模型。
conclusion: 结合策略先验与世界模型规划能有效提升具身智能体在复杂语言指令下的导航性能。
---

## 摘要
在具身智能中，根据自然语言指令导航至视觉指定的目标仍然是一个基本挑战。现有方法要么依赖于难以处理长时程规划的反应式策略，要么采用在动作初始化方面表现不佳的高维空间世界模型。我们提出了 PiJEPA，这是一个两阶段框架，结合了学习到的导航策略与潜空间世界模型规划的优势，用于指令条件下的视觉导航。在第一阶段，我们在 CAST 导航数据集上微调了一个基于 Octo 的通用策略，并辅以冻结的预训练视觉编码器（DINOv2 或 V-JEPA-2），以生成基于当前观测和语言指令的有信息动作分布。在第二阶段，我们利用该策略导出的分布来热启动模型预测路径积分（MPPI）规划，该规划是在一个单独训练的 JEPA 世界模型上进行的，该模型在同一冻结编码器的嵌入空间中预测未来的潜状态。通过从策略先验而非无信息的高斯分布初始化 MPPI 采样分布，我们的规划器能更快地收敛到到达目标的高质量动作序列。我们系统地研究了视觉编码器骨干网络的影响，在策略和世界模型组件中对比了 DINOv2 和 V-JEPA-2。在真实世界导航任务上的实验表明，PiJEPA 显著优于独立策略执行和无信息的世界模型规划，实现了更高的目标到达准确率和指令遵循保真度。

## Abstract
Navigating to a visually specified goal given natural language instructions remains a fundamental challenge in embodied AI. Existing approaches either rely on reactive policies that struggle with long-horizon planning, or employ world models that suffer from poor action initialization in high-dimensional spaces. We present PiJEPA, a two-stage framework that combines the strengths of learned navigation policies with latent world model planning for instruction-conditioned visual navigation. In the first stage, we finetune an Octo-based generalist policy, augmented with a frozen pretrained vision encoder (DINOv2 or V-JEPA-2), on the CAST navigation dataset to produce an informed action distribution conditioned on the current observation and language instruction. In the second stage, we use this policy-derived distribution to warm-start Model Predictive Path Integral (MPPI) planning over a separately trained JEPA world model, which predicts future latent states in the embedding space of the same frozen encoder. By initializing the MPPI sampling distribution from the policy prior rather than from an uninformed Gaussian, our planner converges faster to high-quality action sequences that reach the goal. We systematically study the effect of the vision encoder backbone, comparing DINOv2 and V-JEPA-2, across both the policy and world model components. Experiments on real-world navigation tasks demonstrate that PiJEPA significantly outperforms both standalone policy execution and uninformed world model planning, achieving improved goal-reaching accuracy and instruction-following fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Policy-Guided World Model Planning for Language-Conditioned Visual Navigation》（PiJEPA）的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对具身智能（Embodied AI）中的**语言条件视觉导航**任务，试图解决现有两类主流方法的局限性：
*   **反应式策略（Reactive Policies）：** 如 Octo 或 NoMaD，虽然在短时程控制和指令遵循上表现出色，但由于缺乏对未来状态的显式推理，在长程规划（Long-horizon planning）中容易失败。
*   **世界模型（World Models）：** 如 JEPA-WM，虽然能模拟未来状态进行规划，但在高维动作空间中通常使用无信息的随机初始化（如高斯分布）进行采样搜索（如 MPPI 算法），导致收敛慢且容易陷入局部最优。
*   **核心动机：** 提出 **PiJEPA** 框架，旨在将“策略的语义理解能力”与“世界模型的长程推理能力”结合，利用微调后的策略输出作为世界模型规划的“热启动”先验。

### 2. 方法论：核心思想与技术细节
PiJEPA 采用两阶段流程，核心在于**策略引导的采样初始化**：
*   **第一阶段：策略先验生成**
    *   使用微调后的 **Octo** 通用机器人策略。该策略以当前图像 $o_t$ 和语言指令 $\ell$ 为输入，通过其扩散动作头（Diffusion Head）生成 $N_\pi$ 个候选动作块。
    *   计算这些动作样本的经验均值 $\mu_\pi$ 和标准差 $\sigma_\pi$，形成一个具有语义信息的动作分布先验。
*   **第二阶段：策略引导的 MPPI 规划**
    *   **JEPA 世界模型：** 使用冻结的预训练视觉编码器（DINOv2 或 V-JEPA-2）提取特征，训练一个潜空间预测器 $P_\psi$，在嵌入空间中预测未来状态。
    *   **热启动规划：** MPPI 规划器不再从零均值高斯分布开始，而是从第一阶段得到的 $(\mu_\pi, \sigma_\pi)$ 开始迭代优化。
    *   **目标函数：** 在规划时，通过世界模型前向展开 $H$ 步，最小化预测的末端潜状态与目标图像 $o_g$ 编码之间的欧氏距离。
*   **关键技术：** 引入了坐标变换模块，将 Octo 输出的全局坐标系动作转换为世界模型所需的机器人局部坐标系动作。

### 3. 实验设计
*   **数据集：** 使用 **CAST 导航数据集**。该数据集包含大量视觉导航轨迹，并利用视觉语言模型（VLM）生成了反事实的指令-动作对，以增强模型对语言指令的敏感度。
*   **实验场景：** 真实世界导航任务的验证集评估。
*   **对比方法（Baselines）：**
    1.  **Octo：** 纯反应式策略执行。
    2.  **Default MPPI：** 纯世界模型规划（无信息初始化）。
    3.  **Octo-WM：** 使用策略生成样本，仅用世界模型进行评分筛选（不进行迭代优化）。
    4.  **PiJEPA (Ours)：** 策略引导+迭代优化。
*   **评估指标：** 绝对轨迹误差（ATE）和相对位姿误差（RPE），涵盖 XY 位置（米）和航向角（度）。

### 4. 资源与算力
*   **硬件设备：** 论文明确指出所有实验均在 **4 台 NVIDIA H200 GPU** 上进行。
*   **训练细节：** 涉及 Octo-Small 模型的微调以及基于 ViT 的 JEPA 世界模型的训练。虽然未详细说明总训练时长，但提到 H200 的使用表明了该研究对显存和计算带宽的高要求。
*   **推理延迟：** 策略生成约需 2.13s，MPPI 规划仅增加 0.35s，总计约 2.48s（针对 8 步规划视野）。

### 5. 实验数量与充分性
*   **实验规模：** 论文系统地测试了 **4 种编码器组合**（DINOv2 和 V-JEPA-2 分别作为策略和世界模型的骨干），并对比了 4 种不同的规划策略。
*   **消融与对比：** 实验涵盖了图像编码器（DINOv2）与视频编码器（V-JEPA-2）的性能差异分析，以及“仅评分”与“迭代优化”的对比。
*   **客观性：** 实验在统一的 CAST 验证集上进行，使用了标准的机器人导航评估指标。通过定性轨迹图和定量表格展示了 PiJEPA 在位置准确度上的优势。

### 6. 主要结论与发现
*   **PiJEPA 表现最优：** 在所有配置中，PiJEPA 的位置准确度（ATE XY）均优于独立策略或无信息规划。
*   **分工明确：** 反应式策略擅长局部控制和航向预测；无信息 MPPI 虽然位置精度差，但在航向对齐上表现较好；PiJEPA 结合两者，在轨迹层面的连贯性最好。
*   **编码器影响：** **V-JEPA-2**（视频预训练）在导航任务中整体优于 DINOv2（图像预训练），因为它能捕获更丰富的时空动态。
*   **规划的价值：** 规划的主要贡献在于减少长程任务中的累积误差，而非提升单步精度。

### 7. 优点与亮点
*   **解耦设计：** 将语言理解（策略负责）与物理动力学模拟（世界模型负责）解耦，降低了世界模型的训练难度。
*   **效率提升：** 通过策略引导，MPPI 可以在极少的迭代次数（J=4）和样本数（N=32）下实现高质量收敛，显著降低了在线规划的计算开销。
*   **通用性：** 框架不依赖于特定的视觉编码器，展示了在不同视觉基础模型（Foundation Models）上的迁移能力。

### 8. 不足与局限
*   **世界模型停滞（Stagnation）：** 失败案例分析显示，世界模型有时会预测出几乎静止的潜状态（Rollout 停滞），导致规划器失效。
*   **指令歧义：** 当指令具有高度模糊性（如“跟随那栋建筑”但视野中有多个目标）时，策略先验可能会误导规划器。
*   **闭环频率：** 约 2.5s 的推理延迟对于高速移动的机器人来说仍然偏慢，可能限制其在动态环境中的应用。
*   **实验覆盖：** 实验主要集中在 CAST 数据集的验证集上，缺乏在完全未见过的极端户外环境或复杂室内障碍物环境中的泛化性测试。

（完）
