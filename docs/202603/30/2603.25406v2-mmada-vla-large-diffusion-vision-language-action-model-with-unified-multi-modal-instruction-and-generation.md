---
title: "MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation"
title_zh: MMaDA-VLA：具有统一多模态指令与生成的扩散视觉-语言-动作大模型
authors: "Yang Liu, Pengxiang Ding, Tengyue Jiang, Xudong Wang, Wenxuan Song, Minghui Lin, Han Zhao, Hongyin Zhang, Zifeng Zhuang, Wei Zhao, Siteng Huang, Jinkui Shi, Donglin Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25406v2"
tags: ["query:robot"]
score: 10.0
evidence: 原生预训练大型扩散VLA模型
tldr: 本研究针对现有视觉-语言-动作（VLA）模型在架构开销、时间一致性及环境动力学捕捉方面的不足，提出了MMaDA-VLA。这是一种原生预训练的大型扩散VLA模型，通过离散扩散公式将语言、图像和机器人控制嵌入统一的标记空间。该模型利用掩码标记去噪技术，并行生成未来目标观测和动作块，实现了多模态理解与生成的统一。实验证明，该方法在LIBERO和CALVIN等基准测试中达到了SOTA性能，显著提升了长程任务的成功率和一致性。
selection_source: fresh_fetch
motivation: 现有VLA模型面临架构冗余、时间不一致以及在缺乏额外模块时难以捕捉环境动力学等挑战。
method: 采用原生离散扩散公式将语言、图像和动作嵌入统一标记空间，通过掩码标记去噪并行生成未来目标和动作块。
result: "在LIBERO基准测试中获得98.0%的平均成功率，在CALVIN任务中达到4.78的平均长度，均实现SOTA性能。"
conclusion: MMaDA-VLA通过统一的扩散框架实现了多模态理解与生成，在无需辅助模型的情况下显著增强了机器人操作的鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）模型旨在根据视觉观测和自然语言指令控制机器人进行操作。然而，现有的分层和自回归范式通常会引入架构开销，面临时间不一致性和长时程误差累积的问题，且缺乏在不增加额外模块的情况下捕获环境动力学的机制。为此，我们提出了 MMaDA-VLA，这是一个完全原生的预训练扩散 VLA 大模型，在单一框架中统一了多模态理解与生成。我们的核心思想是一种原生的离散扩散公式，将语言、图像和连续机器人控制嵌入到统一的离散 token 空间中，并通过掩码 token 去噪训练单个骨干网络，从而并行地联合生成未来目标观测和动作块。迭代去噪实现了全局、无序的细化，在无需辅助世界模型的情况下，将动作锚定在预测的未来视觉结果中，提升了长时程一致性。在仿真基准和真实世界任务中的实验表明，该模型达到了最先进的性能，在 LIBERO 上实现了 98.0% 的平均成功率，在 CALVIN 上达到了 4.78 的平均长度。

## Abstract
Vision-Language-Action (VLA) models aim to control robots for manipulation from visual observations and natural-language instructions. However, existing hierarchical and autoregressive paradigms often introduce architectural overhead, suffer from temporal inconsistency and long-horizon error accumulation, and lack a mechanism to capture environment dynamics without extra modules. To this end, we present MMaDA-VLA, a fully native pre-trained large diffusion VLA model that unifies multi-modal understanding and generation in a single framework. Our key idea is a native discrete diffusion formulation that embeds language, images, and continuous robot controls into one discrete token space and trains a single backbone with masked token denoising to jointly generate a future goal observation and an action chunk in parallel. Iterative denoising enables global, order-free refinement, improving long-horizon consistency while grounding actions in predicted future visual outcomes without auxiliary world models. Experiments across simulation benchmarks and real-world tasks show state-of-the-art performance, achieving 98.0% average success on LIBERO and 4.78 average length on CALVIN.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation》** 的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型主要采用分层架构（增加额外策略头）或自回归范式（离散化动作后逐个生成）。这些方法存在架构冗余、训练成本高、长程任务中误差累积严重、时间一致性差，以及缺乏捕捉环境动力学（预测未来视觉变化）的机制等缺陷。
*   **研究动机**：旨在开发一种**原生预训练**的扩散 VLA 模型，通过单一框架统一多模态理解与生成，在不增加额外世界模型模块的情况下，实现动作与未来视觉目标的同步预测，从而提升机器人操作的精准度和长程一致性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **MMaDA-VLA**，利用**离散扩散（Discrete Diffusion）**公式，将语言指令、图像观测和连续机器人动作全部映射到统一的离散 Token 空间中，通过掩码 Token 去噪（Masked Token Denoising）任务训练单一骨干网络。
*   **关键技术细节**：
    *   **统一 Token 化**：文本使用 LLaDA 分词器，图像使用 MAGVIT-v2 量化器，动作则将每个维度离散化为 256 个 bin。
    *   **混合注意力机制（Hybrid Attention）**：模态内部采用双向全注意力（实现全局信息交换），模态之间采用因果注意力（确保信息流向正确），有效解耦了生成目标。
    *   **并行生成与迭代去噪**：模型在推理时并行生成“未来目标观测（Goal Obs）”和“动作块（Action Chunk）”。通过迭代去噪，模型可以根据预测的视觉演变不断修正动作，避免了自回归模型中常见的顺序偏差和误差累积。
    *   **KV 缓存优化**：引入免训练的缓存框架，针对去噪过程中变化较小的 Token 进行选择性刷新，以满足机器人实时控制的低延迟要求。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用了包含 DROID、Fractal、Bridge V2 等在内的 26 个跨具身机器人数据集，总计约 6100 万步数据。
    *   **仿真基准**：**LIBERO**（评估空间、物体、目标和长程泛化能力）和 **CALVIN**（评估长程语言条件下的操作，采用 ABC→D 设置测试环境泛化）。
    *   **真实世界**：使用 AgileX Piper 6 自由度机械臂，执行简单取放、精准堆叠、复杂抽屉存储和长程桌面整理四类任务。
*   **对比方法**：对比了包括 OpenVLA、RT-1、Diffusion Policy、π0、VLA-Adapter、UniVLA、DreamVLA 等在内的数十种最先进（SOTA）的离散和连续动作 VLA 模型。

### 4. 资源与算力
*   **算力投入**：使用了 **8 个 Nvidia H800 GPU 节点**（每个节点配备 8 张 80GB VRAM 的显卡，共计 64 张 H800）。
*   **训练时长**：预训练阶段耗时约 **30 小时**。
*   **模型规模**：基于 80 亿参数（8B）的 MMaDA-Base 骨干网络构建。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的 4 个子任务集和 CALVIN 的长程挑战上进行了详尽测试。
    *   真实世界实验涵盖 4 类任务，每类任务进行 30 次试验。
    *   **消融实验**：针对“有无世界模型预测”、“有无并行去噪”、“不同注意力机制”以及“有无预训练”进行了 5 组对比实验。
*   **充分性评价**：实验设计非常充分且具有说服力。通过大规模预训练数据和跨仿真/现实的对比，客观地证明了离散扩散架构在处理复杂动力学和长程任务时的优越性。

### 6. 主要结论与发现
*   **性能领先**：MMaDA-VLA 在 LIBERO 上达到 98.0% 的平均成功率，在 CALVIN 上达到 4.78 的平均完成长度，均刷新了 SOTA 纪录。
*   **动力学建模的重要性**：消融实验显示，移除“目标图像预测（世界模型属性）”会导致性能大幅下降（CALVIN 平均长度下降 0.48），证明了预测未来视觉状态对动作规划的指导作用。
*   **并行优于串行**：并行生成目标图像和动作比先生成图像再生成动作的效果更好，因为并行去噪允许动作预测利用图像生成的中间特征。
*   **预训练增益**：大规模预训练显著提升了模型的泛化能力，在 LIBERO 上提升了 3.5% 的成功率。

### 7. 优点
*   **架构简洁统一**：无需额外的策略头或复杂的世界模型模块，在单一 Transformer 框架内实现了理解与生成的闭环。
*   **解决顺序偏差**：离散扩散的无序生成特性比自回归模型更适合处理本质上无序的动作维度。
*   **长程鲁棒性**：通过动作块预测和视觉目标锚定，显著降低了长时程任务中的累积误差。
*   **推理效率优化**：通过 KV Cache 方案解决了扩散模型推理慢的痛点，使其具备实时控制能力。

### 8. 不足与局限
*   **视觉生成质量有限**：生成的未来目标图像虽然在语义上一致，但在细粒度细节（如夹持器精确位置、复杂物体纹理）上存在模糊，这可能限制其在极高精度任务中的表现。
*   **计算资源需求高**：尽管训练时间不算极长，但 64 张 H800 的算力门槛对于普通研究机构仍较高。
*   **Token 长度限制**：由于图像和动作都被 Token 化，序列长度会随分辨率或动作块大小增加而迅速膨胀，可能带来计算开销压力。

（完）
