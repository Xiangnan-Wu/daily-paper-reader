---
title: "LILAC: Language-Conditioned Object-Centric Optical Flow for Open-Loop Trajectory Generation"
title_zh: LILAC：用于开环轨迹生成的语言条件化以对象为中心的光流
authors: "Motonari Kambara, Koki Seno, Tomoya Kaichi, Yanan Wang, Komei Sugiura"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25481v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于操作的基于流的视觉-语言-动作模型 (VLA)
tldr: LILAC是一个基于光流的视觉-语言-动作模型（VLA），旨在通过自然语言指令生成机器人操作轨迹。它利用语义对齐损失和提示条件跨模态适配器，从单张RGB图像和文本指令中生成以物体为中心的2D光流，并将其转换为6自由度机械臂动作。该方法减少了对特定机器人数据的依赖，在光流质量和实际操作成功率上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有的机器人操作任务在从预操作图像和自然语言指令生成准确的物体轨迹时，面临指令与光流对齐困难的问题。
method: 提出了LILAC模型，通过语义对齐损失强化语言调节，并利用跨模态适配器融合视觉提示与图文特征，生成2D光流并转换为6自由度轨迹。
result: 实验表明，LILAC在多个基准测试中的光流生成质量以及在真实物理操作实验中的任务成功率均显著优于现有方法。
conclusion: LILAC证明了基于光流的开环轨迹生成在处理自由形式指令和跨模态对齐方面的有效性，为机器人操作提供了一种高效的VLA方案。
---

## 摘要
我们利用基于流的轨迹生成来解决语言条件化的机器人操纵问题，这种方法支持在人类和网络上的物体操纵视频上进行训练，且仅需极少量的特定具身数据。这一任务具有挑战性，因为从操纵前的图像和自然语言指令生成物体轨迹需要适当的指令-流对齐。为了应对这一挑战，我们提出了基于流的语言指令引导开环动作生成器（LILAC）。这种基于流的视觉-语言-动作模型（VLA）从 RGB 图像和自然语言指令中生成以对象为中心的二维光流，并将该光流转换为六自由度（6-DoF）机械臂轨迹。LILAC 包含两个关键组件：语义对齐损失（Semantic Alignment Loss），用于强化语言条件化以生成与指令对齐的光流；以及提示条件化跨模态适配器（Prompt-Conditioned Cross-Modal Adapter），用于将学习到的视觉提示与图像和文本特征对齐，从而为光流生成提供丰富的线索。实验表明，我们的方法在多个基准测试中的生成流质量均优于现有方法。此外，在使用自由格式指令的物理对象操纵实验中，LILAC 展示了比现有方法更高的任务成功率。项目页面见 https://lilac-75srg.kinsta.page/。

## Abstract
We address language-conditioned robotic manipulation using flow-based trajectory generation, which enables training on human and web videos of object manipulation and requires only minimal embodiment-specific data. This task is challenging, as object trajectory generation from pre-manipulation images and natural language instructions requires appropriate instruction-flow alignment. To tackle this challenge, we propose the flow-based Language Instruction-guided open-Loop ACtion generator (LILAC). This flow-based Vision-Language-Action model (VLA) generates object-centric 2D optical flow from an RGB image and a natural language instruction, and converts the flow into a 6-DoF manipulator trajectory. LILAC incorporates two key components: Semantic Alignment Loss, which strengthens language conditioning to generate instruction-aligned optical flow, and Prompt-Conditioned Cross-Modal Adapter, which aligns learned visual prompts with image and text features to provide rich cues for flow generation. Experimentally, our method outperformed existing approaches in generated flow quality across multiple benchmarks. Furthermore, in physical object manipulation experiments using free-form instructions, LILAC demonstrated a superior task success rate compared to existing methods. The project page is available at https://lilac-75srg.kinsta.page/.

---

## 论文详细总结（自动生成）

以下是对论文《LILAC: Language-Conditioned Object-Centric Optical Flow for Open-Loop Trajectory Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作模型（VLA）通常需要海量的特定机器人（具身）数据，且在处理自然语言指令与底层物理动作的对齐时表现欠佳，导致零样本（zero-shot）迁移能力弱。
*   **研究背景**：基于光流（Optical Flow）的方法可以利用互联网上大量的人类操作视频进行预训练，减少对机器人数据的依赖。然而，现有的流模型多为闭环（在线）生成，推理成本高且容易产生误差累积，且难以精确遵循复杂的语言指令。
*   **整体含义**：论文提出了 **LILAC** 框架，旨在通过单张 RGB 图像和语言指令，以开环方式生成以物体为中心的 2D 光流，并将其转化为 6 自由度（6-DoF）的机器人轨迹，实现高效、精准的指令遵循操作。

### 2. 方法论
LILAC 由两个主要模块组成：**光流生成模块**和**动作反标记化器（Action De-Tokenizer）**。
*   **核心思想**：利用多模态大模型（MLLM）生成中间态的“视觉提示”（Visual Prompt），桥接高层语义指令与底层密集光流。
*   **关键技术细节**：
    *   **提示条件化跨模态适配器**：首先调用 MLLM（如 GPT-4o）根据图像和指令生成物体的边界框和位移矢量（以箭头形式渲染在图像上作为视觉提示），随后通过 Transformer 编码器融合图像、指令和视觉提示特征。
    *   **语义对齐解码器**：采用 Transformer 解码器自回归生成 2D 光流序列。
    *   **语义对齐损失（Semantic Alignment Loss）**：通过 $L_1$ 损失强制光流序列的最后一个 Token 与 CLIP 语言嵌入对齐，确保生成的运动轨迹在语义上符合指令要求。
    *   **动作反标记化器**：将 2D 光流结合深度图和相机参数，先通过刚性变换估计粗略轨迹，再利用 Transformer 解码器进行精细化修正，输出 6-DoF 机械臂动作。

### 3. 实验设计
*   **数据集/场景**：
    *   **Robot Flow 基准测试**：基于 Fractal（RT-1）和 BridgeData V2 数据集构建，包含数万个操作片段，并利用 CoTracker-3 生成地面真值光流。
    *   **物理实验场景**：使用丰田 HSR 机器人，涵盖移动放置、移动移动、移动倾倒、抽取纸巾、开抽屉 5 类任务。
*   **对比方法**：
    *   光流生成对比：**Im2Flow2Act**、**FLIP**。
    *   物理实验对比：上述流模型以及最先进的 VLA 模型 **$\pi_0$**。
*   **评价指标**：平均距离误差（ADE）、曲线下面积（AUC）、Precision@K（P@K）以及真实机器人任务成功率。

### 4. 资源与算力
*   **算力设备**：论文明确指出训练使用了 **NVIDIA H200 GPU**。
*   **训练细节**：文中未详细说明具体的训练时长和 GPU 数量，但提到采用分类损失（Cross-Entropy）代替回归损失以加速收敛。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量实验**：在两个大规模子数据集（Fractal 和 BridgeData V2）上进行了详尽测试。
    *   **消融实验**：针对“语义对齐损失”和“视觉提示”分别做了对比，验证了各组件的有效性。
    *   **物理实验**：共进行了 100 次真实机器人试验（5 个任务 $\times$ 20 次）。
    *   **错误分析**：对 100 个 MLLM 生成的视觉提示进行了人工分类分析（如终点错误、方向反向等）。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真基准到真实物理环境的迁移，且对比了当前最前沿的流模型和 VLA 模型，客观性较强。

### 6. 主要结论与发现
*   **性能领先**：LILAC 在光流生成精度（ADE/AUC）上显著优于 Im2Flow2Act 和 FLIP。
*   **物理成功率高**：在真实机器人实验中，LILAC 的平均成功率比表现最好的基准方法高出 14 个百分点。
*   **推理效率**：相比于闭环生成的 $\pi_0$，LILAC 的开环生成速度快约 1.4 倍，且能有效避免随时间增加的复合误差。
*   **视觉提示的作用**：消融实验证明，视觉提示是提升性能最关键的因素，能显著降低 ADE 误差。

### 7. 优点
*   **创新性对齐方案**：通过 MLLM 生成“箭头”视觉提示，巧妙地解决了高层指令与底层像素运动之间的粒度差异问题。
*   **数据效率**：利用光流作为中间表示，使得模型可以从非机器人视频（如人类操作视频）中学习，降低了对昂贵具身数据的依赖。
*   **开环鲁棒性**：证明了在静态环境下，高质量的开环轨迹生成比频繁重规划的闭环方法更具推理优势。

### 8. 不足与局限
*   **静态环境假设**：采用开环生成，意味着模型无法应对执行过程中环境的动态变化（如物体被突然移走）。
*   **2D 流的表达力限制**：2D 光流难以表达复杂的 3D 空间运动（如绕障碍物放置或工具搅拌），缺乏显式的深度维度推理。
*   **对 MLLM 的依赖风险**：如果 MLLM 生成的视觉提示（箭头）出错（实验显示约 37% 的失败率），错误会直接传播到下游的光流生成中。
*   **深度信息依赖**：虽然光流生成仅需 RGB，但最终动作转换仍依赖深度图，在视觉复杂的场景下深度推理可能失效。

（完）
