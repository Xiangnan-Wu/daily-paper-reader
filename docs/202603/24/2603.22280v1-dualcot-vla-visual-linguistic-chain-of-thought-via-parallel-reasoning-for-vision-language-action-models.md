---
title: "DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models"
title_zh: DualCoT-VLA：通过并行推理实现视觉-语言-动作模型的视觉-语言思维链
authors: "Zhide Zhong, Junfeng Li, Junjie He, Haodong Yan, Xin Gong, Guanyi Zhao, Yingjie Cai, Jiantao Gao, Xu Yan, Bingbing Liu, Yingcong Chen, Liuqing Yang, Haoang Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22280v1"
tags: ["query:robot"]
score: 10.0
evidence: VLA模型中用于复杂机器人任务的思维链推理
tldr: 针对视觉-语言-动作（VLA）模型在复杂任务规划和精细空间感知方面的不足，本文提出 DualCoT-VLA。该方法结合了用于高层规划的语言思维链和用于底层空间理解的视觉思维链，并引入并行推理机制，通过可学习查询令牌取代传统的自回归解码，显著降低了推理延迟并提升了模型在多项基准测试及真实场景中的性能。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型难以兼顾高层逻辑与底层视觉细节，且自回归思维链推理速度慢、误差易累积。
method: 提出双重思维链（DualCoT）结合视觉与语言推理，并利用并行推理机制实现单步前向推理以提高效率。
result: 在 LIBERO、RoboCasa GR1 基准测试及真实机器人平台上均取得了最先进的性能表现。
conclusion: DualCoT-VLA 通过多模态并行推理有效增强了机器人的复杂任务处理能力与实时性。
---

## 摘要
视觉-语言-动作（VLA）模型将视觉观测和语言指令直接映射为机器人动作。虽然标准 VLA 模型在简单任务中表现出色，但在处理需要逻辑规划的复杂多步任务，以及需要细粒度空间感知的精确操作时往往面临困难。最近的研究引入了思维链（CoT）推理，赋予 VLA 模型“先思后行”的能力。然而，目前的基于 CoT 的 VLA 模型面临两个关键局限性：1) 由于依赖孤立的单模态 CoT，无法同时捕捉底层视觉细节和高层逻辑规划；2) 逐步自回归解码导致的高推理延迟和复合误差。为了解决这些局限性，我们提出了 DualCoT-VLA，这是一种具有并行推理机制的 VLA 模型视觉-语言 CoT 方法。为了实现全面的多模态推理，我们的方法集成了用于底层空间理解的视觉 CoT 和用于高层任务规划的语言 CoT。此外，为了克服延迟瓶颈，我们引入了一种并行 CoT 机制，该机制结合了两组可学习的查询标记（query tokens），将自回归推理转变为单步前向推理。大量实验表明，我们的 DualCoT-VLA 在 LIBERO 和 RoboCasa GR1 基准测试以及真实世界平台中均达到了最先进的性能。

## Abstract
Vision-Language-Action (VLA) models map visual observations and language instructions directly to robotic actions. While effective for simple tasks, standard VLA models often struggle with complex, multi-step tasks requiring logical planning, as well as precise manipulations demanding fine-grained spatial perception. Recent efforts have incorporated Chain-of-Thought (CoT) reasoning to endow VLA models with a ``thinking before acting'' capability. However, current CoT-based VLA models face two critical limitations: 1) an inability to simultaneously capture low-level visual details and high-level logical planning due to their reliance on isolated, single-modal CoT; 2) high inference latency with compounding errors caused by step-by-step autoregressive decoding. To address these limitations, we propose DualCoT-VLA, a visual-linguistic CoT method for VLA models with a parallel reasoning mechanism. To achieve comprehensive multi-modal reasoning, our method integrates a visual CoT for low-level spatial understanding and a linguistic CoT for high-level task planning. Furthermore, to overcome the latency bottleneck, we introduce a parallel CoT mechanism that incorporates two sets of learnable query tokens, shifting autoregressive reasoning to single-step forward reasoning. Extensive experiments demonstrate that our DualCoT-VLA achieves state-of-the-art performance on the LIBERO and RoboCasa GR1 benchmarks, as well as in real-world platforms.

---

## 论文详细总结（自动生成）

### DualCoT-VLA 论文深度总结

#### 1. 核心问题与研究动机
*   **研究背景**：视觉-语言-动作（VLA）模型虽然在简单机器人任务中表现良好，但在处理需要**长程逻辑规划**的复杂任务和需要**精细空间感知**的精确操作时力不从心。
*   **核心问题**：现有的思维链（CoT）VLA 模型存在两大局限：
    1.  **模态孤立**：现有的 CoT 通常仅限于单一模态（纯文本或纯视觉），无法同时兼顾高层逻辑规划与底层空间细节。
    2.  **推理延迟与误差累积**：大多数模型依赖自回归（AR）解码，逐个生成标记导致推理速度极慢，且一旦中间步骤出错，误差会迅速累积。
*   **研究动机**：开发一种既能融合多模态推理信息，又能实现高效并行推理的 VLA 架构。

#### 2. 方法论
DualCoT-VLA 提出了一种在**连续潜空间**内进行并行推理的视觉-语言思维链范式：
*   **核心思想**：引入两组可学习的查询标记（Query Tokens），通过单次前向传播同时提取视觉空间先验和语言逻辑规划，避免了自回归解码。
*   **关键技术细节**：
    *   **并行隐式 CoT**：在 VLM 输入序列中加入视觉查询标记 $Q_{vis}$ 和语言查询标记 $Q_{lin}$。
    *   **视觉 CoT（几何蒸馏）**：利用冻结的视觉基础模型（Depth Anything 3）作为教师，通过交叉注意力投影器将 $Q_{vis}$ 的隐藏状态与密集的深度特征图对齐，使模型获得 3D 空间感知。
    *   **语言 CoT（步骤级监督）**：利用冻结的轻量级 LLM（如 Qwen3-0.6B）作为解码器，监督 $Q_{lin}$ 生成任务规划文本，迫使 VLM 将逻辑规划压缩进潜空间标记中。
    *   **动作专家（Action Expert）**：采用基于流匹配（Flow-Matching）的扩散 Transformer（DiT）头，利用推理增强后的隐藏状态预测连续动作。
*   **推理流程**：训练时使用辅助教师模型进行监督；推理时丢弃教师模型，仅需一次前向计算即可输出动作。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个任务套件，测试长程和空间能力。
    *   **RoboCasa GR1**：涉及 29 自由度灵巧手的人形机器人桌面任务，测试高维动作协调。
    *   **真实世界**：基于 AgileX Cobot 双臂机器人（Mobile ALOHA 系统），设计了简单（放面包）、中等（放方块）、困难（收集水果）三种任务。
*   **对比方法**：
    *   **非 CoT 模型**：OpenVLA, $\pi_0$, Diffusion Policy 等。
    *   **自回归 CoT 模型**：CoT-VLA, ThinkAct, DeepThinkVLA, LaRA-VLA 等。

#### 4. 资源与算力
*   **硬件**：论文明确提到训练是在 **NVIDIA H100 GPU** 上进行的。
*   **模型规模**：VLM 主干网络使用 Qwen3-VL-4B，辅助解码器使用 Qwen3-0.6B。
*   **训练细节**：LIBERO 任务的学习率为 2.5e-5，RoboCasa 为 3e-5。具体的训练总时长未在文中详细标明，但给出了全局 Batch Size（LIBERO 为 48，RoboCasa 为 256）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 上进行了 500 个回合的评估。
    *   在 RoboCasa 上对 24 个任务进行了每任务 50 次的测试。
    *   真实世界实验每项任务进行 25 次尝试。
    *   **消融实验**：分别验证了仅视觉 CoT、仅语言 CoT 以及双 CoT 的贡献。
    *   **推理速度分析**：对比了非 CoT、自回归 CoT 与并行 CoT 的延迟。
*   **充分性评价**：实验设计非常全面，涵盖了从仿真到真实、从 7 自由度到 29 自由度、从短程到长程的多种维度。对比基准包含了最新的 SOTA 模型，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：在 LIBERO 平均成功率达到 **98.8%**，在 RoboCasa 复杂任务中也显著优于现有模型。
*   **推理高效**：相比自回归 CoT 模型（延迟约 3178ms），DualCoT-VLA 的总推理延迟仅为 **83.2ms**，仅比非 CoT 模型略高，完全满足高频闭环控制需求。
*   **多模态互补**：视觉 CoT 显著提升了空间感知任务（Spatial），而语言 CoT 则对长程任务（Long）至关重要，两者结合效果最佳。

#### 7. 优点
*   **并行化创新**：通过 Query Tokens 将 CoT 从“串行生成”转变为“并行提取”，解决了 VLA 领域长期存在的推理速度瓶颈。
*   **隐式推理**：在潜空间进行推理，既保留了多模态信息的丰富性，又避免了文本解码带来的冗余和幻觉风险。
*   **强大的泛化性**：在灵巧手操作和真实世界非结构化环境中表现出极强的鲁棒性。

#### 8. 不足与局限
*   **依赖高质量教师**：训练过程高度依赖 Depth Anything 3 和高性能 LLM 生成的标注，若教师模型存在偏差，会影响 VLA 的上限。
*   **任务范围限制**：虽然涵盖了多种基准，但实验仍集中在桌面操作（Tabletop Tasks），对于更大范围的移动导航或更复杂的全身协调任务尚待验证。
*   **训练成本**：虽然推理快，但端到端训练一个结合了 VLM、多个教师模型和 DiT 的系统，对算力和显存的要求依然较高。

（完）
