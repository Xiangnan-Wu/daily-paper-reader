---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v2"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人策略的以动作为中心的世界动作模型
tldr: GigaWorld-Policy 是一种高效的以动作为中心的世界-动作模型（WAM），旨在解决现有模型推理开销大及视觉与动作表示耦合的问题。该模型通过因果设计，在训练时利用视频生成提供物理约束，而在推理时可选择性跳过视频生成以加速。实验表明，其推理速度比领先基准快 9 倍，且在多个机器人任务中显著提升了成功率。
selection_source: fresh_fetch
motivation: 现有的世界-动作模型推理开销巨大，且动作预测过度依赖于未来视频生成的质量。
method: 采用以动作为中心的架构，通过因果设计分离动作预测与视频生成，使模型在推理时无需生成视频即可快速输出动作。
result: "在真实机器人平台上，推理速度比 Motus 快 9 倍，成功率提升 7%，在 RoboTwin 2.0 上性能优于 pi-0.5 达 95%。"
conclusion: GigaWorld-Policy 证明了通过解耦视觉预测与动作执行，可以在保持物理一致性的同时大幅提升机器人策略的效率与性能。
---

## 摘要
由预训练视频生成骨干网络初始化的世界-动作模型（WAM）在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模往往会使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们提出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习 2D 像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练表述为两个耦合的部分：模型根据当前观测预测未来动作序列，并同时根据预测的动作和相同的观测生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束鼓励产生符合物理规律的动作。通过防止未来视频标记（tokens）影响动作标记的因果设计，显式的未来视频生成在推理阶段是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化的大规模机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其作为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

### GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型深度总结

#### 1. 核心问题与研究动机
*   **研究背景**：视觉-语言-动作（VLA）模型在机器人控制中表现强劲，但面临“监督稀疏”挑战。为了增强监督信号，近期研究引入了“世界-动作模型”（WAM），通过预测未来视频来辅助动作学习。
*   **核心问题**：
    1.  **推理延迟高**：现有 WAM 通常需要在推理时迭代采样生成未来视频，导致控制频率低，难以实时部署。
    2.  **误差累积**：动作预测往往过度依赖显式的视频生成质量，微小的视觉预测偏差可能导致长程控制失效。
*   **研究动机**：开发一种既能利用视频生成的密集监督信号，又能在推理时摆脱视频生成开销的高效模型。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出“以动作为中心”的架构。在训练阶段，利用未来视觉动力学作为推理信号和密集监督；在推理阶段，将视频预测设为可选，实现低延迟的纯动作解码。
*   **关键技术**：
    *   **因果序列建模（Causal Masking）**：采用统一的 Token 序列 `[观察; 状态; 动作; 未来视频]`。通过特定的因果掩码设计，确保动作 Token 仅依赖当前观察和状态，而未来视频 Token 则依赖动作。这种设计实现了动作与视频生成的解耦。
    *   **流量匹配（Flow Matching）**：使用基于 5B 参数的 Diffusion Transformer（DiT）骨干网络，通过流量匹配目标函数同时优化动作预测损失（$\mathcal{L}_{action}$）和视频预测损失（$\mathcal{L}_{video}$）。
    *   **课程预训练方案**：
        1.  **通用视频初始化**：从大规模互联网视频模型（Wan-2.2）初始化。
        2.  **具身数据预训练**：在约 10,000 小时的机器人视频和人类第一视角视频（如 Ego4D）上训练，注入物理先验。
        3.  **目标任务微调**：在特定机器人的轨迹数据上进行指令对齐。

#### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：使用 RoboTwin 2.0 平台，涵盖 50 多个操作任务，包含清洁场景和随机化场景。
    *   **真实世界**：使用 AgileX PiPER 6自由度机械臂，设计了“清理桌面”、“扫描二维码”、“叠碗”、“扫垃圾”四个复杂任务。
*   **对比基准（Baselines）**：
    *   **VLA 模型**：$\pi$ 0.5、GigaBrain-0、X-VLA。
    *   **WAM 模型**：Motus、Cosmos-Policy。

#### 4. 资源与算力
*   **算力投入**：预训练阶段在公共数据集上消耗了 **6000 GPU 小时**。
*   **训练配置**：全局 Batch Size 为 256，使用 AdamW 优化器，学习率采用余弦退火策略（$1 \times 10^{-4}$ 到 $1 \times 10^{-6}$）。
*   **推理设备**：推理延迟测试基于 **NVIDIA A100 GPU**。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖 50+ 任务，每个任务 100 次测试。
    *   真实世界实验每个任务 20 次尝试，并采用分级评分（如抓取成功 0.5，放置成功 0.5）。
    *   **消融实验**：针对预训练配置、未来帧预测数量（$\Delta$ 间隔）、因果掩码设计、数据效率（10% 到 100% 数据量）进行了详尽测试。
*   **评价**：实验设计非常充分且客观。通过跨仿真与现实的对比，以及与多种 SOTA 方法的直接竞争，有力证明了模型在效率和性能上的双重优势。

#### 6. 主要结论与发现
*   **效率飞跃**：在 A100 上推理延迟仅为 **360ms**，比领先的 WAM 模型 Motus 快 **9 倍**。
*   **性能提升**：在真实世界中，成功率比 Motus 高出 **7%**；在仿真环境 RoboTwin 2.0 中，性能比 $\pi$ 0.5 提升了 **95%**。
*   **数据效率**：GigaWorld-Policy 仅需 **10%** 的训练数据即可达到传统 VLA 模型使用 100% 数据时的成功率。
*   **视觉辅助作用**：消融实验显示，引入未来帧预测作为辅助监督，可将任务成功率从 0.65 提升至 0.83。

#### 7. 优点与亮点
*   **架构优雅**：通过因果掩码巧妙解决了 WAM 推理慢的顽疾，实现了“训练时有视频监督，推理时无视频开销”。
*   **物理一致性**：利用大规模视频预训练注入的物理先验，显著增强了动作的合理性和鲁棒性。
*   **多视图融合**：通过将三视图拼接为复合图像，在不改变骨干网络结构的前提下实现了跨视图推理。

#### 8. 不足与局限
*   **模型体量**：虽然推理比其他 WAM 快，但 5B 参数的 DiT 骨干网络对于嵌入式边缘计算设备（如机器人端侧计算单元）仍有较大压力。
*   **任务复杂度**：真实世界实验虽然包含多步规划，但主要集中在桌面级操作，尚未展示在移动底座或更广阔动态环境中的表现。
*   **视频预测精度**：虽然视频预测是可选的，但其预测质量仍受限于 2D 像素空间，对于极精细的力控任务可能仍需更高维度的表征。

（完）
