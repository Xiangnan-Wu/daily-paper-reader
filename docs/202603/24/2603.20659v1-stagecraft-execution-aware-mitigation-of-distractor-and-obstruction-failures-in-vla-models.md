---
title: "StageCraft: Execution Aware Mitigation of Distractor and Obstruction Failures in VLA Models"
title_zh: StageCraft：VLA 模型中干扰物和障碍物失败的执行感知缓解
authors: "Kartikay Milind Pangaonkar, Prabin Rath, Omkar Patil, Nakul Gopalan"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20659v1"
tags: ["query:robot"]
score: 9.0
evidence: 减轻机器人VLA模型中的执行失败
tldr: "针对视觉语言动作模型（VLA）在面对干扰物和物理障碍时容易失败的问题，本文提出了 StageCraft。这是一种无需训练的即插即用方法，利用大语言模型（VLM）的上下文推理能力，通过分析少量策略执行视频，识别并移除环境中的干扰因素，从而优化初始状态。实验表明，该方法在多种真实任务中将 VLA 性能提升了 40%，有效增强了机器人在复杂环境中的鲁棒性。"
selection_source: fresh_fetch
motivation: 现有的 VLA 模型在存在干扰物或物理障碍的复杂工作空间中容易执行失败，且传统的微调方法在处理未见过的干扰设置时效果有限。
method: 提出一种名为 StageCraft 的无需训练的方法，利用 VLM 对策略执行视频进行推理，识别并预先清理可能导致失败的干扰物或障碍。
result: "在三个真实世界任务领域中，StageCraft 使最先进的 VLA 模型性能绝对提升了 40%，并在仿真实验中展现了良好的可扩展性。"
conclusion: StageCraft 证明了利用互联网规模预训练的 VLM 进行环境干预，是提升现有机器人策略在复杂现实场景中成功率的一种高效且通用的方案。
---

## 摘要
在文本和图像数据上进行的大规模预训练，结合多样化的机器人演示，已帮助视觉-语言-动作模型（VLA）泛化到新的任务、物体和场景。然而，在机器人工作空间中存在干扰物和物理障碍等执行时阻碍的情况下，这些模型仍然容易发生失败。现有的策略改进方法通过微调基础 VLA 来提高泛化能力，但在未见过的干扰物设置中仍然表现不佳。为了解决这个问题，我们研究了是否可以利用互联网规模预训练的大型视觉语言模型（VLM）来对这些阻碍进行推理并缓解策略失败。为此，我们提出了 StageCraft，这是一种无需训练的方法，通过使用基于 VLM 的上下文推理来操纵环境的初始状态，从而提高预训练 VLA 策略的性能。StageCraft 将策略展开视频和成功标签作为输入，并利用 VLM 的推理能力来推断初始状态中哪些物体需要被操纵，以避免预期的执行失败。StageCraft 是一个可扩展的即插即用模块，不会对底层策略引入额外的约束，且仅需要少量的策略展开即可工作。我们评估了结合 StageCraft 的最先进 VLA 模型的性能，并展示了在涉及多种干扰物和障碍物的三个真实世界任务领域中，性能绝对提升了 40%。我们在 RLBench 中的仿真实验实证表明，StageCraft 根据底层策略的强度定制其干预程度，并随着上下文样本的增加而提高性能。StageCraft 的实际效果视频可在 https://stagecraft-decorator.github.io/stagecraft/ 找到。

## Abstract
Large scale pre-training on text and image data along with diverse robot demonstrations has helped Vision Language Action models (VLAs) to generalize to novel tasks, objects and scenes. However, these models are still susceptible to failure in the presence of execution-time impediments such as distractors and physical obstructions in the robot's workspace. Existing policy improvement methods finetune base VLAs to improve generalization, yet they still struggle in unseen distractor settings. To address this problem, we investigate whether internet-scale pretraining of large vision-language models (VLMs) can be leveraged to reason about these impediments and mitigate policy failures. To this end, we propose StageCraft, a training-free approach to improve pretrained VLA policy performance by manipulating the environment's initial state using VLM-based in-context reasoning. StageCraft takes policy rollout videos and success labels as input and leverages VLM's reasoning ability to infer which objects in the initial state need to be manipulated to avoid anticipated execution failures. StageCraft is an extensible plug-and-play module that does not introduce additional constraints on the underlying policy, and only requires a few policy rollouts to work. We evaluate performance of state-of-the-art VLA models with StageCraft and show an absolute 40% performance improvement across three real world task domains involving diverse distractors and obstructions. Our simulation experiments in RLBench empirically show that StageCraft tailors its extent of intervention based on the strength of the underlying policy and improves its performance with more in-context samples. Videos of StageCraft in effect can be found at https://stagecraft-decorator.github.io/stagecraft/ .

---

## 论文详细总结（自动生成）

### StageCraft：VLA 模型中干扰物和障碍物失败的执行感知缓解 —— 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作模型（VLA）虽然在泛化性上表现出色，但在面对现实世界中未见过的**干扰物（Distractors）**和**物理障碍（Obstructions）**时仍然非常脆弱。现有的改进方法通常依赖于对特定任务的微调，这不仅成本高昂，还容易导致“灾难性遗忘”。
**研究动机：** 借鉴人类在执行复杂任务前会先整理环境（如清理桌面）的习惯，论文提出：与其不断微调策略模型，不如在执行前利用大视觉语言模型（VLM）的推理能力，识别并移除可能导致失败的障碍物，从而将环境状态调整回策略模型可处理的分布内。

#### 2. 方法论：核心思想与技术细节
StageCraft 是一种**无需训练（Training-free）**、**即插即用**的模块，其核心流程如下：
*   **核心思想：** 利用 VLM 的上下文学习（In-context Learning）能力，通过分析少量的历史执行视频（包含成功和失败案例），推断出哪些物体是导致失败的“元凶”，并在正式执行任务前通过机械臂将其移除。
*   **关键技术步骤：**
    1.  **物体集合创建（Object-Set Creation）：** 收集少量策略在不同干扰物组合下的执行记录（视频+成功/失败标签）。VLM 对这些记录进行推理，识别出与高成功率相关的物体子集。
    2.  **物体集合转换（Object-Set Transition）：** 当面对一个新场景时，VLM 对比当前物体与历史成功案例，确定需要移除的最小物体集合 $X_{manip}$，以使环境状态转换到策略模型最擅长的配置。
    3.  **环境干预执行：** 使用 **SAM3** 模型进行物体检测和分割，结合相机标定参数将 2D 坐标映射到 3D 空间，最后通过逆运动学（IK）控制机械臂执行简单的“抓取并丢弃”动作。
    4.  **策略执行：** 在清理后的环境中运行原始 VLA 策略。

#### 3. 实验设计
*   **实验场景：** 
    *   **真实世界：** 使用 Franka FR3 机器人，涵盖三个任务：叠杯子（Stack Cups）、摆盘（Setup Plate）、球入碗（Block in Bowl）。
    *   **仿真环境：** 在 RLBench 中进行“抓取红杯子”任务，用于深入分析算法特性。
*   **基准模型（Benchmark）：** 选择了当前最先进的开源 VLA 模型：**Pi0.5** 和 **SmolVLA**。
*   **对比设置：**
    *   **Base：** 无干扰物的原始环境。
    *   **Distractor：** 存在 1-5 个干扰物但不使用 StageCraft。
    *   **StageCraft：** 存在干扰物并使用本文方法进行预处理。
*   **消融实验：** 测试了不同 VLM（Gemini 1.5 Pro, GPT-4o 等）的推理能力，以及上下文样本数量对性能的影响。

#### 4. 资源与算力
*   **模型微调：** 论文提到使用 LeRobot 库对 Pi0.5 和 SmolVLA 进行了全参数微调，每个任务收集了 60 条演示数据。
*   **推理算力：** StageCraft 本身无需训练。推理阶段使用了云端 VLM API（如 Gemini-1.5-Pro）。
*   **硬件细节：** 文中未明确给出微调 VLA 时具体的 GPU 型号和训练时长，但强调了 StageCraft 作为一个推理侧插件，对本地算力要求较低。

#### 5. 实验数量与充分性
*   **实验规模：** 真实世界实验中，每个任务评估了 15 次迭代；仿真实验中，针对不同强度的策略（弱策略 vs 强策略）进行了 100 次评估。
*   **充分性：** 实验设计较为全面，不仅验证了成功率的提升，还通过消融实验证明了“物体集合策略”优于直接让 VLM 盲目清理环境。
*   **客观性：** 引入了不同性能水平的基础策略（$\pi_{weak}$ 和 $\pi_{strong}$），证明了 StageCraft 能根据策略本身的鲁棒性自动调整干预强度（策略越弱，干预越多），体现了实验的深度。

#### 6. 主要结论与发现
*   **性能大幅提升：** 在三个真实任务中，StageCraft 使 VLA 模型的成功率**绝对提升了 40%**，有效恢复了模型在受干扰环境下的表现。
*   **自适应干预：** StageCraft 具有“执行感知”能力，如果基础策略本身足够强，它会减少干预动作；反之则增加。
*   **样本效率：** 仅需 10 个左右的上下文示例，VLM 就能准确识别干扰物。随着样本增加，干预的精准度和策略成功率进一步提高。
*   **VLM 依赖性：** 实验发现，更先进的 VLM（如 Gemini 1.5 Pro）在遵循复杂指令和保持物体识别一致性方面显著优于旧版本。

#### 7. 优点
*   **无需重新训练：** 避免了昂贵的数据收集和微调过程，具有极强的通用性。
*   **即插即用：** 可以作为任何 VLA 模型的“装饰器”（Decorator），不改变原模型结构。
*   **逻辑严密：** 提出的“物体集合转换”策略比简单的提示词工程更稳定，减少了误删任务相关物体的风险。

#### 8. 不足与局限
*   **VLM 窗口限制：** 随着历史记录（视频帧）增加，会迅速消耗 VLM 的上下文窗口长度，限制了长期记忆的扩展。
*   **物体定义局限：** 目前仅适用于可离散化为“物体集合”的环境，对于非结构化障碍（如散落的粉末、液体或大面积遮挡）可能难以处理。
*   **执行开销：** 预清理动作虽然提高了成功率，但也增加了完成任务的总时长。
*   **闭环缺失：** 目前主要针对初始状态进行干预，若在执行过程中产生新的干扰，该模块无法实时响应。

（完）
