---
title: "Fast-WAM: Do World Action Models Need Test-time Future Imagination?"
title_zh: Fast-WAM：世界动作模型在测试时需要未来想象吗？
authors: "Tianyuan Yuan, Zibin Dong, Yicheng Liu, Hang Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16666v2"
tags: ["query:robot"]
score: 10.0
evidence: 用于机器人规划与控制的世界动作模型
tldr: 本研究针对世界动作模型（WAM）因测试时迭代视频生成导致的高延迟问题，提出了 Fast-WAM。该模型在训练阶段保留视频协同训练以学习强大的世界表示，但在推理阶段跳过显式的未来想象直接预测动作。实验表明，Fast-WAM 在多个仿真和真实任务中达到了 SOTA 水平，且推理速度比传统 WAM 快 4 倍以上，证明了视频建模的核心价值在于训练阶段的表征学习而非推理阶段的生成。
selection_source: fresh_fetch
motivation: 探究世界动作模型在推理阶段是否真正需要显式的未来图像生成，以解决现有模型测试时延迟过高的问题。
method: 提出 Fast-WAM 架构，通过在训练时进行视频协同建模来强化特征表示，但在测试时仅执行动作预测而跳过视频生成。
result: Fast-WAM 在 LIBERO 和 RoboTwin 等基准测试中表现优异，推理延迟仅为 190ms，比传统“先想象后执行”模型快 4 倍以上。
conclusion: 视频预测对 WAM 的主要贡献在于训练期间提升表征质量，而非推理时的显式想象，这为开发高效具身智能模型提供了新思路。
---

## 摘要
世界动作模型 (WAMs) 已成为具身控制中视觉-语言-动作 (VLA) 模型的一种极具前景的替代方案，因为它们显式地建模了视觉观测在动作作用下如何演化。大多数现有的 WAM 遵循“先想象后执行”的范式，由于迭代视频去噪导致了显著的测试时延迟，然而目前尚不清楚显式的未来想象对于强大的动作性能是否真的必要。在本文中，我们探讨了 WAM 是否在测试时需要显式的未来想象，或者其优势是否主要源于训练期间的视频建模。通过提出 Fast-WAM，我们将训练期间的视频建模作用与推理期间的显式未来生成解耦。Fast-WAM 是一种在训练时保留视频协同训练但在测试时跳过未来预测的 WAM 架构。我们进一步实例化了多个 Fast-WAM 变体，以便对这两个因素进行受控比较。在这些变体中，我们发现 Fast-WAM 与“先想象后执行”变体相比仍具竞争力，而移除视频协同训练则会导致更大幅度的性能下降。实验表明，在没有具身预训练的情况下，Fast-WAM 在模拟基准测试（LIBERO 和 RoboTwin）以及现实世界任务中均取得了与最先进方法相当的结果。它的运行延迟为 190 毫秒，实现了实时运行，比现有的“先想象后执行”型 WAM 快 4 倍以上。这些结果表明，WAM 中视频预测的主要价值可能在于提高训练期间的世界表示能力，而非在测试时生成未来观测。项目页面：https://yuantianyuan01.github.io/FastWAM/

## Abstract
World Action Models (WAMs) have emerged as a promising alternative to Vision-Language-Action (VLA) models for embodied control because they explicitly model how visual observations may evolve under action. Most existing WAMs follow an imagine-then-execute paradigm, incurring substantial test-time latency from iterative video denoising, yet it remains unclear whether explicit future imagination is actually necessary for strong action performance. In this paper, we ask whether WAMs need explicit future imagination at test time, or whether their benefit comes primarily from video modeling during training. We disentangle the role of video modeling during training from explicit future generation during inference by proposing \textbf{Fast-WAM}, a WAM architecture that retains video co-training during training but skips future prediction at test time. We further instantiate several Fast-WAM variants to enable a controlled comparison of these two factors. Across these variants, we find that Fast-WAM remains competitive with imagine-then-execute variants, while removing video co-training causes a much larger performance drop. Empirically, Fast-WAM achieves competitive results with state-of-the-art methods both on simulation benchmarks (LIBERO and RoboTwin) and real-world tasks, without embodied pretraining. It runs in real time with 190ms latency, over 4$\times$ faster than existing imagine-then-execute WAMs. These results suggest that the main value of video prediction in WAMs may lie in improving world representations during training rather than generating future observations at test time. Project page: https://yuantianyuan01.github.io/FastWAM/

---

## 论文详细总结（自动生成）

这是一份关于论文《Fast-WAM: Do World Action Models Need Test-time Future Imagination?》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：世界动作模型（World Action Models, WAMs）通常采用“先想象后执行”的范式，即先生成未来视觉预测，再根据预测生成动作。这导致测试时（Inference）由于需要迭代进行视频去噪，产生巨大的延迟。
*   **研究动机**：作者质疑“显式的未来想象”在测试阶段是否真的必要。他们试图探究 WAM 的性能提升究竟是源于**训练时的视频建模目标**（学习更好的物理表征），还是源于**推理时的显式未来生成**（提供前瞻信息）。

### 2. 方法论：Fast-WAM
*   **核心思想**：将训练时的视频协同训练（Video Co-training）与推理时的显式生成解耦。在训练时保留视频预测任务以强化表征，但在推理时跳过视频生成，直接从潜空间表征预测动作。
*   **关键技术细节**：
    *   **架构设计**：采用混合 Transformer（Mixture-of-Transformer, MoT）架构，包含一个预训练的视频 DiT（基于 Wan2.2-5B）和一个动作专家 DiT（1B）。
    *   **结构化注意力掩码**：在训练时，动作 Token 可以访问当前帧 Token，但被禁止访问未来的视频 Token，以防止信息泄露。视频 Token 则进行双向建模。
    *   **推理流程**：推理时完全移除未来视频分支。视频 DiT 仅对当前观测进行单次前向传播，提取潜空间特征，随后动作专家基于该特征直接生成动作序列。
    *   **优化目标**：使用联合流匹配（Flow Matching）目标函数。总损失函数为动作损失 $L_{act}$ 和视频损失 $L_{vid}$ 的加权和：$L = L_{act} + \lambda L_{vid}$。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务，评估模型的空间推理和长程规划能力。
    *   **RoboTwin 2.0**：具有挑战性的双臂协同操作基准，包含 50 多个任务，涵盖清洁场景和重度随机化场景。
    *   **真实世界任务**：折叠毛巾（Towel Folding），这是一个涉及可变形物体、长路径且需要闭环控制的复杂任务。
*   **对比方法**：
    *   **VLA 模型**：OpenVLA, $\pi_0$, $\pi_{0.5}$。
    *   **WAM 模型**：Motus, LingBot-VA。
    *   **受控变体（消融）**：Fast-WAM-Joint（联合去噪）、Fast-WAM-IDM（先视频后动作）、Fast-WAM w.o. video co-train（无视频协同训练）。

### 4. 资源与算力
*   **模型规模**：总参数量约 6B（5B 视频主干 + 1B 动作专家）。
*   **算力设备**：推理延迟在单张 **NVIDIA RTX 5090D V2 (32GB)** 上测量。
*   **训练时长**：LIBERO 训练 20k 步，RoboTwin 和真实世界任务训练 30k 步。
*   **延迟表现**：Fast-WAM 的推理延迟仅为 **190ms**，比传统的“先想象后执行”模型（如 810ms）快了 4 倍以上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从仿真到真实的多种任务，LIBERO 进行了 2000 次测试，RoboTwin 每项任务 100 次测试，实验数据量较大。
*   **充分性与公平性**：
    *   作者通过构建三个受控变体（Joint, IDM, No-video），在相同的模型主干和训练配置下进行对比，有效地隔离了“训练目标”和“推理生成”两个变量。
    *   实验对比了在有无“具身预训练（Embodied Pretraining）”情况下的表现，证明了 Fast-WAM 在数据效率上的优势。

### 6. 主要结论与发现
*   **视频建模的价值在于训练**：实验表明，移除视频协同训练会导致性能大幅下降（如 RoboTwin 从 91.8% 降至 83.8%），而推理时跳过视频生成对性能的影响微乎其微。
*   **实时性优势**：Fast-WAM 证明了 WAM 可以在不牺牲性能的前提下实现实时控制（190ms 延迟）。
*   **数据效率**：即使没有大规模具身数据预训练，Fast-WAM 也能在仿真和真实任务中达到或超过 SOTA 模型的水平。

### 7. 优点
*   **高效性**：解决了 WAM 模型在实际机器人部署中因视频生成导致的延迟瓶颈。
*   **科学性**：通过严谨的解耦实验，回答了具身智能领域关于“世界模型是否需要显式想象”的一个基础性问题。
*   **通用性**：在双臂操作、可变形物体处理等多种复杂任务上表现稳健。

### 8. 不足与局限
*   **主干依赖**：模型仍然依赖于较大的预训练视频 DiT（5B），对于算力极度受限的嵌入式设备可能仍有压力。
*   **长程预测的极端情况**：虽然在当前任务中显式想象不重要，但在某些需要极远距离因果推理或极端非线性变化的场景下，显式生成是否仍无必要尚待进一步验证。
*   **预训练数据**：论文主要强调了视频协同训练的作用，但尚未深入探讨不同规模和质量的视频数据对最终动作性能的量化影响。

（完）
