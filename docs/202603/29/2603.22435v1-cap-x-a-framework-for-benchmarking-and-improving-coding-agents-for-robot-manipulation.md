---
title: "CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation"
title_zh: CaP-X：一个用于基准测试和改进机器人操纵编程智能体的框架
authors: "Max Fu, Justin Yu, Karim El-Refai, Ethan Kou, Haoru Xue, Huang Huang, Wenli Xiao, Guanzhi Wang, Fei-Fei Li, Guanya Shi, Jiajun Wu, Shankar Sastry, Yuke Zhu, Ken Goldberg, Linxi \"Jim\" Fan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22435v1"
tags: ["query:robot"]
score: 8.0
evidence: 在VLA方法背景下评估代码即策略智能体
tldr: 本研究针对“代码即策略”（CaP）在机器人操作中的应用，提出了CaP-X开源框架。该框架包含交互式环境CaP-Gym和评估基准CaP-Bench，对12种前沿模型进行了系统评估。研究发现，虽然模型性能依赖人工抽象，但通过多轮交互、执行反馈和自动技能合成等测试时计算手段，可显著提升鲁棒性。基于此，作者开发了CaP-Agent0和CaP-RL，在仿真和真实机器人上均实现了高可靠性。
selection_source: fresh_fetch
motivation: 旨在解决“代码即策略”方法在机器人操作中缺乏系统评估，且过度依赖人工预设抽象的问题。
method: 提出了包含交互环境、评估基准、免训练代理及强化学习算法的综合性框架CaP-X。
result: 实验证明通过增加测试时计算（如反馈和多轮推理）能有效弥补低级原语带来的性能损失。
conclusion: CaP-X为提升机器人编码代理的自主性和可靠性提供了一个标准化的开源平台。
---

## 摘要
“代码即策略”（Code-as-Policy）探讨了可执行代码如何补充数据密集型的视觉-语言-动作（VLA）方法，但其作为具身操纵自主控制器的有效性仍有待深入研究。我们提出了 CaP-X，这是一个用于系统研究机器人操纵中“代码即策略”智能体的开源框架。其核心是 CaP-Gym，这是一个交互式环境，智能体通过合成并执行组合了感知和控制原语的程序来控制机器人。在此基础上，CaP-Bench 在不同的抽象级别、交互程度和感知接地（perceptual grounding）方面评估了前沿的语言和视觉-语言模型。在 12 个模型中，CaP-Bench 揭示了一个一致的趋势：性能随着人工设计的抽象而提高，但随着这些先验知识的移除而下降，暴露了对设计者脚手架（scaffolding）的依赖。同时，我们观察到这种差距可以通过扩展智能体测试时计算（test-time computation）来弥补——通过多轮交互、结构化执行反馈、视觉差异化、自动技能合成和集成推理——即使智能体在底层原语上运行，也能显著提高鲁棒性。这些发现使我们推导出 CaP-Agent0，这是一个无需训练的框架，在仿真和真实具身环境下的多项操纵任务中恢复了人类水平的可靠性。我们进一步引入了 CaP-RL，展示了带有可验证奖励的强化学习可以提高成功率，并能以极小的差距实现从仿真到现实（sim2real）的迁移。总之，CaP-X 为推进具身编程智能体提供了一个规范的、开源的平台。

## Abstract
"Code-as-Policy" considers how executable code can complement data-intensive Vision-Language-Action (VLA) methods, yet their effectiveness as autonomous controllers for embodied manipulation remains underexplored. We present CaP-X, an open-access framework for systematically studying Code-as-Policy agents in robot manipulation. At its core is CaP-Gym, an interactive environment in which agents control robots by synthesizing and executing programs that compose perception and control primitives. Building on this foundation, CaP-Bench evaluates frontier language and vision-language models across varying levels of abstraction, interaction, and perceptual grounding. Across 12 models, CaP-Bench reveals a consistent trend: performance improves with human-crafted abstractions but degrades as these priors are removed, exposing a dependence on designer scaffolding. At the same time, we observe that this gap can be mitigated through scaling agentic test-time computation--through multi-turn interaction, structured execution feedback, visual differencing, automatic skill synthesis, and ensembled reasoning--substantially improves robustness even when agents operate over low-level primitives. These findings allow us to derive CaP-Agent0, a training-free framework that recovers human-level reliability on several manipulation tasks in simulation and on real embodiments. We further introduce CaP-RL, showing reinforcement learning with verifiable rewards improves success rates and transfers from sim2real with minimal gap. Together, CaP-X provides a principled, open-access platform for advancing embodied coding agents.

---

## 论文详细总结（自动生成）

### CaP-X：机器人操纵编程智能体基准测试与改进框架总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文探讨了“代码即策略”（Code-as-Policy, CaP）在机器人操纵中的有效性。背景是当前的机器人控制主要分为两派：一是依赖人工编写复杂逻辑的传统控制，二是依赖海量数据的视觉-语言-动作（VLA）模型。
*   **核心问题**：现有的 CaP 方法高度依赖人类预设的高级原语（如 `stack_objects()`），这掩盖了大型语言模型（LLM）在底层控制和感知推理上的真实能力。
*   **研究动机**：系统性地研究当移除这些“人工脚手架”时，编程智能体的表现如何，以及是否可以通过增加“测试时计算”（Test-time Computation）来弥补性能差距。

#### 2. 论文提出的方法论
论文提出了 **CaP-X** 框架，包含以下核心组件：
*   **CaP-Gym**：一个分层控制环境，将物理仿真器（Robosuite, LIBERO, BEHAVIOR）与代码执行循环（REPL）绑定，支持从底层（关节/位姿）到高层（语义）的原语调用。
*   **CaP-Bench**：一个多维度的评估基准，从**抽象级别**（高层 vs 底层）、**时间交互**（单轮 vs 多轮）和**感知接地**（文本反馈 vs 原始图像 vs 视觉差异）三个维度评估模型。
*   **CaP-Agent0**：一个免训练的智能体框架，关键技术包括：
    *   **视觉差异模块（VDM）**：将视觉观察转化为结构化自然语言，描述前后帧差异。
    *   **自动合成技能库**：从成功的底层执行轨迹中自动提取并固化通用的工具函数。
    *   **并行推理与集成**：同时采样多个候选代码，由中心智能体进行合成。
*   **CaP-RL**：利用可验证奖励（RLVR）和 GRPO 算法对编码模型进行在线强化学习，提升代码生成的成功率。

#### 3. 实验设计
*   **数据集/场景**：集成了 187 个任务，涵盖桌面操纵、双臂协作和移动操纵。
*   **Benchmark 设计**：
    *   **S1-S4（单轮）**：从特权状态（S1）到无示例的底层 API（S4）。
    *   **M1-M4（多轮）**：引入执行错误反馈（M1）、多模态输入（M2）和 VDM 反馈（M3/M4）。
*   **对比方法**：评估了 12 种前沿模型（包括 GPT-5.2、Gemini-3-Pro、Claude 4.5、DeepSeek-V3.1、Qwen-235B 等），并与人类专家编写的代码以及 OpenVLA、$\pi_0$ 等 VLA 模型进行对比。
*   **真实世界验证**：在 Franka Panda 和 AgiBot G1 机器人上进行了零样本（Zero-shot）任务测试。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和总算力消耗。
*   **训练细节**：提到 CaP-RL 过程中对 Qwen2.5-Coder-7B 进行了 50 次迭代的强化学习训练。由于 CaP-Agent0 是免训练的，其主要开销在于推理时的 API 调用。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务/层级进行了 **100 次独立试验**，这在机器人论文中属于较高标准，能有效减少随机性干扰。
*   **覆盖面**：涵盖了从简单的方块提升到复杂的双臂交接、移动导航及真实世界中的数学题求解、机械搜索等。
*   **消融实验**：对 CaP-Agent0 的各个组件（技能库、多轮反馈、并行查询）进行了详细的消融分析，验证了各部分的贡献。实验设计客观、公平，对比基准统一。

#### 6. 论文的主要结论与发现
*   **性能差距**：前沿模型在底层原语上的表现仍显著落后于人类专家，且高度依赖人工设计的抽象层。
*   **测试时计算的威力**：通过多轮交互、视觉差异文本反馈和集成推理，底层原语的成功率可以大幅提升，甚至在某些任务上达到人类水平。
*   **感知接地的形式**：直接输入原始图像（M2）的效果反而不如经过 VDM 转化后的文本描述（M3），说明当前模型在代码合成与视觉理解的跨模态对齐上仍有挑战。
*   **Sim-to-Real 潜力**：CaP-RL 训练的模型在仿真中提升明显，且由于基于抽象 API 推理，可以几乎无损地迁移到真实机器人。

#### 7. 优点
*   **系统性强**：首次系统性地解构了 CaP 框架中“抽象级别”对模型表现的影响。
*   **开源贡献**：提供了统一的 CaP-Gym 环境，降低了后续研究具身编程智能体的门槛。
*   **方法实用**：CaP-Agent0 无需针对特定任务训练，展现了极强的通用性和零样本迁移能力。
*   **闭环反馈**：强调了执行反馈（stdout/stderr）和视觉差异在纠错中的核心作用。

#### 8. 不足与局限
*   **实时性限制**：由于依赖多轮推理和复杂的 VDM 转化，系统在处理需要高频反馈（如接触密集型任务、流体倾倒）时可能存在延迟。
*   **感知依赖**：虽然减少了控制抽象，但仍依赖于 SAM3 或 Molmo 等感知原语的准确性，感知失败会导致整个链条崩溃。
*   **任务复杂度**：虽然涉及了长程任务，但对于极度复杂的动态环境（如多人协作、高速运动物体）的讨论较少。

（完）
