---
title: "Vega: Learning to Drive with Natural Language Instructions"
title_zh: Vega：利用自然语言指令学习驾驶
authors: "Sicheng Zuo, Yuxuan Li, Wenzhao Zheng, Zheng Zhu, Jie Zhou, Jiwen Lu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25741v1"
tags: ["query:robot"]
score: 9.0
evidence: 用于自动驾驶的统一视觉-语言-世界-动作模型
tldr: 本研究针对现有自动驾驶模型缺乏遵循多样化用户指令能力的问题，提出了Vega模型。首先构建了包含10万个场景的大规模指令驾驶数据集InstructScene。Vega采用统一的视觉-语言-世界-动作架构，结合自回归模型处理多模态输入，并利用扩散模型生成未来预测和行驶轨迹。实验证明，Vega在规划性能和指令遵循方面表现优异，为实现智能化和个性化驾驶提供了新路径。
selection_source: fresh_fetch
motivation: 现有自动驾驶模型多将语言用于场景描述，缺乏根据用户多样化指令进行个性化驾驶的能力。
method: 提出了Vega模型，结合自回归范式处理视听输入与扩散范式生成预测及轨迹，并构建了InstructScene数据集。
result: 实验结果显示Vega在自动驾驶规划性能上达到领先水平，并展现出极强的指令遵循能力。
conclusion: 该研究通过整合多模态指令遵循能力，为开发更智能、更个性化的自动驾驶系统奠定了基础。
---

## 摘要
视觉-语言-动作模型重塑了自动驾驶，将语言融入到决策过程中。然而，大多数现有流程仅利用语言模态进行场景描述或推理，缺乏遵循多样化用户指令以实现个性化驾驶的灵活性。为解决这一问题，我们首先构建了一个大规模驾驶数据集（InstructScene），包含约 100,000 个场景，并标注了多样化的驾驶指令及其对应的轨迹。随后，我们提出了一种统一的视觉-语言-世界-动作模型 Vega，用于基于指令的生成与规划。我们采用自回归范式处理视觉输入（视觉）和语言指令（语言），并采用扩散范式生成未来预测（世界建模）和轨迹（动作）。我们通过联合注意力机制实现模态间的交互，并为不同模态使用独立的投影层以增强能力。大量实验表明，我们的方法不仅实现了卓越的规划性能，还展现出强大的指令遵循能力，为更智能、更个性化的驾驶系统铺平了道路。

## Abstract
Vision-language-action models have reshaped autonomous driving to incorporate languages into the decision-making process. However, most existing pipelines only utilize the language modality for scene descriptions or reasoning and lack the flexibility to follow diverse user instructions for personalized driving. To address this, we first construct a large-scale driving dataset (InstructScene) containing around 100,000 scenes annotated with diverse driving instructions with the corresponding trajectories. We then propose a unified Vision-Language-World-Action model, Vega, for instruction-based generation and planning. We employ the autoregressive paradigm to process visual inputs (vision) and language instructions (language) and the diffusion paradigm to generate future predictions (world modeling) and trajectories (action). We perform joint attention to enable interactions between the modalities and use individual projection layers for different modalities for more capabilities. Extensive experiments demonstrate that our method not only achieves superior planning performance but also exhibits strong instruction-following abilities, paving the way for more intelligent and personalized driving systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Vega** 的统一视觉-语言-世界-动作模型，旨在解决自动驾驶中如何精准遵循多样化自然语言指令的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自动驾驶视觉-语言-动作（VLA）模型大多将语言仅用于“场景描述”或“逻辑推理”，缺乏遵循用户多样化、开放式指令（如“靠边停车”、“超车以赶上绿灯”）的灵活性。
*   **研究动机**：传统的模仿学习（Imitation Learning）倾向于学习一种平均的专家策略，无法满足个性化驾驶需求。作者认为，从“模仿驾驶”向“指令驾驶”转变的关键在于建立高维指令与低维动作之间的映射，并引入“世界模型”提供的稠密监督信号来弥补动作监督稀疏的问题。

### 2. 方法论
*   **核心思想**：将自动驾驶建模为一个统一的生成任务。模型同时预测未来的驾驶动作（Action）和未来的场景图像（World Modeling），通过预测未来图像来迫使模型理解指令、动作与环境演变之间的因果关系。
*   **关键技术细节**：
    *   **混合架构**：采用自回归（Autoregressive, AR）与扩散（Diffusion）结合的 Transformer 架构。AR 范式用于理解视觉和文本输入，扩散范式用于生成高保真未来图像和精确的动作轨迹。
    *   **混合 Transformer (MoT)**：为了有效解耦不同模态的处理，模型采用了 MoT 设计，为视觉理解、文本理解、图像生成和动作规划分配独立的 Transformer 参数层，从而提升多任务并行处理的能力。
    *   **因果序列建模**：输入序列按“历史图像 -> 历史动作 -> 驾驶指令 -> 噪声动作 -> 噪声未来图像”排列，利用因果注意力机制确保信息流向符合物理逻辑。
    *   **InstructScene 数据集**：基于 NAVSIM 构建，包含约 10 万个场景。利用强大的 VLM（如 Qwen2.5-VL）结合基于规则的方法，自动生成描述驾驶意图的多样化指令。

### 3. 实验设计
*   **数据集与场景**：主要在 **NAVSIM v1** 和 **NAVSIM v2** 两个基准上进行评估。NAVSIM v2 引入了更具挑战性的反应式交通流模拟。
*   **Benchmark 指标**：包括 PDMS（预测驾驶模型得分）、EPDMS（扩展得分）、碰撞率（NC）、可行驶区域合规性（DAC）、舒适度等。
*   **对比方法**：对比了 TransFuser、UniAD、HydraMDP++、DriveVLA-W0、AutoVLA 等多种 SOTA（最先进）方法，涵盖了基于 BEV 的方法和基于 VLA 的方法。

### 4. 资源与算力
*   **算力使用**：论文明确指出，模型在 **8 台 H20 GPU** 上进行了训练。
*   **训练时长**：总计训练了 **200,000 个步数（steps）**，采用单阶段训练范式，每台设备的 Batch Size 为 1。

### 5. 实验数量与充分性
*   **实验规模**：使用了约 10 万个标注场景进行训练，规模较大。
*   **消融实验**：作者进行了多组消融实验，验证了：
    1.  **未来帧预测任务**：证明了引入图像生成任务确实能提升规划性能。
    2.  **交织序列长度**：探讨了历史图像与动作交织输入对收敛速度的影响。
    3.  **独立动作模块**：验证了使用专门的动作专家层优于直接使用 VLM 或扩散模块。
*   **充分性评价**：实验设计较为全面，涵盖了定量指标对比、定性可视化分析以及针对核心组件的有效性验证，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：Vega 在 NAVSIM v2 上达到了 86.9 的 EPDMS，在使用 Best-of-N 策略后提升至 89.4，超越了现有的 SOTA 方法。
*   **指令遵循能力**：Vega 能够根据不同的指令（如“加速赶超” vs “保持跟车”）生成截然不同的轨迹和对应的未来预测图像，展现了极强的多模态对齐能力。
*   **世界模型的作用**：证明了像素级的稠密监督信号（未来图像预测）能显著改善模型在复杂动态环境下的动作规划质量。

### 7. 优点与亮点
*   **统一框架**：成功将视觉理解、语言指令、世界建模和动作规划整合进一个 Transformer 模型中。
*   **高质量数据集**：构建并开源了 InstructScene，填补了自动驾驶领域大规模指令对齐数据的空白。
*   **解耦设计**：通过 MoT 架构解决了多模态参数干扰问题，兼顾了生成质量与规划精度。

### 8. 不足与局限
*   **指标偏差**：在 NAVSIM v1 上的表现略逊于部分 VLA 方法，作者分析这可能是由于 v1 的评分机制过于偏向保守策略，而 Vega 学习到了更多样化的驾驶行为。
*   **输入限制**：目前主要依赖前视摄像头图像，虽然性能强劲，但在处理侧方或后方盲区指令时可能存在局限。
*   **推理延迟**：由于采用了扩散模型进行生成，虽然图像生成与动作规划是解耦的，但整体推理链路（尤其是生成未来图像时）可能比纯回归模型更耗时。

（完）
