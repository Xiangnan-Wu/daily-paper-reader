---
title: "SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models"
title_zh: SABER：一种针对视觉-语言-动作（VLA）模型的隐蔽代理式黑盒攻击框架
authors: "Xiyang Wu, Guangyao Shi, Qingzi Wang, Zongxia Li, Amrit Singh Bedi, Dinesh Manocha"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24935v1"
tags: ["query:robot"]
score: 9.0
evidence: 针对视觉-语言-动作（VLA）模型的对抗性攻击
tldr: 针对视觉-语言-动作（VLA）模型对指令扰动的敏感性，本文提出了SABER框架。这是一种基于智能体的黑盒攻击方法，利用经过GRPO训练的ReAct攻击者，在有限的编辑预算内生成微小且合理的指令修改。SABER能有效诱导机器人任务失败、增加执行时长或违反约束。实验表明，该方法在LIBERO基准测试中显著降低了多种SOTA VLA模型的性能，为机器人基础模型的红队测试提供了高效、可扩展的方案。
selection_source: fresh_fetch
motivation: VLA模型容易受到微小文本扰动的影响，因此需要一种自动化的黑盒攻击工具来系统地评估其在对抗性指令下的鲁棒性。
method: 开发了SABER框架，通过GRPO训练的ReAct智能体调用多层级编辑工具，在极小预算下生成能诱导目标行为退化的对抗性指令。
result: 在LIBERO基准测试中，SABER显著降低了六种主流VLA模型的成功率并增加了违规行为，且编辑效率远高于GPT等强基准模型。
conclusion: 研究证明微小的指令修改足以破坏机器人任务执行，且基于智能体的黑盒攻击是进行机器人基础模型红队测试的有效且实用的途径。
---

## 摘要
视觉-语言-动作（VLA）模型使机器人能够遵循基于视觉观测的自然语言指令，但指令通道也引入了一个关键漏洞：微小的文本扰动即可改变下游机器人的行为。因此，系统的鲁棒性评估需要一种黑盒攻击者，能够在不同的 VLA 模型中生成极小但有效的指令编辑。为此，我们提出了 SABER，这是一种以代理为中心的方法，用于在受限的编辑预算下自动生成针对 VLA 模型的基于指令的对抗性攻击。SABER 使用经过 GRPO 训练的 ReAct 攻击者，在受限的编辑预算下，利用字符级、标记级和提示级工具生成微小且合理的对抗性指令编辑，从而诱导目标行为退化，包括任务失败、不必要的超长执行以及增加的约束违反。在涵盖六种最先进 VLA 模型的 LIBERO 基准测试中，SABER 使任务成功率降低了 20.6%，动作序列长度增加了 55%，约束违反增加了 33%，同时与强大的基于 GPT 的基准相比，所需的工具调用减少了 21.1%，字符编辑减少了 54.7%。这些结果表明，微小且合理的指令编辑足以显著降低机器人的执行性能，并且代理式黑盒流水线为机器人基础模型的红队测试提供了一种实用、可扩展且自适应的方法。

## Abstract
Vision-language-action (VLA) models enable robots to follow natural-language instructions grounded in visual observations, but the instruction channel also introduces a critical vulnerability: small textual perturbations can alter downstream robot behavior. Systematic robustness evaluation therefore requires a black-box attacker that can generate minimal yet effective instruction edits across diverse VLA models. To this end, we present SABER, an agent-centric approach for automatically generating instruction-based adversarial attacks on VLA models under bounded edit budgets. SABER uses a GRPO-trained ReAct attacker to generate small, plausible adversarial instruction edits using character-, token-, and prompt-level tools under a bounded edit budget that induces targeted behavioral degradation, including task failure, unnecessarily long execution, and increased constraint violations. On the LIBERO benchmark across six state-of-the-art VLA models, SABER reduces task success by 20.6%, increases action-sequence length by 55%, and raises constraint violations by 33%, while requiring 21.1% fewer tool calls and 54.7% fewer character edits than strong GPT-based baselines. These results show that small, plausible instruction edits are sufficient to substantially degrade robot execution, and that an agentic black-box pipeline offers a practical, scalable, and adaptive approach for red-teaming robotic foundation models.

---

## 论文详细总结（自动生成）

以下是对论文《SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：视觉-语言-动作（VLA）模型通过自然语言指令控制机器人，但这种语言接口存在严重的安全漏洞。微小的文本扰动（如拼写错误或词语替换）可能导致机器人执行失败、效率低下或违反安全约束。
*   **核心问题**：现有的攻击方法多依赖手动设计、固定启发式规则或昂贵的 GPT 查询，缺乏一种能够针对“黑盒”且“冻结”的 VLA 模型自动生成隐蔽、高效且低成本对抗指令的通用框架。
*   **整体含义**：论文提出了 SABER 框架，旨在通过智能体（Agentic）方法，在极小的编辑预算下，自动发现能诱导机器人行为退化的指令扰动，为机器人基础模型的红队测试（Red-teaming）提供工具。

### 2. 方法论
*   **核心思想**：将对抗指令生成建模为一个受限优化问题，利用一个基于 **ReAct** 模式的智能体作为攻击者，通过多轮对话调用编辑工具来修改指令。
*   **关键技术细节**：
    *   **两阶段工作流 (FIND → APPLY)**：智能体首先寻找（FIND）指令中适合修改的位置，然后应用（APPLY）具体的扰动操作。
    *   **扰动工具箱**：包含字符级（如拼写错误）、标记级（如近义词替换、属性交换）和提示级（如插入冗余约束或验证子句）三种粒度的工具。
    *   **训练算法 (GRPO)**：使用群体相对策略优化（Group Relative Policy Optimization, GRPO）算法训练攻击智能体。该算法无需访问目标 VLA 模型的梯度，仅通过机器人执行后的反馈（奖励信号）进行学习。
    *   **奖励函数设计**：奖励由“攻击目标达成度”（如任务失败、动作序列增加、约束违反增加）减去“隐蔽性惩罚”（编辑距离和工具调用次数）组成。

### 3. 实验设计
*   **数据集/场景**：使用机器人操作基准测试 **LIBERO**，涵盖四个子集：LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程任务）。
*   **目标模型 (Victim Models)**：针对 6 种最先进的 VLA 模型进行了评估，包括 $\pi_0$、$\pi_{0.5}$、GR00T-N1.5、X-VLA、InternVLA-M1 和 DeepThinkVLA。
*   **对比方法**：主要对比了基于 **GPT-5 mini** 的强力攻击基准（使用相同的工具调用接口但未经过针对性强化学习训练）。

### 4. 资源与算力
*   **模型规格**：攻击智能体采用 **Qwen2.5-3B-Instruct** 作为骨干网络，并使用 **LoRA** 进行微调。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了使用 ART 框架进行训练，并采用了冷启动（Cold-start）数据收集阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 6 种不同的 VLA 模型上进行了跨任务测试，涵盖了任务失败、动作膨胀和约束违反三个攻击目标。
*   **消融实验**：进行了“冷启动（SFT）+ GRPO”与“仅 GRPO”的对比实验，证明了冷启动对于稳定智能体学习工具调用逻辑的必要性。
*   **充分性评价**：实验设计较为充分，不仅验证了攻击的有效性，还通过“隐蔽性指标”（字符编辑数、工具调用数）评估了攻击的效率。跨模型的测试证明了该方法具有良好的迁移性和客观性。

### 6. 主要结论与发现
*   **攻击有效性**：SABER 平均使任务成功率降低了 **20.6%**，使动作序列长度增加了 **55%**，使约束违反增加了 **33%**。
*   **隐蔽性与效率**：相比 GPT 基准，SABER 在达成相似攻击效果的前提下，减少了 **21.1%** 的工具调用和 **54.7%** 的字符编辑，证明其学到了更高杠杆、更难察觉的攻击策略。
*   **脆弱性分布**：规划密集型任务（如长程任务）比简单的感知任务更容易受到指令扰动的影响。

### 7. 优点
*   **黑盒自适应**：无需目标模型的内部参数或梯度，具有极强的通用性。
*   **多目标优化**：不仅关注二元的“成功/失败”，还关注执行效率和物理安全约束，更符合机器人实际应用场景。
*   **高效隐蔽**：通过强化学习优化了编辑预算，生成的对抗指令更接近人类自然的输入错误或表达偏差。

### 8. 不足与局限
*   **模态局限**：目前仅限于文本指令的扰动，尚未涉及视觉观测（图像）的对抗性攻击。
*   **环境局限**：实验主要在 LIBERO 仿真环境中完成，尚未在复杂的现实物理世界中验证攻击的鲁棒性。
*   **防御缺失**：论文重点在于“如何攻击”，对于如何通过对抗训练或指令过滤来防御此类攻击的讨论较少。

（完）
