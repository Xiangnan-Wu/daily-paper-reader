---
title: "Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning"
title_zh: 持久性机器人世界模型：通过强化学习稳定多步外推
authors: "Jai Bardhan, Patrik Drozdik, Josef Sivic, Vladimir Petrik"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25685v1"
tags: ["query:robot"]
score: 10.0
evidence: 通过强化学习稳定动作条件机器人世界模型的多步推演
tldr: 针对机器人世界模型在长程自回归预测中容易出现误差累积和视觉质量退化的问题，本文提出了一种基于强化学习（RL）的后训练方案。该方法通过在模型自身的预测序列上进行训练，并结合对比RL目标、多视图视觉保真度奖励以及变长未来对比协议，显著提升了多步预测的稳定性。在DROID数据集上的实验表明，该方法在各项指标上均达到SOTA，并获得了极高的人类偏好率。
selection_source: fresh_fetch
motivation: 传统的机器人世界模型在长程自回归部署时，由于误差累积会导致预测画面迅速崩溃，难以支持长时间跨度的任务模拟。
method: 引入一种针对扩散模型的对比强化学习后训练方案，利用多视图视觉保真度奖励对模型自身的预测序列进行优化和偏好对齐。
result: "在DROID数据集上刷新了多步预测保真度的记录，LPIPS降低了14%，SSIM提升了9.1%，且在人类盲测中获得80%的偏好率。"
conclusion: 通过强化学习对世界模型进行自回归稳定性训练，可以有效解决长程视频生成的质量退化问题，为复杂机器人任务模拟提供了更可靠的工具。
---

## 摘要
动作条件机器人世界模型根据机器人动作序列生成被操作场景的未来视频帧，为模拟传统物理引擎难以建模的任务提供了一种极具前景的替代方案。然而，这些模型通常针对短期预测进行优化，在自回归部署时会失效：每个预测的视频片段都会作为下一个片段的上下文反馈，导致误差累积，视觉质量迅速下降。我们通过以下贡献解决了这一问题。首先，我们引入了一种强化学习 (RL) 后训练方案，该方案在世界模型自身的自回归外推 (rollouts) 上进行训练，而非基于真实历史数据。我们通过将近期用于扩散模型的对比强化学习目标适配到我们的设置中来实现这一点，并证明其收敛性保证可以完全延续。其次，我们设计了一种训练协议，从同一个外推状态生成并比较多个候选的可变长度未来，从而强化高保真度预测并抑制低保真度预测。第三，我们开发了高效的多视图视觉保真度奖励，结合了跨摄像机视图的互补感知指标，并在片段级别进行聚合，以获得密集、低方差的训练信号。第四，我们展示了我们的方法在 DROID 数据集的外推保真度上达到了新的最先进水平，在所有指标上均优于最强的基准模型（例如，外部摄像机的 LPIPS 降低了 14%，腕部摄像机的 SSIM 提高了 9.1%），在成对比较中胜率为 98%，并在盲测人类研究中获得了 80% 的偏好率。

## Abstract
Action-conditioned robot world models generate future video frames of the manipulated scene given a robot action sequence, offering a promising alternative for simulating tasks that are difficult to model with traditional physics engines. However, these models are optimized for short-term prediction and break down when deployed autoregressively: each predicted clip feeds back as context for the next, causing errors to compound and visual quality to rapidly degrade. We address this through the following contributions. First, we introduce a reinforcement learning (RL) post-training scheme that trains the world model on its own autoregressive rollouts rather than on ground-truth histories. We achieve this by adapting a recent contrastive RL objective for diffusion models to our setting and show that its convergence guarantees carry over exactly. Second, we design a training protocol that generates and compares multiple candidate variable-length futures from the same rollout state, reinforcing higher-fidelity predictions over lower-fidelity ones. Third, we develop efficient, multi-view visual fidelity rewards that combine complementary perceptual metrics across camera views and are aggregated at the clip level for dense, low-variance training signal. Fourth, we show that our approach establishes a new state-of-the-art for rollout fidelity on the DROID dataset, outperforming the strongest baseline on all metrics (e.g., LPIPS reduced by 14% on external cameras, SSIM improved by 9.1% on the wrist camera), winning 98% of paired comparisons, and achieving an 80% preference rate in a blind human study.

---

## 论文详细总结（自动生成）

以下是对论文《Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：动作条件机器人世界模型（World Models, WM）在进行长程自回归预测（Autoregressive Rollouts）时存在**曝光偏差（Exposure Bias）**。
*   **研究动机**：虽然现有的视频扩散模型能生成高质量的短片段，但在自回归部署时，模型必须以自己生成的（带有微小误差的）帧作为后续预测的上下文。这些误差会迅速累积，导致几秒钟内物体结构解体（如碗变成粘稠物）、机器人末端执行器偏离轨迹以及场景失真，限制了其作为机器人仿真器的实用性。

### 2. 方法论：PersistWorld
该论文提出了一种名为 **PersistWorld** 的后训练方案，核心思想是让模型在自己生成的序列上通过强化学习进行优化。
*   **对比强化学习目标**：适配了最新的 DiffusionNFT 方法，将其从流匹配（Flow-matching）模型推广到 $x_0$ 预测参数化的扩散模型（如 EDM）。通过构建“正向分支”（强化当前模型预测）和“负向分支”（排斥当前模型预测），并根据奖励权重进行插值更新。
*   **变长前缀训练协议**：
    1.  **生成共享前缀**：从真实初始状态开始，自回归生成 $P$ 个片段（$P$ 随机采样自 0-9），模拟不同程度的误差累积。
    2.  **分支候选序列**：从冻结的前缀状态出发，独立采样 $K=16$ 个候选后续片段。
    3.  **评分与排序**：利用多视图视觉奖励对候选片段进行评分，并进行组内归一化（z-score），转化为相对优势信号。
*   **多视图视觉奖励**：结合了 LPIPS（感知相似度）、SSIM（结构相似度）和 PSNR（峰值信噪比），跨越腕部相机和两个外部相机视图进行聚合，提供密集且低方差的训练信号。

### 3. 实验设计
*   **数据集**：使用 **DROID** 数据集，这是一个大规模、多样化的机器人操作数据集，包含多种桌面环境和三视角相机设置。
*   **基准模型（Benchmarks）**：对比了 **WPE**、**IRASim** 和 **Ctrl-World**（本文的 Baseline）。
*   **评估指标**：
    *   标准视觉指标：SSIM、PSNR、LPIPS。
    *   任务相关指标：使用掩码（Mask）提取物体中心和机器人中心的保真度。
    *   人类偏好：盲测人类研究（174 对 43 的胜出比例）。
    *   策略评估：在世界模型中运行 VLA 策略（如 $\pi_0$），计算其与真实世界表现的相关性。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练时长**：训练耗时 **3 天**（约 8,000 个训练步）。
*   **参数设置**：使用 LoRA 适配器（rank=64）对 UNet 进行微调，同时微调动作编码器，其余部分（VAE 等）冻结。

### 5. 实验数量与充分性
*   **实验规模**：在 DROID 验证集上进行了 14 步（约 11 秒）的长程外推评估。
*   **消融实验**：非常充分。作者对学习率、奖励函数组成、前缀长度采样策略、组大小（K）、预测步长（H）、KL 正则化等多个维度进行了详尽的消融分析。
*   **客观性与公平性**：
    *   采用了 1 对 1 的成对样本对比，结果显示在 98% 的样本上优于 Baseline。
    *   引入了第三方人类盲测，确保了评估的客观性。
    *   通过掩码评估证明了改进源于物理交互建模而非背景重建。

### 6. 主要结论与发现
*   **性能提升**：PersistWorld 在所有视觉指标上均达到 SOTA。外部相机 LPIPS 降低了 14%，腕部相机 SSIM 提升了 9.1%。
*   **稳定性增强**：通过在自身生成的“不完美历史”上训练，模型学会了纠正漂移，显著延长了稳定预测的时间跨度（从几秒延长到 10 秒以上）。
*   **仿真价值**：该模型在评估机器人策略时，与真实世界表现的相关性（Pearson r=0.822）高于 Baseline，证明了其作为可靠仿真器的潜力。

### 7. 优点
*   **理论完备**：成功将对比 RL 目标推导并应用到 $x_0$ 预测模型中，且无需通过复杂的去噪链进行反向传播。
*   **训练协议巧妙**：变长前缀策略有效地覆盖了从“轻微漂移”到“严重崩溃”的各种误差分布。
*   **实用性强**：显著改善了腕部相机的生成质量，这对于精细操作任务至关重要。

### 8. 不足与局限
*   **计算开销**：训练时每个更新步需要采样 $K=16$ 个候选序列，相比标准监督微调，计算成本显著增加。
*   **物理约束缺失**：目前的奖励函数主要基于像素和感知指标，尚未显式引入物理一致性（如重力、碰撞检测）或几何约束。
*   **应用范围**：目前仅在桌面操作场景（DROID）中验证，对于更广阔的户外环境或高速动态场景的泛化能力尚待观察。

（完）
